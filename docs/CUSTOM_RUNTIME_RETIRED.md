# Official Codex Return

Task: M12DC-OFFICIAL-RETURN-20260921.
Instruction ZIP SHA-256: f790e28d674c99ec06b29004af8fbdd4c2c911741d9dbbfd53ff3312eac217ea.

The custom native runtime is RETIRED_NOT_REQUIRED, not a fallback. Its unresolved
TLS history is UNRESOLVED_ARCHIVED, not an official CLI return gate. Preserve
native source, binaries, Git history, caches and historical evidence in place.

## Frozen Implementation Scope

1. Add an opt-in, single-attempt official CLI shadow transport. Bind the installed
   executable and current request bytes. Use an exclusive batch input directory,
   original credential store, supported read-only/ephemeral controls and separate
   event/final output handling. No network readiness probes or retries.
2. Connect the existing M12DC `_invoke` and shared `_invoke_signed_in_codex`
   through optional arguments only. Preserve existing default behavior and all
   analytical owners, source-use checks, capability schemas and materialization.
3. Capture the final subprocess boundary offline. Recheck the historical 8 A / 8 B
   request fixtures, raw/final contracts, MU/CRCL negatives and default callsites.
   Historical fixtures do not establish a fresh or independent inference result.

No model/catalog/auth qualification, native build, installation, provider/account
switch, cleanup, push, merge, deployment, production mutation or delivery.

The official CLI is not a packet-only sandbox. Read-only does not prohibit other
file reads; ephemeral does not certify input isolation. Managed policy and the
original credential store remain authoritative. Missing telemetry means
INPUT_BOUNDARY_UNVERIFIED; observed outside-input tool use means
UNDECLARED_INPUT_OBSERVED. Neither state certifies source-only inference.

Local preparation does not approve a future synthetic smoke or A/B invocation.

The locally observed app executable reports CLI 0.153.4, but its macOS signature
verification returned invalid signature on 2026-09-21. Do not treat an installed
path or a matching hash as verification of official installation integrity. The
opt-in binding defaults to installation-unverified and must also carry a locally
confirmed ChatGPT auth method before invocation. Offline positive fixtures
simulate these two approvals; they do not confirm the current installation.
