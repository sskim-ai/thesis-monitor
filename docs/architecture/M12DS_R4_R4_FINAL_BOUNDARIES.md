# R4-R4 Final Boundary Contracts

Work instruction: `29e5ed3ed69a12a58ee6d938f6c56bbe9c373914`.
Local-only descendant of R4-R3 `30dcefa2417075be75d2cdb2de3ad530899da7fb`.
No merge, push, deploy, production delivery, persistence, or scheduling changes.

## Statement Ownership

`sec_foreign_statement_boundary_service` binds a table to a table-local caption
or a structural preceding caption/unit. A financial table or another statement
terminates ownership. Only an explicit shared section can span sibling data tables.
Conflicting captions/currencies/scales fail closed. DOM paths and the exact cell,
period headers, payload hash and occurrence identity remain attached to each fact.

`authoritative_prior` resolves document authority before field quality. A selected
filing's own comparative column owns first. Other prior versions require a unique
document or an explicit, connected supersession chain with report identity and
source evidence. Recency alone does not invent supersession. Ambiguity and absent
partial-amendment fields fail closed; a conflicted revenue cannot be rescued from
an older filing while clean operating income may survive independently.

## Market Numeric Claims

`market-numeric-claim-v1` is opt-in for accepted R4-R4 market plans. Model narratives
are numeric-free. Deterministic lines bind exact source values, instrument labels,
dates, units, permission, formula/parents for relations, and formatter identity.
Dates, tenor-bearing labels and maturity codes are not globally exempted.
Registry and selected-source/session permission must both pass. Unowned previous
levels are not quoted. Final text must equal the bound catalog plus numeric-free
narrative, before and after the real production renderer.

Official night facts use the same claim catalog. The existing official source,
same-contract baseline, finality and session gates remain mandatory. Native packet
fields are matched to the existing `_public_value` producer projection, not to an
invented repaired row. Split historical layouts still require exact equality.
Mismatched DWM/close/baseline values are not repaired. Exact outbound substrings
are required for accepted E2E; recorded diagnostic previews do not count.

## Direction Representation

The existing `directional_balance_service` is unchanged, including its frozen
prompt. `accepted_directional_balance_service` enumerates schema choices through
that canonical owner. Schema, prompt, semantic acceptance and renderer use this
bridge: half steps, sum ten, BUY at six or above, HOLD between four-and-a-half and
five-and-a-half, SELL at four or below. No score rounding or direction replacement.
R3 business severity/source/valuation policy stays unchanged.

## Presentation

Exact review-report evidence can label a document as auditor-reviewed interim,
without promoting it to an annual audit or changing source-use authority. Legacy
snapshot storage type is not document-description authority. Proven document
class travels with the comparative financial fact. Unknown classes stay neutral.
Bound price context supplies a date/basis line for displayed entry ranges; no
intraday/finality guess. KRW rounds only for display, with bound values unchanged.

## Execution Gate

All focused and full offline tests, Ruff and diff checks must pass before freeze.
A new whole US14/KR8 collection is then required. Official signed-in Codex uses
gpt-5.6-sol/xhigh, twenty-minute attempts, at most one identical typed-transient
retry per logical request. No semantic retry, fallback, judge or old-output reuse.
Market2/Core8/A8/B8 outputs must reach the final 24-message capture boundary, with
night E2E and complete editorial review, before pre-main readiness can be true.
