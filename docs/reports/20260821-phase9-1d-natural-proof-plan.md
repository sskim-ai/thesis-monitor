# Phase 9.1D Natural Proof Plan

No Scheduled Task or Telegram run is invoked manually. After deployment, inspect the first natural
US and KR terminal packets and their independent working-capital canary archives.

Inventory becomes `LIVE_PASS` when a naturally material total-Inventory relation is selected with
the correct relation and Fact IDs, PIT/freshness PASS, numeric/semantic/causal/quality PASS, terminal
receipt PASS, and production influence 0. Exact Trade AR uses the same gate and must preserve the
`trade_accounts_receivable` semantic. One family may pass while the other remains `NOT_OBSERVED`.

For an empty natural opportunity, retain `NOT_OBSERVED`; do not count suppression as failure. For a
P0, preserve the receipt, disable only this canary if needed, and perform a bounded repair without
changing Phase 9.0E, production delivery, or canonical facts unless the defect reaches them.

Natural proof can proceed in parallel with Phase 9.1E architecture. It is mandatory before any
future user-visible enablement of the corresponding family.
