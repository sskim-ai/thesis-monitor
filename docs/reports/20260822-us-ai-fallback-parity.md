# US AI and Fallback Factual Parity

The deterministic fallback remains the run-32 safety reference and was not
edited. The replay uses the same immutable deterministic message set and the
same canonical Fact catalog.

FCF values, currencies, periods, PPE-only scope, current prices, support and
resistance relations, and available RR values are unchanged. The only RR edit
in the archive candidate is removal of three catalog-external declarations for
unavailable RR. Numeric claims are unchanged and factual mismatches are zero.

Message count remains 14. Exactly-once, receipt, and delivery code paths have no
diff. No fallback formatter or wording was changed.

