# Rehearsal 19:34 AI / Fallback Parity

Both archive-only bundles contain one market digest and seven stock messages in the same ticker
order. The AI bundle passed numeric, semantic, language, and runtime-quality gates; the deterministic
fallback remained complete with zero duplicates and orphans.

Macro parity is semantic rather than verbatim: AI labels the S&P500 move as the prior completed US
regular session and says it is not today's observation; fallback states that no new US cash session
or daily macro observation exists. False-current claims and AI/fallback temporal mismatches are zero.

Inventory parity is exact for the selected three subjects. Price/RR and canonical supply facts stay
within their existing ownership contracts. Selected production preference for this no-send replay is
the validated AI bundle; fallback remains the operational recovery bundle.
