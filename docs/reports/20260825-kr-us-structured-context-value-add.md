# KR/US Structured Context Value Add

## KR

Directional value is `NO_MATERIAL_VALUE` on the exact 8/25 target because official rows were not
published. Safety value is positive: the message states precisely what is pending and what cannot
be concluded. It does not polish overnight US facts into a Korean market-direction claim.

## US

Value is `PASS`. Equal-weight versus cap-weight and sector dispersion add a distinct market
structure relation that was absent from run-37. This supports a more useful broad/concentrated
classification while preserving breadth as Unknown.

## Common Result

The common schema works for both outcomes: a market can carry verified structured values or a
verified absence/publication state. Neither requires a zero default or a market-specific reasoning
schema fork.

`KR_US_REASONING_SCHEMA_COMMON = PASS`.
