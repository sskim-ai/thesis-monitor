# US Message Quality v2

Replay result: `14/14 PASS`.

Mandatory cases:

- CORZ: HPC/colocation execution, energized/billing capacity, margin/cash conversion, and capital
  burden form the decision boundary.
- CRCL: USDC/platform growth, reserve-income dependence, non-interest revenue, and cash-flow quality
  form the decision boundary.
- Market digest: RSP-versus-SPY and sector proxy dispersion are used; breadth remains Unknown.

Quality deltas from the exact instruction-commit baseline:

- Generic synthesis lines: `26 -> 0`.
- Messages with duplicated generic sections: `13 -> 0`.
- Quality v2 failures: `0` after repair.
- Numeric binding: `122` automatic, `0` rejected.
- Average selected message length: `354.6 -> 354.6` characters.

Decision: `US_MESSAGE_QUALITY_V2 = PASS`.
