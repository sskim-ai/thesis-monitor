# Run-42 AI / Fallback Size-Sector Parity

| Claim | AI | Fallback |
| --- | --- | --- |
| Size/style claim | True | True |
| Sector-extrema claim | True | True |

Both paths consume the same `KrMarketDigestPlan`; no duplicate selection or ranking logic exists.

`AI_FALLBACK_SIZE_STYLE_PARITY = PASS`

`AI_FALLBACK_SECTOR_PARITY = PASS`
