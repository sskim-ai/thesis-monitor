# 2026-08-28 US Morning AI / Fallback Parity

The AI and deterministic routes consume the same shared plan. Both preserve the required session claims:

- SPY, QQQ, IWM, and SOXX rose.
- RSP fell while SPY rose, explicitly framed as participation/style rather than breadth.
- XLK was strongest and XLP weakest among supported sector proxies.
- Official breadth was unavailable and not fabricated.

The selected `CONCISE_HYBRID` AI digest omits optional macro detail and exact percentages. The fallback is longer and includes bounded regime/transmission context, but neither route changes temporal roles or replaces current market structure with macro. This is presentation variance, not semantic divergence.

```text
AI_FALLBACK_MARKET_PLAN_DIVERGENCE = 0
AI_FALLBACK_MARKET_SEMANTIC_PARITY = PASS
AI_FALLBACK_TEMPORAL_PARITY = PASS
BROAD_RISK_ON_WITHOUT_SUPPORT = 0
UNSUPPORTED_BREADTH_CLAIM = 0
```
