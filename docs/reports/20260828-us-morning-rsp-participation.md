# 2026-08-28 US Morning RSP Participation

RSP closed at `221.45` with a backend-owned `-0.2972%` session return on `2026-08-27`. It is `CURRENT_DIRECTIONAL` and selected by the shared `PARTICIPATION_STYLE` slot together with SPY.

The final digest correctly says equal-weight S&P500 fell while cap-weighted S&P500 rose. It does not call RSP exchange breadth. The official Nasdaq breadth state remains separately unavailable.

```text
RSP_STATE_VALID = PASS
RSP_STATE_PROPAGATION = PASS
RSP_AS_EXCHANGE_BREADTH = 0
RSP_DIRECTION_INVENTED = 0
SELECTED_RSP_SLOT_UNCONSUMED = 0
```
