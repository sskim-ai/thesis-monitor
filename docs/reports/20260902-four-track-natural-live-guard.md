# Four-Track Natural-Live Guard

The retrospective, production-equivalent, and dedicated-sink gates pass. They are not natural
`LIVE_PASS` evidence.

## Next KR Natural Run

Observe the ordinary scheduled run read-only. Require source readiness, network preflight,
app-server/model reach, candidate/accepted/explicit V2 `8/8/8`, fallback `0`, and `9/9` exactly-once
delivery when the cohort remains eight.

## Next US Natural Run

Observe the ordinary scheduled run read-only. Require source readiness, network preflight,
candidate/accepted/explicit V2 `14/14/14`, fallback `0`, and `15/15` exactly-once delivery when the
cohort remains fourteen. The market message must contain compact same-contract NIGHT D/W/M and the
dated nominal Treasury 3Y/5Y/10Y/30Y block, or fail closed with a precise source reason.

## Prohibitions

- no manual Scheduled Task;
- no historical production replay or resend;
- no production-recipient proof;
- no recipient identifier disclosure;
- no production-state mutation for observation;
- no declaration of natural pass from the `22/22` test-sink result;
- Production Assist remains OFF.

`NEXT_ACTION = WAIT_FOR_NEXT_NATURAL_KR_LIVE + WAIT_FOR_NEXT_NATURAL_US_LIVE`
