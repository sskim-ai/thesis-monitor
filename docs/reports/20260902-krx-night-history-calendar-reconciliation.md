# KRX Night History Calendar Reconciliation

The backfill enumerated `21` XKRX sessions and made `21` official requests: `21` success, `0` failure. No post-cutoff dates were used. Missing/invalid target rows were recorded as `216` explicit rejections and never silently dropped into an aggregate.

`KRX_NIGHT_HISTORY_CALENDAR_RECONCILIATION = PASS`
