# KRX Night History Store

The bounded evidence store contains `78` FINAL normalized bars from `2026-08-03` through `2026-09-01`. Identity is instrument + exact contract + reference date + NIGHT. The focused suite proves identical replay creates no duplicate and a conflicting fingerprint cannot overwrite.

`CONTRACT_IDENTITY_COLLISION = 0`
`KRX_NIGHT_HISTORY_INCREMENTAL = PASS`
