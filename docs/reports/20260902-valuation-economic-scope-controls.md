# Valuation Economic-Scope Controls

The three rejected earnings-quality interpretations cited `financial_quality:2026-06-30`. That
fact describes whether earnings inputs are safe; it does not carry a verified company, segment, or
listed-security valuation scope. The candidates declared `listed_security`, producing the correct
strict mismatch.

For `quality_unknown` plus `insufficient_metadata` only, a missing canonical scope is now owned as
`unknown`. Explicit fact scopes remain unchanged and all explicit mismatches still fail.

- `000660_VALUATION_SCOPE = PASS`
- `010120_VALUATION_SCOPE = PASS`
- `012450_VALUATION_SCOPE = PASS`
- `000660_VALUATION_QUALITY_GUARD = PASS`

