# Model Timeout Policy Parity

The natural accepted V2 runtime default is 1,800 seconds. The production-equivalent replay uses
the same 1,800-second limit and `gpt-5.6-sol` with `xhigh` effort.

No timeout, retry-budget, or model policy was changed by this repair.

`MODEL_TIMEOUT_POLICY_PARITY = PASS`

