# ROIC / FCF Metric Grounding Regression

Pre-model focused metric regressions passed. The fresh model generation then produced one unhandled, evidence-grounded future-modal surface.

| Ticker | Role | Surface | Evidence owns ROIC | Result |
| --- | --- | --- | --- | --- |
| `005490` | `reevaluation_down` | `향후 ROIC 악화가 지속될 때` | yes | `PASS` |
| `005490` | `sell_driver` | `향후 ROIC 악화로 이어질 수 있다` | yes | `FAIL` |

The second row caused `unsupported_future_checkpoint_metric` and the aggregate `unsupported_metric_or_inference`. It is a bounded validator-language P1, not an unsupported ROIC fact and not a production correctness incident.
