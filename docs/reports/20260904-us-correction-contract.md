# 2026-09-04 US Correction Contract

The correction pass remains bounded by the existing retry count. It now receives exact text references, visible rendered spans, safe numeric diagnostics, canonical candidates, and category-specific allowed actions. It may correct a reference, correct wording, or remove an unsafe number; it may not relax validation or invent evidence.

The SKHY basis case now receives a grounded security-basis Unknown path. The exact frozen candidate validates without a model rerun, proving the incident was a contract defect rather than a need to rewrite the candidate.

A later real model E2E required six existing bounded candidate repairs and no batch-schema repair. That accepted artifact then exposed a separate message-quality classification defect, which was repaired using exact owner/semantic/template contracts without increasing retry limits.

| Gate | Result |
|---|---|
| Correction retries increased | `0` |
| Validator threshold weakened | `0` |
| Ticker exceptions | `0` |
| Frozen replay model rerun | `0` |
