# Thesis Monitor — Real Holdout Runner–Adapter Interface Repair & Ownership Proof Resume

## 0. Task identity

Suggested work-instruction filename:

```text
20260906-real-holdout-runner-adapter-interface-repair-and-ownership-proof-resume.md
```

Suggested result bundle:

```text
thesis-monitor-20260906-real-holdout-runner-adapter-repair-ownership-proof-resume-report.zip
```

This is a **bounded execution-harness repair + previously authorized ownership-proof resume task**.

It is NOT:

- an investment-decision architecture redesign;
- a Directional Core / Price-Timing semantic change;
- a transport-topology redesign;
- a prompt/schema/source change;
- a holdout-selection task;
- a monitoring-bootstrap task;
- a production-promotion task.

The repair authorized here exists because the preceding task stopped before the real holdout model was invoked.

The intended fix is:

```text
make the real-holdout runner conform to the already validated adapter interface
```

not:

```text
widen the adapter until an obsolete runner call happens to work
```

After repair, the purpose is **measurement, not tuning**.

---

# 1. Source of truth — split by concern

Do not use one total precedence order for both historical evidence and current code.

## 1.1 Historical measured facts / prior experiment outcomes

The newest result bundle is authoritative:

```text
thesis-monitor-20260906-synthetic-canary-fixture-repair-ownership-proof-resume-report.zip
```

Verified SHA-256:

```text
05da7dda67957f9f1c6dab87885b0a154c4247a8fb1b79a15688e0729b752816
```

Use that bundle as the authoritative source for:

- prior canary outcomes;
- prior transport verdict;
- prior holdout invocation/output counts;
- prior source lock;
- prior freeze evidence;
- prior stop reason;
- prior measured production/no-change state.

## 1.2 Current code / config / worktree

The **current repository HEAD and current worktree** are authoritative for what code and configuration actually exist now.

Do not let the prior result bundle overwrite current repository facts.

Likewise, do not let current code silently rewrite prior experiment history.

## 1.3 Task constraints

This integrated work instruction is authoritative for the current task. It already incorporates the previously supplied Runner–Adapter Repair addendum and the Final Addendum v2 concerning holdout exposure and per-run gates.

No separate addendum file is required to interpret this task.

## 1.4 Knowledge semantics

Use:

```text
Investment Thesis Analysis & Monitoring Knowledge Guide
```

for investment, evidence, valuation, safety, monitoring, ownership and rendering semantics.

## 1.5 Older material

Older handoffs and reports are context only.

## 1.6 Conflict rule

If current repository state conflicts with the newest result bundle:

- record the discrepancy as repository drift;
- identify whether it affects only authorized harness implementation or experiment semantics;
- do not silently choose one side.

If the conflict can affect:

```text
investment semantics
prompt/schema semantics
evidence inputs
model/context construction
transport topology
source lock
experiment validity
```

then:

```text
STOP
```

---

# 2. Prior measured state — authoritative historical baseline

The preceding execution established:

```text
synthetic fixture root cause:
CLOSED

synthetic packet schema preflight:
PASS

schema-valid model canaries:
7 / 7 PASS

architecture-scale transport:
PASS

transport verdict:
TRANSPORT_HEALTHY_AFTER_REVALIDATION

batch semantics:
MODEL_CONTEXT_COUPLED

batch size:
4

batch split semantic equivalence:
FAIL

model:
gpt-5.6-sol

reasoning effort:
xhigh

model timeout:
1800 seconds

authoritative timeout owner count:
1

orphan model processes:
0

architecture hash drift:
0

source drift:
0

prompt/schema semantic drift:
0

current holdout reuse gate:
PASS
```

Production/no-change state:

```text
main merge = 0
production DB mutation = 0
production Telegram send = 0
production scheduler mutation = 0
live Structured Autonomy activation = 0
live V2 activation/change = 0
monitoring registration calls = 0
night-futures mutation = 0
```

These are historical measured facts.

Do not rerun or reinterpret them merely to manufacture a different status.

---

# 3. Exact current blocker

The previous real-holdout FIRST stopped with:

```text
ContinuationTransportAdapter.invoke()
got an unexpected keyword argument 'codex_bin'
```

Classification:

```text
error_type = TypeError
execution_harness_interface_failure = 1
first_model_invocation_started = 0
real_holdout_model_call_count = 0
real_holdout_transport_retry_count = 0
transport_failure = 0
schema_failure = 0
same_generation_repair = 0
selective_rerun = 0
```

Previous stop reason:

```text
REAL_HOLDOUT_RUNNER_ADAPTER_INTERFACE_MISMATCH_BEFORE_MODEL_INVOCATION
```

Therefore the current blocker is classified as an:

```text
execution-harness runner↔adapter interface mismatch
before real-holdout model invocation
```

This is not evidence that:

- the model timed out;
- transport topology failed;
- the ownership architecture failed;
- Directional Core semantics failed;
- Price-Timing semantics failed;
- renderer ownership failed;
- hard investment safety failed;
- the source packet failed.

Those items remain unmeasured on real holdout output unless this task successfully produces such output.

---

# 4. Goal

Repair only the real-holdout execution path so that it calls the already validated `ContinuationTransportAdapter` according to its current canonical invocation contract.

Then:

```text
reverify bounded repair
→ reverify semantic freeze
→ reverify holdout reuse
→ execute FIRST once as a complete frozen-cohort run
→ run FIRST-specific semantic gates
→ if clean, execute A
→ run A-specific semantic gates
→ if clean, execute B
→ run B-specific semantic gates
→ if clean, execute C
→ run C-specific semantic gates
→ aggregate stability/generalization only over validly completed runs
```

The purpose is to measure the already-frozen Directional Core / Price-Timing architecture on untouched real holdout subjects.

Do not optimize the investment results.

Do not tune against the cohort.

Do not target a desired BUY/HOLD/SELL distribution.

---

# 5. Repository provenance gate

Before modifying implementation code:

## 5.1 Capture current state

Record:

```text
git branch --show-current
git rev-parse HEAD
git status --short
```

The latest prior result recorded:

```text
base_sha =
4a847d2c0ea50d190d8df4daef5405b6660967a5

prior work_instruction_commit =
b1ab44a

prior implementation_commit =
00d1248b7846ccf05bc9e3ceaf372a97b71568e0
```

The branch name was not established by the prior result bundle.

Do not invent it.

## 5.2 Compare current repository with prior implementation state

Determine whether current HEAD/worktree contains changes after:

```text
00d1248b7846ccf05bc9e3ceaf372a97b71568e0
```

Classify differences by concern.

Permitted prior/current difference only if independently justified:

```text
authorized task documentation
nonsemantic generated evidence
other clearly unrelated work that cannot affect this experiment
```

If there is unexplained semantic drift affecting the experiment:

```text
STOP
```

Do not reset, cherry-pick, merge, or silently discard unrelated changes.

## 5.3 Work-instruction commit first

Commit this work instruction before implementation repair.

The commit must contain the work instruction only, apart from mechanically unavoidable metadata.

Record:

```text
new_work_instruction_commit
```

Only then begin implementation modification.

---

# 6. Allowed mutation surface

The intended mutation surface is extremely small.

Allowed:

1. the real-holdout experiment/resume runner call site that constructs or forwards arguments into `ContinuationTransportAdapter.invoke()`;
2. an experiment-only invocation helper used exclusively by that runner, if inspection proves it is part of the mismatch;
3. regression tests for the runner↔adapter binding contract;
4. model-free preflight code required to prove that the actual real-holdout runner path binds to the current adapter API;
5. result/report serialization required for this task.

Preferred repair:

```text
real runner conforms to the already validated adapter contract
```

not:

```text
adapter is widened to accept obsolete runner arguments
```

If the defect cannot be repaired inside this bounded execution-harness surface:

```text
STOP
```

with:

```text
REPAIR_SCOPE_EXCEEDED
```

Do not widen the task.

---

# 7. Authorized harness hash drift vs semantic drift

The intended repair may necessarily change the byte hash of a real-holdout runner or experiment-harness file.

Therefore do not require every transport-adjacent file to remain byte-identical.

Explicitly distinguish:

```text
AUTHORIZED_HARNESS_HASH_DRIFT
TRANSPORT_TOPOLOGY_MUTATION
MODEL_SEMANTIC_INPUT_DRIFT
```

## 7.1 Authorized harness hash drift

Allowed:

- real-holdout runner/call-site hash change caused solely by fixing/removing invalid adapter argument binding;
- experiment-only harness-helper hash change required for that same bounded repair;
- model-free runner↔adapter regression/preflight code;
- report/evidence generation changes specific to this task.

Record:

```text
authorized_harness_hash_drift = 1
```

when such a change occurs and is proven bounded.

This alone is not transport semantic drift.

## 7.2 Must remain semantically unchanged

Unless root-cause inspection proves otherwise, the following remain frozen:

```text
ContinuationTransportAdapter canonical invocation contract
instrumented Codex invocation semantics
process lifecycle topology
timeout ownership
batch/context shape
model
reasoning effort
prompt serialization
schema serialization
evidence/model input contents
```

## 7.3 Mixed files

If a changed file contains both:

- real-holdout runner/harness code; and
- validated canary/shared transport code,

produce function-level or call-graph-level diff evidence proving that the previously validated transport/canary path is unaffected.

If that proof cannot be made:

```text
STOP
```

and require a separately authorized broader transport revalidation task.

Do not silently consume another full canary set in this task.

---

# 8. Forbidden mutations

Do not change any of the following.

## 8.1 Investment architecture

```text
Directional Core semantics
Price-Timing semantics
deterministic composer semantics
V2 renderer ownership
BUY/HOLD/SELL thresholds
BUY:SELL balance rules
HOLD lean rules
new-buyer transition rules
holder transition rules
business invalidation rules
```

Current thresholds remain:

```text
BUY if buy >= 6.0
SELL if sell >= 6.0
otherwise HOLD

BUY:SELL sum = 10
increment = 0.5

HOLD 5.5:4.5 = BUY_LEAN
HOLD 5.0:5.0 = NEUTRAL
HOLD 4.5:5.5 = SELL_LEAN
```

## 8.2 Source / evidence

Do not change:

```text
source assembly
fundamental enrichment
source-sufficiency policy
evidence domains
valuation safety
numeric provenance
accounting attribution
ADR/security basis logic
official provisional earnings logic
issuer dedup
future checkpoint logic
```

Do not refresh or replace the frozen holdout source packet because newer data may exist.

## 8.3 Schema / prompt

Do not change:

```text
DecisionEvidencePacket production schema
production market enum
Directional Core prompt semantics
Price-Timing prompt semantics
model output schema
```

Production market enum remains:

```text
kr | us
```

The repaired synthetic fixture contract remains untouched.

## 8.4 Transport semantics

Do not change the established transport topology:

```text
Popen new session
stdin writer
stdout reader
stderr reader
single monotonic watchdog
process-group cleanup
output parse
single receipt
```

Do not:

```text
increase timeout
add a second timeout owner
split shared model contexts
add automatic real-holdout retry
change model
change reasoning effort
```

## 8.5 Production

No:

```text
main merge
production DB mutation
Telegram production send
scheduler mutation
monitoring registration
live Structured Autonomy activation
live V2 activation
night-futures production mutation
```

---

# 9. Phase A — Root-cause proof before repair

Before changing the call site, produce an exact machine-readable comparison between:

```text
current ContinuationTransportAdapter.invoke signature
```

and:

```text
arguments/kwargs constructed by the actual real-holdout runner
```

The report must identify:

- adapter callable location;
- runner call-site location;
- canonical adapter parameters;
- runner-supplied parameters;
- unexpected parameters;
- missing required parameters, if any;
- whether `codex_bin` is a legacy/runner-only parameter;
- whether the already-passed canary path uses a different invocation shape;
- whether the defect lies in the runner, adapter, shared helper, or argument-construction layer.

Do not assume the exception text alone proves the complete fix.

Inspect actual current code.

Required result:

```text
RUNNER_ADAPTER_BINDING_ROOT_CAUSE_CONFIRMED
```

or:

```text
ROOT_CAUSE_NOT_CONFIRMED
```

If root cause is not confirmed:

```text
STOP
```

No speculative patch.

---

# 10. Phase B — Model-free binding preflight

Add a regression gate that exercises the **actual real-holdout runner invocation-construction path** but does not invoke the real model.

The gate must catch:

```text
unexpected keyword argument
missing required argument
wrong positional/keyword binding
runner-owned legacy transport parameters leaking into adapter invocation
```

Use an approach appropriate to current code, for example:

```text
inspect.signature(...).bind(...)
```

or an equivalent typed/spy adapter contract.

The preflight must validate the real runner's final adapter-call shape.

It is not sufficient to instantiate an unrelated synthetic fixture.

Do not create a second alternative adapter API merely to make the test pass.

Required gate:

```text
REAL_HOLDOUT_RUNNER_ADAPTER_BINDING_PREFLIGHT = PASS
```

The previous `codex_bin` mismatch must be covered by a regression assertion.

---

# 11. Phase C — Implement minimal repair

Repair the runner to conform to the canonical adapter invocation contract.

The expected generic fix class is:

```text
legacy or runner-owned transport configuration
must not be forwarded as an unsupported invoke() kwarg
```

Implement only what current code inspection supports.

Do not alter investment semantics while touching the call path.

After repair, produce a bounded diff audit.

Required fields include at least:

```text
authorized_harness_hash_drift

directional_core_semantic_mutation = 0
price_timing_semantic_mutation = 0
composer_semantic_mutation = 0
renderer_semantic_mutation = 0

prompt_semantic_drift = 0
schema_semantic_drift = 0
production_market_enum_mutation = 0

source_assembly_mutation = 0
source_sufficiency_policy_drift = 0
fundamental_source_enrichment_drift = 0

transport_topology_mutation = 0
model_semantic_input_drift = 0
model_context_shape_mutation = 0
timeout_increase_this_task = 0
batch_split_adopted = 0

ticker_specific_production_exception = 0
```

A runner/harness hash change is allowed only when:

```text
authorized_harness_hash_drift = 1
transport_topology_mutation = 0
model_semantic_input_drift = 0
```

---

# 12. Phase D — Tests and freeze reverification

Before any real-holdout model invocation:

1. run focused runner↔adapter regression tests;
2. run the full test suite;
3. run Ruff/current repository lint gates;
4. run the new model-free runner↔adapter binding preflight;
5. reverify investment architecture semantics;
6. reverify source freeze;
7. reverify prompt/schema semantics;
8. reverify model/effort;
9. reverify batch/context semantics;
10. reverify transport topology;
11. classify changed runner/harness hashes as authorized or unauthorized.

Required model/runtime state:

```text
model = gpt-5.6-sol
reasoning_effort = xhigh
batch_semantics = MODEL_CONTEXT_COUPLED
real_run_batch_size = 4
model_timeout_seconds = 1800
model_timeout_owner_count = 1
```

The four-subject shared contexts must not be split.

Do not treat a proven authorized runner/harness file hash change as semantic freeze failure.

However, if any of the following drift:

```text
Directional Core semantics
Price-Timing semantics
renderer/action ownership semantics
prompt/schema semantics
evidence/model semantic inputs
source lock
source-sufficiency policy
model/effort
batch/context shape
transport process topology
timeout ownership
```

then:

```text
STOP
```

---

# 13. Prior model canaries

The prior task completed:

```text
C1 smoke = PASS
C2 Directional Core US = PASS
C3 Directional Core KR = PASS
C4 shared-context US4 = PASS
C5 shared-context KR4 = PASS
C6 Price-Timing US = PASS
C7 Price-Timing KR = PASS

ARCHITECTURE_SCALE_TRANSPORT = PASS
TRANSPORT_HEALTHY_AFTER_REVALIDATION
```

These are authoritative historical evidence.

## 13.1 Historical canaries may be reused only if

The repair touches only:

```text
real-holdout runner/call-site
experiment-only runner helper
model-free binding preflight/regression code
```

and a function-level/call-graph-level proof establishes that the validated canary/adapter/transport path is unchanged.

Then:

```text
do not rerun C1..C7
```

## 13.2 Historical canaries are insufficient if repair changes

Any of:

```text
ContinuationTransportAdapter
instrumented Codex invocation
shared transport service
timeout/process lifecycle
model-context construction
prompt serialization used by canaries
schema serialization used by canaries
```

If so:

```text
STOP
```

and report:

```text
BROADER_TRANSPORT_REVALIDATION_REQUIRED
```

Do not silently spend another full seven-canary set in this task.

---

# 14. Phase E — Current holdout reuse gate

Ordered cohort:

```text
ORCL
UNH
KO
AVGO
095570
058860
246960
099520
403870
014790
079810
060980
061970
012030
225190
245620
```

Frozen source lock:

```text
efe64d0942afa00c3b40131afc4de5fa411babc0680271d1571aaac69816050d
```

Prior measured facts:

```text
prior real-holdout model invocation count = 0
prior real-holdout model output count = 0
pre-lock directional model calls = 0
real-holdout transport retry count = 0
```

The current holdout remains reusable after repair even if an authorized runner/harness file hash changes, provided all are true:

```text
prior real-holdout model invocation count = 0
prior real-holdout model output count = 0

source lock unchanged = 1
architecture semantics unchanged = 1
prompt/schema semantics unchanged = 1
model unchanged = 1
reasoning effort xhigh unchanged = 1
MODEL_CONTEXT_COUPLED unchanged = 1
batch size 4 unchanged = 1
evidence packet unchanged = 1
source-sufficiency policy unchanged = 1
transport topology unchanged = 1
model semantic inputs unchanged = 1

only implementation delta =
proven runner↔adapter binding repair
```

Required result:

```text
current_holdout_reuse_allowed = 1
holdout_output_exposure_state = UNEXPOSED
holdout_retirement_state = ACTIVE_UNEXPOSED
future_unseen_holdout_reuse_allowed = 1
```

If this gate fails:

```text
STOP
```

Do not run this cohort.

Do not select a replacement holdout in the same task.

---

# 15. Holdout state model — exposure, semantic revelation, retirement

Do not use one state field for multiple experiment-integrity questions.

Three separate questions must be represented independently:

```text
Did the model output anything?
Did the output expose a semantic architecture defect?
Is the cohort still eligible to serve as unseen generalization evidence?
```

## 15.1 Output exposure state

Add:

```text
holdout_output_exposure_state =
UNEXPOSED /
PARTIALLY_EXPOSED /
FULLY_EXPOSED
```

### UNEXPOSED

```text
real_holdout_subject_output_count = 0
```

No real investment output has been produced.

The cohort may remain eligible for reuse only under the existing reuse gate.

### PARTIALLY_EXPOSED

At least one real holdout subject/context produced usable investment output, but the complete intended run did not finish.

Examples:

```text
one shared context returns output
then a later context times out

one stage completes for only part of the cohort

parse/transport fails after some usable model output exists
```

This is not automatically an ownership/architecture failure.

However:

```text
future_unseen_holdout_reuse_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
```

Preserve partial evidence.

Unmeasured fields remain:

```text
NOT_MEASURED
```

### FULLY_EXPOSED

A complete FIRST or later run has produced all intended real holdout outputs for that run.

Regardless of PASS or FAIL:

```text
future_unseen_holdout_reuse_allowed = 0
```

The cohort is consumed as unseen generalization evidence.

It must not later be recycled as a fresh holdout after architecture changes.

## 15.2 Semantic revelation state

Add and keep separate:

```text
holdout_semantic_revelation_state =
NO_SEMANTIC_DEFECT_OBSERVED /
REVEALED_FOR_ARCHITECTURE_TUNING /
NOT_MEASURED
```

Use:

```text
REVEALED_FOR_ARCHITECTURE_TUNING
```

only when actual real output exposes a semantic defect requiring investment-architecture, ownership, renderer/action, or hard-safety code repair.

Then:

```text
STOP
same_cohort_hotfix_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
next generic architecture repair requires a new issuer-level holdout
```

Do not use `REVEALED_FOR_ARCHITECTURE_TUNING` merely because a transport or harness failure occurred after partial output.

For partial-output nonsemantic execution failure:

```text
holdout_output_exposure_state = PARTIALLY_EXPOSED
holdout_semantic_revelation_state = NOT_MEASURED
future_unseen_holdout_reuse_allowed = 0
```

## 15.3 Retirement state

Add:

```text
holdout_retirement_state =
ACTIVE_UNEXPOSED /
RETIRED_PARTIAL_EXPOSURE /
RETIRED_AFTER_EVALUATION /
RETIRED_FOR_ARCHITECTURE_REPAIR
```

Mapping:

```text
0 real output
→ ACTIVE_UNEXPOSED

partial real output + nonsemantic execution failure
→ RETIRED_PARTIAL_EXPOSURE

full successful ownership proof
→ RETIRED_AFTER_EVALUATION

real output exposes semantic architecture defect
→ RETIRED_FOR_ARCHITECTURE_REPAIR
```

A retired cohort may remain available as a regression fixture/history source.

It must not later be presented as a new unseen issuer-level holdout.

---

# 16. Consumed regression cohort

Do not issue new model calls for:

```text
PLTR
V
MA
AMZN
XOM
DIS
NKE
MCD
033920
104480
071320
096240
032860
060570
016600
462520
```

This cohort is regression history only.

---

# 17. Phase F — Live-workload coexistence guard

Before each heavy real-model context:

- check authoritative natural KR/US scheduled AI workload contention using the existing coexistence policy;
- do not modify production scheduler;
- do not cancel/preempt natural live runs.

If contention risk becomes plausible:

```text
pause shadow/holdout execution
allow authoritative natural live work to complete
resume only under the existing guard
```

Record:

```text
live_workload_contention_risk
shadow_pause_for_natural_live
natural_live_cancel_count
scheduler_mutation
```

Expected:

```text
natural_live_cancel_count = 0
scheduler_mutation = 0
```

---

# 18. Phase G — FIRST execution semantics

Create a new resume generation ID.

Use exactly the frozen:

```text
ordered cohort
source lock
evidence packet
model
reasoning effort
prompt/schema semantics
batch grouping
batch size
timeout
transport topology
```

Execute:

```text
FIRST
```

**exactly once as one complete FIRST run across the frozen cohort.**

## 18.1 Counting clarification

Because:

```text
batch_semantics = MODEL_CONTEXT_COUPLED
batch_size = 4
```

`FIRST exactly once` does not mean one model invocation total.

A 16-subject Directional Core FIRST normally requires multiple shared-context model invocations according to the frozen runner.

Price-Timing invocations are counted separately according to the frozen architecture.

Report separately:

```text
REAL_HOLDOUT_MODEL_INVOCATION_COUNT
REAL_HOLDOUT_DIRECTIONAL_CONTEXT_COUNT
REAL_HOLDOUT_PRICE_TIMING_CONTEXT_COUNT
REAL_HOLDOUT_SUBJECT_OUTPUT_COUNT
```

Do not conflate:

```text
run count
model invocation count
context count
subject count
subject output count
```

## 18.2 No retry semantics

No automatic retry.

No selective ticker rerun.

No batch splitting.

No timeout increase.

No candidate edit after observing output.

## 18.3 Exposure state must update as output is produced

The output exposure state must reflect actual output, even when a later context fails.

Examples:

```text
no usable subject output
→ UNEXPOSED

some usable subject/context output, incomplete FIRST
→ PARTIALLY_EXPOSED

complete FIRST output
→ FULLY_EXPOSED
```

Do not wait until the end of A/B/C to determine whether the holdout has been exposed.

---

# 19. FIRST execution hard stops

If FIRST encounters before any usable real output:

```text
model timeout
transport/process failure
parse failure
unexpected execution-harness/interface failure
source/freeze discrepancy
hard schema failure
```

then:

```text
STOP
holdout_output_exposure_state = UNEXPOSED
holdout_semantic_revelation_state = NOT_MEASURED
holdout_retirement_state = ACTIVE_UNEXPOSED
ownership fields = NOT_MEASURED
```

Reuse is not automatic; it remains subject to a future explicit reuse gate.

If FIRST encounters a nonsemantic execution failure after some usable real output:

```text
STOP
holdout_output_exposure_state = PARTIALLY_EXPOSED
holdout_semantic_revelation_state = NOT_MEASURED
holdout_retirement_state = RETIRED_PARTIAL_EXPOSURE
future_unseen_holdout_reuse_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
```

Do not treat a partial-output transport/harness failure as an architecture defect.

---

# 20. FIRST semantic gates — mandatory before A

A clean FIRST is not merely a transport/parse completion condition.

After FIRST output completes, run FIRST-specific:

```text
ownership hard gates
renderer/action ownership hard gates
hard-safety gates applicable to FIRST output
```

Only if all pass may A start.

At minimum FIRST must establish:

```text
DIRECTIONAL_CORE_PRICE_TECHNICAL_REFS = 0
DIRECTIONAL_CORE_SUPPLY_REFS = 0
SUPPLY_DIRECTIONAL_CORE_USAGE = 0

BUY_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
SELL_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0

TIMING_STAGE_DIRECTION_MUTATION = 0
TIMING_STAGE_BALANCE_MUTATION = 0
TIMING_STAGE_HOLD_LEAN_MUTATION = 0

PRICE_TIMING_NEW_BUYER_UPGRADE = 0
PRICE_ONLY_HOLDER_REDUCE = 0
PRICE_ONLY_DIRECTIONAL_OWNERSHIP_VIOLATIONS = 0

DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
PRICE_ONLY_DIRECTIONAL_MODEL_CALLS = 0

FINAL_DIRECTION_OWNER = DIRECTIONAL_CORE

PRIMARY_USER_ACTION_WORDING_OWNER = RENDERER
AI_IMPERATIVE_PRIMARY_ACTION = 0

KNOWN_HARD_SAFETY_REGRESSION = 0
```

Required per-run statuses:

```text
first_ownership_gate_status
first_renderer_gate_status
first_hard_safety_gate_status
```

If any required FIRST semantic gate fails:

```text
STOP
do not run A

holdout_output_exposure_state = FULLY_EXPOSED
holdout_semantic_revelation_state = REVEALED_FOR_ARCHITECTURE_TUNING
holdout_retirement_state = RETIRED_FOR_ARCHITECTURE_REPAIR
future_unseen_holdout_reuse_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
```

Do not postpone semantic adjudication until after A/B/C.

---

# 21. Clean FIRST criteria

FIRST is clean only if both execution and semantic gates pass.

Execution conditions:

```text
all required FIRST contexts completed
no timeout
no automatic retry
no selective rerun
no unauthorized source drift
no schema semantic drift
no prompt semantic drift
no model/effort drift
no batch/context drift
no transport topology drift
no model semantic input drift
no hard parsing failure
```

Semantic conditions:

```text
first_ownership_gate_status = PASS
first_renderer_gate_status = PASS
first_hard_safety_gate_status = PASS
```

Authorized runner/harness hash drift does not by itself make FIRST unclean.

Only a clean FIRST permits A.

On a clean FIRST:

```text
holdout_output_exposure_state = FULLY_EXPOSED
holdout_semantic_revelation_state = NO_SEMANTIC_DEFECT_OBSERVED
future_unseen_holdout_reuse_allowed = 0
```

The cohort is already consumed as unseen evidence even though A/B/C may still be required for repeated-run stability analysis.

---

# 22. Phase H — A / B / C with per-run semantic gates

After clean FIRST:

```text
run A
```

Immediately after A output, run:

```text
A-specific ownership validation
A-specific renderer/action ownership validation
A-specific hard-safety validation
```

Required fields:

```text
run_a_ownership_gate_status
run_a_renderer_gate_status
run_a_hard_safety_gate_status
```

Only if all pass may B start.

After B output, run:

```text
B-specific ownership validation
B-specific renderer/action ownership validation
B-specific hard-safety validation
```

Required fields:

```text
run_b_ownership_gate_status
run_b_renderer_gate_status
run_b_hard_safety_gate_status
```

Only if all pass may C start.

After C output, run:

```text
C-specific ownership validation
C-specific renderer/action ownership validation
C-specific hard-safety validation
```

Required fields:

```text
run_c_ownership_gate_status
run_c_renderer_gate_status
run_c_hard_safety_gate_status
```

At the first execution or semantic gate failure:

```text
STOP
```

Do not continue merely to collect more stochastic samples.

If real output exposes a semantic defect during A/B/C:

```text
holdout_semantic_revelation_state = REVEALED_FOR_ARCHITECTURE_TUNING
holdout_retirement_state = RETIRED_FOR_ARCHITECTURE_REPAIR
future_unseen_holdout_reuse_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
```

Across FIRST/A/B/C preserve exactly the same:

```text
source lock
evidence packet
ordered cohort
context grouping
model
reasoning effort
prompt
schema
investment architecture
timeout
transport topology
```

Required:

```text
first_abc_source_drift = 0
model_semantic_input_drift = 0
```

---

# 23. Stability aggregation rule

Stability aggregation is performed only across repeated runs that were validly allowed to complete.

Do not include an invalid run after its own semantic gate failed.

Do not continue to B/C after an earlier run failed merely to increase sample count.

If insufficient valid repeated runs remain for a requested stability metric:

```text
NOT_MEASURED
```

or the repository's canonical equivalent.

Do not infer stability from incomplete evidence.

---

# 24. Phase I — Directional Core stability

After sufficient valid repeated outputs exist, use the pre-existing audit semantics.

Classify each subject using existing categories such as:

```text
STABLE
BOUNDARY_UNCERTAINTY
UNSTABLE
```

Do not redefine thresholds after observing output.

Report complete counts and subject-level evidence.

Do not force all subjects to be STABLE.

Boundary uncertainty is evidence, not a defect to hide.

---

# 25. Phase J — Price-Timing stability

Likewise audit Price-Timing stability under frozen semantics.

Report:

```text
STABLE
BOUNDARY_UNCERTAINTY
UNSTABLE
```

with subject-level evidence.

Do not mutate technical/OHLCV calculations to improve stability.

Unavailable price indicators remain unavailable.

---

# 26. Phase K — Real-output ownership hard gates

When real outputs exist, the following are hard invariants.

Required:

```text
DIRECTIONAL_CORE_PRICE_TECHNICAL_REFS = 0
DIRECTIONAL_CORE_SUPPLY_REFS = 0
SUPPLY_DIRECTIONAL_CORE_USAGE = 0

BUY_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
SELL_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0

TIMING_STAGE_DIRECTION_MUTATION = 0
TIMING_STAGE_BALANCE_MUTATION = 0
TIMING_STAGE_HOLD_LEAN_MUTATION = 0

PRICE_TIMING_NEW_BUYER_UPGRADE = 0
PRICE_ONLY_HOLDER_REDUCE = 0

PRICE_ONLY_DIRECTIONAL_OWNERSHIP_VIOLATIONS = 0

FINAL_DIRECTION_OWNER = DIRECTIONAL_CORE
```

Also preserve system-wide hard gates:

```text
DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
PRICE_ONLY_DIRECTIONAL_MODEL_CALLS = 0
```

If the repository uses equivalent canonical field names:

- report the canonical repository field names;
- explicitly map each field to the invariant above.

Do not weaken an invariant because a field name differs.

## 26.1 Directional Core owns

```text
BUY / HOLD / SELL
BUY:SELL
HOLD lean
fundamental new-buyer stance
fundamental holder stance
business invalidation
```

## 26.2 Price-Timing owns

```text
technical state
timing modifier
entry mode
price review
holder price-review pressure
```

Price Timing may only make a new-buyer stance more conservative:

```text
ATTRACTIVE → WAIT / AVOID
WAIT → AVOID
AVOID → AVOID
```

It may not upgrade.

Thus:

```text
PRICE_TIMING_NEW_BUYER_UPGRADE = 0
```

Price Timing may:

```text
HOLDABLE → REVIEW
```

It may not create:

```text
REDUCE
```

Thus:

```text
PRICE_ONLY_HOLDER_REDUCE = 0
```

Technical breakdown is not business invalidation.

Supply/positioning is not Directional Core evidence.

---

# 27. Phase L — Renderer/action ownership hard gate

Renderer/action ownership is a hard gate on every real-output run.

Require:

```text
PRIMARY_USER_ACTION_WORDING_OWNER = RENDERER
AI_IMPERATIVE_PRIMARY_ACTION = 0
```

Also audit:

```text
structured_action_consistency
renderer_ownership_violations
```

Do not weaken Structured Actionability or renderer ownership to pass the cohort.

Do not recreate broad Korean phrase allowlists.

AI reasoning owns substantive explanation.

Structured state + renderer own the primary user-facing action wording.

If renderer/action ownership fails on any real-output run:

```text
holdout_semantic_revelation_state = REVEALED_FOR_ARCHITECTURE_TUNING
holdout_retirement_state = RETIRED_FOR_ARCHITECTURE_REPAIR
STOP
```

No same-cohort hotfix or rerun.

---

# 28. Phase M — Hard-safety regression

Run established hard-safety audit on every real-output run where applicable.

Preserve at minimum:

```text
evidence identity fencing
numeric provenance
accounting attribution
ADR/security basis
official provisional earnings semantics
future checkpoint logic
source sufficiency
issuer dedup
actionability command detection
renderer ownership
structured contradiction detection
```

Do not weaken validators to obtain a pass.

Do not convert unavailable or Unknown evidence into directional evidence.

Investment safety semantics remain unchanged, including:

```text
no one-quarter EPS annualization into PER
no EPS/BVPS reverse engineering from provider multiples
no ADR/ordinary-share basis mixing
no total-net-income/common-income substitution
no fabricated balance-sheet/FCF facts from provisional earnings
no RSI/MACD-as-valuation
no price/supply-as-business-thesis-delta
no invented support/resistance/target/stop
```

Source-sufficiency hard gates include:

```text
DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
PRICE_ONLY_DIRECTIONAL_MODEL_CALLS = 0
```

If any hard-safety semantic defect requiring code repair is found after real output:

```text
holdout_semantic_revelation_state = REVEALED_FOR_ARCHITECTURE_TUNING
holdout_retirement_state = RETIRED_FOR_ARCHITECTURE_REPAIR
STOP
```

---

# 29. Successful ownership-proof retirement

If FIRST, A, B and C all complete and all required per-run ownership/renderer/hard-safety gates pass, then after final aggregation:

```text
holdout_output_exposure_state = FULLY_EXPOSED
holdout_semantic_revelation_state = NO_SEMANTIC_DEFECT_OBSERVED
holdout_retirement_state = RETIRED_AFTER_EVALUATION
future_unseen_holdout_reuse_allowed = 0
```

The cohort may remain available for regression/history.

It may not later be represented as a fresh unseen holdout.

---

# 30. Production and Night Futures no-change proof

Produce explicit machine-readable proof:

```text
main_merge = 0
production_db_mutation = 0
production_telegram_send = 0
production_scheduler_change = 0
monitoring_registration_calls = 0
live_structured_autonomy_activation = 0
live_v2_change = 0

night_futures_code_mutation = 0
night_futures_decision_packet_injection = 0
```

Night Futures remains separate market/timing context.

It must not be injected into company Directional Core.

---

# 31. Monitoring Bootstrap is out of scope

Do not begin monitoring-bootstrap implementation in this task.

Desired later lifecycle remains:

```text
Initial Analysis
→ explicit user monitoring request
→ stored investment-logic baseline
→ bootstrap fundamental enrichment
→ source-sufficiency check
→ monitoring-ready baseline
→ Daily Delta afterward
```

Important:

```text
bootstrap enrichment != Daily Delta
```

Do not register any stock during this task.

`Monitoring Bootstrap Integration Review` is the next scope only if:

```text
FIRST/A/B/C complete successfully
all per-run ownership gates PASS
all per-run renderer/action ownership gates PASS
all per-run hard-safety gates PASS
final stability/generalization analysis is valid
```

Then:

```text
next_scope =
Monitoring Bootstrap Integration Review
```

If the task stops earlier, `next_scope` must identify the actual blocker.

Do not emit monitoring-bootstrap readiness by default.

---

# 32. Hard-stop matrix

## 32.1 Stop before model invocation if

```text
repository provenance contains semantic drift
repair root cause not confirmed
repair requires widening adapter/transport semantics
binding preflight fails
investment architecture semantics drift
prompt/schema semantics drift
source/evidence inputs drift
source-sufficiency policy drift
model/effort changed
batch/context shape changed
transport topology changed
timeout ownership changed
current holdout reuse gate fails
mixed-file transport path cannot be proven unaffected
```

## 32.2 Stop and require broader transport revalidation if

The repair changes:

```text
ContinuationTransportAdapter
instrumented Codex invocation
shared transport service
timeout/process lifecycle
model-context construction
prompt/schema serialization used by canaries
```

Do not rerun the seven historical canaries inside this task.

## 32.3 Stop after partial output on nonsemantic execution failure

```text
holdout_output_exposure_state = PARTIALLY_EXPOSED
holdout_semantic_revelation_state = NOT_MEASURED
holdout_retirement_state = RETIRED_PARTIAL_EXPOSURE
future_unseen_holdout_reuse_allowed = 0
```

Do not call it an architecture failure.

Do not reuse the same cohort as a new unseen holdout.

## 32.4 Stop at the first per-run semantic failure

For FIRST, A, B or C, if any required:

```text
ownership gate
renderer/action ownership gate
hard-safety gate
```

fails:

```text
STOP
holdout_semantic_revelation_state = REVEALED_FOR_ARCHITECTURE_TUNING
holdout_retirement_state = RETIRED_FOR_ARCHITECTURE_REPAIR
future_unseen_holdout_reuse_allowed = 0
```

Do not continue to the next repeated run.

No same-cohort hotfix or rerun.

---

# 33. Required machine-readable artifacts

Produce Markdown plus machine-readable JSON evidence for at least:

```text
01-repository-provenance
02-runner-adapter-root-cause
03-runner-adapter-binding-contract
04-runner-adapter-binding-preflight
05-bounded-diff-audit
06-architecture-semantic-freeze-reverification
07-source-freeze-reverification
08-transport-topology-freeze-reverification
09-model-semantic-input-freeze
10-current-holdout-reuse-gate
11-live-workload-coexistence-audit
12-resume-source-lock
13-resume-first
14-first-per-run-semantic-gates
15-resume-run-a
16-run-a-per-run-semantic-gates
17-resume-run-b
18-run-b-per-run-semantic-gates
19-resume-run-c
20-run-c-per-run-semantic-gates
21-holdout-exposure-retirement-state
22-resume-core-stability
23-resume-timing-stability
24-resume-dominance-ownership
25-resume-renderer-action-ownership
26-resume-hard-safety-regression
27-production-no-change
28-night-futures-no-change
29-monitoring-bootstrap-next-handoff
30-program-completion
```

Exact filenames may follow repository conventions.

Do not overwrite preceding task evidence.

All model lifecycle receipts created by this task must be included.

No secret-bearing raw command material may be included.

---

# 34. Program-completion report

The final machine-readable completion object must distinguish:

```text
implementation success
execution-harness failure
transport failure
source/freeze failure
semantic/architecture failure
renderer/action ownership failure
hard-safety failure
not-measured state
```

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
final_head_sha
branch

root_cause
repair_scope
runner_adapter_binding_preflight
execution_harness_interface_failure

authorized_harness_hash_drift
transport_topology_mutation
model_semantic_input_drift

model
reasoning_effort
model_timeout_seconds
model_timeout_owner_count
orphan_model_process_count

batch_semantics
real_run_batch_size
batch_split_adopted

architecture_semantic_drift
prompt_semantic_drift
schema_semantic_drift
source_drift
source_sufficiency_policy_drift

current_holdout_source_lock
current_holdout_reuse_allowed

first_model_invocation_started

real_holdout_model_invocation_count
real_holdout_directional_context_count
real_holdout_price_timing_context_count
real_holdout_subject_output_count

real_holdout_transport_retry_count

run_results.first
run_results.a
run_results.b
run_results.c

first_ownership_gate_status
first_renderer_gate_status
first_hard_safety_gate_status

run_a_ownership_gate_status
run_a_renderer_gate_status
run_a_hard_safety_gate_status

run_b_ownership_gate_status
run_b_renderer_gate_status
run_b_hard_safety_gate_status

run_c_ownership_gate_status
run_c_renderer_gate_status
run_c_hard_safety_gate_status

holdout_output_exposure_state
holdout_semantic_revelation_state
holdout_retirement_state
future_unseen_holdout_reuse_allowed
same_cohort_architecture_tuning_rerun_allowed

core_stability_counts
timing_stability_counts

directional_core_price_technical_refs
directional_core_supply_refs
supply_directional_core_usage

buy_without_nonprice_material_anchor
sell_without_nonprice_material_anchor

timing_stage_direction_mutation
timing_stage_balance_mutation
timing_stage_hold_lean_mutation

price_timing_new_buyer_upgrade
price_only_holder_reduce
price_only_directional_ownership_violations

directional_model_calls_on_source_insufficient
price_only_directional_model_calls

final_direction_owner

primary_user_action_wording_owner
ai_imperative_primary_action
renderer_ownership_violations

known_hard_safety_regression

main_merge
production_db_mutation
production_scheduler_change
production_telegram_send
monitoring_registration_calls
live_structured_autonomy_activation
live_v2_change
night_futures_code_mutation

ownership_generalization_verdict
readiness
stop_reason
next_scope
```

If current repository canonical field names differ, explicitly provide the mapping.

Anything not actually measured remains:

```text
NOT_MEASURED
```

Do not convert absence of evidence into PASS or FAIL.

---

# 35. Per-run gate reporting requirements

Every completed real-output run must have its own semantic gate record.

For FIRST:

```text
first_ownership_gate_status
first_renderer_gate_status
first_hard_safety_gate_status
```

For A:

```text
run_a_ownership_gate_status
run_a_renderer_gate_status
run_a_hard_safety_gate_status
```

For B:

```text
run_b_ownership_gate_status
run_b_renderer_gate_status
run_b_hard_safety_gate_status
```

For C:

```text
run_c_ownership_gate_status
run_c_renderer_gate_status
run_c_hard_safety_gate_status
```

Each status should distinguish at minimum:

```text
PASS
FAIL
NOT_MEASURED
```

or repository canonical equivalents.

No next run may start until the preceding run's required semantic gates are PASS.

---

# 36. Real-holdout counting requirements

The final report must explicitly distinguish:

```text
FIRST complete-run count
model invocation count
Directional Core shared-context count
Price-Timing context count
subject output count
```

Required fields:

```text
real_holdout_model_invocation_count
real_holdout_directional_context_count
real_holdout_price_timing_context_count
real_holdout_subject_output_count
```

A batch size of four means multiple model invocations may be required for one complete FIRST.

Do not report:

```text
FIRST exactly once
```

as evidence that:

```text
model invocation count = 1
```

unless the frozen architecture actually produces that result.

---

# 37. Artifact integrity

Create an artifact index containing, for every result artifact:

```text
relative path
SHA-256
byte size
```

Verify the index against the final bundle before completion.

Report:

```text
artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
```

Expected:

```text
artifact_hash_mismatch_count = 0
artifact_size_mismatch_count = 0
```

Also report the final ZIP SHA-256.

---

# 38. Repair-level success condition

This task succeeds at the runner-repair level only if:

```text
runner-adapter root cause confirmed
minimal harness repair completed
binding regression added
binding preflight PASS
full tests PASS
ruff/lint PASS

architecture semantics unchanged
prompt/schema semantics unchanged
source/evidence semantics unchanged
transport topology unchanged
model semantic inputs unchanged

authorized harness hash drift classified correctly
holdout reuse gate PASS
```

Runner/harness byte hashes need not remain identical if their drift is explicitly proven to be authorized harness-only drift.

---

# 39. Ownership-proof program success condition

The ownership-proof program succeeds only if FIRST/A/B/C produce sufficient real output and **each run passes its own semantic gates before the next run is allowed**.

Required hard ownership gates:

```text
DIRECTIONAL_CORE_PRICE_TECHNICAL_REFS = 0
DIRECTIONAL_CORE_SUPPLY_REFS = 0
SUPPLY_DIRECTIONAL_CORE_USAGE = 0

BUY_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
SELL_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0

TIMING_STAGE_DIRECTION_MUTATION = 0
TIMING_STAGE_BALANCE_MUTATION = 0
TIMING_STAGE_HOLD_LEAN_MUTATION = 0

PRICE_TIMING_NEW_BUYER_UPGRADE = 0
PRICE_ONLY_HOLDER_REDUCE = 0
PRICE_ONLY_DIRECTIONAL_OWNERSHIP_VIOLATIONS = 0

DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
PRICE_ONLY_DIRECTIONAL_MODEL_CALLS = 0

FINAL_DIRECTION_OWNER = DIRECTIONAL_CORE
```

Renderer/action hard gates:

```text
PRIMARY_USER_ACTION_WORDING_OWNER = RENDERER
AI_IMPERATIVE_PRIMARY_ACTION = 0
```

Hard-safety regression must pass for every applicable run.

Only then:

```text
holdout_output_exposure_state = FULLY_EXPOSED
holdout_semantic_revelation_state = NO_SEMANTIC_DEFECT_OBSERVED
holdout_retirement_state = RETIRED_AFTER_EVALUATION
future_unseen_holdout_reuse_allowed = 0
```

and readiness may be:

```text
READY_FOR_MONITORING_BOOTSTRAP_INTEGRATION_REVIEW
```

or the repository's already-defined equivalent.

Do not begin that next phase in this task.

---

# 40. Failure semantics

If runner repair succeeds but real execution later fails, do not describe the entire task simply as "repair failed."

Separate:

```text
runner repair result
real-holdout execution result
output exposure state
semantic revelation state
retirement state
ownership measurement result
renderer/action ownership result
hard-safety result
production activation state
```

## 40.1 Execution-harness failure before real output

If no real investment output exists:

```text
holdout_output_exposure_state = UNEXPOSED
holdout_semantic_revelation_state = NOT_MEASURED
holdout_retirement_state = ACTIVE_UNEXPOSED
ownership_generalization_verdict = NOT_MEASURED
```

Do not call it an architecture failure.

`next_scope` should identify the actual remaining execution-harness blocker.

## 40.2 Partial-output nonsemantic execution failure

If some usable real investment output exists but the run fails for a nonsemantic execution reason:

```text
holdout_output_exposure_state = PARTIALLY_EXPOSED
holdout_semantic_revelation_state = NOT_MEASURED
holdout_retirement_state = RETIRED_PARTIAL_EXPOSURE
future_unseen_holdout_reuse_allowed = 0
```

Do not call it an architecture failure.

Do not present the cohort later as a fresh unseen holdout.

## 40.3 Transport-semantic scope exceeded

If repair requires validated transport semantics to change:

```text
STOP
next_scope = BROADER_TRANSPORT_REVALIDATION
```

Do not rely on historical canaries as sufficient proof.

## 40.4 Real-output semantic failure

If real output reveals ownership, renderer, or hard-safety semantic failure:

```text
holdout_semantic_revelation_state =
REVEALED_FOR_ARCHITECTURE_TUNING

holdout_retirement_state =
RETIRED_FOR_ARCHITECTURE_REPAIR
```

Then:

```text
freeze evidence
do not hotfix
do not rerun
do not continue to the next repeated run
```

Recommend:

```text
separately authorized generic architecture repair
→ new issuer-level holdout
→ fresh ownership proof
```

## 40.5 Full success

Only after FIRST/A/B/C + per-run ownership + per-run renderer + per-run hard-safety + valid stability aggregation all succeed:

```text
holdout_retirement_state =
RETIRED_AFTER_EVALUATION

next_scope =
Monitoring Bootstrap Integration Review
```

---

# 41. Final experiment rule

Any real investment output consumes at least part of the holdout's unseen status.

Therefore always distinguish:

```text
Did the model output anything?
```

from:

```text
Did the output expose an architecture defect?
```

from:

```text
Did the complete ownership proof pass?
```

These are three different questions and must not share one state field.

A partial transport/harness failure after output is not automatically a semantic failure, but it does retire the cohort from future unseen-holdout use.

A complete FIRST is not sufficient to begin A unless FIRST's own ownership, renderer/action and hard-safety gates pass.

The same rule applies between A→B and B→C.

---

# 42. Final task principle

The smallest valid next change is:

```text
make the real-holdout runner conform to the already-proven adapter interface
```

not:

```text
change the adapter until the old runner happens to work
```

The repair may create:

```text
AUTHORIZED_HARNESS_HASH_DRIFT
```

but must not create:

```text
TRANSPORT_TOPOLOGY_MUTATION
MODEL_SEMANTIC_INPUT_DRIFT
```

After repair:

```text
measure
do not tune
```

Preserve:

```text
the experiment
the source lock
the investment semantics
the ownership boundaries
the renderer ownership contract
the transport topology
production no-change
```

Once any real output exists, also preserve the correct exposure/retirement state and never misrepresent the cohort as unseen again.
