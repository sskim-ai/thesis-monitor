# Thesis Monitor — Fresh Issuer Ownership Proof with Unresolved Transport Risk Carried

## 0. Task identity and objective

Work instruction:
`20260907-fresh-issuer-ownership-proof-with-unresolved-transport-risk-carried.md`

Suggested result bundle:
`thesis-monitor-20260907-fresh-issuer-ownership-proof-transport-risk-carried-report.zip`

This is the separately authorized real-proof attempt identified by the latest completed fictional diagnostic:

`SEPARATELY_AUTHORIZED_REAL_PROOF_WITH_TRANSPORT_RISK_CARRIED`

Execution of this instruction authorizes one bounded, precommitted, fresh-issuer FIRST/A/B/C ownership-proof attempt using existing data routes and the frozen investment architecture. It does not authorize a deployment or an unlimited reliability experiment.

The current bounded fictional diagnostic is finished. Do not run another three-call fictional probe, another capacity smoke test, or an additional review-only stage before proceeding merely because the historical root cause remains unresolved.

Intended path:

```text
verify existing pause and repository state
→ select a fresh US4/KR12 using existing supported sources and exclusions
→ freeze fresh source evidence, runtime bindings and existing acceptance protocol
→ FIRST → hard gates → A → hard gates → B → hard gates → C → hard gates
→ actual canonical stability/generalization decision
→ retain open quality and runtime limitations explicitly
```

If a required preparation gate cannot be met, stop with its concrete cause. Do not invent a new platform, expand the task, or weaken a gate to start the model.

## 1. Source of truth and input integrity

### Latest diagnostic: historical facts and next-scope authority

Input:
`thesis-monitor-20260907-bounded-fictional-websocket-reconnect-observability-diagnostic-report.zip`

Expected SHA-256:
`1546b6830ab350f7d7cee68f8b2732806aa5e53b3a5cde8ed4648ee1223d49ec`

Verify the ZIP and its internal index before using it. The supplied result has 84 file members, 83 individually indexed payloads, and only `artifact-index.json` excluded from its own index.

Historical predecessor:
`thesis-monitor-20260907-websocket-timeout-runtime-review-first-a-evidence-closeout-report.zip`

Expected SHA-256:
`1ddf30ac6b1420eed68f6e44e7f19ef455ca3e6619588b45be537e1d385612b3`

The latest ZIP contains ten exact historical-baseline copies from that predecessor. Prefer those compact copies for historical facts; consult the full predecessor and actual repository when a required existing contract or full exclusion registry is not in the latest bundle.

### Concern-specific authority

- Historical outcomes: newest applicable measured result; do not overwrite history with current code.
- Actual implementation/configuration: current repository and worktree; do not pretend historical HEAD is current.
- This task's execution permission and boundaries: this instruction.
- Investment/data semantics: existing frozen code/contracts and Investment Thesis Analysis & Monitoring Knowledge Guide.
- Earlier instructions: context and protocol lineage, not permission to undo later fixes or retirements.

If a meaningful conflict cannot be resolved by recorded evidence, expose it and stop the affected execution. Do not silently change history, rerun experiments, or invent missing rules.

## 2. What the latest diagnostic did and did not establish

Measured:

```text
status = PASS_BOUNDED_DIAGNOSTIC_COMPLETED
diagnostic_execution_status = COMPLETED_BOUNDED_BUDGET

fictional subprocess calls = 3
real issuer model calls = 0

call 1 = VALID_OUTPUT_NO_DISCONNECT, 281.826867 seconds
call 2 = VALID_OUTPUT_NO_DISCONNECT, 217.744123 seconds
call 3 = VALID_OUTPUT_NO_DISCONNECT, 205.852900 seconds

input bytes per call = 17704
distinct sessions = 3
distinct runtime namespaces = 3
distinct prompt hashes = 1
distinct schema hashes = 1

observed disconnect signals = 0
observed CLI retry signals = 0
wrapper explicit retries = 0
capacity/context-length diagnostics = 0
orphan processes = 0
evidence preservation failures = 0
```

Not established:

```text
reconnect_path_exercised = NOT_OBSERVED
recovery_observed = NOT_OBSERVED
runtime_root_cause_confirmed = false
root_cause_status = UNRESOLVED
runtime_reliability_status = NOT_ESTABLISHED
upstream_request_attempt_count = UNKNOWN
request_accepted_observability = UNAVAILABLE
formal_generalization_status = NOT_ESTABLISHED
formal_stability_status = NOT_MEASURED
```

The probe did not reproduce the historical 16-successful-context sequence before the seventeenth call failed. Three clean fictional Core calls are not a production reliability proof and do not prove the reconnect path works.

This task carries that unresolved risk. It does not claim the risk has been fixed.

## 3. Historical real evidence remains valid within its measured scope

The historical real cohort completed FIRST and A, each with Core16 and Timing16, and their applicable ownership/renderer/hard-safety gates passed as recorded. Historical B failed on its first Core context after a WebSocket disconnect warning followed by the existing watchdog timeout; C did not run.

Carry forward separately:

```text
historical FIRST hard gates = PASS
historical A hard gates = PASS
historical full four-run proof = INCOMPLETE

descriptive direction agreement = 9/16
descriptive boundary direction changes = 7/16
direction-changing balance step = 0.5
direct BUY ↔ SELL flips = 0

historical FIRST message_quality = FAIL, 14 repeated substantive spans
historical A message_quality = FAIL, 9 repeated substantive spans
historical quality gate role =
ADVISORY_NOT_INCLUDED_IN_RUN_STATUS_OR_PROGRESS_SEQUENCE

issuer-specific evidence omission status = NOT_MEASURED
```

Repeated spans are not failed-issuer counts. The descriptive comparison is not a formal stability verdict.

Do not:
- combine historical FIRST/A with new B/C to finish one proof;
- downgrade recorded hard-gate passes solely because B failed;
- relabel advisory quality FAIL as PASS;
- claim runtime success solves issuer-specific explanation quality;
- tune scores, prompts, aliases or renderer wording against exposed outputs.

## 4. Keep scheduled monitoring paused

Latest completion recorded eight paused objects at:
`2026-09-07T14:20:41.816293+00:00`
(KST: `2026-09-07 23:20:41`).

This is historical observation, not evidence of current state.

Before real calls, use the existing safe observation path to verify:
- US/KR data-generation schedules remain disabled/unloaded;
- US/KR AI-review primary and backup schedules remain paused;
- monitoring fallback and delivery-retry paths remain paused;
- there is no unexpected active monitoring path or running workload conflict.

Do not create another scheduler control system or an entitlement gate.
Do not interpret "not currently running" as "schedule disabled."
Do not treat an unavailable observation as zero contention.

This task changes no schedules and never automatically resumes them. Preserve registration, investment-logic versions, assessment history, warnings, price rules, pending-message state and unrelated Macro/Night Futures workflows.

Unexpected reactivation, unsafe coexistence or unavailable required observation stops model execution. Record the concrete state. Do not kill unrelated jobs, send backlog, remove records, or silently toggle shared schedules.

## 5. Repository provenance and bounded implementation scope

Latest reported provenance:

```text
base_sha = 428cb9523e2f56460b537e556ce3b91ac5e10a2e
work_instruction_commit = 0dfd97f9b53d11857806c7216ccdd7c071192b4f
implementation_commit = 09e6df237209aa1ce6afdbaa1bca124c6dcdcf22
final_head_sha = 446b5ce077d7a5ec0d2a8429fa95f0e23bf8f9d4
branch = codex/20260907-bounded-fictional-websocket-reconnect-observability-diagnostic
```

Record actual branch, HEAD, worktree status and relevant diff. Do not assume the branch above is still active. Do not reset, cherry-pick, merge or discard unrelated work to force a match.

Commit this instruction before any permitted implementation change.

Prefer zero semantic implementation changes. Allowed, only when needed:
- experiment-run configuration for the fresh generation;
- existing selection/source-lock/report glue;
- use of the already validated passive runtime-event extractor;
- narrowly necessary experiment artifact/counter serialization;
- model-free regression tests for actually changed paths.

Forbidden:
- investment architecture, source-sufficiency or accounting/valuation safety changes;
- prompt/schema investment semantics or score/transition changes;
- adapter API, process topology, CLI, protocol, network/TLS or timeout changes;
- new reconnection, retry, backoff, idle-watchdog or recovery policy;
- universe-expansion, broad source-remediation or free-API governance project;
- generic quality/prompt/renderer repair;
- production integration, deployment or schedule resumption.

Unexpected semantic drift requires a concrete separate scope before real calls. Authorized experiment-only byte changes are not automatically semantic drift, but require a bounded diff explanation.

## 6. Model, CLI, context and call-budget freeze

Use the existing real-proof execution path:

```text
model = gpt-5.6-sol
reasoning_effort = xhigh
batch_semantics = MODEL_CONTEXT_COUPLED
subjects_per_context = 4
configured_timeout_seconds = 1800
authoritative_timeout_owner_count = 1

cli_version = codex-cli 0.148.0-alpha.15
cli_binary_sha256 =
7645c3caf5607e4528eb3a15b12496c284c2a918939aed34e863c760c1b421e7
```

Verify the local CLI identity. An unexpected change is not permission to install/downgrade a binary or reinterpret comparability; record and stop for explicit review.

Retain the actual canonical sequential model invocation/session/namespace policy used by the validated runner. Preserve runtime isolation and exact per-context IDs; do not reuse an interactive chat or previous responses as hidden context. Do not change the namespace policy to chase success. Record it before calls.

Expected existing architecture, to be confirmed before execution:

| Run | Core contexts | Timing contexts | Model subprocess attempts, maximum |
|---|---:|---:|---:|
| FIRST | 4 | 4 | 8 |
| A | 4 | 4 | 8 |
| B | 4 | 4 | 8 |
| C | 4 | 4 | 8 |
| Total | 16 | 16 | 32 |

Composer and renderer use the existing deterministic path; they do not receive new LLM calls.

Budgets:

```text
maximum new real model subprocess attempts = 32
new fictional model calls = 0
capacity/health model smoke calls = 0
wrapper explicit retries = 0
failed-call replacement attempts = 0
automatic replacement-cohort attempts = 0
maximum fresh real cohorts in this task = 1
```

Every launched subprocess consumes budget, including zero-output failure.
The 32-call budget is a ceiling, not a target to fill after a stop.
CLI-internal upstream requests remain separately observable/unknown; 32 subprocesses is not a claim of 32 remote accepted requests.

If the actual frozen architecture is materially different, report it before any call; do not silently add invocations or split batches.

## 7. Existing free data routes, not a new data project

Keep the user's external-data policy:
- existing free data routes only;
- no paid provider, account/plan upgrade, new billing, paid fallback or credentials;
- no new free-entitlement, quota-management or cost-audit gate;
- use existing caches, rate-limit behavior and bounded source retries;
- do not scan thousands of issuers for detailed financial data.

This instruction does not certify current account billing. Do not invent that certification. The fixed existing AI inference setting remains distinct from external market-data policy.

Preserve the established separation:

```text
fundamental source sufficiency
≠ price/technical readiness
```

A canonical `UNAVAILABLE_SAFE` price path may remain valid without fabricating prices/indicators or blocking an otherwise permitted fundamental analysis. Malformed, future, identity-incompatible or accounting-basis-invalid data remains blocked.

Use only source validation and freshness requirements already required by the frozen protocol. Do not add new thresholds to force PASS or invent current values.

## 8. Fresh issuer selection with complete exclusions

Use the current supported reference and entire issuer-level exposure/retirement registry, not an old summary count or manually chosen familiar names.

At minimum the latest fully exposed retired cohort remains excluded:

```text
NU / LNG / TCI / WMG
043100 / 025000 / 131030 / 372910
130740 / 045340 / 006910 / 288980
023440 / 064820 / 001440 / 073490
```

This is not the full exclusion registry. Preserve all older retired cohorts, canonical issuer identities, aliases, share classes and ADR/ordinary mappings.

Do not:
- recycle historical B's failed batch or C;
- use only the not-yet-completed Timing subjects;
- treat an issuer alias as a new issuer;
- shrink exclusions to fill a market target;
- treat experiment exclusion as a user-registration ban.

Source-only diagnostic access is not by itself a real-model exposure. Classify histories accurately under the existing contract.

Target: exactly 16 fresh issuers, US4/KR12.

Before source evaluation, freeze the existing deterministic candidate ordering, reserve ordering, selection seed/rule, supported-universe snapshot, exclusion hash and per-market bounded evaluation budgets. Prefer existing canonical policy; do not write a new selector.

Evaluate ordered candidates/reserves until the required source-ready count is met or the precommitted budget is exhausted. Replacement is allowed only pre-model for objective eligibility, identity or source failure—not expected direction, price attractiveness, model behavior or stylistic quality.

Diagnose both US and KR within their bounded budgets even if the first market has an ordinary source shortfall. A global safety/identity-integrity conflict may stop execution, but distinguish it from a market-specific insufficiency.

If US4 and KR12 cannot both be obtained, make no real calls. Report separate market results; do not start a supported-universe expansion inside this task.

## 9. Fresh source generation and evidence cutoff

After both markets pass, build one fresh combined source generation for the selected 16 using unchanged source-assembly semantics. Preserve:
- canonical issuer/security identity and explicit market;
- required fundamental family coverage;
- per-issuer exact packet hashes;
- source provenance, report period, collection time and as-of;
- financial/price/security-basis validation;
- conditional price readiness;
- ordered context grouping.

New sources and packet hashes are expected to differ from historical cohorts. That authorized pre-freeze change is not a falsely claimed "historical source drift = 0." Freeze invariance is required within the new proof.

Freeze the financial/evaluation cutoff once. Wall-clock execution dates, session IDs and artifact timestamps may change, but must not silently refresh financial facts, assessment dates, pricing or future-checkpoint interpretation between FIRST/A/B/C.

Do not count reserve source probes as model calls; report both separately.

## 10. Single final precommit and model-free exact-path check

Use existing implementations; do not introduce a separate review-only task.

Before the first model invocation:
1. finish permitted code/config changes and record immutable implementation commit/diff;
2. finalize the cohort, source lock and actual model-facing request construction;
3. freeze the existing acceptance rules and their role;
4. save one final precommit with hashes and timestamps;
5. run the existing model-free check through the actual runner/adapter-bound files.

Include in precommit:

```text
source generation ID and aggregate source lock
ordered 16 issuers and US4/KR12 grouping
reference/exclusion/selection/reserve hashes
per-issuer evidence hashes and frozen evaluation cutoff
investment architecture/composer/renderer/validator identities
prompt/schema semantics and actual binding contract
model/effort/CLI/transport/timeout identities
run order, stage/context order and namespace policy
32-call cap, no retries and stop matrix
existing pause/coexistence check identity
existing per-context preservation/semantic-check identities
canonical stability/generalization thresholds and status mapping
message_quality gate role = existing advisory role
unresolved transport risk acknowledgement
```

Actual prompt, on-disk schema consts, validator expectations, receipt and manifest must agree through the single identity binding. Distinguish source generation identity from runtime invocation identity. Do not delete `const`, accept obsolete IDs, or compare only in-memory schemas.

Price-Timing model inputs depend on that run's actual Core output. Freeze the construction algorithm before calls; materialize and hash the exact Timing request from that run's preserved Core output immediately before its invocation. Never substitute a prior run's Core output to force identical Timing input.

Reuse validated tests/rehearsal results when relevant hashes are unchanged. Run focused/repository-required tests for touched code; do not mechanically rerun an old 64-context rehearsal or invoke fictional models merely for reassurance.

If existing formal stability/acceptance rules are missing or ambiguous, report the precise contract gap before real calls. Do not invent a permissive success threshold.

## 11. Execution order and hard gates

Run exactly:

```text
FIRST: Core01→02→03→04 → Timing01→02→03→04 → compose/render → run gates
A:     same frozen architecture → run gates
B:     same frozen architecture → run gates
C:     same frozen architecture → run gates
```

Use the existing canonical ordering if it differs only in equivalent deterministic compose/render placement; document it in precommit. Do not change model context grouping.

Before every heavy context, apply the existing safe workload observation. Paused schedules do not prove unrelated active processes are absent. Record observations without treating missing visibility as zero.

After each returned context:
1. persist the original response and lifecycle evidence;
2. validate schema, identity and subject order;
3. resolve permitted source aliases;
4. run all already-applicable hard semantic checks;
5. only then launch the next context.

No next run may start unless the preceding run's existing required execution/ownership/renderer/hard-safety gates PASS.

No model-facing hotfix after real output. Any hard failure ends the task's remaining model calls.

## 12. Ownership, accounting and action boundaries remain frozen

Use canonical field names with explicit mappings where necessary.

Required applicable hard invariants:

```text
DIRECTIONAL_CORE_PRICE_TECHNICAL_REFS = 0
DIRECTIONAL_CORE_SUPPLY_REFS = 0
SUPPLY_DIRECTIONAL_CORE_USAGE = 0
BUY_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
SELL_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
PRICE_ONLY_DIRECTIONAL_MODEL_CALLS = 0

TIMING_STAGE_DIRECTION_MUTATION = 0
TIMING_STAGE_BALANCE_MUTATION = 0
TIMING_STAGE_HOLD_LEAN_MUTATION = 0
PRICE_TIMING_NEW_BUYER_UPGRADE = 0
PRICE_ONLY_HOLDER_REDUCE = 0
PRICE_ONLY_DIRECTIONAL_OWNERSHIP_VIOLATIONS = 0

FINAL_DIRECTION_OWNER = DIRECTIONAL_CORE
PRIMARY_USER_ACTION_WORDING_OWNER = RENDERER
AI_IMPERATIVE_PRIMARY_ACTION = 0
KNOWN_HARD_SAFETY_REGRESSION = 0
```

Core owns fundamental direction, balance/lean, fundamental stances and business invalidation.
Timing can only make new-buyer stance more conservative and may create holder REVIEW pressure, not a price-only REDUCE or business invalidation.
Renderer owns primary user-facing action wording; AI reasoning owns explanation.

Preserve accounting attribution, denominator/security/ADR basis, provisional-earnings semantics, source identity, future-checkpoint and numeric provenance. No EPS annualization from one quarter, backsolving denominators from multiples, fabricated price levels, FCF or ROIC.

A not-run stage is NOT_MEASURED, not a proven zero violation.
Do not confuse transport/schema success with substantive investment correctness.

## 13. Message quality: preserve advisory role and report the actual result

The latest historical contract established message-quality auditing as advisory, not a progression hard gate. Preserve that role in this unchanged-architecture proof. Do not retroactively invalidate historical runs or quietly turn an advisory FAIL into PASS.

Run the existing quality audit on actual rendered outputs for every completed run. Preserve:
- exact messages and linked composed structured state;
- repeated substantive spans, not just aggregate status;
- which subjects/runs the findings concern;
- known limitations such as unmeasured issuer-specific omissions.

Do not hide repeated reasoning, paraphrase after the audit, filter bad messages out of the denominator, or patch the renderer to make a quality score improve.

A quality finding that independently violates an existing hard safety rule is still a hard safety failure; the advisory label cannot excuse it.

Full execution and hard-gate success can coexist with `message_quality=FAIL`. Report both. Do not advertise such a result as complete production readiness or claim explanation quality is solved.

This task does not perform generic message-quality repair. A later bounded quality task, if warranted, requires separately declared criteria and new unseen evaluation after semantic changes.

## 14. Passive runtime observations and exact error semantics

Reuse the already validated observer. No new protocol, network fault injection, packet capture, retry policy or watchdog.

For each actual subprocess, preserve:
- start/spawn/stdin/first-and-last stderr/stdout/exit/parse timeline;
- session and namespace identity;
- actual prompt/schema/output bytes and hashes;
- structured runtime warnings/errors distinguished from echoed prompt/model prose;
- terminal result, root diagnostic, downstream parse state;
- timeout initiator, process-group cleanup and orphan count.

Separate:
```text
wrapper_explicit_retry_count
observed_cli_retry_signal_count
observed_websocket_disconnect_signal_count
upstream_request_attempt_count
request_accepted_observability
```

Absence of warning records is not proof of no upstream retries.
A logged retry ceiling is not an observed request count.
Network readiness is not backend acceptance.

If a disconnect warning occurs but the same call later returns valid output, preserve `DISCONNECT_THEN_SUCCESS_OBSERVED` and let existing terminal hard gates determine continuation. Do not erase the warning or relaunch the call.

If the call terminates with watchdog timeout, explicit capacity/context failure, other nonzero failure or invalid output, stop all remaining model invocations.

Do not replace a primary capacity or disconnect diagnostic with secondary `OUTPUT_FILE_MISSING`.
Do not call every later timeout the same root cause as the historical event.

For pre-spawn guard rejection:
- preserve prepared input and root exception;
- record spawn=0 and receipt_expected=0;
- do not mask the cause as missing transport receipt.

## 15. No retry, no recovery experiment, no new-cohort loop

At the first terminal execution failure, hard schema/identity/semantic/safety failure, unsafe guard state or failed exact preservation:
- preserve all available evidence;
- record the actual attempt and stage coverage;
- leave unused budget unused;
- stop later contexts and runs;
- do not resume failed historical/new B or C;
- do not switch model, CLI or transport;
- do not raise timeout;
- do not shorten/split payload to force success;
- do not select another cohort in this task.

If another disconnect→timeout appears, record recurrence and the full new timeline. End with the evidence-supported next scope. Do not automatically start another three-call probe or broad speculative repair.

This is one risk-carried proof attempt, not a guarantee of completion.

## 16. Immediate preservation: include composed messages, not only model JSON

For every attempted context, preserve what actually exists:

```text
generation/run/stage/context/
  prompt.txt
  schema.json
  context_manifest.json
  identity-preflight.json
  workload-observation.json
  transport_receipt.json       # when expected/created
  output.raw.json              # exact produced bytes, even if invalid
  stdout.raw.log
  stderr.safe.log
  runtime-events.json
  lifecycle-summary.json
  partial-semantic-audit.json
```

For each available issuer's final deterministic result also preserve:
```text
composed-state.json
rendered-message.txt
renderer-input-lineage.json
```

Do not wait for all FIRST/A/B/C to finish before writing successful context artifacts.
Immediate evidence preservation failure is a stop, but it does not make produced output unseen.

Hash and reopen artifacts as they are saved. Preserve raw bytes; normalized derivatives are separate. If confidentiality requires redaction, record original hash/size and derivative status without exposing secrets. Do not label redacted bytes exact raw.

No Telegram sends, production DB writes or test notifications.
Keep raw and later offline-reconstructed artifacts explicitly distinct.

## 17. Exposure, retirement and counting

Keep at least these independent dimensions:

1. Was a model subprocess invoked?
2. Which unique issuers produced actual investment content?
3. Which stage/run outputs passed identity/schema and applicable semantic checks?
4. Is the locked cohort still eligible as unseen evidence?
5. Was a semantic defect observed?
6. Is formal four-run proof complete?

Record raw/partial/invalid output exposure separately from accepted-output count. An identity-invalid response with identifiable real investment judgments is still evidence exposure. Zero accepted rows is not zero raw output.

Issuer-level exposure is monotonic:
- no actual investment output: UNEXPOSED; future reuse is a separate explicit decision, never a same-task retry;
- some but not all issuers output: PARTIALLY_EXPOSED;
- all 16 issuer Core outputs exist: FULLY_EXPOSED, even if Timing is incomplete;
- later A/B/C failure never changes FULLY_EXPOSED back to PARTIALLY_EXPOSED.

Any nonsemantic failure after output preserves the existing cohort-level retirement/exclusion rule for the entire locked 16, not just successful subjects. A semantic defect uses REVEALED_FOR_ARCHITECTURE_TUNING / RETIRED_FOR_ARCHITECTURE_REPAIR or canonical equivalents. Full evaluated cohort remains retired as unseen evidence.

FIRST/A/B/C on one unchanged cohort are permitted only as the already-precommitted repeated protocol. They do not mean the cohort remains unseen after FIRST.

Counters must separate:
```text
planned / attempted / terminal / successful / failed / not-run contexts
FIRST attempted / FIRST completed
model subprocess count / upstream requests unknown
raw stage rows / accepted stage rows / semantic-checked rows
unique issuer exposure / completed issuer results per stage and run
```

Under the confirmed 32-context full-success architecture:
- unique real issuers = 16;
- Core rows across four runs = 64;
- Timing rows across four runs = 64;
- total model stage rows = 128;
- composed/rendered issuer-run results = 64.
These are expectations, not defaults to fill on failure.

## 18. Stability and generalization: compute, do not assume

After all legitimately completed runs, apply the canonical pre-frozen protocol to:
- Core stability, balance/lean and direction-boundary changes;
- Timing stability and stance/entry changes;
- ownership generalization;
- structured/rendered consistency.

Preserve classifications such as STABLE / BOUNDARY_UNCERTAINTY / UNSTABLE only as defined in that protocol.

Four completed runs and zero ownership violations do not automatically imply stable decisions. Do not tune thresholds after seeing results, call 9/16 a probability, or mask small-but-action-changing boundary moves.

If insufficient valid repeats exist, formal four-run stability/generalization remains NOT_MEASURED / NOT_ESTABLISHED. Retain partial runs and failures as evidence but do not put them in valid stability denominators.

Do not merge old real FIRST/A or fictional probe rows into the new proof.

If all execution, existing hard gates and canonical stability/generalization criteria genuinely PASS, hand off to Monitoring Bootstrap Integration Review with any advisory quality/runtime limitations explicitly open.

If canonical stability/generalization FAILS, report the actual failed criterion. Do not promote readiness because all subprocesses happened to finish.

## 19. Outcomes and next-scope limits

| Outcome | Required disposition |
|---|---|
| Source/precommit/guard unavailable before models | Specific preparation blocker, no proof claims; both safe market diagnoses reported where applicable |
| Execution failure with no investment output | Not measured; later reuse requires separate decision |
| Execution failure after partial/full output | Exact coverage and nonsemantic retirement; no retry or cohort loop |
| Existing hard semantic/safety defect | Preserve, retire for generic repair; no hotfix |
| Four runs complete, formal stability fails | Execution complete but formal criterion FAIL; separate remediation scope |
| Hard proof passes, advisory quality fails | Explicit hard-proof result plus quality-open state; not production-ready |
| All canonical proof criteria pass | Handoff to bootstrap integration review only, never implementation/deployment here |

A monitoring-bootstrap review is not automatic production promotion. Do not implement bootstrap, enable schedules, activate live V2/Structured Autonomy or send new-format monitoring messages in this task.

Do not promise a future background continuation. The executor must finish the bounded attempt or produce a truthful terminal stop package.

## 20. Production and user-policy boundaries

Required actual reported zeros:

```text
production_scheduler_mutation = 0
auto_resume_executed = 0
production_db_mutation = 0
production_send = 0
main_merge_or_deployment = 0
monitoring_registration_change = 0
live_v2_or_structured_autonomy_activation = 0
night_futures_change = 0
paid_data_service_change = 0
new_free_api_management_gate = 0
canonical_transport_mutation = 0
model_or_effort_change = 0
timeout_increase = 0
wrapper_explicit_retry_count = 0
historical_B_or_C_resumed = 0
```

These are task boundaries to verify, not values to hardcode irrespective of events. If an unintended mutation happens, report it and stop; never falsify the no-change ledger.

Existing registered issuers and data history are not removed. Experiment exclusions remain separate from actual user support eligibility.

## 21. Deliverables and minimum completion record

Keep reporting compact. Use Markdown plus JSON and per-context originals:

1. input integrity, actual repository provenance and current pause observation;
2. complete exclusion registry reference, bounded US/KR selection and source readiness;
3. fresh source lock and final execution/acceptance precommit;
4. exact per-attempt inputs, outputs, receipts, passive runtime events and early checks;
5. per-run hard gate results, actual composed states and rendered text;
6. per-run advisory message-quality results;
7. formal stability/generalization or precise unmeasured/failed status;
8. exposure/retirement and all counter reconciliations;
9. production/user-policy no-change evidence;
10. completion, payload index and external ZIP checksum.

At minimum completion includes:

```text
base_sha / work_instruction_commit / implementation_commit / final_head_sha / branch
input_zip_sha256 / input_integrity_status
pause_observed_at / observed_paused_schedule_count / unexpected_active_paths

selection_policy_hash / exclusion_registry_hash / source_generation_id / source_lock
ordered_issuers / issuer_market_mix / per_market_source_results
frozen_evaluation_cutoff / precommit_hash / precommit_time / first_spawn_time
acceptance_protocol_hash / quality_gate_role

model / effort / CLI version and hash / context grouping / namespace policy
timeout_seconds / timeout_owner_count / maximum_subprocess_budget

planned_contexts / attempted_contexts / terminal_contexts
successful_contexts / failed_contexts / not_run_contexts
per_stage_per_run_counts
raw_output_document_count / raw_stage_rows / accepted_stage_rows / checked_stage_rows
unique_issuer_output_count / composed_rows / rendered_rows

run_results.first / a / b / c
per_run_ownership_gate / renderer_gate / hard_safety_gate / message_quality
context_preservation_failures / per_context_semantic_failures

wrapper_retry_count / observed_cli_retry_signal_count / observed_disconnect_count
upstream_request_attempt_count / request_accepted_observability
disconnect_then_success_count / disconnect_then_timeout_count
explicit_capacity_count / explicit_context_failure_count / watchdog_timeout_count
orphan_count / primary_failure / failure_stage

exposure_state / semantic_revelation_state / retirement_state
future_unseen_reuse_allowed / same_cohort_tuning_rerun_allowed
core_stability / timing_stability / formal_generalization / acceptance_failures
open_message_quality_findings / carried_runtime_risk

production_and_policy_change_ledger
status / stop_reason / next_scope
indexed_payload_count / zip_member_count / integrity_mismatch_counts
```

Map to existing canonical fields rather than adding production schemas solely for reporting.
NOT_RUN, NOT_MEASURED, NOT_OBSERVED, UNKNOWN and observed zero must remain distinct.

## 22. Finalization

Finalize completion and all substantive artifacts before indexing. Index every payload, including README and completion, except the index itself. Store relative path, SHA-256 and byte size.

Reopen the final ZIP and verify every indexed payload and the actual member count. Resolve PENDING_FINALIZE/default-counter placeholders before delivery. Use an external `.zip.sha256` sidecar; do not embed a self-referential final ZIP checksum.

Keep the new proof distinct from historical evidence. Include only necessary historical comparison copies with source ZIP identities, not redundant full archives.

Final principle:

```text
The fictional diagnostic is complete.
The historical transport cause is unresolved.
Do not repeat diagnostics without a new question.
Do not claim reliability from three clean calls.
Use one fresh, precommitted real proof with the risk explicit.
Preserve every result and stop at the first actual hard failure.
Keep schedules paused and production unchanged.
```
