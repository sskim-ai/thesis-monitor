# Thesis Monitor — New Issuer Final Freeze & Ownership Proof
## Existing data routes; no new free-API gate project

Date: 2026-09-07
Task type: shadow experiment execution, not production activation

Suggested result bundle:
`thesis-monitor-20260907-new-issuer-final-freeze-ownership-proof-existing-data-routes-report.zip`

## 0. User priority and scope correction

The user has clarified that the currently used data routes are intended to be free
and that building a separate free-API gate is not an immediate priority.

Apply this decision as follows:

- Retain the free-data-only operating principle.
- Continue using existing configured data routes and existing safeguards.
- Do not add a provider/endpoint entitlement gate, billing-policy engine, upstream
  licensing inventory, quota reservation service, or new admission subsystem.
- Do not make completion of a new free-entitlement audit a prerequisite for this proof.
- Do not add a paid data provider, paid endpoint fallback, subscription, plan upgrade,
  auto-billing arrangement, trial-to-paid dependency, credential, or limit bypass.
- Reuse existing cache, throttling, retry caps, and live-workload protection.
- Retain the existing source, accounting, identity, ownership, preservation, and
  holdout-integrity checks. Deferring the new cost gate does not disable those checks.

This replaces the immediate audit/implementation sequencing of the earlier
`FREE_API_ONLY` addendum and any draft dedicated free-route admission review.
The addendum's no-paid-expansion and no-fabrication principles remain in force.
The old selection review's zero-model-call rule applied to that completed review.
This new work instruction, when assigned for execution, authorizes the bounded
real proof below after its existing prerequisites pass. It does not retroactively
turn the old review manifest into an executable source lock.

Do not claim that every account, upstream entitlement, or price plan was independently
audited. Record the cost approach as existing-route/no-new-paid-dependency policy,
not a new VERIFIED_FREE certification. A missing new audit document is not a blocker.
If direct evidence of a billable operation or automatic charge appears during normal
work, do not perform that operation or silently substitute a paid route. Report the
specific affected component; do not launch a broad entitlement project.

The AI execution model and its existing cost/configuration are separate from this
external-data constraint and remain unchanged.

## 1. Objective — move to the actual proof

The completed selection review is ready for a separately authorized real proof.

Execute the following bounded sequence:

    preserve the reviewed US4/KR12 selection
    → recheck current source eligibility and live workload using existing code
    → create a NEW final source generation, source lock, and execution precommit
    → validate actual request binding with the existing model-free preflight
    → FIRST
    → FIRST ownership / renderer / hard-safety gates
    → A → gates → B → gates → C → gates
    → frozen stability/generalization assessment
    → handoff, without production activation

This is not another universe expansion, support-count reconciliation, free-API
audit, source-policy redesign, or model-free-only review project.

Do not restart successfully closed repairs merely to produce a new audit.
Stop for a real hard failure, not for the absence of newly invented documentation.

## 2. Historical authority and verified baseline

Latest result:
`thesis-monitor-20260907-new-issuer-holdout-selection-preexecution-readiness-review-report.zip`

SHA-256:
`b2233e37113ac84def1e974e81c413e39f9f378a84182167ca9ae4b24c75bce0`

Canonical completion:
`reports/proofs/21-program-completion.json`

Next-step handoff:
`reports/proofs/20-next-proof-handoff-draft.json`

Historical results:

    selection_review_status = PASS
    terminal_matrix = BOTH_READY_FOR_SELECTION
    source_period_review_status = PASS
    source_provenance_review_status = PASS
    raw_and_canonical_hash_status = PASS
    actual_request_identity_preflight_status = PASS
    explicit_market_preflight_status = PASS

    model_free_simulated_context_count = 128
      synthetic baseline contexts = 64
      real-input model-free request contexts = 64
        fresh = 32
        resumed = 32

    network_request_count = 0
    real_investment_model_invocation_count = 0
    real_subject_output_count = 0
    real_first_a_b_c_execution_count = 0
    executable_proof_source_lock_created = 0

    readiness = READY_FOR_SEPARATELY_AUTHORIZED_NEW_HOLDOUT_PROOF
    next_scope = NEW_ISSUER_HOLDOUT_FINAL_FREEZE_AND_OWNERSHIP_PROOF
    ownership_generalization_verdict = NOT_ESTABLISHED

The archive has 2,545 files: 2,544 individually indexed payload files plus the
self-excluded index. Indexed hashes and sizes were verified with no mismatch.
The 128 preserved outputs are model-free fixtures, not real investment evidence.

Current repository HEAD/worktree remains authoritative for current code.
Historical bundle facts remain authoritative for historical runs.
The latest user priority governs this task's scope.

## 3. Repository provenance and bounded implementation

Latest reported repository identity:

    branch:
      codex/20260907-new-issuer-holdout-selection-preexecution-readiness-review
    base_sha:
      edd24c8bd0bf1d79d8bd49a875bf609eb90752eb
    work_instruction_commit:
      6ee6767001e3de2ea0ad98dd73b6e81772448e13
    implementation_commit:
      f56b7a69cc3bdfd085151d12c43f3a08791554de
    final_head_sha:
      8dcf12fc3d4b3bac2a6e62679feb9e05d222ea1e

Record actual branch, HEAD and worktree state. Do not assume they are unchanged.
Compare later commits by content; report-only commits are not automatically
semantic drift. Do not reset, discard, merge, or cherry-pick unrelated work.

Commit this instruction before implementation. Prefer no new implementation beyond
the existing experiment runner's final-freeze and real-execution wiring.
Allowed changes are experiment-only orchestration, execution manifests and reports.
Do not alter production routing, investment semantics, validators or shared transport.

If implementation changes are needed, run existing focused regressions, full
model-free tests and Ruff once, then record the tested implementation SHA and the
pre-FIRST freeze time. Reuse already verified unchanged tests/evidence appropriately;
do not repeatedly rebuild the 128-context review just to recreate the same PASS.

No investment or validator hotfix after real output is observed.

## 4. Preserve the reviewed selection and issuer exclusions

Proposed US4, in order:

    NVMI
    SKYH
    WKSP
    EROC

Proposed KR12, in order:

    373160
    452200
    389470
    380550
    008970
    047080
    068270
    475830
    033160
    079940
    103140
    278280

Context groups:

    [NVMI, SKYH, WKSP, EROC]
    [373160, 452200, 389470, 380550]
    [008970, 047080, 068270, 475830]
    [033160, 079940, 103140, 278280]

Review-manifest canonical hash:
`6bba480d63661967c80192af6d80287bb3bb9c4d9cf31084eaeeb6a45358b99c`

Selection-policy hash:
`c66583d78dc9155e454df172730691040778a25f5c8e4ab33da63919a172d574`

Exclusion-registry hash recorded by the review:
`f91ec0a8ea6a224a5da3375da28d83f01b46c518e8b94a8da0df23398a3f2415`

Load the actual reviewed issuer/security mapping, policy and exclusion registry.
Reconcile any subsequent real experiment exposure without shrinking exclusions.
Use canonical issuer keys and explicit market metadata, not ticker shape or company
name normalization. Model-free fixtures must not contaminate real exposure counts.

Preserve the current cohort unless objective pre-model identity/source checks fail.
Do not use manual replacements chosen for expected investment results.
If replacement is necessary, use only the existing precommitted reserve order and
bounded evaluation policy. Do not expand the universe in this task.

The 21 nonprimary supported US issuers are NOT 21 source-ready reserves:
there are 2 known source failures and 19 globally untested candidates. The latter
include 18 inside the earlier bounded budget and one outside it. Do not silently
extend that budget or promote untested/failed candidates to PASS.
No source-ready US reserve was established by the latest review.

If the frozen selection cannot meet US4/KR12 within its existing allowed process,
complete safe independent checks for both markets, report the exact source blocker,
and stop before any real model invocation.

## 5. Selected-subject source recheck — no broad audit

The latest review used archived packets and made no network requests. It explicitly
requires a source and workload recheck for future execution.

Use existing canonical source collection, source-sufficiency validation and freshness
rules on the selected subjects only. Reuse valid cache where allowed. Obtain only
updates needed for those rules through existing configured routes. Do not rescan
thousands of companies, reverify all provider accounts, or add discovery infrastructure.

Record actual report periods, filing dates, price dates and validation results.
Do not label retrieval time as financial period or change old facts to "current."
Do not invent a stricter freshness threshold solely to stop the proof; equally do not
ignore an existing hard validation failure.

Preserve the review's known limitations where they remain applicable:

- Normalized official facts were preserved; original provider-response bytes were
  not bundled. Preserve available provenance and label that limitation honestly.
- NVMI's reviewed financial context was 2025-FY, period end 2025-12-31, filing/as-of
  2026-02-17. Check current eligibility through existing rules; do not portray it
  as a 2026 quarterly result.
- Reviewed SKYH, WKSP and EROC financial rows had H1 period_type with 2026-Q2 labels.
  Do not treat an H1 cumulative amount as a standalone quarterly amount.
- Reviewed US price readiness was READY while technical context was PARTIAL_SAFE.
  Price readiness does not manufacture missing indicators or technical features.

Keep fundamental sufficiency, price/technical readiness and monitoring readiness
separate. Valid missing optional prices/technicals may use existing UNAVAILABLE_SAFE
or PARTIAL_SAFE paths. False future dates, wrong security basis and malformed prices
remain hard failures. Missing mandatory fundamental evidence blocks directional calls.

Use existing request limits and bounded retry behavior. Do not build a new quota
system. If a source actually fails or a known limit is reached, use existing handling
and report the affected data; do not retry indefinitely, rotate keys, pay for fallback,
or infer no change in the investment logic.

An ordinary US source failure does not abort safe, independent KR checks. A genuine
shared safety or live-workload problem may defer both.

## 6. Create the final executable source lock — not the review lock

The old review manifest has executable=false, execution_authorized=false, and
proof_source_lock=null. Do not flip its flags, overwrite it, or reuse a simulation
source-lock value as proof authorization.

Create a NEW final source generation and execution precommit after the source checks.
Reference the accepted selection-review hash and preserve all parent artifacts.

Record:

    ordered cohort and explicit markets
    canonical issuer/security identities
    current exclusion-registry identity
    final per-subject raw and canonical packet hashes
    actual reporting dates and mandatory Unknowns
    fundamental and price/technical readiness
    final source generation and final source lock
    tested implementation and architecture/validator identities
    model, effort, context groups and timeout
    runtime generation and source/runtime binding rules
    established hard gates, stopping rules and stability acceptance policy

Source revalidation may change input hashes before this final freeze. Report those
pre-freeze changes rather than falsely claiming zero drift from an older review.
After the freeze, no source refresh, issuer replacement, context regrouping or
investment-logic modification is permitted across FIRST/A/B/C.

## 7. Reuse the proven runtime identity, market and lifecycle path

Freeze existing configuration:

    model = gpt-5.6-sol
    reasoning_effort = xhigh
    batch_semantics = MODEL_CONTEXT_COUPLED
    subjects_per_shared_context = 4
    model_timeout_seconds = 1800
    authoritative_model_timeout_owner_count = 1

Do not add a new timeout owner, increase timeout, split contexts, change model/effort,
or add model retries.

Source-generation identity and runtime-generation identity are different.
Use the existing single binding to construct prompt identity, actual schema const,
validator expectations, receipt identity and manifest. Validate the actual on-disk
prompt and schema that the adapter will consume, not an unrelated in-memory copy.

Use existing model-free checks on the finalized invocation path for all planned
stages/batches and run IDs. Do not replay model-backed fictional canaries.
Do not replace schema const with a permissive string or accept old/new IDs together.

Use explicit packet market values for routing and manifest counts.
Ensure real mode cannot accidentally consume a canned fixture output or a
MODEL_FREE_SIMULATION receipt.

Price-Timing may depend on that run's actual Directional Core result. Preserve and
hash that dependency. Stochastic core outputs can therefore legitimately produce
different timing prompt bytes across repeated runs. Freeze the source, templates,
schema semantics and deterministic assembly rule, not an imaginary byte-identical
timing payload across different core outcomes. Runtime-ID substitutions likewise
must be tracked separately from unauthorized semantic input drift.

## 8. Protect current US/KR monitoring with the existing guard

Recheck live workload just before each heavy source or model context using the
existing supported guard. Historical "no contention" is not future spawn permission.

The reviewed observation backend was:
`LAUNCHCTL_JOB_STATE_PLUS_LSOF_CODEX_RUNTIME_STATE`.

Use the current canonical implementation and current protected-window policy.
Do not hardcode an old dated window. Do not replace an unavailable census with zero.
Do not disable the guard or use a health response as evidence of no contention.

Defer shadow work under existing safe pause behavior when natural monitoring needs
the resource. No cancellation, preemption, scheduler change or new background job.

For a guard failure before spawn:

    spawn_started = 0
    transport_receipt_expected = 0
    preserve the original guard error
    do not replace it with transport_receipt_missing

For actual post-spawn failures preserve the transport receipt and lifecycle evidence.
Do not fabricate receipts when no subprocess existed.

## 9. Bounded real proof protocol

Precommit expected model call/stage counts from the existing runner before FIRST.
The reviewed real-input simulation used 4 core + 4 timing contexts per complete run;
confirm actual canonical behavior, including unavailable-safe branches. Do not add
renderer model calls if rendering is deterministic.

FIRST means one complete cohort-level attempt, not one subprocess.
A/B/C are the already precommitted repetitions, not automatic retries.

Execute:

    FIRST → required run gates
    A     → required run gates
    B     → required run gates
    C     → required run gates
    final stability and bounded generalization review

Only start the next run after every required gate of the prior run passes.
Do not execute all runs first and adjudicate semantic failure afterward.
Stop on the first required execution, identity, preservation, ownership, renderer or
hard-safety failure. No selective context/ticker rerun or continued sample collection.
Do not tune a BUY/HOLD/SELL distribution.

## 10. Preserve each context immediately; audit before continuing

For each attempted context preserve available prompt/schema, binding, manifest and
pre-spawn guard evidence. After any model output, preserve it before validation can
discard it:

    exact raw output (normalized copy separate)
    stdout and safe raw/redacted stderr/logs
    transport receipt where expected
    prompt/schema exact bytes and hashes
    generation/run/stage/batch/issuer mapping
    parse, identity and semantic validation records
    artifact byte counts, SHA-256, secret scan and reopen result

Do not wait until the whole FIRST completes. Do not hide partial/raw output because
identity or schema validation rejected it. If preservation fails, stop before the
next context. Preserve original exceptions and report secondary preservation errors
separately.

After every successful core context run existing applicable schema, identity,
source-sufficiency and ownership validators before the next context.
After a timing or renderer output, run its existing applicable checks as soon as
inputs needed for those checks exist. Do not claim unexecuted-stage checks passed.

Hard ownership requirements remain:

    DIRECTIONAL_CORE_PRICE_TECHNICAL_REFS = 0
    DIRECTIONAL_CORE_SUPPLY_REFS = 0
    SUPPLY_DIRECTIONAL_CORE_USAGE = 0
    BUY_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
    SELL_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
    DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
    PRICE_ONLY_DIRECTIONAL_MODEL_CALLS = 0

    TIMING_STAGE_DIRECTION_MUTATION = 0
    TIMING_STAGE_BALANCE_MUTATION = 0
    TIMING_STAGE_HOLD_LEAN_MUTATION = 0
    PRICE_TIMING_NEW_BUYER_UPGRADE = 0
    PRICE_ONLY_HOLDER_REDUCE = 0
    PRICE_ONLY_DIRECTIONAL_OWNERSHIP_VIOLATIONS = 0

    FINAL_DIRECTION_OWNER = DIRECTIONAL_CORE
    PRIMARY_USER_ACTION_WORDING_OWNER = RENDERER
    AI_IMPERATIVE_PRIMARY_ACTION = 0
    KNOWN_HARD_SAFETY_REGRESSION = 0

Map existing repository field names without weakening their meanings.
No new broad phrase allowlist or ad hoc semantic checker is authorized.

Preserve numeric provenance, accounting attribution, ordinary/ADR/currency basis,
provisional-earnings rules, and Unknown handling. No annualization of one-quarter EPS
into PER, no reverse-engineered EPS/BVPS, no invented technicals or valuation inputs.

## 11. Per-run and final evaluation

Each of FIRST/A/B/C must record independently:

    execution_status
    identity_gate_status
    ownership_gate_status
    renderer_gate_status
    hard_safety_gate_status
    preservation_status
    checked_subject_count / eligible_subject_count
    measured_violation_counts

Zero initialized violations with zero subjects actually checked is NOT a PASS.
An unevaluated gate remains NOT_MEASURED. Keep schema-valid, identity-valid and
semantically checked row counts separate from raw output counts.

Use the pre-existing frozen stability categories and acceptance thresholds:
STABLE / BOUNDARY_UNCERTAINTY / UNSTABLE or canonical equivalents.
Precommit the actual evaluator identity and acceptance contract before real output.
Do not invent new thresholds after observing results, force STABLE, or exclude a
difficult issuer to obtain a good distribution.

Aggregate only validly completed runs. If required repetitions are incomplete, do
not claim the intended stability/generalization proof succeeded.
A PASS is evidence for this bounded cohort and protocol, not proof that every US/KR
listed security is operationally supported.

## 12. Exposure and failure handling

Record three separate dimensions using existing canonical states:

1. Output exposure: UNEXPOSED / PARTIALLY_EXPOSED / FULLY_EXPOSED.
2. Semantic revelation: NOT_MEASURED / NO_SEMANTIC_DEFECT_OBSERVED /
   REVEALED_FOR_ARCHITECTURE_TUNING.
3. Retirement: ACTIVE_UNEXPOSED / RETIRED_PARTIAL_EXPOSURE /
   RETIRED_AFTER_EVALUATION / RETIRED_FOR_ARCHITECTURE_REPAIR.

Also record unique issuers with actual investment output and stage-completion coverage.
Do not equate full issuer exposure with completion of every stage or repeated run.

Model-free fixtures are not real exposure. Actual investment output rejected by an
identity/schema gate is still real exposure; remember the prior NVDA/JPM/WMT/BRK-B
case. Track raw/unparseable output conservatively with evidence, never silently reset
the cohort to unseen.

Before any output, a pre-spawn/source failure leaves exposure unexposed but does not
authorize an automatic retry. A new explicit reuse decision is required later.
If spawn occurred but output observability is uncertain, record that uncertainty
instead of claiming definitely unexposed from a missing file alone.

After partial investment output and nonsemantic failure:
retire the whole precommitted 16-issuer cohort from future unseen proof, including
issuers in not-yet-run contexts. No remaining-subject continuation.

After a real semantic defect:
preserve evidence; mark architecture-repair retirement; do not repair against and
rerun this cohort. A later generic repair requires a newly eligible issuer holdout.

After a clean FIRST, A/B/C may continue because they were precommitted repetitions.
They do not restore unseen status. Full success retires the cohort after evaluation.
If later A/B/C fails, do not downgrade already-full exposure to partial.

No model retry, timeout increase or batch split. A recurrence of the historical
silent 1,800-second stall must be reported as a recurrence with exact lifecycle
evidence, not declared solved because the old fictional probe passed.

## 13. Completion and next scope

Full success requires actual complete FIRST/A/B/C outputs, all required per-context
and per-run checks, valid frozen stability adjudication, exact evidence preservation
and production isolation. Then:

    readiness = READY_FOR_MONITORING_BOOTSTRAP_INTEGRATION_REVIEW
    next_scope = Monitoring Bootstrap Integration Review

Do not implement bootstrap or activate new monitoring messages in this task.

On failure, separate the result of final freeze, execution, identity, semantic checks
and stability. Name the observed blocker and corresponding bounded next scope.
Do not route a successful proof back into a free-API gate project merely because
an entitlement inventory has not been built.

Optional missing technical data is not automatically a new source failure; use the
existing safe contract. Mandatory evidence failure remains blocking.

## 14. Production and scope boundary

No changes to:

    existing US/KR production monitoring behavior or message formats
    main merge, deployment, scheduler or scheduled jobs
    production DB, production caches or warning/thesis/assessment state
    Telegram production sends
    monitoring registration or stop-monitoring state
    live Structured Autonomy or V2 activation
    Night Futures code or company-packet injection

Do not change infrastructure under the label "message-only shadow."
All results remain in experiment artifacts for review.

No new paid data dependency or credential; no separate free-API gate implementation.
Record known actual changes and observations. Do not report total data spend = 0
or all routes VERIFIED_FREE without evidence. "No new paid dependency introduced"
is a narrower and appropriate claim.

## 15. Required result bundle — reuse existing reports, avoid a new framework

Use existing artifact conventions; the following may be consolidated rather than
creating many new services or mandatory report types:

    provenance and input-ZIP verification
    user-priority / no-new-paid-dependency scope note
    selected-cohort source recheck for US and KR
    final source generation, packet hashes and executable precommit
    implementation/template/validator freeze and actual-request preflight
    current guard/lifecycle records
    per-context raw artifacts, receipts and checks
    FIRST/A/B/C independent gate summaries
    exposure, retirement and complete model-call ledger
    stability and bounded ownership verdict
    production-no-change and final completion

Completion must distinguish at minimum:

    historical model-free evidence vs newly produced real output
    final_source_lock_created and final_source_lock
    selected_cohort and actual explicit market grouping
    implementation/tested/final commit identities
    actual model invocations, contexts, raw subject rows and unique exposed issuers
    identity-valid subjects and semantically checked subjects
    each run's execution and ownership/renderer/hard-safety statuses
    measured violations vs NOT_MEASURED
    source/template/architecture/transport drift
    retries, timeouts, orphan count, preservation failures
    exposure/revelation/retirement states
    production and provider/plan changes
    readiness, stop_reason and next_scope

Conclude only what was measured. Do not substitute model-free PASS for real proof.

Finalize completion/README before the artifact index. Include every payload except
the index's unavoidable self-reference, state that exclusion, and verify path,
SHA-256 and byte size from the final archive. Attach final ZIP SHA-256 separately.
Preserve prior artifacts; exclude secrets and clearly label redacted derivatives.

## 16. Final priority

Do not build an administrative gate instead of testing the investment engine.

The priority is now:

    existing data routes
    + already reviewed selection
    + current source/workload recheck
    + final freeze
    + actual FIRST/A/B/C ownership proof

Keep the free-data principle simple, keep the existing safety boundaries intact,
and complete the core evidence-gathering task before adding lower-priority machinery.
