# Thesis Monitor — New Issuer Holdout Selection & Ownership Proof

## 0. Task identity

Suggested work-instruction filename:

```text
20260907-new-issuer-holdout-selection-and-ownership-proof.md
```

Suggested result bundle:

```text
thesis-monitor-20260907-new-issuer-holdout-selection-ownership-proof-report.zip
```

This is a **new unseen issuer-holdout selection + frozen-architecture ownership-proof task**.

It follows:

1. successful runner↔adapter interface repair;
2. partial real-holdout exposure caused by a 1,800-second Directional Core batch03 stall;
3. exact recovery of the eight successful historical Directional Core outputs;
4. offline Directional Core ownership/schema/safety review of those eight outputs;
5. a bounded same-state-namespace fictional US4 → KR4 → KR4 sequential transport probe that passed 3/3 without timeout.

The current task is NOT:

- a runner↔adapter repair task;
- a transport redesign task;
- a timeout-tuning task;
- an investment architecture repair task;
- a replay of the old partially exposed holdout;
- a continuation of the old unexposed eight issuers;
- a monitoring-bootstrap task;
- a production-promotion task.

The intended sequence is:

```text
build complete prior-real-issuer exposure registry
→ deterministically select a completely new issuer cohort
→ build and freeze a new source generation
→ prove source sufficiency and experiment freeze
→ precommit exact context grouping
→ execute FIRST
→ preserve + offline-audit every successful context immediately
→ run FIRST semantic gates
→ if clean, A
→ per-run gates
→ if clean, B
→ per-run gates
→ if clean, C
→ per-run gates
→ stability/generalization proof
→ hand off to Monitoring Bootstrap Integration Review only on full success
```

No model result may be used to change the cohort, source packet, prompt, schema, architecture, timeout, or transport.

---

# 1. Source of truth — split by concern

Use concern-specific authority.

## 1.1 Latest forensic result

The authoritative latest historical result is:

```text
thesis-monitor-20260906-partial-output-forensics-bounded-transport-stall-review-report(1).zip
```

Verified ZIP SHA-256:

```text
c88368357cd4fbde183d90620194307f9e5498cfd75d2dcafad6b8d04ffbc3b4
```

Recompute this checksum at task start.

If it differs:

```text
STOP
LATEST_FORENSIC_RESULT_CHECKSUM_MISMATCH
```

Use that result as the source of truth for:

- recovered historical real outputs;
- partial-output semantic audit;
- historical batch03 forensic facts;
- fictional sequential probe results;
- retired-cohort state;
- real-issuer no-model-call proof;
- evidence-preservation policy;
- latest readiness/next-scope decision.

## 1.2 Current repository

The current repository HEAD/worktree is authoritative for the code/configuration that exists now.

Do not let historical reports overwrite current repository facts.

Do not let current code silently rewrite historical experiment facts.

## 1.3 Current task constraints

This work instruction is authoritative for:

- new issuer selection;
- exclusion registry;
- source lock;
- experiment freeze;
- exact per-context artifact preservation;
- per-context partial semantic audit;
- FIRST/A/B/C execution;
- per-run hard gates;
- holdout exposure/retirement behavior;
- next-scope decisions.

## 1.4 Investment / safety semantics

Use the current Investment Thesis Analysis & Monitoring Knowledge Guide and frozen repository contracts for:

```text
Directional Core ownership
Price-Timing ownership
renderer/action ownership
source sufficiency
numeric provenance
accounting attribution
ADR/security basis
official provisional earnings
price/supply separation
valuation safety
```

Do not reinterpret those semantics to improve proof outcomes.

---

# 2. Latest authoritative historical state

The latest forensic result established:

```text
status = PASS

runner_adapter_repair_status = CLOSED

partial_output_provenance_gap =
RESOLVED_BY_LOCAL_EXACT_RECOVERY

batch01_recovery_status =
EXACT_PROVENANCE_LINKED_OUTPUT_RECOVERED

batch02_recovery_status =
EXACT_PROVENANCE_LINKED_OUTPUT_RECOVERED

recovered_real_subject_output_count = 8

offline schema revalidation = PASS

partial_output_semantic_audit_status =
CLEAN_ON_RECOVERED_DIRECTIONAL_CORE

known_hard_safety_regression = 0

fictional_probe_status = PASS
fictional_probe_sequence_completed = 1
fictional_probe_timeout_count = 0
fictional_probe_model_call_count = 3
sequential_same_namespace_stall_reproduced = 0

batch03 historical root cause =
ROOT_CAUSE_UNRESOLVED

forensic final transport classification =
TRANSIENT_STALL_NOT_REPRODUCED

historical stall fixed claimed = 0

timeout increase in forensic task = 0
transport topology mutation = 0
model semantic input drift = 0
architecture semantic drift = 0

new_real_issuer_model_call_count = 0

readiness =
READY_FOR_NEW_ISSUER_HOLDOUT_SELECTION_AND_OWNERSHIP_PROOF

next_scope =
NEW_ISSUER_HOLDOUT_SELECTION_AND_OWNERSHIP_PROOF
```

The three fictional probe contexts passed under one new runtime state namespace:

```text
probe 01 = PASS
probe 02 = PASS
probe 03 = PASS

same_runtime_state_namespace_hash = 1
retry_count = 0
```

Observed fictional probe exit times were approximately:

```text
batch01 ≈ 121.29 sec
batch02 ≈ 118.04 sec
batch03 ≈ 112.87 sec
```

This establishes that the historical third-context stall did not reproduce under the bounded fictional probe.

It does NOT establish the historical stall's exact root cause.

Do not call the historical stall "fixed."

---

# 3. Historical partial real-output result

The recovered historical eight-subject Directional Core output audit established:

```text
directional_core_price_technical_refs = 0
directional_core_supply_refs = 0
supply_directional_core_usage = 0

buy_without_nonprice_material_anchor = 0
sell_without_nonprice_material_anchor = 0

directional_model_calls_on_source_insufficient = 0
price_only_directional_model_calls = 0

final_direction_owner = DIRECTIONAL_CORE
```

All eight recovered Directional Core rows passed the applicable offline ownership audit.

However:

```text
timing_stage_direction_mutation = NOT_MEASURED
timing_stage_balance_mutation = NOT_MEASURED
timing_stage_hold_lean_mutation = NOT_MEASURED

price_timing_new_buyer_upgrade = NOT_MEASURED
price_only_holder_reduce = NOT_MEASURED
price_only_directional_ownership_violations = NOT_MEASURED

primary_user_action_wording_owner = NOT_MEASURED
ai_imperative_primary_action = NOT_MEASURED
```

because the historical partial FIRST never reached Price-Timing/renderer completion.

Therefore:

```text
ownership_generalization_verdict = NOT_MEASURED
```

The recovered historical eight are useful supporting evidence only.

They are not the new unseen proof cohort.

Do not merge them into the sample counts of the new primary holdout proof.

---

# 4. Old cohorts are permanently excluded from new unseen proof

## 4.1 Retired partial holdout — exclude all 16

```text
ORCL
UNH
KO
AVGO
095570
058860
246960
099520
403870
014790
079810
060980
061970
012030
225190
245620
```

Historical state remains:

```text
holdout_output_exposure_state =
PARTIALLY_EXPOSED

holdout_retirement_state =
RETIRED_PARTIAL_EXPOSURE

future_unseen_holdout_reuse_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
```

Do not reuse even the eight issuers that did not return model output.

## 4.2 Consumed regression cohort — exclude all 16

```text
PLTR
V
MA
AMZN
XOM
DIS
NKE
MCD
033920
104480
071320
096240
032860
060570
016600
462520
```

## 4.3 Historical exposure registry must be broader than this hardcoded list

Before selecting the new cohort, construct a repository-local registry of **all real issuers that have previously been exposed to model-backed thesis/ownership experiments** if such evidence exists in receipts, generation metadata, prior result artifacts, or canonical experiment history.

At minimum scan only the relevant experiment/result history directories.

Do not search unrelated user data.

Build:

```text
prior_real_issuer_exposure_registry
```

with, where available:

```text
issuer
market
ticker
generation_id
invocation_id
experiment_class
model_call_started
usable_output_exists
exposure_class
source_artifact
```

Then define:

```text
NEW_HOLDOUT_EXCLUSION_SET =
known retired partial cohort
+ known consumed regression cohort
+ any additional real issuer proven previously model-exposed
```

A previously exposed real issuer may not re-enter the new unseen cohort merely because it was not listed in an earlier summary.

---

# 5. Repository provenance gate

Before implementation or selection work:

Record:

```text
git branch --show-current
git rev-parse HEAD
git status --short
```

The forensic result reported:

```text
branch =
codex/20260906-partial-output-forensics-bounded-transport-stall-review

base_sha =
4743d3e842ca71ec4f118b6e80b09ae3e6c72a25

work_instruction_commit =
8c11888227778a1bacec25a3b6a72a224a5c6f7c

implementation_commit =
34e6fbb501d6ff8656f195d3272c324c3c4883db

final_head_sha =
34e6fbb501d6ff8656f195d3272c324c3c4883db
```

Do not assume current HEAD remains exactly that SHA.

Classify any later changes.

If current repository contains unexplained semantic changes affecting:

```text
Directional Core
Price-Timing
renderer/action ownership
prompt/schema
source/evidence semantics
source sufficiency
model/context shape
ContinuationTransportAdapter
process lifecycle topology
timeout ownership
```

then:

```text
STOP
UNEXPLAINED_SEMANTIC_REPOSITORY_DRIFT
```

Do not reset, merge, cherry-pick, or silently discard unrelated work.

---

# 6. Work-instruction commit first

Commit this work instruction before any implementation or selection changes.

Record:

```text
new_work_instruction_commit
```

The work-instruction commit should contain only this task instruction apart from mechanically unavoidable metadata.

Only then may experiment-only selection/evidence tooling be changed.

---

# 7. Allowed mutation surface

Allowed only if needed:

```text
experiment-only new-holdout selection tooling
experiment-only historical exposure-registry tooling
experiment-only source-lock/report tooling
experiment-only per-context artifact preservation
experiment-only per-context partial semantic audit orchestration
experiment-only ownership-proof runner/report serialization
```

Strong preference:

```text
reuse existing frozen production semantics and validated experiment harness
```

Do not modify shared production semantics merely to make the proof easier.

If this task would require changing:

```text
ContinuationTransportAdapter canonical semantics
process lifecycle topology
investment model prompt semantics
DecisionEvidencePacket semantics
source-sufficiency semantics
Directional Core / Price-Timing rules
renderer ownership
```

then:

```text
STOP
SCOPE_EXCEEDED
```

and produce a separate required-next-task recommendation.

---

# 8. Forbidden mutations

Do not change:

```text
Directional Core semantics
Price-Timing semantics
deterministic composer semantics
renderer/action ownership semantics

BUY/HOLD/SELL thresholds
BUY:SELL balance rules
HOLD lean rules
new-buyer transition rules
holder transition rules
business invalidation rules

DecisionEvidencePacket semantics
production market enum
Directional Core prompt semantics
Price-Timing prompt semantics
model output schema

source assembly semantics
source sufficiency semantics
fundamental enrichment semantics
valuation safety
numeric provenance
accounting attribution
ADR/security basis
official provisional earnings logic
issuer identity rules

ContinuationTransportAdapter canonical API
instrumented Codex transport topology
process-group lifecycle
timeout ownership

model
reasoning effort
production scheduler
production DB
production Telegram
monitoring registration
Night Futures
```

Production market enum remains:

```text
kr | us
```

Do not introduce:

```text
market = synthetic
```

---

# 9. Model/runtime freeze

The primary proof remains frozen to:

```text
model = gpt-5.6-sol
reasoning_effort = xhigh

batch_semantics =
MODEL_CONTEXT_COUPLED

shared-context subject count =
4

configured model timeout =
1800 seconds

model_timeout_owner_count =
1
```

Do not:

```text
increase timeout
add another timeout owner
split a four-subject shared context
change model
reduce/increase reasoning effort
add automatic retry
```

The historical 1,800-second stall is unresolved but was not reproduced in the bounded fictional sequence.

The correct response is not to raise timeout preemptively.

---

# 10. Historical transport evidence reuse

The latest forensic task already completed:

```text
fictional US4 → KR4 → KR4
same new state namespace
three sequential invocations
3 / 3 PASS
timeout = 0
retry = 0
```

Do not rerun that fictional probe merely to consume more model calls if:

```text
transport topology unchanged
adapter unchanged
model/context construction unchanged
timeout ownership unchanged
```

Instead, reuse the forensic result as historical transport evidence.

If the current task requires a change to those shared semantics:

```text
STOP
BROADER_TRANSPORT_REVALIDATION_REQUIRED
```

Do not quietly rerun the proof under a changed transport implementation.

---

# 11. Phase A — Build prior-real-issuer exposure registry

Before any new real issuer selection:

1. ingest the two known exclusion cohorts;
2. inspect relevant historical model-backed experiment receipts/result artifacts;
3. identify any additional real issuers with prior model exposure;
4. deduplicate by canonical issuer identity.

Required machine-readable output:

```text
prior-real-issuer-exposure-registry.json
```

Include:

```text
registry_count
known_retired_partial_count
known_consumed_regression_count
additional_historical_exposure_count
deduplicated_exclusion_count
```

No model call is allowed in this phase.

---

# 12. Phase B — Precommit selection policy before choosing issuer names

Before producing the final new cohort, write and hash a selection-policy artifact.

Required:

```text
new-holdout-selection-policy.json
```

The policy must define:

```text
target cohort size
target market mix
candidate universe
exclusion registry
eligibility criteria
source-sufficiency criteria
deterministic ordering rule
reserve/replacement rule
diversity rule if supported by canonical metadata
finalization rule
```

The policy must be committed/hashes preserved before any model call.

## 12.1 Target cohort shape

Preserve architecture-scale comparability with the prior proof:

```text
total issuer count = 16
US issuer count = 4
KR issuer count = 12
shared Directional Core context size = 4
```

Expected Directional Core market grouping:

```text
US4
KR4
KR4
KR4
```

unless the existing frozen runner has a different canonical grouping rule.

If so, use the existing canonical rule and document it.

Do not create mixed-market groups merely to alter transport behavior.

## 12.2 Deterministic selection

Selection must not depend on:

```text
model output
desired BUY/HOLD/SELL direction
price trend
desired valuation result
desired ownership result
desired stability result
```

Use a deterministic ordering/ranking rule declared before model execution.

Acceptable approaches include:

```text
stable hash order with a predeclared seed
canonical sorted universe + deterministic index rule
existing repository holdout selector if it is outcome-independent
```

Record the exact selection seed/rule.

## 12.3 Diversity

If canonical sector/industry metadata is already available and reliable, use a simple predeclared diversity constraint to avoid pathological clustering.

For example:

```text
avoid excessive same-sector concentration
```

Do not invent sectors from free-form text.

If canonical diversity metadata is unavailable:

```text
diversity constraint = NOT_APPLIED_METADATA_UNAVAILABLE
```

Do not block the task solely to create a new sector taxonomy.

---

# 13. Candidate reserve policy

Precommit a reserve list before final source-lock completion.

Purpose:

```text
replace candidates that fail objective pre-model eligibility/source-sufficiency gates
without cherry-picking after model exposure
```

The reserve list must be ordered deterministically.

Replacement is allowed only before:

```text
first new-holdout model invocation
```

and only for objective conditions such as:

```text
identity validation failure
unsupported market
source insufficiency
hard source validation failure
duplicate issuer
missing required evidence packet
```

Replacement is NOT allowed for:

```text
valuation appearance
price state
expected direction
theme
model behavior
transport outcome
```

Once the final 16 are source-locked:

```text
replacement_allowed = 0
```

---

# 14. Phase C — Build new source generation

Use the frozen source pipeline.

Create a completely new source generation for the final selected cohort.

Do not reuse the historical retired cohort source lock:

```text
efe64d0942afa00c3b40131afc4de5fa411babc0680271d1571aaac69816050d
```

Required new artifacts include:

```text
new source generation ID
ordered issuer manifest
per-issuer evidence packet hashes
source-sufficiency audit
source identity audit
new aggregate source lock
```

No model call is allowed while building the source generation.

---

# 15. Source-sufficiency gate before final holdout lock

Before finalizing the 16-subject cohort:

- run frozen source-sufficiency validation;
- run identity validation;
- run hard evidence/schema validation.

Do not call Directional Core on a source-insufficient subject.

Required invariant:

```text
DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
```

Use the precommitted reserve list if a selected candidate fails before model exposure.

Once 16 eligible issuers are finalized:

```text
final_holdout_cohort_frozen = 1
reserve_replacement_after_freeze = 0
```

---

# 16. Final holdout precommit artifact

Before any real model call, produce and hash:

```text
new-holdout-precommit.json
```

It must include:

```text
ordered 16 issuers
canonical issuer identities
market
context grouping
selection policy hash
exclusion registry hash
source generation ID
source lock
per-issuer packet hashes
prompt hashes or deterministic prompt-generation identity
schema identities
model
reasoning effort
timeout
timeout owner count
batch semantics
context size
transport topology identity
per-context evidence-preservation policy identity
per-context partial semantic audit policy identity
FIRST/A/B/C stop rules
```

Once this artifact is written:

```text
cohort mutation = 0
source mutation = 0
context grouping mutation = 0
```

---

# 17. New holdout reuse/exposure gate

Immediately before FIRST, prove:

```text
all 16 issuers absent from prior-real-issuer exposure registry
all 16 issuers absent from retired partial cohort
all 16 issuers absent from consumed regression cohort

new source lock finalized
source sufficiency PASS
identity validation PASS

architecture semantic drift = 0
prompt semantic drift = 0
schema semantic drift = 0
model semantic input drift = 0
transport topology mutation = 0

model = gpt-5.6-sol
reasoning_effort = xhigh
batch semantics = MODEL_CONTEXT_COUPLED
context size = 4
timeout = 1800
timeout owner count = 1
```

Initial new cohort state:

```text
holdout_output_exposure_state =
UNEXPOSED

holdout_semantic_revelation_state =
NOT_MEASURED

holdout_retirement_state =
ACTIVE_UNEXPOSED

future_unseen_holdout_reuse_allowed =
1
```

If any gate fails:

```text
STOP
```

No real model call.

---

# 18. Mandatory per-context evidence preservation

This is a hard experiment-integrity requirement.

After **every successful model context**, before launching the next context:

```text
persist exact raw model output
persist stdout
persist stderr/log or safe redacted derivative
persist transport receipt
persist exact prompt
persist exact schema
persist generation/invocation/stage/batch/subject mapping
compute SHA-256 and byte size
run secret scan
verify artifact reopen/read
```

Do not wait for the whole FIRST/A/B/C run to finish.

Required status per successful context:

```text
CONTEXT_EVIDENCE_PRESERVATION = PASS
```

If exact output preservation fails:

```text
STOP
```

Do not launch the next real context.

A successful context whose output is not preserved must still count toward holdout exposure.

Do not hide it by resetting the cohort.

---

# 19. Mandatory per-context partial semantic audit

After preserving each successful real context, run all semantic checks that are already evaluable offline at that point.

This is an experiment-only early-stop mechanism.

It must not change investment semantics.

## 19.1 Directional Core context checks

For each completed Directional Core shared context, immediately evaluate:

```text
schema validity

DIRECTIONAL_CORE_PRICE_TECHNICAL_REFS = 0
DIRECTIONAL_CORE_SUPPLY_REFS = 0
SUPPLY_DIRECTIONAL_CORE_USAGE = 0

BUY_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
SELL_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0

DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
PRICE_ONLY_DIRECTIONAL_MODEL_CALLS = 0

FINAL_DIRECTION_OWNER = DIRECTIONAL_CORE
```

Required status:

```text
PER_CONTEXT_PARTIAL_SEMANTIC_AUDIT =
PASS / FAIL / NOT_MEASURED
```

If a hard Directional Core semantic defect is confirmed:

```text
STOP
```

Do not expose additional real issuers/contexts merely to finish FIRST.

Set:

```text
holdout_semantic_revelation_state =
REVEALED_FOR_ARCHITECTURE_TUNING

holdout_retirement_state =
RETIRED_FOR_ARCHITECTURE_REPAIR

future_unseen_holdout_reuse_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
```

## 19.2 Do not run premature Price-Timing/renderer gates

Before Price-Timing/renderer output exists:

```text
timing/renderer-only gates = NOT_MEASURED
```

Do not convert stage non-execution into PASS.

---

# 20. Exposure state updates during FIRST

Exposure state must update from actual usable output.

## 20.1 No real output yet

```text
holdout_output_exposure_state = UNEXPOSED
```

## 20.2 Some but not all FIRST intended issuer output exists

```text
holdout_output_exposure_state = PARTIALLY_EXPOSED
future_unseen_holdout_reuse_allowed = 0
```

## 20.3 Complete intended FIRST issuer output exists

```text
holdout_output_exposure_state = FULLY_EXPOSED
future_unseen_holdout_reuse_allowed = 0
```

Do not wait for A/B/C to mark the cohort exposed.

---

# 21. Transport failure during FIRST

No automatic retry.

No selective continuation.

No batch splitting.

No timeout increase.

No same-task model-call repair.

## 21.1 Failure before any usable real output

If the first real context fails before producing usable output:

```text
STOP

holdout_output_exposure_state =
UNEXPOSED

holdout_semantic_revelation_state =
NOT_MEASURED

holdout_retirement_state =
ACTIVE_UNEXPOSED
```

Do not automatically reuse the cohort in the same task.

A future task must explicitly decide reuse after transport review.

## 21.2 Failure after any usable partial output

If a later FIRST context fails after one or more successful preserved contexts:

```text
STOP

holdout_output_exposure_state =
PARTIALLY_EXPOSED

holdout_semantic_revelation_state =
NOT_MEASURED
```

unless a semantic defect was already confirmed.

For nonsemantic partial execution failure:

```text
holdout_retirement_state =
RETIRED_PARTIAL_EXPOSURE

future_unseen_holdout_reuse_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
```

Do not:

```text
rerun the whole cohort
resume only remaining issuers
rerun failed context
increase timeout
```

Next scope becomes a bounded transport/runtime review before selecting another real holdout.

---

# 22. Historical stall recurrence handling

The prior historical third-context stall was:

```text
ROOT_CAUSE_UNRESOLVED
TRANSIENT_STALL_NOT_REPRODUCED under fictional probe
```

If the new real proof produces another materially similar 1,800-second silent stall:

```text
HISTORICAL_STALL_PATTERN_RECURRED = 1
```

Preserve exact logs/receipts immediately.

Do not retry.

Do not classify it as transient merely because the fictional probe previously passed.

Next scope:

```text
BOUNDED_TRANSPORT_RUNTIME_DIAGNOSTIC_REPAIR
```

before any additional new real holdout.

---

# 23. FIRST execution

Use the exact frozen precommit.

Execute:

```text
FIRST
```

once as one complete cohort-level attempt.

This does NOT mean one model invocation.

Report separately:

```text
first_complete_run_attempt_count
real_holdout_model_invocation_count
real_holdout_directional_context_count
real_holdout_price_timing_context_count
real_holdout_renderer_context_count
real_holdout_subject_output_count
```

Use the frozen runner's actual stage/context architecture.

Do not invent expected Price-Timing invocation counts.

---

# 24. FIRST run-level hard gates

After FIRST fully completes, and before A:

Run the complete FIRST-applicable:

```text
ownership gates
renderer/action ownership gates
hard-safety gates
```

Required hard invariants include:

```text
DIRECTIONAL_CORE_PRICE_TECHNICAL_REFS = 0
DIRECTIONAL_CORE_SUPPLY_REFS = 0
SUPPLY_DIRECTIONAL_CORE_USAGE = 0

BUY_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
SELL_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0

TIMING_STAGE_DIRECTION_MUTATION = 0
TIMING_STAGE_BALANCE_MUTATION = 0
TIMING_STAGE_HOLD_LEAN_MUTATION = 0

PRICE_TIMING_NEW_BUYER_UPGRADE = 0
PRICE_ONLY_HOLDER_REDUCE = 0
PRICE_ONLY_DIRECTIONAL_OWNERSHIP_VIOLATIONS = 0

DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
PRICE_ONLY_DIRECTIONAL_MODEL_CALLS = 0

FINAL_DIRECTION_OWNER = DIRECTIONAL_CORE

PRIMARY_USER_ACTION_WORDING_OWNER = RENDERER
AI_IMPERATIVE_PRIMARY_ACTION = 0

KNOWN_HARD_SAFETY_REGRESSION = 0
```

Required statuses:

```text
first_ownership_gate_status
first_renderer_gate_status
first_hard_safety_gate_status
```

All must be:

```text
PASS
```

before A.

If a semantic gate fails:

```text
STOP

holdout_output_exposure_state =
FULLY_EXPOSED

holdout_semantic_revelation_state =
REVEALED_FOR_ARCHITECTURE_TUNING

holdout_retirement_state =
RETIRED_FOR_ARCHITECTURE_REPAIR

future_unseen_holdout_reuse_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
```

No same-cohort hotfix.

---

# 25. Clean FIRST state

If FIRST completes and all FIRST run-level gates pass:

```text
holdout_output_exposure_state =
FULLY_EXPOSED

holdout_semantic_revelation_state =
NO_SEMANTIC_DEFECT_OBSERVED

holdout_retirement_state =
RETIRED_AFTER_EVALUATION

future_unseen_holdout_reuse_allowed =
0
```

The cohort is now permanently consumed as unseen evidence.

A/B/C may still run on the same cohort only because they are precommitted repeated-run stability proof, not because the cohort remains unseen.

---

# 26. A / B / C repeated proof

After clean FIRST:

```text
run A
```

After A completes:

```text
run A ownership gate
run A renderer gate
run A hard-safety gate
```

Only if all PASS:

```text
run B
```

After B completes:

```text
run B ownership gate
run B renderer gate
run B hard-safety gate
```

Only if all PASS:

```text
run C
```

After C completes:

```text
run C ownership gate
run C renderer gate
run C hard-safety gate
```

Required statuses:

```text
run_a_ownership_gate_status
run_a_renderer_gate_status
run_a_hard_safety_gate_status

run_b_ownership_gate_status
run_b_renderer_gate_status
run_b_hard_safety_gate_status

run_c_ownership_gate_status
run_c_renderer_gate_status
run_c_hard_safety_gate_status
```

No next run starts unless all gates of the preceding run PASS.

---

# 27. Per-context preservation/audit applies to A/B/C too

The same requirements from FIRST remain active in A/B/C:

After every successful context:

```text
preserve exact output immediately
preserve receipt/logs immediately
hash artifacts immediately
run applicable context-level semantic audit immediately
```

If a hard semantic defect appears:

```text
STOP
```

Do not continue to later contexts or later repeated runs.

If a nonsemantic transport failure occurs during A/B/C:

```text
STOP
```

The cohort remains fully exposed and retired.

Report:

```text
ownership_proof_completion_state =
INCOMPLETE_TRANSPORT_FAILURE
```

Do not retry the failed context in the same task.

---

# 28. Frozen inputs across FIRST/A/B/C

Across all repeated runs preserve:

```text
same ordered issuer cohort
same source generation
same source lock
same evidence packets
same prompt semantics
same schemas
same model
same reasoning effort
same investment architecture
same context grouping
same batch size
same timeout
same transport topology
same source-sufficiency semantics
```

Required:

```text
first_abc_source_drift = 0
model_semantic_input_drift = 0
architecture_semantic_drift = 0
prompt_semantic_drift = 0
schema_semantic_drift = 0
transport_topology_mutation = 0
```

Do not refresh source packets between FIRST/A/B/C.

---

# 29. Stability aggregation

Only runs that were legitimately allowed to complete and passed their own hard gates may enter stability aggregation.

Do not include:

```text
failed run
partially completed run
run after a prior semantic gate should have stopped execution
```

If insufficient valid repeated runs exist:

```text
stability = NOT_MEASURED
```

Do not infer stability from incomplete samples.

---

# 30. Directional Core stability

Use the existing frozen audit semantics.

Classify each subject using repository canonical categories such as:

```text
STABLE
BOUNDARY_UNCERTAINTY
UNSTABLE
```

Do not redefine thresholds after seeing outputs.

Report:

```text
subject-level repeated directions
balance/lean variation
classification
reason
```

Boundary uncertainty is evidence.

Do not force all subjects to STABLE.

---

# 31. Price-Timing stability

Likewise audit frozen Price-Timing output across valid repeated runs.

Use canonical categories such as:

```text
STABLE
BOUNDARY_UNCERTAINTY
UNSTABLE
```

Do not change technical calculations to improve stability.

Unavailable technical indicators remain unavailable.

---

# 32. Ownership semantics

Directional Core owns:

```text
BUY / HOLD / SELL
BUY:SELL
HOLD lean
fundamental new-buyer stance
fundamental holder stance
business invalidation
```

Price-Timing owns:

```text
technical state
timing modifier
entry mode
price review
holder price-review pressure
```

Price-Timing may only make new-buyer stance more conservative:

```text
ATTRACTIVE → WAIT / AVOID
WAIT → AVOID
AVOID → AVOID
```

It may not upgrade.

Required:

```text
PRICE_TIMING_NEW_BUYER_UPGRADE = 0
```

Price-Timing may:

```text
HOLDABLE → REVIEW
```

It may not create:

```text
REDUCE
```

Required:

```text
PRICE_ONLY_HOLDER_REDUCE = 0
```

Technical breakdown is not business invalidation.

Supply/positioning is not Directional Core evidence.

---

# 33. Renderer/action ownership

Hard gate on every complete run:

```text
PRIMARY_USER_ACTION_WORDING_OWNER = RENDERER
AI_IMPERATIVE_PRIMARY_ACTION = 0
```

Also report:

```text
structured_action_consistency
renderer_ownership_violations
```

Do not weaken actionability validators to pass the holdout.

AI reasoning owns substantive explanation.

Structured state + renderer own primary action wording.

---

# 34. Hard investment safety

Run established hard-safety validation on every complete run and all context-applicable outputs.

Preserve:

```text
evidence identity fencing
numeric provenance
accounting attribution
ADR/security basis
official provisional earnings semantics
future checkpoint logic
source sufficiency
issuer dedup
structured contradiction detection
renderer ownership
```

Do not permit:

```text
one-quarter EPS annualization into PER
EPS/BVPS reverse engineering from provider multiples
ADR/ordinary-share basis mixing
total-net-income/common-income substitution
fabricated balance-sheet/FCF facts from provisional earnings
RSI/MACD as valuation
price/supply as fundamental thesis delta
invented support/resistance/target/stop
```

Do not weaken validators to obtain PASS.

---

# 35. Semantic failure handling

If any real output confirms a hard architecture/ownership/renderer/hard-safety defect:

```text
holdout_semantic_revelation_state =
REVEALED_FOR_ARCHITECTURE_TUNING

holdout_retirement_state =
RETIRED_FOR_ARCHITECTURE_REPAIR

future_unseen_holdout_reuse_allowed = 0
same_cohort_architecture_tuning_rerun_allowed = 0
```

Then:

```text
STOP
```

No architecture hotfix in this task.

Do not rerun the same cohort after repair.

Recommended next lifecycle:

```text
GENERIC_ARCHITECTURE_REPAIR
→ new issuer-level holdout
→ fresh ownership proof
```

---

# 36. Nonsemantic A/B/C transport failure handling

If FIRST passed completely but A/B/C later has a nonsemantic transport/process failure:

```text
STOP
```

Do not alter the already retired/exposed state.

Use:

```text
holdout_output_exposure_state =
FULLY_EXPOSED

holdout_semantic_revelation_state =
NO_SEMANTIC_DEFECT_OBSERVED
```

unless a semantic defect had already been observed.

Report:

```text
ownership_proof_completion_state =
INCOMPLETE_TRANSPORT_FAILURE
```

Do not call ownership generalization PASS.

Next scope must address the transport/runtime recurrence before another new real holdout.

---

# 37. No timeout increase

Required:

```text
timeout_increase_this_task = 0
```

The historical real stall:

```text
1800 seconds
```

and the later fictional 3/3 PASS result do not justify a timeout policy change here.

A timeout-policy change requires a separate evidence-based task.

---

# 38. Production and Night Futures no-change

Required:

```text
main_merge = 0
production_db_mutation = 0
production_telegram_send = 0
production_scheduler_change = 0
monitoring_registration_calls = 0
live_structured_autonomy_activation = 0
live_v2_change = 0

night_futures_code_mutation = 0
night_futures_decision_packet_injection = 0
```

This is a shadow/experiment proof.

Do not activate production behavior.

---

# 39. Monitoring Bootstrap remains out of scope

Do not begin Monitoring Bootstrap implementation in this task.

The desired later lifecycle remains:

```text
Initial Analysis
→ explicit monitoring request
→ stored investment-logic baseline
→ bootstrap fundamental enrichment
→ source-sufficiency check
→ monitoring-ready baseline
→ Daily Delta afterward
```

Important:

```text
bootstrap enrichment != Daily Delta
```

Only after the entire new ownership proof succeeds may:

```text
next_scope =
Monitoring Bootstrap Integration Review
```

---

# 40. Required machine-readable artifacts — selection/freeze

Produce at least:

```text
01-repository-provenance
02-latest-forensic-result-integrity
03-prior-real-issuer-exposure-registry
04-new-holdout-exclusion-set
05-new-holdout-selection-policy
06-new-holdout-candidate-reserve-list
07-new-holdout-selection-result
08-new-source-generation
09-source-sufficiency-audit
10-source-identity-audit
11-new-source-lock
12-new-holdout-precommit
13-architecture-semantic-freeze
14-prompt-schema-freeze
15-model-context-freeze
16-transport-topology-freeze
17-holdout-unseen-reuse-gate
18-live-workload-coexistence-audit
```

Exact names may follow repository conventions.

---

# 41. Required machine-readable artifacts — execution

Produce:

```text
19-first-execution-summary
20-first-context-artifact-manifest
21-first-context-partial-semantic-audits
22-first-run-ownership-gate
23-first-run-renderer-gate
24-first-run-hard-safety-gate

25-run-a-execution-summary
26-run-a-context-artifact-manifest
27-run-a-context-partial-semantic-audits
28-run-a-ownership-gate
29-run-a-renderer-gate
30-run-a-hard-safety-gate

31-run-b-execution-summary
32-run-b-context-artifact-manifest
33-run-b-context-partial-semantic-audits
34-run-b-ownership-gate
35-run-b-renderer-gate
36-run-b-hard-safety-gate

37-run-c-execution-summary
38-run-c-context-artifact-manifest
39-run-c-context-partial-semantic-audits
40-run-c-ownership-gate
41-run-c-renderer-gate
42-run-c-hard-safety-gate
```

If execution stops early, later artifacts may be explicit:

```text
NOT_RUN
```

but do not fabricate execution.

---

# 42. Required machine-readable artifacts — final proof

Produce:

```text
43-holdout-exposure-retirement-state
44-core-stability
45-timing-stability
46-ownership-generalization
47-renderer-ownership-proof
48-hard-safety-regression
49-production-no-change
50-night-futures-no-change
51-monitoring-bootstrap-next-handoff
52-program-completion
```

---

# 43. Required raw/context artifact layout

For every successful model context create a directory such as:

```text
model-contexts/
  FIRST/
    DIRECTIONAL_CORE/
      batch-01/
        output.raw.json
        stdout.raw.log
        stderr.raw.log
        transport_log.raw.log
        transport_receipt.json
        prompt.txt
        schema.json
        context_manifest.json
        partial_semantic_audit.json
```

Use equivalent paths for all stages/runs.

Exact raw output must never be replaced by normalized output.

If a normalized derivative is useful:

```text
output.normalized.json
```

store it separately.

---

# 44. Per-context artifact manifest fields

For each context report:

```text
generation_id
run_id
invocation_id
stage
batch_id

subjects
subject_count
market_mix

source_lock
per-subject packet hashes

model
reasoning_effort
timeout
timeout_owner_count

runtime_state_namespace_hash
sequence_position

prompt_sha256
schema_sha256
input_bytes

status
elapsed_to_exit_seconds
first_stderr_seconds
last_stderr_seconds
stderr_bytes
first_stdout_seconds
stdout_bytes
output_bytes
exit_code

output_file_created
output_parsed

termination_initiator
child_cleanup_status
orphan_model_process_count

raw_output_path
raw_output_sha256
raw_output_bytes

receipt_path
receipt_sha256

secret_scan_status
context_evidence_preservation_status
per_context_partial_semantic_audit_status
```

Anything unavailable must remain:

```text
NOT_MEASURED
```

or canonical equivalent.

---

# 45. Program-completion object

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
final_head_sha
branch

latest_forensic_zip_sha256
latest_forensic_bundle_integrity

prior_real_issuer_exposure_registry_count
new_holdout_exclusion_count

new_holdout_selection_policy_hash
new_holdout_selection_seed_or_rule
new_holdout_reserve_count

new_holdout_cohort
new_holdout_us_count
new_holdout_kr_count

new_source_generation_id
new_source_lock

source_sufficiency_status
source_identity_status

model
reasoning_effort
model_timeout_seconds
model_timeout_owner_count
batch_semantics
shared_context_subject_count

architecture_semantic_drift
prompt_semantic_drift
schema_semantic_drift
model_semantic_input_drift
transport_topology_mutation
timeout_increase_this_task

holdout_output_exposure_state
holdout_semantic_revelation_state
holdout_retirement_state
future_unseen_holdout_reuse_allowed
same_cohort_architecture_tuning_rerun_allowed

first_complete_run_attempt_count

real_holdout_model_invocation_count
real_holdout_directional_context_count
real_holdout_price_timing_context_count
real_holdout_renderer_context_count
real_holdout_subject_output_count

context_evidence_preservation_failure_count
per_context_semantic_failure_count
transport_timeout_count
transport_retry_count
historical_stall_pattern_recurred

run_results.first
run_results.a
run_results.b
run_results.c

first_ownership_gate_status
first_renderer_gate_status
first_hard_safety_gate_status

run_a_ownership_gate_status
run_a_renderer_gate_status
run_a_hard_safety_gate_status

run_b_ownership_gate_status
run_b_renderer_gate_status
run_b_hard_safety_gate_status

run_c_ownership_gate_status
run_c_renderer_gate_status
run_c_hard_safety_gate_status

directional_core_price_technical_refs
directional_core_supply_refs
supply_directional_core_usage
buy_without_nonprice_material_anchor
sell_without_nonprice_material_anchor

timing_stage_direction_mutation
timing_stage_balance_mutation
timing_stage_hold_lean_mutation

price_timing_new_buyer_upgrade
price_only_holder_reduce
price_only_directional_ownership_violations

directional_model_calls_on_source_insufficient
price_only_directional_model_calls
final_direction_owner

primary_user_action_wording_owner
ai_imperative_primary_action
renderer_ownership_violations

known_hard_safety_regression

core_stability_counts
timing_stability_counts
ownership_generalization_verdict
ownership_proof_completion_state

main_merge
production_db_mutation
production_scheduler_change
production_telegram_send
monitoring_registration_calls
live_structured_autonomy_activation
live_v2_change
night_futures_code_mutation

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count

readiness
stop_reason
next_scope
```

Do not turn missing measurements into PASS.

---

# 46. Artifact integrity

Create a final artifact index containing for every result artifact:

```text
relative path
SHA-256
byte size
artifact class
run
stage
context/batch
historical vs newly generated
secret-scan status
```

Verify the bundle before completion.

Required:

```text
artifact_hash_mismatch_count = 0
artifact_size_mismatch_count = 0
```

Report final result ZIP SHA-256.

---

# 47. Full-success condition

The ownership proof succeeds only if:

```text
new issuer cohort is genuinely unseen
selection was deterministic/outcome-independent
new source lock frozen before model execution

all required FIRST contexts complete
all FIRST gates PASS

all required A contexts complete
all A gates PASS

all required B contexts complete
all B gates PASS

all required C contexts complete
all C gates PASS

all successful context artifacts preserved exactly

ownership hard invariants PASS
renderer/action ownership PASS
hard-safety PASS

valid repeated-run stability analysis completed

transport retry count = 0
timeout increase = 0
production mutation = 0
```

Then:

```text
ownership_generalization_verdict =
PASS
```

or the repository's canonical equivalent.

And:

```text
holdout_output_exposure_state =
FULLY_EXPOSED

holdout_semantic_revelation_state =
NO_SEMANTIC_DEFECT_OBSERVED

holdout_retirement_state =
RETIRED_AFTER_EVALUATION

future_unseen_holdout_reuse_allowed =
0

readiness =
READY_FOR_MONITORING_BOOTSTRAP_INTEGRATION_REVIEW

next_scope =
Monitoring Bootstrap Integration Review
```

Do not start Monitoring Bootstrap in this task.

---

# 48. Failure decision matrix

## Case A — selection/source freeze failure before model call

```text
holdout_output_exposure_state = UNEXPOSED
ownership_generalization_verdict = NOT_MEASURED
```

Stop and fix selection/source issue in a separate bounded task.

## Case B — first context stalls before any output

```text
holdout_output_exposure_state = UNEXPOSED
holdout_retirement_state = ACTIVE_UNEXPOSED
```

Stop.

No same-task retry.

Next scope:

```text
BOUNDED_TRANSPORT_RUNTIME_REVIEW
```

A future explicit reuse decision is required.

## Case C — FIRST stalls after partial output

```text
holdout_output_exposure_state = PARTIALLY_EXPOSED
holdout_retirement_state = RETIRED_PARTIAL_EXPOSURE
future_unseen_holdout_reuse_allowed = 0
```

Stop.

Do not selectively continue.

Next scope:

```text
BOUNDED_TRANSPORT_RUNTIME_DIAGNOSTIC_REPAIR
```

## Case D — context/run semantic defect

```text
holdout_semantic_revelation_state =
REVEALED_FOR_ARCHITECTURE_TUNING

holdout_retirement_state =
RETIRED_FOR_ARCHITECTURE_REPAIR
```

Stop.

Next scope:

```text
GENERIC_ARCHITECTURE_REPAIR
```

then a completely new holdout.

## Case E — FIRST passes but A/B/C transport fails

Cohort remains:

```text
FULLY_EXPOSED
```

and retired.

Use:

```text
ownership_proof_completion_state =
INCOMPLETE_TRANSPORT_FAILURE
```

Do not claim generalization PASS.

Next scope addresses transport recurrence.

## Case F — full success

Proceed only to:

```text
Monitoring Bootstrap Integration Review
```

in a new authorized task.

---

# 49. Historical partial evidence treatment

The recovered eight historical outputs remain archived supporting evidence.

They may be summarized separately as:

```text
historical_partial_directional_core_evidence =
CLEAN_ON_RECOVERED_DIRECTIONAL_CORE
```

But do not:

```text
add them to new holdout N
use them to fill missing new-holdout subjects
use them to improve stability counts
use them to replace a failed new context
```

The new holdout proof must stand on its own.

---

# 50. Final task principle

The previous harness defect is closed.

The previous partial-output provenance gap is closed.

The historical third-context stall remains causally unresolved but did not reproduce under a bounded fictional same-namespace sequence.

The old real cohort is permanently retired.

Therefore the next valid proof is:

```text
new issuer cohort
+ new source lock
+ frozen architecture
+ exact per-context evidence preservation
+ per-context early semantic audit
+ FIRST/A/B/C per-run hard gates
```

Not:

```text
old cohort retry
remaining-eight continuation
timeout increase
transport redesign without new evidence
```

And not:

```text
select new issuers after seeing model output
```

Precommit first.

Preserve every successful context immediately.

Stop at the first real hard failure.

Measure; do not tune.
