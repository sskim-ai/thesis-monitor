# Thesis Monitor — WebSocket Timeout Runtime Review & FIRST/A Evidence Closeout

## 0. Authorized task

This is a bounded transport/runtime failure review, reporting-contract repair, and offline closeout of two completed real-output runs.

It is NOT a new real-issuer proof, a B continuation, an architecture tuning task, a model/provider change, a monitoring-bootstrap implementation, or a production release.

Execute:

    verify the latest bundle and current repository
    → preserve the completed monitoring pause
    → reconstruct the failed B context from existing evidence
    → distinguish wrapper retry from observed CLI-internal retry
    → reconcile report/state/quality accounting
    → audit FIRST/A outputs and their differences offline
    → produce a concrete evidence-based next-scope recommendation

This task authorizes ZERO new investment-model calls, real or fictional.
No new issuer selection, source refresh, source lock, FIRST/A/B/C, capacity polling, or real-prompt replay.

Use existing data routes. Do not introduce paid data services, upgrades, paid fallbacks, a new free-entitlement audit system, or a new API-budget project.

## 1. Concern-specific authority

Historical measured facts:
    thesis-monitor-20260907-monitoring-pause-completion-fresh-issuer-ownership-proof-report.zip

Expected ZIP SHA-256:
    3b957ec1e2257832fa8fc531ae883e4f0c9f8228449b6f0d78fbbbd9fe1e7309

Independently recompute it and validate the artifact index at task start.
Expected inventory:
    ZIP members = 1165
    indexed payloads = 1164
    only root artifact-index.json is excluded from its own index
    hash mismatch count = 0
    size mismatch count = 0

The latest result controls historical execution facts, not the current worktree.
Current HEAD/worktree controls which implementation is present now.
This instruction controls the newly authorized scope.
The preceding instruction controls what was precommitted for the completed FIRST/A and interrupted B. Do not retroactively change its gates.

Preceding instruction ZIP:
    thesis-monitor-20260907-monitoring-pause-completion-fresh-issuer-ownership-proof-work-instruction.zip

Preceding instruction SHA-256:
    899518ffca1ba67e3f11049e2a660d802e759a0b73a0140c7001010651511eb9

Source-derived statements, new local observations, and inferences must remain separate.
Do not convert an unresolved causal hypothesis into a confirmed root cause.

## 2. Historical progress that must not be lost

Latest source generation:
    20260907-fresh-issuer-source-20260907T090500Z-2b1e0d043243

Latest runtime generation:
    20260907-fresh-issuer-proof-20260907T090500Z-2fc43e587bfe

Source lock:
    eee5764bb11218ab080a65ac6179e7b5d6440be11753ce34f8cd292ffc2504ef

Ordered cohort:
    NU, LNG, TCI, WMG,
    043100, 025000, 131030, 372910,
    130740, 045340, 006910, 288980,
    023440, 064820, 001440, 073490

Execution facts:

| Run | Core attempts / successes | Timing attempts / successes | Run result |
| --- | --- | --- | --- |
| FIRST | 4 / 4 | 4 / 4 | 16/16; ownership, renderer, hard-safety gates report PASS |
| A | 4 / 4 | 4 / 4 | 16/16; ownership, renderer, hard-safety gates report PASS |
| B | 1 / 0 | 0 / 0 | timeout at first Core context; no usable B output |
| C | 0 / 0 | 0 / 0 | NOT_RUN |

Historical actual model subprocess invocations:
    17 = 16 successful + 1 failed

Successful raw model output files:
    16

Successful stage candidate rows:
    64 = 32 Core + 32 Timing

Unique exposed issuers:
    16

Deterministic composed rows / rendered messages:
    32 / 32

Renderer context count is deterministic composition/rendering work.
Do not add it to the count of AI model subprocess invocations.

Formal four-run stability:
    NOT_MEASURED

Ownership generalization:
    NOT_ESTABLISHED

Full proof:
    INCOMPLETE_TRANSPORT_FAILURE

The report also records a separate message-quality FAIL for FIRST and A. A hard ownership PASS must not erase that result.

## 3. Monitoring pause is now complete — preserve it

The latest operational report records:
    US pause = PAUSED_COMPLETE
    KR pause = PAUSED_COMPLETE
    paused scheduler objects = 8
    prior task changes = 6
    latest task additional changes = 2
    forced termination = 0
    automatic restoration = false

Eight named objects:
    com.seungsoo.thesis-monitor.daily
    com.seungsoo.thesis-monitor.kr-close
    thesis-monitor-ai-review-us-primary
    thesis-monitor-ai-review-us-backup
    thesis-monitor-ai-review-kr-primary
    thesis-monitor-ai-review-kr-backup
    com.seungsoo.thesis-monitor.ai-review-fallback
    com.seungsoo.thesis-monitor.ai-review-delivery-retry

Historical timeline in Asia/Seoul:
    2026-09-07 17:10:00–17:10:18:
        existing natural fallback delivered the nine previously held KR items.
        This was before the additional dependencies were disabled.
    2026-09-07 17:35:19:
        latest after-state verification reports complete pause.
    2026-09-07 17:42:54:
        that KR backlog pending = 0; sent = 9.

Nine items comprise a KR digest and eight company messages, not nine companies.
Do not say those historical items remained held, were deleted, or were delivered by this task.
Do not equate task_initiated_telegram_send=0 with no historical automatic delivery.

For the current task:
- Read the existing scheduler state through the existing safe observation path, when available.
- Do not re-enable, send, flush, delete, requeue, or manually mark any delivery.
- Do not force-stop a process.
- Preserve registrations, investment-logic versions, assessments, warnings and price rules.
- Do not change unrelated macro/Night Futures work.
- If observation is unavailable, report CURRENT_STATE_NOT_VERIFIED; do not invent a live check.
- If an unexpected active automatic monitoring path is found, stop implementation and report the drift; do not introduce another scheduler redesign.

No restoration is authorized. A new explicit user request is required to restore schedules.
Current-task scheduler changes should remain zero; retain the historical two additional changes separately.

## 4. Repository provenance and instruction commit

Latest report identifies:
    base_sha:
        63f5f57df51566ed2198f1e007d470bbb61e1a02
    work_instruction_commit:
        2bb58e9ae22f3ec5bd97d3a94e92a3a13e4fe257
    implementation_commit:
        a5a485748e08388c3ddfca61559fd5ab6ad56f25
    final_head_sha:
        46e09b999d6c5834fa5f5adf7ec1ff0df159bfb1
    branch:
        codex/20260907-monitoring-pause-completion-fresh-issuer-ownership-proof

Record actual branch, HEAD and git status before modifying code.
Explain later commits rather than assuming their absence.
Do not reset, discard, merge or cherry-pick unrelated work.
If unexplained changes compromise the frozen evidence or investment semantics, stop.

Commit this work instruction before implementation.
If no implementation change is justified, a forensic-only result is valid; do not manufacture a patch.

## 5. Mutation budget

Allowed only when necessary:
- experiment/report-only error classification;
- experiment/report-only retry observability from preserved logs;
- typed normalization of current and legacy report keys;
- report-level count, exposure, measured-scope and message-quality propagation;
- offline audit tooling that uses frozen validators without modifying their semantics;
- tests with fictional/minimal local fixtures;
- current result serialization and artifact indexing.

Forbidden:
- Directional Core, Price-Timing, composer or renderer decision/prompt semantics;
- numeric, accounting, ADR/share-basis, official provisional-earnings or identity safeguards;
- source-sufficiency rules, source refresh, candidate selection or source packet mutation;
- BUY/HOLD/SELL thresholds, balance increments or HOLD lean rules;
- relaxing schema const/enum restrictions;
- canonical transport API, network protocol, process lifecycle or watchdog topology;
- changes to Codex binary/version, environment variables, model, effort, state namespace lifecycle;
- increased timeout, new retry/backoff policy, auto model fallback, forced HTTP/WebSocket switching;
- stock registration, live V2/Structured Autonomy activation, production deployment or main merge;
- scheduler restoration, production sends, historical database rewriting;
- ticker-specific production exceptions or phrase allowlists.

A supported runtime correction requiring those forbidden changes must be proposed for a separate bounded task with evidence and a test plan; do not implement it here.

## 6. B timeout: reconstruct the exact lifecycle

Failed invocation:
    20260907-fresh-issuer-proof-20260907T090500Z-2fc43e587bfe:b:DIRECTIONAL_CORE:01

Primary files:
    experiment/model-contexts/B/DIRECTIONAL_CORE/batch-01/transport_receipt.json
    experiment/model-contexts/B/DIRECTIONAL_CORE/batch-01/stderr.raw.log
    experiment/model-contexts/B/DIRECTIONAL_CORE/batch-01/transport_log.raw.log
    experiment/model-contexts/B/DIRECTIONAL_CORE/batch-01/context_manifest.json
    reports/proofs/62-reporting-closeout-recovery.json

Receipt:
    started_at = 2026-09-07T10:27:24.428058+00:00
    completed_at = 2026-09-07T10:57:24.502005+00:00
    configured_timeout_seconds = 1800
    elapsed_to_exit_seconds = 1800.056758
    exit_code = -15
    output_bytes = 0
    stdout_bytes = 0
    output_parsed = false
    parse_error = OUTPUT_FILE_MISSING
    termination_initiator = PYTHON_MONOTONIC_LIFECYCLE_WATCHDOG
    termination_signal = 15
    timeout_owner_count = 1
    duplicate_timeout_receipt_count = 0
    orphan_model_process_count = 0
    child_cleanup_status = PROCESS_GROUP_TERMINATED
    request_accepted_observability = UNAVAILABLE

Exact runtime warning:
    2026-09-07T10:28:39.403008Z WARN codex_core::responses_retry:
    stream disconnected - retrying sampling request (1/5 in 202ms)...
    sampling_error=stream disconnected before completion:
    WebSocket protocol error: Connection reset without closing handshake

Monotonic differences derived from the receipt:
    last stderr approximately 74.974474 seconds after invocation start
    subsequent silence until exit approximately 1725.082284 seconds

Use the monotonic clock for duration and wall time for displayed timestamps.
Do not infer a separate request-accepted event from process spawn, session creation, input writing or the retry warning.

Required historical classification, subject to independent verification:
    WATCHDOG_TRANSPORT_TIMEOUT_AFTER_WEBSOCKET_DISCONNECT

Capacity error:
    NOT_PROVEN / NO_EXPLICIT_CAPACITY_DIAGNOSTIC_OBSERVED_IN_THIS_CONTEXT

Exact remote/local cause:
    UNRESOLVED unless further existing evidence directly establishes it.

OUTPUT_FILE_MISSING is a downstream observation, not the sole causal explanation.
The previous capacity failure and earlier silent timeout are separate incidents; do not overwrite or merge them.

## 7. Compare successful and failed requests without replay

Compare FIRST Core01, A Core01 and B Core01.

Expected exact prompt and schema identity:
    prompt/input SHA-256:
        20decffc0c506d66e2ed3c5746c6ce840e9c9f41625738af1e36d4edff4e84dd
    schema SHA-256:
        10d702a4c76c45fa2eed40627f78603071bc2ae7ffcd8faf33059b2332304dfb
    input bytes:
        17940

Recorded elapsed time:
    FIRST Core01: 235.449649 seconds; PASS
    A Core01:     214.238843 seconds; PASS
    B Core01:    1800.056758 seconds; TIMEOUT

B has a separate session ID and runtime-state namespace from the successful earlier run.
The result contains 17 distinct recorded session IDs.
Confirm those facts from logs rather than assuming shared state implies one accumulating conversation.

Compare:
- actual prompt/schema bytes and subject order;
- CLI binary/version and safe configuration identity;
- session ID and namespace hash;
- input and output bytes;
- stdout/stderr timing;
- network-readiness observations;
- retry warnings;
- subprocess exit and cleanup;
- known local coexistence evidence.

Do not conclude that identical input guarantees identical runtime behavior.
Do not identify a context-length error, capacity error, local network failure or backend outage without corresponding evidence.
Do not convert the absence of a diagnostic into proof that its underlying cause is impossible.

The historical_stall_pattern_recurred field needs scoped interpretation:
- 1800-second watchdog timeout occurred again;
- the exact earlier startup-only silent trace was not identical;
- this incident contains an explicit WebSocket disconnection/retry warning.
Report these separately instead of a single unqualified recurrence boolean.

## 8. Retry accounting: distinguish layers

Required separation:
    wrapper_explicit_retry_count = 0
    observed_cli_retry_signal_count = 1
    upstream_request_attempt_count = UNKNOWN
    upstream_accepted_request_count = UNKNOWN

The recorded "1/5" identifies a retry warning with a configured maximum, not five observed retries.
Do not claim the retry request actually reached or was accepted by the server without evidence.
Do not use zero wrapper retry as evidence of zero SDK/CLI-internal retries.
Do not modify internal retry configuration in this task.

If implementing a report-only classifier, test these cases locally with fixtures:
1. WebSocket warning followed by PASS: overall result remains PASS_WITH_WARNING, not timeout.
2. Warning followed by watchdog timeout: preserve both observations.
3. Explicit capacity diagnostic and non-timeout exit: distinct classification.
4. Context-length diagnostic: distinct only when explicitly present.
5. OUTPUT_FILE_MISSING alone: do not invent an upstream cause.
6. Pre-spawn exception: not a post-spawn timeout.
7. No retry evidence: preserve observability limitations.
8. Diagnostic-like words inside user prompt/output must not be misread as runtime stderr events.

Tests must not invoke the model or network. Do not kill a live model on the first nonterminal warning.

## 9. Repair the report closeout contract, not its history

The result records a closeout failure after B:
    KeyError: registry_count
    plus legacy new_holdout_exclusion_count / source-class key assumptions

Historical recovery:
    temporary report clone with factual aliases
    original source proofs unchanged
    model calls = 0
    no prompt/schema/validator/candidate change

If current repository still has this bug, implement a bounded report-input normalizer:
- accept documented current and legacy fields;
- require matching meanings and scopes;
- record canonical-field mapping and source provenance;
- detect contradictory values instead of silently choosing;
- distinguish absent, unknown and actual numeric zero;
- derive counts from available exact membership/receipt/output evidence when supported;
- otherwise leave NOT_MEASURED/UNKNOWN rather than fabricate a value.

Regression tests:
- current-format report;
- supported legacy-format report;
- contradictory dual keys;
- missing key after successful runs plus a terminal failure;
- all pre-spawn/no-output case;
- preservation of original error and original source proofs.

Do not edit the original ZIP. Create new corrected/derived reports with original-to-derived linkage.
Do not hide the reporting bug under the transport cause or replace the transport cause with the report exception.

## 10. Reconcile counts, exposure, semantic scope and quality

Keep separate:
    attempted contexts = 17
    terminal receipts = 17
    successful output contexts = 16
    failed contexts = 1
    Core attempts/successes = 9/8
    Timing attempts/successes = 8/8
    usable B rows = 0
    completed full runs = FIRST and A
    successful stage rows = 64
    unique issuers = 16
    deterministic composed/rendered rows = 32

Do not label B completed_context_count=1 as one successful output context.
Formal stability counts of zero in a NOT_MEASURED report do not mean zero instability.

The original report records:
    exposure = FULLY_EXPOSED
    retirement = RETIRED_PARTIAL_EXPOSURE
    semantic revelation = NOT_MEASURED

It also records:
    retirement_reason = INCOMPLETE_A_B_C_AFTER_FULL_COHORT_EXPOSURE

Preserve the original enum values in historical provenance.
Do not silently redefine them.
Use existing canonical mappings where unambiguous, or add derived scope/reason fields:
    cohort_unique_issuer_exposure = FULLY_EXPOSED
    proof_completion = INCOMPLETE_TRANSPORT_FAILURE
    completed_run_hard_gate_scope = FIRST_AND_A
    completed_run_semantic_observation = NO_REPORTED_HARD_DEFECT_IN_MEASURED_SCOPE
    formal_generalization = NOT_ESTABLISHED
    future_unseen_holdout_reuse_allowed = 0

The entire 16-issuer cohort stays excluded from future unseen proof.
Do not resume B, run C, replay FIRST/A, select only unused stage results, or tune architecture against this cohort.

## 11. Offline FIRST/A evidence and gate review

Use only preserved real outputs, schemas, source packets, normalized rows, composed rows and rendered messages.
Separate them from experiment/model-free-actual-request-preflight mock artifacts.

Reverify:
- raw outputs against actual per-context schema files;
- prompt/schema/receipt/binding/manifest IDs and hashes;
- explicit market fields and subject order;
- raw-to-normalized-to-report lineage;
- Core/Timing inputs used by each composed row;
- direction, balance and HOLD lean preservation;
- applicable frozen ownership validators;
- new-buyer conservatism and price-only holder restrictions;
- renderer action ownership and available hard-safety validation.

There are 16 real raw output files containing 64 stage rows.
All 32 composed/rendered rows are available inside the two execution-summary row arrays.
Do not repeat the old “renderer artifact missing” assumption.
A separate derivative export is allowed for inspection, with source-member and row mapping.

Existing hard-safety reports mark several legacy safeguards REUSED_UNCHANGED.
Report which checks are freshly rerun offline, which are inherited, and which cannot be checked in this environment.
Do not describe all safeguards as freshly measured solely because the aggregate status is PASS.

No production message sends or production DB assessment writes.

## 12. FIRST/A differences are descriptive, not the missing full stability proof

Independent initial mechanical comparison of exact fields found:
    overall_direction identical: 9/16
    overall_direction different: 7/16
    directional_balance identical: 7/16
    fundamental new-buyer stance identical: 13/16
    fundamental holder stance identical: 15/16
    technical_state identical: 13/16
    entry_mode identical: 16/16
    timing_new_buyer_modifier identical: 15/16
    holder_price_review identical: 13/16
    direct BUY↔SELL flip count: 0

Direction changes:
    TCI:    HOLD 4.5:5.5 → SELL 4.0:6.0
    025000: HOLD 5.5:4.5 → BUY 6.0:4.0
    131030: HOLD 5.5:4.5 → BUY 6.0:4.0
    372910: HOLD 5.5:4.5 → BUY 6.0:4.0
    045340: BUY 6.0:4.0 → HOLD 5.5:4.5
    006910: BUY 6.0:4.0 → HOLD 5.5:4.5
    023440: SELL 4.0:6.0 → HOLD 4.5:5.5

All seven observed direction changes cross a classification boundary with a 0.5-point shift.
Recompute rather than merely copying these counts.
Inspect referenced anchors and Unknown treatment to describe what changed.

Use:
    POST_HOC_TWO_RUN_DESCRIPTIVE_COMPARISON

Do NOT:
- turn 9/16 into an official reliability or success probability;
- assign official STABLE/BOUNDARY_UNCERTAINTY/UNSTABLE without the existing protocol and required observations;
- fill B/C from FIRST/A or averages;
- lower the four-run requirement after seeing failure;
- smooth balances, select a preferred answer or edit prompts to suppress observed changes.

If a hard semantic defect is independently established, preserve it and recommend a separate generic repair with a new future holdout. Do not tune it here.

Core request bytes are identical across FIRST/A for all four batches.
Timing inputs may legitimately differ because they include that run's Core outputs/fingerprints.
Check those derived bindings rather than demanding byte equality of all Timing prompts.

## 13. Message-quality FAIL is a separate result

Both run execution summaries contain:
    message_quality.status = FAIL
    errors = ["cross_ticker_substantive_repetition"]

Reported distinct repeated substantive spans:
    FIRST = 14
    A = 9

These are repeated-span metrics, not failed-subject counts or percentages.

The ownership/renderer/hard-safety gates report PASS for the same runs.
Preserve both facts.

Read the precommitted instruction and frozen evaluator code to establish:
- whether this quality check was advisory or a required hard progression gate;
- whether the repetition classifier correctly distinguishes renderer-owned labels/safety wording from model-owned substantive repetition;
- whether common evidence limitations explain part of the repetition;
- whether important issuer-specific evidence was already present but omitted.

Do not invent business facts or fetch new evidence to rewrite these outputs.
Do not add broad phrase allowlists or weaken the quality validator.

If the quality check was advisory:
    retain hard-gate PASS and explicitly report unresolved message-quality FAIL.
If it was a required progression gate:
    identify the actual progression-contract breach with evidence.
Do not retroactively promote an advisory test into a gate or excuse a pre-existing hard gate.

Export original rendered messages and their source mappings for offline inspection only.
No prompt/style/renderer semantic tuning against the exposed cohort in this task.

## 14. Testing and verification

Before changes, record the intended file/function mutation scope.
After changes, run focused report/classifier tests and repository-standard tests/lint as available.
Record actual commands, test counts, exit statuses and relevant excerpts.

Do not claim full tests passed unless run in the actual repository.
No investment-model calls may occur during tests.
Use local fixture tests, not real Codex invocations disguised as smoke tests.

Keep a bounded diff audit:
    architecture/prompt/schema/source semantics changed = 0
    canonical transport/process/watchdog semantics changed = 0
    model/effort/context grouping changed = 0
    current task scheduler changes = 0
    production deployment/send/registration changes = 0

If the needed repair exceeds this scope, stop mutation and provide the evidence and next-scope decision.

## 15. Compact required deliverables

Use repository filenames where possible. Produce Markdown and machine-readable JSON for:

1. Input-integrity and repository provenance.
2. Existing pause-state observation and historical transition accounting.
3. FIRST/A/B/C execution reconciliation.
4. B lifecycle timeline and safe error excerpt.
5. FIRST/A/B request/session/namespace comparison.
6. Wrapper-vs-CLI retry observability and classification.
7. Reporting-contract root cause, bounded patch and regression results.
8. Original-to-derived exposure/retirement/measurement mapping.
9. Offline FIRST/A evidence lineage and measured-scope audit.
10. Descriptive two-run field differences and anchor comparison.
11. Message-quality scope, preserved FAIL and original-message export.
12. Unresolved causes, concrete next-scope decision and completion.

Include the exact historical input ZIP checksum and referenced original-member checksums.
Historical artifacts copied into the new result must remain byte-identical and labeled historical.
Derived exports must be labeled derived; do not present them as new model output.

## 16. Completion fields

At minimum:

    base_sha
    work_instruction_commit
    implementation_commit_or_not_changed
    final_head_sha
    branch
    input_zip_sha256
    input_member_count
    input_indexed_payload_count
    current_pause_observation_status
    historical_natural_delivery_items = 9
    new_scheduler_mutation_count = 0
    auto_resume_executed = 0

    historical_model_subprocess_invocations = 17
    historical_successful_contexts = 16
    historical_failed_contexts = 1
    historical_stage_rows = 64
    historical_unique_exposed_issuers = 16
    historical_composed_rows = 32
    historical_rendered_rows = 32

    first_run_hard_gate_status
    a_run_hard_gate_status
    b_execution_status
    c_execution_status
    first_message_quality_status
    a_message_quality_status
    message_quality_gate_role_at_execution
    formal_stability_status
    formal_generalization_status

    timeout_classification
    capacity_diagnostic_status
    last_stderr_after_start_seconds
    silence_until_exit_seconds
    wrapper_explicit_retry_count
    observed_cli_retry_signal_count
    upstream_request_attempt_count
    request_accepted_observability
    transport_root_cause_confirmed
    unresolved_hypotheses

    original_report_state
    derived_measured_scope_state
    reporting_patch_status
    descriptive_comparison_status
    semantic_validator_rerun_scope
    hard_defect_confirmed_in_offline_scope
    original_evidence_hash_drift = 0

    new_real_issuer_model_calls = 0
    new_fictional_model_calls = 0
    new_source_fetch_calls = 0
    new_holdout_created = 0
    retired_cohort_replayed = 0
    production_send = 0
    production_deployment = 0
    stock_registration_change = 0
    paid_data_service_change = 0
    timeout_increase = 0
    canonical_transport_mutation = 0

    focused_tests
    full_tests
    lint
    next_scope
    readiness
    stop_reason
    payload_count
    zip_member_count
    integrity_mismatches

Missing facts are UNKNOWN/NOT_MEASURED, not numeric zero.
Separate configured expectations from measured values.

## 17. Evidence-based next-scope decision

Do not automatically allocate another real cohort after this review.

If evidence identifies a specific local runtime/transport defect:
    propose a bounded generic runtime repair and a predeclared fictional validation plan.
    Explain why the proposed change targets the observed failure.
    Do not implement live transport changes here.

If cause remains unresolved:
    state that directly.
    Propose at most one narrowly specified diagnostic experiment capable of distinguishing the remaining hypotheses.
    Define its evidence, success/failure criteria, bounded fictional-call budget and stop rule for a separately authorized task.
    Do not poll capacity, increase timeout, switch models, or issue a new 32-call real proof in this task.

If offline review identifies a hard semantic defect:
    retain transport findings independently;
    recommend a separate generic architecture repair and a new future issuer holdout.

If only boundary variability and advisory quality repetition are observed:
    do not declare the architecture invalidated.
    Carry the issues explicitly into the next proof/release decision; do not present them as resolved.

Even if reporting and forensic work PASS:
    runtime reliability is not established by offline tests alone;
    full ownership generalization remains NOT_ESTABLISHED;
    Monitoring Bootstrap and production activation remain out of scope.

## 18. Finalization and principle

Finalize substantive completion before calculating the artifact index.
Index every payload, including completion reports and README, except the index itself.
Distinguish indexed payload count from total ZIP members.
Avoid circular self-hash claims.
Verify ZIP contents after packaging and supply a SHA-256 sidecar.
Do not expose credentials, environment secrets, private authentication state or unsanitized commands.

The current milestone is real:
    two complete 16-issuer runs passed the reported hard ownership gates.

The remaining issues are also real:
    B suffered a WebSocket-disconnection-associated watchdog timeout;
    CLI-internal retry was observed even though wrapper retry was zero;
    the completed runs have boundary-sensitive differences and message-quality FAIL;
    complete four-run proof is still missing.

Do not discard successful evidence, hide quality failures, or consume another cohort merely to repeat a failed execution pattern.
Use the existing evidence first. Preserve the completed pause. Make the next runtime action specific.
