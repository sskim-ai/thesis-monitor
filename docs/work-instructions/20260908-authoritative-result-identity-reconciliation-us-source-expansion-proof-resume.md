# Thesis Monitor — Authoritative Result Identity Reconciliation + US Source Expansion & Fresh Generalization Proof Resume

## 0. Task identity

Suggested work-instruction filename:

```text
20260908-authoritative-result-identity-reconciliation-us-source-expansion-proof-resume.md
```

Suggested result bundle:

```text
thesis-monitor-20260908-authoritative-result-identity-reconciliation-us-source-expansion-proof-resume-report.zip
```

This is a **provenance correction + immediate resume of the already-authorized US free-source expansion and fresh unseen generalization proof**.

The immediately preceding task stopped before any implementation or source/model call because the work instruction contained the wrong SHA-256 for the prior calibration result bundle.

That stop was correct under the written gate, but the gate input itself was wrong.

The current task must correct only that provenance identity and then resume the already-planned work.

Do NOT create another provenance-review-only task after reconciliation passes.

---

# 1. Exact provenance correction

## 1.1 Immediately preceding stopped result

Authoritative latest no-op/stop result:

```text
thesis-monitor-20260908-us-source-universe-expansion-fresh-generalization-proof-resume-report.zip
```

Actual SHA-256:

```text
e2f0c5f84e637ca10a18211c7fa8a4c061232eac5d15ca4874ceccf2930a7fc0
```

Its adjacent checksum file matches that SHA-256.

Its internal artifact index contains:

```text
indexed payloads = 9
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

This latest result performed:

```text
implementation changes = 0
source/provider calls = 0
model calls = 0
scheduler mutation = 0
production mutation = 0
```

and stopped with:

```text
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

## 1.2 Actual authoritative calibration-result bundle

The result that the stopped task intended to use is:

```text
thesis-monitor-20260908-directional-core-boundary-calibration-repair-fresh-generalization-proof-report.zip
```

Its actual SHA-256 is:

```text
ab330fa13a53775ecdd162f36f4f7afeda42415db03bd0c766c48bf3a2eaddac
```

The adjacent checksum file used by the stopped task also recorded:

```text
ab330fa13a53775ecdd162f36f4f7afeda42415db03bd0c766c48bf3a2eaddac
```

The previous work instruction incorrectly froze:

```text
f0a41371e12127a891165860f3c44ce6af7d1b8dcd13d0c5b93570618a075e6a
```

as the expected SHA-256.

That value is now classified as:

```text
ERRONEOUS_PRIOR_INSTRUCTION_EXPECTED_HASH
```

It is NOT the authoritative whole-file identity of the calibration result ZIP.

Required correction:

```text
AUTHORITATIVE_CALIBRATION_RESULT_SHA256 =
ab330fa13a53775ecdd162f36f4f7afeda42415db03bd0c766c48bf3a2eaddac
```

Do not require or search for a ZIP with the erroneous `f0a413...` identity.

---

# 2. Provenance reconciliation gate

At task start:

1. recompute the SHA-256 of the latest no-op stop result;
2. recompute the SHA-256 of the authoritative calibration result;
3. verify each adjacent checksum if present;
4. verify each bundle's internal artifact index.

Required:

```text
LATEST_STOP_RESULT_INTEGRITY = PASS

AUTHORITATIVE_CALIBRATION_RESULT_INTEGRITY = PASS

AUTHORITATIVE_CALIBRATION_RESULT_SHA256 =
ab330fa13a53775ecdd162f36f4f7afeda42415db03bd0c766c48bf3a2eaddac

PROVENANCE_IDENTITY_RECONCILIATION = PASS
```

If the actual local calibration ZIP no longer has that SHA-256:

```text
STOP
AUTHORITATIVE_CALIBRATION_RESULT_CHANGED
```

Do not guess a replacement.

---

# 3. Why no historical work must be repeated

The immediately preceding task stopped before implementation and did not mutate the architecture or source pipeline.

Therefore do not repeat:

```text
Directional calibration root-cause analysis
Directional calibration prompt repair
fictional 6-call calibration proof
runtime namespace repair
runtime isolation proof
historical FIRST/A/B/C proof
```

unless current repository hashes prove those relevant implementations changed after their frozen result.

The provenance mismatch was not a semantic invalidation of the calibration work.

---

# 4. Frozen Directional calibration result

The authoritative calibration bundle established:

```text
calibration_root_cause =
GENERIC_DIRECTIONAL_CALIBRATION_ROOT_CAUSE_CONFIRMED

adjacent_calibration_ambiguity_count = 8
material_evidence_variance_count = 0
true_semantic_divergence_count = 0

directional_calibration_repair_applied = 1

directional_threshold_changed = 0
majority_vote_adopted = 0
fixed_weight_scorecard_adopted = 0
```

Frozen calibration identities:

```text
calibration_contract_sha256 =
3443dc0f7505d01bc5ba965fa0f20128f12f2678d5e16d5f883ce41f47067d33

calibration_freeze_seal_sha256 =
8214ee6e7fbc5fa29e5569dac5c01ab466744e4f343bab4a9d689231cdc6837d
```

Preserve these identities.

If current repository code no longer matches the frozen calibration implementation:

```text
STOP
CALIBRATION_FREEZE_DRIFT
```

Do not silently regenerate the calibration.

---

# 5. Fictional calibration proof remains valid

The authoritative calibration result completed:

```text
fictional fixture count = 8
independent repetitions = 3
model calls = 6
successful model calls = 6

fictional_directional_unstable_count = 0
minimum_positive BUY↔HOLD flip count = 0
minimum_negative SELL↔HOLD flip count = 0
opposite-direction reversal count = 0

fictional_calibration_status = PASS
```

If:

```text
calibration prompt hash unchanged
schema unchanged
model/runtime semantics unchanged
```

then:

```text
fictional calibration rerun count = 0
```

Do not consume another six model calls merely because the provenance gate in the next task was wrong.

---

# 6. Actual current blocker after provenance correction

The authoritative calibration task did not start a fresh real proof because:

```text
US source target = FAIL
KR source target = PASS

US target = 4
US bounded candidates = 9
US attempted = 9
US source sufficient = 2
US source insufficient = 7

real fresh proof model calls = 0
fresh cohort = []
fresh source lock = null
```

Source-sufficient US candidates:

```text
RMD
MSFT
```

The current task therefore resumes at:

```text
US free-source supported-universe expansion
```

not calibration.

---

# 7. User-approved operating policy

## 7.1 Free data routes only

Use existing free/public data routes.

Do not:

```text
add a paid financial API
upgrade to a paid market-data tier
use paid fallback merely to fill the proof cohort
```

Do not lower source-sufficiency standards to compensate for free-data limitations.

Unavailable data remains unavailable.

## 7.2 Existing US/KR monitoring remains paused

The user explicitly requested the existing scheduled US/KR monitoring remain paused.

Do not automatically resume it.

At task start, observe the approved paused monitoring paths.

If already paused:

```text
scheduler mutation = 0
```

If one exact previously approved monitoring path unexpectedly resumed:

```text
pause only that path
record actual mutation
```

Do not alter unrelated schedules.

---

# 8. Repository provenance gate

Before implementation:

```text
git branch --show-current
git rev-parse HEAD
git status --short
```

The immediately preceding no-op result reported:

```text
branch =
codex/20260908-us-source-universe-expansion-fresh-generalization-proof-resume

base_sha =
90bf9cbf64dfc92b00fa473faed110e64164b34c

work_instruction_commit =
418a14e5ebb2f3a980948f933db8d99154d16b14

final_head_sha =
5323a73d9adde926eae5ff2b349faf954b201518
```

Use actual repository state.

Because the stopped task made no implementation change, the new task should normally inherit the frozen calibration/source/runtime implementation.

If unexplained semantic changes affect:

```text
Directional calibration
Price-Timing
ownership/renderer
source sufficiency
prompt/schema
transport
runtime namespace isolation
```

classify them before proceeding.

---

# 9. Work-instruction commit first

Commit this work instruction before implementation.

Record:

```text
new_work_instruction_commit
```

Only then change the bounded US reference/universe surface if root-cause evidence supports it.

---

# 10. Allowed mutation surface

Allowed:

```text
US official/reference universe ingestion
US canonical security-to-issuer mapping
US security/reference membership adapter
free-route support capability mapping
deterministic unseen-candidate ordering
US source-coverage diagnostic tooling
tests for the above
fresh proof orchestration
```

Strong preference:

```text
generic official/reference expansion
```

Do not manually append four desirable tickers.

---

# 11. Forbidden mutation surface

Do not change:

```text
Directional calibration contract
Directional prompt calibration
BUY/HOLD/SELL thresholds
0.5 balance increments
Price-Timing prompt
schemas

source-sufficiency evidence requirements
accounting attribution
ADR/security-basis safety
numeric provenance

model = gpt-5.6-sol
reasoning effort = xhigh
MODEL_CONTEXT_COUPLED
4 subjects/context
1800-second single watchdog

per-model-context runtime namespace isolation
ContinuationTransportAdapter canonical semantics
```

Do not:

```text
majority-vote repeated outputs
average repeated scores
relax stability validator
add paid provider
increase timeout
switch model on capacity
add wrapper auto-retry
```

---

# 12. Preserve the real-exposure exclusion registry

The authoritative calibration result recorded:

```text
fresh exclusion registry count = 149
```

Preserve the canonical real-exposure registry and its current hash.

Do not shrink it.

No previously real-model-exposed issuer may enter the fresh unseen proof.

Source-only diagnostic attempts do not count as model exposure.

---

# 13. Phase A — US supported-universe funnel audit

Before changing membership, report separately:

```text
raw official/reference security rows
normalized securities
canonical security identities
canonical issuer identities
routing-supported securities
source-client-attemptable issuers
real-exposure exclusions
remaining unseen supported issuers
bounded candidate list
```

Identify the exact filters that produced the exhausted 9-candidate tail.

Required classification:

```text
US_SUPPORTED_UNIVERSE_FUNNEL_ROOT_CAUSE_CONFIRMED
```

If not confirmed:

```text
STOP
```

No speculative expansion.

---

# 14. Do not confuse price support with Directional support

Audit whether candidate membership currently requires price-provider support even where:

```text
fundamental evidence can be obtained from SEC/free official sources
and
the existing price contract permits UNAVAILABLE_SAFE
```

If such coupling exists:

```text
OPTIONAL_PRICE_SUPPORT_OVERCOUPLING = 1
```

A generic correction is allowed only if:

```text
Directional fundamental sufficiency remains unchanged
no price is fabricated
no technical claim is created without data
Price-Timing uses its existing unavailable-safe behavior
```

Do not weaken fundamental source sufficiency.

---

# 15. Generic free US reference expansion

Use existing free/public official/reference routes already compatible with the repository, such as the current canonical forms of:

```text
SEC issuer/ticker identity data
Nasdaq Trader / exchange reference data
existing provider capability data
```

Do not scrape arbitrary websites.

Do not add a paid source.

Expand the canonical supported candidate universe by resolving:

```text
security identity
issuer identity
market/security class
supported free fundamental route
```

Do not declare every raw exchange row supported.

---

# 16. Preserve security/issuer eligibility rules

Maintain canonical treatment of:

```text
multiple share classes
ADRs
foreign private issuers
SPACs
units
warrants
funds/investment-company structures
duplicate issuer securities
```

Only existing eligible security classes may enter the proof.

One issuer counts once for unseen-proof purposes.

Use deterministic canonical security selection for multiple eligible share classes.

---

# 17. Freeze expanded US candidate order before source testing

After generic expansion, create:

```text
extended-us-candidate-policy.json
extended-us-candidate-manifest.json
```

Freeze:

```text
candidate universe hash
real-exposure registry hash
ordering method
seed/salt
ordered candidate list
bounded source-evaluation budget
```

Use the repository's existing deterministic outcome-independent selection lineage if available.

Do not reorder candidates after observing source quality.

Recommended bounded evaluation budget:

```text
at least 24 unseen US issuer candidates
```

if legitimately available.

If fewer are legitimately available, use the full eligible set and report the count.

---

# 18. Evaluate US source readiness in frozen order

No model calls in this phase.

Evaluate candidates in the frozen order until:

```text
4 source-sufficient unseen US issuers
```

are obtained.

For each attempted candidate record:

```text
issuer/security identity
framework
official-profile status
official financial status
required evidence families
source-sufficiency result
price readiness
failure taxonomy
packet hash if eligible
```

Stop evaluating extra US candidates once US4 is obtained.

Do not cherry-pick among later candidates.

---

# 19. US failure taxonomy

Classify failures precisely:

```text
TRUE_OFFICIAL_SOURCE_ABSENCE
MISSING_REQUIRED_FUNDAMENTAL_FAMILY
NORMALIZATION_OR_MAPPING_GAP
SECURITY_OR_ACCOUNTING_BASIS_BLOCK
OPTIONAL_PRICE_ONLY_LIMITATION
IDENTITY_OR_SECURITY_CLASS_FAILURE
OTHER_CONFIRMED
UNKNOWN
```

A generic mapping repair is permitted only if:

```text
official evidence exists
semantic mapping is valid
provenance is preserved
the repair applies to a real class of issuers
```

No ticker-specific exceptions.

---

# 20. US target gate

Required:

```text
US source-sufficient unseen count >= 4
```

If still below four after the precommitted bounded evaluation:

```text
STOP PRE-MODEL

readiness =
NOT_READY_US_FREE_SOURCE_COVERAGE
```

Do not lower evidence standards.

Do not change the calibration.

---

# 21. KR path

The authoritative calibration task already had:

```text
KR target = PASS
```

Do not perform another broad KR-universe expansion.

After US passes, select/revalidate 12 unseen KR issuers using:

```text
current deterministic candidate order
updated real-exposure registry
existing free source routes
```

If an initially selected KR issuer fails current source readiness:

```text
use precommitted deterministic reserve order
```

Only broaden KR diagnostics if KR12 cannot actually be restored.

---

# 22. Fresh US4 + KR12 cohort

Only after both markets pass:

```text
US = 4
KR = 12
```

All 16 must:

```text
be absent from real-model exposure history
have canonical issuer identity
have canonical security identity
pass fundamental source sufficiency
```

Price readiness may follow the existing:

```text
READY
PARTIAL_SAFE
UNAVAILABLE_SAFE
```

contract.

Do not fabricate missing price/technical data.

---

# 23. Fresh source generation and lock

Create a brand-new:

```text
source generation
ordered issuer manifest
per-issuer DecisionEvidencePacket hashes
source identity audit
source sufficiency audit
aggregate source lock
```

Do not reuse any source lock belonging to an exposed cohort.

Before model execution freeze:

```text
calibration contract hash
calibration freeze seal
Directional prompt hash
Price-Timing prompt hash
schema hashes
model/runtime identity
namespace isolation identity
```

---

# 24. Fresh proof precommit

Before first real model call, freeze:

```text
ordered US4 + KR12
context grouping
source generation
source lock
packet hashes

calibration identity
prompt/schema identities

model
reasoning effort
batch semantics
timeout
timeout owner
runtime namespace policy

hard gates
stability criteria
message-quality advisory role
runtime-failure rules
```

No investment-semantic edits after real output appears.

---

# 25. FIRST/A/B/C execution

Run:

```text
FIRST
→ required gates
→ A
→ required gates
→ B
→ required gates
→ C
→ required gates
```

At the first required execution/semantic/renderer/hard-safety failure:

```text
STOP
```

No later run.

No wrapper automatic retry.

No selective continuation after failure.

No same-cohort architecture hotfix.

---

# 26. Per-context runtime isolation

Before every real model spawn require:

```text
unique invocation ID
unique runtime namespace
unique context working directory
```

Namespace collision:

```text
STOP BEFORE SPAWN
```

Do not expose the next context.

---

# 27. Immediate evidence preservation

After every successful model context, before the next:

```text
persist raw output
stdout
stderr/log
transport receipt
prompt
schema
context manifest
runtime namespace
working directory identity
session identity if observable
```

Then:

```text
hash
byte-size verification
secret scan
reopen/schema validation
```

Do not wait for the full run.

---

# 28. Per-context Directional hard gate

After each Directional Core context:

```text
schema valid

DIRECTIONAL_CORE_PRICE_TECHNICAL_REFS = 0
DIRECTIONAL_CORE_SUPPLY_REFS = 0
SUPPLY_DIRECTIONAL_CORE_USAGE = 0

BUY_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0
SELL_WITHOUT_NONPRICE_MATERIAL_ANCHOR = 0

DIRECTIONAL_MODEL_CALLS_ON_SOURCE_INSUFFICIENT = 0
PRICE_ONLY_DIRECTIONAL_MODEL_CALLS = 0

FINAL_DIRECTION_OWNER = DIRECTIONAL_CORE
```

If hard failure:

```text
STOP
```

Do not expose further contexts.

---

# 29. Run-level gates

For each complete FIRST/A/B/C run require:

```text
ownership gate = PASS
renderer gate = PASS
hard-safety gate = PASS
```

before the next run.

Keep message quality separate.

Do not make a new hidden hard gate after seeing results.

---

# 30. Fresh stability acceptance

Directional Core readiness requires:

```text
UNSTABLE = 0
```

under the frozen stability contract.

Do not:

```text
majority vote
average balance scores
change thresholds
relax the stability classifier
```

Price-Timing readiness also requires:

```text
UNSTABLE = 0
```

under its unchanged contract.

Report full subject-level repeated results.

---

# 31. Message-quality advisory

Continue measuring:

```text
cross_ticker_substantive_repetition
repeated substantive spans
affected messages where measurable
```

Do not hide a FAIL.

Do not rewrite message prose after seeing fresh proof results.

If all investment/runtime proof conditions pass but message quality remains FAIL:

```text
readiness =
READY_FOR_MESSAGE_QUALITY_REMEDIATION

next_scope =
MESSAGE_SPECIFICITY_AND_REPETITION_REMEDIATION
```

before production-message integration unless separately accepted.

---

# 32. Runtime observations

Continue recording:

```text
CLI retry signals
WebSocket disconnects
capacity messages
watchdog timeouts
wrapper retries
orphan processes
```

Do not modify transport preemptively.

If disconnect occurs and the call later succeeds:

```text
RECOVERY_OBSERVED
```

If a context terminates on capacity/disconnect/timeout:

```text
STOP
```

No model switch.

No timeout increase.

---

# 33. Existing monitoring remains paused

Throughout the task:

```text
automatic monitoring resume = 0
```

Observe the approved paused US/KR schedule set.

Do not send production monitoring messages.

Do not alter stored monitoring theses/history.

---

# 34. Production no-change

Outside approved pause maintenance:

```text
production DB mutation = 0
production send = 0
monitoring registration change = 0
live V2 activation/change = 0
Night Futures change = 0
paid data service change = 0
```

---

# 35. Monitoring Bootstrap remains out of scope

Do not implement Monitoring Bootstrap in this task.

Possible next scopes:

### Fresh Directional stability fails

```text
GENERIC_DIRECTIONAL_CORE_REASONING_ARCHITECTURE_REVIEW
```

### Investment proof passes but message quality fails

```text
MESSAGE_SPECIFICITY_AND_REPETITION_REMEDIATION
```

### All required proof and accepted message-quality conditions pass

```text
Monitoring Bootstrap Integration Review
```

Monitoring remains paused until separately authorized.

---

# 36. Required artifacts — provenance correction

Produce at least:

```text
01-repository-provenance
02-latest-stop-result-integrity
03-authoritative-calibration-result-integrity
04-provenance-identity-reconciliation
05-erroneous-prior-expected-hash-record
06-calibration-freeze-reuse-proof
07-real-exposure-exclusion-registry
08-schedule-pause-observation
```

Required:

```text
provenance reconciliation = PASS
```

before implementation/source expansion.

---

# 37. Required artifacts — US expansion

Produce:

```text
09-us-supported-universe-funnel
10-us-supported-universe-root-cause
11-optional-price-overcoupling-audit
12-us-reference-expansion-decision
13-us-reference-expansion-diff
14-us-reference-expansion-tests
15-extended-us-candidate-policy
16-extended-us-candidate-manifest
17-us-source-readiness-audit
18-us-source-failure-detail
19-us-target-decision
20-kr-pass-preservation
```

---

# 38. Required artifacts — fresh proof if US4 succeeds

Produce:

```text
21-fresh-us4-selection
22-fresh-kr12-selection
23-fresh-source-generation
24-source-identity-audit
25-source-sufficiency-audit
26-fresh-source-lock
27-fresh-proof-precommit
28-investment-semantic-freeze
29-runtime-isolation-freeze
```

Then per FIRST/A/B/C:

```text
execution summary
context artifact manifest
per-context semantic audit
ownership gate
renderer gate
hard-safety gate
message-quality advisory
```

---

# 39. Final required artifacts

Produce:

```text
fresh-holdout-exposure-retirement-state
fresh-directional-core-stability
fresh-price-timing-stability
fresh-ownership-generalization
fresh-renderer-ownership-proof
fresh-hard-safety-regression
fresh-message-quality-summary
runtime-reliability-observations
production-no-change
night-futures-no-change
next-scope-handoff
program-completion
```

---

# 40. Program-completion fields

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
final_head_sha
branch

latest_stop_result_sha256
latest_stop_result_integrity

authoritative_calibration_result_sha256
authoritative_calibration_result_integrity

erroneous_prior_expected_sha256
provenance_identity_reconciliation_status

calibration_freeze_reused
calibration_contract_sha256
calibration_freeze_seal_sha256
calibration_semantic_drift
fictional_calibration_rerun_count

exclusion_registry_count
exclusion_registry_sha256

us_reference_raw_security_count
us_identity_resolved_security_count
us_routing_supported_security_count
us_remaining_unseen_issuer_count

us_bounded_candidate_count
us_attempted_candidate_count
us_source_sufficient_count
us_source_insufficient_count
us_target_status
us_failure_taxonomy_counts

optional_price_support_overcoupling_found
generic_us_reference_expansion_applied
ticker_specific_us_exception_count

kr_target_status
kr_selected_count

fresh_cohort
fresh_source_generation
fresh_source_lock

model
reasoning_effort
batch_semantics
subjects_per_context
timeout
timeout_owner_count

planned_real_contexts
attempted_real_contexts
successful_real_contexts
failed_real_contexts

distinct_real_namespace_count
namespace_collision_count

raw_output_document_count
unique_issuer_output_count

wrapper_retry_count
cli_retry_signal_count
disconnect_count
capacity_failure_count
timeout_count
orphan_count

run_results.first
run_results.a
run_results.b
run_results.c

first_ownership_gate
first_renderer_gate
first_hard_safety_gate
first_message_quality

run_a_ownership_gate
run_a_renderer_gate
run_a_hard_safety_gate
run_a_message_quality

run_b_ownership_gate
run_b_renderer_gate
run_b_hard_safety_gate
run_b_message_quality

run_c_ownership_gate
run_c_renderer_gate
run_c_hard_safety_gate
run_c_message_quality

fresh_core_stability_counts
fresh_timing_stability_counts
fresh_ownership_generalization
fresh_message_quality_status

exposure_state
semantic_revelation_state
retirement_state
future_unseen_reuse_allowed

observed_paused_schedule_count
automatic_monitoring_resume

paid_data_service_change
production_db_mutation
production_send
monitoring_registration_change
live_v2_change
night_futures_change

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count

status
readiness
stop_reason
next_scope
```

Anything not actually measured:

```text
NOT_MEASURED
```

---

# 41. Artifact integrity

Build the final artifact index only after final reports are frozen.

Index every payload except the index itself.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report the final ZIP SHA-256.

---

# 42. Decision matrix

## A. Provenance reconciliation fails using the corrected identities

```text
STOP
```

Do not proceed to source expansion.

## B. Calibration freeze has drifted

```text
STOP
```

Do not rerun fictional calibration automatically.

## C. US universe expansion root cause is not confirmed

```text
STOP
```

No speculative membership changes.

## D. Expanded free US universe still produces fewer than 4 source-sufficient unseen issuers

```text
STOP PRE-MODEL

readiness =
NOT_READY_US_FREE_SOURCE_COVERAGE
```

Do not weaken source standards.

## E. US4/KR12 source lock succeeds

Proceed directly to FIRST/A/B/C in this task.

Do not create another review-only task.

## F. Fresh Directional Core UNSTABLE > 0

```text
NOT_READY_DIRECTIONAL_CORE_STABILITY
```

Do not majority-vote.

## G. Investment proof passes but message quality fails

```text
READY_FOR_MESSAGE_QUALITY_REMEDIATION
```

## H. Full required proof and accepted message quality pass

```text
READY_FOR_MONITORING_BOOTSTRAP_INTEGRATION_REVIEW
```

Monitoring remains paused.

---

# 43. Final task principle

The immediately preceding stop was caused by an erroneous expected SHA-256 in the work instruction.

The observed calibration-result ZIP and its adjacent checksum agree on:

```text
ab330fa13a53775ecdd162f36f4f7afeda42415db03bd0c766c48bf3a2eaddac
```

No source, model, architecture, or production work ran after that false provenance stop.

Therefore the correct next move is:

```text
correct the authoritative result identity
→ preserve the already-passed calibration proof
→ resume US free-source universe expansion
→ obtain US4
→ combine with KR12
→ execute the fresh FIRST/A/B/C proof in the same task
```

Not:

```text
repeat calibration
```

Not:

```text
repeat the provenance-only stop
```

Not:

```text
add paid APIs
```

Not:

```text
manually choose four US companies
```

And not:

```text
create another review-only task once source coverage passes
```

Fix the provenance identity once, then resume the real work.
