# Thesis Monitor — US Source Universe Expansion & Fresh Generalization Proof Resume

## 0. Task identity

Suggested work-instruction filename:

```text
20260908-us-source-universe-expansion-and-fresh-generalization-proof-resume.md
```

Suggested result bundle:

```text
thesis-monitor-20260908-us-source-universe-expansion-fresh-generalization-proof-resume-report.zip
```

This is a **bounded US free-source universe expansion + already-frozen Directional calibration fresh generalization proof resume task**.

The Directional Core boundary-calibration repair is already complete and frozen.

Do NOT reopen or retune it merely because the fresh real cohort could not be formed.

The current blocker is:

```text
US source-sufficient unseen candidate count = 2
required US count = 4
KR source target = PASS
```

The task must therefore:

```text
preserve the frozen Directional calibration repair
→ expand the US supported/unseen candidate universe generically using existing free official/reference routes
→ obtain at least 4 source-sufficient unseen US issuers
→ preserve the already-adequate KR path
→ build a fresh US4 + KR12 source generation/source lock
→ execute FIRST/A/B/C in the same task
```

Do not create another calibration-repair task unless new direct evidence invalidates the frozen calibration proof.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260908-directional-core-boundary-calibration-repair-fresh-generalization-proof-report.zip
```

Verified SHA-256:

```text
f0a41371e12127a891165860f3c44ce6af7d1b8dcd13d0c5b93570618a075e6a
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

The latest bundle contains:

```text
zip members = 434
indexed payloads = 433
hash mismatch = 0
size mismatch = 0
```

The index intentionally excludes only itself.

---

# 2. Calibration repair is complete and frozen

The latest result established:

```text
historical Directional Core:
STABLE               = 4
BOUNDARY_UNCERTAINTY = 4
UNSTABLE             = 8

historical Price-Timing:
STABLE               = 8
BOUNDARY_UNCERTAINTY = 8
UNSTABLE             = 0
```

Offline root-cause audit:

```text
adjacent_calibration_ambiguity_count = 8
all nonstable cases classified as adjacent calibration ambiguity = 12
material_evidence_variance_count = 0
true_semantic_divergence_count = 0

root cause =
GENERIC_DIRECTIONAL_CALIBRATION_ROOT_CAUSE_CONFIRMED
```

The repair preserved:

```text
BUY threshold = 6.0
SELL threshold = 6.0
0.5 score increments
BUY:SELL sum = 10

majority vote = not adopted
fixed-weight scorecard = not adopted
ticker-specific rules = 0
Price-Timing prompt = unchanged
schema = unchanged
```

The calibration contract was frozen with:

```text
calibration_contract_sha256 =
3443dc0f7505d01bc5ba965fa0f20128f12f2678d5e16d5f883ce41f47067d33

calibration_freeze_seal_sha256 =
8214ee6e7fbc5fa29e5569dac5c01ab466744e4f343bab4a9d689231cdc6837d
```

Do not mutate the frozen calibration in this task.

---

# 3. Fictional repeated calibration proof passed

The latest task executed:

```text
8 fictional fixtures
2 four-subject contexts
3 independent repetitions
6 total model calls
```

Measured:

```text
planned_model_call_count = 6
attempted_model_call_count = 6
successful_model_call_count = 6
wrapper_retry_count = 0

fictional_directional_unstable_count = 0
minimum_positive BUY↔HOLD flip count = 0
minimum_negative SELL↔HOLD flip count = 0
opposite-direction reversal count = 0

fictional_calibration_status = PASS
```

Key stable fixtures included:

```text
BALANCED_INCOMPLETE
→ 5.0:5.0 HOLD

POSITIVE_LEAN_INCOMPLETE
→ 5.5:4.5 HOLD BUY_LEAN

MINIMUM_POSITIVE_DIRECTION
→ 6.0:4.0 BUY

STRONG_POSITIVE
→ 7.0:3.0 BUY

NEGATIVE_LEAN_INCOMPLETE
→ 4.5:5.5 HOLD SELL_LEAN

MINIMUM_NEGATIVE_DIRECTION
→ 4.0:6.0 SELL

STRONG_NEGATIVE
→ 3.0:7.0 SELL

UNKNOWN_NOT_NEGATIVE
→ 5.0:5.0 HOLD
```

Do not repeat these 6 model calls if calibration hashes and model/runtime semantics remain unchanged.

---

# 4. Latest fresh real proof did NOT start

Latest result:

```text
fresh_us_target_status = FAIL
fresh_kr_target_status = PASS

fresh_cohort = []
fresh_source_generation = null
fresh_source_lock = null

attempted_real_contexts = 0
successful_real_contexts = 0
real raw outputs = 0

FIRST = NOT_RUN
A = NOT_RUN
B = NOT_RUN
C = NOT_RUN

fresh ownership generalization =
NOT_ESTABLISHED

exposure_state =
UNEXPOSED
```

Therefore no new real issuer from the latest candidate diagnostics was consumed by model exposure.

Source diagnostics alone do not make an issuer model-exposed.

---

# 5. Current US source-coverage facts

Latest US candidate pool after the updated real-exposure exclusions contained exactly 9 bounded candidates.

US source audit:

```text
target_count = 4
bounded_candidate_count = 9
attempted_count = 9

source_sufficient_count = 2
source_insufficient_count = 7
untested_candidate_count = 0

source_target_status = FAIL
```

The two source-sufficient US candidates were:

```text
RMD
MSFT
```

Both reached:

```text
SUFFICIENT_FOR_DIRECTIONAL_JUDGMENT
price_timing_input_readiness = READY
```

The seven failed candidates were:

```text
YARW
DMII
ISOU
SHOT
PBLS
DXYZ
VRAX
```

Observed failure classes include:

### YARW / PBLS / VRAX

```text
INSUFFICIENT_FUNDAMENTAL_EVIDENCE

missing:
CLINICAL_REGULATORY_CURRENT
LIQUIDITY_CASHFLOW_CURRENT
```

### DMII / ISOU / SHOT

```text
INSUFFICIENT_FUNDAMENTAL_EVIDENCE

missing:
BUSINESS_CURRENTORSECTOR_OPERATING_CURRENT
EARNINGS_FINANCIAL_CURRENTORLIQUIDITY_CASHFLOW_CURRENTORSECTOR_OPERATING_CURRENT
```

### DXYZ

```text
SECURITY_OR_ACCOUNTING_BASIS_BLOCK
```

Do not weaken these source-sufficiency requirements merely to fill the cohort.

---

# 6. Current KR source-coverage facts

Latest KR source audit:

```text
target_count = 12
bounded_candidate_count = 24
attempted_count = 16

source_sufficient_count = 12
source_insufficient_count = 4
untested_candidate_count = 8

source_target_status = PASS
```

KR source coverage is not the current blocker.

Do not repeat a broad KR-universe remediation task.

A fresh final source generation may revalidate the selected KR issuers before lock, but do not spend the task re-auditing thousands of KR issuers.

---

# 7. Real-exposure exclusion registry

The latest task correctly appended the fully exposed previous 16-issuer real proof cohort.

Measured:

```text
previous exclusion registry count = 133
new exclusion registry count = 149
newly excluded issuer count = 16
exclusion shrink count = 0
```

Preserve the current canonical registry and all prior exposure history.

No previously model-exposed real issuer may enter the fresh unseen proof.

---

# 8. Why US coverage is now the blocker

Earlier reference expansion produced a deterministic unseen US candidate order of 25 issuers.

After subsequent real proofs consumed additional issuers, the latest fresh selection had only 9 remaining eligible candidates under the frozen bounded candidate input.

Only 2 of those 9 passed source sufficiency.

This is an **unseen-candidate/source-coverage exhaustion issue**, not evidence that the calibration repair failed.

The current task must expand the supported/unseen US candidate universe beyond that exhausted 9-candidate tail.

---

# 9. User-approved data policy

The user has explicitly chosen:

```text
existing free API / official data routes only
no paid financial-data or market-data API
```

Maintain:

```text
SEC / official US reference sources
existing free provider routes
OpenDART for KR
existing safe price routes where available
```

Do not:

```text
add paid provider
upgrade to paid tier
fabricate unavailable price/fundamental fields
lower source sufficiency
```

If a company is fundamentally sufficient but price is safely unavailable under the existing contract:

```text
Directional Core may still be eligible
Price-Timing must use canonical UNAVAILABLE_SAFE / limitation behavior
```

Do not require price readiness merely to enlarge the Directional cohort if the existing source contract does not require it.

---

# 10. Existing scheduled monitoring remains paused

The user explicitly requested the existing US/KR scheduled monitoring remain paused.

Latest observation:

```text
observed_paused_schedule_count = 8
automatic_monitoring_resume = 0
```

Maintain the pause.

If all eight approved paths remain paused:

```text
scheduler mutation = 0
```

If one exact approved path unexpectedly becomes active:

```text
pause only that path
record actual mutation
```

Do not alter unrelated schedules.

Do not automatically resume monitoring after proof success.

---

# 11. Task goal

Perform in this order:

```text
1. verify frozen calibration hashes unchanged
2. verify current exclusion registry / exposed cohort history
3. audit the current US supported-candidate funnel
4. expand the US supported universe generically using existing free official/reference routes
5. freeze a deterministic extended US candidate order before source testing
6. evaluate candidates until 4 source-sufficient unseen US issuers are obtained
7. preserve KR PASS path and select 12 source-sufficient unseen KR issuers
8. create a completely fresh US4 + KR12 source generation/source lock
9. freeze proof inputs
10. execute FIRST/A/B/C
11. require Directional UNSTABLE = 0 for readiness
```

Do not create a separate "selection review only" task after the US target passes.

Continue directly to the fresh real proof in the same task.

---

# 12. Repository provenance gate

Before implementation:

```text
git branch --show-current
git rev-parse HEAD
git status --short
```

Latest result reported:

```text
base_sha =
05c93cf7d768ddb09b670ca74dc69c2b698e5f6b

work_instruction_commit =
45bd95416ee09c494cca0af9cd73f7c1a6fe9f8c

calibration_implementation_commit =
00c65bb233eedad45bf6ecf1805d391ab6be9426

fresh-real implementation_commit reported =
07bb37fb2b4f3de654ff0cceccf8f81ca0a7ce0c
```

Use the actual repository HEAD.

Do not guess from generated summaries if multiple hashes exist.

If unexplained semantic changes affect:

```text
Directional calibration
Price-Timing
ownership
renderer
source sufficiency
prompt/schema
transport/runtime isolation
```

classify before proceeding.

Do not silently reset unrelated work.

---

# 13. Work-instruction commit first

Commit this instruction before implementation.

Record:

```text
new_work_instruction_commit
```

Only then modify the bounded US universe/reference expansion surface if required.

---

# 14. Allowed mutation surface

Allowed:

```text
US official/reference universe ingestion
US canonical security-to-issuer mapping
US reference/security membership adapter
free-route support capability mapping
deterministic unseen-candidate ordering
source-coverage diagnostic/report tooling
tests for the above
fresh proof orchestration
```

Strong preference:

```text
generic official-reference expansion
not manual ticker addition
```

Do not add one-off ticker exceptions merely to obtain US4.

---

# 15. Forbidden mutation surface

Do not change:

```text
Directional calibration contract
Directional thresholds
Directional prompt after calibration freeze
Price-Timing prompt
schemas

source-sufficiency evidence families
accounting attribution
ADR/security basis rules
numeric provenance

model = gpt-5.6-sol
reasoning effort = xhigh
MODEL_CONTEXT_COUPLED
4 subjects/context
1800-second single watchdog

runtime namespace isolation contract
ContinuationTransportAdapter canonical semantics
```

Do not:

```text
majority vote
average repeated model scores
relax stability validator
add paid provider
increase timeout
switch model on capacity
add wrapper auto-retry
```

---

# 16. Phase A — US supported-universe funnel audit

Before expanding, produce a funnel from the current authoritative US reference snapshots.

Count separately:

```text
raw official/reference security rows
normalized securities
identity-resolved securities
routing-supported securities
source-client-supported securities
real-exposure excluded issuers
remaining unseen supported issuers
current bounded candidate list
```

Identify the exact filter responsible for reducing the universe to the latest 9 candidates.

Do not conflate:

```text
officially listed
identity-resolved
routing-supported
price-provider-supported
fundamental-source-supported
source-sufficient
```

These are different concepts.

Required:

```text
US_SUPPORTED_UNIVERSE_FUNNEL_ROOT_CAUSE_CONFIRMED
```

---

# 17. Directional support must not be gated by optional price support

Audit whether candidate membership currently requires a price-provider symbol even when:

```text
fundamental evidence can be built from SEC/free official sources
AND
price can safely be UNAVAILABLE_SAFE
```

If the current supported-universe adapter excludes an otherwise valid issuer only because price support is missing:

```text
classify as
OPTIONAL_PRICE_SUPPORT_OVERCOUPLING
```

A generic correction is allowed only if it preserves:

```text
no fabricated price
no technical claims without data
Price-Timing unavailable-safe behavior
```

Do not weaken fundamental source sufficiency.

---

# 18. Generic US reference expansion

Use existing free/public reference sources already supported by the repository, such as:

```text
SEC issuer/ticker identity data
Nasdaq Trader / exchange reference snapshots
existing verified provider symbol capabilities
```

The exact source depends on the current canonical adapter.

Do not scrape arbitrary websites.

Do not introduce a new paid dataset.

Expand the canonical reference/security membership generically.

Required properties:

```text
canonical issuer identity resolved
canonical security identity resolved
supported exchange/security class
not previously model-exposed
not duplicate issuer/share alias
source clients can attempt official fundamental enrichment
```

Do not declare every raw listing row source-supported.

---

# 19. Security-type and issuer-dedup safety

Preserve:

```text
one issuer counted once for unseen holdout purposes
```

Handle:

```text
multiple share classes
ADRs
units
warrants
SPAC securities
funds/investment companies
foreign private issuers
```

according to existing canonical eligibility rules.

Do not add unsupported security types merely to fill the count.

If an issuer has multiple eligible securities:

```text
deterministically choose the canonical security
```

before source testing.

---

# 20. Freeze extended US candidate order BEFORE source evaluation

After generic membership expansion:

Create:

```text
extended-us-candidate-policy.json
extended-us-candidate-manifest.json
```

The candidate order must be outcome-independent.

Use the repository's existing deterministic selection method / salt lineage where possible.

Do not manually place "good data" companies first after inspecting their source results.

Record:

```text
candidate universe hash
exclusion registry hash
ordering method
selection seed/salt
ordered unseen issuer count
bounded evaluation budget
```

Recommended bounded evaluation budget:

```text
at least 24 unseen US issuers
```

if that many are legitimately available.

Do not require exactly 24 if the canonical eligible universe is smaller.

---

# 21. Source-evaluate US candidates in frozen order

Evaluate source sufficiency in the precommitted order.

Stop once:

```text
4 source-sufficient unseen US issuers
```

have been obtained.

No model calls.

For each attempted candidate report:

```text
identity status
framework
official-profile status
official financial status
required evidence families
source-sufficiency status
price readiness
failure reason
packet hash if eligible
```

Do not continue evaluating extra US candidates after 4 are obtained merely to choose preferable companies.

---

# 22. US failure taxonomy

For source-insufficient candidates distinguish:

```text
TRUE_OFFICIAL_SOURCE_ABSENCE
MISSING_REQUIRED_FUNDAMENTAL_FAMILY
SECURITY_OR_ACCOUNTING_BASIS_BLOCK
NORMALIZATION_OR_MAPPING_GAP
OPTIONAL_PRICE_ONLY_LIMITATION
IDENTITY_OR_SECURITY_CLASS_FAILURE
OTHER_CONFIRMED
UNKNOWN
```

If official data exists but mapping is generically missing:

```text
generic bounded mapping repair may be allowed
```

only if semantically supported.

Do not create ticker-specific mappings.

---

# 23. US target gate

Required:

```text
US source-sufficient unseen count >= 4
```

If the expanded bounded universe still produces fewer than 4:

```text
STOP PRE-MODEL
```

Do not lower source-sufficiency standards.

Produce:

```text
NOT_READY_US_FREE_SOURCE_COVERAGE
```

and a precise remaining-gap report.

---

# 24. KR path

KR already passed in the latest task.

After US passes, select 12 unseen KR issuers from the current deterministic candidate order / updated exposure registry.

Revalidate those 12 during the final fresh source-generation step.

If a selected KR issuer no longer passes due changed source state:

```text
use the precommitted deterministic reserve order
```

Do not launch another full KR universe audit unless the KR target genuinely cannot be restored.

---

# 25. Fresh unseen cohort

Once both targets pass:

```text
US = 4
KR = 12
```

All 16 must:

```text
not exist in real-exposure exclusion registry
have canonical issuer identity
have canonical security identity
pass fundamental source sufficiency
```

Price may be:

```text
READY
PARTIAL_SAFE
UNAVAILABLE_SAFE
```

only under the already-frozen price contract.

---

# 26. Fresh source generation / lock

Create a brand-new:

```text
source generation
ordered issuer manifest
per-issuer DecisionEvidencePacket hashes
source identity audit
source sufficiency audit
aggregate source lock
```

No source packet from a previously exposed cohort may be reused as the fresh proof lock.

Before model execution freeze:

```text
calibration prompt hash
Price-Timing prompt hash
schema hashes
model/runtime identity
namespace isolation identity
```

---

# 27. Real proof precommit

Before first real model call, create an immutable precommit containing:

```text
ordered US4 + KR12
context grouping
source generation
source lock
packet hashes

Directional calibration freeze seal
prompt/schema hashes

model
reasoning effort
timeout
namespace isolation contract

hard gate definitions
stability acceptance
message-quality advisory role
runtime failure rules
```

No semantic edits after real output appears.

---

# 28. FIRST/A/B/C

Run:

```text
FIRST
→ hard gates
→ A
→ hard gates
→ B
→ hard gates
→ C
→ hard gates
```

At first required execution/semantic/renderer/hard-safety failure:

```text
STOP
```

No automatic retry.

No selective continuation.

No architecture hotfix using the same exposed cohort.

---

# 29. Per-context immediate preservation

Before every model spawn:

```text
unique invocation ID
unique runtime namespace
unique working directory
```

After every successful context:

```text
raw output
stdout
stderr/log
receipt
prompt
schema
context manifest
namespace/session/workdir identities
```

must be persisted, hashed and secret-scanned before the next context.

---

# 30. Ownership / renderer / hard-safety gates

Require existing frozen gates for every complete run.

At minimum:

```text
Directional Core uses no price technical refs
Directional Core uses no supply refs
no price-only direction ownership
no BUY/SELL without material non-price anchor

Price-Timing does not mutate direction/balance/HOLD lean
Price-Timing never upgrades new buyer
Price-only path never creates holder REDUCE

final direction owner = Directional Core

primary user action wording owner = renderer
AI imperative primary action = 0

known hard-safety regression = 0
```

No next run until required gates PASS.

---

# 31. Fresh stability acceptance

Formal Directional Core readiness:

```text
UNSTABLE = 0
```

Do not majority-vote runs.

Do not average scores.

Do not weaken the stability validator.

Price-Timing readiness:

```text
UNSTABLE = 0
```

under the unchanged Timing contract.

Report full subject-level repeated results.

---

# 32. Message-quality advisory remains separate

Continue measuring:

```text
cross_ticker_substantive_repetition
repeated substantive span count
affected messages if measurable
```

Do not hide FAIL.

Do not alter renderer wording during the fresh proof after output appears.

If investment proof passes but message quality remains FAIL:

```text
next_scope =
MESSAGE_SPECIFICITY_AND_REPETITION_REMEDIATION
```

before production-message activation unless separately accepted.

---

# 33. Runtime observations

Continue recording:

```text
CLI retry signals
WebSocket disconnects
capacity messages
watchdog timeouts
wrapper retries
orphan processes
```

Do not change transport preemptively.

A disconnect that recovers is recorded as:

```text
RECOVERY_OBSERVED
```

not as universal reliability proof.

A disconnect/capacity/timeout that terminates a context:

```text
STOP
```

No model switch or timeout increase.

---

# 34. Existing monitoring remains paused

Throughout the task:

```text
automatic monitoring resume = 0
```

Observe the approved paused US/KR schedule set.

Do not send production monitoring messages.

Do not mutate registered monitoring theses/history.

---

# 35. Production no-change

Required outside approved pause maintenance:

```text
production DB mutation = 0
production send = 0
monitoring registration change = 0
live V2 activation/change = 0
Night Futures change = 0
paid data service change = 0
```

---

# 36. Monitoring Bootstrap remains out of scope

Do not implement Monitoring Bootstrap in this task.

Possible next scopes:

## Directional stability still fails

```text
GENERIC_DIRECTIONAL_CORE_REASONING_ARCHITECTURE_REVIEW
```

## Investment proof passes but message quality fails

```text
MESSAGE_SPECIFICITY_AND_REPETITION_REMEDIATION
```

## Full proof and accepted message quality pass

```text
Monitoring Bootstrap Integration Review
```

Monitoring remains paused until separately authorized.

---

# 37. Required artifacts — source coverage expansion

Produce at least:

```text
01-repository-provenance
02-latest-result-integrity
03-calibration-freeze-reuse-proof
04-real-exposure-exclusion-registry
05-us-supported-universe-funnel
06-us-supported-universe-root-cause
07-us-reference-expansion-decision
08-us-reference-expansion-diff
09-us-reference-expansion-tests
10-extended-us-candidate-policy
11-extended-us-candidate-manifest
12-us-source-readiness-audit
13-us-source-failure-detail
14-us-target-decision
15-kr-pass-preservation
```

---

# 38. Required artifacts — fresh proof if US target passes

Then produce:

```text
16-fresh-us4-selection
17-fresh-kr12-selection
18-fresh-source-generation
19-source-identity-audit
20-source-sufficiency-audit
21-fresh-source-lock
22-fresh-proof-precommit
23-investment-semantic-freeze
24-runtime-isolation-freeze
25-schedule-pause-observation
```

Then per FIRST/A/B/C:

```text
execution summary
context artifact manifest
per-context semantic audit
ownership gate
renderer gate
hard-safety gate
message-quality advisory
```

---

# 39. Required final artifacts

Produce:

```text
fresh-holdout-exposure-retirement-state
fresh-directional-core-stability
fresh-price-timing-stability
fresh-ownership-generalization
fresh-renderer-ownership-proof
fresh-hard-safety-regression
fresh-message-quality-summary
runtime-reliability-observations
production-no-change
night-futures-no-change
next-scope-handoff
program-completion
```

---

# 40. Program-completion fields

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
final_head_sha
branch

latest_result_zip_sha256
latest_result_integrity

calibration_freeze_reused
calibration_contract_sha256
calibration_freeze_seal_sha256
calibration_semantic_drift

exclusion_registry_count
exclusion_registry_sha256

us_reference_raw_security_count
us_identity_resolved_security_count
us_routing_supported_security_count
us_remaining_unseen_issuer_count

us_bounded_candidate_count
us_attempted_candidate_count
us_source_sufficient_count
us_source_insufficient_count
us_target_status

us_failure_taxonomy_counts
optional_price_support_overcoupling_found
generic_us_reference_expansion_applied
ticker_specific_us_exception_count

kr_target_status
kr_selected_count

fresh_cohort
fresh_source_generation
fresh_source_lock

model
reasoning_effort
batch_semantics
subjects_per_context
timeout
timeout_owner_count

planned_real_contexts
attempted_real_contexts
successful_real_contexts
failed_real_contexts

distinct_real_namespace_count
namespace_collision_count

raw_output_document_count
unique_issuer_output_count

wrapper_retry_count
cli_retry_signal_count
disconnect_count
capacity_failure_count
timeout_count
orphan_count

run_results.first
run_results.a
run_results.b
run_results.c

first_ownership_gate
first_renderer_gate
first_hard_safety_gate
first_message_quality

run_a_ownership_gate
run_a_renderer_gate
run_a_hard_safety_gate
run_a_message_quality

run_b_ownership_gate
run_b_renderer_gate
run_b_hard_safety_gate
run_b_message_quality

run_c_ownership_gate
run_c_renderer_gate
run_c_hard_safety_gate
run_c_message_quality

fresh_core_stability_counts
fresh_timing_stability_counts
fresh_ownership_generalization
fresh_message_quality_status

exposure_state
semantic_revelation_state
retirement_state
future_unseen_reuse_allowed

observed_paused_schedule_count
automatic_monitoring_resume

paid_data_service_change
production_db_mutation
production_send
monitoring_registration_change
live_v2_change
night_futures_change

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count

status
readiness
stop_reason
next_scope
```

Unmeasured values remain:

```text
NOT_MEASURED
```

---

# 41. Artifact integrity

Build the final artifact index only after all final reports are frozen.

Index every payload except the index itself.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final ZIP SHA-256.

---

# 42. Decision matrix

## A. US supported-universe funnel root cause not confirmed

```text
STOP
no real model calls
```

Do not make speculative universe changes.

## B. Expanded US universe still cannot produce 4 source-sufficient unseen issuers

```text
STOP PRE-MODEL

readiness =
NOT_READY_US_FREE_SOURCE_COVERAGE
```

Do not weaken source standards.

## C. US4/KR12 source lock succeeds

Proceed directly to FIRST/A/B/C in the same task.

Do not create another preexecution-review task.

## D. Fresh Directional Core UNSTABLE > 0

```text
NOT_READY_DIRECTIONAL_CORE_STABILITY
```

Preserve/retire the cohort.

Do not majority-vote.

## E. Investment proof passes but message quality fails

```text
READY_FOR_MESSAGE_QUALITY_REMEDIATION
```

## F. All required proof and accepted message-quality conditions pass

```text
READY_FOR_MONITORING_BOOTSTRAP_INTEGRATION_REVIEW
```

Monitoring remains paused.

---

# 43. Final task principle

The Directional calibration repair has already passed its repeated fictional proof.

The latest real proof did not fail because of calibration, transport, model capacity or runtime isolation.

It did not start because the remaining unseen US candidate tail contained only two source-sufficient issuers.

Therefore the correct next move is:

```text
expand the US supported/unseen issuer universe generically
using existing free official/reference routes
→ freeze a larger deterministic candidate order
→ obtain US4 without lowering evidence standards
→ combine with KR12
→ execute the already-frozen fresh generalization proof
```

Not:

```text
re-run fictional calibration
```

Not:

```text
retune the Directional prompt
```

Not:

```text
add paid market-data APIs
```

Not:

```text
manually pick four famous US tickers
```

And not:

```text
create another review-only task after source coverage passes
```

Once US4 is safely available, proceed directly to the real FIRST/A/B/C proof.
