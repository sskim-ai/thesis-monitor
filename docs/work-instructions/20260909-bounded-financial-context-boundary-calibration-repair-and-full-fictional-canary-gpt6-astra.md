# Thesis Monitor — Bounded Financial Context Boundary Calibration Repair & Full Fictional Canary

## 0. Task identity

Suggested work-instruction filename:

```text
20260909-bounded-financial-context-boundary-calibration-repair-and-full-fictional-canary.md
```

Suggested result bundle:

```text
thesis-monitor-20260909-bounded-financial-context-boundary-calibration-repair-full-fictional-canary-report.zip
```

Master-workflow phase:

```text
M12E — Bounded Financial Context Boundary Calibration Repair
        + Full Fictional Canary
```

This task begins only after M12S completed the model-free stability review and froze:

```text
preferred_next_repair =
BOUNDED_FINANCIAL_CONTEXT_BOUNDARY_CALIBRATION_REPAIR

next_scope =
BOUNDED_FINANCIAL_CONTEXT_BOUNDARY_CALIBRATION_REPAIR_AND_FULL_FICTIONAL_CANARY
```

M12S found:

```text
true semantic variance subject count = 0

adjacent balance-bucket calibration ambiguity = 3 subjects

independent new-buyer stance ambiguity = 2 subjects

independent holder stance ambiguity = 1 subject

formal classifier change required = false
```

M12E implements ONLY the primary Directional balance/bucket calibration clarification.

M12E must NOT:

```text
change BUY/SELL threshold
change 0.5 balance increments
change HOLD lean mapping
change the existing conservative tie-break direction
change new-buyer stance contract
change holder stance contract
change formal stability classifier
change financial-context selector
change first-class typed evidence architecture
change financial mappings
change financial semantic validators
change QTD/YTD validator
change working-capital validator
change output schema
change Price-Timing
change renderer substantive ownership
run a real issuer proof
merge/deploy production
resume monitoring schedules
```

After deterministic validation, M12E reruns the same frozen 8-subject fictional canary under a NEW generation.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260909-bounded-directional-financial-context-stability-review-report.zip
```

Verified SHA-256:

```text
d9533246a23d7fc6eab6da6637f1254a8748f7e37aa45252840566df5602f3de
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Latest M12S state:

```text
M12D =
full fictional semantic proof complete
but formal stability FAIL

M12S =
COMPLETE

fresh real proof readiness =
NOT_READY

production readiness =
NOT_READY
```

Reported M12S provenance:

```text
base_sha =
ba511cd1c46a6b3fb0758f56eafa3f190af50884

work_instruction_commit =
1c50d011d9bb12665fb0b5075eff1077c5557ea4

implementation_commit =
8dfe7dadb5f90375d547bd55217437c6420658a2

final/report =
76f882803af3f5e05bc0d31e4e7a30b2f41a8dbf

branch =
codex/20260909-bounded-directional-financial-context-stability-review
```

Use actual repository HEAD as authority at task start.

Latest artifact integrity:

```text
indexed payloads = 38
missing = 0
extra = 0
hash mismatch = 0
size mismatch = 0
```

---

# 2. M12D financial semantic result is frozen

Do not reopen financial-ingestion or grounding work.

M12D full 24-output proof already established:

```text
model contexts = 6 / 6 successful
schema = 24 / 24 PASS

invalid financial refs = 0
hard financial semantic violations = 0

selected typed refs = 45
first-class typed refs = 45
used typed refs = 45

material financial anchor grounding failures = 0
working-capital grounding failures = 0
narrative substitution failures = 0

QTD/YTD false reject = 0
QTD/YTD false accept = 0

price/technical/supply Directional violations = 0
AI imperative primary action = 0

timeout = 0
capacity failure = 0
orphan = 0
wrapper retry = 0
```

M12E is not a financial parser / evidence architecture task.

---

# 3. M12S true-semantic-variance decision is frozen

M12S concluded:

```text
NO_TRUE_SEMANTIC_VARIANCE_FOUND
```

Across repeated outputs:

```text
economic polarity remained stable

material financial domains remained stable

business-thesis economics remained stable

Unknown/limitation treatment did not materially reverse

presentation / anchor emphasis varied
but not enough to constitute material economic interpretation change
```

Do not implement an evidence-interpretation repair in M12E.

---

# 4. Formal M12D stability result remains frozen

Historical M12D formal result:

```text
STABLE = 4
BOUNDARY_UNCERTAINTY = 3
UNSTABLE = 1
opposite direction reversal = 0
```

Per subject:

```text
FIC-FIN-01 = BOUNDARY_UNCERTAINTY
FIC-FIN-02 = UNSTABLE
FIC-FIN-03 = STABLE
FIC-FIN-04 = BOUNDARY_UNCERTAINTY
FIC-FIN-05 = BOUNDARY_UNCERTAINTY
FIC-FIN-06 = STABLE
FIC-FIN-07 = STABLE
FIC-FIN-08 = STABLE
```

Do not rewrite this historical result.

M12E uses a new generation after repair.

---

# 5. Primary M12S root cause

M12S assigned:

```text
FIC-FIN-01 =
ADJACENT_BALANCE_BUCKET_CALIBRATION_AMBIGUITY

FIC-FIN-02 =
ADJACENT_BALANCE_BUCKET_CALIBRATION_AMBIGUITY

FIC-FIN-04 =
ADJACENT_BALANCE_BUCKET_CALIBRATION_AMBIGUITY

FIC-FIN-05 =
HOLDER_STANCE_CALIBRATION_AMBIGUITY
```

Primary repair count:

```text
adjacent bucket = 3
```

Independent secondary ambiguities:

```text
new-buyer stance = FIC-FIN-01, FIC-FIN-02

holder stance = FIC-FIN-05
```

Do not merge the stance ambiguities into M12E unless a direct generic coupling is newly proven by deterministic source inspection.

Default:

```text
new-buyer stance contract = frozen
holder stance contract = frozen
```

---

# 6. Frozen Directional ordinal contract

Current threshold and increment remain:

```text
BUY if buy >= 6.0

SELL if sell >= 6.0

otherwise HOLD

balance increment = 0.5
```

HOLD lean remains:

```text
5.5:4.5 = BUY_LEAN
5.0:5.0 = NEUTRAL
4.5:5.5 = SELL_LEAN
```

Existing ordinal meanings:

```text
5.0 =
balanced, unresolved,
or too incomplete for even a lean

5.5 =
issuer-level material anchor exists,
but sufficient corroboration/currentness/persistence/valuation/
critical KPI support is missing

6.0 =
minimum directional conclusion;
requires material anchor
plus sufficiently current or independent corroboration

6.5+ =
stronger corroboration,
persistence,
quality,
visibility,
or valuation support
beyond the minimum directional conclusion
```

Apply symmetrically to the negative side.

---

# 7. Existing conservative tie-break remains

Frozen rule:

```text
when supplied evidence reasonably fits two adjacent buckets,
choose the less-directional bucket toward 5.0
```

M12E does NOT introduce a new tie-break.

M12E clarifies when the financial-context patterns actually fit adjacent buckets.

Required:

```text
tie-break direction = unchanged

threshold = unchanged

increment = unchanged
```

---

# 8. M12S ambiguity 1 — shared-lineage corroboration

M12S found that richer financial context can expose multiple metrics that share the same underlying source economics.

Example:

```text
operating cash flow

OCF less identified PPE cash-conversion proxy
```

These may be two useful representations,
but they are not automatically two independent corroborating economic facts.

Freeze generic clarification equivalent to:

```text
Corroboration is independent only when it adds a distinct economic fact.

Multiple financial metrics sharing the same underlying cash-flow inputs,
derivation lineage, or substantially overlapping source basis
do not automatically satisfy the independent-corroboration requirement
for moving from a 5.5 lean to a 6.0 directional bucket.
```

This rule must be direction-neutral.

Do not count evidence numerically.

No scorecard.

---

# 9. M12S ambiguity 2 — corroboration vs limiting Unknowns

M12S found no explicit ordinal rule for:

```text
corroborating facts
vs
unresolved causal / persistence limitations
```

Freeze clarification equivalent to:

```text
When a material anchor and supporting evidence exist,
but an unresolved causal, reversibility, persistence, or critical-validation limit
leaves both adjacent buckets reasonably supportable,
apply the existing conservative tie-break toward 5.0.

An Unknown does not mechanically create negative evidence.

Its role is to limit the justified directional strength
when the unresolved issue is material to persistence or causality.
```

No formula.

No mandatory HOLD from LOW confidence.

---

# 10. M12S ambiguity 3 — flat operations + non-operating support

Freeze generic clarification:

```text
A non-operating or financial effect that improves reported net income
while underlying operating performance remains broadly flat
is context explaining earnings composition.

By itself it is not sufficient support for a positive 5.5 lean.

A positive lean requires a separate positive operating/business,
cash-conversion, balance-sheet, valuation,
or other issuer-level material anchor.
```

Apply symmetrically where appropriate.

Do not classify all non-operating gains as low quality or one-off.

---

# 11. M12S ambiguity 4 — minimum BUY vs 6.5+

Freeze generic clarification:

```text
6.0 is the minimum directional bucket.

6.5 or stronger requires evidence beyond minimum direction support
that materially improves persistence, quality, visibility,
independent corroboration, or valuation support.

If valuation, persistence, or another material confirmation remains unresolved
such that both 6.0 and 6.5 are reasonably supportable,
use the existing tie-break and choose 6.0.
```

Symmetric negative rule:

```text
6.5 SELL strength likewise requires stronger persistence/quality/
visibility/corroboration beyond the minimum SELL threshold.
```

---

# 12. Do not turn the contract into a scorecard

Hard prohibition:

```text
number of positive anchors = bucket

OCF = +0.5

net debt = +0.5

Unknown = -0.5

two refs = corroboration

three refs = 6.5
```

Required:

```text
fixed_weight_financial_score_rule_count = 0

evidence_count_bucket_rule_count = 0
```

The ordinal decision remains holistic.

---

# 13. FIC-FIN-01 generic boundary pattern

Historical pattern:

```text
strong operating improvement

positive cash conversion

positive resilience

valuation / persistence remain Unknown

observed:
6.5
6.0
6.5
```

M12S classified:

```text
ADJACENT_BALANCE_BUCKET_CALIBRATION_AMBIGUITY
```

Under the frozen clarification,
when:

```text
minimum BUY support is established

but stronger valuation/persistence support remains unresolved

and 6.0 / 6.5 are both reasonably supportable
```

the existing tie-break implies:

```text
6.0
```

Do not create a ticker-specific instruction.

This is a generic pattern fixture.

---

# 14. FIC-FIN-02 generic boundary pattern

Historical pattern:

```text
positive operating/accounting growth

negative operating cash flow / cash-conversion evidence

OCF and OCF-less-PPE share substantial source lineage

working capital is context

cause / reversibility remain Unknown

observed:
SELL 4.0:6.0
HOLD 4.5:5.5 SELL_LEAN
SELL 4.0:6.0
```

M12S found:

```text
same economic semantic flags across all 3

business thesis = WEAKENED across all 3

same primary cash-conversion domain

material interpretation delta = MINOR_EMPHASIS_DELTA
```

Under the frozen clarification,
if the negative case lacks independent corroboration
and causal/reversibility limits keep both negative 5.5 and 6.0 reasonably supportable,
the existing tie-break implies:

```text
HOLD 4.5:5.5 SELL_LEAN
```

Do not hard-code FIC-FIN-02.

Use a generic cash-conversion-divergence calibration fixture.

---

# 15. FIC-FIN-04 generic boundary pattern

Historical pattern:

```text
underlying operations broadly flat

reported net income improves

financial/non-operating effect contributes materially

recurrence/operating acceleration unresolved

observed:
5.0
5.0
5.5 BUY_LEAN
```

M12S classified:

```text
ADJACENT_BALANCE_BUCKET_CALIBRATION_AMBIGUITY
```

Under the frozen clarification:

```text
non-operating support alone does not create positive lean
```

Therefore when no separate positive issuer-level operating/business/cash/
balance-sheet/valuation anchor exists:

```text
5.0 NEUTRAL
```

is the generic contract outcome.

No ticker-specific branch.

---

# 16. FIC-FIN-05 remains outside the primary repair

Historical:

```text
SELL 4.0:6.0 all 3

business thesis WEAKENED all 3

new buyer AVOID all 3

same debt/liquidity anchors

holder:
REDUCE
REVIEW
REDUCE
```

M12S classified:

```text
HOLDER_STANCE_CALIBRATION_AMBIGUITY
```

M12E must NOT change:

```text
HOLDABLE / REVIEW / REDUCE
```

contract.

Measure it again in the new canary.

If it persists after core balance stabilization:

```text
next scope may be BOUNDED_HOLDER_STANCE_CALIBRATION_REPAIR
```

---

# 17. New-buyer stance remains outside the primary repair

M12S found independent ambiguity:

```text
FIC-FIN-01:
ATTRACTIVE / WAIT / ATTRACTIVE

FIC-FIN-02:
AVOID / WAIT / WAIT
```

Current contract is under-specified for:

```text
balance strength

valuation Unknown

confirmation requirement
```

M12E does NOT repair:

```text
ATTRACTIVE / WAIT / AVOID
```

Measure repeated new-buyer stance again.

If it remains variable after balance stabilization:

```text
next scope may be BOUNDED_NEW_BUYER_STANCE_CALIBRATION_REPAIR
```

Do not hide this variance.

---

# 18. Formal stability classifier remains frozen

M12S decision:

```text
KEEP
```

The classifier correctly detects output instability.

Required:

```text
formal_classifier_change_count = 0
```

Do not weaken it to turn historical M12D into PASS.

Use the same classifier on the new M12E generation.

---

# 19. Implementation owner

M12S identified current bucket contract owner:

```text
app/services/directional_balance_service.py
```

Use actual repository code as authority.

Preferred repair:

```text
bounded generic ordinal guidance clarification
```

not:

```text
new scoring engine
new bucket calculator
ticker rules
financial-domain hard table
```

If the same contract is duplicated in another model-facing prompt/service,
centralize only if the current architecture already supports one canonical owner
and the change is semantic-neutral.

Do not broaden refactoring scope unnecessarily.

---

# 20. Prompt/change scope

M12E is expected to change the Directional balance calibration guidance.

Allowed changes ONLY:

```text
shared-lineage corroboration clarification

corroboration vs limiting Unknown clarification

flat operations + non-operating context clarification

6.0 vs 6.5 minimum/stronger-direction clarification
```

Do not change:

```text
financial grounding instructions

QTD/YTD wording

working-capital grounding

missing-data nonnegative rule

financial-sector routing

ownership rules

price/supply exclusion
```

Produce line-by-line semantic diff.

---

# 21. Deterministic generic calibration fixtures

Before model calls, create contract fixtures that are not keyed to fictional tickers.

At minimum:

## CAL-BAL-01 — strong quality, unresolved stronger confirmation

```text
positive operating anchor
positive cash conversion
positive resilience
valuation/persistence material Unknown

expected ordinal contract:
minimum directional positive = 6.0
not 6.5 when adjacent fit remains unresolved
```

## CAL-BAL-02 — negative cash conversion with shared lineage

```text
healthy demand/accounting growth
negative OCF
negative OCF-less-PPE from same underlying cash-flow lineage
cause/reversibility Unknown

expected:
negative lean 5.5
rather than minimum SELL 6.0
when no distinct corroborating negative fact exists
```

## CAL-BAL-03 — flat operations + non-operating support

```text
operations flat
net income helped by financial/non-operating effect
recurrence Unknown
no separate positive operating/business/cash/balance/valuation anchor

expected:
5.0 NEUTRAL
not BUY_LEAN 5.5
```

## CAL-BAL-04 — independent corroboration

```text
material anchor
plus a distinct independent supporting economic fact
no material causal/persistence blocker

expected:
6.0 may be supported
```

## CAL-BAL-05 — stronger 6.5 support

```text
minimum-direction case
plus materially stronger persistence/visibility/valuation/independent corroboration

expected:
6.5 may be supported
```

These are contract tests/documented expectations,
not a deterministic numeric scorecard.

---

# 22. Symmetry fixtures

Add negative-side symmetric contract cases.

At minimum:

```text
negative lean 5.5

minimum SELL 6.0

stronger SELL 6.5
```

Ensure the text is direction-symmetric.

Do not accidentally make negative buckets easier/harder than positive buckets.

---

# 23. Conservative tie-break deterministic audit

Add a model-free calibration audit over the generic fixtures.

Required:

```text
if two adjacent buckets are explicitly marked reasonably supportable
→ selected contract bucket = less directional toward 5.0
```

This may be a rule/fixture audit,
not a model output generator.

No automatic investment scoring.

---

# 24. Frozen evidence architecture regressions

Before model calls prove unchanged:

```text
FIRST_CLASS_TYPED_EVIDENCE_UNIFICATION

selected-only typed projection

alias reuse

financial_decision_context detail block

material-claim scoped working-capital grounding

QTD/YTD validator

other financial semantic validators
```

Required:

```text
financial_context_selection_change_count = 0

first_class_projection_change_count = 0

working_capital_validator_semantic_change_count = 0

qtd_ytd_validator_semantic_change_count = 0

other_financial_validator_change_count = 0
```

---

# 25. Source sufficiency / lifecycle / warnings frozen

Required:

```text
source_sufficiency_semantic_change_count = 0

daily_delta_semantic_change_count = 0

monitoring_lifecycle_semantic_change_count = 0

warning_semantic_change_count = 0
```

No provider/source work.

---

# 26. Price-Timing / renderer frozen

Required:

```text
price_timing_prompt_change_count = 0

price_timing_semantic_change_count = 0

renderer_substantive_change_count = 0
```

Primary action wording remains renderer-owned.

No AI imperative primary action.

---

# 27. New-buyer / holder contracts frozen

Required:

```text
new_buyer_stance_contract_change_count = 0

holder_stance_contract_change_count = 0
```

M12E measures these fields but does not repair them.

---

# 28. Phase A gate

Before model calls require:

```text
calibration contract diff limited to allowed four clarification areas

threshold unchanged

increment unchanged

HOLD lean unchanged

tie-break direction unchanged

no scorecard/count rule

generic calibration fixtures PASS

symmetry fixtures PASS

tie-break deterministic audit PASS

first-class financial architecture regressions PASS

financial validators unchanged

new-buyer/holder contracts unchanged

source sufficiency unchanged

Daily Delta unchanged

Price-Timing unchanged

renderer ownership unchanged

focused pytest PASS

full repository pytest PASS

ruff PASS

git diff --check PASS

production side-effect firewall PASS
```

If any fail:

```text
STOP
NO_MODEL_CALLS
```

---

# 28A. Model transition — authoritative from this task forward

The user has changed the project model target from the prior runtime to:

```text
Codex / implementation-reasoning target =
gpt-6 astra

AI investment-judgment / fictional-canary target =
gpt-6 astra

reasoning effort =
xhigh
```

This model change is intentional and becomes the default for subsequent Thesis Monitor
Codex work and AI judgment/model-validation tasks unless the user explicitly changes it again.

Hard rules:

```text
no silent fallback to gpt-5.6-sol

no silent fallback to another GPT model

no automatic downgrade of reasoning effort

if gpt-6 astra / xhigh is unavailable or rejected by the runtime:
STOP
MODEL_TARGET_UNAVAILABLE
```

Do not translate the requested target into another model identifier without explicit user authorization.

Because the model family/runtime target changes at M12E:

```text
all post-change model calls must use a NEW generation/runtime identity

pre-change gpt-5.6-sol outputs remain historical evidence

pre-change and post-change results may be compared descriptively,
but must not be described as same-model regression evidence

formal post-change stability must be measured entirely on the new gpt-6 astra generation
```

The M12S root-cause analysis remains valid as an analysis of the preserved pre-change outputs,
but M12E must independently validate the repaired calibration under `gpt-6 astra / xhigh`.

Required completion fields:

```text
implementation_model_target = gpt-6 astra
investment_judgment_model_target = gpt-6 astra
reasoning_effort = xhigh
model_target_fallback_count = 0
model_target_availability_status
cross_model_same_runtime_claim_count = 0
```

If the Codex execution environment has a separate model selector from the investment-judgment
runner, configure both to the requested target where supported. Do not claim the Codex executor
used `gpt-6 astra / xhigh` unless the execution receipt/runtime evidence confirms it.


# 29. Full fictional canary — new generation

Only after Phase A PASS.

Create a NEW generation ID.

Use the exact same frozen 8 subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Do not alter case values/evidence/narrative.

Do not reuse M12D outputs in the new formal sample.

---

# 30. Canary topology

Run:

```text
8 subjects

2 shared contexts

4 subjects per context

3 repetitions
```

Total intended:

```text
6 fictional model calls

24 subject outputs
```

No real issuers.

No judge calls.

---

# 31. Frozen runtime

Use exactly the new user-authorized runtime:

```text
model = gpt-6 astra

reasoning = xhigh

runtime mode = MODEL_CONTEXT_COUPLED

subjects per context = 4

timeout = 1800 seconds

single authoritative watchdog = true

wrapper auto-retry = 0

batch split = 0
```

Each context:

```text
unique invocation ID

unique runtime namespace

unique working directory

unique session identity
```

---

# 32. Whole-generation stop rule

On any hard semantic failure:

```text
stop generation

preserve outputs

do not selectively continue later contexts
```

On runtime failure:

```text
classify separately

no wrapper retry
```

Any post-repair rerun requires another new generation.

Do not stitch samples.

---

# 33. Hard semantic gates remain

Across completed outputs require zero:

```text
invalid financial evidence refs

material financial grounding failures

true working-capital grounding failures

narrative substitution failures

price/technical/supply Directional refs

partial PPE called FCF

prior-year-end called YoY

partial debt called total debt

total liabilities called debt

normalized/adjusted earnings invention

financial-sector generic misuse

missing optional evidence treated as bearish

fixed financial score behavior

QTD/YTD false reject

QTD/YTD false accept

AI imperative primary action
```

---

# 34. Primary M12E calibration targets

The new canary must specifically audit these generic patterns.

## Strong quality + unresolved stronger confirmation

For the frozen strong-quality case,
record whether each repetition uses:

```text
6.0
or
6.5
```

and whether the output's explicit evidence satisfies the new stronger-bucket condition.

Do not pass a 6.5 output merely because BUY direction is stable
if the output does not establish the stronger required support.

## Cash-conversion divergence

For the frozen cash-divergence case,
record:

```text
shared-lineage corroboration treatment

causal/reversibility Unknown treatment

5.5 vs 6.0 negative bucket
```

If the output selects 6.0,
the audit must identify the distinct independent corroborating negative fact
or explain why the case no longer reasonably fits the adjacent 5.5 bucket.

## Flat operations + non-operating effect

Record whether:

```text
5.0 vs 5.5
```

matches the clarified contract.

A non-operating boost alone must not justify BUY_LEAN.

---

# 35. Core balance stability hard target

M12E's primary repair succeeds only if the three targeted adjacent-bucket subjects no longer show unexplained repeated-run balance variance.

Required:

```text
FIC-FIN-01 directional_balance unique count = 1

FIC-FIN-02 directional_balance unique count = 1

FIC-FIN-04 directional_balance unique count = 1
```

and:

```text
FIC-FIN-02 overall_direction unique count = 1
```

The stable bucket must also satisfy the clarified generic contract.

Do not accept stable-but-contract-inconsistent results.

---

# 36. Other core cases remain regressions

Required:

```text
FIC-FIN-03 core direction/balance semantics remain valid

FIC-FIN-05 SELL 4.0:6.0 pattern does not regress due unrelated balance repair

FIC-FIN-06 working-capital focal case remains valid

FIC-FIN-07 missing optional context remains nonnegative

FIC-FIN-08 financial-sector exclusion remains valid
```

No case-specific prompt rules.

---

# 37. New-buyer stance is measured, not repaired

For every repetition report:

```text
fundamental_new_buyer.stance
```

Specifically track:

```text
FIC-FIN-01

FIC-FIN-02
```

Do not fail the boundary-repair implementation merely because
new-buyer stance remains independently ambiguous
unless the current formal stability classifier correctly classifies that variance.

But:

```text
fresh_real_proof_readiness
```

must remain NOT_READY if material new-buyer stance instability remains unresolved.

---

# 38. Holder stance is measured, not repaired

Track:

```text
FIC-FIN-05
HOLDABLE / REVIEW / REDUCE
```

Do not alter the holder contract.

If holder stance remains variable while core balance/direction is stable:

```text
classify as remaining independent holder stance ambiguity
```

and schedule it as a separate next repair before fresh real proof.

---

# 39. Formal stability classifier remains unchanged

Run the SAME classifier after all 6 calls complete.

Report:

```text
STABLE

BOUNDARY_UNCERTAINTY

UNSTABLE
```

No majority vote.

No balance averaging.

No classifier change.

Required:

```text
opposite_direction_reversal_count = 0
```

Historical M12D result remains frozen.

---

# 40. Add core-only stability view

In addition to the unchanged formal classifier,
produce a descriptive model-free view:

```text
CORE_BALANCE_DIRECTION_STABLE

CORE_BALANCE_DIRECTION_UNSTABLE
```

based only on:

```text
overall_direction

directional_balance

hold_lean
```

This does NOT replace the formal classifier.

It is used to determine whether M12E repaired its intended target
while stance ambiguity remains separately visible.

---

# 41. Fingerprint audit remains required

Reuse the M12S deterministic fingerprint methodology on the new 24 outputs.

Report:

```text
material interpretation delta

economic semantic flags

material anchor domains

Unknown treatment

balance bucket

new-buyer stance

holder stance
```

Do not use an LLM for the fingerprint.

This proves whether any new instability is semantic vs calibration.

---

# 42. Message specificity advisory

If full canary completes,
run the existing advisory.

Measure:

```text
typed financial anchor specificity

period specificity

case-specific checkpoint

generic substantive repetition

renderer-introduced repetition
```

Do not change messages in M12E solely for advisory quality.

---

# 43. No real issuer calls

Required:

```text
model_calls_real = 0

real_issuer_model_exposure_count = 0
```

Fresh real proof remains a later phase.

---

# 44. Production side-effect firewall

Required:

```text
provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Observe approved 8 paused monitoring paths at start/end.

---

# 45. Required artifacts — provenance / repair contract

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12e-scope-freeze

04-m12s-root-cause-reuse-proof

05-directional-bucket-contract-before-after

06-shared-lineage-corroboration-contract

07-limiting-unknown-ordinal-contract

08-non-operating-lean-boundary-contract

09-minimum-vs-strong-direction-contract

10-calibration-symmetry-contract
```

---

# 46. Required deterministic fixture artifacts

Produce:

```text
11-cal-bal-01-strong-quality-boundary

12-cal-bal-02-cash-conversion-divergence

13-cal-bal-03-flat-operations-non-operating

14-cal-bal-04-independent-corroboration

15-cal-bal-05-stronger-6-5-support

16-negative-side-symmetry-fixtures

17-conservative-tiebreak-deterministic-audit

18-scorecard-negative-control
```

---

# 47. Required freeze/no-change artifacts

Produce:

```text
19-first-class-financial-evidence-freeze-proof

20-financial-selector-freeze-proof

21-working-capital-validator-freeze-proof

22-qtd-ytd-validator-freeze-proof

23-other-financial-validator-freeze-proof

24-new-buyer-contract-freeze-proof

25-holder-contract-freeze-proof

26-formal-classifier-freeze-proof

27-price-timing-no-change-proof

28-source-sufficiency-no-change-proof

29-daily-delta-no-change-proof

30-renderer-ownership-no-change-proof
```

---

# 48. Required deterministic validation artifacts

Produce:

```text
31-focused-test-results

32-full-test-results

33-ruff-and-diff-results

34-model-call-gate
```

---

# 49. Required fictional-canary artifacts

If Phase A passes:

```text
35-fictional-canary-generation-manifest

36-fictional-canary-source-lock

37-run-1-context-01

38-run-1-context-02

39-run-2-context-01

40-run-2-context-02

41-run-3-context-01

42-run-3-context-02

43-full-fictional-semantic-audit

44-full-fictional-core-balance-calibration-audit

45-full-fictional-grounding-audit

46-full-fictional-formal-stability

47-full-fictional-core-only-stability

48-full-fictional-fingerprint-audit

49-full-fictional-stance-variance-audit

50-full-fictional-message-specificity-advisory

51-runtime-observations
```

Preserve raw:

```text
prompt
schema
output
receipt
transport log
run document
```

for every attempted model call.

---

# 50. Required completion / next-scope artifacts

Produce:

```text
52-boundary-repair-success-decision

53-new-buyer-stance-followup-decision

54-holder-stance-followup-decision

55-fresh-real-proof-readiness-decision

56-production-no-change

57-schedule-pause-observation

58-master-workflow-update

59-program-completion
```

---

# 51. M12E deterministic acceptance criteria

Pass Phase A only if:

```text
only the bounded ordinal calibration guidance changed

6.0 threshold unchanged

0.5 increment unchanged

HOLD lean unchanged

existing tie-break direction unchanged

shared-lineage metrics not automatically independent corroboration

limiting Unknowns constrain strength without becoming negative evidence

flat operations + non-operating boost alone does not create positive lean

6.5+ requires stronger support beyond minimum direction

positive/negative symmetry preserved

no fixed scorecard

no evidence-count bucket rule

first-class financial architecture unchanged

financial validators unchanged

new-buyer/holder contracts unchanged

formal classifier unchanged

source sufficiency unchanged

Daily Delta unchanged

focused/full tests PASS

ruff PASS

git diff --check PASS
```

---

# 52. M12E full-canary primary acceptance criteria

The primary boundary repair succeeds only if:

```text
6 / 6 model contexts complete

24 / 24 rows schema PASS

hard financial semantic violations = 0

FIC-FIN-01 directional_balance unique count = 1

FIC-FIN-02 directional_balance unique count = 1

FIC-FIN-02 overall_direction unique count = 1

FIC-FIN-04 directional_balance unique count = 1

targeted stable buckets are consistent with the clarified generic contract

price/technical/supply violations = 0

grounding failures = 0

QTD/YTD false rejects = 0

QTD/YTD false accepts = 0

AI imperative primary action = 0

wrapper retry = 0

unexpected timeout/capacity/orphan = 0
```

The formal classifier must still be measured,
but formal `BOUNDARY_UNCERTAINTY` from an independent stance field
does not by itself mean the primary balance repair failed.

It DOES keep fresh-real readiness blocked until the stance issue is separately resolved.

---

# 53. Fresh-real readiness logic

## A. Core balance repair passes AND all formal fields stable

Set:

```text
fresh_real_proof_readiness = READY

next_scope =
FRESH_REAL_FINANCIAL_CONTEXT_GENERALIZATION_PROOF
```

Only if:

```text
no material new-buyer ambiguity remains

no material holder ambiguity remains

formal UNSTABLE = 0

formal unresolved BOUNDARY_UNCERTAINTY = 0
or any remaining advisory variance is explicitly outside the frozen formal contract
```

Do not loosen the formal contract in M12E.

## B. Core balance repair passes but new-buyer stance remains unstable

Set:

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
BOUNDED_NEW_BUYER_STANCE_CALIBRATION_REPAIR
```

If holder ambiguity also remains independently:

```text
record it as later follow-up
```

Do not combine unless one generic contract is proven.

## C. Core balance repair passes but holder stance remains unstable

Set:

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
BOUNDED_HOLDER_STANCE_CALIBRATION_REPAIR
```

## D. Both independent stance ambiguities remain

Prefer sequential repair.

Default order:

```text
1. NEW_BUYER stance
2. HOLDER stance
```

unless the new canary proves one is clearly more material or more broadly recurrent.

## E. Core balance repair fails

Set:

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
BOUNDED_FINANCIAL_CONTEXT_BOUNDARY_CALIBRATION_REPAIR_V2
```

or a more precise root-cause scope based on the new fingerprint audit.

Do not change threshold automatically.

---

# 54. Production readiness

Even if M12E fully stabilizes all fictional fields:

```text
production_readiness = NOT_READY
```

Still required:

```text
fresh unseen real generalization proof

production integration review

explicit user authorization
```

Monitoring schedules remain paused.

---

# 55. Program-completion fields

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
report_commit
final_head_sha
branch

latest_result_zip_sha256
latest_result_integrity

m12s_status
m12e_status

boundary_calibration_repair_status

directional_threshold_changed
directional_increment_changed
hold_lean_contract_changed
calibration_tiebreak_direction_changed

shared_lineage_corroboration_contract_status
limiting_unknown_ordinal_contract_status
non_operating_lean_boundary_contract_status
minimum_vs_strong_direction_contract_status
calibration_symmetry_status

fixed_weight_financial_score_rule_count
evidence_count_bucket_rule_count

financial_context_selection_change_count
first_class_projection_change_count

working_capital_validator_semantic_change_count
qtd_ytd_validator_semantic_change_count
other_financial_validator_change_count

new_buyer_stance_contract_change_count
holder_stance_contract_change_count
formal_classifier_change_count

source_sufficiency_semantic_change_count
daily_delta_semantic_change_count
warning_semantic_change_count

fictional_generation_id
fictional_subject_count
fictional_context_count
fictional_repetition_count

implementation_model_target
investment_judgment_model_target
reasoning_effort
model_target_availability_status
model_target_fallback_count
cross_model_same_runtime_claim_count

model_calls_real
model_calls_fictional
model_calls_judge

model_context_success_count
model_context_failure_count
wrapper_retry_count
timeout_count
capacity_failure_count
orphan_process_count

fictional_output_row_count
fictional_schema_pass_count

hard_financial_semantic_violation_count
invalid_financial_reference_count
grounding_failure_count

fic_fin_01_directional_balance_unique_count
fic_fin_02_directional_balance_unique_count
fic_fin_02_direction_unique_count
fic_fin_04_directional_balance_unique_count

target_bucket_contract_violation_count

formal_stable_count
formal_boundary_uncertainty_count
formal_unstable_count
opposite_direction_reversal_count

core_only_stable_count
core_only_unstable_count

new_buyer_stance_variance_subject_count
holder_stance_variance_subject_count

message_specificity_advisory_status

real_issuer_model_exposure_count

provider_source_fetches

production_db_mutations
monitoring_registrations
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count

fresh_real_proof_readiness
production_readiness
status
stop_reason
next_scope
```

Anything not measured must remain:

```text
NOT_MEASURED
```

---

# 56. Artifact integrity

Freeze:

```text
program completion
master workflow
all deterministic reports
all model-call artifacts
```

before final artifact index.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final result ZIP SHA-256.

---

# 57. Final task principle

M12S found the remaining core Directional issue is not financial-data interpretation.

It is:

```text
the same economic evidence is being placed inconsistently
into adjacent ordinal balance buckets.
```

The correct M12E repair is:

```text
clarify what counts as independent corroboration

clarify how limiting Unknowns constrain directional strength

clarify flat operations + non-operating support

clarify minimum 6.0 vs stronger 6.5+

preserve the existing tie-break toward 5.0

preserve threshold/increment

rerun the full 8 × 3 fictional canary

separate core balance success
from independent new-buyer/holder stance ambiguity
```

Not:

```text
change the 6.0 threshold
```

Not:

```text
change 0.5 increments
```

Not:

```text
average repeated model outputs
```

Not:

```text
majority-vote BUY/HOLD/SELL
```

Not:

```text
add a fixed financial scorecard
```

Not:

```text
repair new-buyer and holder stance in the same patch
without a shared generic root cause
```

Not:

```text
run a real issuer cohort before fictional stability is fully resolved
```

And not:

```text
resume production monitoring
```

Stabilize the ordinal balance contract first.
