# Thesis Monitor — Directional Financial Context Consumption & Specificity Implementation

## 0. Task identity

Suggested work-instruction filename:

```text
20260909-directional-financial-context-consumption-and-specificity-implementation.md
```

Suggested result bundle:

```text
thesis-monitor-20260909-directional-financial-context-consumption-specificity-implementation-report.zip
```

Master-workflow phase:

```text
M12 — Directional Financial Context Consumption & Specificity Implementation
```

This task begins only after M11 has completed.

M11 completed the six-domain accounting-evidence layer and classified readiness as:

```text
READY_WITH_KNOWN_OPTIONAL_GAPS
```

M12 is the first phase where selected `financial_context` is intentionally exposed to Directional Core.

M12 must:

```text
activate a deterministic compact financial decision context
for Directional Core only

implement the frozen M4 Directional specificity contract

preserve all existing direction/balance calibration thresholds

run full deterministic regression

then run a bounded fictional model canary
to verify the model actually uses the richer evidence safely
```

M12 must NOT:

```text
change Price-Timing ownership
change renderer ownership
change source sufficiency
activate new warnings
run a fresh real holdout
merge/deploy production
resume monitoring schedules
```

If M12 model canary passes, the next expected scope is a fresh real generalization proof.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260909-non-operating-financial-income-effects-financial-domain-mapping-implementation-report.zip
```

Verified SHA-256:

```text
0d1c45dea755076649b2d564b3da948def7509993c681d5030dfe1e3008a7173
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Latest M11 state:

```text
M1 = COMPLETE
M2 = COMPLETE
M3 = COMPLETE
M4 = COMPLETE
M5 = COMPLETE
M6 = COMPLETE
M7 = COMPLETE
M8 = COMPLETE
M9 = COMPLETE
M10 = COMPLETE
M11 = COMPLETE

status = M11_COMPLETE
production_readiness = NOT_READY

financial_domain_coverage_readiness =
READY_WITH_KNOWN_OPTIONAL_GAPS

next_scope =
DIRECTIONAL_FINANCIAL_CONTEXT_CONSUMPTION_AND_SPECIFICITY_IMPLEMENTATION
```

Reported M11 provenance:

```text
base_sha =
c9ddbcb7ea149ce317eda6b16be300a2149c7e6e

work_instruction_commit =
af1931336d4499592e84d203c6fcb53b5a33ed54

implementation_commit =
a5d561c238d13b4904bd116aed3d7e47a61f4abd

branch =
codex/20260909-non-operating-financial-income-effects-financial-domain-mapping-implementation
```

M11 used the established pre-report-commit artifact convention:

```text
report_commit = NOT_MEASURED
final_head_sha = NOT_MEASURED
```

At M12 start use actual repository HEAD as authority and record the actual M11 final/report state.

M11 artifact integrity independently verified:

```text
payload count = 53
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

---

# 2. M11 financial-domain readiness frozen for M12

M11 completed the M4 six-domain backlog:

```text
same-period comparison = IMPLEMENTED

OCF / PPE / simple cash conversion = IMPLEMENTED_SELECTIVE

debt / liquidity = IMPLEMENTED_SELECTIVE

inventory / receivables / working capital = IMPLEMENTED_SELECTIVE

non-operating / financial effects = IMPLEMENTED_SELECTIVE
```

Known optional gaps:

```text
preserved_real_us_income_statement_archive_absent

financial_sector_specific_framework_deferred

issuer_specific_granularity_varies
```

M11 explicitly decided:

```text
generic_source_mapping_package_required_first = false
```

Therefore M12 may activate the available typed evidence with those limitations.

Do not claim universal US/KR six-domain coverage.

---

# 3. M11 non-operating safety frozen for M12

M11 proved:

```text
KR non-financial candidates = 6

financial income direct = 6
financial cost direct = 6
tax direct = 6

interest income direct = 1
interest expense direct = 1

safe net financial effect = 6
  - official direct = 1
  - derived from exact aggregates = 5

financial-sector generic non-operating emissions = 0
```

Hard negative controls all remained zero:

```text
universal non-operating total

adjusted net income
normalized net income
normalized EPS
effective tax rate
reconstructed operating profit
recurring earnings score
materiality score
```

M12 must not cause the model to invent any of these.

---

# 4. Directional Core frozen ownership / calibration

M12 changes evidence input specificity.

M12 does NOT change the existing Directional calibration.

Freeze:

```text
BUY threshold:
buy >= 6.0

SELL threshold:
sell >= 6.0

otherwise:
HOLD

balance:
BUY + SELL = 10

increment:
0.5
```

HOLD lean:

```text
5.5 : 4.5 = BUY_LEAN
5.0 : 5.0 = NEUTRAL
4.5 : 5.5 = SELL_LEAN
```

Preserve the frozen boundary-calibration principle:

```text
when supplied evidence reasonably fits adjacent buckets,
choose the less-directional bucket toward 5.0
```

Do NOT:

```text
change 6.0 threshold
change 0.5 increment
add majority voting
add score averaging
add fixed weighted financial scorecard
```

---

# 5. Existing ownership boundaries remain hard

Directional Core owns:

```text
BUY / HOLD / SELL

BUY:SELL balance

HOLD lean

fundamental new-buyer stance

fundamental holder stance

business invalidation
```

Price-Timing owns:

```text
technical state
timing modifier
entry mode
price review
holder price-review pressure
```

Price-Timing may only make the new-buyer stance more conservative.

M12 financial_context consumption occurs only in Directional Core.

Required:

```text
Price-Timing financial_context semantic consumption = 0
```

No price/technical/supply evidence may enter the new financial-context block.

---

# 6. User-approved operating constraints

## 6.1 Existing free/public source policy

No paid provider.

M12 consumes already-built archived/fixture financial_context.

Required:

```text
provider_source_fetches = 0
```

## 6.2 Monitoring remains paused

Observe the approved eight US/KR monitoring schedule paths at start/end.

If all remain paused:

```text
scheduler mutation = 0
```

If one exact approved path unexpectedly resumes:

```text
pause only that exact path
record mutation
```

Do not resume automatically.

## 6.3 Production side effects remain prohibited

Required:

```text
production DB mutation = 0
monitoring registration = 0
assessment persistence = 0
warning mutation = 0
notification queue write = 0
production send = 0
main merge = 0
deploy = 0
```

---

# 7. Repository provenance gate

Before implementation:

```text
git branch --show-current
git rev-parse HEAD
git status --short
```

Audit drift in:

```text
financial_context schema
M6–M11 financial adapters
compact_ai_context
Directional prompt
Directional output schema
ownership validator
renderer
Price-Timing
source sufficiency
monitoring lifecycle
```

If unexplained semantic drift prevents a stable M12 baseline:

```text
STOP
UNEXPLAINED_M12_BASELINE_DRIFT
```

Do not silently reset unrelated work.

---

# 8. Work-instruction commit first

Commit this instruction before implementation.

Record:

```text
new_work_instruction_commit
```

Only then modify Directional input/prompt semantics.

---

# 9. M12 primary architecture

Preferred flow:

```text
DecisionEvidencePacket
+
typed financial_context
        ↓
deterministic financial decision-context builder
        ↓
bounded structured Financial Decision Context
        ↓
Directional Core compact AI context
        ↓
existing Directional output schema
        ↓
existing ownership / semantic validators
        ↓
existing renderer
```

Do NOT serialize all raw `financial_context` indiscriminately.

Do NOT make the model parse the full packet.

Create a deterministic, bounded context layer.

---

# 10. Financial Decision Context object

Create an internal Directional-input representation equivalent to:

```text
financial_decision_context:
  sector_framework
  evidence_items[]
  unavailable_or_not_applicable[]
  limitations[]
```

Each evidence item should preserve, as applicable:

```text
evidence_id / source_ref
metric
value
currency / unit scale
period type
period start/end or point-in-time date
comparison kind
comparison value / derived delta where already available
evidence status:
  DIRECT_REPORTED / DERIVED_SAFE
quality
limitations
semantic category
```

Semantic category may include:

```text
PERFORMANCE_TREND
CASH_CONVERSION
FINANCIAL_RESILIENCE
WORKING_CAPITAL
NON_OPERATING_EFFECT
VALUATION_READINESS
SECTOR_KPI
OTHER_FINANCIAL_CONTEXT
```

Use existing project naming conventions where possible.

Do not create a score.

---

# 11. Deterministic selection — bounded, not scored

The builder must prevent prompt bloat.

Use deterministic evidence selection rules based on:

```text
source validity
period recency/comparability
domain safety
sector applicability
redundancy/overlap
```

Do NOT rank by a proprietary bullish/bearish numeric score.

Preferred rule:

```text
retain the best non-overlapping current/comparable evidence
per applicable financial domain
```

with a bounded overall count.

Target:

```text
enough context for the model to identify 1–3 material anchors
without dumping every source fact
```

The exact evidence-item cap should be justified from actual prompt-size measurements.

Do not hard-code a tiny cap that drops a material domain without an audit.

---

# 12. Domain selection priority is semantic, not bullish/bearish

The deterministic builder may prefer, when available:

```text
current + comparable operating result

OCF and safe OCF-PPE cash-conversion context

complete debt/liquidity context

material inventory/receivables context

safe non-operating/financial effect context
```

because these are distinct economic dimensions.

It must NOT prefer:

```text
positive facts over negative facts

negative facts over positive facts

BUY-supporting facts over SELL-supporting facts
```

Evidence selection must be direction-neutral.

Required audit:

```text
selection_direction_bias_count = 0
```

---

# 13. Correlated evidence / redundancy control

M4 frozen principle:

```text
revenue positive
operating profit positive
net income positive
```

are not automatically three independent positive anchors.

The prompt must tell Core:

```text
Do not count correlated statement lines as independent evidence merely because each is positive/negative.
Identify the economic conclusion they jointly support.
```

Examples:

```text
current profitability
margin trend
cash conversion
balance-sheet resilience
working-capital quality
non-operating contribution
```

The deterministic builder should also avoid obvious duplicate parent/child evidence.

---

# 14. Period specificity

Directional Core must preserve:

```text
QTD
YTD
FY
TTM
POINT_IN_TIME
```

distinctions.

When stronger period evidence exists, forbid vague reasoning that collapses:

```text
latest quarter
and
cumulative YTD
```

into one claim.

Required fictional cases include:

```text
latest-quarter profit
but YTD operating loss

YTD improvement
but latest-quarter margin deterioration
```

The model should explain both rather than choosing one silently.

---

# 15. Same-period comparison / prior-year-end distinction

Core may use:

```text
prior_year_comparable
```

as same-period trend evidence.

Core may use:

```text
prior_year_end
```

as point-in-time balance change since year-end.

Core must not describe:

```text
prior_year_end comparison
```

as YoY.

Add semantic validator/check where feasible.

Required canary violation count:

```text
year_end_as_yoy_count = 0
```

---

# 16. Cash-conversion specificity

When valid OCF/PPE context exists, Core may distinguish:

```text
accounting profit
from
cash conversion
```

Allowed future reasoning examples:

```text
profit growth but weak operating cash flow

profit growth with strong operating cash flow

large identified PPE burden limits simple cash conversion
```

Forbidden:

```text
OCF - PPE = FCF
```

unless the metric is explicitly canonical full FCF, which M6/M11 did not establish.

Required:

```text
partial_capex_called_fcf_count = 0
```

Use wording such as:

```text
simple cash-conversion proxy
OCF less identified PPE capex
```

where relevant.

---

# 17. Debt / liquidity specificity

Core may use M9 evidence only when the packet says:

```text
complete interest-bearing debt total
```

or valid direct liquidity context.

Core must NOT infer:

```text
total liabilities = debt

partial mapped debt = total debt

restricted cash = freely available cash
```

If debt completeness is absent:

```text
do not claim net debt / leverage from partial components
```

Missing debt context is nonnegative.

Financial-sector generic industrial net-debt context remains excluded.

---

# 18. Working-capital specificity

Core may use:

```text
inventory
trade receivables
trade payables
prior-year comparable
prior-year-end
```

when present and sector-applicable.

Core must NOT invent:

```text
DSO
DIO
DPO
CCC
operating working capital
```

from the M10 component context.

Core must not automatically interpret:

```text
inventory increase = bad
receivables increase = bad
```

It should frame them as:

```text
confirmation / cash-conversion / demand-quality checkpoints
```

unless stronger supplied evidence establishes the implication.

---

# 19. Non-operating / financial-effects specificity

For non-financial operating companies, Core may distinguish:

```text
operating improvement
from
net-income change explained by financial/non-operating effects
```

using M11 context.

Core must NOT:

```text
call other income one-off automatically

call financial income low-quality automatically

construct normalized earnings

construct adjusted net income

construct normalized EPS

classify equity-method income as financial income automatically

treat tax effect as operating performance
```

For banks/insurers:

```text
generic non-operating financial-income interpretation remains disabled
```

---

# 20. Financial-sector routing

The financial decision-context builder must respect:

```text
SECTOR_FRAMEWORK_REQUIRED
```

from M9/M10/M11.

For:

```text
bank
insurance
reinsurance
financial institution
```

do NOT inject generic:

```text
industrial net debt
working-capital quality
interest income as non-operating
```

into Directional context.

If the project has a sector-specific financial framework already,
continue using it separately.

M12 does not design a new bank/insurance framework.

Required:

```text
financial_sector_generic_financial_context_leak_count = 0
```

---

# 21. Missing / unavailable evidence semantics

Prompt must state explicitly:

```text
missing / unavailable / not supplied
is not negative evidence
```

A missing financial domain may:

```text
limit confidence
limit specificity
create a confirmation requirement
```

but must not mechanically increase SELL balance.

Required fictional case:

```text
otherwise identical issuer
with optional financial_context removed
```

must not become more bearish solely because the field is missing.

---

# 22. 1–3 value-relevant anchors

Frozen M4 specificity rule:

Directional explanation should select:

```text
1–3 enterprise-value-relevant anchors
```

when evidence supports them.

Do not require exactly 3.

Allowed:

```text
1 strong anchor
2 mixed anchors
3 distinct anchors
```

Forbidden:

```text
listing every available accounting field
```

The explanation should prioritize:

```text
what matters to the investment conclusion
```

not data completeness.

---

# 23. Absolute decision vs confidence

Richer context may change:

```text
direction
balance
HOLD lean
new-buyer stance
holder stance
```

because it changes fundamental evidence.

That is intended.

However the model must distinguish:

```text
business direction
from
confidence / Unknown
```

Missing evidence should not be converted into a directional negative.

Do not add a new confidence score if one already exists.

Use existing schema.

---

# 24. No fixed financial scorecard

Hard prohibition:

```text
OCF positive = +1
net debt negative = +1
inventory up = -1
financial income large = -1
```

or equivalent fixed weighted scoring.

M12 prompt may give reasoning principles and examples.

The final balance remains the model's holistic Directional judgment under the frozen 0.5 scale.

Required code audit:

```text
fixed_financial_score_rule_count = 0
```

---

# 25. Directional prompt changes

M12 is allowed and expected to modify the Directional Core prompt.

The prompt must add bounded instructions for:

```text
financial_decision_context use

1–3 material anchors

period specificity

cash-conversion distinction

debt completeness

working-capital caution

non-operating attribution

sector applicability

missing-data nonnegative behavior

correlated-evidence non-duplication
```

Do NOT modify:

```text
BUY/SELL threshold
balance arithmetic
HOLD lean mapping
ownership boundaries
price/supply exclusion
```

Freeze prompt hash after implementation.

---

# 26. Directional input activation

For equivalent source packets:

Pre-M12:

```text
financial_context exists in packet
but is omitted from compact AI context
```

Post-M12:

```text
selected financial_decision_context is intentionally included
```

Therefore:

```text
compact_ai_context_changed_count > 0
```

is expected for fixtures with eligible financial evidence.

This is NOT an input leak in M12.

Instead M12 must prove:

```text
only approved financial fields changed
```

Create a structured diff proof.

For fixtures with:

```text
no eligible financial_context
```

compact input should remain unchanged except for any empty-block serialization intentionally introduced.

Prefer omitting an empty block.

---

# 27. Price / technical / supply non-leak

The new financial context builder must never import:

```text
price
OHLCV
technical indicators
support/resistance
foreign/institutional flows
supply score
positioning
```

Required:

```text
financial_context_price_ref_count = 0
financial_context_technical_ref_count = 0
financial_context_supply_ref_count = 0
```

Existing Directional price/supply hard gates remain.

---

# 28. Evidence reference integrity

Any financial fact used in the model context must retain a valid:

```text
evidence ID / source ref
```

The Directional output's supporting references must resolve to supplied evidence.

Do not allow hallucinated financial source IDs.

Required validator:

```text
output evidence ref ∈ supplied evidence refs
```

or existing equivalent.

Required model-canary:

```text
invalid_financial_reference_count = 0
```

---

# 29. Claim/evidence semantic checks

Where feasible without creating an accounting expert system,
extend deterministic post-output validation for high-risk claims.

At minimum detect:

```text
"FCF" claim sourced only to ocf_less_ppe_capex proxy

"YoY" claim sourced only to prior_year_end comparison

"net debt" claim without complete net_debt evidence

"normalized/adjusted earnings" claim absent explicit company-defined metric

financial-sector generic non-operating claim
```

These checks should be generic.

Do not attempt to validate every natural-language interpretation.

---

# 30. Renderer ownership remains frozen

Renderer remains the owner of primary user-facing action wording.

M12 must not move issuer-specific reasoning into renderer templates.

Required:

```text
renderer substantive-analysis change count = 0
```

The renderer may naturally display the richer model-owned reasoning.

Primary imperative action wording remains renderer-owned.

No AI imperative primary action.

---

# 31. Message specificity advisory

For M12 fictional canary outputs, run a small advisory audit.

Measure:

```text
specific financial anchor present when material evidence exists

period/basis specificity

issuer/case-specific checkpoint

generic substantive repetition

renderer-introduced repetition
```

Do not solve repetition by synonyms.

The main M12 acceptance is semantic correctness and specificity,
not zero repeated words.

---

# 32. Model runtime — frozen

Use the previously frozen experiment runtime:

```text
model =
gpt-5.6-sol

reasoning =
xhigh

runtime mode =
MODEL_CONTEXT_COUPLED

subjects per shared context =
4

timeout =
1800 seconds

single authoritative watchdog =
yes

wrapper auto-retry =
0

batch split =
0
```

Each context must have:

```text
unique invocation ID
unique runtime namespace
unique working directory
unique session identity
```

No shared namespace across contexts.

---

# 33. Model-call gating

M12 has two phases.

## Phase A — implementation / deterministic proof

Required before any model call:

```text
focused tests PASS

full tests PASS

ruff PASS

git diff --check PASS

prompt/input snapshot audit PASS

source sufficiency unchanged

Price-Timing unchanged

renderer ownership unchanged

production side-effect firewall PASS
```

If Phase A fails:

```text
STOP
NO_MODEL_CALLS
```

## Phase B — bounded fictional model canary

Only if Phase A passes.

M12 does NOT run a fresh real holdout.

---

# 34. Fictional canary design

Create exactly 8 fictional issuers.

No real tickers.

No real-company names.

The 8 cases must cover distinct financial patterns.

## FIC-FIN-01 — strong quality

```text
profit growth
strong OCF
healthy simple cash conversion
complete low net debt / net cash
working capital controlled
```

Expected reasoning:

```text
positive anchor specificity
no invented FCF
```

## FIC-FIN-02 — profit / cash divergence

```text
profit growth
weak OCF
large working-capital consumption
```

Expected:

```text
cash conversion limits conviction
```

No automatic SELL solely from one weak OCF period unless overall supplied evidence warrants it.

## FIC-FIN-03 — latest quarter vs cumulative conflict

```text
latest QTD operating profit
but YTD operating loss
```

Expected:

```text
explicit period distinction
```

## FIC-FIN-04 — non-operating net-income boost

```text
weak/flat operating result
net income improves
large safe net financial effect
```

Expected:

```text
do not call net-income improvement operating improvement
```

No normalized earnings.

## FIC-FIN-05 — leverage / liquidity pressure

```text
profit positive
complete high debt context
low cash
```

Expected:

```text
financial resilience is a limiting anchor
```

No total-liabilities-as-debt inference.

## FIC-FIN-06 — inventory / receivables build

```text
sales/profit growth
inventory and trade receivables rise materially
prior-year-end comparison only
```

Expected:

```text
do not call the balance movement YoY
do not automatically call it deterioration
identify it as a working-capital confirmation point
```

## FIC-FIN-07 — missing optional context

Create two logically equivalent variants:

```text
one with optional financial domain unavailable
one without that optional block
```

Expected:

```text
missing does not mechanically make direction more bearish
```

## FIC-FIN-08 — financial-sector exclusion

Use fictional insurer/bank-like subject.

Supply financial-sector facts that would be misleading under industrial rules.

Expected:

```text
no generic industrial net debt
no generic working-capital interpretation
no interest income classified as non-operating
```

Use only sector-valid existing evidence context.

---

# 35. Fictional canary execution topology

Use:

```text
8 subjects
2 shared contexts
4 subjects per context
3 repetitions
```

Total model calls:

```text
6
```

No batching change.

No majority vote.

No averaging.

Each repetition is independently audited.

---

# 36. Fictional canary hard semantic gates

Across all 24 subject outputs:

Required zero:

```text
invalid evidence references

price/technical/supply references in Directional reasoning

partial PPE proxy called FCF

prior-year-end called YoY

partial debt called total debt

total liabilities called debt

normalized/adjusted earnings invented

financial-sector generic industrial financial reasoning

missing financial_context used as negative evidence

fixed-score language or deterministic financial scoring

AI imperative primary action wording
```

Required:

```text
material financial anchor usage where the case contains material evidence
```

Case-specific expected semantic assertions must pass.

---

# 37. Fictional Directional stability

Run the existing formal stability classifier on the fictional 8-case canary.

Report:

```text
STABLE
BOUNDARY_UNCERTAINTY
UNSTABLE
```

Do not create a new stability definition.

Required hard gate:

```text
opposite-direction reversal count = 0
```

For deliberately threshold-adjacent cases,
`BOUNDARY_UNCERTAINTY` may be reported according to the frozen classifier.

Do NOT majority-vote results.

If material financial-context interpretation flips unpredictably across repetitions:

```text
M12_MODEL_CANARY_FAIL
```

and do not proceed to real holdout planning as ready.

---

# 38. No real-company model proof in M12

M12 must NOT call the model on:

```text
new unseen real issuer cohort
historical exposed real holdout as if unseen
```

The purpose of M12 is:

```text
implementation
+
bounded semantic canary
```

If the canary passes:

```text
next_scope =
FRESH_REAL_FINANCIAL_CONTEXT_GENERALIZATION_PROOF
```

A later task may perform the full new real proof using a fresh cohort.

---

# 39. Exposure registry

Fictional subjects do not consume real-issuer unseen holdout capacity.

If any real issuer is accidentally used in a model call:

```text
record exposure immediately
retire it from unseen proof
```

Required intended:

```text
real_issuer_model_exposure_count = 0
```

---

# 40. Source sufficiency remains unchanged

M12 is not a source gate task.

Required:

```text
source_sufficiency_semantic_change_count = 0
```

A richer Directional context may exist only when optional evidence is available.

Subjects with valid old source sufficiency remain eligible even if the new optional financial context is absent.

No universal financial-domain gate.

---

# 41. Daily Delta / monitoring lifecycle unchanged

M3 semantics remain frozen.

M12 financial-context activation affects:

```text
current absolute Directional judgment
```

It does not change:

```text
bootstrap != Daily Delta

missing refresh != no_material_change

price-only != business thesis delta
```

Required:

```text
daily_delta_semantic_change_count = 0
```

No production assessments.

---

# 42. Warning semantics unchanged

M12 may allow Directional reasoning to mention a financial risk.

M12 does NOT open/close warning records.

Required:

```text
warning_semantic_change_count = 0
warning_mutations = 0
```

Future warning activation remains separate.

---

# 43. Prompt token / context-size audit

Measure pre/post Directional compact context size.

For representative packet fixtures report:

```text
pre chars/tokens estimate

post chars/tokens estimate

financial context contribution

max item count

truncation count
```

No hidden silent truncation of a material selected item.

If the new context causes existing non-financial evidence to be dropped by a context-size limit:

```text
STOP
M12_CONTEXT_BUDGET_REGRESSION
```

or implement a deterministic safe compaction strategy with tests.

Do not expand by dumping raw accounting facts.

---

# 44. Legacy / no-financial-context behavior

For legacy packet fixtures with no `financial_context`:

Expected:

```text
Directional compact context semantically unchanged
```

apart from intentional stable prompt instruction changes.

The financial evidence block should be:

```text
omitted
```

rather than filled with negative/unavailable boilerplate where possible.

Required:

```text
legacy_financial_absence_directional_penalty = 0
```

---

# 45. Deterministic input serialization

The new financial decision context must serialize deterministically.

Same packet:

```text
→ same selected evidence refs
→ same ordering
→ same compact financial block
→ same hash
```

No runtime timestamps/random ordering.

Required:

```text
financial_context_builder_idempotency = PASS
```

---

# 46. Implementation scope

Preferred changed code:

```text
Directional financial-context selector/builder

compact_ai_context or Directional context construction

Directional prompt

generic output semantic checks for high-risk financial claims

tests / fictional canary fixtures
```

Avoid changing:

```text
source adapters M6–M11

Price-Timing

renderer substantive templates

monitoring lifecycle

source sufficiency

notification
scheduler
```

If a required change falls outside this scope:

```text
STOP
M12_SCOPE_EXCEEDED
```

unless it is semantic-neutral compatibility plumbing.

---

# 47. Deterministic validation requirements

Before model calls run:

```text
focused pytest
full repository pytest
ruff
git diff --check
```

Focused suite must include:

```text
M5 FinancialContext schema

M6–M11 financial-domain regressions

financial decision-context selector

sector routing

period specificity

comparison-kind safety

cash-conversion label safety

debt completeness safety

working-capital formula prohibition

non-operating normalization prohibition

evidence-ref integrity

Price-Timing isolation

source-sufficiency no-change

Daily Delta no-change

renderer ownership regression
```

All PASS before Phase B.

---

# 48. Production side-effect firewall

Required M12 final counts:

```text
provider_source_fetches = 0

production_db_mutations = 0
monitoring_registrations = 0
assessment_persistence_mutations = 0
warning_mutations = 0
notification_queue_writes = 0
production_sends = 0

main_merges = 0
deployments = 0
live_v2_changes = 0
night_futures_changes = 0

automatic_monitoring_resume = 0
```

Model calls are allowed only for the fictional canary:

```text
expected fictional model calls = 6
expected real model calls = 0
expected judge model calls = 0
```

---

# 49. Required artifacts — provenance / contract

Produce at least:

```text
01-repository-provenance
02-latest-result-integrity
03-m12-scope-freeze
04-m4-directional-specificity-contract-reuse-proof
05-m5-m11-financial-context-readiness-proof

06-directional-financial-context-input-contract
07-financial-context-selection-contract
08-financial-context-sector-routing-contract
09-financial-context-redundancy-contract
10-financial-context-period-specificity-contract
11-financial-context-missing-data-contract
```

---

# 50. Required artifacts — implementation / no-regression

Produce:

```text
12-financial-context-builder-implementation
13-directional-prompt-before-after
14-directional-compact-context-before-after
15-context-budget-audit
16-financial-context-builder-idempotency

17-price-technical-supply-non-leak-proof
18-price-timing-no-change-proof
19-renderer-ownership-no-change-proof
20-source-sufficiency-no-change-proof
21-daily-delta-no-change-proof
22-warning-no-change-proof
23-legacy-no-financial-context-compatibility
```

---

# 51. Required artifacts — semantic validators

Produce:

```text
24-financial-evidence-ref-integrity-control
25-fcf-label-negative-control
26-comparison-label-negative-control
27-debt-completeness-claim-control
28-normalized-earnings-negative-control
29-financial-sector-generic-claim-control
30-fixed-scorecard-negative-control
```

---

# 52. Required artifacts — fictional model canary

Produce:

```text
31-fictional-financial-canary-manifest
32-fictional-financial-canary-inputs

33-canary-context-01-run-1
34-canary-context-02-run-1

35-canary-context-01-run-2
36-canary-context-02-run-2

37-canary-context-01-run-3
38-canary-context-02-run-3

39-fictional-canary-semantic-audit
40-fictional-canary-stability
41-fictional-canary-message-specificity-advisory
42-runtime-observations
```

Preserve raw prompt/output/receipt artifacts for all 6 calls.

---

# 53. Required validation / completion artifacts

Produce:

```text
43-focused-test-results
44-full-test-results
45-ruff-and-diff-results

46-directional-financial-context-activation-decision
47-fresh-real-proof-readiness-decision
48-production-no-change
49-schedule-pause-observation
50-master-workflow-update
51-program-completion
```

---

# 54. M12 acceptance criteria — deterministic layer

Deterministic implementation passes only if:

```text
financial decision-context builder is deterministic

sector-applicability rules are enforced

missing evidence remains nonnegative

price/technical/supply cannot enter financial block

period/comparison kinds are explicit

FCF label misuse is blocked

debt-completeness misuse is blocked

normalized earnings inventions are blocked where deterministically detectable

source sufficiency unchanged

Price-Timing unchanged

renderer ownership unchanged

Daily Delta unchanged

warning semantics unchanged

focused/full tests PASS

ruff PASS

git diff --check PASS
```

---

# 55. M12 acceptance criteria — model canary

Fictional canary passes only if:

```text
all 6 model calls complete successfully

all 24 subject outputs schema-valid

all supplied output evidence refs resolve

hard financial semantic violation count = 0

price/technical/supply Directional violations = 0

financial-sector generic misuse = 0

missing-context bearish-default violation = 0

fixed-scorecard violation = 0

AI imperative primary action = 0

opposite-direction reversal count = 0

no unexplained transport failure

no wrapper retry
```

If a case has intended threshold-adjacent ambiguity,
use the existing stability classifier.

Do not majority-vote.

---

# 56. Model canary failure handling

If a hard semantic violation occurs:

```text
STOP
M12_MODEL_CANARY_FAIL
```

Record:

```text
case
raw output
evidence supplied
violated contract
whether root cause is:
  prompt
  context selection
  validator
  schema
  runtime
```

Do not patch the result.

Do not continue to a real holdout.

If one model context transport-fails:

```text
follow existing single-watchdog / no-wrapper-retry policy
```

Do not selectively rerun only successful subjects to manufacture a complete cohort.

---

# 57. Next-scope decision

## A. Deterministic + fictional canary PASS

Recommended:

```text
next_scope =
FRESH_REAL_FINANCIAL_CONTEXT_GENERALIZATION_PROOF
```

That later task should use:

```text
fresh unseen real issuer cohort
new source lock
FIRST / A / B / C repeated proof
formal Directional stability
message specificity advisory
ownership / renderer / hard-safety gates
```

and must preserve exposure retirement rules.

## B. Deterministic PASS, model canary specificity weak but hard safety PASS

Recommended:

```text
DIRECTIONAL_FINANCIAL_SPECIFICITY_PROMPT_REFINEMENT
```

only if the issue is generic and demonstrated across fictional cases.

Do not change score thresholds.

## C. Model canary hard semantic failure

Recommended:

```text
BOUNDED_DIRECTIONAL_FINANCIAL_CONTEXT_REPAIR
```

No real proof.

## D. Context selection itself is inadequate

Recommended:

```text
FINANCIAL_DECISION_CONTEXT_SELECTION_REPAIR
```

Do not compensate by dumping all financial facts into the model.

---

# 58. Master workflow update

Update:

```text
docs/MASTER_WORKFLOW.md
```

Expected transition:

```text
M11 COMPLETE
→
M12 COMPLETE
→
fresh real financial-context generalization proof
```

only if M12 canary passes.

Production readiness remains:

```text
NOT_READY
```

Monitoring remains paused.

---

# 59. Program-completion fields

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
report_commit
final_head_sha
branch

latest_result_zip_sha256
latest_result_integrity

m1_status
m2_status
m3_status
m4_status
m5_status
m6_status
m7_status
m8_status
m9_status
m10_status
m11_status
m12_status

financial_domain_readiness_input
financial_context_consumption_status

financial_context_builder_status
financial_context_builder_idempotency_status
financial_context_selected_item_cap

financial_context_input_fixture_count
financial_context_input_changed_count

financial_context_price_ref_count
financial_context_technical_ref_count
financial_context_supply_ref_count

selection_direction_bias_count

fcf_label_violation_count
year_end_as_yoy_count
partial_debt_total_claim_count
normalized_earnings_claim_violation_count
financial_sector_generic_financial_context_leak_count

fixed_financial_score_rule_count

directional_threshold_changed
directional_increment_changed
hold_lean_contract_changed
calibration_tiebreak_changed

directional_prompt_changed
price_timing_prompt_changed
renderer_substantive_change_count

source_sufficiency_semantic_change_count
daily_delta_semantic_change_count
warning_semantic_change_count

legacy_financial_absence_directional_penalty

fictional_subject_count
fictional_context_count
fictional_repetition_count

model_calls_real
model_calls_fictional
model_calls_judge

model_context_success_count
model_context_failure_count
wrapper_retry_count
timeout_count
capacity_failure_count
orphan_process_count

fictional_output_row_count
fictional_schema_pass_count
invalid_financial_reference_count
hard_financial_semantic_violation_count

fictional_stable_count
fictional_boundary_uncertainty_count
fictional_unstable_count
opposite_direction_reversal_count

message_specificity_advisory_status

real_issuer_model_exposure_count

provider_source_fetches

production_db_mutations
monitoring_registrations
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

main_merges
deployments
live_v2_changes
night_futures_changes

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count

fresh_real_proof_readiness
production_readiness
status
stop_reason
next_scope
```

Anything not actually measured:

```text
NOT_MEASURED
```

---

# 60. Artifact integrity

Freeze:

```text
program completion
master workflow update
all reports
all raw model prompts/outputs/receipts
```

before final artifact-index creation.

Index every payload except the index itself.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final ZIP SHA-256.

---

# 61. Final task principle

M5–M11 built the accounting-evidence layer while deliberately keeping it away from the model.

M12 is the controlled activation point.

The correct flow is:

```text
typed financial_context
→ deterministic, sector-aware compact selection
→ Directional Core
→ 1–3 value-relevant anchors
→ evidence-grounded specificity
→ bounded fictional model canary
```

Not:

```text
dump all accounting fields into the prompt
```

Not:

```text
create a financial scorecard
```

Not:

```text
change BUY/SELL thresholds
```

Not:

```text
make missing data bearish
```

Not:

```text
let price/supply contaminate Directional Core
```

Not:

```text
invent normalized earnings
```

Not:

```text
run a fresh real holdout before the fictional semantic canary passes
```

And not:

```text
resume production monitoring
```

Activate financial evidence carefully, prove the model uses it correctly,
then move to the fresh real generalization proof.
