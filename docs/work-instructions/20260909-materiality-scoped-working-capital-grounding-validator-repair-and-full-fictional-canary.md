# Thesis Monitor — Materiality-Scoped Working-Capital Grounding Validator Repair & Full Fictional Canary

## 0. Task identity

Suggested work-instruction filename:

```text
20260909-materiality-scoped-working-capital-grounding-validator-repair-and-full-fictional-canary.md
```

Suggested result bundle:

```text
thesis-monitor-20260909-materiality-scoped-working-capital-grounding-validator-repair-full-fictional-canary-report.zip
```

Master-workflow phase:

```text
M12C — Bounded Financial Grounding Contract Repair
        Materiality/Claim-Scoped Working-Capital Grounding
        + Full Fictional Canary
```

This task begins only after M12B stopped with:

```text
status = M12B_CANARY_FAIL
stop_reason = FIRST_CLASS_TYPED_GROUNDING_INSUFFICIENT
```

However, M12C must NOT assume the automatic M12B handoff to an output-schema change is correct.

The new evidence shows that the first-class architecture itself worked broadly:

```text
selected typed refs = 26
first-class typed refs = 26
used typed refs = 26

material financial anchor grounding failure = 0
narrative substitution failure = 0
irrelevant financial ref failure = 0

working-capital grounding failure = 1
```

The single blocker is FIC-FIN-02 on run-2/context-01.

M12C must first determine whether that one failure is:

```text
A. a true claim-level grounding failure
or
B. a validator overreach / false reject
   caused by requiring a working-capital checkpoint
   merely because a working-capital typed item was selected.
```

Only if B is proven may the validator be repaired.

If A is proven, do NOT weaken the validator and do NOT run new model calls.
Stop and hand off to the previously recommended output-grounding-schema scope.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260909-first-class-typed-financial-evidence-index-implementation-full-fictional-canary-report.zip
```

Verified SHA-256:

```text
66b53e4991ec46774f2000c871650d2a4390bd88505cc4ae802be0bbc17844cd
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Artifact integrity independently verified:

```text
indexed payload count = 112
missing = 0
extra = 0
hash mismatch = 0
size mismatch = 0
```

Latest M12B state:

```text
M12A = COMPLETE

M12B =
BLOCKED

first-class typed financial projection =
PASS

fresh real proof readiness =
NOT_READY

production readiness =
NOT_READY
```

Reported provenance:

```text
base_sha =
efd31109b0f52bd7151f37cebef03820e40eb2ef

work_instruction_commit =
c92194414db5200cf30df1f912f9ebedc6036a43

implementation_commit =
746ef9ba1586973d84b49fd233e460660f5b91fa

branch =
codex/20260909-first-class-typed-financial-evidence-index-implementation-full-fictional-canary
```

M12B used the established pre-report-commit convention:

```text
report_commit = NOT_MEASURED
final_head_sha = NOT_MEASURED
```

Use actual repository HEAD as authority.

---

# 2. M12B deterministic implementation is frozen

M12B successfully implemented:

```text
FIRST_CLASS_TYPED_EVIDENCE_UNIFICATION
```

Measured:

```text
selected typed financial refs = 15

first-class projections = 15

suppressed typed projection = 0

alias renumber = 0

duplicate alias = 0

neutral typed financial statements = 15

financial_decision_context detail removed = 0

output schema change = 0

renderer substantive change = 0
```

Legacy controls:

```text
no-financial-context FIC-FIN-07 remains byte-equivalent

financial-sector FIC-FIN-08 receives no generic industrial financial leak
```

Historical regressions:

```text
old FIC-FIN-06 remains FAIL

corrected FIC-FIN-06 remains PASS

FIC-FIN-03 QTD/YTD regression remains PASS
```

Phase A:

```text
focused pytest = 594 passed

full pytest = 3056 passed

Ruff = PASS

git diff --check = PASS
```

Do not reopen the first-class evidence architecture in M12C unless the forensic phase proves a direct implementation defect.

---

# 3. M12B canary result — exact completed sample

Generation:

```text
20260909-m12b-fictional-20260909T085320Z-746ef9ba1586
```

Completed contexts:

```text
run-1/context-01 = PASS
run-1/context-02 = PASS
run-2/context-01 = transport/schema PASS, semantic audit FAIL
```

Completed model calls:

```text
3 / 6
```

Completed subject rows:

```text
12 / 24
```

Transport/runtime:

```text
3 / 3 context calls returned successfully

timeout = 0
capacity failure = 0
orphan = 0
wrapper retry = 0
```

Formal stability:

```text
NOT_MEASURED
```

because the full 6-call generation did not complete.

Do not infer stability from the partial 12-row sample.

---

# 4. M12B generic safety result

Across the completed 12 subject outputs:

```text
invalid financial refs = 0

price/technical/supply Directional violations = 0

partial PPE called FCF = 0

prior-year-end called YoY = 0

partial debt called total debt = 0

normalized earnings claims = 0

financial-sector generic misuse = 0

fixed financial score rules = 0

QTD/YTD false reject = 0

QTD/YTD false accept = 0

AI imperative primary action = 0
```

The only hard-semantic count was:

```text
working_capital_grounding_failure_count = 1
```

---

# 5. FIC-FIN-06 architecture objective was achieved

FIC-FIN-06 was the original first-class grounding focal case.

Under M12B run-1/context-02:

```text
FIC-FIN-06 = PASS
```

The first-class architecture successfully made:

```text
inventory
trade receivables
```

available and directly usable as ordinary evidence refs.

Therefore M12C must not treat M12B as a wholesale failure of Option A.

---

# 6. New blocker — FIC-FIN-02 run-2/context-01

Frozen fictional case identity:

```text
ticker = FIC-FIN-02
case = profit_cash_divergence
```

Its frozen case purpose is:

```text
profit growth / accounting profit improvement
vs
weak operating cash flow / weak simple cash conversion
```

Primary expected semantic behavior:

```text
cash conversion limits conviction

do not mechanically SELL from one weak OCF period

working-capital context may support diagnosis/confirmation
but is not the focal canary contract
```

Selected typed financial refs in the M12B context:

```text
E01 =
ocf_less_ppe_capex
semantic category = CASH_CONVERSION

E08 =
operating_cash_flow
semantic category = CASH_CONVERSION

E04 =
inventory
semantic category = WORKING_CAPITAL
comparison = prior_year_end
```

Run-2 output used all three typed financial refs somewhere in the candidate.

Measured:

```text
selected typed refs = 3
first-class typed refs = 3
used typed refs = 3

material financial anchor failure = 0
narrative substitution failure = 0
irrelevant ref failure = 0
```

Only:

```text
working_capital_grounding_failure = 1
```

remained.

---

# 7. Exact FIC-FIN-02 run-2 evidence behavior

The model made cash conversion the actual directional anchor.

Examples:

```text
material_directional_anchor_basis:
E08
```

where:

```text
E08 = typed operating_cash_flow
```

Dominant evidence cited:

```text
E08
E01
```

which are both typed cash-conversion refs.

The candidate also directly used inventory typed ref:

```text
E04
```

inside an Unknown treatment:

```text
"재고 증가는 맥락 자료로만 취급한다."
```

This is consistent with the M10/M12 contract:

```text
inventory increase is not automatically deterioration
```

The failing `risk_context` was:

```text
"운전자본 흡수가 지속될 수 있으나
그 원인과 가역성은 확인되지 않았다."
```

and cited narrative:

```text
E02 structural-risk
E09 remaining-unknown
```

not E04.

The business reevaluation conditions focused on:

```text
OCF recovery
cash conversion improvement/deterioration
```

and did not force an inventory-specific checkpoint.

---

# 8. Core forensic question

Before changing code, reproduce the exact reason:

```text
working_capital_checkpoint_not_used
```

was emitted.

Determine whether the trigger is currently based on:

```text
selected WORKING_CAPITAL evidence exists
```

rather than:

```text
a material claim actually relies on that specific typed working-capital fact
```

Required classifications:

```text
SELECTION_TRIGGERED_OVERREACH

CLAIM_TRIGGERED_VALID_FAILURE

CANARY_CASE_CONTRACT_MISMATCH

OTHER_GENERIC_VALIDATOR_DEFECT
```

Do not implement until exactly one root cause is supported by code + preserved output.

---

# 9. Frozen canary case distinction

The fictional canary intentionally has different focal domains.

## FIC-FIN-02

```text
case = profit_cash_divergence

hard focal financial domain =
CASH_CONVERSION
```

Inventory is secondary context.

## FIC-FIN-06

```text
case = inventory_receivables_build

hard focal financial domain =
WORKING_CAPITAL
```

This distinction must be preserved in the canary audit.

Do not turn every selected financial domain into a mandatory focal checkpoint.

---

# 10. Production-validator principle

The production/shared validator should remain generic.

Do NOT add:

```text
if ticker == FIC-FIN-02
```

or any case ID special case to production code.

The generic grounding principle should be:

```text
a typed financial ref is required when
a material financial claim actually relies on that typed financial fact.
```

Not:

```text
every selected financial fact must appear in a material checkpoint.
```

Selected financial evidence may legitimately be:

```text
context-only

Unknown / confirmation context

supporting but non-dominant

not used at all if immaterial
```

without becoming a hard failure.

---

# 11. Metric-specific claim grounding

If the output explicitly claims:

```text
inventory increased / fell

trade receivables increased / fell

trade payables changed

specific working-capital balance moved
```

and the matching typed financial ref is selected,
the claim must cite a relevant typed working-capital ref.

This remains a hard grounding rule.

Example:

```text
"재고가 연말 대비 증가했다."
```

with selected inventory ref:

```text
must cite inventory typed ref.
```

A narrative duplicate alone is insufficient.

---

# 12. Generic working-capital narrative claim

A generic claim such as:

```text
"운전자본 사용이 지속될 수 있다."
```

may arise from:

```text
management/business narrative

cash-flow evidence

multiple working-capital components

source-supplied risk narrative
```

A selected inventory ref alone is not necessarily sufficient evidence
for the full generic working-capital claim.

Therefore M12C must not mechanically require:

```text
inventory ref
```

in every generic "working capital" sentence solely because inventory was selected.

Instead validate:

```text
the specific typed fact is grounded where the output actually uses that fact.
```

If the claim explicitly attributes the generic working-capital conclusion
to inventory/receivables/payables,
then the corresponding typed refs are required.

---

# 13. Context / Unknown use is legitimate

If a selected inventory ref is used in:

```text
Unknown treatment

risk limitation

confirmation context
```

with wording such as:

```text
"inventory increase is context only;
the cause and reversibility remain unknown"
```

that is legitimate typed financial use.

The validator must not require the same typed ref to also appear in:

```text
business_reevaluation_up/down
```

unless the actual output makes inventory a material reevaluation condition.

No duplicate citation obligation.

---

# 14. Canary-specific semantic audit remains separate

The fictional canary may have per-case expected focal domains.

This belongs in:

```text
fictional canary audit / manifest
```

not in generic production semantics keyed by ticker.

For FIC-FIN-06, the canary may require:

```text
a real working-capital checkpoint grounded to typed WC refs
```

because that is the case's purpose.

For FIC-FIN-02, the canary should require:

```text
cash-conversion grounding
```

and only require working-capital-specific grounding
if the output itself promotes working capital to a material directional claim.

---

# 15. Phase A — forensic gate before implementation

Create:

```text
FIC-FIN-02 run-1 PASS replay

FIC-FIN-02 run-2 FAIL replay

FIC-FIN-06 M12B PASS replay

old M12G FIC-FIN-06 FAIL replay

corrected FIC-FIN-06 PASS replay
```

Trace for each:

```text
selected financial refs

used refs

material anchors

working-capital-specific claims

working-capital-specific refs

reevaluation/checkpoint refs

error trigger
```

Then write:

```text
grounding-validator-root-cause.json
```

No code changes before this root-cause artifact is frozen.

---

# 16. Branch A — if validator overreach is CONFIRMED

Only if the forensic classification is:

```text
SELECTION_TRIGGERED_OVERREACH
```

or:

```text
CANARY_CASE_CONTRACT_MISMATCH
```

perform a bounded generic repair.

The repair must change:

```text
working-capital grounding requirement
```

from:

```text
selection-triggered
```

to:

```text
material-claim / metric-use triggered
```

while preserving FIC-FIN-06 hard grounding.

Do not change unrelated financial semantic validators.

---

# 17. Branch B — if output failure is genuinely valid

If forensic classification is:

```text
CLAIM_TRIGGERED_VALID_FAILURE
```

then:

```text
STOP
NO_MODEL_CALLS
```

Do NOT weaken the validator.

Set:

```text
next_scope =
DIRECTIONAL_OUTPUT_FINANCIAL_GROUNDING_SCHEMA_REVIEW_OR_IMPLEMENTATION
```

or a more precise output-structure scope supported by the forensic evidence.

M12C is then complete as a root-cause review only.

---

# 18. Required repaired behavior if Branch A executes

After repair:

## M12B FIC-FIN-02 run-2

Expected:

```text
PASS
```

only if:

```text
cash-conversion typed refs remain materially grounded

inventory E04 is directly used as context/Unknown

the output does not make an ungrounded inventory-specific deterioration claim
```

## FIC-FIN-06 old narrative-only output

Expected:

```text
FAIL
```

with typed WC grounding errors preserved.

## FIC-FIN-06 corrected output

Expected:

```text
PASS
```

## M12B FIC-FIN-06 run-1

Expected:

```text
PASS
```

---

# 19. Positive validator fixtures

Add generic positive fixtures.

At minimum:

### A. Context-only inventory

Selected inventory typed ref.

Output says:

```text
inventory increased since year-end
but this alone does not establish deterioration
```

and cites inventory typed ref in the same Unknown/context claim.

No inventory-specific reevaluation condition.

Expected:

```text
PASS
```

### B. Cash-conversion focal case with secondary inventory

OCF / cash-conversion typed refs own the material anchor.

Inventory typed ref is used only as context.

Expected:

```text
PASS
```

### C. Specific inventory checkpoint

Output says:

```text
inventory normalization is a key confirmation condition
```

and cites inventory typed ref.

Expected:

```text
PASS
```

### D. Specific receivables checkpoint

Output cites typed trade-receivable ref.

Expected:

```text
PASS
```

---

# 20. Negative validator fixtures

At minimum:

### A. Inventory claim without inventory typed ref

```text
"inventory rose materially"
```

but only narrative ref cited.

Expected:

```text
FAIL
```

when typed inventory is selected.

### B. Receivables collection claim without typed AR ref

Expected:

```text
FAIL
```

### C. Inventory-specific reevaluation without typed inventory ref

Expected:

```text
FAIL
```

### D. Irrelevant typed financial ref

Inventory claim cites only OCF/net-debt typed ref.

Expected:

```text
FAIL
```

### E. FIC-FIN-06 narrative substitution

Must remain:

```text
FAIL
```

---

# 21. No financial-selector change

Freeze M12B first-class architecture:

```text
selected typed financial refs

first-class projection

neutral financial statements

alias ordering

catalog
```

Required:

```text
financial_context_selection_change_count = 0

first_class_projection_change_count = 0
```

This task is not another evidence-surface redesign.

---

# 22. No prompt-only repair

Freeze the current M12G/M12B Directional prompt.

Required:

```text
directional_prompt_change_count = 0
```

If the validator root cause is selection-triggered overreach,
fix the validator/audit contract directly.

Do not add another prompt instruction.

---

# 23. Output schema remains frozen in Branch A

If Branch A executes:

```text
output_schema_change_count = 0
```

Do not add a new financial grounding output field.

A schema change is considered only in Branch B,
after a genuine claim-level model failure is proven.

---

# 24. Calibration remains frozen

Required unchanged:

```text
BUY >= 6.0

SELL >= 6.0

otherwise HOLD

0.5 increments

HOLD 5.5:4.5 = BUY_LEAN

HOLD 5.0:5.0 = NEUTRAL

HOLD 4.5:5.5 = SELL_LEAN

adjacent-bucket conservative tie-break toward 5.0
```

No voting.

No averaging.

No financial scorecard.

---

# 25. QTD/YTD validator remains frozen

M12R repair remains authoritative.

Required:

```text
QTD/YTD semantic change count = 0
```

Historical FIC-FIN-03 must remain PASS.

Positive/negative period fixtures remain unchanged.

---

# 26. Other financial validators remain frozen

Do not alter:

```text
FCF label validation

prior-year-end vs YoY validation

debt completeness validation

normalized earnings validation

financial-sector routing

invalid evidence-ref validation
```

Required:

```text
non_working_capital_financial_validator_change_count = 0
```

---

# 27. Source sufficiency / lifecycle / warning no-change

Required:

```text
source_sufficiency_semantic_change_count = 0

daily_delta_semantic_change_count = 0

monitoring_lifecycle_semantic_change_count = 0

warning_semantic_change_count = 0
```

No source/provider changes.

---

# 28. Price / technical / supply isolation

Required zero:

```text
directional_core_price_technical_refs

directional_core_supply_refs

typed_financial_price_ref_count

typed_financial_technical_ref_count

typed_financial_supply_ref_count
```

No Price-Timing changes.

---

# 29. Phase B deterministic test gate

Only if Branch A repair is executed, before model calls require:

```text
M12B FIC-FIN-02 run-2 = PASS after repair

M12B FIC-FIN-02 run-1 = PASS

M12B FIC-FIN-06 run-1 = PASS

old M12G FIC-FIN-06 = FAIL

corrected FIC-FIN-06 = PASS

positive validator fixtures PASS

negative validator fixtures rejected

FIC-FIN-03 QTD/YTD regression PASS

first-class projection unchanged

Directional prompt unchanged

output schema unchanged

threshold/calibration unchanged

source sufficiency unchanged

Daily Delta unchanged

focused pytest PASS

full pytest PASS

ruff PASS

git diff --check PASS

production side-effect firewall PASS
```

If any fail:

```text
STOP
NO_MODEL_CALLS
```

---

# 30. New full fictional generation

Only after Branch A + deterministic gate pass.

Create a NEW generation.

Use the same frozen 8 cases:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Do not alter values.

Do not remove narrative duplicates.

Do not reuse M12B outputs in the formal stability sample.

---

# 31. Canary topology

Run:

```text
8 subjects

2 contexts

4 subjects per context

3 repetitions
```

Total intended:

```text
6 fictional model calls

24 subject outputs
```

No real issuers.

No judge calls.

---

# 32. Frozen runtime

Use:

```text
model = gpt-5.6-sol

reasoning = xhigh

runtime mode = MODEL_CONTEXT_COUPLED

subjects per context = 4

timeout = 1800 seconds

single authoritative watchdog = true

wrapper auto-retry = 0

batch split = 0
```

Every context:

```text
unique invocation ID

unique runtime namespace

unique working directory

unique session identity
```

---

# 33. Whole-generation stop rule

On any hard semantic failure:

```text
stop generation immediately

preserve emitted outputs

do not selectively continue remaining contexts
```

On runtime failure:

```text
classify runtime separately

no wrapper retry
```

Any repaired rerun requires a new generation.

---

# 34. Canary hard gates

Across all completed outputs require zero:

```text
invalid financial evidence refs

material financial anchor grounding failures

true metric-specific working-capital grounding failures

narrative substitution failures

irrelevant financial ref grounding failures

price/technical/supply Directional refs

partial PPE called FCF

prior-year-end called YoY

partial debt called total debt

total liabilities called debt

normalized/adjusted earnings invention

financial-sector generic misuse

missing optional context treated as bearish

fixed financial scoring

QTD/YTD false reject

QTD/YTD false accept

AI imperative primary action
```

Do NOT count a secondary selected WC item as a failure
merely because it is absent from reevaluation fields,
if it is not a material checkpoint claim.

---

# 35. Canary case expectations

## FIC-FIN-01

Strong quality.

Typed positive financial anchor.

## FIC-FIN-02

Profit/cash divergence.

Hard expectation:

```text
cash conversion limits conviction

typed OCF / simple cash-conversion refs ground the material conclusion

inventory may remain context-only
unless promoted to a material WC claim
```

## FIC-FIN-03

QTD/YTD conflict explicit.

## FIC-FIN-04

Non-operating boost separated from operating improvement.

## FIC-FIN-05

Debt/liquidity pressure grounded to complete typed evidence.

## FIC-FIN-06

Working-capital focal case.

Hard expectation:

```text
inventory / receivables material checkpoint
must be grounded to relevant typed WC refs
```

## FIC-FIN-07

Missing optional context remains nonnegative.

## FIC-FIN-08

Financial-sector generic industrial reasoning remains excluded.

---

# 36. Formal stability

If all 6 calls complete and hard semantics pass,
run the existing formal stability classifier.

Report:

```text
STABLE

BOUNDARY_UNCERTAINTY

UNSTABLE
```

Do not majority-vote.

Do not average balances.

Required:

```text
opposite_direction_reversal_count = 0
```

M12B partial directions are descriptive only and are not part of this new formal sample.

---

# 37. Boundary observation

M12B partial output showed:

```text
FIC-FIN-02:
run-1 SELL 4.0:6.0
run-2 HOLD 4.5:5.5

FIC-FIN-03:
run-1 HOLD 5.0:5.0
run-2 HOLD 5.5:4.5
```

Do not pre-classify these.

If the new full generation completes,
let the existing formal classifier determine:

```text
STABLE / BOUNDARY_UNCERTAINTY / UNSTABLE
```

No calibration change in M12C.

---

# 38. Message specificity advisory

If the full canary completes,
run the existing message/specificity advisory.

Measure:

```text
typed financial anchor specificity

period specificity

case-specific checkpoints

generic substantive repetition

renderer-introduced repetition
```

Advisory remains separate from hard semantics.

---

# 39. No real issuer model calls

Required:

```text
model_calls_real = 0

real_issuer_model_exposure_count = 0
```

Fresh real proof remains a later task.

---

# 40. Production side-effect firewall

Required:

```text
provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Approved 8 monitoring schedule paths remain paused.

---

# 41. Required artifacts — forensic phase

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12c-scope-freeze

04-m12b-partial-canary-reproduction

05-fic-fin-02-run1-grounding-trace

06-fic-fin-02-run2-grounding-trace

07-fic-fin-06-grounding-control-trace

08-working-capital-grounding-trigger-audit

09-grounding-validator-root-cause
```

The root-cause artifact must freeze Branch A or Branch B before implementation.

---

# 42. Required artifacts — Branch A repair

If Branch A executes, produce:

```text
10-working-capital-grounding-contract-before-after

11-materiality-claim-trigger-contract

12-production-validator-genericity-proof

13-fic-fin-02-run2-regression

14-fic-fin-06-old-fail-regression

15-fic-fin-06-corrected-pass-regression

16-positive-grounding-fixtures

17-negative-grounding-fixtures

18-validator-scope-diff
```

---

# 43. Required freeze/no-change proofs

Produce:

```text
19-first-class-projection-freeze-proof

20-directional-prompt-freeze-proof

21-output-schema-freeze-proof

22-qtd-ytd-validator-freeze-proof

23-other-financial-validator-freeze-proof

24-price-timing-no-change-proof

25-source-sufficiency-no-change-proof

26-daily-delta-no-change-proof

27-renderer-ownership-no-change-proof
```

---

# 44. Required deterministic validation

Produce:

```text
28-focused-test-results

29-full-test-results

30-ruff-and-diff-results

31-model-call-gate
```

If Branch B is selected,
`31-model-call-gate` must say:

```text
NO_MODEL_CALLS
```

---

# 45. Required fictional-canary artifacts

Only if Branch A + gate pass:

```text
32-fictional-canary-generation-manifest

33-fictional-canary-source-lock

34-run-1-context-01

35-run-1-context-02

36-run-2-context-01

37-run-2-context-02

38-run-3-context-01

39-run-3-context-02

40-full-fictional-semantic-audit

41-full-fictional-grounding-audit

42-full-fictional-validator-audit

43-full-fictional-stability

44-full-fictional-message-specificity-advisory

45-runtime-observations
```

Preserve raw prompt/schema/output/receipt/transport log/run document.

---

# 46. Required completion artifacts

Produce:

```text
46-fresh-real-proof-readiness-decision

47-production-no-change

48-schedule-pause-observation

49-master-workflow-update

50-program-completion
```

---

# 47. M12C Branch A acceptance criteria

Validator repair is accepted only if:

```text
root cause = selection-triggered overreach
or canary case-contract mismatch

FIC-FIN-02 run-2 becomes PASS for the correct semantic reason

its cash-conversion typed anchor remains grounded

its inventory typed ref remains valid context/Unknown use

FIC-FIN-06 old narrative-only output remains FAIL

corrected FIC-FIN-06 remains PASS

metric-specific ungrounded inventory/AR claims still FAIL

production validator has no fictional ticker/case branches

first-class architecture unchanged

prompt unchanged

schema unchanged

threshold/calibration unchanged

focused/full tests PASS

ruff PASS

git diff --check PASS
```

---

# 48. Full-canary acceptance criteria

If Branch A proceeds to model canary:

```text
6 / 6 model contexts complete

24 / 24 rows schema PASS

hard financial semantic violation count = 0

true working-capital grounding failure count = 0

material-anchor grounding failure count = 0

narrative substitution failure count = 0

QTD/YTD false reject = 0

QTD/YTD false accept = 0

price/technical/supply violations = 0

AI imperative primary action = 0

opposite-direction reversal count = 0

wrapper retry = 0

unexpected timeout/capacity/orphan = 0
```

Formal stability must be measured.

---

# 49. Failure handling

## If forensic says M12B FIC-FIN-02 failure was genuine

Do not repair validator.

Set:

```text
status =
M12C_REVIEW_COMPLETE_OUTPUT_STRUCTURE_REQUIRED

next_scope =
DIRECTIONAL_OUTPUT_FINANCIAL_GROUNDING_SCHEMA_REVIEW_OR_IMPLEMENTATION

fresh_real_proof_readiness =
NOT_READY
```

No model calls.

## If validator repair passes but new semantic blocker appears

Set:

```text
next_scope =
BOUNDED_DIRECTIONAL_FINANCIAL_CONTEXT_REPAIR
```

Do not broaden automatically.

## If hard semantics pass but stability fails

Separate:

```text
interpretation variability

vs

threshold boundary uncertainty
```

Do not change calibration in M12C.

## If runtime fails

Classify runtime separately.

---

# 50. Next-scope decision

## A. Branch A repair + full canary PASS

```text
fresh_real_proof_readiness = READY

next_scope =
FRESH_REAL_FINANCIAL_CONTEXT_GENERALIZATION_PROOF
```

Do not start the real proof inside M12C.

## B. Branch B genuine output-structure failure

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
DIRECTIONAL_OUTPUT_FINANCIAL_GROUNDING_SCHEMA_REVIEW_OR_IMPLEMENTATION
```

## C. New bounded semantic blocker

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
BOUNDED_DIRECTIONAL_FINANCIAL_CONTEXT_REPAIR
```

---

# 51. Production readiness

Even if the full M12C canary passes:

```text
production_readiness = NOT_READY
```

Still required:

```text
fresh unseen real generalization proof

production integration review

explicit user authorization
```

Schedules remain paused.

---

# 52. Program-completion fields

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
report_commit
final_head_sha
branch

latest_result_zip_sha256
latest_result_integrity

m12b_status
m12c_status

working_capital_grounding_root_cause
working_capital_grounding_repair_status

fic_fin_02_run1_regression_status
fic_fin_02_run2_regression_status
fic_fin_06_old_regression_status
fic_fin_06_corrected_regression_status

production_validator_fictional_branch_count

positive_grounding_fixture_count
positive_grounding_fixture_pass_count
negative_grounding_fixture_count
negative_grounding_fixture_rejected_count

financial_context_selection_change_count
first_class_projection_change_count

directional_prompt_change_count
output_schema_change_count

qtd_ytd_validator_semantic_change_count
non_working_capital_financial_validator_change_count

directional_threshold_changed
directional_increment_changed
hold_lean_contract_changed
calibration_tiebreak_changed

source_sufficiency_semantic_change_count
daily_delta_semantic_change_count
warning_semantic_change_count

fictional_generation_id
fictional_subject_count
fictional_context_count
fictional_repetition_count

model_calls_real
model_calls_fictional
model_calls_judge

model_context_success_count
model_context_failure_count
wrapper_retry_count
timeout_count
capacity_failure_count
orphan_process_count

fictional_output_row_count
fictional_schema_pass_count

invalid_financial_reference_count
hard_financial_semantic_violation_count

material_financial_anchor_grounding_failure_count
working_capital_grounding_failure_count
narrative_substitution_failure_count
irrelevant_financial_ref_grounding_failure_count

validator_false_reject_count
validator_false_accept_count

fictional_stable_count
fictional_boundary_uncertainty_count
fictional_unstable_count
opposite_direction_reversal_count

message_specificity_advisory_status

real_issuer_model_exposure_count

provider_source_fetches

production_db_mutations
monitoring_registrations
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count

fresh_real_proof_readiness
production_readiness
status
stop_reason
next_scope
```

Anything unmeasured:

```text
NOT_MEASURED
```

---

# 53. Artifact integrity

Freeze all reports/master workflow/model-call artifacts before final index.

Required:

```text
hash mismatch = 0

size mismatch = 0

secret scan failure = 0
```

Report final result ZIP SHA-256.

---

# 54. Final task principle

M12B materially improved grounding.

The first-class architecture solved the original FIC-FIN-06 failure,
and every selected typed ref in the completed 12-output sample was used somewhere.

The remaining blocker is narrower:

```text
does a secondary selected inventory fact
have to appear in a material reevaluation checkpoint
even when the actual case is cash-conversion divergence
and the inventory fact is used only as context/Unknown?
```

Do not answer that by automatically adding a new output schema.

The correct order is:

```text
reproduce the exact validator trigger

→ distinguish selection-triggered requirement
  from material-claim-triggered requirement

→ repair only if the validator is overreaching

→ preserve FIC-FIN-06 hard working-capital grounding

→ rerun the full fictional canary from a clean generation

→ only then authorize fresh real proof
```

Not:

```text
weaken all financial grounding
```

Not:

```text
special-case FIC-FIN-02
```

Not:

```text
require every selected financial fact in reevaluation fields
```

Not:

```text
add another prompt-only instruction
```

Not:

```text
change thresholds/calibration
```

Not:

```text
start a real cohort before the full fictional canary passes
```

And not:

```text
resume production monitoring
```

Make the grounding contract materiality-aware, not selection-count-driven.
