# Thesis Monitor — Bounded Astra Transport Timeout Review & New Full Fictional Canary

## 0. Task identity

Suggested work-instruction filename:

```text
20260910-bounded-astra-transport-timeout-review-and-new-full-fictional-canary.md
```

Suggested result bundle:

```text
thesis-monitor-20260910-bounded-astra-transport-timeout-review-new-full-fictional-canary-report.zip
```

Master-workflow phase:

```text
M12T — Bounded GPT-6 Astra Transport Timeout Review
       + New Full Fictional Financial Canary
```

This task begins only after M12F completed its deterministic semantic work but stopped during the new GPT-6 Astra canary because:

```text
run-1/context-02
→ MODEL_TIMEOUT
→ 1800-second watchdog
→ no output
```

M12T must first perform a bounded transport/runtime forensic review.

If that review shows no deterministic prompt/config/runtime regression that requires repair,
and the failure is best classified as transient/recoverable transport/service degradation,
M12T must continue in the SAME task to a completely new full 8-subject × 3-repeat fictional canary.

This follows the project rule:

```text
bounded preflight
→ if safe, continue directly to the actual proof
```

M12T must NOT become another preparation-only phase if the transport review passes.

M12T must NOT:

```text
increase timeout automatically
add wrapper auto-retry
split the 4-subject shared context
change model target
change reasoning effort
change financial semantics
change calibration contracts
change source sufficiency
change Price-Timing
change renderer
run real issuers
merge/deploy production
resume monitoring schedules
```

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260909-financial-exclusion-validator-repair-leverage-boundary-full-fictional-canary-report.zip
```

Verified SHA-256:

```text
e04c88c992cdf7e13415e2d0fb945d343c404a8e6cc620e00f26a91a3bb20383
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

The result bundle contains:

```text
202 indexed payloads
0 missing
0 extra
0 hash mismatches
0 size mismatches
```

Recompute the artifact index independently before using the result.

---

# 2. M12F repository provenance

Reported final state:

```text
branch =
codex/20260909-financial-exclusion-leverage-boundary-m12f

base_sha =
19b3dd7bc2353f418452e2db67c78e886a73cbc6

work_instruction_commit =
a7029856c189599c96f6c357bb2c949a47af10a3

implementation_commit =
13d21ae25b9a61b95ab433bb5070d4cf8fb21666

report_commit =
be3ee8d17e97edcf82ab2c8535d222c03b13667f

final_head_sha =
be3ee8d17e97edcf82ab2c8535d222c03b13667f
```

At M12T start use actual repository HEAD as authority.

Record:

```text
actual_base_sha
actual_branch
working_tree_state
remote_branch_sha
```

If unexplained semantic drift exists in the Directional / runtime surface:

```text
STOP
UNEXPLAINED_M12T_BASELINE_DRIFT
```

---

# 3. Model target — frozen

User-authorized model target remains:

```text
Codex authoring:
gpt-6-astra / xhigh

investment-judgment runner:
gpt-6-astra / xhigh
```

M12F successfully measured:

```text
Codex authoring target match = true

investment runner target match = true

fallback count = 0
```

M12T hard rules:

```text
no gpt-5.6-sol fallback

no alternative model fallback

no ultra authoring

no reasoning downgrade

no silent model-id substitution
```

If Codex authoring is not confirmed:

```text
gpt-6-astra / xhigh
```

STOP before code/review changes:

```text
AUTHORING_MODEL_TARGET_MISMATCH
```

If model runner target is unavailable:

```text
STOP
MODEL_TARGET_UNAVAILABLE
```

---

# 4. M12F deterministic semantic work is frozen

M12F completed and passed the intended deterministic repairs.

## 4.1 Financial-sector explicit exclusion validator

Root cause:

```text
TOKEN_PRESENCE_IGNORES_EXPLICIT_NON_APPLICATION_SCOPE
```

Deterministic repair status:

```text
PASS
```

Offline original FIC-FIN-08 replay:

```text
PASS
```

Fixtures:

```text
positive fixtures = 12
negative fixtures = 20
all PASS
```

The repair correctly distinguishes:

```text
"순부채를 적용한다"
from
"보험사에는 일반 영업기업의 순부채 틀을 적용하지 않는다"
```

Do not reopen this validator unless a new full-canary row proves an actual defect.

## 4.2 GPT-6 Astra leverage boundary

M12F froze:

```text
root cause =
ASTRA_OUTPUT_CONSISTENT_WITH_CURRENT_5_5_CONTRACT
```

FIC-FIN-05 case semantics:

```text
CURRENT_ABSOLUTE_CONDITION_WITHOUT_EXPLICIT_BASELINE_DETERIORATION
```

Frozen generic leverage pattern:

```text
complete high interest-bearing debt
+
thin cash
+
stable current operating profit
+
refinancing / maturity severity unresolved

→ HOLD 4.5:5.5 SELL_LEAN
```

under the existing conservative adjacent-bucket contract.

Historical GPT-5.6 Sol SELL 6.0 is:

```text
CROSS_MODEL_DESCRIPTIVE_ONLY
```

Do not force the old-model label.

M12T must not change this leverage contract.

---

# 5. M12F deterministic test status

Local Phase A passed:

```text
focused pytest = 135 / 135 PASS

full pytest = 3174 / 3174 PASS

ruff = PASS

git diff --check = PASS
```

Threshold / ownership protections remained:

```text
6.0 threshold unchanged

0.5 increment unchanged

HOLD lean unchanged

conservative tie-break direction unchanged

source sufficiency unchanged

Daily Delta unchanged

Price-Timing unchanged

renderer ownership unchanged
```

Do not reopen these in the transport review.

---

# 6. M12F new canary runtime result

Generation:

```text
20260909-m12f-fictional-20260909T150248Z-04f38944c4bf
```

Planned:

```text
6 model contexts
24 subject outputs
```

Actual:

```text
model calls attempted = 2

context success = 1

context timeout failure = 1

subject outputs = 4

remaining contexts started = 0
```

Because the generation is incomplete:

```text
formal stability = NOT_MEASURED

core-only stability = NOT_MEASURED

new-buyer variance = NOT_MEASURED

holder variance = NOT_MEASURED

FIC-FIN-05 new observation = NOT_MEASURED

FIC-FIN-08 new observation = NOT_MEASURED
```

Do not infer stability or stance outcome from this partial generation.

---

# 7. Exact M12F transport observations

## context-01

```text
model = gpt-6-astra
effort = xhigh

started =
2026-09-09T15:03:16.283455Z

finished =
2026-09-09T15:22:09.762848Z

elapsed =
1133.48 seconds

status =
PASS

output size =
20429 bytes

wrapper retry =
0

timeout =
false
```

During the call:

```text
WebSocket stream disconnected
Connection reset without closing handshake
CLI internal sampling retry 1/5
```

The CLI recovered and completed.

## context-02

```text
model = gpt-6-astra
effort = xhigh

started =
2026-09-09T15:22:09.811639Z

finished =
2026-09-09T15:52:09.821574Z

elapsed =
1800.01 seconds

status =
MODEL_TIMEOUT

output =
none

return code =
-15

wrapper retry =
0
```

During the call:

```text
WebSocket stream disconnected
Connection reset without closing handshake
CLI internal sampling retry 1/5
```

The call did not return before the authoritative watchdog expired.

---

# 8. Historical GPT-6 Astra transport baseline

The immediately prior M12E GPT-6 Astra/xhigh attempt used the same:

```text
1800-second watchdog

4 subjects per shared context

MODEL_CONTEXT_COUPLED runtime

no wrapper retry
```

and completed both attempted model contexts successfully.

Measured M12E elapsed times:

```text
context-01:
~371.47 seconds

context-02:
~329.53 seconds
```

M12E prompt byte sizes:

```text
context-01 = 28025 bytes

context-02 = 19420 bytes
```

M12F prompt byte sizes:

```text
context-01 = 28025 bytes

context-02 = 19420 bytes
```

Therefore:

```text
M12F did NOT increase prompt byte size relative to the prior Astra attempt.
```

Observed runtime slowdown:

```text
context-01:
~371 sec → ~1133 sec

context-02:
~330 sec → timeout at 1800 sec
```

Both M12F contexts also experienced a WebSocket reset/retry event.

This strongly supports investigating:

```text
transient transport/service slowdown
or
CLI internal post-disconnect sampling behavior
```

before assuming:

```text
prompt/context-size regression.
```

It does NOT by itself prove the exact backend root cause.

---

# 9. M12T transport forensic classifications

Freeze exactly one primary root cause before the new canary.

Allowed primary classifications:

```text
TRANSIENT_WEBSOCKET_OR_SERVICE_DEGRADATION_LIKELY

REPEATABLE_ASTRA_CONTEXT_LATENCY_REGRESSION

PROMPT_OR_CONTEXT_BUDGET_REGRESSION

LOCAL_RESOURCE_OR_PROCESS_CONTENTION

RUNNER_WATCHDOG_OR_PROCESS_MANAGEMENT_DEFECT

CLI_INTERNAL_RETRY_RECOVERY_DEFECT

UNRESOLVED_RUNTIME_CAUSE

MIXED
```

Secondary classifications may be recorded.

Do not use:

```text
MODEL_SEMANTIC_FAILURE
```

for a call that produced no output.

---

# 10. Required offline runtime comparison

Before any new model call, compare M12E vs M12F:

```text
model id

reasoning effort

CLI version

runtime mode

timeout

batch size

subject ordering

prompt byte size

schema byte size

runtime namespace isolation

working-directory isolation

network readiness

WebSocket retry events

wrapper retry count

process exit behavior

output existence

elapsed time
```

Also inspect:

```text
transport logs

receipts

run documents

runtime-state metadata
```

Do not rely only on summary reports.

---

# 11. Prompt/context regression gate

Required:

```text
M12F context-01 prompt size
vs
M12E context-01 prompt size

M12F context-02 prompt size
vs
M12E context-02 prompt size
```

Already observed:

```text
byte sizes identical
```

Verify this from the source artifacts.

If semantic prompt content changed,
classify whether the change could reasonably explain a 3×–5× runtime shift.

Do not equate:

```text
different prompt hash
```

with:

```text
runtime regression
```

without content-size/complexity evidence.

If a true prompt explosion or accidental duplication is found:

```text
STOP
M12T_PROMPT_CONTEXT_REGRESSION_FOUND
```

and repair that bounded issue before model calls.

---

# 12. Runtime isolation gate

Verify M12F:

```text
unique invocation IDs

unique runtime namespaces

unique working directories

unique session IDs

no orphan process

no previous process holding the same runtime state
```

If isolation is broken:

```text
STOP
M12T_RUNTIME_ISOLATION_DEFECT
```

Do not run the full canary until repaired.

---

# 13. Watchdog policy remains frozen by default

Current authoritative timeout:

```text
1800 seconds
```

M12T default:

```text
KEEP 1800
```

Do NOT automatically increase the timeout merely because one call timed out.

Do NOT add wrapper retry.

Do NOT add batch split.

Required default:

```text
timeout change count = 0

wrapper retry change count = 0

subjects-per-context change count = 0
```

A timeout increase requires a separately justified runtime contract
and must not be introduced in M12T unless the forensic evidence proves
the current timeout is systematically below normal Astra/xhigh completion time.

Given prior Astra contexts completed in ~330–371 seconds,
one 1800-second timeout is insufficient evidence for such a change.

---

# 14. CLI internal retry policy

The logs show:

```text
codex_core::responses_retry
stream disconnected
retrying sampling request (1/5)
```

This is an internal CLI sampling recovery,
not the Thesis Monitor wrapper retry.

M12T must distinguish:

```text
CLI_INTERNAL_RETRY

THESIS_MONITOR_WRAPPER_RETRY
```

Wrapper retry remains:

```text
0
```

Do not claim "no retry at all" if the CLI internally retried.

Report:

```text
cli_internal_retry_event_count

wrapper_retry_count
```

separately.

Do not modify undocumented/internal CLI retry behavior unless a supported configuration already exists and a generic defect is proven.

---

# 15. New full-canary authorization gate

M12T may continue to a new full canary in the SAME task only if the runtime review finds:

```text
no prompt/context-size regression requiring repair

no runtime namespace/isolation defect

no watchdog/process-management defect

no local deterministic configuration defect

model target = gpt-6-astra / xhigh

network readiness passes

semantic code/config remains frozen
```

and the primary runtime classification is one of:

```text
TRANSIENT_WEBSOCKET_OR_SERVICE_DEGRADATION_LIKELY

UNRESOLVED_RUNTIME_CAUSE
```

with no evidence of a deterministic local defect.

This is the user's authorization for:

```text
bounded review
→ if safe
→ new full fictional generation
```

No separate confirmation turn is required.

---

# 16. If the runtime review finds a deterministic defect

Examples:

```text
namespace reuse

stale working directory

duplicated prompt payload

watchdog ownership bug

process-group cleanup defect

runner starts two model processes

broken stdin/stdout handling
```

Then:

```text
repair only that bounded runtime defect
```

Run:

```text
focused tests
full tests
ruff
git diff --check
```

Then, if the repair is deterministic and safe,
continue in the same task to the new full canary.

Do not alter financial semantics.

---

# 17. If the runtime cause remains unresolved

If:

```text
no deterministic defect is found

and historical Astra contexts show the same workload can complete well under 1800 seconds
```

classify:

```text
TRANSIENT_WEBSOCKET_OR_SERVICE_DEGRADATION_LIKELY
```

or:

```text
UNRESOLVED_RUNTIME_CAUSE_NO_LOCAL_DEFECT
```

Then run ONE clean new full generation under the same frozen runtime.

Do not run a separate one-context probe first.
The full generation itself is the proof.

If another timeout occurs:

```text
stop immediately

do not increase timeout

do not retry the context

do not continue remaining contexts

next_scope =
ASTRA_TRANSPORT_RUNTIME_ARCHITECTURE_REVIEW
```

A repeated timeout provides materially stronger evidence than the single M12F timeout.

---

# 18. Hosted CI portability — separate P1, but new regression must not worsen

M12F hosted CI was red:

```text
3169 passed
5 failed
lint skipped
```

Failure classes:

```text
3 historical git-object dependencies

2 local-only archive dependencies

1 of those local-archive failures was newly added by M12F
```

This is NOT the M12F model timeout root cause.

M12T should not let CI portability work expand the transport-review scope.

However:

```text
do not add any new local-absolute-path test dependency

do not add new unavailable historical git-object dependencies
```

If a small semantic-neutral change in M12T touches tests,
new tests must be hosted-CI portable.

Preserve a separate P1 handoff:

```text
HOSTED_CI_HISTORICAL_ARTIFACT_PORTABILITY_REPAIR
```

unless the user later authorizes merging it into a dedicated engineering cleanup task.

Do not claim hosted CI PASS unless it actually passes.

---

# 19. Semantic contracts remain frozen

M12T must preserve M12F/M12E contracts:

```text
financial exclusion assertion-vs-exclusion validator

FIC-FIN-05 leverage boundary

M12E 5.0 / 5.5 / 6.0 / 6.5 boundary clarification

shared-lineage corroboration rule

limiting-Unknown ordinal rule

flat operations + non-operating support rule

minimum vs stronger direction rule

first-class typed financial evidence

material-claim scoped WC grounding

QTD/YTD Korean validator

FCF / YoY / debt-completeness / normalized-earnings validators
```

Required:

```text
financial semantic change count = 0

Directional calibration semantic change count = 0

source mapping change count = 0
```

unless a true runtime defect requires semantic-neutral plumbing.

---

# 20. Threshold / ownership freeze

Required unchanged:

```text
BUY threshold = 6.0

SELL threshold = 6.0

increment = 0.5

HOLD 5.5:4.5 = BUY_LEAN

HOLD 5.0:5.0 = NEUTRAL

HOLD 4.5:5.5 = SELL_LEAN

conservative adjacent tie-break toward 5.0
```

No:

```text
majority voting

balance averaging

financial scorecard
```

Price-Timing remains separate.

Renderer remains primary action wording owner.

---

# 21. Phase A deterministic gate

Before new model calls require:

```text
authoring model = gpt-6-astra / xhigh

latest result ZIP integrity PASS

runtime comparison complete

root cause classification frozen

prompt/context regression audit PASS

runtime isolation audit PASS

watchdog ownership audit PASS

no semantic contract drift

exclusion validator deterministic regressions PASS

leverage contract deterministic replay PASS

M12E boundary calibration fixtures PASS

first-class financial regressions PASS

QTD/YTD regressions PASS

WC grounding regressions PASS

source sufficiency unchanged

Daily Delta unchanged

Price-Timing unchanged

renderer unchanged

focused pytest PASS

full local pytest PASS

ruff PASS

git diff --check PASS

production side-effect firewall PASS
```

If Phase A fails:

```text
STOP
NO_MODEL_CALLS
```

---

# 22. New full GPT-6 Astra generation

If Phase A passes, create a NEW generation ID.

Use the exact frozen 8 fictional subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Do not reuse M12F model output in the new formal sample.

Do not continue from M12F run-1/context-02.

Start:

```text
run-1/context-01
```

---

# 23. Full canary topology

Run:

```text
8 subjects

2 shared contexts

4 subjects/context

3 repetitions
```

Total intended:

```text
6 fictional model calls

24 subject outputs
```

No real issuer calls.

No judge calls.

---

# 24. Frozen runtime for new generation

Use:

```text
model = gpt-6-astra

reasoning = xhigh

runtime mode = MODEL_CONTEXT_COUPLED

subjects per context = 4

timeout = 1800 seconds

single authoritative watchdog = true

wrapper auto-retry = 0

batch split = 0
```

Each context must have:

```text
unique invocation ID

unique runtime namespace

unique working directory

unique session identity
```

Receipt must record:

```text
observed model

observed effort

elapsed seconds

timeout

CLI internal retry events

wrapper retry events
```

---

# 25. Whole-generation stop rule

If any context:

```text
times out

transport fails

hard semantic validation fails
```

then:

```text
stop immediately

preserve all emitted artifacts

do not selectively continue later contexts

do not wrapper-retry

do not increase timeout inside the generation
```

Any later retry after repair/review must be a NEW generation.

---

# 26. Runtime success gate

For the full generation require:

```text
6 / 6 contexts return successfully

timeout count = 0

capacity failure = 0

orphan count = 0

wrapper retry count = 0
```

CLI internal retries are permitted only as observed runtime behavior,
but must be reported separately.

A successful call with an internal WebSocket reconnect is:

```text
transport recovered
```

not:

```text
no transport issue
```

---

# 27. Financial hard-semantic gates

Across all 24 rows require zero:

```text
invalid financial evidence refs

material financial grounding failures

WC grounding failures

narrative substitution failures

price / technical / supply Directional refs

partial PPE called FCF

prior-year-end called YoY

partial debt called total debt

total liabilities called debt

normalized / adjusted earnings invention

financial-sector true generic-industrial misuse

explicit-exclusion false reject

missing optional evidence treated as bearish

fixed financial scoring

QTD/YTD false reject

QTD/YTD false accept

AI imperative primary action
```

---

# 28. GPT-6 Astra bucket contracts to validate

## FIC-FIN-01

Current M12E boundary contract target:

```text
BUY 6.0:4.0
```

when stronger 6.5 support remains unresolved.

All 3 repetitions should be contract-consistent.

## FIC-FIN-02

Current target:

```text
HOLD 4.5:5.5
SELL_LEAN
```

under:

```text
cash-conversion divergence

shared-lineage financial evidence

causal/reversibility Unknown
```

## FIC-FIN-04

Current target:

```text
HOLD 5.0:5.0
NEUTRAL
```

when:

```text
operations are flat

positive offset is financial/non-operating composition

no separate positive issuer-level anchor exists
```

## FIC-FIN-05

M12F frozen Astra leverage target:

```text
HOLD 4.5:5.5
SELL_LEAN
```

under:

```text
complete high debt
thin cash
stable operating profit
refinancing/maturity severity unresolved
```

Do not compare against the old Sol SELL label as a hard target.

---

# 29. FIC-FIN-08 exclusion contract

Each repetition must allow:

```text
"보험사에는 일반 영업기업의 순부채/운전자본 틀을 적용하지 않는다"
```

as explicit non-applicability.

Hard require:

```text
explicit exclusion false reject = 0

actual financial-sector industrial-framework misuse = 0
```

The validator must still reject actual misuse elsewhere in the candidate.

---

# 30. Core balance stability

If full generation completes:

Required target:

```text
FIC-FIN-01 directional balance unique count = 1

FIC-FIN-02 directional balance unique count = 1

FIC-FIN-04 directional balance unique count = 1

FIC-FIN-05 directional balance unique count = 1
```

Each stable balance must also be contract-consistent.

Do not accept:

```text
stable but semantically wrong
```

---

# 31. Formal stability

Run the unchanged formal classifier.

Report:

```text
STABLE

BOUNDARY_UNCERTAINTY

UNSTABLE
```

Do not:

```text
majority vote

average balances

weaken classifier
```

Required:

```text
opposite-direction reversal count = 0
```

---

# 32. Stance follow-up

Measure:

```text
fundamental_new_buyer stance variance

fundamental_holder stance variance
```

Do not repair them in M12T.

If core direction/balance is fully stable but stance fields remain formally ambiguous:

```text
fresh_real_proof_readiness = NOT_READY
```

and choose the smallest bounded stance follow-up.

Default sequencing if both remain:

```text
new-buyer stance first

holder stance second
```

unless the full Astra sample proves another order.

---

# 33. Cross-model comparison

Produce:

```text
CROSS_MODEL_DESCRIPTIVE_ONLY
```

table for:

```text
GPT-5.6 Sol historical M12D

GPT-6 Astra M12E partial

GPT-6 Astra M12F partial

GPT-6 Astra M12T new full generation
```

Do NOT compute a same-model improvement rate across Sol and Astra.

Within Astra generations, compare only when contracts and inputs are the same.

---

# 34. Runtime latency audit for new generation

For each of 6 calls record:

```text
elapsed seconds

prompt bytes

output bytes

internal CLI retry events

network readiness

timeout status
```

Summarize:

```text
median elapsed

max elapsed

context-01 distribution

context-02 distribution

internal retry count

retry-success count
```

Compare descriptively with M12E and M12F.

Do not automatically turn latency variance into a correctness failure if all calls complete under the frozen watchdog.

---

# 35. Message specificity advisory

If full generation completes, run the existing advisory.

Measure:

```text
typed financial anchor specificity

period specificity

case-specific checkpoints

generic substantive repetition

renderer-introduced repetition
```

Do not modify renderer/message copy in M12T.

---

# 36. Real issuer exposure

Required:

```text
model_calls_real = 0

real_issuer_model_exposure_count = 0
```

Fresh real proof remains later.

---

# 37. Production side-effect firewall

Required:

```text
provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Observe the approved 8 paused schedules at start/end.

---

# 38. Required artifacts — provenance / integrity

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12t-scope-freeze

04-model-target-contract

05-authoring-model-provenance

06-investment-runner-model-provenance
```

---

# 39. Required transport forensic artifacts

Produce:

```text
07-m12e-vs-m12f-runtime-comparison

08-prompt-context-size-comparison

09-runtime-isolation-audit

10-watchdog-process-management-audit

11-websocket-internal-retry-audit

12-context-01-latency-forensic

13-context-02-timeout-forensic

14-transport-root-cause-classification

15-runtime-change-decision
```

---

# 40. Required frozen-semantic proof artifacts

Produce:

```text
16-financial-exclusion-validator-freeze-proof

17-leverage-boundary-freeze-proof

18-m12e-bucket-calibration-freeze-proof

19-first-class-financial-evidence-freeze-proof

20-working-capital-validator-freeze-proof

21-qtd-ytd-validator-freeze-proof

22-other-financial-validator-freeze-proof

23-source-sufficiency-no-change-proof

24-daily-delta-no-change-proof

25-price-timing-no-change-proof

26-renderer-ownership-no-change-proof
```

---

# 41. Required deterministic validation artifacts

Produce:

```text
27-focused-test-results

28-full-local-test-results

29-ruff-and-diff-results

30-hosted-ci-portability-observation

31-model-call-gate
```

Do not claim hosted CI PASS unless actually green.

Preserve the separate P1 portability backlog if still red.

---

# 42. Required new full-canary artifacts

If Phase A passes:

```text
32-fictional-canary-generation-manifest

33-fictional-canary-source-lock

34-run-1-context-01

35-run-1-context-02

36-run-2-context-01

37-run-2-context-02

38-run-3-context-01

39-run-3-context-02

40-full-fictional-semantic-audit

41-full-fictional-exclusion-validator-audit

42-full-fictional-leverage-boundary-audit

43-full-fictional-grounding-audit

44-full-fictional-formal-stability

45-full-fictional-core-only-stability

46-full-fictional-stance-variance-audit

47-full-fictional-runtime-latency-audit

48-cross-model-descriptive-table

49-full-fictional-message-specificity-advisory

50-runtime-observations
```

Preserve for every attempted context:

```text
prompt

schema

raw output

receipt

transport log

run document
```

---

# 43. Required completion artifacts

Produce:

```text
51-transport-proof-success-decision

52-exclusion-validator-full-proof-decision

53-leverage-boundary-full-proof-decision

54-core-balance-stability-decision

55-new-buyer-stance-followup-decision

56-holder-stance-followup-decision

57-fresh-real-proof-readiness-decision

58-hosted-ci-portability-handoff

59-production-no-change

60-schedule-pause-observation

61-master-workflow-update

62-program-completion
```

---

# 44. M12T Phase A acceptance criteria

Before any new model call:

```text
authoring model = gpt-6-astra / xhigh

latest result integrity = PASS

M12E vs M12F runtime comparison complete

prompt/context size regression = false

runtime isolation defect = false

watchdog/process-management defect = false

local deterministic transport config defect = false
OR bounded defect repaired + tests PASS

timeout remains 1800 unless a separately proven systematic contract defect exists

wrapper retry remains 0

subjects per context remains 4

semantic contracts unchanged

focused/full local tests PASS

ruff PASS

git diff --check PASS

production side-effect firewall PASS
```

---

# 45. M12T full-canary acceptance criteria

If new generation runs:

```text
6 / 6 contexts return successfully

24 / 24 rows schema PASS

timeout count = 0

capacity failure = 0

orphan count = 0

wrapper retry = 0

hard financial semantic violation count = 0

explicit exclusion false reject = 0

financial-sector true misuse = 0

grounding failures = 0

QTD/YTD false reject = 0

QTD/YTD false accept = 0

price/technical/supply violation = 0

AI imperative primary action = 0

FIC-FIN-01 balance unique count = 1

FIC-FIN-02 balance unique count = 1

FIC-FIN-04 balance unique count = 1

FIC-FIN-05 balance unique count = 1

target bucket contract violation count = 0

opposite-direction reversal count = 0
```

Formal stability must be measured.

---

# 46. Failure handling

## A. Second Astra timeout occurs under the same clean runtime

Stop.

Do not increase timeout.

Do not retry the failed context.

Set:

```text
next_scope =
ASTRA_TRANSPORT_RUNTIME_ARCHITECTURE_REVIEW
```

The repeated timeout materially strengthens the case for a runtime-level adaptation.

## B. Hard semantic failure

Set:

```text
next_scope =
BOUNDED_DIRECTIONAL_FINANCIAL_CONTEXT_REPAIR_GPT6_ASTRA
```

using the exact failed contract.

## C. Core bucket contract inconsistency

Set:

```text
next_scope =
BOUNDED_ASTRA_BOUNDARY_CALIBRATION_REPAIR
```

Do not change threshold automatically.

## D. Core stable but new-buyer stance ambiguous

```text
next_scope =
BOUNDED_NEW_BUYER_STANCE_CALIBRATION_REPAIR_GPT6_ASTRA
```

## E. Core stable but holder stance ambiguous

```text
next_scope =
BOUNDED_HOLDER_STANCE_CALIBRATION_REPAIR_GPT6_ASTRA
```

---

# 47. Fresh-real readiness

Only set:

```text
fresh_real_proof_readiness = READY
```

if:

```text
transport proof complete

full 6-call fictional generation complete

hard semantics pass

core balance/direction stable

formal stability has no unresolved material stance ambiguity

no real issuer exposure occurred
```

Then:

```text
next_scope =
FRESH_REAL_FINANCIAL_CONTEXT_GENERALIZATION_PROOF_GPT6_ASTRA_XHIGH
```

Do not start that real proof inside M12T.

Otherwise:

```text
fresh_real_proof_readiness = NOT_READY
```

---

# 48. Production readiness

Even if M12T passes:

```text
production_readiness = NOT_READY
```

Still required:

```text
fresh unseen real GPT-6 Astra generalization proof

production integration review

explicit user authorization
```

Monitoring remains paused.

---

# 49. Program-completion fields

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
report_commit
final_head_sha
branch

latest_result_zip_sha256
latest_result_integrity

m12f_status
m12t_status

implementation_model_target
implementation_reasoning_effort
investment_judgment_model_target
investment_judgment_reasoning_effort
authoring_model_target_match
investment_runner_target_match
model_target_fallback_count

m12e_context01_elapsed_seconds
m12e_context02_elapsed_seconds
m12f_context01_elapsed_seconds
m12f_context02_elapsed_seconds

m12e_context01_prompt_bytes
m12e_context02_prompt_bytes
m12f_context01_prompt_bytes
m12f_context02_prompt_bytes

transport_root_cause
runtime_change_required
timeout_seconds
timeout_change_count
wrapper_retry_change_count
subjects_per_context_change_count

cli_internal_retry_event_count
wrapper_retry_count

fictional_generation_id
fictional_subject_count
fictional_context_count
fictional_repetition_count

model_calls_real
model_calls_fictional
model_calls_judge

model_context_success_count
model_context_failure_count
timeout_count
capacity_failure_count
orphan_process_count

fictional_output_row_count
fictional_schema_pass_count

hard_financial_semantic_violation_count
explicit_exclusion_false_reject_count
financial_sector_true_misuse_count
grounding_failure_count

fic_fin_01_directional_balance_unique_count
fic_fin_02_directional_balance_unique_count
fic_fin_04_directional_balance_unique_count
fic_fin_05_directional_balance_unique_count
target_bucket_contract_violation_count

formal_stable_count
formal_boundary_uncertainty_count
formal_unstable_count
opposite_direction_reversal_count

core_only_stable_count
core_only_unstable_count

new_buyer_stance_variance_subject_count
holder_stance_variance_subject_count

message_specificity_advisory_status

hosted_ci_status
hosted_ci_failure_count
hosted_ci_portability_backlog_count

real_issuer_model_exposure_count

provider_source_fetches
production_db_mutations
monitoring_registrations
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count

fresh_real_proof_readiness
production_readiness
status
stop_reason
next_scope
```

Anything not measured must be:

```text
NOT_MEASURED
```

---

# 50. Artifact integrity

Freeze:

```text
program completion

master workflow

all runtime forensic artifacts

all attempted model-call artifacts
```

before final artifact index creation.

Required:

```text
hash mismatch = 0

size mismatch = 0

secret scan failure = 0
```

Report final ZIP SHA-256.

---

# 51. Final task principle

M12F did not fail because the exclusion repair or leverage contract was wrong.

Its deterministic work passed.

The new proof was interrupted by runtime behavior:

```text
same Astra/xhigh workload
same prompt byte sizes
same 1800-second watchdog

prior Astra:
~5–6 minute completion

M12F:
~18.9 minute recovered context
+
30-minute timeout
```

with WebSocket reset events in both M12F contexts.

The correct next move is:

```text
forensically rule out a local deterministic defect

→ keep timeout / wrapper retry / batch size unchanged by default

→ if safe, immediately start one clean NEW full fictional generation

→ if a second timeout occurs, stop and escalate the runtime architecture
   instead of stretching the watchdog or adding retries
```

Not:

```text
increase timeout after one failure
```

Not:

```text
auto-retry the timed-out context
```

Not:

```text
split the cohort silently
```

Not:

```text
change financial semantics
```

Not:

```text
force GPT-6 Astra to reproduce GPT-5.6 Sol labels
```

Not:

```text
run real issuers before the full fictional proof finishes
```

And not:

```text
resume production monitoring
```

Review the runtime once, then prove the frozen GPT-6 Astra contract cleanly if the local system is sound.
