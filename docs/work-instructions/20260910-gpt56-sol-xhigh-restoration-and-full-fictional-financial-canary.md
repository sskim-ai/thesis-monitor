# Thesis Monitor — GPT-5.6 Sol / xhigh Restoration & Full Fictional Financial Canary

## 0. Task identity

Suggested work-instruction filename:

```text
20260910-gpt56-sol-xhigh-restoration-and-full-fictional-financial-canary.md
```

Suggested result bundle:

```text
thesis-monitor-20260910-gpt56-sol-xhigh-restoration-full-fictional-financial-canary-report.zip
```

Master-workflow phase:

```text
M12W — GPT-5.6 Sol / xhigh Restoration
       + Latest Semantic Contract Freeze
       + Full Fictional Financial Canary
```

This task begins after M12V failed its selected GPT-6 Astra runtime architecture proof.

The project decision is now:

```text
proof-critical investment judgment model =
gpt-5.6-sol / xhigh

default Codex authoring/review target =
gpt-5.6-sol / xhigh

GPT-6 Astra =
SUSPENDED_FROM_PROOF_CRITICAL_PATH
```

The reason is not that GPT-6 Astra produced bad investment reasoning.

The reason is:

```text
intermittent no-final-output runtime failure remained unresolved

M12F:
1800-second timeout

M12U:
1800-second timeout

M12V:
runtime architecture changed to a 2400-second finite cap
→ first context still timed out at ~2400 seconds
→ output bytes = 0

backend/request progress remained unobservable

supported same-account direct Astra transport was unavailable
```

This satisfies the previously agreed rollback condition:

```text
if Astra fails again
or root cause remains unclear
→ return the proof-critical investment runner to GPT-5.6 Sol / xhigh
```

M12W must not spend more time debugging Astra.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260910-astra-transport-runtime-architecture-review-full-fictional-canary-report.zip
```

Verified SHA-256:

```text
ca5fabf54d8d88f37073b2d4923de49e27520b94d2ad910eb52f988ccb3de092
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Artifact integrity independently verified:

```text
artifact-index payloads = 146
missing = 0
extra = 0
hash mismatch = 0
size mismatch = 0
```

Recompute before trusting the bundle.

---

# 2. M12V decisive runtime facts

M12V selected:

```text
preferred_runtime_architecture =
INCREASED_FINITE_ABSOLUTE_WATCHDOG
```

Old Astra cap:

```text
1800 seconds
```

Selected experimental Astra cap:

```text
2400 seconds
```

Reasoning:

```text
retain finite hard stop

retain wrapper retry = 0

retain 4 subjects/context

test whether additional long-tail tolerance
would allow intermittent Astra requests to complete
```

Actual M12V proof:

```text
run-1/context-01

model =
gpt-6-astra

effort =
xhigh

configured timeout =
2400 seconds

elapsed =
~2404.42 seconds

status =
MODEL_TIMEOUT

stdout bytes =
0

output exists =
false

wrapper retry =
0

CLI internal retry =
0

orphan =
0
```

Therefore the selected Astra tail-tolerance hypothesis failed.

---

# 3. M12V unresolved cause — frozen

M12V runtime contract recorded:

```text
root_cause =
INTERMITTENT_NO_FINAL_OUTPUT_WITH_UNRESOLVED_REMOTE_OR_TRANSPORT_CAUSE
```

Observed limitation:

```text
backend request accepted state = unavailable

backend response ID = unavailable

backend sampling progress = unavailable

reliable backend progress heartbeat = unavailable
```

CLI JSON/event support exists,
but M12V found it does NOT provide a documented recurring sampling heartbeat
suitable for safely extending an idle deadline.

Supported same-account direct Astra/xhigh transport:

```text
UNSUPPORTED
```

Do not reopen these options in M12W.

---

# 4. Astra status from M12W forward

Freeze:

```text
GPT-6 Astra is not deleted from the project.

It is removed from:
- proof-critical fictional stability validation
- fresh unseen real generalization proof
- production-readiness evidence

until separately reauthorized.
```

Astra may later be used only in a separate:

```text
MODEL_COMPATIBILITY_OR_QUALITY_EXPERIMENT
```

That later experiment must not block Thesis Monitor completion.

M12W model calls:

```text
Astra calls = 0
```

---

# 5. GPT-5.6 Sol target — authoritative

Canonical runtime target:

```text
model =
gpt-5.6-sol

reasoning effort =
xhigh
```

Apply to:

```text
fictional investment-judgment canary

future fresh real investment-judgment proof

proof-critical calibration validation
```

Default Codex authoring/review target for M12W:

```text
gpt-5.6-sol / xhigh
```

Hard proof rule:

```text
investment runner MUST attest:
gpt-5.6-sol / xhigh

otherwise:
STOP
SOL_RUNNER_MODEL_TARGET_MISMATCH
```

Codex authoring provenance rule:

```text
request gpt-5.6-sol / xhigh

record actual provenance

if the environment explicitly attests a different selected authoring model:
STOP
AUTHORING_MODEL_TARGET_MISMATCH

if authoring model attestation is technically unavailable:
record NOT_MEASURED
do not falsely claim a match
```

No fallback to Astra.

No fallback to another model.

---

# 6. Historical Sol runtime evidence

GPT-5.6 Sol / xhigh has materially stronger proof history in this project.

Historical evidence includes prior full real/fake workloads where the project recorded:

```text
full 32-context real proof completion
timeout = 0
capacity failure = 0
orphan = 0
```

and the pre-Astra M12D fictional financial-context proof:

```text
6 / 6 fictional model contexts complete

24 / 24 outputs schema PASS

timeout = 0
capacity failure = 0
orphan = 0
wrapper retry = 0
```

Those historical outputs used earlier semantic contracts
and are NOT direct regressions for M12W.

But they establish that:

```text
gpt-5.6-sol / xhigh
+
4 subjects/context
+
finite 1800-second watchdog
+
wrapper retry 0
```

has already completed the relevant project workload class.

At task start independently locate and verify the available historical Sol runtime receipts.

If historical evidence cannot be found:

```text
do not fabricate it
record HISTORICAL_SOL_RUNTIME_EVIDENCE_NOT_FOUND
```

The user decision to restore Sol remains valid regardless.

---

# 7. Runtime contract after restoration

M12W proof-critical runtime:

```text
model =
gpt-5.6-sol

reasoning =
xhigh

runtime mode =
MODEL_CONTEXT_COUPLED

subjects per context =
4

timeout =
1800 seconds

single authoritative watchdog =
true

wrapper auto-retry =
0

batch split =
0
```

This intentionally removes the Astra-specific:

```text
2400-second experimental timeout
```

from the proof-critical runner.

Required:

```text
selected_timeout_seconds = 1800

wrapper_retry_count = 0

subjects_per_context = 4
```

No selective retry.

No sample stitching.

---

# 8. M12V runtime instrumentation — retain if semantic-neutral

M12V added/strengthened runtime observability such as:

```text
process spawn time

first/last stderr/stdout byte timing where measurable

configured timeout

termination initiator

process cleanup state

runtime namespace/workdir identity

CLI internal retry count

output byte counts
```

These are useful.

Do NOT delete them merely because Astra is suspended.

Retain instrumentation only if:

```text
it is transport/model neutral

it does not change prompt/model semantics

it does not alter process behavior beyond observation

it works under gpt-5.6-sol
```

Remove or bypass only Astra-specific runtime assumptions.

Produce:

```text
runtime instrumentation compatibility proof
```

---

# 9. Latest semantic baseline — M12U remains authoritative

M12V was runtime-focused.

M12W must preserve the latest completed semantic changes from M12U.

These include:

## 9.1 First-class typed financial evidence

```text
selected typed financial refs
→ ordinary evidence[] first-class projection

same aliases

same canonical refs

neutral factual statements

financial_decision_context retained
```

## 9.2 Working-capital grounding

```text
material-claim / metric-use scoped

not selection-count triggered
```

## 9.3 QTD/YTD Korean period semantics

Valid explicit period contrast includes:

```text
분기
최근 분기
누적
누계
연초 이후
YTD
```

when linked to the relevant QTD/YTD evidence.

## 9.4 Explicit financial-sector exclusion

The validator must recognize true non-application semantics including:

```text
적용하지 않는다

적용 대상이 아니다

평가 대상이 아니다

is not applicable

does not apply
```

without immunizing actual misuse.

## 9.5 Market expectation / leverage economic independence

Frozen M12U classification:

```text
CONDITIONAL_ON_SAME_UNRESOLVED_RISK
```

For the leverage scenario:

```text
market-expectation evidence that depends on
the same unresolved refinancing downside

does NOT automatically provide independent corroboration
for crossing from 5.5 SELL_LEAN to minimum SELL 6.0.
```

This is a generic economic-independence rule.

Do not undo it merely because the model returns to Sol.

---

# 10. Latest Directional ordinal contract — preserve

Freeze:

```text
BUY if buy >= 6.0

SELL if sell >= 6.0

otherwise HOLD

balance increments = 0.5
```

HOLD lean:

```text
5.5:4.5 = BUY_LEAN

5.0:5.0 = NEUTRAL

4.5:5.5 = SELL_LEAN
```

Existing conservative tie-break:

```text
if supplied evidence reasonably fits adjacent buckets,
choose the less-directional bucket toward 5.0
```

No threshold change.

No increment change.

No voting.

No balance averaging.

No fixed scorecard.

---

# 11. M12E/M12U boundary clarifications — preserve

Keep:

## Shared-lineage corroboration

```text
multiple metrics sharing substantially the same underlying economic/source lineage
do not automatically provide independent corroboration.
```

## Limiting Unknowns

```text
material causal/reversibility/persistence Unknowns
limit justified directional strength
when adjacent buckets are both supportable.

Unknown is not mechanically negative evidence.
```

## Flat operations + non-operating boost

```text
non-operating/financial benefit alone
does not create positive BUY_LEAN
when underlying operations are broadly flat
and no separate issuer-level positive anchor exists.
```

## Minimum 6.0 vs stronger 6.5+

```text
6.5+ requires support beyond the minimum directional conclusion:
persistence, visibility, quality, independent corroboration,
or valuation support.
```

---

# 12. Frozen fictional target contracts

M12W uses the latest generic contract,
not historical model labels.

## FIC-FIN-01

Generic target:

```text
BUY 6.0 : 4.0
```

when minimum BUY support exists
but stronger 6.5 confirmation remains unresolved.

## FIC-FIN-02

Generic target:

```text
HOLD 4.5 : 5.5
SELL_LEAN
```

for:

```text
profit/accounting improvement
+
weak OCF / cash conversion
+
shared-lineage cash evidence
+
material cause/reversibility Unknown
```

without a distinct established negative corroborating axis.

## FIC-FIN-04

Generic target:

```text
HOLD 5.0 : 5.0
NEUTRAL
```

when:

```text
underlying operations are broadly flat

reported net income is helped by non-operating/financial effect

no separate positive issuer-level operating/business/cash/
balance-sheet/valuation anchor exists
```

## FIC-FIN-05

Latest generic target:

```text
HOLD 4.5 : 5.5
SELL_LEAN
```

when:

```text
complete high debt

thin cash

stable profitable operations

refinancing/maturity severity unresolved

market expectation is conditional on that same unresolved refinancing downside

no distinct established current pricing/expectation risk exists
```

Do NOT use historical GPT-5.6 Sol SELL 6.0 output as ground truth.

The semantic contract changed after that historical output.

---

# 13. Historical Sol comparison policy

Old GPT-5.6 Sol outputs may be compared descriptively.

But where the contract changed after those outputs,
label:

```text
SAME_MODEL_DIFFERENT_CONTRACT_HISTORICAL
```

Do NOT say:

```text
Sol regressed
```

or:

```text
Sol improved
```

based on old-vs-new labels alone.

M12W is the first authoritative Sol proof under the latest full semantic contract.

---

# 14. Astra comparison policy

Any Astra comparison must be labeled:

```text
CROSS_MODEL_DESCRIPTIVE_ONLY
```

Do not use Astra outputs to set M12W target labels.

Do not compute a combined stability rate.

Astra timeout artifacts remain runtime evidence only.

---

# 15. Sol restoration implementation boundary

Allowed changes:

```text
proof-critical model target

model-target configuration

runtime contract timeout from Astra experimental 2400 back to Sol 1800

Astra-specific proof gating / status labels

model provenance reporting

runtime instrumentation compatibility plumbing
```

Forbidden semantic changes:

```text
financial-context selection

first-class financial evidence

financial validators

market-expectation independence

leverage contract

Directional ordinal calibration

source sufficiency

Daily Delta

warning lifecycle

Price-Timing

renderer ownership

fictional source facts
```

---

# 16. No semantic repair in M12W

M12W is:

```text
MODEL/RUNTIME RESTORATION
+
FULL PROOF
```

not another calibration design phase.

Required:

```text
financial_semantic_change_count = 0

directional_semantic_change_count = 0

fictional_case_change_count = 0

source_mapping_change_count = 0
```

If deterministic regression reveals an actual semantic defect:

```text
STOP
NO_MODEL_CALLS
```

and create a bounded semantic follow-up.

Do not silently repair semantics inside the restoration task.

---

# 17. Runtime compatibility preflight

Before model calls verify:

```text
gpt-5.6-sol / xhigh accepted by the actual runner

1800-second watchdog active

wrapper retry = 0

4 subjects/context

unique invocation ID

unique runtime namespace

unique working directory

unique session identity

process-group cleanup working

network readiness PASS

M12V observability instrumentation does not alter Sol execution
```

Do not run a one-context model probe.

If deterministic preflight passes,
proceed directly to the full new generation.

---

# 18. Model-target fail-closed behavior

If the runner reports:

```text
model != gpt-5.6-sol
```

or:

```text
reasoning effort != xhigh
```

then:

```text
STOP
SOL_RUNNER_MODEL_TARGET_MISMATCH
```

No automatic fallback.

No Astra fallback.

No lower reasoning effort.

---

# 19. Deterministic semantic regression gate

Before model calls require PASS for:

```text
first-class typed financial evidence

working-capital materiality grounding

QTD/YTD Korean period semantics

financial-sector explicit exclusion
including "적용 대상이 아니다"

FCF label safety

prior-year-end vs YoY safety

debt completeness safety

normalized earnings safety

financial-sector routing

market-expectation economic-independence fixtures

FIC-FIN-05 leverage target contract

M12E ordinal boundary fixtures

source sufficiency no-change

Daily Delta no-change

Price-Timing ownership

renderer ownership
```

---

# 20. Required local validation

Before model calls:

```text
focused pytest PASS

full repository local pytest PASS

ruff PASS

git diff --check PASS
```

Hosted CI historical portability failures remain a separate P1.

M12W must introduce:

```text
new hosted-CI failure count = 0
```

Do not broaden this task into hosted-CI cleanup.

---

# 21. Production side-effect gate

Before model calls verify:

```text
provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Approved monitoring schedules remain paused.

---

# 22. New full Sol generation

After all deterministic gates pass:

Create a NEW generation ID.

Do not reuse:

```text
old Sol M12D outputs

Astra M12E/M12F/M12T outputs

failed Astra M12U/M12V generations
```

Use exact frozen 8 fictional cases:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

No case-value changes.

No source edits to make Sol fit the targets.

---

# 23. Full fictional topology

Run:

```text
8 subjects

2 shared contexts

4 subjects/context

3 repetitions
```

Total:

```text
6 model calls

24 subject outputs
```

Model:

```text
gpt-5.6-sol
```

Reasoning:

```text
xhigh
```

No real issuer calls.

No judge calls.

---

# 24. Whole-generation stop rule

On any:

```text
transport timeout

transport failure

schema failure

hard semantic failure

target-bucket contract failure
```

stop the entire generation immediately.

Required:

```text
preserve emitted outputs

do not continue later contexts

do not retry only the failed context

do not stitch generations

wrapper retry = 0
```

Any repaired rerun must use a new full generation.

---

# 25. Sol runtime acceptance

Require:

```text
6 / 6 contexts return successfully

timeout count = 0

capacity failure count = 0

orphan process count = 0

wrapper retry count = 0
```

CLI internal retry events, if any, must be reported separately.

Do not equate:

```text
wrapper retry = 0
```

with:

```text
no internal transport recovery event
```

---

# 26. Financial hard-semantic gates

Across all 24 outputs require zero:

```text
invalid financial evidence refs

material financial grounding failures

working-capital grounding failures

narrative substitution failures

price/technical/supply Directional refs

partial PPE called FCF

prior-year-end called YoY

partial debt called total debt

total liabilities called debt

normalized/adjusted earnings invention

true financial-sector industrial-framework misuse

explicit-exclusion false reject

missing optional evidence treated as bearish

fixed financial score behavior

QTD/YTD false reject

QTD/YTD false accept

AI imperative primary action
```

---

# 27. FIC-FIN-01 target

All repetitions must be audited against:

```text
BUY 6.0:4.0
```

under the latest minimum-vs-stronger Directional contract.

Required:

```text
directional balance unique count = 1

target contract violations = 0
```

If Sol repeatedly chooses 6.5:

```text
do not automatically change target

audit whether the latest supplied evidence actually establishes
the stronger-support condition.
```

A stable but contract-inconsistent output fails.

---

# 28. FIC-FIN-02 target

All repetitions:

```text
HOLD 4.5:5.5
SELL_LEAN
```

under the latest shared-lineage + limiting-Unknown contract.

Required:

```text
direction unique count = 1

balance unique count = 1

target contract violations = 0
```

Do not use historical old-Sol SELL 6.0 as target.

---

# 29. FIC-FIN-04 target

All repetitions:

```text
HOLD 5.0:5.0
NEUTRAL
```

when non-operating improvement is the only positive offset to flat operations.

Required:

```text
balance unique count = 1

target contract violations = 0
```

---

# 30. FIC-FIN-05 target

All repetitions use the latest M12U contract:

```text
HOLD 4.5:5.5
SELL_LEAN
```

because the market-expectation downside in this fixture is conditional
on the same unresolved refinancing premise.

Required:

```text
balance unique count = 1

target contract violations = 0
```

Do not force historical old-Sol:

```text
SELL 4.0:6.0
```

The contract is newer than that historical output.

---

# 31. FIC-FIN-03 period conflict

Require:

```text
QTD and YTD evidence remain distinct

quarter-period result and cumulative result are explicitly separated

QTD/YTD false reject = 0

QTD/YTD false accept = 0
```

No exact wording requirement.

---

# 32. FIC-FIN-06 working-capital focal case

Require:

```text
inventory / receivables material claims
grounded to relevant typed financial refs

old narrative-substitution behavior remains invalid
```

No selection-triggered overreach.

---

# 33. FIC-FIN-07 missing optional context

Require:

```text
missing optional financial context
does not mechanically create bearish evidence
```

No fake typed rows.

---

# 34. FIC-FIN-08 financial sector

Require:

```text
financial-sector appropriate reasoning

no industrial net-debt/WC application

explicit non-applicability language
such as "적용 대상이 아니다"
does not trigger false reject
```

Required:

```text
explicit exclusion false reject = 0

true industrial-framework misuse = 0
```

---

# 35. Core balance stability

If the full generation completes, compute for all 8 subjects:

```text
overall direction unique count

directional balance unique count

HOLD lean unique count
```

Hard primary targets:

```text
FIC-FIN-01 balance unique count = 1

FIC-FIN-02 balance unique count = 1

FIC-FIN-04 balance unique count = 1

FIC-FIN-05 balance unique count = 1
```

Also report all other subjects.

---

# 36. Formal stability

Run the unchanged formal stability classifier.

Report:

```text
STABLE

BOUNDARY_UNCERTAINTY

UNSTABLE
```

No majority vote.

No averaging.

No classifier weakening.

Required:

```text
opposite-direction reversal count = 0
```

---

# 37. Business-delta / new-buyer / holder stance

Measure separately:

```text
business_thesis_change

fundamental_new_buyer

fundamental_holder
```

Do not modify these contracts in M12W.

If core balance/direction is stable but stance/delta fields remain variable,
freeze the smallest follow-up.

Possible next scopes:

```text
BOUNDED_BUSINESS_DELTA_CONTRACT_REPAIR_SOL

BOUNDED_NEW_BUYER_STANCE_CALIBRATION_REPAIR_SOL

BOUNDED_HOLDER_STANCE_CALIBRATION_REPAIR_SOL
```

Do not combine independent issues.

---

# 38. Fresh-real readiness threshold

M12W may set:

```text
fresh_real_proof_readiness = READY
```

only if:

```text
6 / 6 Sol contexts complete

24 / 24 rows schema PASS

hard semantics = PASS

target core buckets = contract-consistent

formal UNSTABLE count = 0

formal BOUNDARY_UNCERTAINTY count = 0

business-delta variance requiring repair = 0

new-buyer stance variance requiring repair = 0

holder stance variance requiring repair = 0

runtime judged suitable for unseen real cohort integrity

real issuer exposure = 0
```

If any formal stance/delta ambiguity remains:

```text
fresh_real_proof_readiness = NOT_READY
```

and select the smallest bounded follow-up.

---

# 39. Real holdout suitability

After the fictional proof, explicitly assess the restored Sol runtime for real holdout use.

Remember:

```text
once real model output exists,
the precommitted cohort is exposed.

a failed partial real cohort cannot be selectively resumed
and still count as unseen proof.
```

Required real-proof runtime principles:

```text
wrapper retry = 0

no selective failed-name rerun

no cohort stitching

new cohort required after exposed partial failure
```

Only mark Sol runtime suitable if the fictional proof supports it
and historical Sol evidence is consistent.

---

# 40. If Sol runtime itself fails

If a Sol context times out or transport-fails:

```text
STOP

do not switch back to Astra

do not increase timeout inside the generation

do not retry the failed context
```

Set:

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
SOL_RUNTIME_REGRESSION_REVIEW
```

This would be a materially different result
because Sol previously had strong project-specific completion evidence.

---

# 41. Cross-model descriptive report

Produce a compact table for:

```text
historical GPT-5.6 Sol

GPT-6 Astra attempts

new GPT-5.6 Sol M12W
```

Separate:

```text
runtime reliability

semantic contract version

core balance output

formal stability
```

Labels:

```text
CROSS_MODEL_DESCRIPTIVE_ONLY

SAME_MODEL_DIFFERENT_CONTRACT_HISTORICAL

CURRENT_AUTHORITATIVE_PROOF
```

Do not calculate a simplistic "which model is smarter" score.

---

# 42. Message specificity advisory

If the full canary completes, run the existing advisory:

```text
typed financial anchor specificity

period specificity

case-specific checkpoints

generic substantive repetition

renderer-introduced repetition
```

Do not change renderer/message copy in M12W solely for advisory results.

---

# 43. No real model calls

Required:

```text
model_calls_real = 0

real_issuer_model_exposure_count = 0
```

M12W is fictional proof only.

---

# 44. Production firewall

Required:

```text
provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Observe approved paused schedules at start/end.

Do not resume them.

---

# 45. Hosted CI portability

Preserve the separate known backlog:

```text
HOSTED_CI_HISTORICAL_ARTIFACT_PORTABILITY_REPAIR
```

M12V reported:

```text
hosted CI failure count = 5

new hosted-CI failure count = 0
```

M12W must introduce:

```text
new hosted-CI failure count = 0
```

Do not make the five historical portability failures a blocker
for the model restoration/full fictional proof
unless M12W itself changes their behavior.

Do not claim hosted CI PASS if it remains red.

---

# 46. Required artifacts — provenance / latest integrity

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12w-scope-freeze

04-model-rollback-decision

05-astra-proof-path-suspension

06-sol-model-target-contract

07-authoring-model-provenance

08-sol-runner-model-provenance
```

---

# 47. Required runtime restoration artifacts

Produce:

```text
09-m12v-failed-runtime-contract-reuse-proof

10-astra-2400-timeout-failure-summary

11-sol-historical-runtime-evidence

12-sol-restored-runtime-contract

13-runtime-timeout-before-after

14-runtime-model-before-after

15-runtime-instrumentation-compatibility

16-process-cleanup-proof

17-runtime-isolation-proof

18-no-fallback-proof
```

---

# 48. Required semantic freeze artifacts

Produce:

```text
19-m12u-semantic-baseline-freeze

20-first-class-financial-evidence-freeze

21-working-capital-validator-freeze

22-qtd-ytd-validator-freeze

23-financial-sector-exclusion-freeze

24-market-expectation-independence-freeze

25-leverage-target-freeze

26-directional-ordinal-contract-freeze

27-source-sufficiency-no-change

28-daily-delta-no-change

29-price-timing-no-change

30-renderer-ownership-no-change
```

---

# 49. Required deterministic validation artifacts

Produce:

```text
31-focused-test-results

32-full-local-test-results

33-ruff-and-diff-results

34-hosted-ci-portability-observation

35-model-call-gate
```

---

# 50. Required full fictional artifacts

If deterministic gate passes:

```text
36-fictional-canary-generation-manifest

37-fictional-canary-source-lock

38-run-1-context-01

39-run-1-context-02

40-run-2-context-01

41-run-2-context-02

42-run-3-context-01

43-run-3-context-02

44-full-fictional-semantic-audit

45-full-fictional-target-bucket-audit

46-full-fictional-exclusion-audit

47-full-fictional-market-expectation-leverage-audit

48-full-fictional-grounding-audit

49-full-fictional-business-delta-audit

50-full-fictional-formal-stability

51-full-fictional-core-only-stability

52-full-fictional-stance-variance

53-full-fictional-runtime-audit

54-cross-model-descriptive-table

55-full-fictional-message-specificity-advisory

56-runtime-observations
```

For every attempted context preserve:

```text
prompt

schema

raw output

receipt

transport log

run document
```

---

# 51. Required completion artifacts

Produce:

```text
57-sol-restoration-success-decision

58-sol-runtime-real-holdout-suitability

59-core-balance-stability-decision

60-business-delta-followup-decision

61-new-buyer-stance-followup-decision

62-holder-stance-followup-decision

63-fresh-real-proof-readiness-decision

64-astra-future-experiment-handoff

65-hosted-ci-portability-handoff

66-production-no-change

67-schedule-pause-observation

68-master-workflow-update

69-program-completion
```

---

# 52. Deterministic acceptance criteria

Before model calls require:

```text
latest M12V result integrity PASS

Astra proof-critical suspension recorded

runner target = gpt-5.6-sol / xhigh

timeout restored = 1800

subjects/context = 4

wrapper retry = 0

no fallback

M12U semantic baseline preserved

financial exclusion regression PASS

market-expectation independence fixtures PASS

FIC-FIN-05 latest target contract PASS

M12E ordinal calibration fixtures PASS

first-class financial evidence regressions PASS

WC grounding regressions PASS

QTD/YTD regressions PASS

source sufficiency unchanged

Daily Delta unchanged

Price-Timing unchanged

renderer unchanged

focused/full local tests PASS

ruff PASS

git diff --check PASS

production side-effect firewall PASS
```

---

# 53. Full fictional acceptance criteria

Require:

```text
6 / 6 model contexts complete

24 / 24 subject outputs schema PASS

model = gpt-5.6-sol

effort = xhigh

timeout count = 0

capacity failure = 0

orphan count = 0

wrapper retry = 0

hard financial semantic violations = 0

explicit exclusion false rejects = 0

financial-sector true misuse = 0

market-expectation/leverage target violations = 0

grounding failures = 0

QTD/YTD false reject = 0

QTD/YTD false accept = 0

price/technical/supply violations = 0

AI imperative primary action = 0

FIC-FIN-01 balance unique count = 1

FIC-FIN-02 balance unique count = 1

FIC-FIN-04 balance unique count = 1

FIC-FIN-05 balance unique count = 1

all targeted balances contract-consistent

opposite-direction reversal = 0
```

Formal stability must be measured.

---

# 54. Next-scope logic

## A. Full Sol proof is completely stable

If:

```text
formal STABLE = 8
BOUNDARY_UNCERTAINTY = 0
UNSTABLE = 0

hard semantics PASS

runtime PASS
```

then:

```text
fresh_real_proof_readiness = READY

next_scope =
FRESH_REAL_FINANCIAL_CONTEXT_GENERALIZATION_PROOF_GPT56_SOL_XHIGH
```

Do not start the real proof inside M12W.

## B. Core balance stable but new-buyer stance remains variable

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
BOUNDED_NEW_BUYER_STANCE_CALIBRATION_REPAIR_GPT56_SOL
```

## C. Core balance stable but holder stance remains variable

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
BOUNDED_HOLDER_STANCE_CALIBRATION_REPAIR_GPT56_SOL
```

## D. Business delta varies materially

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
BOUNDED_BUSINESS_DELTA_CONTRACT_REPAIR_GPT56_SOL
```

## E. Core bucket target fails

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
BOUNDED_SOL_DIRECTIONAL_CONTRACT_REPAIR
```

Do not change threshold automatically.

## F. Sol runtime fails

```text
fresh_real_proof_readiness = NOT_READY

next_scope =
SOL_RUNTIME_REGRESSION_REVIEW
```

Do not return to Astra automatically.

---

# 55. Production readiness

Even if M12W passes:

```text
production_readiness = NOT_READY
```

Still required:

```text
fresh unseen real GPT-5.6 Sol generalization proof

production integration review

explicit user authorization
```

Existing production monitoring stays paused.

---

# 56. Program-completion fields

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
report_commit
final_head_sha
branch

latest_result_zip_sha256
latest_result_integrity

m12v_status
m12w_status

astra_proof_critical_status
astra_model_calls
astra_future_experiment_scope

implementation_model_target
implementation_reasoning_effort

investment_judgment_model_target
investment_judgment_reasoning_effort

authoring_model_target_match
runner_model_target_match
model_target_fallback_count

previous_astra_timeout_seconds
selected_sol_timeout_seconds
timeout_change_count

subjects_per_context
context_topology_change_count
wrapper_retry_count

runtime_instrumentation_retained
runtime_instrumentation_semantic_change_count

historical_sol_runtime_evidence_status
historical_sol_completed_context_count
historical_sol_timeout_count

financial_semantic_change_count
directional_semantic_change_count
fictional_case_change_count

financial_context_selection_change_count
first_class_projection_change_count

working_capital_validator_semantic_change_count
qtd_ytd_validator_semantic_change_count
financial_exclusion_validator_semantic_change_count
market_expectation_contract_change_count

directional_threshold_changed
directional_increment_changed
hold_lean_contract_changed
calibration_tiebreak_direction_changed

source_sufficiency_semantic_change_count
daily_delta_semantic_change_count
warning_semantic_change_count

fictional_generation_id
fictional_subject_count
fictional_context_count
fictional_repetition_count

model_calls_real
model_calls_fictional
model_calls_judge

model_context_success_count
model_context_failure_count
cli_internal_retry_event_count
wrapper_retry_count
timeout_count
capacity_failure_count
orphan_process_count

fictional_output_row_count
fictional_schema_pass_count

hard_financial_semantic_violation_count
invalid_financial_reference_count
grounding_failure_count
explicit_exclusion_false_reject_count
financial_sector_true_misuse_count
market_expectation_leverage_target_violation_count

fic_fin_01_directional_balance_unique_count
fic_fin_02_directional_balance_unique_count
fic_fin_04_directional_balance_unique_count
fic_fin_05_directional_balance_unique_count

target_bucket_contract_violation_count

formal_stable_count
formal_boundary_uncertainty_count
formal_unstable_count
opposite_direction_reversal_count

business_delta_variance_subject_count
new_buyer_stance_variance_subject_count
holder_stance_variance_subject_count

runtime_median_elapsed_seconds
runtime_max_elapsed_seconds
sol_runtime_real_holdout_suitability

message_specificity_advisory_status

hosted_ci_status
hosted_ci_failure_count
new_hosted_ci_failure_count
hosted_ci_portability_backlog_count

real_issuer_model_exposure_count

provider_source_fetches
production_db_mutations
monitoring_registrations
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count

fresh_real_proof_readiness
production_readiness
status
stop_reason
next_scope
```

Anything not actually measured must remain:

```text
NOT_MEASURED
```

---

# 57. Artifact integrity

Freeze all reports/model artifacts/master workflow before final index.

Required:

```text
hash mismatch = 0

size mismatch = 0

secret scan failure = 0
```

Report final ZIP SHA-256.

---

# 58. Final task principle

The Astra decision is complete enough for now.

M12V tried a bounded runtime adaptation:

```text
1800 → 2400 seconds
```

and the very first Astra/xhigh context still produced:

```text
0 final output bytes
+
2400-second timeout
```

while the backend sampling state remained unobservable.

Continuing to tune Astra would turn Thesis Monitor development
into an Astra transport-debugging project.

The project goal is different.

Therefore:

```text
suspend Astra from the proof-critical path

→ preserve all completed financial semantic improvements

→ restore GPT-5.6 Sol / xhigh

→ restore the proven 1800-second / 4-subject / no-wrapper-retry runtime

→ run the entire latest 8 × 3 fictional financial proof from scratch

→ measure hard semantics + target buckets + formal stability

→ only then move to a fresh unseen real cohort
```

Do NOT:

```text
debug Astra further in M12W

mix Astra and Sol outputs in one formal sample

force old Sol labels when the semantic contract has changed

increase Sol timeout preemptively

add retries

average repeated outputs

majority-vote BUY/HOLD/SELL

resume production monitoring
```

Finish Thesis Monitor on the stable proof path first.
Astra can be revisited later as a separate compatibility experiment.
