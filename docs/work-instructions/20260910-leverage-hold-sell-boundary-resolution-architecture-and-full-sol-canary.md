# Thesis Monitor — Leverage HOLD/SELL Boundary Resolution Architecture + Full Sol Canary

## 0. Task identity

Suggested work-instruction filename:

```text
20260910-leverage-hold-sell-boundary-resolution-architecture-and-full-sol-canary.md
```

Suggested result bundle:

```text
thesis-monitor-20260910-leverage-hold-sell-boundary-resolution-architecture-full-sol-canary-report.zip
```

Master-workflow phase:

```text
M12AB — GPT-5.6 Sol / xhigh
        A. M12AA Boundary-Band Audit Correction
        B. HOLD/SELL Boundary Resolution Architecture Review
        C. Bounded Implementation if One Safe Architecture Is Selected
        D. Full Fictional Financial Canary
```

M12AA finally completed the full intended fictional sample:

```text
6 / 6 model contexts complete
24 / 24 outputs schema PASS
runtime hard failure = 0
objective semantic hard failure = 0
```

This is the first complete sample after the canary stop-policy repair.

The key finding is now empirical rather than hypothetical:

```text
FIC-FIN-05 repetition 1:
SELL 4.0:6.0

FIC-FIN-05 repetition 2:
HOLD 4.5:5.5 SELL_LEAN

FIC-FIN-05 repetition 3:
SELL 4.0:6.0
```

All three outputs preserved the hard economic semantics.

Therefore FIC-FIN-05 is a genuine:

```text
HOLD / SELL adjacent-boundary case
```

under the current single-call model behavior.

M12AB must NOT majority-vote the three outputs.
M12AB must NOT force 6.0 merely because two of three runs selected it.
M12AB must NOT force 5.5 merely because the conservative tie-break exists.

Instead, M12AB must decide how the production architecture should represent and resolve
a case where the model considers two adjacent ordinal buckets reasonably defensible.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260910-boundary-band-canary-policy-financial-framework-application-scope-full-sol-canary-report.zip
```

Verified SHA-256:

```text
0416d8719b5e71e79de6591f574a2ba3332657dd32deddb870bbaa8d2e854a7a
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
indexed payload count = 272

missing = 0
extra = 0
hash mismatch = 0
size mismatch = 0
```

Recompute before trusting the bundle.

---

# 2. M12AA repository provenance

Reported:

```text
branch =
codex/20260910-boundary-band-canary-m12aa

base_sha =
e6e4cea878d4af93f44f299ff58791b651cc4afe

work_instruction_commit =
6f783fe63a6dc727ffc19fa8d53e7cfcf84b8998

implementation_commit =
d29b96a21ff2146bb1a446a2a60ce8e076f5fce9

report_commit =
20182366f704d0ad178d22e8b5816462679c138a
```

The exported completion file used:

```text
final_head_sha = RESOLVED_FROM_GIT_AT_BUNDLE_EXPORT
```

At M12AB start record actual repository HEAD directly.

If unexplained drift exists in:

```text
canary stop policy
FIC-FIN-05 band contract
Directional ordinal contract
business-delta validator
financial-framework application-role validator
Sol runtime
```

STOP:

```text
UNEXPLAINED_M12AB_BASELINE_DRIFT
```

---

# 3. Model/runtime remains frozen

Proof-critical model:

```text
gpt-5.6-sol
```

Reasoning:

```text
xhigh
```

Runtime:

```text
MODEL_CONTEXT_COUPLED

4 subjects/context

1800-second finite watchdog

single authoritative watchdog

wrapper retry = 0

batch split = 0
```

No Astra calls.

No model fallback.

No runtime changes.

If runner is not:

```text
gpt-5.6-sol / xhigh
```

STOP:

```text
SOL_RUNNER_MODEL_TARGET_MISMATCH
```

---

# 4. M12AA runtime result — frozen successful

M12AA completed all 6 contexts.

Measured:

```text
model_context_success_count = 6

model_context_failure_count = 0

timeout_count = 0

capacity_failure_count = 0

orphan_process_count = 0

wrapper_retry_count = 0

CLI internal retry count = 0

runtime median ≈ 455.06 sec

runtime max ≈ 540.92 sec
```

Sol runtime real-holdout suitability was reported:

```text
READY
```

M12AB is not a runtime phase.

---

# 5. M12AA hard semantic result — frozen successful

M12AA full sample:

```text
objective semantic hard failure count = 0

schema hard failure count = 0

runtime hard failure count = 0

invalid financial ref count = 0

grounding failure count = 0

financial-sector true misuse count = 0

financial-framework exclusion false reject count = 0

financial-framework false accept count = 0

business-delta alias-resolution failure count = 0

business-delta false reject count = 0

business-delta false accept count = 0

unsupported absolute-state-to-delta count = 0
```

This means the project has now demonstrated:

```text
full financial evidence grounding
QTD/YTD semantics
WC semantics
debt completeness
financial-sector framework routing
business-delta evidence identity
```

across all 24 fictional outputs.

Do not reopen these solved semantics in M12AB.

---

# 6. M12AA stable core subjects

The following repeated core outputs were fully stable:

```text
FIC-FIN-01:
BUY 6.5:3.5
× 3

FIC-FIN-02:
HOLD 4.5:5.5 SELL_LEAN
× 3

FIC-FIN-03:
HOLD 5.5:4.5 BUY_LEAN
× 3

FIC-FIN-04:
HOLD 5.0:5.0 NEUTRAL
× 3

FIC-FIN-06:
HOLD 5.5:4.5 BUY_LEAN
× 3

FIC-FIN-07:
HOLD 5.0:5.0 NEUTRAL
× 3

FIC-FIN-08:
HOLD 5.0:5.0 NEUTRAL
× 3
```

Therefore:

```text
7 of 8 subjects have stable core direction/balance/lean.
```

FIC-FIN-05 is the only core-direction instability.

---

# 7. Formal M12AA stability result

Frozen formal result:

```text
STABLE = 6

BOUNDARY_UNCERTAINTY = 1

UNSTABLE = 1

opposite-direction reversal = 0
```

Per subject:

```text
FIC-FIN-01 = STABLE

FIC-FIN-02 = STABLE

FIC-FIN-03 = STABLE

FIC-FIN-04 = STABLE

FIC-FIN-05 = UNSTABLE

FIC-FIN-06 = STABLE

FIC-FIN-07 = STABLE

FIC-FIN-08 = BOUNDARY_UNCERTAINTY
```

FIC-FIN-08's core direction/balance itself is stable.
Its formal boundary classification is caused by holder-stance variance,
not core Directional variance.

---

# 8. FIC-FIN-05 complete repeated result

Observed:

```text
run 1:
SELL 4.0:6.0
confidence MEDIUM
new buyer AVOID
holder REVIEW
business delta UNCHANGED

run 2:
HOLD 4.5:5.5 SELL_LEAN
confidence MEDIUM
new buyer WAIT
holder REVIEW
business delta UNCHANGED

run 3:
SELL 4.0:6.0
confidence MEDIUM
new buyer AVOID
holder REDUCE
business delta UNCHANGED
```

Hard economics remained consistent:

```text
large complete interest-bearing debt = negative

thin cash buffer = negative

stable positive operating profit = counterevidence

refinancing terms / maturity concentration = Unknown severity limit

market-expectation downside remains conditional
on the same unresolved refinancing premise

business delta = UNCHANGED
because no baseline deterioration is supplied
```

The instability is the exact placement at:

```text
negative 5.5
vs
negative 6.0
```

not a reversal of the underlying economics.

---

# 9. M12AA boundary-band audit contains a deterministic comparator bug

The frozen allowed band was:

```text
HOLD 4.5:5.5 SELL_LEAN

SELL 4.0:6.0
```

The audit represented the SELL entry as:

```json
{
  "direction": "SELL",
  "buy": 4.0,
  "sell": 6.0,
  "lean": null
}
```

But normalized model output represents non-HOLD lean as:

```text
NOT_HOLD
```

Therefore the exact comparator incorrectly classified:

```text
SELL 4.0:6.0 / NOT_HOLD
```

as:

```text
OUT_OF_BAND_OTHER
```

twice.

This is wrong.

The correct M12AA band reclassification is:

```text
IN_BAND_MINIMUM_SELL = 2

IN_BAND_HOLD_SELL_LEAN = 1

OUT_OF_BAND = 0

stable_preference =
MIXED_BOUNDARY
```

M12AB must repair this audit normalization before any architecture decision.

This is an audit/harness bug only.

It does not change the model outputs.

---

# 10. Required non-HOLD lean normalization

Freeze generic normalization:

```text
For overall_direction != HOLD:

lean values:
null
NOT_HOLD
not-applicable representation

must normalize to one canonical non-HOLD state
for boundary-band comparison.
```

Do not treat:

```text
null vs NOT_HOLD
```

as a semantic difference.

For HOLD rows:

```text
BUY_LEAN
NEUTRAL
SELL_LEAN
```

remain material.

Add positive/negative fixtures.

---

# 11. Corrected FIC-FIN-05 interpretation

After audit correction:

```text
all 3 FIC-FIN-05 outputs are inside
the predeclared semantic boundary band.
```

Therefore M12AA's reported:

```text
stable_preference = OUT_OF_BAND
```

must NOT be carried forward.

The truthful classification is:

```text
MIXED_BOUNDARY
```

because:

```text
2 runs = minimum SELL 6.0
1 run = HOLD SELL_LEAN 5.5
```

Do not majority-vote.

---

# 12. M12AA generated next_scope must be corrected

M12AA reported:

```text
next_scope =
BOUNDED_NEW_BUYER_STANCE_CALIBRATION_REPAIR_GPT56_SOL
```

That is not the correct priority.

Why:

```text
FIC-FIN-05 new-buyer variance:
AVOID / WAIT / AVOID

is coupled to the more fundamental core variance:
SELL / HOLD / SELL.
```

The project must resolve the core HOLD/SELL boundary architecture first.

Only after core output is stable should new-buyer/holder stance be independently repaired.

Correct next priority:

```text
LEVERAGE_HOLD_SELL_BOUNDARY_RESOLUTION_ARCHITECTURE
```

FIC-FIN-08 holder stance remains an independent later issue.

---

# 13. Primary M12AB question

The complete sample proves:

```text
the same supplied economics can produce
HOLD 5.5 or SELL 6.0
under a single model call.
```

A production system cannot solve this by:

```text
running the model 3 times and taking a majority.
```

Production normally needs one decision flow.

M12AB must answer:

```text
How should Thesis Monitor represent and resolve
a genuinely adjacent Directional boundary
in a single model invocation
without pretending one side is uniquely certain?
```

---

# 14. Architecture options to evaluate

Evaluate at least the following.

## Option A — Keep raw single-point LLM balance

```text
model emits exactly one balance
and production accepts 5.5 or 6.0 variability.
```

Problem:

```text
same evidence can change user meaning from HOLD to SELL.
```

Likely not acceptable for proof-critical production,
but evaluate explicitly.

## Option B — Force one side with more prompt wording

```text
add another instruction telling the model
when to use 5.5 vs 6.0.
```

Historical evidence:

```text
multiple bounded prompt/calibration clarifications
have already produced both outputs.
```

Do not prefer this unless a genuinely missing semantic rule is discovered.

## Option C — Majority vote / repeated model calls

Forbidden for proof-critical output.

Reasons:

```text
cost/latency
holdout exposure
non-deterministic production semantics
majority is not evidence
```

Reject.

## Option D — Average balances

Forbidden.

Reject.

## Option E — Treat any predeclared boundary band as formal stability PASS

Insufficient for production because:

```text
HOLD and SELL remain different primary directions.
```

The band is useful for measurement,
not enough to resolve the user-facing decision.

## Option F — Explicit adjacent-boundary metadata + deterministic conservative resolver

Candidate architecture:

```text
AI identifies that two adjacent buckets are reasonably supportable.

It supplies:
- preferred raw bucket
- adjacent bucket toward 5.0
- boundary-active status
- evidence refs / limitation refs explaining the boundary.

A deterministic resolver applies the already-frozen conservative tie-break
and chooses the less-directional bucket toward 5.0
for the final operational Directional balance.
```

This would represent:

```text
raw model preference
and
resolved production direction
```

separately.

Evaluate carefully.

## Option G — Deterministic semantic ordinal mapper

Candidate architecture:

```text
AI does not own the final numeric balance directly.

AI classifies:
- directional polarity
- anchor sufficiency
- corroboration sufficiency
- adjacent ambiguity
- stronger-direction support

A deterministic mapper converts
that semantic state to 5.0 / 5.5 / 6.0 / 6.5...
```

Potential advantage:

```text
numeric encoding becomes deterministic.
```

Potential risk:

```text
turning qualitative investment judgment
into a hidden scorecard / brittle rules engine.
```

Evaluate, do not assume.

## Option H — New user-facing boundary direction enum

Example:

```text
HOLD_SELL_BOUNDARY
```

Potential advantage:

```text
honestly represents uncertainty.
```

Potential cost:

```text
changes the frozen BUY/HOLD/SELL surface
and downstream lifecycle/renderer contracts.
```

Evaluate but do not select casually.

---

# 15. Architecture evaluation criteria

For each option report:

```text
single-call stability

faithfulness to AI economic judgment

deterministic numeric semantics

compatibility with existing conservative tie-break

new-buyer/holder consistency

renderer impact

output-schema impact

monitoring lifecycle impact

Daily Delta impact

valuation/price ownership impact

holdout-proof integrity

complexity

risk of hidden scorecard behavior
```

Use qualitative labels.

Do not weighted-score options.

---

# 16. Preferred architecture principle

Prefer an architecture that preserves:

```text
AI owns the economic interpretation.

Deterministic code owns only
the mechanical resolution of explicitly declared adjacent ambiguity.
```

Do not move:

```text
business-quality judgment
financial interpretation
market expectations
valuation judgment
```

into deterministic scoring code.

This makes:

```text
Option F
```

the preferred candidate to evaluate first,
but M12AB must still prove it is feasible and generic.

---

# 17. Boundary declaration must be generic

If explicit boundary metadata is selected,
it must not be:

```text
if ticker == FIC-FIN-05
```

or:

```text
if debt is 900 and cash is 40
```

Generic boundary meaning:

```text
The model judges that two adjacent ordinal buckets
are both reasonably supportable after considering
material anchors, counterevidence, and material limitations.
```

Examples:

```text
5.5 ↔ 6.0

6.0 ↔ 6.5

5.0 ↔ 5.5
```

only when genuinely adjacent.

---

# 18. Proposed additive boundary contract

If Option F is selected,
evaluate an additive experimental output object equivalent to:

```json
{
  "adjacent_boundary": {
    "status": "NONE | ADJACENT_BUCKETS_REASONABLE",
    "other_balance": {
      "buy": 4.5,
      "sell": 5.5
    },
    "reason": "...",
    "evidence_refs": ["E..."]
  }
}
```

Exact field names may differ,
but semantics must include:

```text
whether an adjacent alternative is genuinely reasonable

which adjacent balance

why

which supplied evidence/Unknown supports that ambiguity
```

The existing model-selected balance remains observable as:

```text
raw model preference
```

for audit.

---

# 19. Deterministic boundary resolver

If Option F is selected,
the resolver must:

```text
1. verify boundary status is valid

2. verify the alternate balance is exactly one allowed 0.5 step adjacent

3. verify it is toward 5.0 when applying the conservative tie-break

4. verify evidence refs are valid

5. reject malformed/non-adjacent boundary declarations

6. select the less-directional balance toward 5.0
   as the resolved production balance

7. derive final overall_direction and HOLD lean mechanically
   from the resolved balance
```

Do not use majority vote.

Do not inspect ticker identity.

Do not calculate a financial score.

---

# 20. Raw vs resolved Directional state

If a deterministic boundary resolver is implemented,
preserve both:

```text
RAW_MODEL_DIRECTIONAL_STATE

RESOLVED_DIRECTIONAL_STATE
```

Raw is for:

```text
audit
model-quality analysis
boundary diagnostics
```

Resolved is for:

```text
production-comparable Directional result
renderer/downstream decision flow
```

Do not silently mutate without preserving provenance.

---

# 21. Ownership decision must be explicit

Current architecture says Directional Core owns:

```text
overall direction
balance
lean
business thesis change
new-buyer stance
holder stance
```

M12AB must explicitly decide whether:

```text
AI owns raw balance
while deterministic Core resolver owns final balance
```

is still consistent with this ownership principle.

If not,
stop and use an architecture-review handoff.

Do not smuggle a new ownership model into code.

---

# 22. New-buyer/holder coupling

The full M12AA sample shows:

```text
FIC-FIN-05:
core SELL → new buyer AVOID
core HOLD → new buyer WAIT

holder:
REVIEW / REVIEW / REDUCE
```

Therefore new-buyer variance is at least partly downstream of core direction.

M12AB must not repair new-buyer stance first.

If final/resolved core state is introduced,
audit whether:

```text
new-buyer/holder stances remain logically compatible
with the resolved core state.
```

Do not mechanically map:

```text
HOLD → WAIT
SELL → AVOID
```

unless the existing stance contract already requires it.

Stance repair remains separate unless inconsistency is created by the resolver.

---

# 23. FIC-FIN-08 holder variance

M12AA FIC-FIN-08:

```text
core:
HOLD 5.0:5.0 NEUTRAL
× 3

new buyer:
WAIT × 3

holder:
REVIEW / HOLDABLE / HOLDABLE
```

This is an independent holder-stance ambiguity.

Do not let it distract from the FIC-FIN-05 core boundary.

After core boundary architecture is resolved,
FIC-FIN-08 holder stance is likely the next independent stance review.

---

# 24. Directional confidence variance

M12AA confidence variance count = 1.

The varying subject is:

```text
FIC-FIN-06
MEDIUM / LOW / LOW
```

Its core direction/balance and stances remained stable.

Treat confidence variance as observational for now.

Do not make confidence a hard production direction mapper.

---

# 25. Corrected M12AA reclassification artifact

Before architecture implementation,
produce a corrected offline M12AA summary:

```text
FIC-FIN-05:

run 1 =
IN_BAND_MINIMUM_SELL

run 2 =
IN_BAND_HOLD_SELL_LEAN

run 3 =
IN_BAND_MINIMUM_SELL

out_of_band_count =
0

stable_preference =
MIXED_BOUNDARY
```

Do not rewrite the archived M12AA result bundle.

Create a new M12AB correction artifact referencing it.

---

# 26. Formal historical result remains frozen

M12AA formal classifier correctly said:

```text
FIC-FIN-05 = UNSTABLE
```

because actual raw primary direction changed:

```text
SELL ↔ HOLD
```

The band-audit comparator bug does NOT make the old formal result wrong.

Do not retroactively rewrite:

```text
UNSTABLE → STABLE
```

M12AB architecture is intended to determine
whether a single-call resolved Directional state can become stable.

---

# 27. Phase A model-free architecture gate

Before any output-schema/runtime model change:

Produce:

```text
boundary-band normalization correction

corrected M12AA FIC-FIN-05 audit

FIC-FIN-05 three-run semantic fingerprint comparison

new-buyer coupling audit

holder coupling audit

all architecture option reviews

preferred architecture decision

ownership compatibility decision

output-schema impact decision

renderer/downstream impact decision
```

Then choose exactly one:

```text
BOUNDARY_DECLARATION_PLUS_CONSERVATIVE_RESOLUTION

DETERMINISTIC_SEMANTIC_ORDINAL_MAPPER

NEW_BOUNDARY_DIRECTION_STATE

KEEP_RAW_SINGLE_POINT_AND_ACCEPT_INSTABILITY

NO_SAFE_ARCHITECTURE_SELECTED
```

Do not give a menu as final output.

---

# 28. Stop if no safe architecture is selected

If:

```text
NO_SAFE_ARCHITECTURE_SELECTED
```

then:

```text
STOP
NO MODEL CALLS
fresh_real_proof_readiness = NOT_READY
```

Set a precise next architecture scope.

Do not add another prompt-only exact-bucket tweak.

---

# 29. Bounded implementation authorization

If the architecture review selects:

```text
BOUNDARY_DECLARATION_PLUS_CONSERVATIVE_RESOLUTION
```

and proves:

```text
additive schema impact is manageable

ownership remains coherent

renderer can consume resolved core state without hidden contradiction

no scorecard is introduced
```

M12AB is authorized to implement it in the SAME task.

This avoids another preparation-only phase.

If implementation requires broad migration of production persistence,
stop and separate the migration.

Experimental fictional canary support may be implemented first
without production activation.

---

# 30. Schema compatibility requirement

Any new boundary metadata must be:

```text
additive
optional for legacy outputs
explicitly versioned
```

Legacy outputs without boundary metadata must remain parseable.

Required:

```text
legacy parse PASS

legacy roundtrip/hash behavior unchanged
where current compatibility tests require it
```

No unrelated schema redesign.

---

# 31. Boundary declaration hard validators

If implemented, reject:

```text
non-adjacent alternative balance

alternative on the wrong side of 5.0

invalid balance sum

non-0.5 increment

invalid evidence refs

boundary status active with no alternate balance

boundary active for exactly identical balance

boundary declaration that contradicts overall polarity
```

No ticker-specific rules.

---

# 32. Conservative resolver is mechanical only

If boundary is valid:

```text
resolved balance =
less directional of the two adjacent balances toward 5.0
```

Examples:

```text
negative 5.5 ↔ 6.0
→ resolve to 5.5

positive 6.0 ↔ 6.5
→ resolve to 6.0

neutral 5.0 ↔ positive 5.5
→ resolve to 5.0
```

This applies the already-frozen tie-break.

It does NOT decide whether a boundary exists.

That remains an AI economic-judgment question.

---

# 33. Boundary declaration canary expectations

If Option F is implemented,
for the frozen FIC-FIN-05 source pattern the model should ideally identify:

```text
ADJACENT_BUCKETS_REASONABLE
```

with:

```text
negative 5.5 ↔ negative 6.0
```

across all repetitions.

Resolved final state should then be:

```text
HOLD 4.5:5.5 SELL_LEAN
```

across all repetitions
through deterministic tie-break resolution.

Important:

```text
the raw preferred balance may remain 5.5 or 6.0.
```

The canary must report raw and resolved stability separately.

---

# 34. Do not force boundary declarations everywhere

Stable subjects should normally remain:

```text
boundary status = NONE
```

unless the supplied economics genuinely support an adjacent range.

Hard regression controls:

```text
FIC-FIN-01 strong-quality current evidence
must not be automatically boundary-active merely because valuation is missing.

FIC-FIN-04 flat operations/non-operating case
must not invent a BUY_LEAN boundary.

FIC-FIN-07 missing optional data
must not invent directional boundary evidence.
```

---

# 35. Model prompt change limit

If Option F requires a model-facing change,
it must be narrowly limited to:

```text
when two adjacent ordinal buckets are genuinely both supportable,
declare the adjacent boundary explicitly
rather than hiding the ambiguity in a single numeric point.
```

Do not add case-specific examples containing:

```text
FIC-FIN-05
900 debt
40 cash
```

Generic examples may use fictional unlabeled scenarios.

No threshold changes.

---

# 36. Existing financial semantics remain frozen

No change to:

```text
FIRST_CLASS_TYPED_EVIDENCE_UNIFICATION

WC grounding

QTD/YTD semantics

debt completeness

financial-sector application-role validator

business-delta alias resolution

business-delta absolute-state vs change rule

market-expectation economic independence

financial exclusion semantics
```

Required semantic change counters:

```text
financial_semantic_change_count = 0

business_delta_semantic_change_count = 0

financial_framework_semantic_change_count = 0
```

Boundary architecture is Directional-output semantics,
not financial-fact semantics.

---

# 37. Threshold/increment/tie-break freeze

Required unchanged:

```text
BUY threshold = 6.0

SELL threshold = 6.0

increment = 0.5

HOLD 5.5:4.5 = BUY_LEAN

HOLD 5.0:5.0 = NEUTRAL

HOLD 4.5:5.5 = SELL_LEAN

tie-break direction = toward 5.0
```

No scorecard.

No evidence-count rule.

No majority vote.

No averaging.

---

# 38. Deterministic tests before model calls

At minimum test:

## Band normalization

```text
SELL 4.0:6.0 / null
=
SELL 4.0:6.0 / NOT_HOLD
for non-HOLD comparison
```

## Boundary resolver

If selected:

```text
5.5 / 6.0 negative adjacent → 5.5

6.0 / 6.5 positive adjacent → 6.0

5.0 / 5.5 positive adjacent → 5.0

non-adjacent → reject

wrong-side alternate → reject
```

## Legacy compatibility

Boundary absent → old behavior.

## Hard semantic regressions

All M12AA financial/delta/application-role regressions PASS.

---

# 39. Phase B model-call gate

Before new model calls require:

```text
latest ZIP integrity PASS

band comparator repaired

corrected FIC-FIN-05 audit =
2 minimum SELL
1 HOLD SELL_LEAN
0 out-of-band
MIXED_BOUNDARY

preferred architecture frozen

ownership compatibility PASS

no hidden scorecard

schema compatibility PASS if changed

renderer/downstream compatibility PASS or explicitly gated experimental-only

all financial hard semantics unchanged

threshold/increment/tie-break unchanged

Sol runtime unchanged

focused pytest PASS

full local pytest PASS

ruff PASS

git diff --check PASS

production side-effect firewall PASS
```

If any hard gate fails:

```text
STOP
NO MODEL CALLS
```

---

# 40. New full Sol canary

If implementation proceeds,
create a NEW generation.

Do not reuse M12AA outputs in the formal new sample.

Use exact frozen 8 subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Topology:

```text
8 subjects

2 contexts

4 subjects/context

3 repetitions

6 model calls

24 outputs
```

Model:

```text
gpt-5.6-sol
```

Reasoning:

```text
xhigh
```

No real issuers.

No judge calls.

---

# 41. New canary stop policy remains M12AA policy

Stop immediately only for:

```text
runtime/schema hard failure

objective semantic hard failure

invalid boundary schema/reference if boundary architecture is implemented
```

Do NOT stop for:

```text
raw model balance calibration observation

raw model stance variance

raw confidence variance

message advisory
```

Collect the full sample.

---

# 42. Raw vs resolved stability report

If a resolver architecture is implemented,
report TWO independent views.

## Raw model stability

```text
raw overall direction
raw balance
raw lean
```

## Resolved production stability

```text
resolved overall direction
resolved balance
resolved lean
```

Do not hide raw instability.

The goal is not to pretend the model stopped being uncertain.

The goal is to make the operational policy deterministic
when the model explicitly identifies an adjacent boundary.

---

# 43. FIC-FIN-05 acceptance target under boundary architecture

Preferred success pattern:

```text
boundary declaration =
ADJACENT_BUCKETS_REASONABLE
× 3

boundary pair =
4.5:5.5 ↔ 4.0:6.0
× 3

resolved final =
HOLD 4.5:5.5 SELL_LEAN
× 3
```

Raw preferred balance may vary.

If boundary declaration itself varies:

```text
boundary-declaration stability FAIL
```

and the architecture is not yet proven.

Do not majority-vote declaration.

---

# 44. Stable-subject regression

Require no new resolved-core instability for:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Any new boundary activation must be audited for semantic legitimacy.

Do not make boundary metadata a generic uncertainty escape hatch.

---

# 45. Stance follow-up policy

After resolved core stability is measured:

```text
new-buyer/holder stance variance remains independently visible.
```

If FIC-FIN-05 core resolves stably but:

```text
new buyer still AVOID ↔ WAIT
```

then next scope may be:

```text
BOUNDED_NEW_BUYER_STANCE_CALIBRATION_REPAIR_GPT56_SOL
```

If FIC-FIN-08 holder still:

```text
REVIEW ↔ HOLDABLE
```

then next scope may be:

```text
BOUNDED_HOLDER_STANCE_CALIBRATION_REPAIR_GPT56_SOL
```

Order core first, stance second.

---

# 46. Formal classifier handling

Do NOT weaken the historical formal classifier.

For the new architecture,
produce:

```text
RAW_FORMAL_STABILITY

RESOLVED_FORMAL_STABILITY
```

If resolved architecture is production-authoritative,
a new formal classifier version may consume:

```text
resolved core fields
```

but this must be explicitly versioned.

Do not rewrite historical M12AA classification.

---

# 47. Real-proof readiness

Set:

```text
fresh_real_proof_readiness = READY
```

only if:

```text
full 6-call Sol generation completes

objective semantic hard failures = 0

financial/delta/application-role semantics PASS

boundary architecture itself is stable

resolved core direction/balance is stable

no unresolved material stance inconsistency remains

Sol runtime remains suitable for exposed real holdout

real issuer exposure = 0
```

If the boundary architecture is not implemented,
M12AB remains a review phase and readiness stays NOT_READY.

---

# 48. Production activation remains prohibited

Even if experimental boundary architecture passes:

```text
production_readiness = NOT_READY
```

Still required:

```text
fresh unseen real Sol generalization proof

production integration review

explicit user authorization
```

Monitoring schedules remain paused.

---

# 49. Required artifacts — provenance / correction

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12ab-scope-freeze

04-sol-runtime-freeze

05-m12aa-band-comparator-bug-reproduction

06-non-hold-lean-normalization-contract

07-corrected-m12aa-fic-fin-05-band-audit

08-m12aa-next-scope-correction
```

---

# 50. Required artifacts — boundary forensic

Produce:

```text
09-fic-fin-05-three-run-raw-comparison

10-fic-fin-05-economic-semantic-invariance-audit

11-fic-fin-05-core-boundary-root-cause

12-new-buyer-core-coupling-audit

13-holder-core-coupling-audit

14-fic-fin-08-independent-holder-variance-audit

15-confidence-variance-audit
```

---

# 51. Required architecture option artifacts

Produce:

```text
16-option-a-raw-single-point

17-option-b-prompt-force

18-option-c-majority-vote-rejection

19-option-d-average-rejection

20-option-e-band-as-pass-only

21-option-f-boundary-declaration-conservative-resolver

22-option-g-semantic-ordinal-mapper

23-option-h-new-boundary-direction-state

24-architecture-comparison

25-preferred-boundary-architecture-decision

26-directional-ownership-compatibility-decision

27-output-schema-impact-decision

28-renderer-downstream-impact-decision
```

---

# 52. Required implementation artifacts if selected

If Option F or another bounded architecture is selected and implemented:

```text
29-boundary-output-contract

30-boundary-schema-before-after

31-boundary-validator-contract

32-boundary-resolver-contract

33-raw-vs-resolved-state-contract

34-boundary-provenance-contract

35-legacy-compatibility-proof

36-stable-subject-no-boundary-regression

37-boundary-deterministic-fixtures
```

If no implementation occurs,
produce no-change/STOP artifacts instead.

---

# 53. Required semantic freeze artifacts

Produce:

```text
38-first-class-financial-evidence-freeze

39-working-capital-validator-freeze

40-qtd-ytd-validator-freeze

41-financial-framework-application-role-freeze

42-business-delta-alias-resolution-freeze

43-business-delta-semantics-freeze

44-market-expectation-independence-freeze

45-directional-threshold-increment-tiebreak-freeze

46-source-sufficiency-no-change

47-daily-delta-no-change

48-price-timing-no-change
```

Renderer changes only if required by selected boundary architecture
and must be separately reported.

---

# 54. Required deterministic validation artifacts

Produce:

```text
49-focused-test-results

50-full-local-test-results

51-ruff-and-diff-results

52-hosted-ci-portability-observation

53-model-call-gate
```

No new hosted-CI portability failures.

---

# 55. Required full-canary artifacts if implementation proceeds

Produce:

```text
54-fictional-canary-generation-manifest

55-fictional-canary-source-lock

56-run-1-context-01

57-run-1-context-02

58-run-2-context-01

59-run-2-context-02

60-run-3-context-01

61-run-3-context-02

62-full-fictional-hard-semantic-audit

63-full-fictional-boundary-declaration-audit

64-full-fictional-raw-core-stability

65-full-fictional-resolved-core-stability

66-full-fictional-fic-fin-05-boundary-audit

67-full-fictional-business-delta-audit

68-full-fictional-financial-framework-audit

69-full-fictional-grounding-audit

70-full-fictional-stance-variance

71-full-fictional-confidence-variance

72-full-fictional-runtime-audit

73-full-fictional-message-specificity-advisory
```

Preserve raw prompt/schema/output/receipt/log artifacts for every call.

---

# 56. Required completion artifacts

Produce:

```text
74-band-comparator-repair-success-decision

75-boundary-architecture-success-decision

76-directional-ownership-success-decision

77-fic-fin-05-boundary-resolution-success-decision

78-core-resolved-stability-decision

79-new-buyer-stance-followup-decision

80-holder-stance-followup-decision

81-sol-runtime-real-holdout-suitability

82-fresh-real-proof-readiness-decision

83-hosted-ci-portability-handoff

84-astra-future-experiment-handoff

85-production-no-change

86-schedule-pause-observation

87-master-workflow-update

88-program-completion
```

---

# 57. Deterministic acceptance criteria

Before model calls require:

```text
latest M12AA ZIP integrity PASS

M12AA band comparator bug reproduced

non-HOLD lean normalization fixed

corrected FIC-FIN-05:
2 minimum SELL
1 HOLD SELL_LEAN
0 out-of-band
MIXED_BOUNDARY

M12AA next-scope priority corrected to core boundary first

one preferred boundary architecture selected

no majority vote

no averaging

no fixed financial scorecard

ownership compatibility PASS

legacy compatibility PASS if schema changes

all solved financial/delta/application-role semantics unchanged

threshold/increment/tie-break unchanged

Sol runtime unchanged

focused/full local tests PASS

ruff PASS

git diff --check PASS

production side-effect firewall PASS
```

---

# 58. Full-canary acceptance criteria if implementation proceeds

Require:

```text
6 / 6 contexts complete

24 / 24 schema PASS

timeout = 0

capacity failure = 0

orphan = 0

wrapper retry = 0

objective semantic hard failure = 0

invalid financial refs = 0

grounding failures = 0

business-delta violations = 0

financial-sector true misuse = 0

legitimate exclusion false reject = 0

QTD/YTD false reject/accept = 0

price/technical/supply Directional violation = 0

AI imperative primary action = 0
```

If boundary declaration/resolver is selected:

```text
FIC-FIN-05 boundary declaration unique state = 1

FIC-FIN-05 declared adjacent pair unique = 1

FIC-FIN-05 resolved direction/balance unique count = 1

resolved target obeys frozen conservative tie-break

stable subjects do not acquire invalid boundary states
```

Raw model balance may remain variable and must be reported.

---

# 59. Failure handling

## A. Band comparator fix fails

```text
STOP
next_scope =
BOUNDARY_BAND_AUDIT_NORMALIZATION_REPAIR
```

## B. No safe boundary architecture is selected

```text
STOP
NO MODEL CALLS

fresh_real_proof_readiness =
NOT_READY

next_scope =
DIRECTIONAL_BOUNDARY_OUTPUT_ARCHITECTURE_REVIEW_V2
```

## C. Boundary metadata/resolver implemented but boundary declaration varies

```text
next_scope =
BOUNDARY_DECLARATION_STABILITY_REVIEW_GPT56_SOL
```

Do not majority-vote.

## D. Resolved core stable but new-buyer stance varies

```text
next_scope =
BOUNDED_NEW_BUYER_STANCE_CALIBRATION_REPAIR_GPT56_SOL
```

## E. Resolved core stable but holder stance varies

```text
next_scope =
BOUNDED_HOLDER_STANCE_CALIBRATION_REPAIR_GPT56_SOL
```

## F. Sol runtime fails

```text
next_scope =
SOL_RUNTIME_REGRESSION_REVIEW
```

No Astra fallback.

---

# 60. Program-completion fields

Include at least:

```text
base_sha
work_instruction_commit
implementation_commit
report_commit
final_head_sha
branch

latest_result_zip_sha256
latest_result_integrity

m12aa_status
m12ab_status

band_comparator_root_cause
non_hold_lean_normalization_status

corrected_fic_fin_05_in_band_minimum_sell_count
corrected_fic_fin_05_in_band_hold_sell_lean_count
corrected_fic_fin_05_out_of_band_count
corrected_fic_fin_05_stable_preference

m12aa_reported_next_scope
m12ab_corrected_priority

preferred_boundary_architecture
boundary_architecture_implemented
boundary_output_schema_change_count
boundary_renderer_change_count
boundary_ownership_change_count

raw_directional_balance_preserved
resolved_directional_balance_enabled

directional_threshold_changed
directional_increment_changed
hold_lean_contract_changed
calibration_tiebreak_direction_changed

majority_vote_rule_count
balance_averaging_rule_count
fixed_score_rule_count
evidence_count_bucket_rule_count

financial_semantic_change_count
business_delta_semantic_change_count
financial_framework_semantic_change_count

fictional_generation_id
fictional_subject_count
fictional_context_count
fictional_repetition_count

model_calls_real
model_calls_fictional
model_calls_judge

model_context_success_count
model_context_failure_count
timeout_count
capacity_failure_count
orphan_process_count
cli_internal_retry_event_count
wrapper_retry_count

fictional_output_row_count
fictional_schema_pass_count

objective_semantic_hard_failure_count
invalid_financial_reference_count
grounding_failure_count
business_delta_contract_violation_count
financial_sector_true_misuse_count
financial_framework_exclusion_false_reject_count

fic_fin_05_raw_directional_balance_values
fic_fin_05_boundary_declaration_values
fic_fin_05_resolved_directional_balance_values

fic_fin_05_raw_balance_unique_count
fic_fin_05_boundary_declaration_unique_count
fic_fin_05_resolved_balance_unique_count

raw_formal_stable_count
raw_formal_boundary_uncertainty_count
raw_formal_unstable_count

resolved_formal_stable_count
resolved_formal_boundary_uncertainty_count
resolved_formal_unstable_count

new_buyer_stance_variance_subject_count
holder_stance_variance_subject_count
directional_confidence_variance_subject_count

sol_runtime_real_holdout_suitability

hosted_ci_status
hosted_ci_failure_count
new_hosted_ci_failure_count

real_issuer_model_exposure_count

provider_source_fetches
production_db_mutations
monitoring_registrations
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count

fresh_real_proof_readiness
production_readiness
status
stop_reason
next_scope
```

Anything unmeasured:

```text
NOT_MEASURED
```

---

# 61. Artifact integrity

Freeze all reports/model artifacts/master workflow before final index.

Required:

```text
hash mismatch = 0

size mismatch = 0

secret scan failure = 0
```

Report final result ZIP SHA-256.

---

# 62. Final task principle

M12AA solved the methodological problem that had prevented the project
from seeing a complete repeated sample.

The complete sample now tells us something real:

```text
FIC-FIN-05 is not "wrong 5.5" or "wrong 6.0".

Under the current single-point output contract,
the same economic evidence produces:
SELL 6.0
HOLD 5.5
SELL 6.0
```

All three preserve the same hard economics.

Therefore:

```text
this is a real adjacent-boundary representation problem.
```

The next step is not:

```text
pick the majority

force 6.0

force 5.5

add another prompt line

repair new-buyer stance first
```

The correct flow is:

```text
correct the band-audit normalization bug

→ reclassify the complete FIC-FIN-05 sample as MIXED_BOUNDARY

→ review how a single production call should represent adjacent ambiguity

→ prefer keeping economic judgment in AI
   while making only explicit boundary resolution deterministic

→ implement only if ownership/schema impact is bounded

→ rerun the full Sol/xhigh 8 × 3 canary

→ report raw uncertainty and resolved operational stability separately

→ then handle any remaining new-buyer/holder ambiguity

→ only after that move to a fresh unseen real cohort
```

Do not hide model uncertainty.
Make the operational policy around that uncertainty explicit and reproducible.
