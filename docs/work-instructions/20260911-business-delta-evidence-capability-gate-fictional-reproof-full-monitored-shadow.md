# Thesis Monitor — Business-Delta Evidence Capability Gate + Full Fictional Reproof + Full Monitored Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260911-business-delta-evidence-capability-gate-fictional-reproof-full-monitored-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260911-business-delta-evidence-capability-gate-fictional-reproof-full-monitored-shadow-report.zip
```

Master-workflow phase:

```text
M12AI — Integrated-Main Business-Delta Evidence Capability
         A. Freeze M12AH temporal-scope + M12AG ownership repairs
         B. Separate absolute Core evidence from observed delta evidence
         C. Add deterministic per-ticker business-delta capability gate
         D. Full 8 × 3 two-stage fictional reproof
         E. Full active-monitored same-packet shadow comparison
         F. Combined boundary/delta/holder policy decision
```

M12AH successfully solved the financial temporal-scope blocker.

The M12AH new shadow then progressed one stage farther:

```text
context-01 monolithic:
4 / 4 semantic PASS

context-01 Stage 1:
3 / 4 semantic PASS

failing ticker:
003690

failure:
UNSUPPORTED_ABSOLUTE_STATE_TO_DELTA
```

This is a different failure class.

The frozen business-delta validator correctly rejected the Stage 1 result.

The next task must NOT weaken that validator.

The correct problem is:

```text
Stage 1 was allowed to choose STRENGTHENED
from a stored current thesis statement
that was baseline/context,
not observed change evidence.
```

M12AI must make that impossible by input/schema contract,
not by post-hoc majority vote or another ticker-specific prompt sentence.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260911-financial-claim-temporal-scope-risk-context-full-shadow-rerun-report.zip
```

Verified SHA-256:

```text
8bf0c08091100e1ade31a4e8a467368f67c2b0a44cc208ebef3d25abe265a690
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
indexed payloads = 147
ZIP entries including artifact-index = 148

hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute before trusting the bundle.

---

# 2. M12AH status

M12AH status:

```text
STOPPED_OBJECTIVE_SEMANTIC_HARD_FAILURE
```

Generation:

```text
20260911-m12ah-shadow-20260911T041251Z-568a26e1ce74
```

Runtime:

```text
planned shadow model calls = 18

completed = 2
not run = 16

monolithic calls = 1
Stage 1 calls = 1
Stage 2 calls = 0

timeout = 0
capacity failure = 0
orphan = 0
wrapper retry = 0
```

The stop was semantic,
not runtime.

---

# 3. M12AH temporal-scope repair — freeze successful

M12AH proved:

```text
m12ag 005490 exact replay = PASS

m12ag context-01 four-row temporal re-audit =
4 / 4 PASS

conditional net-debt false reject = 0

prospective net-debt false reject = 0

unsupported current net-debt false accept = 0

risk_context blanket-current classification removed = true

PROSPECTIVE_RISK_SCENARIO enabled = true
```

Focused tests:

```text
123 passed
```

Full local:

```text
3440 passed
1 warning
```

Ruff:

```text
PASS
```

git diff --check:

```text
PASS
```

Do not reopen financial temporal-scope semantics in M12AI.

---

# 4. M12AG monitoring ownership repair — freeze successful

Preserve:

```text
price confirmation Core leak = 0

price risk-reward Core leak = 0

supply Core leak = 0

Timing fact loss = 0

business-delta price-evidence violation = 0
```

Known lineage remains:

```text
monitoring:confirmation_transition
from price_structure.registered_rule_state.confirmation
→ Timing-owned, not Directional Core

monitoring:risk_reward_transition
from price_structure.risk_reward
→ Timing-owned, not Directional Core
```

No ownership change in M12AI.

---

# 5. Two-stage architecture — freeze

Preserve the integrated architecture:

```text
Stage 1 — Core Economic Judgment

Stage 2 — Fundamental Stance

Deterministic Composer
```

Preserve:

```text
Stage 1 has no stance fields

Stage 2 has no writable core fields

core snapshot hash

post-compose core immutability

final external schema compatibility

Price-Timing compatibility

Renderer compatibility

persistence reader compatibility
```

No Stage 2 redesign.

---

# 6. Exact new M12AH failure — 003690

M12AH monolithic control:

```text
ticker = 003690

overall direction =
BUY 6.0:4.0

business_thesis_change =
UNCHANGED
```

Monolithic business-delta audit:

```text
PASS
```

M12AH Stage 1:

```text
ticker = 003690

overall direction =
BUY 6.0:4.0

business_thesis_change =
STRENGTHENED
```

Stage 1 business-delta audit:

```text
FAIL

UNSUPPORTED_ABSOLUTE_STATE_TO_DELTA
```

Important:

```text
primary direction was identical.

the failure is business delta,
not core BUY/HOLD/SELL.
```

---

# 7. Exact unsupported Stage 1 evidence

Stage 1 used:

```text
E19

domain =
BUSINESS_CURRENT

category =
thesis

label =
핵심 투자 논리
```

Statement:

```text
재보험 갱신요율과 손해율 관리가 안정적인 언더라이팅 이익을 만들고
투자수익과 자본적정성이 ROE·배당여력을 높이는지가 핵심이다.

2026년 자사주 전량 소각은
주당가치와 자본배분 측면의 긍정 신호지만,

장기 재평가는 합산비율·대형재해 손실·투자수익률과
자본적정성이 지속적으로 뒷받침해야 한다.
```

Stage 1 interpreted:

```text
"자사주 소각은 자본배분과 주당가치 측면의 의미 있는 긍정 변화"
```

and set:

```text
STRENGTHENED
```

But the validator correctly classified E19 as:

```text
STORED_THESIS_OR_CONFIGURED_SIGNAL_NOT_OBSERVED_DELTA
```

with:

```text
supported_change_directions = []
```

Therefore:

```text
E19 may describe the current stored investment logic.

E19 does NOT by itself prove
that the investment logic strengthened relative to baseline.
```

---

# 8. The model already had the correct generic instruction

The M12AH monolithic and Stage 1 prompts both contained:

```text
business_thesis_change is a change assessment,
not an absolute quality label.

Use STRENGTHENED or WEAKENED only when supplied evidence
establishes a meaningful change relative to a prior or baseline state.

A positive or negative current condition alone does not establish change.

When no supplied fact establishes a directional baseline change,
use UNCHANGED.
```

Therefore the root cause is NOT:

```text
missing business-delta prompt wording.
```

Do not add an equivalent sentence again.

---

# 9. Root-cause classification

Freeze the M12AH failure primarily as:

```text
BUSINESS_DELTA_EVIDENCE_SURFACE_NOT_STRUCTURALLY_SEPARATED
```

The current model sees:

```text
absolute/current Core evidence

stored thesis baseline/context

configured strengthen/weaken signals

observed change evidence
```

inside one broad evidence catalog.

Although the validator can distinguish them after generation,
the model can still select an ineligible absolute/baseline item
and emit a delta state that the hard validator must reject.

The next fix should move that distinction BEFORE generation.

---

# 10. Architecture options review

Before implementation evaluate:

## Option A — add more prompt wording

Reject by default.

The correct rule is already present
and the model still violated it.

## Option B — keep current architecture and hard-stop

Safe but operationally poor.

One stochastic unsupported delta blocks the entire real monitored shadow.

## Option C — post-hoc rewrite unsupported STRENGTHENED/WEAKENED to UNCHANGED

Safer than accepting invalid output,
but hides model/schema noncompliance and mutates a model-owned field after generation.

Do not prefer.

## Option D — deterministic business-delta capability + dynamic schema constraint

Preferred.

Determine BEFORE model call whether the input packet contains
any eligible observed baseline-change evidence.

When none exists:

```text
business_thesis_change can only be UNCHANGED.
```

Constrain the per-ticker structured-output schema accordingly.

When valid observed delta evidence exists:

```text
allow AI judgment.
```

## Option E — separate third AI Business-Delta stage

Architecturally clean but adds more model calls and complexity.

Reserve unless Option D cannot express the contract safely.

Select one architecture before model calls.

Preferred:

```text
BUSINESS_DELTA_CAPABILITY_DYNAMIC_SCHEMA
```

---

# 11. Core principle

The frozen contract already logically implies:

```text
NO OBSERVED BASELINE CHANGE EVIDENCE
→
UNCHANGED ONLY
```

This is not:

```text
a financial score

majority vote

economic weighting rule

ticker-specific target
```

It is an evidence-sufficiency rule.

AI still owns economic interpretation
when valid observed change evidence exists.

---

# 12. Add BusinessDeltaEvidenceCapability

Introduce an internal pre-model object.

Suggested states:

```text
UNCHANGED_ONLY

AI_JUDGMENT

INPUT_AMBIGUOUS
```

Exact names may differ.

Semantics:

## UNCHANGED_ONLY

```text
The complete eligible input catalog contains
no evidence that establishes a meaningful business/financial change
relative to a prior/baseline state.
```

Allowed final value:

```text
UNCHANGED
```

## AI_JUDGMENT

```text
At least one eligible observed change fact exists.
AI must judge materiality/direction against the stored thesis baseline.
```

Allowed:

```text
STRENGTHENED
UNCHANGED
WEAKENED
UNRESOLVED
```

## INPUT_AMBIGUOUS

```text
A change-like item exists
but its lineage/baseline/change semantics cannot be safely classified.
```

Required:

```text
PRE_MODEL_HARD_STOP
```

Do not guess.

---

# 13. Capability is computed from the FULL input catalog

Do NOT compute the capability from refs the model chose after generation.

Compute before the model call from:

```text
the complete per-ticker fundamental evidence catalog.
```

This avoids:

```text
model omits a valid change ref
→ false UNCHANGED_ONLY
```

The gate must know what evidence was available,
not only what the model selected.

---

# 14. Eligible observed delta evidence

Generic eligible classes include:

```text
same-period current vs prior comparable fundamental comparisons

verified financial metric transitions with a real baseline

current vs previous fundamental monitoring transition

dated issuer/business event with clear new-vs-baseline meaning

customer/order/margin/cash-flow/debt/liquidity/capital-allocation change
when supplied as an observed event or comparison

typed financial context whose comparison semantics
establish an actual direction of change
```

Eligibility alone does NOT force STRENGTHENED or WEAKENED.

It only enables AI judgment.

---

# 15. Baseline/context-only evidence

The following do NOT establish change by themselves:

```text
stored current investment thesis

stored thesis drivers

stored strengthen/weaken/invalidation conditions

current single-point operating fact without comparison

current single-point valuation

market expectations

macro exposure

price/technical/supply facts

configured warning conditions

current business quality statement
without baseline transition
```

These may remain useful for:

```text
Core absolute direction

thesis baseline/context

reevaluation conditions

risk context

stance
```

but not as observed delta proof.

---

# 16. 003690 specific role after repair

E19 remains available as:

```text
THESIS_BASELINE_CONTEXT
or equivalent
```

It may help the model understand:

```text
what the investment logic is

what capital-allocation dimension matters
```

But E19 must NOT appear in:

```text
eligible_business_delta_refs
```

unless a separate verified event source establishes
the actual new cancellation event relative to baseline.

For the exact M12AH packet,
the offline capability should be:

```text
UNCHANGED_ONLY
```

unless another valid observed delta fact in the complete catalog is found.

Do not hard-code ticker 003690.

---

# 17. Actual capital-allocation events remain eligible when properly sourced

Do not create a rule such as:

```text
share cancellation can never strengthen a thesis.
```

If a future packet contains:

```text
official dated cancellation event

and the stored baseline did not already incorporate it

and it materially validates a capital-allocation thesis driver
```

then the event may be:

```text
eligible observed delta evidence.
```

The issue in M12AH is evidence provenance,
not the economic concept.

---

# 18. Single-point earnings are not automatically delta

Example:

```text
current Q2 operating income exists
```

without:

```text
same-period comparison
prior baseline
verified transition
```

does NOT by itself establish:

```text
STRENGTHENED
or
WEAKENED.
```

It may support current absolute Core direction.

This distinction is essential.

---

# 19. Market expectations are not business delta evidence

Preserve:

```text
MARKET_EXPECTATIONS
```

as valuation/expectation context.

It cannot by itself prove:

```text
business thesis strengthened
business thesis weakened
```

Expectations may change investment attractiveness,
but business_thesis_change is a fundamental baseline-change assessment.

---

# 20. Dynamic schema constraint

For each candidate/ticker,
generate a structured-output schema reflecting capability.

If:

```text
capability = UNCHANGED_ONLY
```

then:

```text
business_thesis_change enum =
["UNCHANGED"]
```

If:

```text
capability = AI_JUDGMENT
```

then:

```text
business_thesis_change enum =
["STRENGTHENED","UNCHANGED","WEAKENED","UNRESOLVED"]
```

If:

```text
INPUT_AMBIGUOUS
```

do not invoke the model.

No post-hoc rewriting is needed.

---

# 21. Business-delta evidence view

Add a small model-facing per-ticker view,
shared identically by monolithic and Stage 1.

Suggested:

```text
BUSINESS_DELTA_EVIDENCE_VIEW:
{
  capability: UNCHANGED_ONLY | AI_JUDGMENT,
  baseline_context_refs: [...],
  eligible_change_refs: [...],
  eligible_change_direction_hints: {...},
  excluded_change_refs: [
    {ref, reason}
  ]
}
```

Do not dump every excluded ref if prompt size becomes excessive.

At minimum the model must know:

```text
which refs are valid observed delta evidence.
```

This is a structural evidence surface,
not another natural-language admonition.

---

# 22. Same delta view for monolithic and Stage 1

For fair architecture comparison:

```text
monolithic control

two-stage Stage 1
```

must receive identical:

```text
business-delta capability

eligible delta refs

baseline-context classification
```

Required:

```text
business_delta_view_semantic_equality = PASS
```

Do not give Stage 1 a stricter or looser delta surface.

---

# 23. Strengthened / Weakened validation

When:

```text
capability = AI_JUDGMENT
```

and output is:

```text
STRENGTHENED
or
WEAKENED
```

require:

```text
at least one cited eligible_change_ref

and no contradiction with supported change direction semantics.
```

Stored thesis/baseline refs may additionally be cited
to explain materiality,
but cannot be the only delta support.

---

# 24. UNRESOLVED validation

Do not let:

```text
no evidence
```

become:

```text
UNRESOLVED.
```

When capability is:

```text
UNCHANGED_ONLY
```

UNRESOLVED is forbidden.

When capability is:

```text
AI_JUDGMENT
```

UNRESOLVED should require a real reason such as:

```text
conflicting eligible observed change facts

materially ambiguous observed transition

baseline comparability conflict
```

Not merely:

```text
insufficient confirmation of an absolute current condition.
```

---

# 25. UNCHANGED remains an AI option when evidence exists

Even with:

```text
AI_JUDGMENT
```

the model may choose:

```text
UNCHANGED
```

if observed improvement/deterioration is not material to the investment logic.

This preserves the critical distinction:

```text
operating signal
!=
investment-thesis change.
```

This is especially important for FIC-FIN-06.

---

# 26. Business-thesis context consistency

When:

```text
capability = UNCHANGED_ONLY
```

the model may use baseline/current thesis refs in:

```text
business_thesis_context
```

but the prose must not assert:

```text
the thesis strengthened
the thesis weakened
a configured condition has been fulfilled
```

without eligible observed delta evidence.

Add a bounded consistency validator.

Do not require unnatural wording.

---

# 27. Absolute Core direction remains separate

The business-delta gate must NOT prevent the same evidence
from supporting absolute Core direction
when that use is otherwise valid.

Example:

```text
current profitable operation
may support BUY/HOLD absolute reasoning

but may still be insufficient to prove STRENGTHENED.
```

Do not conflate:

```text
delta eligibility
with
Core evidence eligibility.
```

---

# 28. Stored thesis role audit

M12AI must separately audit:

```text
stored thesis as Core baseline/context

vs
stored thesis as material objective directional anchor.
```

Do NOT automatically change Core anchor ownership in this task.

If a stored-thesis-only anchor is found to be causing
unsupported BUY/SELL without independent current evidence:

```text
record it as a separate compatibility issue
for next scope.
```

Do not broaden M12AI unless required for hard safety.

---

# 29. M12AH exact 003690 offline replay

Before model calls,
apply the new capability logic to the exact M12AH packet.

Required report:

```text
full available evidence catalog

baseline-context refs

eligible observed delta refs

excluded delta refs + reasons

capability
```

Expected candidate:

```text
E19 =
BASELINE_ONLY
```

If no other valid observed delta exists:

```text
capability =
UNCHANGED_ONLY
```

Dynamic schema must make:

```text
STRENGTHENED
```

impossible.

Do not rewrite the old M12AH output.

Historical result remains:

```text
invalid raw Stage 1 delta.
```

---

# 30. M12AH context-01 capability audit

Run the new pre-model delta capability offline for:

```text
000660
003690
005490
005930
```

Compare monolithic/Stage 1 input catalogs.

Required:

```text
same capability per ticker
same eligible-change ref set
```

before new generation.

---

# 31. Frozen fictional subject capability audit

Before fictional model calls,
compute delta capability for all:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Do not use prior outputs to choose capability.

Use only frozen input evidence.

Important:

```text
FIC-FIN-06
```

contains the known operating-improvement / thesis-delta question.

Its capability must be determined from whether the packet
contains valid observed comparison evidence,
not from the historical STRENGTHENED/UNCHANGED outputs.

---

# 32. Full fictional reproof is required

M12AI changes:

```text
model-facing business-delta evidence view

per-ticker Stage 1 / monolithic schema constraints
```

Therefore prior fictional model outputs are not sufficient.

Run a NEW full fictional two-stage proof.

Use:

```text
gpt-5.6-sol / xhigh

8 subjects
2 contexts
4 subjects/context
3 repetitions

Stage 1 = 6 calls
Stage 2 = 6 calls

total = 12 calls
24 final outputs
```

No judge calls.

---

# 33. Fictional stop policy

Hard stop only for:

```text
runtime failure

schema failure

invalid evidence identity

objective financial semantic failure

business-delta gate violation

price/technical/supply Core contamination

Stage 2 core mutation
```

Do NOT stop for:

```text
same-direction balance variation

primary threshold variation

valid AI_JUDGMENT delta variation

holder/new-buyer variation

confidence variation
```

Collect all 24 outputs.

This task is diagnostic as well as corrective.

---

# 34. Fictional business-delta hard acceptance

Require across all 24:

```text
UNCHANGED_ONLY subject:
business_thesis_change = UNCHANGED

AI_JUDGMENT subject with STRENGTHENED/WEAKENED:
at least one eligible delta ref

UNRESOLVED:
has actual eligible ambiguity/conflict

stored thesis/configured signal alone:
never supports changed delta

market expectations:
never support business delta

price/timing/supply:
never support business delta
```

Required:

```text
business_delta_capability_violation_count = 0
```

---

# 35. FIC-FIN-06 interpretation

Do NOT force FIC-FIN-06 to:

```text
STRENGTHENED
```

or:

```text
UNCHANGED.
```

If valid observed operating improvement exists,
AI may judge whether it materially validates the thesis.

Record:

```text
operating_change_evidence

baseline thesis driver

materiality reasoning

business_thesis_change
```

If three repetitions still differ,
that is:

```text
BUSINESS_DELTA_MATERIALITY_AMBIGUITY
```

not evidence-surface contamination.

That distinction is the purpose of M12AI.

---

# 36. Full monitored shadow after fictional hard pass

If the 24-output fictional proof has:

```text
zero hard semantic / gate failures
```

run a completely NEW active-monitored shadow generation.

Do not require full fictional decision-material stability
before shadow.

Existing monitored names are an exposed diagnostic cohort.

---

# 37. Active monitored universe

Re-enumerate active monitored stocks read-only.

Last verified reference:

```text
22 names
```

Do not hard-code count.

Record:

```text
task_start_active_monitor_count
task_start_active_monitor_tickers
reference_added_tickers
reference_removed_tickers
```

No monitoring mutation.

---

# 38. Frozen same-packet shadow contract

For every active monitored ticker:

```text
one reproducible local frozen packet

same packet hash for:
- monolithic
- Stage 1
- Stage 2 evidence context
```

No provider refresh.

Required:

```text
provider_source_fetches = 0
```

No stale/fresh mixing.

---

# 39. Full shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic calls = context_count

Stage 1 calls = context_count

Stage 2 calls = context_count

total shadow calls = 3 × context_count
```

At 22 names:

```text
18 calls
```

No repetitions.

No fresh unseen issuer.

---

# 40. Shadow stop policy

Hard stop only for:

```text
runtime/schema failure

packet mismatch

invalid evidence identity

objective financial semantic failure

business-delta capability contract violation

price/timing/supply Core contamination

financial-sector framework misuse

ADR/security-basis violation

Stage 2 core mutation

production-side-effect attempt
```

Do NOT stop for monolithic-vs-two-stage decision differences.

Collect all comparable names.

---

# 41. Shadow comparison taxonomy

Preserve:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

Add diagnostic sublabels:

```text
DELTA_CAPABILITY_UNCHANGED_ONLY

DELTA_AI_JUDGMENT

DELTA_MATERIALITY_DIFFERENCE

DELTA_EVIDENCE_SELECTION_DIFFERENCE
```

No majority vote.

---

# 42. Special 003690 shadow acceptance

For the same frozen packet used in M12AH,
if capability remains:

```text
UNCHANGED_ONLY
```

then both:

```text
monolithic
Stage 1
```

must output:

```text
business_thesis_change = UNCHANGED
```

Core direction may still differ
if architecture changes its absolute reasoning.

Do not force BUY.

---

# 43. Business-delta comparison becomes interpretable only after gating

For every monitored ticker,
report:

```text
delta capability

eligible delta refs

monolithic delta

Stage 1 delta

whether difference is:
- evidence selection
- materiality judgment
- unsupported output
```

After M12AI,
an unsupported output should be structurally impossible
for UNCHANGED_ONLY subjects.

---

# 44. New delta materiality evidence from monitored names

Use the full shadow cohort to identify real examples where:

```text
AI_JUDGMENT
```

is active.

For those names,
study whether monolithic and Stage 1 differ on:

```text
STRENGTHENED
UNCHANGED
WEAKENED
UNRESOLVED
```

with the SAME eligible delta evidence.

These are the best real analogs for:

```text
FIC-FIN-06.
```

Do not infer from UNCHANGED_ONLY names.

---

# 45. Boundary and holder diagnostics remain secondary

Also collect:

```text
primary direction differences

holder stance differences

new-buyer differences
```

to contextualize:

```text
FIC-FIN-05
FIC-FIN-08
```

But do not repair those contracts in M12AI.

The only semantic repair in M12AI is business-delta evidence capability.

---

# 46. Model prompt philosophy

Do not add ticker-specific examples.

Do not add:

```text
"for 003690 use UNCHANGED"
```

Do not add a long prose rule already present.

The model-facing structural change should be:

```text
explicit delta evidence view
+
dynamic allowed schema
```

Generic prompt wording may only explain the new machine-readable view.

---

# 47. No post-hoc delta mutation

Required:

```text
post_model_business_delta_override_count = 0
```

Do not let the model output STRENGTHENED
and silently rewrite it to UNCHANGED afterward.

The allowed value must be constrained before generation.

This preserves auditability.

---

# 48. No scorecard / majority / evidence count

Required zero:

```text
fixed_business_delta_score_rule_count

delta_majority_vote_rule_count

delta_evidence_count_threshold_rule_count

balance_averaging_rule_count
```

One valid observed change fact can enable AI_JUDGMENT.

Quantity is not materiality.

---

# 49. Main integration remains frozen

Do not:

```text
fetch/merge newer main

merge integration branch into main
```

Run on current integrated-main lineage.

Final main merge remains blocked.

---

# 50. Production side-effect firewall

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Schedules remain paused.

---

# 51. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12ai-scope-freeze

04-integrated-main-lineage-freeze

05-m12ah-003690-failure-reproduction

06-monolithic-vs-stage1-delta-prompt-equivalence-proof

07-003690-e19-evidence-role-forensic

08-business-delta-validator-root-cause

09-business-delta-pre-model-architecture-options

10-business-delta-capability-architecture-decision
```

---

# 52. Required delta capability artifacts

Produce:

```text
11-business-delta-evidence-capability-contract

12-eligible-observed-delta-evidence-contract

13-baseline-context-only-evidence-contract

14-business-delta-evidence-view-contract

15-dynamic-business-delta-schema-contract

16-unchanged-only-consistency-contract

17-ai-judgment-changed-delta-grounding-contract

18-unresolved-delta-evidence-contract
```

---

# 53. Required offline audits

Produce:

```text
19-m12ah-003690-capability-replay

20-m12ah-context01-four-ticker-capability-audit

21-stored-thesis-baseline-role-audit

22-current-single-point-financial-delta-audit

23-market-expectation-delta-exclusion-audit

24-price-timing-supply-delta-exclusion-audit

25-capital-allocation-event-delta-eligibility-controls
```

---

# 54. Required deterministic fixtures

At minimum:

```text
DELTA-CAP-01:
stored thesis only
→ UNCHANGED_ONLY

DELTA-CAP-02:
configured strengthen/weaken condition only
→ UNCHANGED_ONLY

DELTA-CAP-03:
current single-period operating fact only
→ UNCHANGED_ONLY

DELTA-CAP-04:
same-period current vs prior improvement
→ AI_JUDGMENT

DELTA-CAP-05:
verified dated issuer event vs baseline
→ AI_JUDGMENT

DELTA-CAP-06:
price confirmation transition only
→ UNCHANGED_ONLY

DELTA-CAP-07:
market-expectation change only
→ UNCHANGED_ONLY

DELTA-CAP-08:
ambiguous change-like lineage
→ INPUT_AMBIGUOUS / pre-model hard stop

DELTA-CAP-09:
eligible positive change evidence
but economically immaterial
→ AI may still choose UNCHANGED

DELTA-CAP-10:
conflicting eligible positive/negative changes
→ UNRESOLVED may be allowed
```

---

# 55. Full test gate before fictional calls

Require:

```text
latest result integrity PASS

M12AH temporal-scope regressions PASS

M12AG ownership regressions PASS

two-stage architecture unchanged

003690 offline capability replay PASS

dynamic schema fixtures PASS

no post-hoc delta override

current validator remains strict

focused tests PASS

full local tests PASS

ruff PASS

git diff --check PASS

production side-effect firewall PASS

runner = gpt-5.6-sol / xhigh
```

---

# 56. Required fictional reproof artifacts

Produce:

```text
26-fictional-delta-capability-manifest

27-fictional-delta-evidence-view-manifest

28-fictional-generation-manifest

29-stage1-run1-context01

30-stage1-run1-context02

31-stage2-run1-context01

32-stage2-run1-context02

33-stage1-run2-context01

34-stage1-run2-context02

35-stage2-run2-context01

36-stage2-run2-context02

37-stage1-run3-context01

38-stage1-run3-context02

39-stage2-run3-context01

40-stage2-run3-context02

41-fictional-hard-semantic-audit

42-fictional-business-delta-capability-audit

43-fictional-business-delta-materiality-audit

44-fictional-primary-direction-stability

45-fictional-new-buyer-stability

46-fictional-holder-stability

47-fictional-core-immutability-audit

48-fictional-runtime-audit

49-fictional-shadow-gate-decision
```

Artifact 49 allows monitored shadow if:

```text
runtime/schema/objective hard semantics PASS

business_delta_capability_violation_count = 0

core mutation = 0
```

Do not require perfect decision-material stability.

---

# 57. Required monitored shadow setup artifacts

Produce:

```text
50-task-start-active-monitored-universe

51-shadow-packet-inventory

52-shadow-packet-hash-manifest

53-shadow-business-delta-capability-manifest

54-shadow-business-delta-evidence-view-manifest

55-shadow-batching-manifest

56-shadow-model-call-gate
```

---

# 58. Required full shadow artifacts

Produce:

```text
57-shadow-monolithic-model-artifacts

58-shadow-stage1-model-artifacts

59-shadow-stage2-model-artifacts

60-shadow-final-composition-artifacts

61-shadow-per-ticker-comparison

62-shadow-business-delta-capability-audit

63-shadow-core-direction-differences

64-shadow-business-delta-differences

65-shadow-new-buyer-differences

66-shadow-holder-differences

67-shadow-same-direction-calibration-differences

68-shadow-expected-contract-corrections

69-shadow-potential-architecture-regressions

70-shadow-unresolved-review-required

71-shadow-financial-sector-audit

72-shadow-adr-security-basis-audit

73-shadow-cyclical-valuation-audit

74-shadow-core-immutability-audit

75-shadow-runtime-audit

76-shadow-aggregate-summary

77-shadow-architecture-decision
```

Preserve raw model-call artifacts.

---

# 59. Required combined diagnostics

Produce:

```text
78-fic-fin-05-vs-monitored-primary-boundary-analogs

79-fic-fin-06-vs-monitored-delta-materiality-analogs

80-fic-fin-08-vs-monitored-holder-analogs

81-real-business-delta-materiality-lessons

82-combined-fictional-monitored-root-cause-summary

83-next-bounded-policy-decision
```

---

# 60. Shadow / fictional interpretation rules

Do not treat:

```text
monolithic
```

as ground truth.

Do not treat:

```text
two-stage
```

as ground truth.

The delta capability gate only establishes:

```text
whether a changed state is evidence-eligible.
```

It does NOT decide:

```text
whether an eligible observed change
is material enough to strengthen/weaken the investment logic.
```

That remains the AI/economic judgment to analyze.

---

# 61. Fresh real proof remains blocked

At M12AI completion:

```text
fresh_real_proof_readiness = NOT_READY
```

even if full fictional and monitored shadow complete.

The goal is to finally obtain:

```text
clean architecture compatibility evidence
without unsupported delta noise.
```

Use the combined result to perform one bounded:

```text
boundary / delta-materiality / holder policy review
```

before fresh unseen proof.

---

# 62. Final main merge remains blocked

Always:

```text
final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY
```

No main merge.

No deployment.

No monitoring resume.

---

# 63. Failure handling

## A. Capability cannot be determined safely

```text
next_scope =
BUSINESS_DELTA_EVIDENCE_LINEAGE_ARCHITECTURE_REVIEW
```

No model calls.

## B. Dynamic schema destabilizes unrelated core semantics

```text
next_scope =
BUSINESS_DELTA_SCHEMA_COUPLING_REVIEW
```

## C. Fictional hard semantics fail

Do not run monitored shadow.

Use the smallest failing contract.

## D. Full shadow shows unsupported changed delta despite capability gate

```text
STOP
BUSINESS_DELTA_GATE_IMPLEMENTATION_FAILURE
```

## E. Full shadow shows clean gating but real materiality differences

```text
next_scope =
BUSINESS_THESIS_DELTA_MATERIALITY_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

## F. Shadow also shows primary-direction/holder systematic differences

Use:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

with the completed cohort.

---

# 64. Program-completion fields

Include at least:

```text
base_integration_head_sha

integration_branch

latest_result_zip_sha256
latest_result_integrity

business_delta_root_cause

business_delta_capability_contract_version

business_delta_view_enabled

dynamic_business_delta_schema_enabled

post_model_business_delta_override_count

fixed_business_delta_score_rule_count
delta_majority_vote_rule_count
delta_evidence_count_threshold_rule_count

m12ah_003690_capability
m12ah_003690_eligible_delta_refs
m12ah_003690_baseline_only_refs

fictional_subject_count
fictional_repetition_count

fictional_unchanged_only_subject_count
fictional_ai_judgment_subject_count
fictional_input_ambiguous_subject_count

fictional_stage1_model_calls
fictional_stage2_model_calls
fictional_model_calls_total

fictional_output_count
fictional_business_delta_capability_violation_count
fictional_business_delta_materiality_variance_subject_count

fictional_primary_direction_unstable_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_unchanged_only_ticker_count
shadow_ai_judgment_ticker_count
shadow_input_ambiguous_ticker_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count

shadow_business_delta_capability_violation_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count

price_confirmation_core_leak_count
price_risk_reward_core_leak_count
supply_core_leak_count
timing_fact_loss_count

conditional_netdebt_false_reject_count
prospective_netdebt_false_reject_count
current_unsupported_netdebt_false_accept_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

investment_judgment_model_target
investment_judgment_reasoning_effort

focused_test_result
full_test_result
ruff_result
git_diff_check

fictional_shadow_gate_status
two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything not measured:

```text
NOT_MEASURED
```

---

# 65. Artifact integrity

Freeze all artifacts before the final artifact index.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final result ZIP SHA-256.

---

# 66. Final task principle

M12AH proved that the latest temporal-scope repair is correct.

The next failure is not another financial parser bug.

It is:

```text
the model had one broad Core evidence catalog

→ saw a favorable stored thesis statement

→ treated that baseline/current statement as observed change

→ emitted STRENGTHENED

→ the correct hard validator rejected it.
```

The correct next architecture is:

```text
classify business-delta evidence before model generation

→ expose an explicit delta-only evidence view

→ if no observed baseline-change evidence exists,
   constrain the schema to UNCHANGED

→ if valid change evidence exists,
   let AI judge materiality and direction

→ keep absolute Core reasoning separate

→ rerun the full fictional two-stage proof

→ then run the complete active-monitored same-packet shadow

→ finally use real monitored examples
   to decide the true boundary / delta-materiality / holder policies
```

Do NOT:

```text
weaken the business-delta validator

tell the model "003690 should be UNCHANGED"

count pieces of evidence to determine delta

majority-vote repeated outputs

post-hoc rewrite invalid model output

remove stored thesis from Core entirely without separate review

reopen temporal-scope or price ownership

resume a partial shadow generation

refresh providers

merge into main

start fresh unseen proof

return to Astra

resume production monitoring
```

Separate:
`what the thesis currently is`
from
`what evidence proves that it changed`.
