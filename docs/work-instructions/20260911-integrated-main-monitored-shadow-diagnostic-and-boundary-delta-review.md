# Thesis Monitor — Integrated-Main Monitored Shadow Diagnostic + Boundary/Delta Review

## 0. Task identity

Suggested work-instruction filename:

```text
20260911-integrated-main-monitored-shadow-diagnostic-and-boundary-delta-review.md
```

Suggested result bundle:

```text
thesis-monitor-20260911-integrated-main-monitored-shadow-diagnostic-boundary-delta-review-report.zip
```

Master-workflow phase:

```text
M12AF — Integrated-Main Diagnostic Shadow
         A. Freeze the successful main-integration/two-stage architecture
         B. Reclassify the completed fictional findings
         C. Run the existing monitored-stock same-packet shadow cohort
         D. Compare monolithic vs two-stage decision-material impact
         E. Use combined fictional + monitored evidence to choose the next bounded repair
```

This task begins after M12AE-R2.

M12AE-R2 successfully proved:

```text
latest main integration baseline = PASS

two-stage structural architecture = PASS

Stage 1 / Stage 2 ownership isolation = PASS

core immutability = PASS

final external schema compatibility = PASS

integrated persistence reader compatibility = PASS

integrated Price-Timing compatibility = PASS

integrated Renderer compatibility = PASS

12 / 12 fictional stage model calls = PASS

24 / 24 fictional final outputs schema-valid

runtime hard failure = 0

objective semantic hard failure = 0

grounding failure = 0
```

M12AE-R2 did NOT run the monitored-stock shadow because its original gate required
all fictional decision-material stability classes to be PASS.

M12AF intentionally changes that one workflow gate:

```text
Existing monitored stocks are already exposed.
The shadow phase is read-only and diagnostic.
Therefore a fictional calibration/boundary stability blocker
does NOT prevent running monitored compatibility diagnostics,
provided structural architecture + objective hard semantics already passed.
```

This change does NOT authorize:

```text
fresh unseen real proof

production activation

main merge

monitoring writes

warnings

notifications
```

The goal is to learn whether the remaining fictional boundary/delta/holder variance
generalizes to the existing real monitored universe
before making another synthetic-only contract change.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260911-main-integration-two-stage-directional-fictional-monitored-shadow-validation-report.zip
```

Verified SHA-256:

```text
b72cf76bf454b67237347caf31b457dc5b56b57d92765eba568769c0fd19c98d
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
artifact_count = 176
missing = 0
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute independently.

---

# 2. Integrated-main baseline — preserve exactly

M12AE-R2 froze:

```text
main_frozen_sha =
d18e68b1e944d7749d093b08797fcd9498412680

main_commit_date =
2026-09-05T15:42:14+09:00

M12AD source head =
69661d6754b0555e4c11caba8a53bd55a1fe7140

integration branch =
codex/20260911-main-integration-two-stage-directional

integration merge commit =
f1078d283b0bc963553fd25ade272b6002b7a4c2

post-merge baseline sha =
f1078d283b0bc963553fd25ade272b6002b7a4c2

final integration head at report closeout =
a9a0ea52be9f3a8e4c1eedbae522e94078e2c656
```

At M12AF task start:

```text
resolve actual integration-branch HEAD
```

and verify it is the expected M12AE-R2 lineage.

Do NOT fetch/merge a newer moving main in M12AF.

The integration proof is against the already-frozen main baseline.

A later final-main-merge task will handle any drift.

---

# 3. M12AE-R2 main integration proof — frozen successful

Post-merge integration baseline:

```text
focused = 452 passed

full local = 3388 passed
2 warnings

ruff = PASS

git diff --check = PASS

runtime config smoke = PASS

integration conflicts = 0

M12AD semantic fingerprint = PRESERVED

latest-main behavior = PRESERVED
```

Do not reopen main integration.

No merge back to `main`.

---

# 4. Two-stage architecture — freeze

M12AE-R2 selected:

```text
STAGE1_CORE_THEN_STAGE2_STANCE_DETERMINISTIC_COMPOSER
```

Structural proof:

```text
Stage 1 writable stance fields = 0

Stage 2 writable core fields = 0

Pydantic extra policy = FORBID

cross-subject composition = REJECT

core mutation after stance = 0

external final schema change = 0
```

Integrated consumers:

```text
persistence readers = PASS

Price-Timing consumer = PASS

Renderer consumer = PASS
```

Do not change the two-stage model prompts,
schemas,
ownership,
or composer in M12AF.

M12AF is a diagnostic shadow phase.

---

# 5. Monolithic control — reuse frozen control

M12AE-R2 froze the integrated monolithic control before two-stage implementation.

Control:

```text
prompt_sha256 =
ffec08bb75ecc3d1caae3f341833d867c695010bff3561ceed2a351d73861d0d

schema_sha256 =
e8eba3acda7e0289423132d33cd3cca9ba121439154df6a67c607a7d34beb1a0

control code sha256 =
3ff975fab9188171566258f0105453230b4279754eccb3836335158296ea092c
```

M12AF must reuse this frozen control for monitored shadow comparison.

Do not regenerate a semantically different control prompt.

If the frozen control cannot be reproduced byte-for-byte:

```text
STOP
MONOLITHIC_CONTROL_DRIFT
```

---

# 6. Proof-critical model/runtime

Use:

```text
model = gpt-5.6-sol

reasoning = xhigh
```

Runtime:

```text
MODEL_CONTEXT_COUPLED

up to 4 subjects/context

1800-second finite watchdog

single authoritative watchdog

wrapper auto-retry = 0

batch split = 0
```

No GPT-6 Astra.

No fallback.

No timeout increase.

If actual runner differs:

```text
STOP
SOL_RUNNER_MODEL_TARGET_MISMATCH
```

---

# 7. M12AE-R2 fictional hard-semantic result — freeze successful

M12AE-R2 completed:

```text
Stage 1 model calls = 6 / 6

Stage 2 model calls = 6 / 6

total = 12 / 12

final outputs = 24 / 24

timeout = 0

capacity failure = 0

orphan = 0

wrapper retry = 0

core mutation after stance = 0

objective semantic hard failure = 0
```

Also preserve:

```text
financial grounding

working-capital grounding

QTD/YTD semantics

debt completeness

financial-sector framework safety

business-delta evidence identity

price/technical/supply ownership
```

No semantic repair in M12AF.

---

# 8. Completed fictional findings — reclassify for diagnostic use

M12AE-R2 blockers:

## FIC-FIN-05

```text
Stage 1 overall direction:
SELL / HOLD / HOLD

balance:
4.0:6.0
4.5:5.5
4.5:5.5

business delta:
UNCHANGED ×3

Stage 2 new buyer:
WAIT ×3

Stage 2 holder:
REVIEW ×3
```

Hard economics remained coherent.

Interpretation for M12AF:

```text
INTRINSIC_ADJACENT_PRIMARY_BOUNDARY_CANDIDATE
```

because stance decoupling did not remove the core HOLD/SELL threshold variation.

Do not majority-vote.

Do not force 5.5 or 6.0.

## FIC-FIN-06

```text
Stage 1 direction:
HOLD / HOLD / BUY

balance:
5.5:4.5
5.5:4.5
6.0:4.0

business delta:
STRENGTHENED / STRENGTHENED / UNCHANGED

Stage 2 new buyer:
WAIT ×3

Stage 2 holder:
HOLDABLE ×3
```

Interpretation for M12AF:

```text
POSITIVE_THRESHOLD_BOUNDARY
+
OPERATING_SIGNAL_VS_THESIS_DELTA_AMBIGUITY
```

Do not assume either STRENGTHENED or UNCHANGED is correct yet.

## FIC-FIN-08

```text
Stage 1 core:
HOLD 5.0:5.0 ×3

business delta:
UNCHANGED ×3

new buyer:
WAIT ×3

holder:
HOLDABLE / HOLDABLE / REVIEW
```

Interpretation:

```text
HOLDER_UNKNOWN_SEVERITY_BOUNDARY
```

The unresolved question is whether:
- ordinary structural claims/capital uncertainty are enough for REVIEW
or
- REVIEW requires a more confirmed material holding risk.

---

# 9. Why monitored shadow is now allowed

The previous gate:

```text
fictional decision-material stability must fully PASS
before any monitored shadow call
```

was appropriate when architecture/hard semantics were still unproven.

That condition is now too restrictive for diagnostics because:

```text
two-stage ownership is proven

core immutability is proven

hard financial semantics are clean

runtime is clean

the remaining findings are exactly architecture-impact/stability questions
that existing exposed monitored names can help contextualize
```

Therefore M12AF allows monitored shadow if:

```text
architecture integrity = PASS

objective semantic hard failure count = 0

production side-effect firewall = PASS
```

even though:

```text
fresh_real_proof_readiness remains NOT_READY.
```

---

# 10. Shadow is diagnostic, not readiness proof

Do NOT claim:

```text
shadow PASS = fresh real generalization PASS
```

The shadow phase answers:

```text
Does two-stage generation create unexpected decision-material changes
on existing monitored stocks when compared on the exact same evidence packet?
```

It does NOT answer:

```text
Will the architecture generalize to unseen companies?
```

Fresh unseen proof stays blocked after M12AF.

---

# 11. Active monitored universe — task-start authority

At task start:

```text
re-enumerate the active monitored universe in read-only mode.
```

Preferred authority:

```text
existing monitoring backend/store
```

If the remote/list endpoint is unavailable,
use the repository/local persisted monitoring store only if it is an authoritative mirror.

Do not silently assume the historical count.

Last verified reference universe:

```text
22 active names
8 KRX
14 US
```

Reference KRX:

```text
000660
003690
005490
005930
010120
012450
047810
086280
```

Reference US:

```text
CORZ
CPNG
CRCL
GOOGL
HUT
IBM
MU
RXRX
SKHY
SNDK
TSLA
TSM
WRD
WULF
```

Record:

```text
task_start_active_monitor_count

task_start_active_monitor_tickers

reference_added_tickers

reference_removed_tickers
```

If no authoritative active list can be obtained:

```text
STOP
ACTIVE_MONITORED_UNIVERSE_UNRESOLVED
```

Do not use a stale list as if current.

---

# 12. Frozen same-packet requirement

For every active monitored ticker:

Retrieve ONE reproducible fundamental evidence packet from existing local/archived state.

Preferred priority:

```text
1. latest archived accepted DecisionEvidencePacket

2. latest deterministic nonproduction packet already persisted/cached locally

3. another reproducible local packet with full alias/catalog lineage
```

Do NOT call external providers to refresh data.

Required default:

```text
provider_source_fetches = 0
```

For every ticker compute:

```text
shadow_packet_sha256
```

The exact packet must be used for:

```text
monolithic control

two-stage Stage 1

two-stage Stage 2 evidence/citation context
```

If packet differs:

```text
NOT_COMPARABLE_PACKET_MISMATCH
```

Do not compare outputs.

---

# 13. Packet staleness

Record:

```text
packet_as_of_date

packet_age_days if derivable
```

Stale evidence does not automatically invalidate architecture comparison.

Reason:

```text
both paths use the identical stale packet.
```

But do not interpret shadow output as the company's current investment view
if the packet is stale.

Label the entire shadow phase:

```text
ARCHITECTURE_COMPATIBILITY_ONLY
```

---

# 14. Shadow call topology

For N active monitored names:

```text
context_count = ceil(N / 4)
```

Run:

```text
monolithic control calls = context_count

two-stage Stage 1 calls = context_count

two-stage Stage 2 calls = context_count
```

Total:

```text
3 × context_count
```

At N=22:

```text
6 + 6 + 6 = 18
```

but do not hard-code 18.

No repeated monitored runs.

One monolithic + one two-stage comparison per ticker.

---

# 15. Shadow order

For each frozen group:

```text
1. monolithic control

2. two-stage Stage 1

3. two-stage Stage 2

4. deterministic composition

5. same-packet comparison
```

Record:

```text
context membership

packet hashes

monolithic invocation ID

Stage 1 invocation ID

Stage 2 invocation ID

core snapshot hash
```

No packet refresh between calls.

---

# 16. Shadow hard-stop conditions

Stop the shadow phase only for:

```text
runtime hard failure

schema failure

invalid evidence identity

same-packet hash mismatch

objective financial semantic failure

Stage 2 core mutation

ticker mismatch

production-side-effect attempt
```

Do NOT stop because:

```text
one monitored ticker changes direction

one ticker changes holder stance

one ticker changes new-buyer stance

one ticker changes business delta
```

Those differences are the purpose of the diagnostic cohort.

Collect all comparable names.

---

# 17. Per-ticker comparison fields

Compare:

## Core

```text
overall_direction

directional_balance

hold_lean

directional_confidence

business_thesis_change
```

## Stance

```text
fundamental_new_buyer.stance

fundamental_holder.stance
```

## Semantic structure

```text
material anchor domains

dominant evidence refs

major Unknowns

business reevaluation conditions

business invalidation condition
```

## Safety

```text
invalid evidence refs

financial grounding

business-delta support

financial-sector framework use

ADR/security basis

price/technical/supply contamination
```

---

# 18. Compatibility taxonomy

Each monitored ticker gets one primary label:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

NOT_COMPARABLE_PACKET_UNAVAILABLE

NOT_COMPARABLE_PACKET_MISMATCH

OTHER_REVIEW_REQUIRED
```

Secondary labels may be added.

---

# 19. Same-direction calibration changes are not automatic regressions

Examples:

```text
BUY 6.0 → BUY 6.5

HOLD 5.0 → HOLD 5.5 BUY_LEAN

SELL 6.0 → SELL 6.5
```

with stable:

```text
overall direction
business delta
new buyer
holder
```

classify:

```text
SAME_DIRECTION_CALIBRATION_CHANGE
```

Report it.

Do not call it a regression solely because the exact balance differs.

---

# 20. Decision-material differences require evidence review

For every:

```text
PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE
```

create a compact forensic record:

```text
ticker

same packet hash

monolithic field/value

two-stage field/value

shared evidence refs

different evidence refs

major Unknowns

relevant frozen contract

classification rationale
```

Then resolve to:

```text
EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

Do not automatically assume the monolithic answer is correct.

Do not automatically assume the two-stage answer is correct.

---

# 21. Expected contract correction

Use only when two-stage output clearly better follows an already-frozen contract.

Examples:

```text
monolithic holder REDUCE
→ two-stage REVIEW

while severity/persistence remains materially unresolved
under the frozen holder contract
```

or:

```text
monolithic business delta WEAKENED
→ two-stage UNCHANGED

while same packet contains no baseline deterioration
```

No new semantic rule may be invented after seeing a difference.

---

# 22. Potential architecture regression

Use when two-stage output changes a decision-material field
without support from:

```text
the same packet

the frozen semantic contract

or removal of monolithic cross-field interference
```

Examples:

```text
Stage 1 loses a material evidence domain

business delta ignores valid prior/baseline evidence

Stage 2 holder contradicts Stage 1 severity/Unknowns

Stage 2 new buyer becomes ATTRACTIVE despite a critical unresolved confirmation condition

ADR/security basis changes

insurance gets industrial net-debt/WC reasoning
```

Every regression must cite exact evidence refs.

---

# 23. Diagnostic patterns to specifically look for

Use the existing fictional findings as hypotheses,
not targets.

## Pattern A — leverage / resilience boundary

Search monitored names for:

```text
material debt/liquidity weakness
+
operating counterevidence
+
unresolved refinancing/severity
```

Check whether monolithic vs two-stage changes:

```text
HOLD ↔ SELL
WAIT ↔ AVOID
REVIEW ↔ REDUCE
```

Do not force analogy where the real packet differs.

## Pattern B — positive operating improvement + quality Unknown

Search for cases where:

```text
operating metrics improve
but cash/working-capital/customer/quality confirmation remains unresolved
```

Check:

```text
HOLD ↔ BUY
STRENGTHENED ↔ UNCHANGED
```

This contextualizes FIC-FIN-06.

## Pattern C — financial-sector holder uncertainty

For financial-sector monitored names,
check whether:

```text
ordinary structural risk + unresolved capital trend
```

creates:

```text
HOLDABLE ↔ REVIEW
```

This contextualizes FIC-FIN-08.

---

# 24. Financial-sector hard audit

For:

```text
003690 코리안리
```

if a comparable packet exists:

Require:

```text
insurance-appropriate underwriting/capital reasoning

no industrial net-debt/WC application

legitimate exclusion/non-application wording accepted
```

Any failure is:

```text
OBJECTIVE_SEMANTIC_HARD_FAILURE
```

not a compatibility difference.

---

# 25. ADR/security-basis hard audit

For:

```text
SKHY
```

if a comparable packet exists:

Require:

```text
ADR/security basis preserved

currency basis preserved

no denominator reconstruction

no provider multiple back-solving
```

Two-stage architecture must not alter security-basis semantics.

---

# 26. Cyclical valuation hard audit

For cyclical monitored names including memory/materials where applicable:

Preserve the configured valuation logic.

Do not flatten into:

```text
low current PER = cheap
```

when the stored framework uses:

```text
mid-cycle
P/B
FCF
ROIC
SOTP
or other sector-aware methods
```

Architecture comparison must preserve the stored framework.

---

# 27. No Price-Timing contamination

The shadow compatibility question is fundamental architecture.

Do not refresh or inject current:

```text
price

technical

supply/flow
```

into the Directional comparison.

If the frozen packet already includes fundamental market-expectation/valuation context,
preserve it identically.

Price-Timing remains downstream and out of the primary shadow comparison.

---

# 28. No model/prompt changes before shadow

M12AF must NOT change:

```text
Stage 1 prompt

Stage 2 prompt

monolithic control prompt

Stage 1 schema

Stage 2 schema

final composer semantics

Directional thresholds

holder/new-buyer contracts

business-delta contract
```

before shadow execution.

Reason:

```text
the task is diagnosing the already-measured two-stage architecture.
```

If a deterministic shadow-runner bug is found,
repair only the runner/reporting plumbing.

Do not repair model semantics before seeing the shadow cohort.

---

# 29. No fresh unseen calls

Required:

```text
model_calls_real_fresh_unseen = 0
```

Existing monitored names are exposed compatibility subjects,
not fresh unseen subjects.

Do not select new companies in M12AF.

---

# 30. Production side-effect firewall

Required:

```text
provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Integration branch work/read-only model calls are allowed.

No merge into `main`.

---

# 31. Schedule state

Observe approved monitoring schedules at start/end.

Do not resume them.

Do not mutate unrelated jobs.

Record:

```text
observed_paused_schedule_count

scheduler_mutation_count

automatic_monitoring_resume
```

---

# 32. Shadow completeness

Report:

```text
active monitored count

packet available count

packet unavailable count

packet mismatch count

shadow completed ticker count
```

Do not hide missing packets.

If a packet cannot be reproduced without provider refresh:

```text
NOT_COMPARABLE_PACKET_UNAVAILABLE
```

and continue with other names.

Unlike fresh proof,
a few unavailable diagnostic names do not require stopping the entire shadow phase.

But they prevent claiming:

```text
FULL_MONITORED_UNIVERSE_COMPATIBILITY
```

---

# 33. Shadow aggregate counts

Report:

```text
NO_DECISION_MATERIAL_CHANGE count

SAME_DIRECTION_CALIBRATION_CHANGE count

PRIMARY_DIRECTION_CHANGE count

BUSINESS_DELTA_CHANGE count

NEW_BUYER_STANCE_CHANGE count

HOLDER_STANCE_CHANGE count

MULTI_FIELD_DECISION_CHANGE count

EXPECTED_CONTRACT_CORRECTION count

POTENTIAL_ARCHITECTURE_REGRESSION count

OTHER_REVIEW_REQUIRED count

NOT_COMPARABLE count
```

Break down by:

```text
KR / US
sector/framework where available
```

No percentage-based pass gaming.

---

# 34. Shadow architecture decision

At the end choose one:

```text
TWO_STAGE_COMPATIBILITY_CLEAN

TWO_STAGE_COMPATIBILITY_CLEAN_WITH_EXPECTED_CORRECTIONS

TWO_STAGE_COMPATIBILITY_HAS_BOUNDED_REGRESSIONS

TWO_STAGE_COMPATIBILITY_HAS_SYSTEMIC_REGRESSION

TWO_STAGE_COMPATIBILITY_INCOMPLETE
```

Do not say simply PASS/FAIL without classifying the differences.

---

# 35. Combined fictional + monitored evidence decision

After shadow, assess each remaining fictional issue.

## FIC-FIN-05

Question:

```text
Does HOLD/SELL threshold sensitivity
also appear materially in monitored leverage/resilience cases?
```

Classify:

```text
SYNTHETIC_EDGE_ONLY_SO_FAR

REAL_MONITORED_ANALOG_PRESENT

INSUFFICIENT_REAL_ANALOG_COVERAGE
```

Do not infer from unrelated companies.

## FIC-FIN-06

Question:

```text
Do monitored operating-improvement / quality-Unknown cases
show similar HOLD/BUY or STRENGTHENED/UNCHANGED differences?
```

Classify.

## FIC-FIN-08

Question:

```text
Does a real financial-sector monitored name
show HOLDABLE/REVIEW architecture sensitivity
under the same holder contract?
```

Classify.

---

# 36. Business-delta semantic review — diagnostic only

M12AF must produce an offline report on FIC-FIN-06:

```text
positive operating checkpoint
vs
investment-thesis strengthening
```

Key distinction:

```text
a comparable-period metric improvement
is a positive operating signal

but does not necessarily mean the stored investment logic strengthened
unless it materially validates/improves a thesis driver relative to its baseline.
```

Do NOT change the business-delta model prompt in M12AF.

Use monitored shadow evidence to decide whether this distinction
needs a later contract repair.

---

# 37. Holder semantic review — diagnostic only

M12AF must produce an offline report on FIC-FIN-08:

```text
ordinary structural risk
+
unresolved capital/validation data
```

vs:

```text
confirmed material holder reconsideration risk.
```

Question:

```text
Should missing confirmation alone ever elevate HOLDABLE → REVIEW?
```

Use the frozen holder contract.

Do NOT change Stage 2 prompt in M12AF.

Use the monitored financial-sector shadow comparison if available.

---

# 38. Primary-boundary policy review — diagnostic only

M12AF must produce an offline report on:

```text
FIC-FIN-05 negative 5.5 ↔ 6.0

FIC-FIN-06 positive 5.5 ↔ 6.0
```

The report must distinguish:

```text
raw primary direction instability

economic semantic stability

Stage 2 stance stability

threshold adjacency
```

Do not majority-vote.

Do not average balances.

Do not deterministically rewrite raw direction in M12AF.

---

# 39. No readiness relaxation yet

Even if shadow is clean:

```text
fresh_real_proof_readiness = NOT_READY
```

throughout M12AF.

Reason:

```text
M12AF is diagnostic.
```

Its purpose is to decide the next bounded semantic/policy repair
using both fictional and monitored evidence.

Fresh unseen proof requires one subsequent task.

---

# 40. Next-scope logic

## A. Shadow shows zero architecture regressions and boundary issues look synthetic/rare

Use:

```text
next_scope =
DECISION_BOUNDARY_AND_DELTA_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

This task should resolve:
- adjacent primary threshold policy
- FIC-FIN-06 thesis-delta semantics
- FIC-FIN-08 holder semantics
without changing architecture.

## B. Shadow shows repeated primary-direction differences across monitored names

Use:

```text
next_scope =
TWO_STAGE_CORE_STABILITY_ARCHITECTURE_REVIEW_ON_INTEGRATED_MAIN
```

Do not proceed to fresh real.

## C. Shadow shows systematic business-delta differences

Use:

```text
next_scope =
BUSINESS_THESIS_DELTA_SEMANTICS_REVIEW_ON_INTEGRATED_MAIN
```

## D. Shadow shows systematic new-buyer/holder changes

Use:

```text
next_scope =
FUNDAMENTAL_STANCE_STAGE_COMPATIBILITY_REVIEW_ON_INTEGRATED_MAIN
```

## E. Shadow shows decision-material regressions caused by missing evidence in Stage 1/2

Use:

```text
next_scope =
TWO_STAGE_EVIDENCE_SURFACE_COMPATIBILITY_REPAIR
```

## F. Shadow is clean except same-direction calibration differences

Record them as non-material compatibility variance
and proceed to the bounded fictional policy review next.

---

# 41. Main merge remains prohibited

Do not merge integration branch into `main`.

At completion report:

```text
final_main_merge_readiness = NOT_READY
```

because fresh unseen real proof has not occurred.

No final deployment.

---

# 42. Hosted CI portability

Carry the known portability backlog.

M12AE-R2 did not run hosted CI on the unpushed integration branch.

M12AF should not make hosted CI a blocker for the diagnostic shadow,
but must introduce:

```text
new hosted-CI portability regression count = 0
```

if CI is run.

Do not broaden into cleanup.

---

# 43. Required artifacts — latest baseline reuse

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12af-scope-freeze

04-integrated-main-baseline-reuse-proof

05-two-stage-architecture-freeze-proof

06-monolithic-control-freeze-proof

07-fictional-hard-semantic-reuse-proof

08-fictional-blocker-reclassification
```

---

# 44. Required monitored setup artifacts

Produce:

```text
09-task-start-active-monitored-universe

10-reference-vs-task-start-universe-diff

11-shadow-packet-source-contract

12-shadow-packet-inventory

13-shadow-packet-hash-manifest

14-shadow-batching-manifest

15-shadow-model-call-gate
```

---

# 45. Required monitored model artifacts

For every shadow context preserve:

```text
monolithic control:
prompt
schema
raw output
receipt
transport log
run document

two-stage Stage 1:
prompt
schema
raw output
receipt
transport log
run document

two-stage Stage 2:
prompt
schema
raw output
receipt
transport log
run document
```

Stage 2 artifacts must include:

```text
parent Stage 1 invocation ID
core snapshot hash by ticker
```

---

# 46. Required monitored comparison artifacts

Produce:

```text
16-shadow-per-ticker-comparison-table

17-shadow-core-direction-differences

18-shadow-business-delta-differences

19-shadow-new-buyer-differences

20-shadow-holder-differences

21-shadow-same-direction-calibration-differences

22-shadow-expected-contract-corrections

23-shadow-potential-architecture-regressions

24-shadow-unresolved-review-required

25-shadow-sector-coverage

26-shadow-financial-sector-audit

27-shadow-adr-security-basis-audit

28-shadow-cyclical-valuation-framework-audit

29-shadow-core-immutability-audit

30-shadow-runtime-audit

31-shadow-aggregate-summary

32-shadow-architecture-decision
```

---

# 47. Required combined-diagnostic artifacts

Produce:

```text
33-fic-fin-05-boundary-vs-monitored-leverage-analogs

34-fic-fin-06-operating-signal-vs-thesis-delta-review

35-fic-fin-06-vs-monitored-growth-quality-analogs

36-fic-fin-08-holder-risk-severity-review

37-fic-fin-08-vs-monitored-financial-sector-analogs

38-combined-fictional-monitored-root-cause-summary

39-next-bounded-repair-decision
```

---

# 48. Required final completion artifacts

Produce:

```text
40-existing-monitored-impact-summary

41-two-stage-shadow-compatibility-readiness

42-fresh-real-proof-readiness-decision

43-final-main-merge-readiness-note

44-production-no-change

45-schedule-pause-observation

46-hosted-ci-portability-handoff

47-astra-future-experiment-handoff

48-master-workflow-update

49-program-completion
```

---

# 49. Shadow model-call acceptance

Before shadow calls:

```text
latest result integrity PASS

integration branch lineage verified

two-stage code hash frozen

monolithic control hash frozen

no prompt/schema semantic change since M12AE-R2

active monitored universe resolved

frozen packet inventory complete for all available names

same-packet hash contract PASS

provider refresh disabled

production write firewall PASS

runner target = gpt-5.6-sol / xhigh
```

---

# 50. Shadow runtime acceptance

Require for all attempted contexts:

```text
timeout = 0

capacity failure = 0

orphan = 0

wrapper retry = 0
```

If one context transport-fails:

```text
STOP SHADOW
```

No selective retry.

Preserve prior completed outputs.

No fresh-real exposure implications,
but do not stitch shadow generations.

---

# 51. Shadow hard-semantic acceptance

Across completed monitored outputs require zero:

```text
invalid evidence refs

financial grounding failures

business-delta unsupported-change violations

financial-sector framework misuse

ADR/security-basis violation

price/technical/supply Directional contamination

Stage 2 core mutation

ticker mismatch
```

These are objective hard failures.

Architecture output differences are not hard failures.

---

# 52. Program-completion fields

Include at least:

```text
base_integration_head_sha

integration_branch

monolithic_control_prompt_sha256

monolithic_control_schema_sha256

two_stage_service_sha256

investment_judgment_model_target
investment_judgment_reasoning_effort

task_start_active_monitor_count
task_start_active_monitor_tickers

reference_added_tickers
reference_removed_tickers

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_context_count

shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count

shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_kr_count
shadow_us_count
shadow_sector_coverage_count

shadow_financial_sector_framework_failure_count
shadow_adr_security_basis_failure_count
shadow_cyclical_valuation_framework_failure_count

shadow_core_mutation_after_stance_count

shadow_runtime_timeout_count
shadow_runtime_capacity_failure_count
shadow_runtime_orphan_count
shadow_wrapper_retry_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

fic_fin_05_combined_diagnostic_classification
fic_fin_06_combined_diagnostic_classification
fic_fin_08_combined_diagnostic_classification

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything not measured:

```text
NOT_MEASURED
```

---

# 53. Fresh-real readiness remains blocked in M12AF

Always:

```text
fresh_real_proof_readiness = NOT_READY
```

at M12AF completion.

This is intentional.

M12AF gathers the missing existing-universe compatibility evidence
that the prior fictional gate prevented.

The next task may repair the exact remaining semantic/policy issue
with much better evidence.

---

# 54. Production readiness

Always:

```text
production_readiness = NOT_READY
```

No main merge.

No deployment.

No monitoring resume.

---

# 55. Final task principle

M12AE-R2 proved the important structural work:

```text
latest main integration is clean

two-stage architecture is structurally sound

Stage 2 cannot mutate Stage 1

final consumers remain compatible

financial hard semantics remain clean
```

The remaining fictional findings are:

```text
FIC-FIN-05:
adjacent HOLD/SELL primary boundary
with stable WAIT/REVIEW stances

FIC-FIN-06:
adjacent HOLD/BUY boundary
plus STRENGTHENED/UNCHANGED thesis-delta ambiguity
with stable WAIT/HOLDABLE stances

FIC-FIN-08:
stable core/new-buyer
but HOLDABLE/REVIEW holder ambiguity
```

It is no longer efficient to keep tuning these only against synthetic cases
while the existing monitored universe is available as a safe,
already-exposed compatibility cohort.

The correct M12AF flow is:

```text
freeze the current integrated two-stage implementation

→ do NOT change prompts or thresholds

→ freeze one reproducible local packet per active monitored stock

→ run same-packet monolithic control once

→ run same-packet two-stage once

→ compare all existing monitored names

→ identify whether core/delta/holder differences
   are synthetic-edge phenomena or real architecture effects

→ classify expected corrections vs regressions

→ choose the next bounded repair using both evidence sets
```

Do NOT:

```text
start fresh unseen proof

write shadow results into production

refresh providers

merge into main

change investment-logic versions

create warnings

change prompts before shadow

majority-vote outputs

return to Astra

resume monitoring
```

Use the existing monitored universe now
to decide what actually matters before making the next semantic change.
