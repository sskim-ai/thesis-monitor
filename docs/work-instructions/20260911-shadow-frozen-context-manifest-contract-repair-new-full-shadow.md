# Thesis Monitor — Shadow Frozen-Context Manifest Contract Repair + New Full Monitored Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260911-shadow-frozen-context-manifest-contract-repair-new-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260911-shadow-frozen-context-manifest-contract-repair-new-full-shadow-report.zip
```

Master-workflow phase:

```text
M12AL — Integrated-Main Shadow Harness Repair
         A. Freeze the successful M12AK formal fictional proof
         B. Repair shadow frozen-context manifest producer/verifier contract only
         C. Offline verify all 6 prepared shadow contexts
         D. Start a NEW full active-monitored shadow generation
         E. Complete monolithic vs two-stage same-packet comparison
         F. Produce the boundary / delta-materiality / holder policy handoff
```

M12AK successfully repaired the fictional aggregate finalizer
and completed a NEW formal whole fictional proof.

The fictional proof is now formally valid:

```text
Stage 1 calls = 6 / 6
Stage 2 calls = 6 / 6
total calls = 12 / 12

context hard gates = 12 / 12 PASS

Stage 1 rows = 24 / 24 PASS
Stage 2 rows = 24 / 24 PASS
final compositions = 24

aggregate finalization = PASS

business-delta capability violations = 0
business-delta direction violations = 0

direction-hint projection mismatch = 0
unsafe metric auto-direction = 0

objective financial semantic failures = 0
core mutation = 0

timeout = 0
orphan = 0
wrapper retry = 0
```

The monitored shadow was authorized,
all 22 local packets were available,
and all 6 frozen shadow contexts were prepared.

But the shadow stopped before any model call because:

```text
shadow program-state writer emitted:
contexts

shadow frozen-state verifier required:
frozen_contexts
```

This is a deterministic shadow-runtime manifest contract mismatch.

M12AL must fix ONLY that contract first.

Do not rerun the fictional model proof
unless the manifest repair unexpectedly changes any semantic/model-facing hash.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260911-fictional-finalization-context-batch-repair-new-whole-proof-full-shadow-report.zip
```

Verified SHA-256:

```text
3643125d30b6d661c9a7d50d9ca168808c3600bc5717c27a0cf12e7682260ee0
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
indexed payloads = 263
missing = 0
extra = 0
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

ZIP contains:

```text
263 indexed payloads
+ artifact-index.json
= 264 entries
```

Recompute independently.

---

# 2. M12AK formal fictional proof — authoritative and reusable

Formal M12AK fictional generation:

```text
generation_id =
20260911-m12ai-fictional-20260911T073155Z-858474603de1
```

Despite the inherited generation-name prefix,
M12AK explicitly records:

```text
formal_new_whole_proof = true
status = PASS
```

Formal result:

```text
aggregate finalization = PASS

DirectionalCoreBatch >4 use count = 0

24 unique final compositions

context-preserving finalization contract =
context-preserving-proof-finalization-v1
```

Do not rerun the 12-call fictional proof in M12AL
if all semantic/model-facing code hashes remain unchanged.

---

# 3. M12AK proof-harness repair — freeze successful

M12AK correctly separated:

```text
model/context schema
from
proof aggregation
```

The frozen model/context limit remains:

```text
DirectionalCoreBatch max candidates = 4
```

M12AK did NOT widen the limit.

Final aggregation uses already validated per-context rows
instead of wrapping all 8 subjects in a model-context schema.

Preserve this contract.

---

# 4. M12AK formal fictional diagnostic result

The formal fictional proof contains no hard semantic failures.

Decision-material diagnostics are intentionally non-blocking for shadow:

## FIC-FIN-01

```text
direction =
BUY ×3

business delta =
STRENGTHENED ×3

new buyer =
WAIT ×3

holder =
HOLDABLE ×3
```

## FIC-FIN-02

```text
direction =
HOLD ×3

business delta =
WEAKENED / UNRESOLVED / WEAKENED

new buyer =
WAIT ×3

holder =
REVIEW ×3
```

Interpretation:

```text
mixed positive/negative observed delta evidence
still has one materiality/ambiguity boundary.
```

The typed-direction evidence surface itself is now clean.

## FIC-FIN-05

```text
direction =
SELL / HOLD / SELL

business delta =
UNCHANGED ×3

new buyer =
AVOID / WAIT / WAIT

holder =
REVIEW ×3
```

Interpretation:

```text
primary HOLD/SELL threshold boundary remains.
new-buyer variation is coupled to the primary threshold.
```

## FIC-FIN-06

```text
direction =
HOLD ×3

business delta =
STRENGTHENED ×3

new buyer =
WAIT ×3

holder =
HOLDABLE ×3
```

The previous STRENGTHENED/UNCHANGED delta instability
is no longer present in this formal proof.

## FIC-FIN-08

```text
direction =
HOLD ×3

business delta =
UNCHANGED ×3

new buyer =
WAIT ×3

holder =
HOLDABLE / REVIEW / REVIEW
```

Interpretation:

```text
holder HOLDABLE/REVIEW boundary remains.
```

M12AL must NOT repair these policies.

The whole point of the full monitored shadow is to contextualize them
with existing real monitored names first.

---

# 5. Shadow gate had already passed

M12AK shadow model-call gate:

```text
status = PASS

active monitor count = 22

packet available count = 22

provider source fetches = 0

planned model calls = 18

model =
gpt-5.6-sol

reasoning =
xhigh

business-delta capability = PASS

fictional shadow gate = PASS

production side-effect firewall = PASS
```

No shadow model call had been made before the runtime verifier failed.

---

# 6. Exact M12AK shadow failure

M12AK shadow stop:

```text
status =
FAIL

failure_stage =
pre_model_shadow_runtime_validation

stop_reason =
SHADOW_FROZEN_CONTEXT_MANIFEST_MISSING

completed_model_calls =
0

planned_shadow_model_calls =
18

production_side_effects =
0

post_freeze_code_change =
false
```

Failure detail:

```text
"shadow program state records contexts
 while frozen-state verifier requires frozen_contexts"
```

This is not:

```text
a missing frozen-context file

a packet mismatch

a model schema failure

a financial semantic failure

a business-delta failure

a runtime transport failure
```

It is:

```text
MANIFEST PRODUCER / VERIFIER KEY CONTRACT MISMATCH.
```

---

# 7. Existing program state proves the frozen contexts exist

M12AK shadow program state contains:

```text
context_count = 6

contexts = [ ... 6 rows ... ]

status = FROZEN
```

Each context row already contains:

```text
context number

ticker membership

packet sha256 by ticker

business_delta_view_sha256

monolithic prompt path + sha256

monolithic schema path + sha256

Stage 1 prompt path + sha256

Stage 1 schema path + sha256

Stage 2 schema path + sha256
```

And the ZIP contains actual directories:

```text
shadow/frozen-contexts/context-01
...
shadow/frozen-contexts/context-06
```

Therefore the failure is not missing data.

---

# 8. Canonical manifest contract review

Before implementation select ONE canonical manifest contract.

Evaluate:

## Option A — canonical key `contexts`

Producer already emits:

```text
contexts
```

Verifier consumes:

```text
contexts
```

Pros:

```text
smallest current-state change
matches existing M12AK program-state
```

## Option B — canonical key `frozen_contexts`

Producer emits:

```text
frozen_contexts
```

Verifier consumes:

```text
frozen_contexts
```

Pros:

```text
more explicit semantic name
```

Cons:

```text
requires producer changes and may drift from other runner state shapes.
```

## Option C — internal normalized accessor with one emitted canonical key

Preferred robust pattern:

```text
writer emits one canonical key

reader normalizes legacy historical key if present

internal representation is one canonical list
```

No manifest may emit both keys with divergent content.

Select and document exactly one writer contract.

---

# 9. Preferred compatibility principle

Preferred:

```text
one canonical writer key
+
bounded read compatibility for historical local artifacts
```

Do NOT maintain two simultaneously writable fields.

If legacy compatibility accepts both:

```text
contexts
frozen_contexts
```

then require:

```text
if both exist:
their normalized content must be identical
else HARD FAIL.
```

No precedence guessing.

---

# 10. Manifest schema

Add an explicit internal manifest model or validator.

Suggested fields:

```text
contract_version

generation_id

status

context_count

contexts:
  - context_id
  - tickers
  - packet_sha256
  - business_delta_view_sha256
  - inputs:
      monolithic_prompt
      monolithic_schema
      stage1_prompt
      stage1_schema
      stage2_schema
```

Exact field names may follow current repository style.

Required invariants:

```text
len(contexts) = context_count

context IDs unique

every active ticker appears exactly once

no duplicate ticker across contexts

all listed input files exist

all file sha256 values match

all packet hashes match frozen packet files

no path escapes frozen shadow artifact root
```

---

# 11. Active universe / context coverage

For the M12AK reference universe:

```text
22 active monitored tickers
6 contexts
```

Coverage invariant:

```text
union(context ticker sets)
=
active monitored ticker set
```

And:

```text
intersection of any two context ticker sets =
empty
```

No missing or duplicate name.

Do not rely only on context count.

---

# 12. Context size invariant

Each shadow context:

```text
1 to 4 tickers
```

because the actual model context limit remains max 4.

Required:

```text
context_size_over_limit_count = 0
```

Do not change max 4.

---

# 13. Frozen input file verification

Before any model call,
for every context verify:

```text
monolithic prompt file exists + hash matches

monolithic schema exists + hash matches

Stage 1 prompt exists + hash matches

Stage 1 schema exists + hash matches

Stage 2 schema exists + hash matches
```

Also verify:

```text
packet hash by ticker

business-delta view hash

model target

reasoning effort

timeout/retry contract
```

If one mismatch:

```text
PRE_MODEL_HARD_STOP
```

---

# 14. Producer/verifier round-trip fixture

Add:

```text
write shadow manifest
→ serialize
→ read with frozen-state verifier
→ normalized object identical
```

Required:

```text
SHADOW-MANIFEST-01 = PASS
```

This exact round trip would have caught M12AK before a proof task.

---

# 15. Historical M12AK offline replay

After the manifest repair,
use the preserved M12AK shadow program state and frozen-context files
to test the verifier OFFLINE.

Required:

```text
6 / 6 contexts recognized

22 / 22 ticker coverage

all file hashes PASS

all packet hashes PASS

business-delta views PASS

model config PASS

production firewall PASS
```

Label:

```text
M12AK_HISTORICAL_SHADOW_PREFLIGHT_REPLAY
```

This does NOT reuse the stopped M12AK shadow generation for model proof.

It only proves the manifest repair.

---

# 16. No fictional model rerun if semantic hashes unchanged

Before shadow model calls compare semantic/model-facing hashes against M12AK.

At minimum:

```text
business_delta_evidence_service

direction_timing_ownership_service

directional_financial_context_service

two_stage_directional_service

monolithic prompt builder

Stage 1 prompt builder

Stage 2 prompt builder

model schemas

financial validators

business-delta validators

context-preserving finalizer
```

The M12AL code change should be limited to:

```text
shadow manifest writer / verifier / runner plumbing
```

If:

```text
model prompt semantic change count = 0

model schema semantic change count = 0

financial semantic change count = 0

business-delta semantic change count = 0

two-stage semantic change count = 0
```

then:

```text
reuse M12AK formal fictional proof
do not call fictional models.
```

If any semantic hash drifts:

```text
STOP
FICTIONAL_FORMAL_PROOF_INVALIDATED_BY_SHADOW_REPAIR
```

Do not silently proceed.

---

# 17. No reuse of stopped M12AK shadow generation

Although no model calls occurred,
M12AK shadow generation is formally stopped.

Therefore M12AL must create:

```text
NEW shadow generation ID

NEW shadow program-state manifest

NEW invocation IDs

NEW runtime namespace
```

It may reuse the same reproducible local packet contents
only after recomputing/confirming their hashes.

Do not "resume" the stopped generation.

---

# 18. Active monitored universe

At M12AL shadow-generation start,
re-enumerate active monitored stocks read-only.

Last verified M12AK reference:

```text
22 names

000660
003690
005490
005930
010120
012450
047810
086280
CORZ
CPNG
CRCL
GOOGL
HUT
IBM
MU
RXRX
SKHY
SNDK
TSLA
TSM
WRD
WULF
```

Use actual task-start active set.

Record:

```text
task_start_active_monitor_count

task_start_active_monitor_tickers

reference_added_tickers

reference_removed_tickers
```

Do not mutate registrations.

---

# 19. Same-packet shadow contract

For each active ticker:

```text
one reproducible local frozen packet
```

Exact same packet hash feeds:

```text
monolithic control

two-stage Stage 1

two-stage Stage 2 evidence/citation context
```

No external provider refresh.

Required:

```text
provider_source_fetches = 0
```

No stale/fresh mixing.

---

# 20. Business-delta view equality

For every ticker,
monolithic and Stage 1 must receive identical:

```text
BusinessDeltaEvidenceCapability

eligible_change_refs

baseline_context_refs

eligible_change_direction_hints

direction-unspecified eligible refs
```

Required:

```text
monolithic_stage1_delta_view_equality = PASS
```

No architecture comparison is valid otherwise.

---

# 21. Shadow model/runtime contract

Use:

```text
model = gpt-5.6-sol

reasoning = xhigh

up to 4 tickers/context

1800-second finite watchdog

wrapper auto-retry = 0

batch split = 0
```

No Astra.

No fallback.

No timeout increase.

---

# 22. Shadow topology

For:

```text
N = active monitored count
```

use:

```text
context_count = ceil(N / 4)

monolithic calls = context_count

Stage 1 calls = context_count

Stage 2 calls = context_count

total calls = 3 × context_count
```

At N=22:

```text
context_count = 6

monolithic = 6
Stage 1 = 6
Stage 2 = 6

total = 18
```

No repetitions.

No fresh unseen issuers.

---

# 23. Per-context execution order

For each frozen context:

```text
1. monolithic control

2. Stage 1 Core

3. Stage 2 Stance

4. deterministic final composition

5. context hard semantic audit
```

Then continue to the next context.

Do not compare only after losing context lineage.

Each final row must retain:

```text
context ID

ticker

packet hash

monolithic invocation ID

Stage 1 invocation ID

Stage 2 invocation ID

core snapshot sha256
```

---

# 24. Shadow stop policy

Hard stop only for:

```text
runtime hard failure

schema hard failure

frozen manifest/hash mismatch

packet mismatch

invalid evidence identity

objective financial semantic failure

business-delta capability violation

business-delta proven direction contradiction

price/technical/supply Core contamination

financial-sector framework misuse

ADR/security-basis violation

Stage 2 core mutation

production-side-effect attempt

aggregate finalization identity failure
```

Do NOT stop because monolithic and two-stage judgments differ.

Those differences are the purpose of the cohort.

---

# 25. Full shadow finalization

After all context calls succeed:

```text
validate per-context outputs

compose per ticker

aggregate 22 validated comparison rows
outside model-context schemas
```

Do NOT:

```text
wrap 22 candidates into DirectionalCoreBatch
```

Required:

```text
exactly one final comparison row per active ticker

no duplicate ticker

no missing ticker

no cross-context ticker

all packet hashes preserved
```

---

# 26. Shadow comparison taxonomy

Use:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

Delta sublabels:

```text
DELTA_UNCHANGED_ONLY

DELTA_AI_JUDGMENT

DELTA_MATERIALITY_DIFFERENCE

DELTA_EVIDENCE_SELECTION_DIFFERENCE

DELTA_DIRECTION_HINT_DIFFERENCE
```

Neither path is automatically ground truth.

---

# 27. Required review for every decision-material difference

For every:

```text
PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE
```

produce:

```text
ticker

packet hash

monolithic value

two-stage value

shared evidence refs

selected evidence differences

major Unknowns

relevant frozen contract

forensic classification
```

Resolve to one of:

```text
EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

No silent acceptance.

---

# 28. Existing monitored compatibility decision

At end classify:

```text
TWO_STAGE_COMPATIBILITY_CLEAN

TWO_STAGE_COMPATIBILITY_CLEAN_WITH_EXPECTED_CORRECTIONS

TWO_STAGE_COMPATIBILITY_HAS_BOUNDED_REGRESSIONS

TWO_STAGE_COMPATIBILITY_HAS_SYSTEMIC_REGRESSION

TWO_STAGE_COMPATIBILITY_INCOMPLETE
```

Report aggregate counts by:

```text
KR / US

sector/framework where available
```

No arbitrary percentage pass threshold.

---

# 29. Special financial-sector audit

For:

```text
003690
```

require:

```text
insurance-appropriate framework

no industrial net-debt/WC application

price confirmation/risk-reward remains outside Core

business delta follows capability gate
```

Any objective misuse is a hard failure.

---

# 30. Special ADR/security-basis audit

For:

```text
SKHY
```

require:

```text
ADR/security basis preserved

currency basis preserved

no denominator reconstruction

no provider multiple back-solving
```

Hard failure on basis mismatch.

---

# 31. Cyclical valuation audit

For memory/material cyclical names,
preserve sector-aware valuation logic.

Do not allow the architecture comparison to flatten into:

```text
low current PER = cheap
```

where stored frameworks use:

```text
mid-cycle
P/B
FCF
ROIC
SOTP
or other sector-aware methods.
```

---

# 32. Boundary/delta/holder combined diagnostics

Only after the full shadow completes,
compare real monitored behavior to the formal fictional issues.

## FIC-FIN-05 — Primary threshold / new-buyer coupling

Formal fictional:

```text
direction =
SELL / HOLD / SELL

new buyer =
AVOID / WAIT / WAIT

holder =
REVIEW ×3

business delta =
UNCHANGED ×3
```

Questions:

```text
Do real same-packet monitored names cross HOLD/SELL
between monolithic and two-stage?

When they do,
does new-buyer stance move with the primary direction
while holder remains stable?

Is the effect concentrated near 5.5/6.0?
```

## FIC-FIN-02 — Delta materiality

Formal fictional:

```text
direction =
HOLD ×3

business delta =
WEAKENED / UNRESOLVED / WEAKENED
```

Question:

```text
Among real AI_JUDGMENT delta names,
do monolithic and Stage 1 differ
when the same safe positive/negative observed evidence is present?
```

This is now a true materiality/ambiguity question,
not a direction-hint propagation defect.

## FIC-FIN-06 — Positive delta

Formal fictional:

```text
direction =
HOLD ×3

business delta =
STRENGTHENED ×3
```

Question:

```text
Do real operating-improvement cases produce stable
STRENGTHENED/UNCHANGED materiality judgments?
```

## FIC-FIN-08 — Holder boundary

Formal fictional:

```text
core =
HOLD ×3

holder =
HOLDABLE / REVIEW / REVIEW
```

Question:

```text
Do uncertainty-heavy or financial-sector real names
show similar HOLDABLE/REVIEW architecture sensitivity?
```

Do not repair any policy in M12AL.

---

# 33. No fresh unseen proof in M12AL

Even if the full monitored shadow is clean:

```text
fresh_real_proof_readiness = NOT_READY
```

The next step should be one bounded policy review using:

```text
formal fictional proof
+
complete monitored shadow.
```

Expected next scope when compatibility is clean:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

Do not start fresh unseen issuers in M12AL.

---

# 34. Main / production / remote policy

M12AL is LOCAL-ONLY.

Forbidden:

```text
git push

remote GitHub push

raw model artifact push

main merge

deployment

production monitoring resume
```

Required:

```text
remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Local commits and local report ZIP are allowed.

Do not ask the user for remote-push authorization.

---

# 35. Production side-effect firewall

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0
```

Schedules remain paused.

Observe only.

---

# 36. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12al-scope-freeze

04-integrated-main-lineage-freeze

05-m12ak-formal-fictional-proof-reuse-decision

06-m12ak-shadow-stop-reproduction

07-shadow-manifest-writer-audit

08-shadow-manifest-verifier-audit

09-contexts-vs-frozen-contexts-root-cause

10-canonical-shadow-manifest-contract-decision
```

---

# 37. Required manifest repair artifacts

Produce:

```text
11-shadow-frozen-context-manifest-contract

12-shadow-manifest-producer-contract

13-shadow-manifest-verifier-contract

14-shadow-manifest-normalization-contract

15-dual-key-conflict-negative-control

16-context-coverage-contract

17-context-input-hash-verification-contract

18-shadow-manifest-roundtrip-fixture
```

---

# 38. Required offline M12AK shadow replay artifacts

Produce:

```text
19-m12ak-shadow-program-state-inventory

20-m12ak-shadow-context-file-inventory

21-m12ak-shadow-manifest-offline-normalization

22-m12ak-shadow-context-coverage-replay

23-m12ak-shadow-input-hash-replay

24-m12ak-shadow-preflight-offline-replay

25-m12ak-shadow-replay-decision
```

Artifact 25 must say:

```text
HARNESS_CONTRACT_FIX_VALIDATED
NOT_A_NEW_SHADOW_MODEL_PROOF
```

---

# 39. Required semantic non-impact artifacts

Produce:

```text
26-model-prompt-semantic-hash-freeze

27-model-schema-semantic-hash-freeze

28-business-delta-semantic-hash-freeze

29-financial-semantic-hash-freeze

30-two-stage-semantic-hash-freeze

31-context-preserving-finalizer-freeze

32-fictional-formal-proof-reuse-decision
```

If artifact 32 is not:

```text
REUSE_AUTHORIZED
```

STOP.

Do not run shadow under uncertain semantic drift.

---

# 40. Required deterministic tests

Add at minimum:

```text
SHADOW-MANIFEST-01:
writer → verifier roundtrip PASS

SHADOW-MANIFEST-02:
6 contexts / 22 tickers coverage PASS

SHADOW-MANIFEST-03:
duplicate ticker across contexts → FAIL

SHADOW-MANIFEST-04:
missing ticker → FAIL

SHADOW-MANIFEST-05:
context >4 tickers → FAIL

SHADOW-MANIFEST-06:
input file missing → FAIL

SHADOW-MANIFEST-07:
input file hash mismatch → FAIL

SHADOW-MANIFEST-08:
packet hash mismatch → FAIL

SHADOW-MANIFEST-09:
both legacy/current keys present but differ → FAIL

SHADOW-MANIFEST-10:
legacy historical key normalized read-only → PASS
if backward compatibility is implemented
```

No model calls before all required fixtures pass.

---

# 41. Required test artifacts

Produce:

```text
33-focused-test-results

34-full-local-test-results

35-ruff-and-diff-results

36-hosted-ci-portability-observation

37-new-shadow-model-call-gate
```

Shadow gate requires:

```text
formal fictional proof reuse authorized

manifest contract PASS

historical M12AK offline preflight PASS

22/actual active ticker packet coverage PASS

semantic hashes unchanged

production firewall PASS

model = gpt-5.6-sol / xhigh
```

---

# 42. Required NEW shadow setup artifacts

Produce:

```text
38-shadow-generation-manifest

39-task-start-active-monitored-universe

40-shadow-packet-inventory

41-shadow-packet-hash-manifest

42-shadow-delta-capability-manifest

43-shadow-direction-hint-manifest

44-shadow-frozen-context-manifest

45-shadow-batching-manifest
```

Use a NEW generation ID.

Do not reuse the stopped M12AK generation ID.

---

# 43. Required shadow raw model artifacts

For each context preserve locally:

```text
monolithic:
prompt
schema
raw output
receipt
transport log
run document

Stage 1:
prompt
schema
raw output
receipt
transport log
run document

Stage 2:
prompt
schema
raw output
receipt
transport log
run document
```

Stage 2 run document includes:

```text
parent Stage 1 invocation ID

core snapshot sha256 by ticker
```

Do not push these artifacts remotely.

---

# 44. Required shadow analysis artifacts

Produce:

```text
46-shadow-context-hard-semantic-audit

47-shadow-final-composition-audit

48-shadow-aggregate-finalization-audit

49-shadow-per-ticker-comparison

50-shadow-business-delta-capability-audit

51-shadow-business-delta-direction-audit

52-shadow-core-direction-differences

53-shadow-business-delta-differences

54-shadow-new-buyer-differences

55-shadow-holder-differences

56-shadow-same-direction-calibration-differences

57-shadow-expected-contract-corrections

58-shadow-potential-architecture-regressions

59-shadow-unresolved-review-required

60-shadow-financial-sector-audit

61-shadow-adr-security-basis-audit

62-shadow-cyclical-valuation-audit

63-shadow-core-immutability-audit

64-shadow-runtime-audit

65-shadow-aggregate-summary

66-shadow-architecture-decision
```

---

# 45. Required combined diagnostic artifacts

Produce:

```text
67-fic-fin-05-vs-monitored-primary-boundary-analogs

68-fic-fin-02-vs-monitored-delta-materiality-analogs

69-fic-fin-06-vs-monitored-positive-delta-analogs

70-fic-fin-08-vs-monitored-holder-analogs

71-real-architecture-compatibility-lessons

72-combined-fictional-monitored-root-cause-summary

73-next-bounded-policy-decision
```

---

# 46. Required final completion artifacts

Produce:

```text
74-shadow-manifest-repair-success-decision

75-formal-fictional-proof-reuse-success-decision

76-full-shadow-completion-decision

77-existing-monitored-impact-summary

78-two-stage-shadow-compatibility-decision

79-fresh-real-proof-readiness-decision

80-final-main-merge-readiness-note

81-production-no-change

82-schedule-pause-observation

83-remote-push-prohibition-audit

84-master-workflow-update

85-program-completion
```

---

# 47. Shadow acceptance criteria

Require:

```text
all planned shadow contexts complete

all planned monolithic calls complete

all planned Stage 1 calls complete

all planned Stage 2 calls complete

one final comparison row per active ticker

aggregate finalization = PASS

timeout = 0

orphan = 0

wrapper retry = 0

packet mismatch = 0

invalid refs = 0

objective financial semantic failures = 0

business-delta capability violations = 0

business-delta direction contradictions = 0

price/technical/supply Core contamination = 0

financial-sector framework misuse = 0

ADR/security basis violation = 0

Stage 2 core mutation = 0

production side effects = 0
```

Decision-material monolithic/two-stage differences are measured,
not hard failures.

---

# 48. Compatibility decision criteria

If:

```text
potential architecture regression count = 0

unresolved review-required count = 0
```

classify:

```text
TWO_STAGE_COMPATIBILITY_CLEAN
```

or:

```text
TWO_STAGE_COMPATIBILITY_CLEAN_WITH_EXPECTED_CORRECTIONS
```

depending on observed expected corrections.

If bounded regressions exist:

```text
TWO_STAGE_COMPATIBILITY_HAS_BOUNDED_REGRESSIONS
```

If broad/systematic:

```text
TWO_STAGE_COMPATIBILITY_HAS_SYSTEMIC_REGRESSION
```

No percentage threshold.

---

# 49. Next-scope logic

## A. Shadow completes cleanly

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

This policy review must use:
- M12AK formal fictional proof
- M12AL complete monitored shadow

## B. Primary-direction architecture regressions appear

```text
next_scope =
TWO_STAGE_CORE_STABILITY_COMPATIBILITY_REVIEW
```

## C. Business-delta differences dominate

```text
next_scope =
BUSINESS_THESIS_DELTA_MATERIALITY_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

## D. Stance differences dominate

```text
next_scope =
FUNDAMENTAL_STANCE_COMPATIBILITY_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

## E. Shadow manifest/finalizer fails again

```text
next_scope =
SHADOW_PROOF_HARNESS_ARCHITECTURE_REVIEW
```

No fresh unseen proof.

---

# 50. Fresh-real / main / production readiness

At M12AL completion:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY
```

Even if shadow compatibility is clean.

Reason:

```text
one bounded boundary / delta-materiality / holder policy review
must now use the completed fictional + monitored evidence.
```

No fresh unseen calls in M12AL.

---

# 51. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch

latest_result_zip_sha256
latest_result_integrity

m12ak_formal_fictional_reuse_status
m12ak_formal_fictional_generation_id

shadow_manifest_root_cause
shadow_manifest_contract_version
shadow_manifest_canonical_key
shadow_manifest_legacy_key_supported

shadow_manifest_roundtrip_status
shadow_manifest_dual_key_conflict_test_status

m12ak_historical_shadow_preflight_replay_status
m12ak_historical_context_count
m12ak_historical_ticker_coverage_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
business_delta_semantic_change_count
financial_semantic_change_count
two_stage_semantic_change_count

directional_core_batch_max_items
directional_core_batch_max_items_changed

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status

shadow_business_delta_capability_violation_count
shadow_business_delta_direction_violation_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_financial_sector_framework_failure_count
shadow_adr_security_basis_failure_count
shadow_cyclical_valuation_framework_failure_count

shadow_core_mutation_after_stance_count

shadow_runtime_timeout_count
shadow_runtime_orphan_count
shadow_wrapper_retry_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything not measured:

```text
NOT_MEASURED
```

---

# 52. Artifact integrity

Freeze all local artifacts before final artifact index.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report result ZIP SHA-256.

Do not push the report/raw artifacts to GitHub.

---

# 53. Final task principle

M12AK successfully solved the fictional proof-harness defect
and produced the formal whole fictional proof
we had been working toward.

The new blocker is smaller and earlier:

```text
the shadow writer already created
6 frozen contexts and their files,

but wrote the manifest list under:
contexts

while the verifier looked for:
frozen_contexts.
```

Therefore the correct next flow is:

```text
freeze M12AK formal fictional proof

→ repair one shadow manifest producer/verifier contract

→ prove the repair offline against M12AK's existing 6 frozen contexts

→ prove semantic/model-facing hashes did not change

→ DO NOT rerun the 12-call fictional proof

→ create a new shadow generation

→ run all existing active monitored names:
   monolithic + Stage 1 + Stage 2

→ complete same-packet architecture comparisons

→ finally use real monitored evidence
   to review the remaining:
   primary boundary,
   business-delta materiality,
   holder boundary.
```

Do NOT:

```text
change model prompts

change schemas

change business-delta semantics

change financial semantics

rerun fictional models unnecessarily

resume the stopped shadow generation

accept both manifest keys with silent precedence

widen the max-4 context contract

start fresh unseen proof

push raw artifacts to GitHub

merge into main

deploy

return to Astra

resume production monitoring
```

Fix the shadow harness contract,
then finally measure the existing monitored universe.
