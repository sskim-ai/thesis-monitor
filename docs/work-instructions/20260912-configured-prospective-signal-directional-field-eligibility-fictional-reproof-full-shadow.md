# Thesis Monitor — Configured Prospective Signal / Directional Field Eligibility + Full Fictional Reproof + Full Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260912-configured-prospective-signal-directional-field-eligibility-fictional-reproof-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260912-configured-prospective-signal-directional-field-eligibility-fictional-reproof-full-shadow-report.zip
```

Master-workflow phase:

```text
M12AT — Integrated-Main Configured-Signal Field Ownership
         A. Freeze successful M12AS UNCHANGED claim-scope repair
         B. Reproduce exact 005490 prospective weaken-signal / sell-driver conflict
         C. Freeze the semantic contract of current directional-driver fields
         D. Separate configured future signals from currently fulfilled directional evidence
         E. Add pre-model field-use eligibility and post-model consistency
         F. Start a NEW full 8 × 3 two-stage fictional proof if model-facing field eligibility changes
         G. If hard gates pass, run NEW full active-monitored same-packet shadow
         H. Hand off the complete clean cohort to boundary / delta / holder policy review
```

M12AS successfully repaired the intended business-delta UNCHANGED
configured-condition negation false reject.

Exact SNDK replay:

```text
PASS
```

Configured-condition / no-observed-change fixtures:

```text
false accept = 0
false reject = 0
```

M12AQ formal fictional proof was re-audited and reused:

```text
formal_fictional_reuse_status = REUSE_AUTHORIZED
```

The NEW monitored shadow then stopped after only:

```text
2 / 18 planned model calls
```

at:

```text
context-01 / Stage 1 / 005490
```

with:

```text
net_debt_claim_without_complete_net_debt_evidence
```

The exact failing field:

```text
sell_drivers[0].text
```

Text:

```text
"현금창출 약화와 순부채 증가 또는 자본효율 하락은
 투자회수 논리를 훼손하는 조건이다."
```

Bound evidence refs are configured thesis weaken signals:

```text
stock.thesis.weaken_signals:
"FCF 감소와 순부채 증가가 동반"

stock.thesis.weaken_signals:
"CAPEX 증가에도 ROIC가 하락"
```

The SAME configured refs used in:

```text
business_reevaluation_down
```

are correctly classified as:

```text
FUTURE_REEVALUATION_CONDITION
```

and PASS.

The current financial-claim classifier instead applies:

```text
sell_drivers
→ ALWAYS_CURRENT_DIRECTION_PATH
```

before configured-prospective evidence semantics can control the role.

This creates:

```text
CURRENT_DIRECTIONAL_BASIS
```

and triggers net-debt completeness
for a condition that the model explicitly described as prospective.

M12AT must resolve the FIELD-OWNERSHIP contract,
not merely add a phrase exception.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260912-business-delta-unchanged-configured-condition-negation-scope-full-shadow-report.zip
```

Verified SHA-256:

```text
abefe909f1b6e665d5a8bae6085f518165b75528959711179af0eada68ccfa1d
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent ZIP verification:

```text
entries = 218

artifact-index / payload integrity:
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute independently.

---

# 2. M12AS status

Program status:

```text
BLOCKED_AFTER_2_OF_18_SHADOW_CALLS
```

Shadow generation:

```text
20260911-m12ai-shadow-20260912T095409Z-3da60ab83df5
```

Completed:

```text
monolithic context-01 = PASS
Stage 1 context-01 = HARD FAIL

Stage 2 calls = 0
remaining contexts = NOT RUN

timeout = 0
orphan = 0
wrapper retry = 0
```

Failure:

```text
ticker = 005490

error =
net_debt_claim_without_complete_net_debt_evidence

field =
sell_drivers[0].text
```

Do not resume or stitch this stopped generation.

---

# 3. M12AS intended repair — freeze successful

Preserve:

```text
business-delta-unchanged-claim-scope-v1

configured condition reference

configured condition fulfilled

configured condition not fulfilled

explicit no observed change

true current thesis-change assertion
```

Exact SNDK replay:

```text
PASS
```

Formal fictional proof reuse:

```text
REUSE_AUTHORIZED
```

Do not reopen UNCHANGED claim-polarity semantics.

---

# 4. M12AR FCF claim-scope — freeze

Preserve:

```text
claim-local / evidence-local FCF validation

no candidate-global PPE-proxy contamination

Stage-2 confirmation condition ref binding

Stage-2 invalidation condition ref binding

current unsupported FCF safety

not-FCF disclaimer safety
```

IBM historical failure boundary is closed.

---

# 5. M12AQ financial-sector exclusion — freeze

Preserve:

```text
exclusion connective scope

coordinated forbidden-framework object scope

replacement-clause scope

true insurance industrial-framework misuse hard failure
```

No financial-sector changes.

---

# 6. M12AP expectation independence — freeze

Preserve:

```text
MarketExpectationEvidenceView

independent support vs conditional context-only

context-only expectation excluded from material anchor

pre/post expectation-view identity
```

No changes.

---

# 7. M12AO business-delta convergence — freeze

Preserve:

```text
BusinessDeltaEvidenceView
as single source of evidence semantics

no legacy raw-text eligibility re-derivation

no legacy raw-text direction re-derivation

pre/post delta-view identity
```

No changes.

---

# 8. Exact 005490 Stage-1 candidate

The stopped Stage-1 result:

```text
overall_direction =
HOLD

directional_balance =
5.5 : 4.5

hold_lean =
BUY_LEAN

business_thesis_change =
UNCHANGED

directional_confidence =
LOW
```

Material directional anchor:

```text
recent operating profit / issuer-level current operating evidence
```

The failing sell driver:

```text
classification =
STRUCTURAL_RISK

text =
"현금창출 약화와 순부채 증가 또는 자본효율 하락은
 투자회수 논리를 훼손하는 조건이다."

refs =
configured weaken-signal refs
```

This sell driver was NOT part of:

```text
material_directional_anchor_basis
```

and the candidate did not say:

```text
net debt has increased now.
```

---

# 9. Exact configured source semantics

The failing refs originate from:

```text
stock.thesis.weaken_signals
```

including:

```text
"FCF 감소와 순부채 증가가 동반"

"CAPEX 증가에도 ROIC가 하락"
```

These are configured conditions:

```text
IF observed later,
the thesis weakens.
```

They are not evidence that:

```text
FCF already decreased

net debt already increased

ROIC already fell despite CAPEX growth.
```

Configured signal ≠ fulfilled signal.

This lifecycle distinction is already frozen elsewhere.

---

# 10. Existing financial-claim precedence defect

Current role logic effectively does:

```text
if explicit conditional syntax:
    classify future/configured

elif numeric:
    current numeric

elif field in ALWAYS_CURRENT_DIRECTION_PATHS:
    CURRENT_DIRECTIONAL_BASIS
```

and:

```text
sell_drivers
```

belongs to:

```text
ALWAYS_CURRENT_DIRECTION_PATHS.
```

Therefore a configured prospective condition
without an explicit `if/when/하면` marker
becomes current solely because of field path.

The phrase:

```text
"... 훼손하는 조건이다"
```

is prospective/conditional in meaning,
but not recognized before field precedence.

---

# 11. Do NOT solve this as a pure net-debt exception

Forbidden repair:

```text
if sell_driver contains "조건"
then net-debt completeness is not required.
```

Why this is insufficient:

```text
sell_drivers may be semantically current-driver fields.

If so, a future configured condition should not be there at all.

Simply exempting current-evidence requirements
would blur field ownership
and could let unfulfilled risks influence current direction.
```

M12AT must first freeze the `sell_drivers` field contract.

---

# 12. Field-contract architecture review

Before implementation, classify `sell_drivers` as exactly one:

```text
A. CURRENT_DIRECTIONAL_DRIVER_ONLY

B. CURRENT_OR_PROSPECTIVE_RISK_DRIVER

C. MIXED_FIELD_WITH_EXPLICIT_ROLE_METADATA
```

Use:

```text
existing schema semantics

prompt contract

historical valid outputs

direction/timing ownership rules

material anchor rules

renderer/downstream usage
```

Do not choose based only on the current 005490 candidate.

Freeze the result in an artifact before code changes.

---

# 13. Preferred interpretation to test

The preferred safety candidate is:

```text
CURRENT_DIRECTIONAL_DRIVER_ONLY
```

because:

```text
field name = sell_drivers

current financial validator already treats it as always-current

actual BUY/SELL directional logic consumes sell-driver evidence

future weaken/invalidation conditions already have dedicated fields:
business_reevaluation_down
risk_context
stored thesis weaken signals
```

If repository audit confirms this,
M12AT should structurally prevent configured prospective refs
from entering:

```text
buy_drivers
sell_drivers
dominant_evidence
core_investment_judgment
material_directional_anchor_basis
```

unless a separate current fulfilled fact supports the same claim.

---

# 14. Alternative mixed-field interpretation

If repository audit proves that `sell_drivers`
is intentionally a mixed risk inventory field,
then M12AT may choose:

```text
CURRENT_OR_PROSPECTIVE_RISK_DRIVER
```

ONLY with explicit role metadata / deterministic evidence role.

Then:

```text
prospective configured sell driver
```

must be unable to:

```text
support material_directional_anchor_basis

act as independent dominant_evidence

prove current SELL

trigger current financial completeness
```

This alternative requires clear downstream role separation.

Do not allow implicit mixed semantics.

---

# 15. Do not leave the field contract implicit

Forbidden:

```text
keep sell_drivers mixed,
infer current/future only from prose regex,
and let downstream code guess.
```

This would repeat the same class of bug.

Whichever architecture is chosen,
field/evidence role must be explicit and auditable.

---

# 16. Add ConfiguredSignalEvidenceView

Preferred internal source-of-truth:

```text
ConfiguredSignalEvidenceView
```

or equivalent.

For each configured thesis signal ref,
track:

```text
source role:
STRENGTHEN_SIGNAL
WEAKEN_SIGNAL
INVALIDATION_SIGNAL

fulfillment state:
CONFIGURED_ONLY
FULFILLED_BY_CURRENT_EVIDENCE
UNKNOWN

current_directional_driver_eligible:
true/false

future_reevaluation_eligible:
true/false

risk_context_eligible:
true/false

material_anchor_eligible:
true/false

dominant_evidence_eligible:
true/false
```

Do not expose as user-facing schema.

---

# 17. Configured-only default

For source lineage such as:

```text
stock.thesis.strengthen_signals

stock.thesis.weaken_signals

stock.thesis.invalidation_signals
```

default:

```text
CONFIGURED_ONLY
```

unless supplied current evidence independently establishes fulfillment.

Configured-only signals:

```text
are not current facts

do not prove business delta

do not prove current BUY/SELL

do not prove current net debt / cash-flow deterioration.
```

---

# 18. Current fulfillment must require real evidence

A configured weaken signal can become:

```text
FULFILLED_BY_CURRENT_EVIDENCE
```

only when current packet evidence establishes the condition.

Example:

```text
configured:
"FCF 감소와 순부채 증가가 동반"

current typed evidence:
safe comparable FCF/FCF-equivalent evidence
AND
safe comparable complete net-debt evidence

→ may mark fulfilled
```

Do not infer fulfillment merely because:

```text
the signal exists
the model repeats it
a risk context mentions it
```

No model self-certification.

---

# 19. Field-use eligibility — preferred current-driver contract

If `sell_drivers` is frozen as current-only:

Configured-only refs must be forbidden from:

```text
buy_drivers
sell_drivers
dominant_evidence
material_directional_anchor_basis
core_investment_judgment
```

as current supporting refs.

They remain allowed in:

```text
business_reevaluation_up/down

risk_context
when clearly prospective

unknown / monitoring-condition prose
where schema semantics permit.
```

The Stage-1 model should not be offered configured-only refs
as legal refs for current driver fields.

---

# 20. Dynamic per-field ref constraints

Preferred structural enforcement:

```text
per-ticker Stage-1 schema
```

restricts:

```text
buy_drivers[*].evidence_refs
sell_drivers[*].evidence_refs
dominant_evidence.evidence_refs
material_directional_anchor_basis
core_investment_judgment.evidence_refs
```

to refs whose field-use eligibility permits current directional use.

Do not post-hoc rewrite model output.

The invalid selection should be structurally impossible.

---

# 21. Future fields retain configured refs

Do not remove configured thesis signals from the model entirely.

Keep them available to:

```text
business_reevaluation_up

business_reevaluation_down

risk_context / risk inventory
where prospective use is legal
```

The model still needs them to explain:

```text
what would strengthen
what would weaken
what would invalidate
what to monitor next.
```

This is ownership routing,
not evidence deletion.

---

# 22. 005490 expected behavior after current-only field contract

For the exact frozen 005490 packet:

```text
E01 / E17 configured weaken refs
```

must remain available for:

```text
business_reevaluation_down
```

but unavailable as current `sell_drivers` refs
unless fulfillment is separately established.

The model may still output current sell drivers from:

```text
actual current issuer risk evidence
actual valuation evidence
actual current structural facts
```

if such refs exist.

Do not force overall direction or balance.

---

# 23. If sell_drivers is intentionally mixed

If Option B/C is proven instead,
then every sell-driver row must carry an internal role such as:

```text
CURRENT_DIRECTIONAL_DRIVER

PROSPECTIVE_CONFIGURED_RISK
```

and downstream current-direction / financial-completeness validators
must treat them differently.

Required:

```text
prospective configured risk
cannot enter material anchor

cannot prove current business delta

cannot prove fulfilled current financial condition.
```

This route changes schema/ownership more broadly
and should be selected only if repository audit clearly requires it.

---

# 24. Exact M12AS historical candidate offline replay

After the architecture is frozen,
replay the exact stopped 005490 candidate.

Two possible expected results:

## Current-only contract selected

The historical candidate remains:

```text
INVALID
```

because configured-only refs were used in current `sell_drivers`.

Preferred generic error:

```text
configured_future_signal_used_as_current_directional_driver
```

NOT:

```text
net_debt_claim_without_complete_net_debt_evidence
```

The future `business_reevaluation_down` rows remain PASS.

## Mixed-role contract selected

The historical sell driver may PASS only if it is explicitly classified:

```text
PROSPECTIVE_CONFIGURED_RISK
```

and does not count as current directional evidence.

Do not silently reinterpret the existing row without role proof.

---

# 25. Financial completeness must consume field-use role

Net-debt completeness should ask:

```text
Is this claim currently asserting/applying net debt
as current business/directional evidence?
```

not:

```text
Does any current-direction-named field mention net debt?
```

But current-only field misuse remains a separate hard validation.

Separate errors:

```text
FIELD_OWNERSHIP_VIOLATION

CURRENT_FINANCIAL_EVIDENCE_COMPLETENESS_VIOLATION
```

Do not conflate them.

---

# 26. Current net-debt hard controls remain

Still FAIL:

```text
"현재 순부채가 증가했다."
without complete evidence

"순부채 부담이 SELL 근거다."
without complete evidence

configured condition ref marked fulfilled
without supporting complete current evidence

sell driver uses current net-debt assertion
with incomplete debt/cash basis.
```

No weakening.

---

# 27. Configured prospective conditions remain safe in future fields

PASS:

```text
business_reevaluation_down:
"현금창출 감소와 순부채 증가가 동반되면 하향 재평가한다."

risk_context:
"현금창출 감소와 순부채 증가의 동반 발생이 주요 위험이다."

configured weaken condition:
"FCF 감소와 순부채 증가가 동반"
```

provided:

```text
no current fulfillment assertion is made.
```

---

# 28. Do not let configured conditions affect business delta

Preserve M12AI/M12AO:

```text
configured strengthen/weaken/invalidation refs
do not establish business_thesis_change.
```

No change.

A current fulfilled event must separately qualify
through BusinessDeltaEvidenceView.

---

# 29. Do not let configured conditions become material corroboration

Configured-only signals:

```text
do not count as an independent issuer-level fact

do not move 5.5 → 6.0

do not provide a second corroborating axis

do not justify confidence upgrades.
```

They are a monitoring/risk contract until fulfilled.

No fixed score rule.

---

# 30. Model-facing change implications

If current-driver ref eligibility is added to the Stage-1 schema,
this is a model-facing semantic change.

Then:

```text
M12AQ formal fictional proof CANNOT be reused.
```

A NEW full fictional 8 × 3 proof is mandatory.

Preferred M12AT should assume:

```text
new fictional proof required
```

if pre-model field-use eligibility is implemented.

Do not preserve proof reuse at the expense of correct field ownership.

---

# 31. Minimal prompt change

Prefer:

```text
no new long prose prompt rule.
```

The prompt already separates:

```text
current directional evidence
vs
business reevaluation conditions.
```

The structural field-ref eligibility should do the heavy lifting.

If one concise generic line is needed:

```text
configured strengthen/weaken/invalidation signals are future conditions
until current evidence establishes fulfillment;
use them in reevaluation/risk fields, not current directional-driver fields.
```

Do not add ticker-specific examples.

---

# 32. Model schema contract

Do not change:

```text
external final Directional schema.
```

Per-call allowed-ref enums may become dynamic.

Required:

```text
final_user_facing_schema_change_count = 0
```

No persisted-schema migration.

---

# 33. Monolithic / Stage-1 equality

For same-packet shadow comparison,
monolithic and Stage 1 must receive identical
configured-signal field-use eligibility.

Required:

```text
monolithic_stage1_configured_signal_view_equality = PASS
```

Do not constrain only Stage 1.

---

# 34. Stage-2 stance contract

Stage 2 may use configured business conditions in:

```text
confirmation_business_condition

business_invalidation_condition
```

when those conditions are appropriate to stance semantics.

Do not accidentally remove configured signals from Stage 2.

But configured conditions cannot be presented as:

```text
currently fulfilled
```

without supporting current evidence.

Preserve current stance safety.

---

# 35. Exact 005490 field-use audit

Produce a per-ref table for:

```text
E01
E17
E09
E25
```

or canonical equivalents.

For each:

```text
source lineage

configured signal kind

fulfillment state

allowed current fields

allowed future fields

actual selected fields in historical candidate

validation result
```

This must explain the failure structurally.

---

# 36. Configured-signal fixtures

At minimum:

## CFG-FIELD-P01

```text
weaken signal only

used in business_reevaluation_down
→ PASS
```

## CFG-FIELD-P02

```text
invalidation signal only

used in holder/business invalidation condition
→ PASS
```

## CFG-FIELD-P03

```text
weaken signal

used in risk_context as prospective risk
→ PASS
```

## CFG-FIELD-N01

```text
configured-only weaken signal

used as current sell driver
under CURRENT_DIRECTIONAL_DRIVER_ONLY contract
→ FAIL
```

## CFG-FIELD-N02

```text
configured-only strengthen signal
used as current buy driver
→ FAIL
```

## CFG-FIELD-N03

```text
configured-only signal
used in material_directional_anchor_basis
→ FAIL
```

## CFG-FIELD-N04

```text
configured-only signal
used as dominant independent evidence
→ FAIL
```

## CFG-FIELD-P04

```text
configured signal + separate safe current evidence proves fulfillment

fulfilled current evidence ref
used as current driver

configured signal may be cited as baseline condition context
→ PASS
```

---

# 37. Fulfillment fixtures

At minimum:

```text
configured:
net debt increase

current:
complete net debt comparable increase
→ fulfillment may be established
```

Negative:

```text
configured:
net debt increase

current:
partial debt only
→ fulfillment NOT established
```

Negative:

```text
configured:
FCF decrease

current:
OCF-PPE proxy only
→ do NOT claim management-defined FCF decrease
```

Preserve FCF safety.

---

# 38. M12AS intended-repair regressions

Re-run:

```text
exact SNDK UNCHANGED replay

configured-condition nonfulfillment positive fixtures

true current thesis-change negative fixtures

003690 absolute-state-to-delta regression

FIC-FIN-05 UNCHANGED_ONLY regression
```

All must PASS.

---

# 39. Freeze all completed contracts

Re-run:

```text
M12AR FCF claim scope

M12AQ financial-sector exclusion

M12AP expectation independence

M12AO business-delta convergence

M12AN PPE-proxy label + FCF safety

M12AM Stage-2 Korean lexical safety

M12AG timing ownership

M12AH financial temporal scope

QTD/YTD

WC grounding

debt completeness

ADR/security basis

two-stage core immutability
```

No regressions.

---

# 40. Architecture decision artifact before model calls

Produce exactly one final decision:

```text
CURRENT_DIRECTIONAL_DRIVER_ONLY_WITH_CONFIGURED_REF_FENCING

or

MIXED_SELL_DRIVER_WITH_EXPLICIT_PROSPECTIVE_ROLE
```

If neither can be implemented safely without broad schema migration:

```text
STOP
CONFIGURED_SIGNAL_DIRECTIONAL_FIELD_ARCHITECTURE_REVIEW_REQUIRED
```

No model calls.

---

# 41. Preferred implementation acceptance

If current-only architecture selected:

Require:

```text
configured-only ref selectable in reevaluation fields = true

configured-only ref selectable in risk context = true

configured-only ref selectable in current buy/sell drivers = false

configured-only ref selectable as material anchor = false

configured-only ref selectable as dominant current evidence = false

configured-only ref selectable in current core judgment = false
unless the prose is explicitly baseline/future context
and the field contract allows that use
```

Keep scope minimal.

---

# 42. NEW full fictional proof

Any pre-model field-ref eligibility change
invalidates prior formal proof reuse.

Run:

```text
NEW generation ID

gpt-5.6-sol / xhigh

8 subjects

2 contexts

4 subjects/context

3 repetitions

Stage 1 calls = 6

Stage 2 calls = 6

total = 12

final outputs = 24
```

No judge calls.

Do not reuse M12AQ outputs as formal M12AT proof.

---

# 43. Fictional hard-stop policy

Hard stop only for:

```text
runtime/schema hard failure

invalid evidence identity

configured-signal field-ownership violation

false fulfillment assertion

financial hard semantic failure

PPE-proxy FCF violation

business-delta capability / direction violation

expectation anchor-eligibility violation

financial-sector scope violation

price/technical/supply Core contamination

Stage-2 actual contamination

core mutation

aggregate finalization failure
```

Do NOT stop for valid decision-material variance.

---

# 44. Formal fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

configured-only current-driver violations = 0

configured-signal false fulfillment count = 0

current net-debt false accept count = 0

future configured financial false reject count = 0

business-delta semantic projection mismatch = 0

expectation anchor violation = 0

financial-sector false reject = 0

PPE-proxy FCF violation = 0

Stage-2 lexical false positive = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material variance does not block shadow.

---

# 45. Shadow authorization

If fictional hard gates pass:

```text
run a NEW full active-monitored same-packet shadow.
```

Do NOT require perfect fictional stability.

Existing monitored names are exposed compatibility subjects.

---

# 46. Active monitored universe

Re-enumerate active monitored stocks read-only.

Last verified reference:

```text
22 names
```

Use actual current active set.

No monitoring registration / stop changes.

---

# 47. Same-packet contract

For every active ticker:

```text
one reproducible local packet

same packet hash for:
monolithic
Stage 1
Stage 2 evidence context
```

No provider refresh.

Required:

```text
provider_source_fetches = 0
```

---

# 48. Shadow configured-signal view equality

For every ticker:

```text
monolithic ConfiguredSignalEvidenceView
=
Stage 1 ConfiguredSignalEvidenceView
```

Required:

```text
source roles
fulfillment states
current-driver eligibility
future-field eligibility
material-anchor eligibility
```

must match.

No architecture comparison is valid otherwise.

---

# 49. Full shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic = context_count

Stage 1 = context_count

Stage 2 = context_count

total = 3 × context_count
```

At N=22:

```text
18 calls
```

No repetitions.

No fresh unseen issuers.

---

# 50. Shadow hard-stop policy

Hard stop only for:

```text
runtime/schema failure

manifest/hash mismatch

packet mismatch

invalid evidence identity

configured-signal field-ownership violation

false configured-signal fulfillment

objective financial semantic failure

PPE-proxy-as-FCF violation

business-delta capability/direction violation

expectation anchor-eligibility violation

financial-sector scope violation

price/technical/supply Core contamination

Stage-2 actual contamination

ADR/security-basis violation

core mutation

production side effect

aggregate finalization failure
```

Do not stop on architecture decision differences.

Collect the full cohort.

---

# 51. Shadow configured-signal audit

For every candidate using configured thesis signals,
report:

```text
ticker

signal ref

signal source role

fulfillment state

field selected in

field-use eligibility

current/future claim role

validation result
```

This provides direct evidence that the new ownership contract is not over-broad.

---

# 52. Shadow comparison taxonomy

Use existing compatibility taxonomy:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

Add diagnostic labels:

```text
CONFIGURED_SIGNAL_CURRENT_DRIVER_CORRECTION

CONFIGURED_SIGNAL_FULFILLMENT_DIFFERENCE

CONFIGURED_SIGNAL_FIELD_USE_DIFFERENCE
```

Neither path is automatically ground truth.

---

# 53. Full compatibility goal

M12AT should finally produce:

```text
one final comparison row per active monitored ticker

aggregate finalization PASS

all decision-material differences reviewed.
```

Do not repair boundary / holder / delta materiality mid-generation.

Complete the cohort.

---

# 54. Policy handoff after complete shadow

If the full shadow completes without systematic architecture regression:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

Use:

```text
NEW M12AT formal fictional proof
+
M12AT complete monitored shadow
```

Focus:

```text
1. adjacent primary HOLD ↔ BUY/SELL thresholds

2. FIC-FIN-02 / FIC-FIN-06 delta materiality

3. FIC-FIN-08 holder HOLDABLE ↔ REVIEW

4. new-buyer stance differences with stable core

5. which prior shadow differences were actually caused by
   configured-signal field misuse
```

---

# 55. Local-only / production firewall

M12AT is LOCAL-ONLY.

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Do not ask for GitHub push authorization.

Monitoring schedules remain paused.

---

# 56. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12at-scope-freeze

04-integrated-main-lineage-freeze

05-m12as-005490-failure-reproduction

06-sell-driver-field-contract-audit

07-configured-signal-source-lineage-forensic

08-financial-claim-precedence-forensic

09-configured-signal-field-ownership-options

10-configured-signal-field-ownership-decision
```

---

# 57. Required configured-signal artifacts

Produce:

```text
11-configured-signal-evidence-view-contract

12-configured-only-vs-fulfilled-contract

13-current-directional-field-eligibility-contract

14-future-reevaluation-field-eligibility-contract

15-risk-context-configured-signal-contract

16-material-anchor-configured-signal-contract

17-dominant-evidence-configured-signal-contract

18-configured-signal-fulfillment-evidence-contract

19-monolithic-stage1-configured-view-equality-contract
```

---

# 58. Required exact replay / fixture artifacts

Produce:

```text
20-m12as-005490-exact-field-use-audit

21-m12as-005490-historical-candidate-replay

22-configured-signal-field-positive-fixtures

23-configured-signal-field-negative-fixtures

24-configured-signal-fulfillment-positive-fixtures

25-configured-signal-false-fulfillment-negative-fixtures

26-net-debt-current-claim-regressions

27-future-net-debt-condition-regressions
```

---

# 59. Required frozen-regression artifacts

Produce:

```text
28-m12as-unchanged-claim-scope-freeze

29-m12ar-fcf-claim-scope-freeze

30-m12aq-financial-sector-scope-freeze

31-m12ap-expectation-independence-freeze

32-m12ao-business-delta-convergence-freeze

33-m12an-ppe-proxy-label-freeze

34-m12am-stage2-lexical-freeze

35-monitoring-transition-ownership-freeze

36-financial-temporal-scope-freeze

37-qtd-ytd-wc-debt-safety-freeze

38-adr-security-basis-freeze

39-two-stage-ownership-freeze

40-price-timing-renderer-no-change
```

---

# 60. Required model-facing impact artifacts

Produce:

```text
41-model-prompt-semantic-diff

42-model-schema-semantic-diff

43-configured-signal-view-model-surface-diff

44-final-user-schema-no-change-proof

45-fictional-formal-reproof-decision
```

If current-driver ref fencing changes model-facing field eligibility:

```text
artifact 45 = NEW_FORMAL_PROOF_REQUIRED
```

Do not reuse M12AQ formal proof.

---

# 61. Required deterministic test gate

Produce:

```text
46-focused-test-results

47-full-local-test-results

48-ruff-and-diff-results

49-hosted-ci-portability-observation

50-new-fictional-model-call-gate
```

No model calls before artifact 50 PASS.

---

# 62. Required NEW fictional artifacts

If reproof required, produce:

```text
51-fictional-generation-manifest

52-fictional-configured-signal-view-manifest

53-fictional-delta-view-manifest

54-fictional-expectation-view-manifest

55-stage1-run1-context01

56-stage1-run1-context02

57-stage2-run1-context01

58-stage2-run1-context02

59-stage1-run2-context01

60-stage1-run2-context02

61-stage2-run2-context01

62-stage2-run2-context02

63-stage1-run3-context01

64-stage1-run3-context02

65-stage2-run3-context01

66-stage2-run3-context02

67-fictional-context-hard-semantic-audit

68-fictional-configured-signal-field-use-audit

69-fictional-business-delta-audit

70-fictional-market-expectation-audit

71-fictional-financial-sector-audit

72-fictional-ppe-proxy-fcf-audit

73-fictional-stage2-language-audit

74-fictional-final-composition-audit

75-fictional-aggregate-finalization-audit

76-fictional-primary-direction-diagnostic

77-fictional-delta-materiality-diagnostic

78-fictional-new-buyer-diagnostic

79-fictional-holder-diagnostic

80-fictional-core-immutability-audit

81-fictional-runtime-audit

82-fictional-shadow-gate-decision
```

---

# 63. Required shadow setup artifacts

If artifact 82 authorizes shadow:

```text
83-task-start-active-monitored-universe

84-shadow-packet-inventory

85-shadow-packet-hash-manifest

86-shadow-configured-signal-view-manifest

87-shadow-delta-view-manifest

88-shadow-expectation-view-manifest

89-shadow-frozen-context-manifest

90-shadow-batching-manifest

91-shadow-model-call-gate
```

---

# 64. Required full shadow artifacts

Produce:

```text
92-shadow-monolithic-model-artifacts

93-shadow-stage1-model-artifacts

94-shadow-stage2-model-artifacts

95-shadow-context-hard-semantic-audit

96-shadow-configured-signal-field-use-audit

97-shadow-business-delta-audit

98-shadow-market-expectation-audit

99-shadow-financial-sector-audit

100-shadow-fcf-claim-scope-audit

101-shadow-stage2-language-audit

102-shadow-final-composition-audit

103-shadow-aggregate-finalization-audit

104-shadow-per-ticker-comparison

105-shadow-core-direction-differences

106-shadow-business-delta-differences

107-shadow-new-buyer-differences

108-shadow-holder-differences

109-shadow-same-direction-calibration-differences

110-shadow-expected-contract-corrections

111-shadow-potential-architecture-regressions

112-shadow-unresolved-review-required

113-shadow-adr-security-basis-audit

114-shadow-cyclical-valuation-audit

115-shadow-core-immutability-audit

116-shadow-runtime-audit

117-shadow-aggregate-summary

118-shadow-architecture-decision
```

---

# 65. Required combined diagnostics

If full shadow completes:

```text
119-fic-fin-05-vs-monitored-primary-boundary-analogs

120-fic-fin-02-vs-monitored-delta-materiality-analogs

121-fic-fin-06-vs-monitored-positive-delta-analogs

122-fic-fin-08-vs-monitored-holder-analogs

123-new-buyer-monolithic-vs-two-stage-analogs

124-configured-signal-field-use-real-lessons

125-combined-fictional-monitored-root-cause-summary

126-next-bounded-policy-decision
```

---

# 66. Required completion artifacts

Produce:

```text
127-configured-signal-field-ownership-success-decision

128-new-fictional-proof-success-decision

129-full-shadow-completion-decision

130-existing-monitored-impact-summary

131-two-stage-shadow-compatibility-decision

132-fresh-real-proof-readiness-decision

133-final-main-merge-readiness-note

134-production-no-change

135-schedule-pause-observation

136-remote-push-prohibition-audit

137-master-workflow-update

138-program-completion
```

---

# 67. Formal fictional acceptance

Require:

```text
all planned fictional calls complete

all 24 Stage-1 rows hard PASS

all 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

configured-only current-driver violations = 0

false configured-signal fulfillment = 0

current financial false accept = 0

future configured-condition false reject = 0

business-delta semantic projection mismatch = 0

market-expectation anchor violation = 0

financial-sector false reject = 0

PPE-proxy FCF violation = 0

Stage-2 lexical false positive = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material variance does not block shadow.

---

# 68. Full shadow acceptance

Require:

```text
all planned shadow calls complete

all active tickers represented exactly once

aggregate finalization PASS

configured-signal field-use violation = 0

configured-signal false fulfillment = 0

current net-debt false accept = 0

future configured net-debt false reject = 0

business-delta semantic projection mismatch = 0

market-expectation anchor violation = 0

financial-sector scope error = 0

PPE-proxy FCF violation = 0

Stage-2 actual contamination = 0

invalid refs = 0

ADR/security-basis failure = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0

production side effects = 0
```

Investment-decision differences are measured,
not hard failures.

---

# 69. Fresh-real / main / production readiness

At M12AT completion:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY
```

Even if the full cohort completes.

The next task must use the complete clean fictional + monitored cohort
for the bounded:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

No fresh unseen proof inside M12AT.

---

# 70. Failure handling

## A. Repository proves sell_drivers is intentionally mixed,
but no explicit role can be added safely

```text
next_scope =
DIRECTIONAL_DRIVER_FIELD_ROLE_ARCHITECTURE_REVIEW
```

No model calls.

## B. Current-only ref fencing removes valid current driver evidence

```text
STOP
CONFIGURED_SIGNAL_FIELD_FENCING_TOO_BROAD
```

## C. Repair lets configured-only refs become current material anchors

```text
STOP
CONFIGURED_SIGNAL_OWNERSHIP_REPAIR_TOO_PERMISSIVE
```

## D. Fulfillment classifier allows partial/incomplete net debt or FCF proxy to certify fulfillment

```text
STOP
CONFIGURED_SIGNAL_FULFILLMENT_SAFETY_REGRESSION
```

## E. NEW fictional hard semantic failure

Do not run shadow.

Use smallest failing contract.

## F. Full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

---

# 71. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch

latest_result_zip_sha256
latest_result_integrity

m12as_failure_ticker
m12as_failure_error
m12as_failure_field_path

configured_signal_field_root_cause

sell_driver_field_contract

configured_signal_view_contract_version

configured_signal_count

configured_only_signal_count
fulfilled_signal_count
unknown_fulfillment_signal_count

configured_only_current_driver_eligible_count

configured_only_current_driver_violation_count
configured_signal_false_fulfillment_count

m12as_005490_historical_candidate_status

model_prompt_semantic_change_count
model_schema_semantic_change_count
configured_signal_model_view_change_count
final_user_schema_change_count

business_delta_semantic_change_count
expectation_semantic_change_count
financial_semantic_change_count
two_stage_semantic_change_count

investment_judgment_model_target
investment_judgment_reasoning_effort

fictional_generation_id

fictional_stage1_model_calls
fictional_stage2_model_calls
fictional_model_calls_total

fictional_stage1_row_count
fictional_stage2_row_count
fictional_final_composition_count

fictional_configured_signal_current_driver_violation_count
fictional_configured_signal_false_fulfillment_count
fictional_future_configured_condition_false_reject_count

fictional_business_delta_violation_count
fictional_expectation_anchor_violation_count
fictional_financial_sector_violation_count
fictional_ppe_proxy_fcf_violation_count
fictional_stage2_language_false_positive_count

fictional_primary_direction_unstable_subject_count
fictional_business_delta_materiality_variance_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count

fictional_core_mutation_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status

shadow_configured_signal_current_driver_violation_count
shadow_configured_signal_false_fulfillment_count
shadow_future_configured_condition_false_reject_count

shadow_current_netdebt_false_accept_count

shadow_business_delta_violation_count
shadow_expectation_anchor_violation_count
shadow_financial_sector_violation_count
shadow_ppe_proxy_fcf_violation_count
shadow_stage2_language_false_positive_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything not measured:

```text
NOT_MEASURED
```

---

# 72. Artifact integrity

Freeze all local artifacts before final artifact index.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final ZIP SHA-256.

Do not push report/raw artifacts to GitHub.

---

# 73. Final task principle

M12AS successfully fixed the intended SNDK UNCHANGED-negation bug.

The new stop exposes a deeper field-ownership question:

```text
005490 has configured thesis weaken signals:

"FCF 감소와 순부채 증가가 동반"
"CAPEX 증가에도 ROIC가 하락"

The model correctly uses them in:
business_reevaluation_down

but also puts them into:
sell_drivers

as:
"... 투자회수 논리를 훼손하는 조건이다."

The financial validator currently assumes:
sell_drivers = current directional basis

and therefore demands current complete net-debt evidence.
```

The correct M12AT flow is:

```text
first freeze what sell_drivers semantically means

→ if it is current directional evidence:
   fence configured-only signals out of current driver fields
   before model generation

→ keep configured signals available for reevaluation/risk/invalidation fields

→ require real current evidence to mark a configured signal fulfilled

→ separate field-ownership violations
   from financial-completeness violations

→ preserve every completed delta / FCF / expectation / financial-sector fix

→ run a completely new full fictional proof
   if field-ref eligibility changes model-facing semantics

→ if hard gates pass,
   run a completely new full active-monitored shadow

→ finally hand the complete cohort
   to the true boundary / delta-materiality / holder policy review
```

Do NOT:

```text
make "조건이다" a blanket net-debt exemption

allow unfulfilled weaken signals to become current SELL evidence

delete configured thesis signals from the packet

pretend configured signal = fulfilled signal

use model repetition as fulfillment proof

post-hoc rewrite sell drivers

resume the stopped M12AS shadow

stitch prior partial generations

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring
```

Future conditions belong to monitoring until evidence proves they are current facts.
