# Thesis Monitor — Fictional Case FCF Prospective-Scope Alignment + New Full Proof + Full Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260912-fictional-case-fcf-prospective-scope-alignment-full-proof-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260912-fictional-case-fcf-prospective-scope-alignment-full-proof-full-shadow-report.zip
```

Master-workflow phase:

```text
M12AU — Integrated-Main Fictional Case-Semantic Alignment
         A. Freeze M12AT configured-signal field-ownership architecture
         B. Reproduce exact FIC-FIN-01 false reject
         C. Remove/refactor legacy candidate-global FCF case assertion
         D. Make fictional case semantics consume the generic claim-local FCF contract
         E. Start a NEW full 8 × 3 two-stage fictional proof
         F. If hard gates pass, run NEW full active-monitored same-packet shadow
         G. Hand off the complete clean cohort to boundary / delta / holder policy review
```

M12AT successfully completed the deterministic architecture work it was designed to do:

```text
sell_drivers field contract =
CURRENT_DIRECTIONAL_DRIVER_ONLY_WITH_CONFIGURED_REF_FENCING

configured-signal view =
configured-signal-evidence-view-v1

configured-only thesis signals:
- remain available in reevaluation / prospective risk / invalidation fields
- are fenced out of current directional-driver/material-anchor fields
- require separate current evidence before fulfillment
```

M12AT deterministic tests passed before the model call.

A NEW formal fictional proof was then correctly required
because model-facing per-field ref eligibility changed.

The new proof stopped after exactly ONE Stage-1 context because of:

```text
ticker =
FIC-FIN-01

error =
strong_quality_proxy_mislabeled_fcf
```

The generic financial semantic validator for the same row reported:

```text
affirmative_proxy_as_fcf_violation_count = 0

partial_capex_called_fcf_count = 0

unsupported_current_fcf_claim_count = 0

configured_only_current_driver_violation_count = 0

configured_signal_false_fulfillment_count = 0

financial_semantics.valid = true
```

The failing error is generated by an inherited,
FIC-FIN-01-specific global-text assertion in the fictional proof harness.

M12AU must align that case-specific control
with the already completed generic claim-local FCF safety architecture.

Do not weaken true OCF-PPE→FCF misuse detection.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260912-configured-prospective-signal-directional-field-eligibility-fictional-reproof-full-shadow-report.zip
```

Verified SHA-256:

```text
9ceeba4f53cdb44ff03f3908a992be10a4d396d3e1320e3cbcb1379526a104d3
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
artifact_count = 182

hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

ZIP entries:

```text
182 indexed payloads
+ artifact-index.json
= 183 entries
```

Recompute independently.

---

# 2. M12AT status

M12AT program status:

```text
BLOCKED
```

Failure:

```text
generation_id =
20260911-m12ai-fictional-20260912T120605Z-74aa807a8d41

failed stage =
run-1 / Stage 1 / context-01

completed model calls =
1 / 12

schema rows =
4 / 4 PASS

semantic rows =
3 PASS
1 FAIL

failed ticker =
FIC-FIN-01

failed error =
strong_quality_proxy_mislabeled_fcf

shadow calls =
0 / 18
```

Runtime:

```text
transport = PASS

timeout = 0

orphan = 0

wrapper retry = 0

selective rerun = 0

post-freeze hotfix = 0
```

Do NOT continue or stitch this stopped generation.

---

# 3. M12AT configured-signal architecture — preserve

M12AT froze:

```text
CURRENT_DIRECTIONAL_DRIVER_ONLY_WITH_CONFIGURED_REF_FENCING
```

Reasons:

```text
buy/sell drivers are current-evidence fields

financial validator already treats current driver paths as current

future reevaluation / invalidation fields already exist

composer/downstream has no prospective sell-driver role

implicit mixed field semantics are unsafe
```

M12AU must preserve this architecture.

Do NOT reopen the sell-driver field-contract decision.

---

# 4. M12AT model-facing ref eligibility — preserve

Configured-only refs from:

```text
stock.thesis.strengthen_signals

stock.thesis.weaken_signals

stock.thesis.invalidation_signals
```

must remain unavailable as current refs in:

```text
buy_drivers

sell_drivers

material_directional_anchor_basis

dominant_evidence

current core directional judgment
where current supporting evidence is required
```

They remain available for:

```text
business_reevaluation_up

business_reevaluation_down

risk_context when prospective

invalidation / confirmation condition fields
```

Do not reverse this field fencing.

---

# 5. Exact FIC-FIN-01 row

The stopped FIC-FIN-01 Stage-1 candidate:

```text
overall_direction =
BUY

directional_balance =
6.5 : 3.5

business_thesis_change =
STRENGTHENED

directional_confidence =
MEDIUM
```

Current material anchors:

```text
operating improvement

operating cash-flow improvement

safe complete negative net debt
```

Current buy drivers:

```text
same-period revenue + operating profit improvement

same-period operating cash-flow improvement

safe negative net debt / financial buffer
```

Configured-only signals were NOT used as current drivers.

M12AT financial semantics confirmed:

```text
configured_only_current_driver_violation_count = 0

configured_signal_false_fulfillment_count = 0
```

Therefore the configured-signal architecture itself did not fail.

---

# 6. Exact prospective FCF sentence

The FIC-FIN-01 candidate used the configured weaken condition in:

```text
business_reevaluation_down[0].text
```

Sentence:

```text
"잉여현금흐름 감소와 순부채 증가가 함께 확인되면
 사업 논리를 하향 재평가한다."
```

This is a future / configured weakening condition.

It does NOT assert:

```text
current FCF decreased

current FCF equals a PPE-only proxy

current net debt increased

the configured signal is fulfilled now.
```

The generic financial validator correctly accepted it.

---

# 7. Exact inherited fictional case checker

The current inherited case-semantic helper contains logic equivalent to:

```python
text = " ".join(_all_text(candidate.model_dump(mode="json")))

if ticker == "FIC-FIN-01" and ("FCF" in text or "잉여현금흐름" in text):
    errors.append("strong_quality_proxy_mislabeled_fcf")
```

This means:

```text
ANY occurrence of:
FCF
or
잉여현금흐름

ANYWHERE in the candidate

→ FIC-FIN-01 FAIL
```

It ignores:

```text
field role

local evidence refs

current vs future scope

configured-signal lifecycle

PPE-proxy vs actual FCF concept

negation / disclaimer
```

This is obsolete relative to the generic financial semantic architecture
built in M12AN/M12AR/M12AT.

---

# 8. Root-cause classification

Freeze the primary root cause as:

```text
FIC_FIN_01_CASE_SPECIFIC_GLOBAL_FCF_TOKEN_ASSERTION
IGNORES_CLAIM_LOCAL_TEMPORAL_AND_EVIDENCE_SCOPE
```

or exact code-audited equivalent.

This is NOT:

```text
a model prompt failure

a configured-signal field-ownership failure

a generic financial semantic failure

a PPE-proxy metadata regression

a business-delta failure.
```

---

# 9. Architecture options

Before implementation, evaluate:

## Option A — whitelist future reevaluation field

Example:

```text
ignore FCF text inside business_reevaluation_down
```

Too narrow if the same global assertion can false-reject:
- Stage-2 confirmation condition
- invalidation condition
- not-FCF disclaimer
- other safe FCF concepts

Do not prefer.

## Option B — remove the FIC-FIN-01 case-specific FCF assertion entirely

Preferred IF the generic financial semantic validator
now fully covers the original safety purpose.

Requires negative-regression proof that true proxy→FCF misuse
still hard-fails.

## Option C — refactor the FIC-FIN-01 check
to consume the generic claim-local financial semantic audit

Also valid if the case checker has a distinct fixture-level purpose.

Preferred decision principle:

```text
ONE FCF semantic engine
```

Do not keep a separate raw-global-token semantic engine.

---

# 10. Identify the original purpose of strong_quality_proxy_mislabeled_fcf

Before deleting/refactoring,
audit why this fixture check originally existed.

Likely original purpose:

```text
FIC-FIN-01 has OCF-PPE cash-conversion proxy
and must not have that proxy called FCF.
```

Verify actual fixture source history/code.

Do NOT simply delete a hard safety check
without proving the generic validator now supersedes it.

---

# 11. Generic FCF safety is now authoritative

The repository already has a generic financial semantic contract
that distinguishes:

```text
AFFIRMATIVE_FCF_ATTRIBUTION

NUMERIC_FCF_ATTRIBUTION

PROXY_AS_FCF_ATTRIBUTION

PROXY_DESCRIPTIVE_CASH_CONVERSION

EXPLICIT_NOT_FCF_DISCLAIMER

prospective/configured FCF conditions

unsupported current FCF claims
```

It also binds claims to local evidence scope.

That generic contract should be the authoritative FCF semantic engine.

M12AU should not maintain a ticker-specific global lexical substitute.

---

# 12. If the case check is retired

If Option B is selected:

Remove only:

```text
ticker == FIC-FIN-01
and global "FCF"/"잉여현금흐름" text presence
→ strong_quality_proxy_mislabeled_fcf
```

Do NOT remove:

```text
material_financial_anchor_not_used

profit_cash_divergence_not_explained

non_operating_effect_not_used

financial_resilience_anchor_not_used

working_capital_checkpoint_not_used

unavailable_financial_context_used

missing_financial_context_used_as_negative

financial_sector_generic_context_used
```

or any unrelated fixture-specific controls.

Scope the change narrowly.

---

# 13. If the case check is retained

If Option C is selected,
the FIC-FIN-01 case check may only fail when
the generic financial semantic audit already identifies
a relevant true FCF/proxy violation
or an equivalent claim-local violation.

For example:

```text
if ticker == FIC-FIN-01
and generic_financial_semantics.affirmative_proxy_as_fcf_violation_count > 0:
    case fixture fail
```

Do NOT rescan all text independently.

No duplicate semantics.

---

# 14. Exact M12AT FIC-FIN-01 offline replay

After repair,
revalidate the exact stopped M12AT candidate.

Expected:

```text
configured-only current-driver violation = 0

configured-signal false fulfillment = 0

affirmative proxy-as-FCF violation = 0

partial-capex-called-FCF = 0

unsupported current FCF = 0

strong_quality_proxy_mislabeled_fcf = 0

financial semantics = PASS

case semantic status = PASS
```

Do not rewrite model output.

---

# 15. True proxy→FCF negative regression

Construct/reuse a FIC-FIN-01 candidate where:

```text
OCF-PPE proxy ref
```

is used in a current claim:

```text
"FCF improved."

"잉여현금흐름이 증가했다."

"이 OCF-PPE 지표가 FCF다."

"사실상 FCF다."
```

Expected:

```text
generic financial semantic validator = FAIL
```

and, if case-level fixture mirror remains:

```text
case semantic = FAIL
```

The M12AU repair must not let this pass.

---

# 16. Future configured FCF positive regression

PASS:

```text
business_reevaluation_down:

"FCF 감소와 순부채 증가가 함께 확인되면
 하향 재평가한다."
```

when the evidence is a configured weaken condition
and no current fulfillment is asserted.

PASS:

```text
holder invalidation:

"FCF가 장기간 정체하면 보유 논리를 재검토한다."
```

when supported by configured invalidation evidence.

These are concept references / future conditions,
not current proxy attribution.

---

# 17. Not-FCF disclaimer positive regression

PASS:

```text
"이 지표는 현금 전환 대용치이며
 관리 기준 잉여현금흐름이 아니다."
```

The presence of:

```text
잉여현금흐름
```

must not trigger the legacy FIC-FIN-01 global check.

Preserve M12AN.

---

# 18. Current unsupported FCF safety

Do NOT create an unsafe hole.

Current assertions such as:

```text
"현재 FCF가 증가했다."

"FCF는 100이다."

"FCF가 양수다."
```

without proper current FCF evidence
must remain conservatively validated under
the generic financial semantic contract.

Separate:

```text
current unsupported FCF
```

from:

```text
configured future FCF condition.
```

---

# 19. Current net-debt safety remains

The FIC-FIN-01 reevaluation condition also mentions:

```text
순부채 증가
```

Preserve:

```text
configured future net-debt condition
→ allowed in future field

current net-debt claim
→ requires complete current evidence
```

No temporal-scope regression.

---

# 20. Configured-signal field ownership remains strict

The new FCF case-semantic repair must NOT permit configured-only refs
back into current buy/sell drivers.

Re-run exact M12AS 005490 historical field-use audit:

Expected:

```text
configured future signal used as current sell driver
→ INVALID field ownership
```

The error should remain:

```text
configured_future_signal_used_as_current_directional_driver
```

or equivalent.

Do not regress M12AT's purpose.

---

# 21. Field-use fixtures

Re-run:

```text
configured weaken signal in business_reevaluation_down → PASS

configured weaken signal in prospective risk context → PASS

configured-only weaken signal in sell_drivers → FAIL

configured-only strengthen signal in buy_drivers → FAIL

configured-only signal in material anchor → FAIL

configured-only signal in dominant evidence → FAIL

fulfilled configured signal with safe current evidence → may become current eligible
```

No changes.

---

# 22. No model-facing semantic change required

Preferred M12AU repair is:

```text
fictional proof-harness / case-semantic validation only.
```

Required default:

```text
model prompt semantic change count = 0

model schema semantic change count = 0

configured-signal model-view change count = 0

BusinessDeltaEvidenceView change count = 0

MarketExpectationEvidenceView change count = 0

financial evidence projection change count = 0

Stage-1/Stage-2 ownership change count = 0
```

If the implementation changes any model-facing semantic hash:

```text
STOP
UNPLANNED_MODEL_SURFACE_DRIFT
```

Review before proof.

---

# 23. Why a NEW full fictional proof is still mandatory

Even if M12AU only changes the case-semantic harness,
M12AT introduced a genuine model-facing configured-signal field-eligibility change
and only completed:

```text
1 / 12
```

formal model calls.

Therefore the field-ownership architecture has NOT yet received
a full formal proof.

M12AU must run:

```text
NEW full fictional generation
```

from scratch.

Do NOT:

```text
reuse M12AT first-call output as formal proof

continue M12AT

rerun only FIC-FIN-01.
```

---

# 24. Formal proof model/runtime

Use:

```text
gpt-5.6-sol / xhigh

1800-second finite watchdog

4 subjects/context

wrapper retry = 0

batch split = 0
```

No Astra.

No fallback.

---

# 25. NEW fictional topology

Frozen subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Topology:

```text
3 repetitions

2 contexts per repetition

Stage 1 = 6 calls

Stage 2 = 6 calls

total = 12

final composed rows = 24
```

No judge calls.

---

# 26. Fictional hard-stop policy

Hard stop only for:

```text
runtime/schema failure

invalid evidence identity

configured-signal field-ownership violation

configured-signal false fulfillment

objective financial semantic failure

true proxy-as-FCF violation

unsupported current FCF claim

business-delta capability/direction violation

expectation anchor-eligibility violation

financial-sector scope violation

price/technical/supply Core contamination

Stage-2 actual contamination

core mutation

aggregate finalization failure
```

Do NOT hard-stop for:

```text
safe future FCF condition text

safe configured net-debt future condition text

valid decision-material variance.
```

---

# 27. Formal fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

configured-only current-driver violation = 0

configured-signal false fulfillment = 0

true proxy-as-FCF violation = 0

safe future FCF false reject = 0

unsupported current FCF false accept = 0

future net-debt condition false reject = 0

current net-debt false accept = 0

business-delta semantic projection mismatch = 0

expectation anchor violation = 0

financial-sector false reject = 0

Stage-2 lexical false positive = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material variance does not block shadow.

---

# 28. Shadow authorization

If the formal fictional hard gates pass:

```text
run a NEW full active-monitored same-packet shadow.
```

Do NOT require perfect fictional direction/delta/stance stability.

Existing monitored names are exposed compatibility subjects.

---

# 29. Active monitored universe

Re-enumerate active monitored stocks read-only at shadow start.

Last verified reference:

```text
22 names
```

Use the actual task-start active set.

No registrations/stops.

---

# 30. Same-packet contract

For each active ticker:

```text
one reproducible local packet

same packet hash for:
monolithic
Stage 1
Stage 2 evidence context
```

No provider refresh.

Required:

```text
provider_source_fetches = 0
```

---

# 31. Same configured-signal view across paths

For every ticker:

```text
monolithic ConfiguredSignalEvidenceView
=
Stage 1 ConfiguredSignalEvidenceView
```

Required:

```text
source roles

fulfillment states

current-driver eligibility

future-field eligibility

material-anchor eligibility

dominant-evidence eligibility
```

must match.

---

# 32. Shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic calls = context_count

Stage 1 calls = context_count

Stage 2 calls = context_count

total = 3 × context_count
```

At N=22:

```text
18 calls
```

No repetitions.

No fresh unseen issuers.

---

# 33. Shadow hard-stop policy

Hard stop only for:

```text
runtime/schema failure

manifest/hash mismatch

packet mismatch

invalid evidence identity

configured-signal field-ownership violation

configured-signal false fulfillment

objective financial semantic failure

true PPE-proxy-as-FCF attribution

unsupported current FCF claim

business-delta capability/direction violation

expectation anchor-eligibility violation

financial-sector scope violation

price/technical/supply Core contamination

Stage-2 actual contamination

ADR/security-basis failure

core mutation

production side effect

aggregate finalization failure
```

Do NOT hard-stop on legal configured future FCF conditions.

---

# 34. Full shadow configured-signal audit

For every configured signal selected by a candidate,
report:

```text
ticker

ref

signal source role

fulfillment state

selected field

field-use eligibility

claim temporal role

validation result
```

This must directly prove the field-fencing architecture
works in real monitored packets.

---

# 35. Full shadow FCF audit

For every candidate containing FCF language:

```text
claim field

claim temporal role

bound refs

proxy-related?
true/false

configured/future?
true/false

current supported?
true/false

validation result
```

This ensures the removal/refactor of the FIC-FIN-01 case checker
does not create real FCF safety holes.

---

# 36. Freeze all completed prior repairs

Re-run deterministic regressions for:

```text
M12AS UNCHANGED claim scope

M12AR FCF claim/evidence scope

M12AQ financial-sector exclusion

M12AP market-expectation independence

M12AO business-delta single-source convergence

M12AN PPE-proxy metadata + not-FCF safety

M12AM Stage-2 Korean lexical matcher

M12AG timing ownership

M12AH financial temporal scope

QTD/YTD

WC grounding

debt completeness

ADR/security basis

two-stage core immutability
```

No regressions.

---

# 37. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12au-scope-freeze

04-integrated-main-lineage-freeze

05-m12at-fic-fin-01-failure-reproduction

06-fic-fin-01-case-semantic-code-audit

07-strong-quality-proxy-check-original-purpose-audit

08-generic-fcf-validator-coverage-audit

09-case-specific-vs-generic-semantic-overlap-audit

10-fic-fin-01-case-check-architecture-decision
```

---

# 38. Required exact replay / fixture artifacts

Produce:

```text
11-m12at-fic-fin-01-exact-offline-replay

12-fic-fin-01-true-proxy-as-fcf-negative-control

13-fic-fin-01-future-fcf-condition-positive-control

14-fic-fin-01-not-fcf-disclaimer-positive-control

15-m12as-005490-configured-field-ownership-regression

16-current-fcf-claim-negative-regressions

17-future-netdebt-condition-positive-regressions

18-current-netdebt-negative-regressions
```

---

# 39. Required semantic freeze artifacts

Produce:

```text
19-configured-signal-field-ownership-freeze

20-business-delta-view-freeze

21-market-expectation-view-freeze

22-fcf-claim-scope-freeze

23-ppe-proxy-label-freeze

24-financial-sector-scope-freeze

25-stage2-korean-lexical-freeze

26-monitoring-transition-ownership-freeze

27-financial-temporal-scope-freeze

28-qtd-ytd-wc-debt-safety-freeze

29-adr-security-basis-freeze

30-two-stage-ownership-freeze

31-price-timing-renderer-no-change
```

---

# 40. Required model-facing hash artifacts

Produce:

```text
32-model-prompt-semantic-hash-freeze

33-model-schema-semantic-hash-freeze

34-configured-signal-view-semantic-hash-freeze

35-business-delta-view-semantic-hash-freeze

36-expectation-view-semantic-hash-freeze

37-financial-evidence-projection-semantic-hash-freeze

38-model-facing-no-change-decision
```

Artifact 38 expected:

```text
NO_MODEL_FACING_SEMANTIC_CHANGE
```

If not:

```text
STOP
UNPLANNED_MODEL_SURFACE_DRIFT
```

---

# 41. Required deterministic test gate

Produce:

```text
39-focused-test-results

40-full-local-test-results

41-ruff-and-diff-results

42-hosted-ci-portability-observation

43-new-fictional-model-call-gate
```

No model calls before artifact 43 PASS.

---

# 42. Required NEW fictional artifacts

Produce:

```text
44-fictional-generation-manifest

45-fictional-configured-signal-view-manifest

46-fictional-delta-view-manifest

47-fictional-expectation-view-manifest

48-stage1-run1-context01

49-stage1-run1-context02

50-stage2-run1-context01

51-stage2-run1-context02

52-stage1-run2-context01

53-stage1-run2-context02

54-stage2-run2-context01

55-stage2-run2-context02

56-stage1-run3-context01

57-stage1-run3-context02

58-stage2-run3-context01

59-stage2-run3-context02

60-fictional-context-hard-semantic-audit

61-fictional-case-semantic-audit

62-fictional-configured-signal-field-use-audit

63-fictional-fcf-claim-scope-audit

64-fictional-business-delta-audit

65-fictional-market-expectation-audit

66-fictional-financial-sector-audit

67-fictional-stage2-language-audit

68-fictional-final-composition-audit

69-fictional-aggregate-finalization-audit

70-fictional-primary-direction-diagnostic

71-fictional-delta-materiality-diagnostic

72-fictional-new-buyer-diagnostic

73-fictional-holder-diagnostic

74-fictional-core-immutability-audit

75-fictional-runtime-audit

76-fictional-shadow-gate-decision
```

---

# 43. Required shadow setup artifacts

If artifact 76 authorizes shadow:

```text
77-task-start-active-monitored-universe

78-shadow-packet-inventory

79-shadow-packet-hash-manifest

80-shadow-configured-signal-view-manifest

81-shadow-delta-view-manifest

82-shadow-expectation-view-manifest

83-shadow-frozen-context-manifest

84-shadow-batching-manifest

85-shadow-model-call-gate
```

---

# 44. Required full shadow artifacts

Produce:

```text
86-shadow-monolithic-model-artifacts

87-shadow-stage1-model-artifacts

88-shadow-stage2-model-artifacts

89-shadow-context-hard-semantic-audit

90-shadow-configured-signal-field-use-audit

91-shadow-fcf-claim-scope-audit

92-shadow-business-delta-audit

93-shadow-market-expectation-audit

94-shadow-financial-sector-audit

95-shadow-stage2-language-audit

96-shadow-final-composition-audit

97-shadow-aggregate-finalization-audit

98-shadow-per-ticker-comparison

99-shadow-core-direction-differences

100-shadow-business-delta-differences

101-shadow-new-buyer-differences

102-shadow-holder-differences

103-shadow-same-direction-calibration-differences

104-shadow-expected-contract-corrections

105-shadow-potential-architecture-regressions

106-shadow-unresolved-review-required

107-shadow-adr-security-basis-audit

108-shadow-cyclical-valuation-audit

109-shadow-core-immutability-audit

110-shadow-runtime-audit

111-shadow-aggregate-summary

112-shadow-architecture-decision
```

---

# 45. Required combined diagnostics

If full shadow completes:

```text
113-fic-fin-05-vs-monitored-primary-boundary-analogs

114-fic-fin-02-vs-monitored-delta-materiality-analogs

115-fic-fin-06-vs-monitored-positive-delta-analogs

116-fic-fin-08-vs-monitored-holder-analogs

117-new-buyer-monolithic-vs-two-stage-analogs

118-configured-signal-field-use-real-lessons

119-real-fcf-claim-scope-lessons

120-combined-fictional-monitored-root-cause-summary

121-next-bounded-policy-decision
```

---

# 46. Required completion artifacts

Produce:

```text
122-fic-fin-01-case-semantic-alignment-success-decision

123-configured-signal-field-ownership-preservation-decision

124-new-fictional-proof-success-decision

125-full-shadow-completion-decision

126-existing-monitored-impact-summary

127-two-stage-shadow-compatibility-decision

128-fresh-real-proof-readiness-decision

129-final-main-merge-readiness-note

130-production-no-change

131-schedule-pause-observation

132-remote-push-prohibition-audit

133-master-workflow-update

134-program-completion
```

---

# 47. Full formal fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

FIC-FIN-01 safe future FCF false reject = 0

FIC-FIN-01 true proxy-as-FCF negative controls PASS

configured-only current-driver violation = 0

configured-signal false fulfillment = 0

true PPE-proxy-as-FCF violation = 0

unsupported current FCF false accept = 0

future configured FCF false reject = 0

current net-debt false accept = 0

future net-debt condition false reject = 0

business-delta semantic projection mismatch = 0

expectation anchor violation = 0

financial-sector false reject = 0

Stage-2 lexical false positive = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material variance does not block shadow.

---

# 48. Full shadow acceptance

Require:

```text
all planned shadow calls complete

all active tickers represented exactly once

aggregate finalization PASS

configured-signal field-use violation = 0

configured-signal false fulfillment = 0

true proxy-as-FCF violation = 0

safe configured/future FCF false reject = 0

unsupported current FCF false accept = 0

current net-debt false accept = 0

future configured net-debt false reject = 0

business-delta semantic projection mismatch = 0

expectation anchor violation = 0

financial-sector scope error = 0

Stage-2 actual contamination = 0

invalid refs = 0

ADR/security-basis failure = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0

production side effects = 0
```

Investment-decision differences are measured,
not hard failures.

---

# 49. Policy handoff after complete shadow

If full shadow completes without systematic architecture regression:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

Use:

```text
NEW M12AU formal fictional proof
+
M12AU complete monitored shadow
```

Do NOT perform that policy repair in M12AU.

---

# 50. Local-only / production firewall

M12AU is LOCAL-ONLY.

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Do not ask for GitHub push authorization.

Monitoring schedules remain paused.

---

# 51. Failure handling

## A. Generic FCF validator does NOT fully replace the old fixture check

Use Option C:

```text
case-level mirror consumes generic financial semantic audit
```

Do not restore global text scan.

## B. Exact FIC-FIN-01 future condition still fails

```text
next_scope =
FICTIONAL_CASE_SEMANTIC_ARCHITECTURE_REVIEW
```

No model calls.

## C. Removing/refactoring the fixture check lets true proxy→FCF misuse pass

```text
STOP
FIC_FIN_01_CASE_REPAIR_TOO_PERMISSIVE
```

## D. Configured-signal field fencing regresses

```text
STOP
CONFIGURED_SIGNAL_FIELD_OWNERSHIP_REGRESSION
```

## E. NEW fictional hard semantic failure

Do not run shadow.

Use the smallest failing contract.

## F. Full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

---

# 52. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch

latest_result_zip_sha256
latest_result_integrity

m12at_failure_ticker
m12at_failure_error

fic_fin_01_case_semantic_root_cause

strong_quality_proxy_case_check_action:
REMOVED
or
REFRACTORED_TO_GENERIC_FINANCIAL_SEMANTICS

generic_fcf_validator_supersedes_case_check

m12at_fic_fin_01_exact_replay_status

fic_fin_01_safe_future_fcf_false_reject_count
fic_fin_01_true_proxy_as_fcf_false_accept_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
configured_signal_model_view_change_count
business_delta_view_change_count
expectation_view_change_count
financial_evidence_projection_change_count

investment_judgment_model_target
investment_judgment_reasoning_effort

fictional_generation_id

fictional_stage1_model_calls
fictional_stage2_model_calls
fictional_model_calls_total

fictional_stage1_row_count
fictional_stage2_row_count
fictional_final_composition_count

fictional_configured_signal_current_driver_violation_count
fictional_configured_signal_false_fulfillment_count

fictional_proxy_as_fcf_violation_count
fictional_safe_future_fcf_false_reject_count
fictional_unsupported_current_fcf_false_accept_count

fictional_current_netdebt_false_accept_count
fictional_future_netdebt_false_reject_count

fictional_business_delta_violation_count
fictional_expectation_anchor_violation_count
fictional_financial_sector_violation_count
fictional_stage2_language_false_positive_count

fictional_primary_direction_unstable_subject_count
fictional_business_delta_materiality_variance_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count

fictional_core_mutation_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status

shadow_configured_signal_current_driver_violation_count
shadow_configured_signal_false_fulfillment_count

shadow_proxy_as_fcf_violation_count
shadow_safe_future_fcf_false_reject_count
shadow_unsupported_current_fcf_false_accept_count

shadow_current_netdebt_false_accept_count
shadow_future_netdebt_false_reject_count

shadow_business_delta_violation_count
shadow_expectation_anchor_violation_count
shadow_financial_sector_violation_count
shadow_stage2_language_false_positive_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything unmeasured:

```text
NOT_MEASURED
```

---

# 53. Artifact integrity

Freeze all local artifacts before the final artifact index.

Required:

```text
hash mismatch = 0

size mismatch = 0

secret scan failure = 0
```

Report final ZIP SHA-256.

Do not push raw/report artifacts to GitHub.

---

# 54. Final task principle

M12AT's configured-signal field-ownership architecture did not cause
the first proof failure.

The exact FIC-FIN-01 row shows:

```text
configured current-driver misuse = 0

false fulfillment = 0

generic FCF semantic violations = 0
```

The hard stop came from an inherited fixture-specific helper:

```text
if FIC-FIN-01
and ANY candidate text contains
"FCF" or "잉여현금흐름"
→ strong_quality_proxy_mislabeled_fcf
```

That global lexical rule predates the completed claim-local FCF architecture
and can no longer distinguish:

```text
current proxy mislabel
from
safe configured future FCF condition
from
not-FCF disclaimer.
```

The correct M12AU flow is:

```text
preserve the configured-signal current-driver fencing

→ audit the original purpose of the FIC-FIN-01 case check

→ retire it if generic financial semantics fully supersede it

or

→ refactor it to consume the generic claim-local FCF audit

→ prove true OCF-PPE→FCF misuse still fails

→ prove future configured FCF conditions pass

→ start a completely NEW full 12-call fictional proof

→ if all hard gates pass,
   run a completely NEW full active-monitored shadow

→ finally hand the complete clean cohort
   to the actual boundary / delta-materiality / holder policy review
```

Do NOT:

```text
remove generic FCF safety

turn OCF-PPE into FCF

weaken configured-signal field ownership

whitelist only the exact FIC-FIN-01 sentence

keep two independent FCF semantic engines

resume the stopped M12AT generation

rerun only FIC-FIN-01

stitch prior shadow generations

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring
```

One generic claim-local FCF safety contract
should govern every ticker and every field.
