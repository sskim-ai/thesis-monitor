# Thesis Monitor — Financial-Sector Exclusion Connective Scope + Full Fictional Reproof + Full Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260912-financial-sector-exclusion-connective-scope-fictional-reproof-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260912-financial-sector-exclusion-connective-scope-fictional-reproof-full-shadow-report.zip
```

Master-workflow phase:

```text
M12AQ — Integrated-Main Financial-Sector Framework Scope
         A. Freeze successful M12AP MarketExpectationEvidenceView architecture
         B. Reproduce exact FIC-FIN-08 contrastive-exclusion false reject
         C. Repair exclusion-predicate connective / coordinated-object scope
         D. Preserve current-claim and financial-sector misuse hard controls
         E. Start a NEW full 8 × 3 two-stage fictional proof
         F. If hard gates pass, run NEW full active-monitored same-packet shadow
         G. Hand off the complete clean cohort to boundary / delta / holder policy review
```

M12AP successfully implemented the intended market-expectation
economic-independence architecture.

The new expectation view / field-use contract passed its deterministic gate
and the first two Stage-1 calls showed:

```text
expectation material-anchor violations = 0

expectation dominant-evidence violations = 0

expectation driver-classification violations = 0

expectation projection mismatch = 0

pre/post expectation-view identity mismatch = 0
```

The M12AP generation stopped at a DIFFERENT, previously known class of issue:

```text
FIC-FIN-08

errors:
net_debt_claim_without_complete_net_debt_evidence
financial_sector_generic_reasoning
```

The candidate explicitly said:

```text
"보험사이므로 산업회사식 순부채와 운전자본 틀을 배제하고
 인수 규율과 규제자본을 중심으로 본다."
```

This is an explicit NON-APPLICATION / replacement statement.

The current financial-framework classifier nevertheless assigned:

```text
net_debt → UNRESOLVED
working_capital → UNRESOLVED
```

and counted both as application.

M12AQ must fix the exclusion-connective scope
without weakening true current-claim and true financial-sector misuse controls.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260912-market-expectation-economic-independence-anchor-eligibility-fictional-reproof-full-shadow-report.zip
```

Verified SHA-256:

```text
de80fcb7710d2cb62b3cdacd6992b3f6557784f01fd880d38382d620234e7346
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
indexed payloads = 187
actual payloads excluding artifact-index = 187

missing = 0
extra = 0
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute independently.

---

# 2. M12AP status

M12AP status:

```text
BLOCKED_AFTER_2_OF_12_FICTIONAL_CALLS
```

Generation:

```text
20260911-m12ai-fictional-20260912T020515Z-45c464e9fa43
```

Completed:

```text
run-1 Stage 1 context-01 = PASS
run-1 Stage 1 context-02 = HARD FAIL
```

Model/runtime:

```text
gpt-5.6-sol / xhigh

completed model calls = 2
remaining fictional calls not run = 10

monitored shadow calls = 0

wrapper retry = 0
provider fetch = 0
remote push = 0
main merge = 0
deployment = 0
production sends = 0
```

Do NOT continue or stitch this stopped generation.

---

# 3. M12AP MarketExpectationEvidenceView — freeze successful

M12AP introduced a model-facing / validator-shared expectation view.

Preserve:

```text
INDEPENDENT_DIRECTIONAL_SUPPORT

CONDITIONAL_CONTEXT_ONLY

INDEPENDENCE_UNKNOWN

ALREADY_REFLECTED_OR_COUNTERWEIGHT
where supported
```

Preserve:

```text
context-only expectation refs cannot enter material_directional_anchor_basis

context-only expectation refs cannot be independent dominant_evidence

context-only expectation refs may remain market_expectation_context

context-only expectation refs may describe asymmetry/caution
when not treated as independent corroboration
```

Exact M12AP first-two-call expectation audit:

```text
material-anchor violation = 0

dominant-evidence violation = 0

driver-classification violation = 0

projection mismatch = 0

pre/post identity mismatch = 0
```

Do not reopen the expectation architecture in M12AQ.

---

# 4. M12AO / M12AP expectation regression controls — preserve

The following deterministic replays must remain:

```text
exact M12AO double-count candidate
→ FAIL

M12AK valid SELL 6.0 candidate
→ PASS

M12AK valid HOLD 5.5 candidate
→ PASS
```

Do not weaken or broaden the expectation contract.

---

# 5. M12AO business-delta convergence — freeze

Preserve:

```text
BusinessDeltaEvidenceView
is the single source of truth

post-model validator consumes canonical roles/directions

legacy raw-text eligibility re-derivation = 0

legacy raw-text direction re-derivation = 0

semantic projection mismatch = 0

pre/post delta-view identity mismatch = 0
```

No business-delta changes in M12AQ.

---

# 6. M12AN PPE-proxy / FCF safety — freeze

Preserve:

```text
label = cash_conversion_ocf_less_ppe

metric_refs = []

ocf_less_ppe_capex is NOT management-defined FCF

explicit not-FCF disclaimer = safe

affirmative PPE-proxy-as-FCF attribution = hard FAIL
```

No FCF changes.

---

# 7. M12AM Stage-2 Korean lexical safety — freeze

Preserve:

```text
real "주가" language contamination blocked

"수주가 / 발주가" false positives eliminated
```

No language matcher changes.

---

# 8. M12AG/M12AH monitoring ownership + temporal scope — freeze

Preserve:

```text
price confirmation / risk-reward transitions
remain outside Directional Core

Timing facts retained

risk_context supports prospective-risk role

configured future financial checks
are not current fulfilled claims

current unsupported financial claims still hard fail
```

No ownership/temporal changes.

---

# 9. Exact M12AP FIC-FIN-08 candidate

FIC-FIN-08 Stage-1 output:

```text
overall_direction =
HOLD 5.0 : 5.0 NEUTRAL

business_thesis_change =
UNCHANGED

directional_confidence =
LOW
```

Sector interpretation:

```text
"보험사이므로 산업회사식 순부채와 운전자본 틀을 배제하고
 인수 규율과 규제자본을 중심으로 본다."
```

Evidence refs:

```text
E03
E05
E07
```

This sentence explicitly says:

```text
industrial net-debt framework = excluded

industrial working-capital framework = excluded

underwriting discipline = used

regulatory capital = used
```

It does NOT assert:

```text
current net debt magnitude

current net debt direction

current working-capital condition

industrial net debt as an insurance valuation/risk framework.
```

---

# 10. Exact M12AP classifier failure

Run-document classification:

```text
financial_framework_roles.application_count = 2
```

Claims:

```text
net_debt:
role = UNRESOLVED
kind = UNCERTAIN_OR_AMBIGUOUS

working_capital:
role = UNRESOLVED
kind = UNCERTAIN_OR_AMBIGUOUS
```

Resulting hard errors:

```text
net_debt_claim_without_complete_net_debt_evidence

financial_sector_generic_reasoning
```

Financial grounding itself:

```text
PASS
```

Market-expectation audit:

```text
PASS
```

Ownership:

```text
PASS
```

Therefore this is a scope-classification false reject.

---

# 11. Root-cause hypothesis to verify in code

Primary expected root cause:

```text
EXCLUSION_PREDICATE_CONNECTIVE_FORM_NOT_RECOGNIZED
```

Specifically:

```text
"틀을 배제하고 ..."
```

uses:

```text
배제하- + -고
```

as a connective exclusion predicate.

The classifier appears to recognize other exclusion/contrastive shapes
but does not bind the coordinated framework object to
the connective exclusion predicate.

Do not assume this exact implementation until code audit confirms it.

Freeze the actual root cause from code.

---

# 12. Structural clause interpretation

The sentence should parse conceptually as:

```text
LEFT OBJECT NP:
산업회사식 순부채와 운전자본 틀을

LEFT PREDICATE:
배제하고

RIGHT REPLACEMENT NP:
인수 규율과 규제자본을

RIGHT PREDICATE:
중심으로 본다
```

The left predicate establishes:

```text
EXPLICIT_NON_APPLICATION
```

for all forbidden industrial frameworks
inside the left object span.

The right predicate applies only to:

```text
underwriting discipline
regulatory capital
```

It must not leak backward and turn the excluded left frameworks into applications.

---

# 13. Coordinated-framework object scope

Support coordinated excluded framework objects such as:

```text
순부채와 운전자본 틀을 배제하고 ...

순부채·운전자본 틀을 제외하고 ...

순부채나 운전자본 틀을 적용하지 않고 ...
```

All relevant framework concepts in the excluded object span
must inherit the exclusion role.

Do not classify one as excluded and the other as unresolved
merely because they share one predicate.

---

# 14. Exclusion connective morphology

The fix must be predicate/role structural,
not one exact phrase whitelist.

Audit connective forms of existing supported exclusion predicates.

At minimum consider actual repository semantics for forms equivalent to:

```text
배제하고
제외하고
적용하지 않고
사용하지 않고
보지 않고
배제한 채
```

Only support forms that can be implemented safely
under the current bounded classifier.

Do not create a general Korean parser.

The minimum required new proof is:

```text
X 틀을 배제하고 Y를 중심으로 본다
```

---

# 15. Existing contrastive contracts remain

Preserve already supported forms from prior phases:

```text
X 대신 Y

X이/가 아니라 Y

X이 아닌 Y

X보다는 Y

X를 적용하지 않는다

X는 적용 대상이 아니다

explicit exclusion / non-application
```

M12AQ must be additive/repairing,
not a rewrite that regresses those fixtures.

---

# 16. Reference-role contract

For FIC-FIN-08 exact M12AP sentence,
acceptable canonical roles are:

```text
EXPLICIT_NON_APPLICATION
```

for:

```text
net_debt
working_capital
```

or, if current role ontology defines it more precisely:

```text
CONTRASTIVE_REPLACEMENT
```

The critical hard requirements are:

```text
application_count = 0

actual industrial-framework misuse count = 0

net-debt completeness trigger = 0
```

Do not force a label name if current canonical ontology already distinguishes
explicit exclusion vs replacement.

---

# 17. Shared role classifier

The same canonical framework role result must drive:

```text
net-debt completeness validator

financial-sector generic-framework validator

working-capital sector-framework validator
```

No downstream lexical scan may override:

```text
EXPLICIT_NON_APPLICATION
or
CONTRASTIVE_REPLACEMENT.
```

Required:

```text
shared_application_scope_consistency = PASS
```

---

# 18. Net-debt completeness safety remains hard

True current net-debt claims must still fail without complete evidence.

Examples:

```text
"현재 순부채가 높다."

"순부채가 증가했다."

"높은 순부채가 SELL 근거다."

"순부채 부담이 현재 복원력을 훼손한다."
```

when complete net-debt evidence is unavailable.

An exclusion sentence must not exempt a separate current claim elsewhere.

---

# 19. Financial-sector misuse remains hard

Still FAIL:

```text
"보험사지만 순부채가 높아 SELL이다."

"보험사 운전자본 악화가 핵심 하방이다."

"산업회사식 순부채를 주요 보험업 가치평가 기준으로 사용한다."
```

unless an explicitly supported insurance-specific contract says otherwise.

M12AQ does NOT legalize industrial-framework application.

It only recognizes actual exclusion correctly.

---

# 20. Mixed exclusion + actual application

Example:

```text
"산업회사식 순부채 틀은 배제하고 규제자본을 본다.
 하지만 현재 순부채가 높아 SELL이다."
```

Expected:

```text
sector_interpretation exclusion = PASS

separate current net-debt directional claim = FAIL
```

The exclusion clause is local.

Do not grant sentence/document-wide immunity.

---

# 21. Negated exclusion predicates

Do NOT misclassify:

```text
"순부채 틀을 배제하지 않고 본다."
```

as exclusion.

Likewise:

```text
"순부채를 제외할 수 없다."
```

is not non-application.

The predicate polarity itself must be correct.

No substring rule such as:

```text
contains "배제"
→ excluded
```

is acceptable.

---

# 22. Replacement predicate backward leakage

The later predicate:

```text
"규제자본을 중심으로 본다"
```

must apply to the replacement/right object.

It must not cause:

```text
순부채
운전자본
```

from the prior excluded span
to be marked:

```text
ASSERTED_STATE
APPLIED_DECISION_FRAMEWORK
UNRESOLVED due "본다".
```

Add a specific regression fixture for this leakage.

---

# 23. Exact M12AP offline replay

After repair,
revalidate the exact preserved FIC-FIN-08 Stage-1 candidate.

Required:

```text
net_debt role =
EXPLICIT_NON_APPLICATION
or canonical equivalent

working_capital role =
EXPLICIT_NON_APPLICATION
or canonical equivalent

application_count = 0

net_debt_claim_without_complete_net_debt_evidence = 0

financial_sector_generic_reasoning = 0

partial_debt_total_claim_count = 0
or no false count caused by excluded framework reference

financial_sector_generic_financial_context_leak_count = 0

financial semantic status = PASS
```

Do not rewrite the model output.

---

# 24. Historical positive regressions

Re-run prior known valid forms,
including at minimum:

```text
"보험사에는 산업회사식 순부채와 운전자본 대신
 보험 인수 규율과 규제자본 기준을 적용해야 한다."

"보험사이므로 산업회사식 순부채·운전자본 틀이 아니라
 인수 규율과 규제자본으로 판단한다."

"보험사이므로 인수 규율과 규제자본을 적용하며
 산업회사식 순부채·운전자본 틀은 배제한다."
```

Expected:

```text
PASS
actual industrial application = 0
```

No regression.

---

# 25. New positive fixtures

At minimum:

## SECTOR-EXCL-P01

```text
"산업회사식 순부채와 운전자본 틀을 배제하고
 인수 규율과 규제자본을 중심으로 본다."
→ PASS
```

## SECTOR-EXCL-P02

```text
"산업회사식 순부채 틀을 제외하고 규제자본을 본다."
→ PASS
```

## SECTOR-EXCL-P03

```text
"산업회사식 운전자본 틀을 적용하지 않고
 인수 규율을 본다."
→ PASS
```

## SECTOR-EXCL-P04

```text
"순부채·운전자본 틀을 배제한 채
 보험 인수와 규제자본을 평가한다."
→ PASS
```

Only if each form is safely supported by the implemented contract.

---

# 26. Negative fixtures

At minimum:

## SECTOR-EXCL-N01

```text
"순부채 틀을 배제하지 않고 본다."
→ NOT exclusion
```

## SECTOR-EXCL-N02

```text
"순부채 틀을 제외할 수 없다."
→ NOT exclusion
```

## SECTOR-EXCL-N03

```text
"순부채 틀을 배제한다고 하지만
 현재 순부채가 높아 SELL이다."
→ current claim FAIL
```

## SECTOR-EXCL-N04

```text
"보험사 운전자본 악화가 핵심 하방이다."
→ financial-sector generic framework misuse FAIL
```

## SECTOR-EXCL-N05

```text
"순부채와 규제자본을 함께 핵심 보험업 기준으로 본다."
→ net-debt application remains detectable
```

No false immunity.

---

# 27. No model prompt change by default

The model produced the correct sector interpretation.

Preferred:

```text
Stage-1 prompt semantic change count = 0

Stage-2 prompt semantic change count = 0

monolithic prompt semantic change count = 0
```

This is a deterministic validator/parser repair.

If prompt changes are claimed necessary:

```text
STOP
explain why the exact correct model sentence cannot be recognized deterministically.
```

---

# 28. No model schema change

Required:

```text
model schema semantic change count = 0
```

The candidate already fits the schema.

---

# 29. MarketExpectationEvidenceView no-change

Required:

```text
expectation model view change count = 0

context-only expectation material-anchor violation count = 0

expectation projection mismatch count = 0

pre/post expectation view identity mismatch count = 0
```

Re-run exact expectation fixtures and replays.

M12AQ is not an expectation redesign.

---

# 30. Business-delta convergence no-change

Required:

```text
BusinessDeltaEvidenceView unchanged

business_delta_semantic_projection_mismatch_count = 0

pre/post delta-view identity mismatch count = 0

legacy raw-text eligibility re-derivation count = 0

legacy raw-text direction re-derivation count = 0
```

Re-run:

```text
M12AN FIC-FIN-06 UNRESOLVED replay

M12AJ FIC-FIN-02 mixed-evidence replay

003690 UNCHANGED_ONLY regression

FIC-FIN-05 UNCHANGED_ONLY regression
```

All must PASS.

---

# 31. PPE-proxy / Stage-2 lexical / ownership / temporal regressions

Re-run all frozen safety regressions:

```text
PPE proxy not-FCF disclaimer

affirmative proxy-as-FCF hard failure

"주가" vs "수주가/발주가"

price/timing monitoring ownership

financial temporal scope

conditional net-debt safety

QTD/YTD

working-capital grounding

debt completeness

ADR/security basis
```

No regressions.

---

# 32. Deterministic gate before new model calls

Require:

```text
latest M12AP ZIP integrity PASS

exact M12AP FIC-FIN-08 replay PASS

prior financial-sector exclusion forms PASS

new connective exclusion fixtures PASS

negative application/double-negative fixtures PASS

shared role classifier consistency PASS

true current net-debt hard controls PASS

true financial-sector misuse controls PASS

M12AP expectation fixtures/replays PASS

M12AO business-delta convergence regressions PASS

M12AN PPE-proxy regressions PASS

M12AM Stage-2 lexical regressions PASS

monitoring ownership regressions PASS

financial temporal-scope regressions PASS

focused tests PASS

full local tests PASS

ruff PASS

git diff --check PASS

production firewall PASS

model = gpt-5.6-sol / xhigh
```

No model calls before this gate.

---

# 33. NEW full fictional generation required

M12AP stopped after two calls
and validator code will change afterward.

Therefore:

```text
NEW generation ID

NEW Stage-1 outputs

NEW Stage-2 outputs

NEW receipts/logs

NEW final compositions
```

Do NOT:

```text
continue M12AP

reuse its first two outputs as formal proof

rerun only FIC-FIN-08
```

---

# 34. Fictional topology

Frozen subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Topology:

```text
3 repetitions

2 contexts / repetition

4 subjects / context

Stage 1 calls = 6

Stage 2 calls = 6

total = 12

final outputs = 24
```

Model:

```text
gpt-5.6-sol / xhigh
```

No judge calls.

---

# 35. Fictional hard-stop policy

Hard stop only for:

```text
runtime/schema hard failure

invalid evidence identity

objective financial semantic failure

true financial-sector industrial-framework misuse

legitimate financial-sector exclusion false reject

PPE-proxy FCF true violation

business-delta capability violation

business-delta canonical direction contradiction

market-expectation anchor-eligibility violation

pre/post expectation-view mismatch

price/technical/supply Core contamination

Stage-2 actual contamination

core mutation

aggregate finalization failure
```

Do NOT stop for valid decision-material variance.

---

# 36. FIC-FIN-08 acceptance

Across all repetitions:

```text
insurance-appropriate underwriting/capital reasoning

industrial net-debt/WC frameworks not applied

legitimate exclusion / replacement language accepted

financial-sector true misuse = 0

financial-sector false reject = 0
```

The exact wording may vary.

Do not require one phrase.

---

# 37. FIC-FIN-05 expectation acceptance

Preserve M12AP expectation contract.

Across all repetitions:

```text
context-only expectation refs
must not enter material_directional_anchor_basis

context-only expectation refs
must not become independent dominant evidence

expectation asymmetry context remains legal
```

Directional balance is not frozen.

---

# 38. Formal fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

financial-sector exclusion false reject = 0

financial-sector true misuse = 0

context-only expectation material-anchor violation = 0

expectation view mismatch = 0

business-delta semantic projection mismatch = 0

business-delta true direction contradiction = 0

PPE-proxy FCF violation = 0

not-FCF disclaimer false reject = 0

Stage-2 lexical false positive = 0

financial hard semantic failure = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material variance alone does not block shadow.

---

# 39. Shadow authorization

If fictional hard gates pass:

```text
run a NEW full active-monitored same-packet shadow.
```

Do NOT require perfect fictional direction/stance stability.

Existing monitored stocks are an exposed compatibility cohort.

---

# 40. Active monitored universe

Re-enumerate active monitored names read-only at shadow start.

Last verified reference:

```text
22 active monitored stocks
```

Use actual current active set.

No registration/stop mutation.

---

# 41. Same-packet shadow contract

For each active ticker:

```text
one reproducible local frozen packet
```

Exact same packet hash feeds:

```text
monolithic

Stage 1

Stage 2 evidence context
```

No provider refresh.

Required:

```text
provider_source_fetches = 0
```

---

# 42. Same canonical views across architecture paths

For every monitored ticker,
monolithic and Stage 1 must receive semantically identical:

```text
BusinessDeltaEvidenceView

MarketExpectationEvidenceView

financial framework / sector context

owned evidence catalog
```

Required:

```text
monolithic_stage1_delta_view_equality = PASS

monolithic_stage1_expectation_view_equality = PASS
```

---

# 43. Shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic calls = context_count

Stage 1 calls = context_count

Stage 2 calls = context_count

total = 3 × context_count
```

At N=22:

```text
18 calls
```

No repetitions.

No fresh unseen issuers.

---

# 44. Shadow hard-stop policy

Hard stop only for:

```text
runtime/schema hard failure

manifest/hash mismatch

packet mismatch

invalid evidence identity

objective financial semantic failure

financial-sector true framework misuse

legitimate exclusion false reject

PPE-proxy-as-FCF violation

business-delta capability/direction violation

market-expectation anchor-eligibility violation

price/technical/supply Core contamination

Stage-2 actual contamination

ADR/security-basis violation

core mutation

production side-effect attempt

aggregate finalization failure
```

Do not stop because monolithic/two-stage investment decisions differ.

---

# 45. Real financial-sector audit

For every financial-sector monitored name,
especially:

```text
003690
```

report:

```text
sector framework

industrial net-debt reference roles

industrial working-capital reference roles

insurance-appropriate replacement framework

application count

false reject count
```

No industrial-framework leakage.

No legitimate exclusion false reject.

---

# 46. Shadow comparison taxonomy

Use:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

Expectation diagnostic labels remain:

```text
EXPECTATION_CONTEXT_ONLY_CORRECTION

EXPECTATION_INDEPENDENT_ANCHOR_USED

EXPECTATION_FIELD_USE_DIFFERENCE

EXPECTATION_INDEPENDENCE_METADATA_GAP
```

Neither architecture is automatically ground truth.

---

# 47. Full shadow completion goal

After all contexts:

```text
one final comparison row per active ticker

no missing ticker

no duplicate ticker

all packet hashes preserved

core hashes preserved

aggregate finalization PASS
```

Then classify all decision-material differences.

This is the long-blocked compatibility cohort.
Do not introduce new policy repairs mid-generation.

---

# 48. Combined policy handoff

If the full shadow completes cleanly,
produce the final evidence package for:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

Use:

```text
NEW formal fictional proof

+

complete active-monitored shadow
```

Focus on:

```text
1. HOLD 5.5 ↔ BUY/SELL 6.0 adjacent primary boundary

2. mixed-evidence business-delta materiality

3. holder HOLDABLE ↔ REVIEW boundary

4. new-buyer stance differences when core is identical

5. whether expectation context-only restrictions explain
   some prior threshold differences
```

Do NOT perform those policy changes inside M12AQ.

---

# 49. Remote / main / production policy

M12AQ is LOCAL-ONLY.

Required:

```text
remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Do not ask for GitHub push authorization.

---

# 50. Production side-effect firewall

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0
```

Schedules remain paused.

---

# 51. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12aq-scope-freeze

04-integrated-main-lineage-freeze

05-m12ap-fic-fin-08-failure-reproduction

06-financial-framework-role-classifier-code-audit

07-exclusion-predicate-connective-root-cause

08-coordinated-framework-object-scope-audit

09-replacement-predicate-backward-leakage-audit

10-financial-sector-scope-repair-architecture-decision
```

---

# 52. Required scope-contract artifacts

Produce:

```text
11-financial-framework-exclusion-connective-contract

12-exclusion-predicate-polarity-contract

13-coordinated-framework-object-contract

14-replacement-clause-scope-contract

15-shared-framework-role-consumer-contract

16-current-claim-locality-contract

17-double-negative-exclusion-negative-control
```

---

# 53. Required exact replay / fixture artifacts

Produce:

```text
18-m12ap-fic-fin-08-exact-offline-replay

19-prior-contrastive-replacement-regression

20-prior-korean-copular-negation-regression

21-prior-explicit-exclusion-regression

22-new-connective-exclusion-positive-fixtures

23-exclusion-double-negative-negative-fixtures

24-exclusion-plus-separate-current-claim-negative-fixtures

25-financial-sector-true-misuse-regressions
```

---

# 54. Required frozen-regression artifacts

Produce:

```text
26-market-expectation-evidence-view-freeze

27-expectation-replay-regressions

28-business-delta-convergence-freeze

29-business-delta-replay-regressions

30-ppe-proxy-fcf-safety-freeze

31-stage2-korean-lexical-freeze

32-monitoring-transition-ownership-freeze

33-financial-temporal-scope-freeze

34-qtd-ytd-wc-debt-safety-freeze

35-adr-security-basis-freeze

36-two-stage-ownership-freeze

37-price-timing-renderer-no-change
```

---

# 55. Required deterministic test gate

Produce:

```text
38-focused-test-results

39-full-local-test-results

40-ruff-and-diff-results

41-hosted-ci-portability-observation

42-new-fictional-model-call-gate
```

No model calls before artifact 42 PASS.

---

# 56. Required NEW fictional artifacts

Produce:

```text
43-fictional-generation-manifest

44-fictional-delta-view-manifest

45-fictional-expectation-view-manifest

46-stage1-run1-context01

47-stage1-run1-context02

48-stage2-run1-context01

49-stage2-run1-context02

50-stage1-run2-context01

51-stage1-run2-context02

52-stage2-run2-context01

53-stage2-run2-context02

54-stage1-run3-context01

55-stage1-run3-context02

56-stage2-run3-context01

57-stage2-run3-context02

58-fictional-context-hard-semantic-audit

59-fictional-financial-sector-scope-audit

60-fictional-market-expectation-independence-audit

61-fictional-business-delta-convergence-audit

62-fictional-ppe-proxy-fcf-safety-audit

63-fictional-stage2-language-audit

64-fictional-final-composition-audit

65-fictional-aggregate-finalization-audit

66-fictional-primary-direction-diagnostic

67-fictional-business-delta-materiality-diagnostic

68-fictional-new-buyer-diagnostic

69-fictional-holder-diagnostic

70-fictional-core-immutability-audit

71-fictional-runtime-audit

72-fictional-shadow-gate-decision
```

---

# 57. Required shadow setup artifacts

If artifact 72 authorizes shadow:

```text
73-task-start-active-monitored-universe

74-shadow-packet-inventory

75-shadow-packet-hash-manifest

76-shadow-delta-view-manifest

77-shadow-expectation-view-manifest

78-shadow-frozen-context-manifest

79-shadow-batching-manifest

80-shadow-model-call-gate
```

---

# 58. Required full shadow artifacts

Produce:

```text
81-shadow-monolithic-model-artifacts

82-shadow-stage1-model-artifacts

83-shadow-stage2-model-artifacts

84-shadow-context-hard-semantic-audit

85-shadow-financial-sector-scope-audit

86-shadow-market-expectation-independence-audit

87-shadow-business-delta-convergence-audit

88-shadow-ppe-proxy-fcf-safety-audit

89-shadow-stage2-language-audit

90-shadow-final-composition-audit

91-shadow-aggregate-finalization-audit

92-shadow-per-ticker-comparison

93-shadow-core-direction-differences

94-shadow-business-delta-differences

95-shadow-new-buyer-differences

96-shadow-holder-differences

97-shadow-same-direction-calibration-differences

98-shadow-expectation-context-only-corrections

99-shadow-potential-architecture-regressions

100-shadow-unresolved-review-required

101-shadow-adr-security-basis-audit

102-shadow-cyclical-valuation-audit

103-shadow-core-immutability-audit

104-shadow-runtime-audit

105-shadow-aggregate-summary

106-shadow-architecture-decision
```

---

# 59. Required combined diagnostics

If full shadow completes:

```text
107-fic-fin-05-vs-monitored-primary-boundary-analogs

108-expectation-independence-vs-primary-threshold-analysis

109-fic-fin-02-vs-monitored-delta-materiality-analogs

110-fic-fin-06-vs-monitored-positive-delta-analogs

111-fic-fin-08-vs-monitored-holder-analogs

112-new-buyer-monolithic-vs-two-stage-analogs

113-real-financial-sector-framework-lessons

114-real-expectation-independence-lessons

115-combined-fictional-monitored-root-cause-summary

116-next-bounded-policy-decision
```

---

# 60. Required completion artifacts

Produce:

```text
117-financial-sector-exclusion-connective-repair-success-decision

118-market-expectation-independence-preservation-decision

119-business-delta-convergence-preservation-decision

120-new-fictional-proof-success-decision

121-full-shadow-completion-decision

122-existing-monitored-impact-summary

123-two-stage-shadow-compatibility-decision

124-fresh-real-proof-readiness-decision

125-final-main-merge-readiness-note

126-production-no-change

127-schedule-pause-observation

128-remote-push-prohibition-audit

129-master-workflow-update

130-program-completion
```

---

# 61. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch

latest_result_zip_sha256
latest_result_integrity

m12ap_failure_ticker
m12ap_failure_errors

financial_sector_scope_root_cause

financial_framework_scope_contract_version

exclusion_connective_support_enabled

coordinated_framework_object_scope_enabled

replacement_predicate_backward_leak_count

shared_application_scope_consistency

m12ap_fic_fin_08_offline_replay_status

financial_sector_exclusion_false_reject_count
financial_sector_true_misuse_count

net_debt_current_claim_false_accept_count

market_expectation_view_contract_version

context_only_expectation_material_anchor_violation_count
expectation_view_projection_mismatch_count
pre_post_expectation_view_identity_mismatch_count

business_delta_semantic_projection_mismatch_count
pre_post_delta_view_identity_mismatch_count

ppe_proxy_fcf_safety_regression_count
stage2_language_safety_regression_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
financial_sector_validator_semantic_change_count

investment_judgment_model_target
investment_judgment_reasoning_effort

fictional_generation_id

fictional_stage1_model_calls
fictional_stage2_model_calls
fictional_model_calls_total

fictional_stage1_row_count
fictional_stage2_row_count
fictional_final_composition_count

fictional_financial_sector_exclusion_false_reject_count
fictional_financial_sector_true_misuse_count

fictional_expectation_anchor_violation_count
fictional_expectation_view_projection_mismatch_count

fictional_business_delta_semantic_projection_mismatch_count
fictional_business_delta_direction_contradiction_count

fictional_ppe_proxy_fcf_violation_count
fictional_stage2_language_false_positive_count

fictional_primary_direction_unstable_subject_count
fictional_business_delta_materiality_variance_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count

fictional_core_mutation_count

fictional_runtime_timeout_count
fictional_runtime_orphan_count
fictional_wrapper_retry_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status

shadow_financial_sector_exclusion_false_reject_count
shadow_financial_sector_true_misuse_count

shadow_expectation_anchor_violation_count
shadow_expectation_view_projection_mismatch_count

shadow_business_delta_semantic_projection_mismatch_count
shadow_business_delta_direction_contradiction_count

shadow_ppe_proxy_fcf_violation_count
shadow_stage2_language_false_positive_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_adr_security_basis_failure_count
shadow_cyclical_valuation_framework_failure_count

shadow_core_mutation_after_stance_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything unmeasured:

```text
NOT_MEASURED
```

---

# 62. Fresh-real / main / production readiness

At M12AQ completion:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY
```

Even if fictional and shadow complete.

The next task should use the complete clean cohort for:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

No fresh unseen proof inside M12AQ.

---

# 63. Failure handling

## A. Exact M12AP sentence still false-rejects

```text
next_scope =
FINANCIAL_SECTOR_FRAMEWORK_SCOPE_PARSER_ARCHITECTURE_REVIEW
```

No model calls.

## B. Repair lets true insurance industrial-framework use pass

```text
STOP
FINANCIAL_SECTOR_SCOPE_REPAIR_TOO_PERMISSIVE
```

## C. Repair weakens current net-debt completeness

```text
STOP
NET_DEBT_COMPLETENESS_REGRESSION
```

## D. Expectation architecture regresses

```text
STOP
MARKET_EXPECTATION_INDEPENDENCE_REGRESSION
```

## E. NEW fictional hard semantic failure

Do not run shadow.

Use the smallest failing contract.

## F. Full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

## G. Full shadow shows systematic architecture regression

Use the appropriate bounded compatibility review instead.

---

# 64. Artifact integrity

Freeze all local artifacts before final artifact index.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final ZIP SHA-256.

Do not push raw/report artifacts to GitHub.

---

# 65. Final task principle

M12AP's new market-expectation independence architecture worked
on the rows that were actually evaluated.

The stopped FIC-FIN-08 row failed for an older, narrower reason:

```text
model:
"보험사이므로 산업회사식 순부채와 운전자본 틀을 배제하고
 인수 규율과 규제자본을 중심으로 본다."

intended meaning:
industrial net debt/WC = explicitly excluded
insurance underwriting/regulatory capital = applied

validator:
net debt = UNRESOLVED
working capital = UNRESOLVED
application_count = 2
→ hard false reject
```

The correct M12AQ flow is:

```text
keep MarketExpectationEvidenceView unchanged

→ structurally recognize exclusion predicates in connective form

→ apply the exclusion predicate to the coordinated left framework object

→ prevent the right-side "본다" predicate from leaking backward

→ preserve true current net-debt and true insurance-framework misuse hard failures

→ offline replay the exact M12AP candidate

→ start a completely new full fictional proof

→ if hard semantics pass,
   run a completely new full active-monitored shadow

→ finally hand the complete clean fictional + monitored cohort
   to the real boundary / delta-materiality / holder policy review
```

Do NOT:

```text
add only the exact string "배제하고" as a whitelist

treat every occurrence of "배제" as negation

weaken net-debt completeness

legalize industrial net-debt/WC reasoning for insurers

reopen MarketExpectationEvidenceView

reopen BusinessDeltaEvidenceView

resume the stopped M12AP generation

stitch prior shadow generations

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring
```

Recognize what the sentence actually says:
the industrial framework is being excluded, not applied.
