# Thesis Monitor — Market-Expectation Economic Independence / Anchor Eligibility + Full Fictional Reproof + Full Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260912-market-expectation-economic-independence-anchor-eligibility-fictional-reproof-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260912-market-expectation-economic-independence-anchor-eligibility-fictional-reproof-full-shadow-report.zip
```

Master-workflow phase:

```text
M12AP — Integrated-Main Expectation-Independence Contract
         A. Freeze successful M12AO business-delta convergence
         B. Reproduce exact FIC-FIN-05 conditional-expectation double count
         C. Add a canonical pre-model MarketExpectationEvidenceView
         D. Restrict context-only dependent expectations from material-anchor fields
         E. Converge post-model validation on the same expectation view
         F. Start a NEW full 8 × 3 two-stage fictional proof
         G. If hard gates pass, run NEW full active-monitored same-packet shadow
         H. Hand off the complete clean cohort to boundary/delta/holder policy review
```

M12AO successfully implemented the intended business-delta validator convergence.

The new business-delta single-source-of-truth path itself passed:

```text
pre-model BusinessDeltaEvidenceView
=
post-model validator semantic source

business_delta_semantic_projection_mismatch_count = 0

pre/post delta view identity mismatch = 0

legacy raw-text direction re-derivation = 0

legacy raw-text eligibility re-derivation = 0
```

The exact M12AN FIC-FIN-06 `UNRESOLVED` candidate passed offline under the converged validator.

The new formal fictional generation then stopped at a DIFFERENT contract:

```text
FIC-FIN-05

error =
fic_fin_05_conditional_expectation_double_counted
```

The failure is not a business-delta regression.

M12AP must preserve M12AO's delta convergence
and structurally encode the already-frozen market-expectation economic-independence rule.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260911-business-delta-single-source-validation-convergence-fictional-reproof-full-shadow-report.zip
```

Verified SHA-256:

```text
32e62c0fe690e1b77fd8f098f205ca9ad87c7ec89bdf8c0529c7b8df9d64dcb6
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
artifact_count = 218
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

ZIP entries:

```text
218 indexed payloads
+ artifact-index.json
= 219
```

Recompute independently.

---

# 2. M12AO provenance / local-only policy

Reported:

```text
branch =
codex/20260911-business-delta-validation-convergence-m12ao

base integration head =
0af990327e8b8d0c7c0d68d656bb2dcbd7035bf9

work instruction commit =
c951ca451ce1df02f379bb0d32576ba670963e77

implementation head =
4e52e41b1e3850e36b477b02cc5597550d3db131
```

At task start record actual:

```text
branch
HEAD
working tree
remote tracking state
```

This task remains LOCAL-ONLY.

Required:

```text
remote_push_count = 0
raw_model_artifact_remote_push_count = 0
main_branch_mutations = 0
main_merges = 0
deployments = 0
```

Do not request GitHub push approval.

---

# 3. M12AO deterministic implementation — freeze successful

M12AO deterministic gate:

```text
latest result integrity = PASS

M12AN FIC-FIN-06 offline replay = PASS

M12AJ FIC-FIN-02 mixed-evidence replay = PASS

M12AH 003690 absolute-state regression = PASS

FIC-FIN-05 UNCHANGED_ONLY regression = PASS

business_delta_semantic_projection_mismatch_count = 0

pre_post_delta_view_identity_mismatch_count = 0

PPE-proxy FCF safety = PASS

Stage-2 lexical regression = PASS
```

Tests:

```text
focused =
154 passed

full local =
3573 passed, 2 warnings

ruff =
PASS

git diff --check =
PASS
```

Do not reopen the business-delta convergence architecture in M12AP.

---

# 4. M12AO proof progress

The NEW M12AO fictional generation completed:

```text
6 proof-critical model calls
```

Call documents:

```text
run-1 Stage 1 context-01 = PASS
run-1 Stage 1 context-02 = PASS
run-1 Stage 2 context-01 = PASS
run-1 Stage 2 context-02 = PASS
run-2 Stage 1 context-01 = PASS
run-2 Stage 1 context-02 = FAIL
```

Across the six completed documents:

```text
rows evaluated = 24

row PASS = 23

row FAIL = 1

business-delta audit failures = 0

runtime transport failures = 0

wrapper retry = 0
```

The only row failure:

```text
FIC-FIN-05
fic_fin_05_conditional_expectation_double_counted
```

No monitored shadow calls ran.

Do not reuse or continue the stopped generation.

---

# 5. Exact M12AO FIC-FIN-05 failing result

M12AO run-2 Stage-1 FIC-FIN-05:

```text
overall_direction =
SELL

directional_balance =
4.0 : 6.0

business_thesis_change =
UNCHANGED

directional_confidence =
MEDIUM
```

Business-delta:

```text
PASS
```

Financial grounding / financial semantics / ownership:

```text
PASS
```

Only the conditional expectation independence contract failed.

---

# 6. Exact evidence set

FIC-FIN-05 evidence includes:

## E03

```text
STRUCTURAL_RISK

"A high complete debt balance and thin cash buffer reduce resilience."
```

## E05

```text
typed complete interest-bearing debt = 900
```

## E06

```text
typed cash and cash equivalents = 40
```

## E07

```text
MARKET_EXPECTATIONS

"Expectations do not appear to allow for a prolonged refinancing burden."
```

## E08

```text
DATA_QUALITY_LIMIT

"Refinancing terms and maturity concentration are not supplied."
```

Thus:

```text
current high debt + thin cash =
confirmed negative resilience evidence

prolonged refinancing burden severity =
not established

market expectation downside =
conditional on the unresolved refinancing outcome
```

---

# 7. Exact M12AO double-count use

The failing candidate used E07 in:

```text
material_directional_anchor_basis

dominant_evidence

standalone sell_driver

core_investment_judgment

market_expectation_context
```

Specifically:

```text
material_directional_anchor_basis =
E03 + E05 + E06 + E07
```

and:

```text
dominant_evidence =
confirmed high debt
+ thin cash
+ favorable refinancing expectations
```

The candidate therefore elevated E07
from contextual surprise/asymmetry evidence
into a material independent directional anchor.

That is the key error.

---

# 8. Critical historical contrast — valid SELL 6.0 can exist without double count

M12AK formal proof contained a PASS FIC-FIN-05 result:

```text
overall_direction =
SELL 4.0 : 6.0
```

with:

```text
material_directional_anchor_basis =
E03 + E05 + E06
```

E07 was NOT in:

```text
material_directional_anchor_basis

dominant_evidence
```

but E07 was used in:

```text
market_expectation_context
```

and as an:

```text
OTHER_EVIDENCE sell_driver
```

to describe downside asymmetry.

That row passed.

Therefore the frozen contract is NOT:

```text
E07 can never be discussed

or

SELL 6.0 is forbidden.
```

The contract is:

```text
conditional same-risk expectation evidence
must not be counted as an independent material corroborating anchor.
```

This distinction is authoritative for M12AP.

---

# 9. Existing prompt already states the correct rule

The Stage-1 prompt already says:

```text
Source-category independence is not economic independence.

A market-expectation claim conditional on the same materially unresolved
business or financing downside does not automatically add independent
corroboration to cross from a 5.5 lean to minimum direction.

It may describe surprise asymmetry or caution,
but a stronger bucket requires the adverse condition to be sufficiently
established or the expectation evidence to establish a distinct current
pricing/expectation risk.

Judge distinct established economic propositions,
not source categories or reference counts.
```

Therefore the root cause is NOT missing prompt prose.

Do not add the same instruction again.

The next fix must structurally expose / enforce
expectation economic independence.

---

# 10. Root-cause classification

Freeze primary root cause:

```text
MARKET_EXPECTATION_ECONOMIC_DEPENDENCY_NOT_STRUCTURALLY_ENCODED
```

Current state:

```text
model receives MARKET_EXPECTATIONS evidence

prompt describes independence concept

post-model FIC-FIN-05 hard validator can reject double counting

but no pre-model canonical evidence role tells the model/schema
whether a given expectation ref is:
- independent anchor eligible
- conditional context only
- unresolved/unknown independence
```

This is analogous to the earlier business-delta issue:
a semantic rule exists,
but is not structurally represented before generation.

---

# 11. Architecture options

Evaluate before implementation.

## Option A — add more prompt wording

Reject by default.

The rule is already explicit and was still violated stochastically.

## Option B — downgrade the double-count error to advisory only

Reject by default.

Using the same unresolved causal downside
as if it were independent corroboration can change HOLD ↔ SELL / BUY
and is decision-material.

This is not merely prose style.

## Option C — blanket-ban all MARKET_EXPECTATIONS from directional anchors

Too broad.

A genuinely distinct current pricing/expectation risk
may be economically relevant.

Do not adopt unless architecture review proves the broader rule is intended.

## Option D — canonical MarketExpectationEvidenceView
with anchor eligibility and field-use constraints

Preferred.

## Option E — separate expectation AI stage

Too broad unless Option D cannot safely represent the contract.

Select exactly one architecture before model calls.

Preferred:

```text
CANONICAL_MARKET_EXPECTATION_EVIDENCE_VIEW
```

---

# 12. Add MarketExpectationEvidenceView

Add an internal pre-model per-ticker view.

Suggested roles:

```text
INDEPENDENT_DIRECTIONAL_SUPPORT

CONDITIONAL_CONTEXT_ONLY

ALREADY_REFLECTED_OR_COUNTERWEIGHT

INDEPENDENCE_UNKNOWN

NOT_MARKET_EXPECTATION
```

Exact names may differ.

The important distinction is:

```text
anchor eligible
vs
context only.
```

---

# 13. Conservative default

For a MARKET_EXPECTATIONS evidence item:

```text
source category alone does NOT prove economic independence.
```

Preferred default:

```text
CONDITIONAL_CONTEXT_ONLY
or
INDEPENDENCE_UNKNOWN
```

unless the packet contains a distinct,
structured basis that proves current expectation/pricing information
independent from the unresolved issuer-side condition.

Do not infer independence from:
- the fact that the source domain says MARKET_EXPECTATIONS
- the number of refs
- wording intensity
- the model selecting the ref

---

# 14. Independent directional-support evidence

An expectation item may be:

```text
INDEPENDENT_DIRECTIONAL_SUPPORT
```

only when structured input establishes a distinct proposition such as:

```text
consensus / guidance / order / pricing expectation gap

current market-implied assumption with independent source basis

valuation / expectation evidence with explicit current reference

a separately established expectation axis
not conditional on the same unresolved issuer-side risk
```

Exact supported source classes must be derived from the actual repository.

Do not invent synthetic market data.

---

# 15. Conditional context-only evidence

Classify an expectation item as:

```text
CONDITIONAL_CONTEXT_ONLY
```

when its downside/upside meaning depends materially on
an issuer/business/financing condition that is itself unresolved.

FIC-FIN-05 E07 is the reference example:

```text
expectations do not allow prolonged refinancing burden

while

refinancing terms and maturity concentration are not supplied.
```

E07 may describe:

```text
surprise asymmetry
caution
what would matter if the burden materializes
```

but must not be treated as a separate established material anchor.

---

# 16. Field-use contract for CONTEXT_ONLY expectation evidence

A `CONDITIONAL_CONTEXT_ONLY` expectation ref MAY be used in:

```text
market_expectation_context

core_investment_judgment
when clearly described as conditional/asymmetry context

uncertainty/caution prose where schema semantics permit

an OTHER_EVIDENCE buy/sell driver
only as expectation asymmetry context,
not as a material independent corroborating anchor
```

It MUST NOT be used in:

```text
material_directional_anchor_basis

dominant_evidence
as an independent dominant proposition
```

Do not mechanically change the final balance
based solely on this restriction.

The model may still choose SELL/BUY 6.0
if OTHER independent/established evidence supports it.

---

# 17. Field-use contract for INDEPENDENT_DIRECTIONAL_SUPPORT

When a market-expectation ref is safely classified:

```text
INDEPENDENT_DIRECTIONAL_SUPPORT
```

it may be eligible for:

```text
material_directional_anchor_basis

dominant_evidence

buy/sell drivers

market_expectation_context

core judgment
```

subject to the normal directional evidence rules.

This preserves the project's ability to use genuinely independent
market-expectation evidence.

---

# 18. INDEPENDENCE_UNKNOWN behavior

If economic independence cannot be safely determined:

```text
do not guess anchor eligibility.
```

Preferred:

```text
context-only / conservative use
```

not:

```text
pre-model hard stop
```

unless the evidence item itself is malformed.

Unknown independence should reduce how the evidence can be counted,
not become negative evidence.

---

# 19. Structured basis, not free-text guesswork

Preferred classification inputs:

```text
source lineage

structured expectation evidence basis

explicit linked refs

safe valuation / consensus / guidance metadata

known unresolved condition refs

packet provenance
```

Do NOT make the primary classifier:

```text
keyword overlap between "refinancing" strings.
```

If current packet schemas do not contain enough structured information
to prove independence:

```text
default to context-only.
```

Document the limitation.

---

# 20. Optional dependency refs

If a small additive internal field is needed,
the architecture may use:

```text
depends_on_refs

independence_basis_refs

anchor_eligible
```

or equivalent.

Do not add user-facing schema fields.

Do not mutate persisted investment-thesis history.

This is model-input / validation metadata.

---

# 21. Model-facing evidence view

Add a compact view such as:

```text
MARKET_EXPECTATION_EVIDENCE_VIEW:
{
  E07: {
    role: CONDITIONAL_CONTEXT_ONLY,
    material_anchor_eligible: false,
    dominant_evidence_eligible: false,
    reason: SAME_UNRESOLVED_FINANCING_DOWNSIDE,
    dependency_refs: [E08]
  }
}
```

Exact shape may differ.

The model must not need to infer the field role from prose alone.

---

# 22. Dynamic field-level schema restrictions

Preferred structural enforcement:

For each ticker,
construct Stage-1 schema so that:

```text
material_directional_anchor_basis
```

may select only refs with:

```text
material_anchor_eligible = true
```

Likewise:

```text
dominant_evidence.evidence_refs
```

must exclude context-only expectation refs
when the field semantics imply dominant independent evidence.

Do not widen the external final schema.

This is a model-call schema constraint.

---

# 23. Buy/sell-driver handling

Do not blanket-remove context-only expectations from:

```text
buy_drivers
sell_drivers
```

because M12AK demonstrated a valid use:

```text
OTHER_EVIDENCE
expectation asymmetry
```

However:

```text
a context-only expectation driver
must not be reinterpreted downstream
as independent material corroboration.
```

If the existing schema can safely constrain its classification to:

```text
OTHER_EVIDENCE
```

do so.

Otherwise enforce post-model with the canonical expectation view.

No ticker-specific rule.

---

# 24. Canonical source of truth post-model

Post-model expectation-independence validation
must consume the SAME frozen MarketExpectationEvidenceView.

Do not reconstruct dependence independently from raw Korean/English prose.

Required:

```text
pre_model_expectation_view_sha256
=
post_model_validator_expectation_view_sha256
```

or equivalent object identity.

This avoids another pre/post semantic drift.

---

# 25. Exact M12AO failed-candidate replay

Revalidate the exact stopped M12AO FIC-FIN-05 candidate.

It contains E07 in:

```text
material_directional_anchor_basis

dominant_evidence
```

Expected:

```text
FAIL
```

under the new canonical view.

Preferred error:

```text
context_only_market_expectation_used_as_material_anchor
```

or equivalent generic error.

Do not rely only on the old ticker-specific error name.

The candidate remains historically invalid.

---

# 26. Exact M12AK SELL 6.0 passing replay

Revalidate the preserved M12AK FIC-FIN-05 SELL 6.0 candidate.

It used:

```text
material anchors =
E03 + E05 + E06

E07 =
market_expectation_context
+
OTHER_EVIDENCE sell driver
```

Expected:

```text
PASS
```

The new contract must NOT accidentally ban valid expectation-asymmetry context.

This is a critical positive regression.

---

# 27. FIC-FIN-05 HOLD 5.5 passing replay

Also revalidate an M12AK HOLD 4.5:5.5 result.

Expected:

```text
PASS
```

E07 context use must remain legal.

No exact balance target.

---

# 28. Generic negative fixtures

At minimum:

## EXP-INDEP-N01

```text
unresolved financing condition

market expectation conditional on that same condition

expectation ref added to material_directional_anchor_basis
→ FAIL
```

## EXP-INDEP-N02

```text
same conditional expectation ref in dominant_evidence
as independent dominant proposition
→ FAIL
```

## EXP-INDEP-N03

```text
source category = MARKET_EXPECTATIONS
with no independent basis metadata

marked independent solely because of category
→ FAIL deterministic preflight
```

## EXP-INDEP-N04

```text
context-only expectation
used as a second corroborating axis
for a minimum directional threshold
while unresolved cause remains the only causal basis
→ FAIL / contract violation
```

No numeric target rule.

---

# 29. Generic positive fixtures

At minimum:

## EXP-INDEP-P01

```text
conditional same-risk expectation

used only in market_expectation_context
→ PASS
```

## EXP-INDEP-P02

```text
conditional same-risk expectation

used as OTHER_EVIDENCE asymmetry driver

not in material anchor / dominant independent evidence
→ PASS
```

## EXP-INDEP-P03

```text
SELL 6.0 based on complete high debt + thin cash

expectation context remains conditional

expectation not material anchor
→ PASS
```

## EXP-INDEP-P04

```text
distinct structured expectation evidence
with independent basis refs
→ material anchor eligible
```

## EXP-INDEP-P05

```text
independence unknown
→ context-only
not negative evidence
```

---

# 30. Already-reflected / counterweight behavior

If the actual repository has cases where expectations already reflect
the adverse or favorable condition,
preserve the principle:

```text
expectations already reflecting stress
do not mechanically create another negative axis.
```

Likewise:

```text
strong fundamentals + already elevated expectations
do not mechanically create an extra positive axis.
```

Do not implement a score.

Do not turn this into fixed expectation weighting.

---

# 31. Business-delta convergence no-change

Required:

```text
BusinessDeltaEvidenceView unchanged

post-model-business-delta-validator-v2 unchanged

legacy raw-text eligibility re-derivation = 0

legacy raw-text direction re-derivation = 0

business_delta_semantic_projection_mismatch_count = 0

pre/post delta-view identity mismatch = 0
```

Re-run exact M12AN FIC-FIN-06 offline replay.

Expected:

```text
PASS
```

No regression.

---

# 32. PPE-proxy / FCF repair no-change

Preserve:

```text
label = cash_conversion_ocf_less_ppe

metric_refs = []

explicit not-FCF disclaimer safety

affirmative proxy-as-FCF hard failure
```

No FCF regression.

---

# 33. Stage-2 lexical no-change

Preserve:

```text
real "주가" contamination blocked

"수주가 / 발주가" false positives eliminated
```

No changes.

---

# 34. Monitoring ownership / temporal scope no-change

Preserve all completed contracts:

```text
price/timing transition ownership

financial risk temporal scope

conditional net-debt safety

financial-sector framework scope

QTD/YTD safety

WC grounding

debt completeness

ADR/security basis
```

---

# 35. Full deterministic gate before model calls

Require:

```text
latest ZIP integrity PASS

M12AO business-delta convergence regressions PASS

exact M12AO failed FIC-FIN-05 candidate → FAIL for generic expectation-anchor violation

exact M12AK SELL 6.0 candidate → PASS

exact M12AK HOLD 5.5 candidate → PASS

MarketExpectationEvidenceView built for all 8 fictional subjects

pre/post expectation view identity PASS

context-only refs excluded from material-anchor schema fields

independent-support positive fixture PASS

unknown-independence conservative fixture PASS

PPE-proxy FCF regressions PASS

Stage-2 lexical regressions PASS

monitoring ownership regressions PASS

financial temporal-scope regressions PASS

focused tests PASS

full local tests PASS

ruff PASS

git diff --check PASS

production firewall PASS

model = gpt-5.6-sol / xhigh
```

No model calls before this gate.

---

# 36. Model-facing change means NEW full fictional proof

M12AP changes:

```text
model-facing expectation evidence metadata/view

and likely Stage-1 per-ticker field-ref schema constraints
```

Therefore a NEW formal fictional proof is mandatory.

Do not reuse M12AK.

Do not continue M12AO.

---

# 37. NEW fictional proof topology

Frozen subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Topology:

```text
3 repetitions

2 contexts / repetition

4 subjects / context

Stage 1 calls = 6

Stage 2 calls = 6

total = 12

final outputs = 24
```

Model:

```text
gpt-5.6-sol / xhigh
```

No judge calls.

---

# 38. Fictional hard-stop policy

Hard stop only for:

```text
runtime/schema hard failure

invalid evidence identity

objective financial semantic failure

PPE-proxy FCF true violation

business-delta capability violation

business-delta canonical direction contradiction

market-expectation anchor-eligibility violation

pre/post expectation-view mismatch

price/technical/supply Core contamination

Stage-2 actual contamination

core mutation

aggregate finalization failure
```

Do NOT stop for:

```text
valid primary threshold variation

valid delta materiality variation

new-buyer variation

holder variation

confidence variation
```

Collect all 24 outputs if hard gates pass.

---

# 39. FIC-FIN-05 formal acceptance

Across all repetitions require:

```text
business_thesis_change = UNCHANGED

high complete debt + thin cash may be negative material anchors

stable operating profit remains counterevidence

refinancing terms / maturity concentration remain Unknown

E07 conditional expectation may describe asymmetry

E07 must NOT be used as independent material anchor
while classified CONTEXT_ONLY
```

Directional balance is NOT frozen.

Allowed raw outcomes may still include:

```text
HOLD 4.5:5.5

SELL 4.0:6.0
```

if each is semantically justified without expectation double count.

---

# 40. Fictional expectation audit

Report for every market-expectation ref:

```text
alias

role

material_anchor_eligible

dominant_evidence_eligible

independence basis refs

dependency refs

selected fields in model output
```

Required:

```text
context_only_expectation_material_anchor_violation_count = 0

expectation_view_projection_mismatch_count = 0
```

No hidden field-use mismatch.

---

# 41. Shadow authorization

If NEW fictional hard gates pass:

```text
run a NEW full active-monitored same-packet shadow.
```

Do NOT require perfect fictional decision-material stability.

The monitored universe is exposed compatibility evidence.

---

# 42. Active monitored universe

Re-enumerate active monitored names read-only.

Last verified reference:

```text
22 active monitored stocks
```

Use actual current list.

No registration / stop mutation.

---

# 43. Same-packet shadow contract

For every active ticker:

```text
one reproducible local packet

same packet hash for:
monolithic
Stage 1
Stage 2 evidence context
```

No provider refresh.

Required:

```text
provider_source_fetches = 0
```

---

# 44. Same expectation view across architecture paths

For fair comparison,
monolithic and Stage 1 must receive semantically identical:

```text
MarketExpectationEvidenceView

material-anchor eligibility

dependency / independence basis

field-use restrictions
```

Required:

```text
monolithic_stage1_expectation_view_equality = PASS
```

Likewise preserve:

```text
monolithic_stage1_delta_view_equality = PASS
```

---

# 45. Full shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic = context_count

Stage 1 = context_count

Stage 2 = context_count

total = 3 × context_count
```

At N=22:

```text
18 model calls
```

No repetitions.

No fresh unseen names.

---

# 46. Shadow hard-stop policy

Hard stop only for:

```text
runtime/schema failure

manifest/hash mismatch

packet mismatch

invalid evidence identity

financial hard semantic failure

true PPE-proxy-as-FCF violation

business-delta capability/direction violation

market-expectation anchor-eligibility violation

pre/post expectation-view mismatch

price/technical/supply Core contamination

Stage-2 actual contamination

financial-sector misuse

ADR/security-basis violation

core mutation

production-side-effect attempt

aggregate finalization failure
```

Do not stop because monolithic and two-stage investment decisions differ.

Collect the cohort.

---

# 47. Real market-expectation inventory

For every active monitored ticker,
report:

```text
market-expectation ref count

independent directional-support count

conditional context-only count

independence-unknown count

already-reflected/counterweight count
if supported by the chosen contract
```

This is essential to judge whether the new conservative rule
is too broad in real packets.

Do not hide if most real expectation evidence lacks structured independence basis.

---

# 48. Shadow comparison taxonomy

Use the existing compatibility taxonomy:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

Add expectation-specific diagnostic labels:

```text
EXPECTATION_CONTEXT_ONLY_CORRECTION

EXPECTATION_INDEPENDENT_ANCHOR_USED

EXPECTATION_FIELD_USE_DIFFERENCE

EXPECTATION_INDEPENDENCE_METADATA_GAP
```

Neither path is automatically ground truth.

---

# 49. Primary-threshold diagnostic

For every monitored:

```text
HOLD 5.5 ↔ BUY/SELL 6.0
```

difference,
audit whether the difference is associated with:

```text
a market-expectation ref

and whether that ref is:
independent anchor eligible
or context only.
```

This will help distinguish:

```text
true adjacent economic boundary
from
expectation double-count artifacts.
```

Do not resolve the boundary in M12AP.

---

# 50. Delta / holder / new-buyer diagnostics remain observational

Also collect:

```text
business-delta materiality differences

holder HOLDABLE ↔ REVIEW

new-buyer WAIT ↔ ATTRACTIVE/AVOID
```

Do not repair those contracts here.

The purpose of M12AP is expectation independence plus a clean complete cohort.

---

# 51. Remote / main / production policy

M12AP is LOCAL-ONLY.

Required:

```text
remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Do not ask for GitHub push authorization.

---

# 52. Production side-effect firewall

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0
```

Monitoring schedules remain paused.

---

# 53. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12ap-scope-freeze

04-integrated-main-lineage-freeze

05-m12ao-fic-fin-05-failure-reproduction

06-m12ao-fic-fin-05-expectation-use-forensic

07-m12ak-fic-fin-05-sell-pass-forensic

08-m12ak-fic-fin-05-hold-pass-forensic

09-market-expectation-prompt-contract-audit

10-expectation-independence-root-cause

11-expectation-independence-architecture-options

12-expectation-independence-architecture-decision
```

---

# 54. Required expectation-view artifacts

Produce:

```text
13-market-expectation-evidence-view-contract

14-independent-directional-support-contract

15-conditional-context-only-contract

16-independence-unknown-contract

17-expectation-field-use-contract

18-expectation-material-anchor-schema-contract

19-expectation-dominant-evidence-schema-contract

20-expectation-driver-use-contract

21-pre-post-expectation-view-identity-contract
```

---

# 55. Required exact replay / fixtures

Produce:

```text
22-m12ao-failed-candidate-offline-replay

23-m12ak-sell-6-pass-offline-replay

24-m12ak-hold-5_5-pass-offline-replay

25-expectation-independence-negative-fixtures

26-expectation-independence-positive-fixtures

27-source-category-not-economic-independence-fixtures

28-independent-current-expectation-support-fixtures

29-unknown-independence-conservative-fixtures
```

---

# 56. Required frozen-regression artifacts

Produce:

```text
30-business-delta-convergence-freeze

31-m12an-fic-fin-06-delta-replay

32-ppe-proxy-fcf-safety-freeze

33-stage2-korean-lexical-freeze

34-monitoring-transition-ownership-freeze

35-financial-temporal-scope-freeze

36-financial-sector-framework-freeze

37-two-stage-ownership-freeze

38-price-timing-renderer-no-change
```

---

# 57. Required deterministic test gate

Produce:

```text
39-focused-test-results

40-full-local-test-results

41-ruff-and-diff-results

42-hosted-ci-portability-observation

43-new-fictional-model-call-gate
```

No model calls before artifact 43 PASS.

---

# 58. Required NEW fictional artifacts

Produce:

```text
44-fictional-generation-manifest

45-fictional-delta-view-manifest

46-fictional-expectation-view-manifest

47-stage1-run1-context01

48-stage1-run1-context02

49-stage2-run1-context01

50-stage2-run1-context02

51-stage1-run2-context01

52-stage1-run2-context02

53-stage2-run2-context01

54-stage2-run2-context02

55-stage1-run3-context01

56-stage1-run3-context02

57-stage2-run3-context01

58-stage2-run3-context02

59-fictional-context-hard-semantic-audit

60-fictional-market-expectation-independence-audit

61-fictional-business-delta-convergence-audit

62-fictional-ppe-proxy-fcf-safety-audit

63-fictional-stage2-language-audit

64-fictional-final-composition-audit

65-fictional-aggregate-finalization-audit

66-fictional-primary-direction-diagnostic

67-fictional-business-delta-materiality-diagnostic

68-fictional-new-buyer-diagnostic

69-fictional-holder-diagnostic

70-fictional-core-immutability-audit

71-fictional-runtime-audit

72-fictional-shadow-gate-decision
```

---

# 59. Required shadow setup artifacts

If artifact 72 authorizes shadow:

```text
73-task-start-active-monitored-universe

74-shadow-packet-inventory

75-shadow-packet-hash-manifest

76-shadow-delta-view-manifest

77-shadow-expectation-view-manifest

78-shadow-frozen-context-manifest

79-shadow-batching-manifest

80-shadow-model-call-gate
```

---

# 60. Required full shadow artifacts

Produce:

```text
81-shadow-monolithic-model-artifacts

82-shadow-stage1-model-artifacts

83-shadow-stage2-model-artifacts

84-shadow-context-hard-semantic-audit

85-shadow-market-expectation-independence-audit

86-shadow-business-delta-convergence-audit

87-shadow-ppe-proxy-fcf-safety-audit

88-shadow-stage2-language-audit

89-shadow-final-composition-audit

90-shadow-aggregate-finalization-audit

91-shadow-per-ticker-comparison

92-shadow-core-direction-differences

93-shadow-business-delta-differences

94-shadow-new-buyer-differences

95-shadow-holder-differences

96-shadow-same-direction-calibration-differences

97-shadow-expectation-context-only-corrections

98-shadow-potential-architecture-regressions

99-shadow-unresolved-review-required

100-shadow-financial-sector-audit

101-shadow-adr-security-basis-audit

102-shadow-cyclical-valuation-audit

103-shadow-core-immutability-audit

104-shadow-runtime-audit

105-shadow-aggregate-summary

106-shadow-architecture-decision
```

Preserve raw model artifacts locally only.

---

# 61. Required combined diagnostics

If full shadow completes:

```text
107-fic-fin-05-vs-monitored-primary-boundary-analogs

108-expectation-independence-vs-primary-threshold-analysis

109-fic-fin-02-vs-monitored-delta-materiality-analogs

110-fic-fin-06-vs-monitored-positive-delta-analogs

111-fic-fin-08-vs-monitored-holder-analogs

112-new-buyer-monolithic-vs-two-stage-analogs

113-real-expectation-independence-lessons

114-combined-fictional-monitored-root-cause-summary

115-next-bounded-policy-decision
```

---

# 62. Required completion artifacts

Produce:

```text
116-market-expectation-independence-success-decision

117-business-delta-convergence-preservation-decision

118-new-fictional-proof-success-decision

119-full-shadow-completion-decision

120-existing-monitored-impact-summary

121-two-stage-shadow-compatibility-decision

122-fresh-real-proof-readiness-decision

123-final-main-merge-readiness-note

124-production-no-change

125-schedule-pause-observation

126-remote-push-prohibition-audit

127-master-workflow-update

128-program-completion
```

---

# 63. Full fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

context-only expectation material-anchor violations = 0

expectation view projection mismatches = 0

business-delta capability violations = 0

business-delta semantic projection mismatches = 0

business-delta canonical direction contradictions = 0

PPE-proxy FCF violations = 0

not-FCF disclaimer false rejects = 0

Stage-2 language false positives = 0

financial hard semantic failures = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material variance alone does not block shadow.

---

# 64. Full shadow acceptance

Require:

```text
all planned shadow calls complete

all active tickers represented exactly once

aggregate finalization PASS

expectation-view monolithic/Stage1 equality = PASS

context-only expectation material-anchor violations = 0

expectation-view projection mismatch = 0

business-delta semantic projection mismatch = 0

true business-delta direction contradiction = 0

true PPE-proxy-as-FCF violation = 0

Stage-2 actual contamination = 0

invalid refs = 0

financial hard semantic failure = 0

price/technical/supply Core contamination = 0

financial-sector misuse = 0

ADR/security-basis failure = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0

production side effects = 0
```

Investment-decision differences are measured,
not hard failures.

---

# 65. Fresh-real / main / production readiness

At M12AP completion:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY
```

Even if fictional and shadow complete.

The next task should be the bounded:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

using the complete clean fictional + monitored cohort.

No fresh unseen proof inside M12AP.

---

# 66. Failure handling

## A. Expectation independence cannot be determined without brittle text heuristics

Use conservative context-only default.

If even that cannot be integrated safely:

```text
next_scope =
MARKET_EXPECTATION_EVIDENCE_PROVENANCE_ARCHITECTURE_REVIEW
```

No model calls.

## B. New contract bans the M12AK valid SELL 6.0 candidate

```text
STOP
EXPECTATION_INDEPENDENCE_REPAIR_TOO_RESTRICTIVE
```

## C. New contract allows the exact M12AO double-count candidate

```text
STOP
EXPECTATION_INDEPENDENCE_REPAIR_TOO_PERMISSIVE
```

## D. Business-delta convergence regresses

```text
STOP
BUSINESS_DELTA_CONVERGENCE_REGRESSION
```

## E. NEW fictional hard semantics fail

Do not run shadow.

Use the smallest failing contract.

## F. Full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

## G. Full shadow reveals systematic architecture regressions

Use the appropriate bounded compatibility review instead.

---

# 67. Program-completion fields

Include at least:

```text
base_integration_head_sha

integration_branch

latest_result_zip_sha256
latest_result_integrity

m12ao_failure_ticker
m12ao_failure_error

market_expectation_independence_root_cause

market_expectation_view_contract_version

market_expectation_ref_count

expectation_independent_directional_support_count
expectation_conditional_context_only_count
expectation_independence_unknown_count
expectation_already_reflected_counterweight_count

context_only_expectation_material_anchor_violation_count
expectation_view_projection_mismatch_count
pre_post_expectation_view_identity_mismatch_count

m12ao_failed_candidate_offline_replay_status
m12ak_sell6_candidate_offline_replay_status
m12ak_hold55_candidate_offline_replay_status

business_delta_semantic_projection_mismatch_count
pre_post_delta_view_identity_mismatch_count
legacy_raw_text_direction_rederivation_count
legacy_raw_text_eligibility_rederivation_count

ppe_proxy_fcf_safety_regression_count
stage2_language_safety_regression_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
expectation_model_view_change_count

investment_judgment_model_target
investment_judgment_reasoning_effort

fictional_generation_id

fictional_stage1_model_calls
fictional_stage2_model_calls
fictional_model_calls_total

fictional_stage1_row_count
fictional_stage2_row_count
fictional_final_composition_count

fictional_expectation_anchor_violation_count
fictional_expectation_view_projection_mismatch_count

fictional_business_delta_capability_violation_count
fictional_business_delta_semantic_projection_mismatch_count
fictional_business_delta_direction_contradiction_count

fictional_ppe_proxy_fcf_violation_count
fictional_stage2_language_false_positive_count

fictional_primary_direction_unstable_subject_count
fictional_business_delta_materiality_variance_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count

fictional_core_mutation_count

fictional_runtime_timeout_count
fictional_runtime_orphan_count
fictional_wrapper_retry_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_market_expectation_ref_count
shadow_expectation_independent_support_count
shadow_expectation_context_only_count
shadow_expectation_unknown_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status

shadow_expectation_anchor_violation_count
shadow_expectation_view_projection_mismatch_count

shadow_business_delta_semantic_projection_mismatch_count
shadow_business_delta_direction_contradiction_count

shadow_ppe_proxy_fcf_violation_count
shadow_stage2_language_false_positive_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_financial_sector_framework_failure_count
shadow_adr_security_basis_failure_count
shadow_cyclical_valuation_framework_failure_count

shadow_core_mutation_after_stance_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything unmeasured:

```text
NOT_MEASURED
```

---

# 68. Artifact integrity

Freeze all local artifacts before the final artifact index.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final ZIP SHA-256.

Do not push report/raw artifacts to GitHub.

---

# 69. Final task principle

M12AO fixed the intended business-delta architecture.

The new failure demonstrates a parallel issue in market expectations:

```text
the prompt already says
source-category independence != economic independence,

but the model can still place a market-expectation ref
that depends on an unresolved refinancing downside into:

material_directional_anchor_basis
and
dominant_evidence,

making the same underlying risk look like an independent second axis.
```

The historical contrast is decisive:

```text
valid M12AK SELL 6.0:
high debt + thin cash = material anchors
E07 expectation = asymmetry context
→ PASS

invalid M12AO SELL 6.0:
high debt + thin cash + E07 expectation = material anchors
→ double-count FAIL
```

The correct M12AP flow is:

```text
keep the investment-balance target unfrozen

→ structurally classify market-expectation evidence
   as independent-support vs context-only

→ constrain context-only expectations from material-anchor fields

→ let them still describe surprise asymmetry/caution

→ make post-model validation consume the same canonical expectation view

→ preserve all completed business-delta / FCF / lexical / ownership fixes

→ start a brand-new full fictional proof

→ if hard semantics pass,
   start a brand-new full monitored shadow

→ only after the clean complete cohort,
   review the remaining genuine boundary / delta-materiality / holder policies
```

Do NOT:

```text
force FIC-FIN-05 to HOLD

force FIC-FIN-05 to SELL

ban market expectations entirely

count source categories as independent evidence

add another equivalent prompt paragraph

downgrade genuine double counting to harmless calibration

post-hoc rewrite the model balance

resume the stopped M12AO generation

stitch prior shadow generations

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring
```

Market expectations may matter.
They just cannot become a second independent fact
when they are conditional on the same unresolved first fact.
