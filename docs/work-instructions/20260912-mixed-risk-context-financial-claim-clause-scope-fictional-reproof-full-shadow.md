# Thesis Monitor — Mixed Risk-Context Financial Claim Clause Scope + New Full Fictional Proof + Full Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260912-mixed-risk-context-financial-claim-clause-scope-fictional-reproof-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260912-mixed-risk-context-financial-claim-clause-scope-fictional-reproof-full-shadow-report.zip
```

Master-workflow phase:

```text
M12AV — Integrated-Main Mixed Risk-Context Temporal Scope
         A. Freeze M12AU fictional-case FCF semantic alignment
         B. Freeze M12AT configured-signal field ownership
         C. Reproduce exact FIC-FIN-01/02/04 future-net-debt false rejects
         D. Make financial-framework claim temporal scope clause-local
         E. Preserve current-net-debt and current financial claim hard safety
         F. Start a NEW full 8 × 3 two-stage fictional proof
         G. If hard gates pass, run NEW full active-monitored same-packet shadow
         H. Hand off the complete clean cohort to boundary / delta-materiality / holder policy review
```

M12AU successfully completed its intended repair:

```text
FIC-FIN-01 legacy global FCF token checker removed

generic claim-local financial semantic validator is authoritative

exact M12AT FIC-FIN-01 replay = PASS

future configured FCF condition = PASS

true PPE-proxy-as-FCF negative control = FAIL as required

unsupported current FCF negative control = FAIL as required

configured-signal current-driver fencing preserved
```

A NEW full fictional proof was then correctly started because
M12AT had changed model-facing current-driver ref eligibility
and that architecture still required formal proof.

The new M12AU proof stopped after the first Stage-1 context:

```text
completed model calls = 1 / 12

schema rows = 4 / 4 PASS

semantic rows:
FIC-FIN-01 = FAIL
FIC-FIN-02 = FAIL
FIC-FIN-03 = PASS
FIC-FIN-04 = FAIL

common error:
net_debt_claim_without_complete_net_debt_evidence
```

The failing rows all use a mixed `risk_context` shape:

```text
current structural/business risk clause
+
future configured financial-risk clause
```

The same future net-debt concept inside `business_reevaluation_down`
is already correctly accepted.

The remaining defect is therefore a financial-claim temporal-scope
classification problem inside a mixed `risk_context`.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260912-fictional-case-fcf-prospective-scope-alignment-full-proof-full-shadow-report.zip
```

Verified SHA-256:

```text
e11cb387cdf4fbcf8c95985f8c52406ff6f8d6b4cf709e9db3411e24e1aa5e66
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification from the delivered bundle:

```text
artifact_count = 175

ZIP entries including artifact-index = 176

missing = 0
extra = 0
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute independently.

---

# 2. M12AU status

M12AU status:

```text
BLOCKED_AFTER_1_OF_12_FICTIONAL_CALLS
```

Generation:

```text
20260911-m12ai-fictional-20260912T125928Z-9f7d58c7d8aa
```

Model/runtime:

```text
model = gpt-5.6-sol
reasoning = xhigh

completed Stage-1 calls = 1
completed Stage-2 calls = 0

monitored shadow calls = 0

wrapper retry = 0
provider fetch = 0
remote push = 0
main merge = 0
deployment = 0
production sends = 0
```

Do NOT continue or selectively rerun the stopped generation.

---

# 3. M12AU intended FCF repair — freeze successful

M12AU selected:

```text
ONE_GENERIC_CLAIM_LOCAL_FCF_SEMANTIC_ENGINE
```

and removed the obsolete:

```text
FIC-FIN-01 global text contains "FCF" / "잉여현금흐름"
→ strong_quality_proxy_mislabeled_fcf
```

case rule.

Preserve:

```text
future configured FCF condition = legal

not-FCF disclaimer = legal

actual PPE-proxy-as-FCF attribution = hard FAIL

unsupported current FCF claim = hard FAIL
```

Do not reintroduce a ticker-specific FCF scanner.

---

# 4. M12AT configured-signal field ownership — freeze

Preserve:

```text
CURRENT_DIRECTIONAL_DRIVER_ONLY_WITH_CONFIGURED_REF_FENCING
```

Configured-only strengthen/weaken/invalidation refs:

```text
may be used in:
business_reevaluation_up/down
prospective risk context
invalidation / confirmation conditions

must not be used as:
current buy driver
current sell driver
material directional anchor
dominant current evidence
```

unless separate safe current evidence proves fulfillment.

No field-ownership changes in M12AV.

---

# 5. Exact failed FIC-FIN-01 risk context

FIC-FIN-01 `risk_context`:

```text
refs:
fictional:FIC-FIN-01:structural-risk
m12at:FIC-FIN-01:invalidate
m12at:FIC-FIN-01:weaken

text:
"경쟁에 따른 가격결정력 압박이 잠재 위험이며,
 향후 구조적 현금유출이나
 현금창출력·순부채의 동반 악화는
 핵심 훼손 조건이다."
```

Semantic structure:

```text
Clause A:
current structural/business risk
= pricing-power pressure

Clause B:
future/prospective configured financial risk
= future structural cash outflow
  or cash-generation + net-debt deterioration
  as a thesis-damage condition
```

The net-debt clause does NOT assert:

```text
current net debt is high
current net debt increased
current net debt is already deteriorating
```

---

# 6. Exact failed FIC-FIN-02 risk context

```text
"운전자본 흡수가 지속될 가능성이 있으며,
 향후 구조적 현금유출이나
 현금창출력·순부채의 동반 악화는
 핵심 위험 조건이다."
```

Again:

```text
current/ongoing working-capital risk clause
+
future configured net-debt/cash-generation risk clause
```

The future net-debt clause is prospective.

---

# 7. Exact failed FIC-FIN-04 risk context

```text
"비영업 지원이 반복되지 않을 가능성이 있으며,
 향후 구조적 현금유출이나
 현금창출력·순부채의 동반 악화는
 핵심 위험 조건이다."
```

Again:

```text
current structural/quality uncertainty
+
future configured financial-risk condition.
```

No current net-debt assertion.

---

# 8. Critical FIC-FIN-03 contrast

FIC-FIN-03 in the SAME context passed.

Its risk context:

```text
"단일 분기의 수익성이 지속적 회복을 보장하지 않으며,
 향후 구조적 현금유출이나
 현금창출력·순부채의 동반 악화가 확인되면
 위험이 커진다."
```

The key lexical difference:

```text
"확인되면"
```

matches the current generic conditional-language rule.

Therefore the net-debt claim is classified as future/conditional
and does not require current complete net-debt evidence.

This is strong evidence that the failing rows are temporal-scope
classification failures, not true net-debt misuse.

---

# 9. `business_reevaluation_down` is already correct

All four rows also contain forms such as:

```text
"현금창출력 감소와 순부채 확대가 함께 확인되면
 사업 판단을 하향 재평가한다."
```

Those claims are already classified as:

```text
FUTURE_REEVALUATION_CONDITION
```

and do not cause the hard failure.

Do not modify the future reevaluation field contract.

The remaining defect is specifically the mixed `risk_context`.

---

# 10. Current implementation defect

Current financial claim-role logic conceptually does:

```text
if recognized explicit conditional syntax:
    classify future/configured

elif all evidence refs are configured-only
and risk_context:
    classify prospective risk

elif risk_context has current magnitude / fulfilled wording:
    current

elif risk_context matches prospective-risk wording
and ALL refs are configured-only:
    prospective

else:
    UNKNOWN_OR_AMBIGUOUS
```

For the failed rows:

```text
"향후 ... 핵심 위험/훼손 조건이다"
```

is not recognized by the narrow conditional matcher,
and the `risk_context` refs are mixed:

```text
one current structural-risk ref
+
configured weaken/invalidation refs
```

Therefore:

```text
_claim_has_only_configured_prospective_evidence = false
```

and the net-debt framework claim becomes:

```text
UNKNOWN_OR_AMBIGUOUS
or current-like application
```

which requires complete current net-debt evidence.

This is too coarse.

---

# 11. Root-cause classification

Freeze the root cause as:

```text
MIXED_RISK_CONTEXT_FINANCIAL_CLAIM_SCOPE_IS_FIELD_LEVEL
INSTEAD_OF_LOCAL_FINANCIAL_CLAUSE_LEVEL
```

Secondary manifestation:

```text
EXPLICIT_FUTURE_NOMINAL_RISK_CONSTRUCTION
("향후 ... 위험/훼손 조건")
IS_NOT RECOGNIZED AS PROSPECTIVE
UNLESS ALL FIELD REFS ARE CONFIGURED-ONLY.
```

This is not:

```text
a model prompt problem

a configured-signal field-ownership failure

a net-debt completeness policy failure

a BusinessDeltaEvidenceView failure.
```

---

# 12. Do NOT solve this by adding one token to the global conditional regex

Forbidden primary repair:

```text
add "향후" to _CONDITIONAL_FINANCIAL_LANGUAGE
and classify the whole row as future.
```

Why unsafe:

```text
"향후에도 현재 순부채가 높다"
contains future wording
but still makes a current magnitude assertion.

"현재 순부채가 높고 향후 더 증가하면 위험하다"
contains both a current claim and a future condition.
```

A whole-field future label would hide genuine current claims.

M12AV must classify the financial concept at local clause/span scope.

---

# 13. Do NOT solve by "any configured ref means prospective"

Forbidden:

```text
if risk_context has any configured signal ref
→ all financial claims are prospective.
```

Mixed risk contexts intentionally contain both:

```text
current structural risk
and
future configured risk.
```

A single configured ref must not immunize unrelated current claims.

---

# 14. Preferred architecture

Preferred:

```text
CLAUSE_LOCAL_FINANCIAL_FRAMEWORK_TEMPORAL_SCOPE
```

For each financial framework occurrence:

```text
1. identify the local clause/span containing the financial concept

2. classify that local clause:
   CURRENT
   FUTURE_CONFIGURED
   PROSPECTIVE_RISK
   INVALIDATION
   UNKNOWN

3. bind/compare available evidence roles relevant to the framework

4. require current complete evidence only for current/ambiguous claims.
```

Do not replace the whole financial framework system.

Refine the claim granularity.

---

# 15. Local financial clause/span model

Introduce an internal representation such as:

```text
FinancialFrameworkClaimSpan
```

or extend existing `FrameworkClaim`.

Suggested fields:

```text
framework

field_path

full_field_text

local_clause_text

start

end

bound_evidence_refs

temporal_role

current_magnitude_detected

current_fulfillment_detected

future_scope_detected

configured_support_refs
```

Exact shape may follow repository conventions.

No user-facing schema change.

---

# 16. Clause boundaries

Use a bounded deterministic clause splitter.

At minimum handle:

```text
.
,
;
:
newline

그리고
이며
하지만
그러나
반면
다만

and
but
however
while
```

only as safely required.

Do not build a full Korean parser.

For the exact fictional rows,
the financial clause must isolate:

```text
"향후 구조적 현금유출이나
 현금창출력·순부채의 동반 악화는
 핵심 위험/훼손 조건이다"
```

from the preceding current structural-risk clause.

---

# 17. Future/prospective cues

Within the LOCAL financial clause,
recognize bounded future/prospective constructions.

Examples:

```text
향후

미래

앞으로

...하면

...되면

...확인되면

...경우

...시에

...위험 조건

...훼손 조건

...재평가 조건

risk if

future risk

would weaken

if/when/unless
```

Do not rely on future cue alone if current magnitude/fulfillment is explicit.

---

# 18. Current-claim override

The following must remain current,
even if future language also appears nearby:

```text
"현재 순부채가 높다."

"순부채가 이미 증가했다."

"현재 순부채 부담이 높고
 향후 더 증가하면 위험하다."

"net debt is currently high,
 and would be worse if it rises further."
```

Required:

```text
current magnitude / fulfilled assertion
has precedence for the current clause.
```

If the same field contains both current and future net-debt claims:

```text
extract/classify both claims separately
or
fail closed if deterministic separation is unsafe.
```

Do not label the whole field prospective.

---

# 19. Configured-signal support

For a prospective financial clause,
configured signal evidence is supporting context.

Use the frozen:

```text
ConfiguredSignalEvidenceView
```

to identify refs originating from:

```text
strengthen signal
weaken signal
invalidation signal
```

Do not rederive configured source roles from free text.

Where needed,
derive the framework concepts referenced by configured signals
using the existing canonical financial-framework detector.

Example:

```text
configured weaken signal:
"FCF 감소와 순부채 증가가 동반"

→ contains:
cash-flow/FCF concept
net-debt concept
```

This can support a local future net-debt risk clause.

---

# 20. Mixed ref field is allowed

`risk_context` may legitimately have:

```text
current structural-risk ref

+

configured future weaken/invalidation refs
```

The presence of the current structural ref does not make
the local future net-debt clause current.

Likewise the presence of configured refs
does not make the local current structural clause future.

Temporal role belongs to the local claim,
not the whole evidence-ref array.

---

# 21. Evidence binding in mixed risk context

Do not require perfect one-ref-per-clause mapping
if the schema only has one risk-context ref array.

But require enough deterministic evidence to support safe classification:

For a prospective net-debt clause:

```text
future/prospective local wording

AND

at least one configured weaken/invalidation ref
that references the relevant financial concept
or a clearly generic prospective-risk contract.
```

If no relevant configured support exists:

```text
UNKNOWN_OR_AMBIGUOUS
```

and fail closed for current-evidence safety.

Do not guess.

---

# 22. Exact FIC-FIN-01 expected replay

After repair:

```text
risk_context net-debt local clause =
PROSPECTIVE_RISK_SCENARIO

business_reevaluation_down net-debt claim =
FUTURE_REEVALUATION_CONDITION

current net-debt claim count =
0

net_debt_claim_without_complete_net_debt_evidence =
0

row status =
PASS
```

The current structural pricing-power risk clause remains current risk context.

No claim loss.

---

# 23. Exact FIC-FIN-02 expected replay

Expected:

```text
working-capital / cash-conversion current risk
remains current/contextual

future net-debt deterioration clause =
PROSPECTIVE_RISK_SCENARIO

net-debt completeness false reject =
0

row status =
PASS
```

Do not reclassify current working-capital context as future
merely because it shares the field.

---

# 24. Exact FIC-FIN-04 expected replay

Expected:

```text
non-operating/current quality uncertainty
remains current

future cash-generation/net-debt deterioration clause
=
PROSPECTIVE_RISK_SCENARIO

net-debt false reject =
0

row status =
PASS
```

---

# 25. FIC-FIN-03 positive regression

The already-valid:

```text
"... 순부채의 동반 악화가 확인되면 ..."
```

must remain:

```text
future/prospective

PASS
```

No regression.

---

# 26. Current-net-debt negative fixtures

At minimum:

## RISK-SCOPE-N01

```text
risk_context:
"현재 순부채가 높다."

no complete current net-debt evidence
→ FAIL
```

## RISK-SCOPE-N02

```text
"순부채가 이미 증가했다."
→ FAIL without complete comparable evidence
```

## RISK-SCOPE-N03

```text
"현재 순부채가 높고,
 향후 더 증가하면 위험하다."
→ current claim remains detectable and FAILS
```

## RISK-SCOPE-N04

```text
current net-debt clause
+
unrelated configured signal ref
→ current claim still FAILS
```

## RISK-SCOPE-N05

```text
future word "향후"
but clause says:
"향후에도 현재 순부채가 높다"
→ current magnitude remains current
```

---

# 27. Prospective positive fixtures

At minimum:

## RISK-SCOPE-P01

```text
"향후 순부채가 증가하면 하향 재평가한다."
configured weaken ref
→ PASS
```

## RISK-SCOPE-P02

```text
"향후 현금창출력·순부채의 동반 악화는 핵심 위험 조건이다."
configured weaken ref
→ PASS
```

## RISK-SCOPE-P03

```text
"순부채 증가 위험을 모니터링한다."
configured risk ref
no current assertion
→ PASS as prospective
```

## RISK-SCOPE-P04

```text
current non-financial structural-risk clause
+
future net-debt configured-risk clause
in same risk_context
→ PASS
```

## RISK-SCOPE-P05

```text
English:
"competitive pressure remains a current risk,
 while future deterioration in cash generation
 and net debt would be a key thesis risk."
→ future net-debt clause PASS
```

---

# 28. Same-framework mixed current/future fixture

Required:

```text
"현재 순부채가 높고,
 향후 더 증가하면 논리를 하향 재평가한다."
```

The classifier must produce conceptually:

```text
Claim A:
CURRENT net debt high

Claim B:
FUTURE additional net-debt increase
```

Current claim requires current complete evidence.

Future claim does not.

If the implementation cannot safely split this:

```text
UNKNOWN_OR_AMBIGUOUS
→ fail closed
```

Do not silently label both future.

---

# 29. Current magnitude precedence

Preserve/strengthen:

```text
현재
이미
지금
높은
과도한
부담이 크다
증가했다
악화됐다
```

and English equivalents.

These must override a general future-risk reading
for the local clause where they actually assert current state.

No future-word loophole.

---

# 30. Business-reevaluation field no-change

Re-run:

```text
business_reevaluation_down:
"현금창출력 감소와 순부채 확대가 함께 확인되면 ..."
```

Expected:

```text
FUTURE_REEVALUATION_CONDITION
PASS
```

No change.

---

# 31. Configured-signal field ownership no-change

Preserve M12AT:

```text
configured-only ref in current sell_driver → FAIL

configured-only ref in current buy_driver → FAIL

configured-only ref in material anchor → FAIL

configured-only ref in dominant current evidence → FAIL

configured-only ref in business_reevaluation → PASS

configured-only ref in prospective risk_context → PASS
```

The M12AV repair must not weaken current-driver fencing.

---

# 32. FCF semantics no-change

Preserve M12AU/M12AR/M12AN:

```text
future configured FCF condition → PASS

not-FCF disclaimer → PASS

true PPE-proxy-as-FCF → FAIL

unsupported current FCF → FAIL
```

No changes.

---

# 33. Business-delta no-change

Preserve:

```text
BusinessDeltaEvidenceView single source

UNCHANGED_ONLY

AI_JUDGMENT

no raw-text eligibility re-derivation

no raw-text direction re-derivation
```

No changes.

---

# 34. Market-expectation no-change

Preserve:

```text
MarketExpectationEvidenceView

conditional context-only expectation
cannot become independent material anchor
```

No changes.

---

# 35. Financial-sector scope no-change

Preserve:

```text
insurance industrial-framework exclusion
including connective "배제하고"
```

No changes.

---

# 36. Stage-2 lexical no-change

Preserve:

```text
real 주가 contamination detection

수주가 / 발주가 false-positive avoidance
```

No changes.

---

# 37. Model-facing semantic impact

Preferred M12AV repair is:

```text
post-model financial framework claim extraction / temporal classification only.
```

Default required:

```text
model prompt semantic change count = 0

model schema semantic change count = 0

configured-signal model-view change count = 0

BusinessDeltaEvidenceView change count = 0

MarketExpectationEvidenceView change count = 0

financial evidence projection change count = 0
```

However:

```text
a NEW formal fictional proof is STILL required
```

because M12AT introduced model-facing configured-signal ref fencing
and no complete formal proof has yet been achieved.

Do not reuse M12AQ.

---

# 38. Deterministic offline replay gate

Before new model calls:

```text
exact M12AU FIC-FIN-01 replay → PASS

exact M12AU FIC-FIN-02 replay → PASS

exact M12AU FIC-FIN-04 replay → PASS

FIC-FIN-03 prior PASS → remains PASS

same-field current + future net-debt negative fixture → current claim detected

future net-debt condition fixtures → PASS

configured-signal current-driver fencing regressions → PASS

M12AU FCF regressions → PASS

M12AS UNCHANGED regressions → PASS

M12AP expectation regressions → PASS

M12AQ financial-sector regressions → PASS

M12AO business-delta regressions → PASS

QTD/YTD/WC/debt/ADR safety → PASS

focused tests → PASS

full local tests → PASS

ruff → PASS

git diff --check → PASS

production firewall → PASS
```

No model calls before all deterministic gates pass.

---

# 39. NEW formal fictional proof

Create:

```text
NEW generation ID
```

Model:

```text
gpt-5.6-sol

reasoning = xhigh
```

Topology:

```text
8 subjects

2 contexts

4 subjects/context

3 repetitions

Stage 1 calls = 6

Stage 2 calls = 6

total = 12

final composed outputs = 24
```

No judge calls.

No selective rerun.

---

# 40. Fictional hard-stop policy

Hard stop only for:

```text
runtime/schema failure

invalid evidence identity

true current financial evidence completeness failure

mixed risk-context temporal-scope violation

configured-signal field-ownership violation

configured-signal false fulfillment

true PPE-proxy-as-FCF violation

unsupported current FCF claim

business-delta capability/direction violation

market-expectation anchor violation

financial-sector scope violation

price/technical/supply Core contamination

Stage-2 actual contamination

ADR/security-basis failure

core mutation

aggregate finalization failure
```

Do NOT hard-stop on:

```text
safe future net-debt configured conditions

safe future FCF configured conditions

valid decision-material variance.
```

---

# 41. Formal fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

mixed-risk future net-debt false reject = 0

current net-debt false accept = 0

configured-only current-driver violation = 0

configured-signal false fulfillment = 0

future configured FCF false reject = 0

true proxy-as-FCF violation = 0

unsupported current FCF false accept = 0

business-delta semantic mismatch = 0

expectation anchor violation = 0

financial-sector false reject = 0

Stage-2 lexical false positive = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material instability does not block shadow.

---

# 42. Shadow authorization

If formal fictional hard gates pass:

```text
run a NEW full active-monitored same-packet shadow.
```

Do not require perfect direction/delta/stance stability.

---

# 43. Active monitored universe

Re-enumerate active monitored names read-only at shadow start.

Last verified reference:

```text
22 active names
```

Use actual current set.

No monitoring membership mutation.

---

# 44. Same-packet contract

For every active ticker:

```text
one reproducible local frozen packet

same packet hash for:
monolithic
Stage 1
Stage 2
```

No provider refresh.

Required:

```text
provider_source_fetches = 0
```

---

# 45. Full shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic calls = context_count
Stage 1 calls = context_count
Stage 2 calls = context_count

total = 3 × context_count
```

At N=22:

```text
18 calls
```

No repetitions.

No fresh unseen issuers.

---

# 46. Shadow hard-stop policy

Hard stop only for:

```text
runtime/schema failure

manifest/hash mismatch

packet mismatch

invalid evidence identity

true current financial semantic failure

mixed risk-context temporal-scope violation

configured-signal field-use violation

false configured fulfillment

true PPE-proxy-as-FCF violation

unsupported current FCF claim

business-delta violation

market-expectation anchor violation

financial-sector scope violation

Stage-2 actual contamination

ADR/security-basis failure

core mutation

production-side-effect attempt

aggregate finalization failure
```

Do not hard-stop on architecture decision differences.

Collect the cohort.

---

# 47. Shadow mixed-risk audit

For every `risk_context` containing financial concepts,
report:

```text
ticker

full risk_context text

framework

local financial clause/span

temporal role

current magnitude detected?

current fulfillment detected?

future/prospective cue detected?

configured supporting refs

requires current evidence?

validation result
```

This is mandatory to prove the fix is not over-permissive.

---

# 48. Shadow comparison taxonomy

Use existing compatibility taxonomy:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

Neither path is automatically ground truth.

---

# 49. Complete compatibility goal

M12AV must produce:

```text
one final comparison row per active ticker

aggregate finalization PASS

all decision-material differences reviewed.
```

Do not introduce boundary/delta/holder policy changes mid-generation.

---

# 50. Policy handoff after complete shadow

If the full shadow completes without systematic architecture regression:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

Use:

```text
NEW M12AV formal fictional proof
+
M12AV complete monitored shadow
```

Focus on:

```text
1. remaining adjacent primary-direction differences

2. FIC-FIN-02 / FIC-FIN-06 delta materiality

3. FIC-FIN-08 holder HOLDABLE / REVIEW

4. new-buyer stance differences with stable core

5. whether configured-signal fencing
   and expectation-independence contracts
   removed prior synthetic/real boundary noise.
```

---

# 51. Local-only / production firewall

M12AV is LOCAL-ONLY.

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Do not request GitHub push authorization.

Monitoring schedules remain paused.

---

# 52. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12av-scope-freeze

04-integrated-main-lineage-freeze

05-m12au-fic-fin-01-netdebt-failure-reproduction

06-m12au-fic-fin-02-netdebt-failure-reproduction

07-m12au-fic-fin-04-netdebt-failure-reproduction

08-fic-fin-03-pass-contrast-forensic

09-risk-context-financial-role-code-audit

10-mixed-risk-context-root-cause

11-financial-claim-clause-scope-options

12-financial-claim-clause-scope-decision
```

---

# 53. Required clause-scope artifacts

Produce:

```text
13-financial-framework-claim-span-contract

14-local-financial-clause-segmentation-contract

15-current-vs-future-local-role-contract

16-risk-context-mixed-claim-contract

17-current-magnitude-precedence-contract

18-configured-signal-framework-support-contract

19-mixed-ref-risk-context-contract

20-same-framework-current-and-future-contract
```

---

# 54. Required exact replay / fixture artifacts

Produce:

```text
21-m12au-fic-fin-01-exact-offline-replay

22-m12au-fic-fin-02-exact-offline-replay

23-m12au-fic-fin-04-exact-offline-replay

24-fic-fin-03-pass-regression

25-risk-context-current-netdebt-negative-fixtures

26-risk-context-prospective-netdebt-positive-fixtures

27-risk-context-same-framework-mixed-fixtures

28-business-reevaluation-netdebt-regression
```

---

# 55. Required frozen-regression artifacts

Produce:

```text
29-m12au-fcf-case-semantic-freeze

30-m12at-configured-signal-field-ownership-freeze

31-m12as-unchanged-claim-scope-freeze

32-m12ar-fcf-claim-scope-freeze

33-m12aq-financial-sector-scope-freeze

34-m12ap-expectation-independence-freeze

35-m12ao-business-delta-convergence-freeze

36-m12an-ppe-proxy-label-freeze

37-m12am-stage2-lexical-freeze

38-monitoring-transition-ownership-freeze

39-qtd-ytd-wc-debt-safety-freeze

40-adr-security-basis-freeze

41-two-stage-ownership-freeze

42-price-timing-renderer-no-change
```

---

# 56. Required model-facing impact artifacts

Produce:

```text
43-model-prompt-semantic-hash-freeze

44-model-schema-semantic-hash-freeze

45-configured-signal-view-semantic-hash-freeze

46-business-delta-view-semantic-hash-freeze

47-expectation-view-semantic-hash-freeze

48-financial-evidence-projection-semantic-hash-freeze

49-model-facing-no-change-decision
```

Expected:

```text
NO_MODEL_FACING_SEMANTIC_CHANGE
```

for the M12AV repair itself.

The NEW formal proof is still required because
M12AT field-fencing has not yet completed a whole formal proof.

---

# 57. Required deterministic test gate

Produce:

```text
50-focused-test-results

51-full-local-test-results

52-ruff-and-diff-results

53-hosted-ci-portability-observation

54-new-fictional-model-call-gate
```

No model calls before artifact 54 PASS.

---

# 58. Required NEW fictional artifacts

Produce:

```text
55-fictional-generation-manifest

56-fictional-configured-signal-view-manifest

57-fictional-delta-view-manifest

58-fictional-expectation-view-manifest

59-stage1-run1-context01

60-stage1-run1-context02

61-stage2-run1-context01

62-stage2-run1-context02

63-stage1-run2-context01

64-stage1-run2-context02

65-stage2-run2-context01

66-stage2-run2-context02

67-stage1-run3-context01

68-stage1-run3-context02

69-stage2-run3-context01

70-stage2-run3-context02

71-fictional-context-hard-semantic-audit

72-fictional-mixed-risk-context-audit

73-fictional-configured-signal-field-use-audit

74-fictional-fcf-claim-scope-audit

75-fictional-business-delta-audit

76-fictional-market-expectation-audit

77-fictional-financial-sector-audit

78-fictional-stage2-language-audit

79-fictional-final-composition-audit

80-fictional-aggregate-finalization-audit

81-fictional-primary-direction-diagnostic

82-fictional-delta-materiality-diagnostic

83-fictional-new-buyer-diagnostic

84-fictional-holder-diagnostic

85-fictional-core-immutability-audit

86-fictional-runtime-audit

87-fictional-shadow-gate-decision
```

---

# 59. Required shadow setup artifacts

If artifact 87 authorizes shadow:

```text
88-task-start-active-monitored-universe

89-shadow-packet-inventory

90-shadow-packet-hash-manifest

91-shadow-configured-signal-view-manifest

92-shadow-delta-view-manifest

93-shadow-expectation-view-manifest

94-shadow-frozen-context-manifest

95-shadow-batching-manifest

96-shadow-model-call-gate
```

---

# 60. Required full shadow artifacts

Produce:

```text
97-shadow-monolithic-model-artifacts

98-shadow-stage1-model-artifacts

99-shadow-stage2-model-artifacts

100-shadow-context-hard-semantic-audit

101-shadow-mixed-risk-context-audit

102-shadow-configured-signal-field-use-audit

103-shadow-fcf-claim-scope-audit

104-shadow-business-delta-audit

105-shadow-market-expectation-audit

106-shadow-financial-sector-audit

107-shadow-stage2-language-audit

108-shadow-final-composition-audit

109-shadow-aggregate-finalization-audit

110-shadow-per-ticker-comparison

111-shadow-core-direction-differences

112-shadow-business-delta-differences

113-shadow-new-buyer-differences

114-shadow-holder-differences

115-shadow-same-direction-calibration-differences

116-shadow-expected-contract-corrections

117-shadow-potential-architecture-regressions

118-shadow-unresolved-review-required

119-shadow-adr-security-basis-audit

120-shadow-cyclical-valuation-audit

121-shadow-core-immutability-audit

122-shadow-runtime-audit

123-shadow-aggregate-summary

124-shadow-architecture-decision
```

---

# 61. Required combined diagnostics

If full shadow completes:

```text
125-fic-fin-05-vs-monitored-primary-boundary-analogs

126-fic-fin-02-vs-monitored-delta-materiality-analogs

127-fic-fin-06-vs-monitored-positive-delta-analogs

128-fic-fin-08-vs-monitored-holder-analogs

129-new-buyer-monolithic-vs-two-stage-analogs

130-real-mixed-risk-context-lessons

131-configured-signal-field-use-real-lessons

132-combined-fictional-monitored-root-cause-summary

133-next-bounded-policy-decision
```

---

# 62. Required completion artifacts

Produce:

```text
134-mixed-risk-context-scope-repair-success-decision

135-configured-signal-field-ownership-preservation-decision

136-new-fictional-proof-success-decision

137-full-shadow-completion-decision

138-existing-monitored-impact-summary

139-two-stage-shadow-compatibility-decision

140-fresh-real-proof-readiness-decision

141-final-main-merge-readiness-note

142-production-no-change

143-schedule-pause-observation

144-remote-push-prohibition-audit

145-master-workflow-update

146-program-completion
```

---

# 63. Formal fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

mixed-risk future net-debt false reject = 0

current net-debt false accept = 0

configured-only current-driver violation = 0

configured-signal false fulfillment = 0

future configured FCF false reject = 0

true proxy-as-FCF violation = 0

unsupported current FCF false accept = 0

business-delta semantic projection mismatch = 0

expectation anchor violation = 0

financial-sector false reject = 0

Stage-2 lexical false positive = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material variance does not block shadow.

---

# 64. Full shadow acceptance

Require:

```text
all planned shadow calls complete

all active tickers represented exactly once

aggregate finalization PASS

mixed-risk future financial false reject = 0

current financial false accept = 0

configured-signal field-use violation = 0

configured-signal false fulfillment = 0

true PPE-proxy-as-FCF violation = 0

unsupported current FCF false accept = 0

business-delta violation = 0

expectation anchor violation = 0

financial-sector scope error = 0

Stage-2 actual contamination = 0

invalid refs = 0

ADR/security-basis failure = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0

production side effects = 0
```

Investment-decision differences are measured,
not hard failures.

---

# 65. Fresh-real / main / production readiness

At M12AV completion:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY
```

Even if the full cohort completes.

The next task should consume the complete clean cohort for:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

No fresh unseen proof inside M12AV.

---

# 66. Failure handling

## A. Exact FIC-FIN-01/02/04 risk contexts still false-reject

```text
next_scope =
FINANCIAL_FRAMEWORK_CLAIM_SPAN_ARCHITECTURE_REVIEW
```

No model calls.

## B. Repair lets current net-debt claims pass because the field contains future wording

```text
STOP
MIXED_RISK_SCOPE_REPAIR_TOO_PERMISSIVE
```

## C. Configured-signal current-driver fencing regresses

```text
STOP
CONFIGURED_SIGNAL_FIELD_OWNERSHIP_REGRESSION
```

## D. FCF safety regresses

```text
STOP
FCF_SAFETY_REGRESSION
```

## E. NEW fictional hard semantic failure

Do not run shadow.

Use the smallest failing contract.

## F. Full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

---

# 67. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch

latest_result_zip_sha256
latest_result_integrity

m12au_failed_tickers
m12au_failure_error

mixed_risk_context_root_cause

financial_framework_claim_span_contract_version

local_financial_clause_enabled

current_magnitude_precedence_enabled

configured_framework_support_binding_enabled

m12au_fic_fin_01_replay_status
m12au_fic_fin_02_replay_status
m12au_fic_fin_04_replay_status
fic_fin_03_regression_status

mixed_risk_future_netdebt_false_reject_count
current_netdebt_false_accept_count
same_field_current_future_ambiguous_count

configured_signal_field_ownership_regression_count
configured_signal_false_fulfillment_count

fcf_safety_regression_count
business_delta_regression_count
expectation_regression_count
financial_sector_regression_count
stage2_lexical_regression_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
configured_signal_view_change_count
business_delta_view_change_count
expectation_view_change_count
financial_evidence_projection_change_count

investment_judgment_model_target
investment_judgment_reasoning_effort

fictional_generation_id

fictional_stage1_model_calls
fictional_stage2_model_calls
fictional_model_calls_total

fictional_stage1_row_count
fictional_stage2_row_count
fictional_final_composition_count

fictional_mixed_risk_false_reject_count
fictional_current_financial_false_accept_count

fictional_configured_signal_current_driver_violation_count
fictional_configured_signal_false_fulfillment_count

fictional_proxy_as_fcf_violation_count
fictional_unsupported_current_fcf_false_accept_count

fictional_business_delta_violation_count
fictional_expectation_anchor_violation_count
fictional_financial_sector_violation_count
fictional_stage2_language_false_positive_count

fictional_primary_direction_unstable_subject_count
fictional_business_delta_materiality_variance_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count

fictional_core_mutation_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status

shadow_mixed_risk_false_reject_count
shadow_current_financial_false_accept_count

shadow_configured_signal_field_violation_count
shadow_configured_signal_false_fulfillment_count

shadow_proxy_as_fcf_violation_count
shadow_unsupported_current_fcf_false_accept_count

shadow_business_delta_violation_count
shadow_expectation_anchor_violation_count
shadow_financial_sector_violation_count
shadow_stage2_language_false_positive_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything unmeasured:

```text
NOT_MEASURED
```

---

# 68. Artifact integrity

Freeze all local artifacts before the final artifact index.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final ZIP SHA-256.

Do not push report/raw artifacts to GitHub.

---

# 69. Final task principle

M12AU successfully removed the obsolete FIC-FIN-01 global FCF case checker.

The next hard stop is a distinct mixed-field temporal-scope defect:

```text
risk_context contains:

current non-financial structural risk
+
future configured financial risk

The current validator classifies temporal scope
at too coarse a field/ref level.

Because not ALL risk_context refs are configured-only,
a future clause such as:

"향후 현금창출력·순부채의 동반 악화는 핵심 위험 조건이다"

falls to:
UNKNOWN / current-evidence-required

and triggers:
net_debt_claim_without_complete_net_debt_evidence.
```

FIC-FIN-03 proves the intended semantics:

```text
the same concept with "확인되면"
is already recognized as future and passes.
```

The correct M12AV flow is:

```text
keep current net-debt completeness hard

→ classify the financial concept at local clause/span scope

→ separate current structural-risk clause
  from future configured financial-risk clause

→ preserve current-magnitude precedence

→ use configured-signal roles as supporting context,
  not whole-field immunity

→ offline replay FIC-FIN-01/02/04 and FIC-FIN-03

→ start a completely NEW full 12-call fictional proof

→ if all hard gates pass,
   run a completely NEW full active-monitored shadow

→ finally hand the complete clean cohort
   to the real boundary / delta-materiality / holder policy review
```

Do NOT:

```text
add "향후" as a whole-field magic exception

treat any configured ref as field-wide future immunity

weaken current net-debt safety

reopen configured-signal driver fencing

reopen BusinessDeltaEvidenceView

reopen MarketExpectationEvidenceView

reopen FCF semantics

resume the stopped M12AU generation

stitch prior partial shadows

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring
```

Temporal scope belongs to the financial claim,
not to the entire mixed risk field.
