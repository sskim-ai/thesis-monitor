# Thesis Monitor — Configured FCF Prospective Support Mapping + New Full Fictional Proof + Full Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260913-configured-fcf-prospective-support-mapping-fictional-reproof-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260913-configured-fcf-prospective-support-mapping-fictional-reproof-full-shadow-report.zip
```

Master-workflow phase:

```text
M12AY — Integrated-Main Configured FCF Support Mapping
         A. Freeze successful M12AX monitoring-obligation repair
         B. Freeze M12AW nominal-condition and M12AV clause-local scope
         C. Reproduce exact M12AX 4-row unsupported-current-FCF failure
         D. Add configured-support financial concept mapping for true FCF
         E. Keep OCF / OCF-PPE distinct from FCF
         F. Preserve current unsupported-FCF hard safety
         G. Offline re-audit M12AX/M12AV/M12AW completed proof evidence
         H. Start a NEW full 8 × 3 two-stage fictional proof
         I. If hard gates pass, run NEW full active-monitored same-packet shadow
         J. Hand off the complete clean cohort to boundary / delta-materiality / holder policy review
```

M12AX successfully repaired the Korean monitoring-obligation language
it was designed to address.

Before the new model call:

```text
exact M12AW FIC-FIN-01 replay = PASS

exact M12AW FIC-FIN-02 replay = PASS

exact M12AW FIC-FIN-04 replay = PASS

FIC-FIN-03 monitoring control = PASS

M12AV completed 40-row replay = 40 / 40 PASS

monitoring-obligation positive fixtures = PASS

current-claim negative fixtures = PASS

no-configured-support fail-closed fixtures = PASS

M12AW nominal-condition regressions = PASS

M12AT configured-signal field ownership = PASS

M12AU/M12AR/M12AN FCF safety = PASS
```

A NEW formal proof was then started.

The first Stage-1 context returned:

```text
transport = PASS
schema = 4 / 4 PASS

FIC-FIN-01 = FAIL
FIC-FIN-02 = FAIL
FIC-FIN-03 = FAIL
FIC-FIN-04 = FAIL

common error =
unsupported_current_fcf_claim
```

All four rows used explicit future/configured FCF language:

```text
"향후 FCF 감소와 순부채 증가가 ... 확인되면 ..."
```

Their corresponding configured weaken evidence already contains:

```text
statement =
"FCF 감소와 순부채 증가가 동반"

metric_refs =
["FCF"]

configured_signal.source_role =
WEAKEN_SIGNAL

configured_signal.fulfillment_state =
CONFIGURED_ONLY

risk_context_eligible =
true

current_directional_driver_eligible =
false
```

The current generic FCF row validator creates a claim with:

```text
framework = free_cash_flow
```

and asks whether framework-relevant configured support exists.

The configured-support matcher delegates to:

```text
financial_frameworks_in_text(...)
```

but that vocabulary does not expose:

```text
free_cash_flow
```

for true `FCF / free cash flow / 잉여현금흐름`.

Therefore a future FCF claim cannot find the configured signal
that explicitly names FCF,
and the claim falls back to current/unsupported:

```text
unsupported_current_fcf_claim
```

M12AY must add the missing support-concept mapping
without turning OCF or OCF-PPE into FCF.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260913-korean-prospective-monitoring-obligation-scope-fictional-reproof-full-shadow-report.zip
```

Verified SHA-256:

```text
d4b40b0951524571f34b73b409105980a4871c8ae4ef69f1cd56f6f9daef7b1d
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
artifact_count = 357

ZIP entries including artifact-index = 358

hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute independently.

---

# 2. M12AX status

M12AX status:

```text
BLOCKED_AFTER_1_OF_12_FICTIONAL_CALLS
```

Generation:

```text
20260911-m12ai-fictional-20260912T230529Z-284ef4f9f72b
```

Runtime:

```text
model = gpt-5.6-sol
reasoning = xhigh

completed calls = 1 / 12

Stage 1 rows = 4

all 4 rows =
schema PASS
semantic FAIL

shadow calls = 0 / 18

timeout = 0
orphan = 0
wrapper retry = 0
selective rerun = 0
post-freeze hotfix = 0
```

Common failure:

```text
unsupported_current_fcf_claim
```

Do NOT continue or stitch the stopped generation.

---

# 3. M12AX monitoring-obligation repair — freeze successful

Preserve:

```text
prospective-financial-monitoring-obligation-v1
```

Supported forms include bounded:

```text
감시해야 한다
모니터링해야 한다
주시해야 한다
추적해야 한다
점검해야 한다
monitor whether
should be monitored
needs monitoring
```

with:

```text
configured framework-relevant support required

current magnitude precedence

current fulfillment precedence

current numeric precedence

no-support fail closed
```

Do not reopen this language contract.

---

# 4. M12AW nominalized-condition repair — freeze

Preserve:

```text
prospective-financial-condition-nominalization-v1
```

Examples:

```text
"순부채 증가의 확인은 하향 조건이다."
→ prospective with configured support

"순부채 증가가 확인됐다."
→ current

"순부채가 증가했고 이는 하향 조건이다."
→ current
```

No changes.

---

# 5. M12AV clause-local scope — freeze

Preserve:

```text
financial-framework-claim-span-v1
```

Temporal role belongs to:

```text
local financial claim/span
```

not the whole mixed `risk_context`.

No changes.

---

# 6. M12AT configured-signal field ownership — freeze

Preserve:

```text
CURRENT_DIRECTIONAL_DRIVER_ONLY_WITH_CONFIGURED_REF_FENCING
```

Configured-only signals:

```text
may be used in future reevaluation / prospective risk / invalidation

may NOT be current buy/sell drivers

may NOT be current material anchors

may NOT be dominant current evidence

may NOT self-certify fulfillment.
```

No changes.

---

# 7. Exact M12AX FIC-FIN-01 failure

`risk_context`:

```text
"경쟁이 가격결정력을 좁힐 수 있으며,
 향후 FCF 감소와 순부채 증가의 동반 또는
 지속적인 음의 영업현금흐름이 확인되면
 사업 논리 훼손을 재검토해야 한다."
```

Refs include:

```text
current structural risk

configured weaken:
"FCF 감소와 순부채 증가가 동반"

configured invalidation:
persistent negative OCF
```

The `net_debt` part is no longer the failing contract.

Failure:

```text
unsupported_current_fcf_claim
```

because the FCF row cannot establish
framework-relevant configured support.

---

# 8. Exact M12AX FIC-FIN-02 failure

`risk_context`:

```text
"운전자본 흡수가 지속될 수 있으며,
 향후 FCF 감소와 순부채 증가의 동반 또는
 구조적인 음의 영업현금흐름이 확인되면
 하방 재평가가 필요하다."
```

The configured weaken evidence explicitly names:

```text
FCF
```

but FCF support matching fails.

Same error:

```text
unsupported_current_fcf_claim
```

---

# 9. Exact M12AX FIC-FIN-03 failure

`risk_context`:

```text
"단일 흑자 분기가 지속적 회복을 입증하지 못하며,
 향후 FCF 감소와 순부채 증가 또는
 구조적인 음의 영업현금흐름이 확인되면
 사업 논리 훼손을 검토해야 한다."
```

This row had passed previous net-debt temporal tests.

It now fails only because:

```text
free_cash_flow configured support is unavailable to the matcher.
```

This confirms the defect is generic,
not tied to one prior false-reject form.

---

# 10. Exact M12AX FIC-FIN-04 failure

`risk_context`:

```text
"금융효과가 반복되지 않을 수 있으며,
 향후 FCF 감소와 순부채 증가의 동반 또는
 지속적인 음의 영업현금흐름이 확인되면
 사업 논리 훼손을 재검토해야 한다."
```

Again:

```text
configured future FCF concept exists

but configured-support concept mapping
cannot expose free_cash_flow.
```

---

# 11. Exact structured configured signal

For FIC-FIN-01 the prompt already exposes:

```text
label =
"설정된 미래 논리 조건"

statement =
"FCF 감소와 순부채 증가가 동반"

metric_refs =
["FCF"]

configured_signal =
{
  source_role: WEAKEN_SIGNAL,
  fulfillment_state: CONFIGURED_ONLY,
  current_directional_driver_eligible: false,
  future_reevaluation_eligible: true,
  risk_context_eligible: true,
  material_anchor_eligible: false,
  dominant_evidence_eligible: false
}
```

This is strong structured evidence that the configured signal
contains an FCF concept.

Do not ignore it and re-infer only from prose.

---

# 12. Current support-matching defect

Current logic conceptually:

```text
_claim_has_framework_relevant_configured_evidence(
    claim.framework,
    configured evidence
)

→ financial_frameworks_in_text(label + statement)

→ compare claim.framework membership
```

The candidate FCF validation creates:

```text
claim.framework = "free_cash_flow"
```

But the support detector's existing framework vocabulary
is built around concepts such as:

```text
net_debt
working_capital
non_operating_interest
```

and does not expose:

```text
free_cash_flow
```

for:

```text
FCF
free cash flow
잉여현금흐름
```

Thus:

```text
framework_relevant_configured = false
```

even though the configured signal explicitly says FCF.

---

# 13. Root-cause classification

Freeze:

```text
CONFIGURED_FINANCIAL_SUPPORT_CONCEPT_VOCABULARY
OMITS_FREE_CASH_FLOW
WHILE FCF CURRENTNESS VALIDATOR USES FRAMEWORK_FREE_CASH_FLOW
```

or exact code-audited equivalent.

This is NOT:

```text
a monitoring-obligation bug

a nominal-condition bug

a clause-splitting bug

a configured-signal field-ownership bug

a model output error.
```

---

# 14. Do NOT simply add FCF to the general industrial-framework scanner

Primary forbidden repair:

```text
add FCF / free cash flow to the same _FRAMEWORKS map
used by general financial-framework application claims
```

without proving downstream effects.

Why risky:

The general framework scanner feeds logic for:

```text
framework application

financial-sector generic-framework misuse

application_count

net-debt / working-capital framework roles

sector exclusion semantics
```

FCF is not analogous to:

```text
industrial net-debt framework

industrial working-capital framework.
```

Blindly adding FCF there could:

```text
create new FrameworkClaim rows everywhere

change bank/insurance application counts

change field-role semantics

duplicate generic FCF claim validation.
```

M12AY should separate:

```text
configured financial support concepts
```

from:

```text
general framework application claims
```

unless code audit proves a unified representation is safe.

---

# 15. Preferred architecture

Preferred:

```text
CONFIGURED_FINANCIAL_CONCEPT_SUPPORT
```

Create one canonical helper such as:

```text
configured_financial_concepts_for_evidence(ref)
```

or:

```text
financial_support_concepts_in_evidence(ref)
```

that returns a bounded set of support concepts.

At minimum:

```text
free_cash_flow
net_debt
working_capital
non_operating_interest
```

where justified by existing contracts.

Use existing framework detection for existing concepts
and add a narrowly defined true-FCF concept mapping.

Do not change candidate framework scanning unless required.

---

# 16. Structured evidence first

For configured evidence,
prefer structured metadata when available.

For:

```text
metric_refs = ["FCF"]
```

map:

```text
FCF
→ free_cash_flow
```

This mapping is authoritative only for the configured-support concept.

Do NOT infer:

```text
OCF
→ free_cash_flow

operating_cash_flow
→ free_cash_flow

ocf_less_ppe_capex
→ free_cash_flow

PPE cash outflow
→ free_cash_flow
```

These are different concepts.

---

# 17. Text fallback for explicit FCF vocabulary

When structured metric refs are unavailable,
bounded explicit text may support `free_cash_flow` only for:

```text
FCF

free cash flow

free-cash-flow

잉여현금흐름
```

Do NOT use vague:

```text
현금흐름

현금창출

cash flow

cash generation
```

alone as FCF.

Fail closed.

---

# 18. OCF remains distinct

Configured evidence:

```text
"영업현금흐름 감소"
metric_refs = ["OCF"]
```

must NOT satisfy:

```text
claim.framework = free_cash_flow
```

It may support an:

```text
operating_cash_flow
```

concept if/when that contract exists,
but not FCF.

No silent substitution.

---

# 19. OCF-PPE proxy remains distinct

Configured or current evidence involving:

```text
ocf_less_ppe_capex

OCF less PPE

현금전환 대용치
```

must NOT satisfy:

```text
free_cash_flow
```

unless a separate authoritative management-defined FCF evidence contract exists.

Preserve:

```text
ppe_only_not_management_defined_fcf
```

This is a hard requirement.

---

# 20. FCF configured support does not prove current FCF

Even if configured evidence maps to:

```text
free_cash_flow
```

its fulfillment state remains:

```text
CONFIGURED_ONLY
```

until current evidence independently proves fulfillment.

Therefore:

```text
configured FCF concept
can support FUTURE temporal classification

but cannot support:
"current FCF decreased"
"current FCF is negative"
"FCF is X"
```

No current-state promotion.

---

# 21. Exact FCF future classification

For:

```text
risk_context:
"향후 FCF 감소와 순부채 증가가 함께 확인되면 ..."
```

with configured weaken signal:

```text
statement = "FCF 감소와 순부채 증가가 동반"
metric_refs = ["FCF"]
```

Expected:

```text
free_cash_flow configured support = true

net_debt configured support = true

FCF claim temporal role =
PROSPECTIVE_RISK_SCENARIO

net-debt claim temporal role =
PROSPECTIVE_RISK_SCENARIO

unsupported_current_fcf_claim = 0

net_debt completeness false reject = 0
```

---

# 22. `business_reevaluation_down` remains future

For the same configured signal:

```text
business_reevaluation_down:
"향후 FCF 감소와 순부채 증가가 함께 확인되면 ..."
```

Expected:

```text
FUTURE_REEVALUATION_CONDITION
```

No current FCF evidence required.

No regression.

---

# 23. Current FCF assertion remains hard

These MUST fail without safe current FCF evidence:

```text
"현재 FCF가 감소했다."

"FCF가 이미 악화됐다."

"현재 FCF는 음수다."

"FCF는 100이다."

"FCF 감소가 확인됐다."
```

Even if the candidate also cites a configured weaken signal.

Configured support cannot convert a current assertion into future.

---

# 24. Current FCF + future condition in same field

Example:

```text
"현재 FCF가 감소했고,
 향후 추가 감소 시 논리를 하향 재평가한다."
```

Expected conceptual split:

```text
Claim A:
current FCF decrease
→ requires current safe FCF evidence

Claim B:
future additional FCF decrease
→ prospective
```

If deterministic separation is unsafe:

```text
fail closed.
```

Do not label the whole field future.

---

# 25. True current FCF evidence contract

Audit whether repository has any safe current evidence
whose canonical metric is truly:

```text
free_cash_flow
or
management-defined/reporting-defined FCF
```

If yes:

```text
current FCF assertions may be supported by that evidence
under existing basis/period rules.
```

If no:

```text
current FCF assertion remains unsupported.
```

Do not synthesize FCF from OCF-PPE.

---

# 26. Framework/concept naming

Do not overload names invisibly.

Preferred distinction:

```text
FrameworkClaim.framework
for framework application semantics

ConfiguredFinancialConcept
for future-signal support semantics
```

If the existing system must reuse the same string:

```text
free_cash_flow
```

document that this use is:

```text
support-concept identity
```

not:

```text
industrial framework application.
```

---

# 27. Exact M12AX first-call offline replay

After repair,
revalidate the exact four M12AX Stage-1 rows.

Required:

```text
FIC-FIN-01 = PASS
FIC-FIN-02 = PASS
FIC-FIN-03 = PASS
FIC-FIN-04 = PASS

unsupported_current_fcf_claim_count = 0

current FCF false accept = 0

configured-signal field-use violations = 0

configured false fulfillment = 0
```

Do not rewrite candidates.

---

# 28. Re-audit prior completed proof evidence

Before new model calls revalidate:

```text
M12AV completed 40 rows = 40 / 40 PASS

M12AW first-call 4 rows = 4 / 4 PASS

M12AX first-call 4 rows = 4 / 4 PASS after M12AY
```

Also preserve:

```text
M12AT configured-field fixtures

M12AR IBM FCF scope fixtures

M12AN PPE-proxy FCF fixtures
```

No regressions.

---

# 29. Required positive support fixtures

At minimum:

## FCF-SUPPORT-P01

```text
configured weaken:
statement = "FCF 감소와 순부채 증가가 동반"
metric_refs = ["FCF"]

risk:
"향후 FCF 감소가 확인되면 하향 재평가한다."

→ free_cash_flow configured support = true
→ prospective
→ PASS
```

## FCF-SUPPORT-P02

```text
configured invalidation:
"FCF가 구조적으로 감소"
metric_refs = ["FCF"]

risk:
"FCF 감소 여부를 감시해야 한다."

→ prospective monitoring
→ PASS
```

## FCF-SUPPORT-P03

```text
configured weaken:
"잉여현금흐름 감소가 지속"
no structured metric ref

risk:
"향후 잉여현금흐름 감소 시 ..."
→ bounded explicit text fallback
→ PASS
```

## FCF-SUPPORT-P04

```text
English:
configured = "free cash flow declines"
risk = "monitor whether free cash flow declines"
→ PASS
```

---

# 30. Required negative support fixtures

## FCF-SUPPORT-N01

```text
configured:
"영업현금흐름 감소"
metric_refs = ["OCF"]

risk:
"향후 FCF 감소 시 ..."
→ configured free_cash_flow support = false
→ fail closed / unsupported
```

## FCF-SUPPORT-N02

```text
configured:
"OCF less PPE declines"
metric = ocf_less_ppe_capex

risk:
"향후 FCF 감소 시 ..."
→ free_cash_flow support = false
```

## FCF-SUPPORT-N03

```text
configured:
"현금창출력 약화"
no FCF explicit vocabulary

risk:
"향후 FCF 감소 시 ..."
→ free_cash_flow support = false
```

## FCF-SUPPORT-N04

```text
configured FCF weaken signal exists

candidate:
"현재 FCF가 감소했다."
no current safe FCF evidence

→ current claim
→ FAIL
```

## FCF-SUPPORT-N05

```text
configured FCF signal

candidate:
"FCF는 100이다."
→ current numeric
→ FAIL without safe current FCF
```

---

# 31. Mixed FCF + net-debt concept fixture

Required:

```text
configured:
"FCF 감소와 순부채 증가가 동반"
metric_refs = ["FCF"]

risk:
"향후 FCF 감소와 순부채 증가가 함께 확인되면 ..."
```

Expected support concepts:

```text
free_cash_flow
net_debt
```

Expected temporal roles:

```text
FCF = prospective
net debt = prospective
```

No double counting.

No current evidence requirement.

---

# 32. Existing financial-framework application counts must not change unexpectedly

Because FCF support mapping is NOT intended to create
a new general application framework,
audit:

```text
financial-sector application_count

net-debt framework application_count

working-capital framework application_count

contrastive exclusion roles

insurance/bank framework misuse counts
```

before/after.

Expected:

```text
no unrelated count changes.
```

If adding FCF to a shared scanner changes these counts:

```text
STOP
GENERAL_FRAMEWORK_SURFACE_EXPANSION_DETECTED
```

Use a separate support-concept helper instead.

---

# 33. ConfiguredSignalEvidenceView remains source of lifecycle truth

Use:

```text
source_role

fulfillment_state

field eligibility
```

from the existing configured-signal view.

Do not create a second signal-lifecycle engine.

The new concept map only answers:

```text
what financial concept does this configured signal refer to?
```

not:

```text
is it fulfilled?
```

---

# 34. Business-delta semantics — freeze

Configured signals remain:

```text
baseline/configured context
not observed delta evidence
```

No change.

No raw-text direction/eligibility rederivation.

---

# 35. Market-expectation semantics — freeze

Preserve:

```text
MarketExpectationEvidenceView

context-only expectation material-anchor restrictions
```

No change.

---

# 36. Financial-sector semantics — freeze

Preserve:

```text
insurance/bank specialized framework rules

industrial net-debt/WC exclusion

"배제하고" connective handling
```

FCF support mapping must not alter these.

---

# 37. FCF proxy semantics — freeze

Preserve:

```text
OCF-PPE ≠ FCF

not-FCF disclaimer = safe

proxy-as-FCF attribution = hard FAIL

configured FCF concept support ≠ current FCF evidence
```

No change.

---

# 38. Monitoring/nominal/clause temporal semantics — freeze

Preserve:

```text
M12AX monitoring obligation

M12AW nominal condition

M12AV local clause scope
```

The only new capability is:

```text
free_cash_flow configured support concept matching.
```

---

# 39. No model-facing semantic change expected

Preferred M12AY repair is:

```text
post-model configured-support concept matching only.
```

Required:

```text
model_prompt_semantic_change_count = 0

model_schema_semantic_change_count = 0

configured_signal_view_change_count = 0

BusinessDeltaEvidenceView change_count = 0

MarketExpectationEvidenceView change_count = 0

financial evidence projection change_count = 0

two_stage_semantic_change_count = 0
```

If any model-facing hash changes:

```text
STOP
UNPLANNED_MODEL_SURFACE_DRIFT
```

Review before proof.

---

# 40. Deterministic gate before model calls

Require:

```text
latest ZIP integrity PASS

exact M12AX 4-row replay = 4 / 4 PASS

M12AV 40-row replay = 40 / 40 PASS

M12AW 4-row replay = 4 / 4 PASS

true FCF configured-support positives PASS

OCF-not-FCF negative support fixtures PASS

OCF-PPE-not-FCF support fixtures PASS

current unsupported FCF negative fixtures PASS

M12AX monitoring-obligation regressions PASS

M12AW nominal-condition regressions PASS

M12AV mixed-risk regressions PASS

M12AT configured-signal field-ownership regressions PASS

M12AR/M12AN FCF proxy regressions PASS

M12AS UNCHANGED regressions PASS

M12AP expectation regressions PASS

M12AQ financial-sector regressions PASS

M12AO business-delta regressions PASS

QTD/YTD / WC / debt / ADR safety PASS

focused tests PASS

full local tests PASS

ruff PASS

git diff --check PASS

production firewall PASS

model = gpt-5.6-sol / xhigh
```

No model calls before all pass.

---

# 41. NEW full fictional proof is mandatory

M12AX stopped after:

```text
1 / 12
```

formal calls.

M12AT's model-facing configured-signal field fencing
still has not completed a full formal proof.

Therefore:

```text
NEW generation ID

NEW full 12-call fictional proof
```

is mandatory.

Do NOT:

```text
continue M12AX

reuse its first-call outputs as formal proof

rerun only failed rows.
```

---

# 42. Formal proof topology

Subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Topology:

```text
3 repetitions

2 contexts / repetition

4 subjects / context

Stage 1 calls = 6

Stage 2 calls = 6

total = 12

final compositions = 24
```

Model:

```text
gpt-5.6-sol / xhigh
```

No judge calls.

No fallback.

No selective rerun.

---

# 43. Fictional hard-stop policy

Hard stop only for:

```text
runtime/schema hard failure

invalid evidence identity

configured FCF support mapping violation

true current unsupported FCF claim

PPE-proxy-as-FCF violation

current financial completeness failure

monitoring-obligation scope violation

nominal-condition scope violation

mixed-risk clause scope violation

configured-signal field-use violation

configured-signal false fulfillment

business-delta violation

market-expectation anchor violation

financial-sector scope violation

price/technical/supply Core contamination

Stage-2 actual contamination

ADR/security-basis failure

core mutation

aggregate finalization failure
```

Do NOT stop for:

```text
safe future/configured FCF condition

safe future net-debt condition

valid decision-material variance.
```

---

# 44. Formal fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

configured FCF support false negative = 0

OCF-as-FCF support false positive = 0

OCF-PPE-as-FCF support false positive = 0

current unsupported FCF false accept = 0

proxy-as-FCF violation = 0

monitoring-obligation false reject = 0

nominal-condition false reject = 0

mixed-risk false reject = 0

configured-only current-driver violation = 0

configured-signal false fulfillment = 0

business-delta violation = 0

expectation anchor violation = 0

financial-sector false reject = 0

Stage-2 lexical false positive = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material variance does not block shadow.

---

# 45. Formal diagnostics

Report across 3 repetitions:

```text
overall direction

directional balance

business delta

new-buyer stance

holder stance

confidence
```

Keep visibility into:

```text
FIC-FIN-02 delta materiality

FIC-FIN-05 primary threshold

FIC-FIN-06 delta materiality

FIC-FIN-08 holder boundary
```

Do not repair policy variance inside M12AY.

---

# 46. Shadow authorization

If formal fictional hard gates pass:

```text
run a NEW full active-monitored same-packet shadow.
```

No fresh unseen issuers.

---

# 47. Active monitored universe

Re-enumerate active monitored names read-only.

Last verified reference:

```text
22 active names
```

Use actual task-start list.

No monitoring membership mutations.

---

# 48. Same-packet shadow contract

For every active ticker:

```text
one reproducible local frozen packet

same packet hash feeds:
monolithic
Stage 1
Stage 2
```

No provider refresh.

Required:

```text
provider_source_fetches = 0
```

---

# 49. Canonical view equality

Require monolithic / Stage-1 equality for:

```text
ConfiguredSignalEvidenceView

configured financial concept support view

BusinessDeltaEvidenceView

MarketExpectationEvidenceView

financial evidence projection

sector framework
```

No valid compatibility comparison otherwise.

---

# 50. Full shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic calls = context_count

Stage 1 calls = context_count

Stage 2 calls = context_count

total = 3 × context_count
```

At N=22:

```text
18 calls
```

No repetitions.

---

# 51. Shadow hard-stop policy

Hard stop only for:

```text
runtime/schema hard failure

manifest/hash mismatch

packet mismatch

invalid evidence identity

configured FCF support mapping violation

true unsupported current FCF claim

true PPE-proxy-as-FCF violation

current financial completeness violation

monitoring/nominal/mixed-risk temporal-scope violation

configured-signal field-use violation

configured-signal false fulfillment

business-delta violation

market-expectation anchor violation

financial-sector scope violation

Stage-2 contamination

ADR/security-basis failure

core mutation

production side effect

aggregate finalization failure
```

Do not hard-stop because investment decisions differ.

---

# 52. Shadow configured-FCF support audit

For every candidate containing explicit FCF language,
report:

```text
ticker

field_path

local clause

FCF term

claim temporal role

bound refs

configured refs

configured source role

configured fulfillment state

structured metric_refs

configured support concepts

free_cash_flow support matched?

current safe FCF evidence present?

requires current evidence?

validation result
```

This is mandatory to prove
the new mapping is neither too narrow nor too permissive.

---

# 53. Shadow OCF / OCF-PPE negative audit

For every monitored candidate with:

```text
OCF

operating cash flow

ocf_less_ppe_capex

cash-conversion proxy
```

confirm:

```text
not promoted to free_cash_flow support
unless an independent explicit FCF configured signal exists.
```

Report false-positive count.

Required:

```text
0
```

---

# 54. Shadow comparison taxonomy

Use:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

Neither architecture is automatically ground truth.

---

# 55. Complete compatibility goal

M12AY must produce:

```text
one final comparison row per active ticker

all active tickers represented exactly once

aggregate finalization PASS

complete monolithic-vs-two-stage compatibility cohort.
```

Do not introduce decision policy repairs mid-generation.

---

# 56. Policy handoff after complete shadow

If full shadow completes without systematic architecture regression:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

Use:

```text
NEW M12AY formal fictional proof

+

M12AY complete monitored shadow
```

Focus:

```text
1. remaining adjacent primary-direction differences

2. FIC-FIN-02 / FIC-FIN-06 business-delta materiality

3. FIC-FIN-08 holder HOLDABLE / REVIEW boundary

4. new-buyer stance differences with stable core

5. which earlier real/fictitious differences disappeared
   after support-concept, expectation, field-ownership,
   and temporal-scope corrections.
```

No policy repair inside M12AY.

---

# 57. Local-only / production firewall

M12AY is LOCAL-ONLY.

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Do not request GitHub push authorization.

Monitoring schedules remain paused.

---

# 58. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12ay-scope-freeze

04-integrated-main-lineage-freeze

05-m12ax-unsupported-current-fcf-failure-reproduction

06-configured-fcf-structured-evidence-forensic

07-financial-support-concept-code-audit

08-general-framework-vs-support-concept-separation-audit

09-fcf-support-mapping-options

10-fcf-support-mapping-decision
```

---

# 59. Required support-concept artifacts

Produce:

```text
11-configured-financial-support-concept-contract

12-free-cash-flow-support-concept-contract

13-structured-metric-ref-priority-contract

14-explicit-fcf-text-fallback-contract

15-ocf-not-fcf-support-contract

16-ocf-less-ppe-not-fcf-support-contract

17-current-fcf-evidence-separation-contract

18-configured-lifecycle-no-fulfillment-contract

19-general-framework-application-no-change-contract
```

---

# 60. Required exact replay / fixtures

Produce:

```text
20-m12ax-fic-fin-01-exact-offline-replay

21-m12ax-fic-fin-02-exact-offline-replay

22-m12ax-fic-fin-03-exact-offline-replay

23-m12ax-fic-fin-04-exact-offline-replay

24-configured-fcf-support-positive-fixtures

25-ocf-not-fcf-support-negative-fixtures

26-ocf-ppe-not-fcf-support-negative-fixtures

27-current-unsupported-fcf-negative-fixtures

28-mixed-fcf-netdebt-configured-support-fixtures
```

---

# 61. Required prior-proof offline re-audits

Produce:

```text
29-m12av-completed-40-row-offline-reaudit

30-m12aw-first-call-four-row-offline-reaudit

31-m12ax-first-call-four-row-offline-reaudit
```

Expected:

```text
M12AV = 40 / 40 PASS

M12AW = 4 / 4 PASS

M12AX = 4 / 4 PASS after M12AY
```

Diagnostic only.

Do not stitch old proof generations.

---

# 62. Required frozen-regression artifacts

Produce:

```text
32-m12ax-monitoring-obligation-freeze

33-m12aw-nominal-condition-freeze

34-m12av-clause-local-scope-freeze

35-m12au-fcf-case-semantic-freeze

36-m12at-configured-signal-field-ownership-freeze

37-m12as-unchanged-claim-scope-freeze

38-m12ar-fcf-claim-scope-freeze

39-m12aq-financial-sector-scope-freeze

40-m12ap-expectation-independence-freeze

41-m12ao-business-delta-convergence-freeze

42-m12an-ppe-proxy-label-freeze

43-m12am-stage2-lexical-freeze

44-monitoring-transition-ownership-freeze

45-qtd-ytd-wc-debt-safety-freeze

46-adr-security-basis-freeze

47-two-stage-ownership-freeze

48-price-timing-renderer-no-change
```

---

# 63. Required model-facing impact artifacts

Produce:

```text
49-model-prompt-semantic-hash-freeze

50-model-schema-semantic-hash-freeze

51-configured-signal-view-semantic-hash-freeze

52-business-delta-view-semantic-hash-freeze

53-expectation-view-semantic-hash-freeze

54-financial-evidence-projection-semantic-hash-freeze

55-model-facing-no-change-decision
```

Expected:

```text
NO_MODEL_FACING_SEMANTIC_CHANGE
```

for the M12AY repair.

---

# 64. Required deterministic test gate

Produce:

```text
56-focused-test-results

57-full-local-test-results

58-ruff-and-diff-results

59-hosted-ci-portability-observation

60-new-fictional-model-call-gate
```

No model calls before artifact 60 PASS.

---

# 65. Required NEW fictional artifacts

Produce:

```text
61-fictional-generation-manifest

62-fictional-configured-signal-view-manifest

63-fictional-configured-financial-support-concept-manifest

64-fictional-delta-view-manifest

65-fictional-expectation-view-manifest

66-stage1-run1-context01

67-stage1-run1-context02

68-stage2-run1-context01

69-stage2-run1-context02

70-stage1-run2-context01

71-stage1-run2-context02

72-stage2-run2-context01

73-stage2-run2-context02

74-stage1-run3-context01

75-stage1-run3-context02

76-stage2-run3-context01

77-stage2-run3-context02

78-fictional-context-hard-semantic-audit

79-fictional-configured-fcf-support-audit

80-fictional-monitoring-obligation-audit

81-fictional-nominal-condition-audit

82-fictional-mixed-risk-context-audit

83-fictional-configured-signal-field-use-audit

84-fictional-fcf-claim-scope-audit

85-fictional-business-delta-audit

86-fictional-market-expectation-audit

87-fictional-financial-sector-audit

88-fictional-stage2-language-audit

89-fictional-final-composition-audit

90-fictional-aggregate-finalization-audit

91-fictional-primary-direction-diagnostic

92-fictional-delta-materiality-diagnostic

93-fictional-new-buyer-diagnostic

94-fictional-holder-diagnostic

95-fictional-core-immutability-audit

96-fictional-runtime-audit

97-fictional-shadow-gate-decision
```

---

# 66. Required shadow setup artifacts

If artifact 97 authorizes shadow:

```text
98-task-start-active-monitored-universe

99-shadow-packet-inventory

100-shadow-packet-hash-manifest

101-shadow-configured-signal-view-manifest

102-shadow-configured-financial-support-concept-manifest

103-shadow-delta-view-manifest

104-shadow-expectation-view-manifest

105-shadow-frozen-context-manifest

106-shadow-batching-manifest

107-shadow-model-call-gate
```

---

# 67. Required full shadow artifacts

Produce:

```text
108-shadow-monolithic-model-artifacts

109-shadow-stage1-model-artifacts

110-shadow-stage2-model-artifacts

111-shadow-context-hard-semantic-audit

112-shadow-configured-fcf-support-audit

113-shadow-monitoring-obligation-audit

114-shadow-nominal-condition-audit

115-shadow-mixed-risk-context-audit

116-shadow-configured-signal-field-use-audit

117-shadow-fcf-claim-scope-audit

118-shadow-business-delta-audit

119-shadow-market-expectation-audit

120-shadow-financial-sector-audit

121-shadow-stage2-language-audit

122-shadow-final-composition-audit

123-shadow-aggregate-finalization-audit

124-shadow-per-ticker-comparison

125-shadow-core-direction-differences

126-shadow-business-delta-differences

127-shadow-new-buyer-differences

128-shadow-holder-differences

129-shadow-same-direction-calibration-differences

130-shadow-expected-contract-corrections

131-shadow-potential-architecture-regressions

132-shadow-unresolved-review-required

133-shadow-adr-security-basis-audit

134-shadow-cyclical-valuation-audit

135-shadow-core-immutability-audit

136-shadow-runtime-audit

137-shadow-aggregate-summary

138-shadow-architecture-decision
```

---

# 68. Required combined diagnostics

If full shadow completes:

```text
139-fic-fin-05-vs-monitored-primary-boundary-analogs

140-fic-fin-02-vs-monitored-delta-materiality-analogs

141-fic-fin-06-vs-monitored-positive-delta-analogs

142-fic-fin-08-vs-monitored-holder-analogs

143-new-buyer-monolithic-vs-two-stage-analogs

144-real-configured-fcf-support-lessons

145-real-monitoring-obligation-lessons

146-real-configured-signal-field-use-lessons

147-combined-fictional-monitored-root-cause-summary

148-next-bounded-policy-decision
```

---

# 69. Required completion artifacts

Produce:

```text
149-configured-fcf-support-mapping-success-decision

150-monitoring-obligation-preservation-decision

151-nominal-condition-preservation-decision

152-mixed-risk-context-preservation-decision

153-configured-signal-field-ownership-preservation-decision

154-new-fictional-proof-success-decision

155-full-shadow-completion-decision

156-existing-monitored-impact-summary

157-two-stage-shadow-compatibility-decision

158-fresh-real-proof-readiness-decision

159-final-main-merge-readiness-note

160-production-no-change

161-schedule-pause-observation

162-remote-push-prohibition-audit

163-master-workflow-update

164-program-completion
```

---

# 70. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch

latest_result_zip_sha256
latest_result_integrity

m12ax_failure_error
m12ax_failed_tickers

configured_fcf_support_root_cause

configured_financial_support_concept_contract_version

free_cash_flow_support_mapping_enabled

structured_fcf_metric_ref_mapping_enabled
explicit_fcf_text_fallback_enabled

ocf_maps_to_fcf_support = false
ocf_less_ppe_maps_to_fcf_support = false

general_framework_application_surface_change_count

m12ax_four_row_replay_status
m12av_40_row_replay_status
m12aw_four_row_replay_status

configured_fcf_support_false_negative_count
ocf_as_fcf_support_false_positive_count
ocf_ppe_as_fcf_support_false_positive_count

current_unsupported_fcf_false_accept_count

monitoring_obligation_regression_count
nominal_condition_regression_count
mixed_risk_scope_regression_count
configured_signal_field_ownership_regression_count
configured_signal_false_fulfillment_count

business_delta_regression_count
expectation_regression_count
financial_sector_regression_count
stage2_lexical_regression_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
configured_signal_view_change_count
business_delta_view_change_count
expectation_view_change_count
financial_evidence_projection_change_count
two_stage_semantic_change_count

investment_judgment_model_target
investment_judgment_reasoning_effort

fictional_generation_id

fictional_stage1_model_calls
fictional_stage2_model_calls
fictional_model_calls_total

fictional_stage1_row_count
fictional_stage2_row_count
fictional_final_composition_count

fictional_configured_fcf_support_false_negative_count
fictional_ocf_as_fcf_support_false_positive_count
fictional_ocf_ppe_as_fcf_support_false_positive_count
fictional_current_unsupported_fcf_false_accept_count

fictional_proxy_as_fcf_violation_count
fictional_monitoring_obligation_false_reject_count
fictional_nominal_condition_false_reject_count
fictional_mixed_risk_false_reject_count

fictional_configured_signal_current_driver_violation_count
fictional_configured_signal_false_fulfillment_count

fictional_business_delta_violation_count
fictional_expectation_anchor_violation_count
fictional_financial_sector_violation_count
fictional_stage2_language_false_positive_count

fictional_primary_direction_unstable_subject_count
fictional_business_delta_materiality_variance_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count

fictional_core_mutation_count
fictional_timeout_count
fictional_orphan_count
fictional_wrapper_retry_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status

shadow_configured_fcf_support_false_negative_count
shadow_ocf_as_fcf_support_false_positive_count
shadow_ocf_ppe_as_fcf_support_false_positive_count
shadow_current_unsupported_fcf_false_accept_count

shadow_proxy_as_fcf_violation_count
shadow_monitoring_obligation_false_reject_count
shadow_nominal_condition_false_reject_count
shadow_mixed_risk_false_reject_count

shadow_configured_signal_field_violation_count
shadow_configured_signal_false_fulfillment_count

shadow_business_delta_violation_count
shadow_expectation_anchor_violation_count
shadow_financial_sector_violation_count
shadow_stage2_language_false_positive_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count
shadow_timeout_count
shadow_orphan_count
shadow_wrapper_retry_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything not measured:

```text
NOT_MEASURED
```

---

# 71. Fresh-real / main / production readiness

At M12AY completion:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY
```

Even if fictional and shadow complete.

The next bounded task should be:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

No fresh unseen proof inside M12AY.

---

# 72. Failure handling

## A. Exact M12AX 4-row future FCF claims still false-reject

```text
next_scope =
CONFIGURED_FINANCIAL_CONCEPT_SUPPORT_ARCHITECTURE_REVIEW
```

No model calls.

## B. OCF starts satisfying FCF support

```text
STOP
OCF_FCF_CONCEPT_COLLAPSE_REGRESSION
```

## C. OCF-PPE starts satisfying FCF support

```text
STOP
PPE_PROXY_FCF_SUPPORT_REGRESSION
```

## D. General financial-framework application counts change unexpectedly

```text
STOP
GENERAL_FRAMEWORK_SURFACE_EXPANSION_DETECTED
```

Use separate support-concept mapping.

## E. Current unsupported FCF begins passing from configured support

```text
STOP
CONFIGURED_FCF_SUPPORT_CURRENTNESS_REGRESSION
```

## F. NEW fictional hard semantic failure

Do not run shadow.

Use the smallest bounded failing contract.

## G. Full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

---

# 73. Artifact integrity

Freeze all local artifacts before final artifact index.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final ZIP SHA-256.

Do not push report/raw artifacts to GitHub.

---

# 74. Final task principle

M12AX fixed Korean monitoring-obligation language correctly.

The new failure is not another language-form problem.

The exact configured signal already says:

```text
statement:
"FCF 감소와 순부채 증가가 동반"

metric_refs:
["FCF"]

source role:
WEAKEN_SIGNAL

fulfillment:
CONFIGURED_ONLY
```

The future risk clause says:

```text
"향후 FCF 감소와 순부채 증가가 확인되면..."
```

But the configured-support matcher cannot connect them
because the support vocabulary does not expose:

```text
free_cash_flow
```

for true FCF.

The correct M12AY flow is:

```text
keep monitoring/nominal/clause temporal semantics unchanged

→ create a separate configured-financial-support concept map

→ map structured metric_refs ["FCF"]
   to free_cash_flow

→ optionally use bounded explicit FCF text fallback

→ NEVER map OCF or OCF-PPE to FCF

→ let configured FCF support prove only FUTURE relevance,
   never current fulfillment

→ prove current unsupported FCF still fails

→ offline replay all prior failed rows

→ start a completely NEW full 12-call fictional proof

→ if all hard gates pass,
   run a completely NEW full active-monitored shadow

→ finally hand the complete clean cohort
   to the actual boundary / delta-materiality / holder policy review
```

Do NOT:

```text
add FCF blindly to industrial framework application scanning

treat cash flow / 현금창출 as FCF

treat OCF as FCF

treat OCF-PPE as FCF

turn configured FCF into current FCF evidence

reopen configured-signal field ownership

reopen BusinessDeltaEvidenceView

reopen MarketExpectationEvidenceView

resume/stitch M12AX

selectively rerun only failed rows

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring
```

A configured FCF condition can support a future FCF claim.
It still does not prove that FCF has changed today.
