# Thesis Monitor — FCF Claim-Local Temporal Scope + Negated Fulfillment Polarity + New Full Fictional Proof + Full Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260913-fcf-claim-local-temporal-scope-negated-fulfillment-fictional-reproof-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260913-fcf-claim-local-temporal-scope-negated-fulfillment-fictional-reproof-full-shadow-report.zip
```

Master-workflow phase:

```text
M12AZ — Integrated-Main FCF Temporal Claim Scope
         A. Freeze successful M12AY configured-FCF support mapping
         B. Reproduce exact M12AY 4-row failure
         C. Make FCF currentness classification claim-local, not whole-field
         D. Add bounded affirmative-vs-negated current-fulfillment polarity
         E. Preserve current FCF and PPE-proxy hard safety
         F. Offline re-audit all completed M12AY/M12AV/M12AW/M12AX evidence
         G. Start a NEW full 8 × 3 two-stage fictional proof
         H. If hard gates pass, run NEW full active-monitored same-packet shadow
         I. Hand off the complete clean cohort to boundary / delta-materiality / holder policy review
```

M12AY successfully completed the configured FCF support mapping it was designed to add.

Before the new model proof:

```text
structured metric_refs ["FCF"] → free_cash_flow support = PASS

explicit FCF text fallback = PASS

OCF → free_cash_flow support = false

OCF-PPE → free_cash_flow support = false

general financial-framework application surface change = 0

M12AX 4-row replay = PASS

M12AV 40-row replay = PASS

M12AW 4-row replay = PASS

monitoring-obligation regression = 0

nominal-condition regression = 0

mixed-risk regression = 0

configured-signal field-ownership regression = 0

business-delta regression = 0

expectation regression = 0

financial-sector regression = 0

Stage-2 lexical regression = 0
```

A NEW formal fictional proof then progressed to:

```text
9 / 12 model calls

Stage 1 calls = 5
Stage 2 calls = 4

Stage 1 rows = 20
Stage 2 rows = 16

completed proof rows = 36

PASS = 32
FAIL = 4

final compositions observed = 16
```

The only hard failure occurred at:

```text
run-3 / Stage 1 / context-01
```

for all four subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
```

Common error:

```text
unsupported_current_fcf_claim
```

The configured FCF support audit itself passed:

```text
configured_fcf_support_matched = true

configured_fcf_support_false_negative_count = 0

ocf_as_fcf_support_false_positive_count = 0

ocf_ppe_as_fcf_support_false_positive_count = 0
```

The common `risk_context` shape was:

```text
<current structural/business context>.
잉여현금흐름 감소와 순부채 증가의 동반 확인은 약화 조건이고
지속적인 음의 영업현금흐름은 무효화 조건이나,
현재 충족된 사실은 아니다.
```

The current FCF validator classified that mixed field as:

```text
CURRENT_DIRECTIONAL_BASIS
```

even though the sentence explicitly says
the configured weakening/invalidation conditions are NOT currently fulfilled.

The root cause is not the configured-support map.

The remaining defect is that the specialized FCF currentness path
still classifies the WHOLE `FinancialClaimRow.text`,
whereas net-debt and other framework claims already have
local claim/span boundaries.

M12AZ must converge FCF temporal classification onto the same
claim-local semantics.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260913-configured-fcf-prospective-support-mapping-fictional-reproof-full-shadow-report.zip
```

Verified SHA-256:

```text
dc62c4a461740c9f6fe5496b753db0caab2051abbc27643f049dc075da01b05e
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
artifact_count = 338

ZIP entries including artifact-index = 339

hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute independently.

---

# 2. M12AY status

M12AY status:

```text
BLOCKED_AFTER_9_OF_12_FICTIONAL_CALLS
```

Generation:

```text
20260911-m12ai-fictional-20260913T013910Z-9c127e271105
```

Runtime:

```text
model = gpt-5.6-sol
reasoning = xhigh

completed model calls = 9

Stage 1 model calls = 5
Stage 2 model calls = 4

completed rows = 36

PASS rows = 32
FAIL rows = 4

timeout = 0
orphan = 0
wrapper retry = 0
selective rerun = 0
post-freeze hotfix = 0

shadow model calls = 0
```

Do NOT continue or stitch the stopped generation.

---

# 3. M12AY configured FCF support mapping — freeze successful

Preserve:

```text
configured-financial-support-concept-v1
```

Structured mapping:

```text
metric_refs ["FCF"]
→ free_cash_flow
```

Bounded explicit fallback:

```text
FCF
free cash flow
free-cash-flow
잉여현금흐름
→ free_cash_flow support concept
```

ONLY for configured-support matching.

Preserve hard exclusions:

```text
OCF ≠ FCF

operating_cash_flow ≠ FCF

ocf_less_ppe_capex ≠ FCF

OCF-PPE cash-conversion proxy ≠ FCF

generic "cash flow" / "현금흐름" ≠ FCF

generic "cash generation" / "현금창출" ≠ FCF
```

No changes.

---

# 4. General framework surface — freeze

M12AY correctly kept:

```text
general financial framework scanner
```

separate from:

```text
configured financial support concepts.
```

Preserve:

```text
general_framework_application_surface_change_count = 0
```

Do not add `free_cash_flow` to the industrial framework scanner
unless a new architectural review explicitly authorizes it.

---

# 5. M12AX / M12AW / M12AV temporal repairs — freeze

Preserve all existing temporal semantics:

```text
M12AV:
financial-framework-claim-span-v1
local financial clause/span

M12AW:
prospective-financial-condition-nominalization-v1

M12AX:
prospective-financial-monitoring-obligation-v1
```

Do not reopen those contracts.

---

# 6. Exact M12AY FIC-FIN-01 failure

`risk_context`:

```text
"경쟁에 따른 가격결정력 압박 가능성이 있다.
 잉여현금흐름 감소와 순부채 증가의 동반 확인은 약화 조건이고
 지속적인 음의 영업현금흐름은 무효화 조건이나,
 현재 충족된 사실은 아니다."
```

Configured refs include:

```text
weaken:
FCF 감소와 순부채 증가가 동반

invalidation:
persistent negative OCF
```

The candidate says:

```text
the FCF/net-debt weakening condition exists

the persistent negative OCF invalidation condition exists

neither is currently fulfilled.
```

It does NOT say:

```text
current FCF decreased.
```

Expected:

```text
FCF configured weakening claim = prospective

current FCF evidence required = false
```

---

# 7. Exact FIC-FIN-02 failure

`risk_context`:

```text
"운전자본 흡수가 이어질 수 있다.
 잉여현금흐름 감소와 순부채 증가의 동반 확인은 약화 조건이고
 지속적인 음의 영업현금흐름은 무효화 조건이나,
 현재 충족된 사실은 아니다."
```

Again:

```text
current WC/structural concern
+
future configured FCF/net-debt condition
+
explicit NOT-currently-fulfilled statement.
```

Expected FCF role:

```text
PROSPECTIVE_RISK_SCENARIO
```

---

# 8. Exact FIC-FIN-03 failure

`risk_context`:

```text
"최근 분기 흑자가 지속 가능한 회복을 보장하지 않는다.
 잉여현금흐름 감소와 순부채 증가의 동반 확인은 약화 조건이고
 지속적인 음의 영업현금흐름은 무효화 조건이나,
 현재 충족된 사실은 아니다."
```

Again:

```text
no current FCF deterioration assertion.
```

---

# 9. Exact FIC-FIN-04 failure

`risk_context`:

```text
"비영업 지원이 반복되지 않을 수 있다.
 잉여현금흐름 감소와 순부채 증가의 동반 확인은 약화 조건이고
 지속적인 음의 영업현금흐름은 무효화 조건이나,
 현재 충족된 사실은 아니다."
```

Again:

```text
future/configured financial conditions
explicitly denied as currently fulfilled.
```

---

# 10. Net-debt contrast proves the architectural mismatch

For the same failed rows,
the `net_debt` framework claim already has:

```text
span_contract =
financial-framework-claim-span-v1

local_clause_text =
"잉여현금흐름 감소와 순부채 증가의 동반 확인은 약화 조건"
```

The local span excludes:

```text
"현재 충족된 사실은 아니다"
```

from the net-debt framework claim.

Therefore net-debt no longer causes the hard failure.

The FCF currentness path does NOT use that local span architecture.

It constructs a synthetic FCF `FrameworkClaim` from:

```text
FinancialClaimRow.text
```

which is the ENTIRE risk-context field.

That is the convergence defect.

---

# 11. Exact current FCF implementation defect

Current specialized path conceptually:

```python
def financial_claim_row_requires_current_fcf_evidence(row, evidence_by_ref):
    claim = FrameworkClaim(
        framework="free_cash_flow",
        text=row.text,
        field_path=row.field_path,
        evidence_refs=row.bound_evidence_refs,
        role=UNRESOLVED,
    )
    return financial_claim_requires_current_evidence(claim, ...)
```

No:

```text
local_clause_text
local_clause_start
local_clause_end
full_field_text span identity
```

is supplied.

Therefore:

```text
financial_claim_role()
```

runs against the whole mixed field.

This violates the claim-local architecture already adopted for other
financial frameworks.

---

# 12. Secondary polarity defect

`financial_claim_role()` currently defines:

```text
fulfilled =
_CURRENT_FULFILLMENT_LANGUAGE.search(text)
```

and `_CURRENT_FULFILLMENT_LANGUAGE` includes bare:

```text
현재
이미
지금
```

as well as actual affirmative fulfillment verbs.

Thus the suffix:

```text
"현재 충족된 사실은 아니다"
```

contains:

```text
현재
```

and is treated as evidence of current fulfillment
even though the semantic polarity is explicitly negative.

M12AZ should repair this too,
but the primary architecture must remain claim-local.

Do NOT solve only by adding one exception string.

---

# 13. Root-cause classification

Freeze the primary root cause as:

```text
FCF_CURRENTNESS_PATH_REMAINS_FIELD_LEVEL
WHILE_OTHER_FINANCIAL_FRAMEWORKS_USE_LOCAL_CLAIM_SPANS
```

Secondary root cause:

```text
CURRENT_FULFILLMENT_MATCHER_DOES_NOT_DISTINGUISH
AFFIRMATIVE_FROM_NEGATED_CURRENT_FULFILLMENT
```

These are one bounded temporal-claim-scope task.

---

# 14. Preferred architecture

Preferred:

```text
FCF_TEMPORAL_CLAIM_SPAN
```

or generic:

```text
FinancialTemporalClaimSpan
```

that allows FCF currentness to consume the SAME local temporal semantics
as net debt / WC / non-operating framework claims.

Each FCF occurrence or FCF semantic clause should carry:

```text
field_path

full_field_text

local_clause_text

local_clause_start

local_clause_end

bound_evidence_refs

framework/support concept = free_cash_flow

configured-support refs

temporal role
```

No user-facing schema change.

---

# 15. Do NOT add FCF to the general framework application scanner

M12AZ must preserve M12AY's separation:

```text
general framework application scanning
vs
configured financial support concepts
```

The FCF temporal span may reuse the generic clause splitter
without making FCF a generic industrial framework.

Preferred:

```text
reuse clause segmentation infrastructure
but keep FCF-specific semantic identity.
```

No financial-sector application-count drift.

---

# 16. Reuse generic clause segmentation

The same deterministic clause/span boundaries used by:

```text
financial-framework-claim-span-v1
```

should be reused for FCF where possible.

Do not create a second incompatible Korean clause splitter
unless the existing splitter cannot represent FCF safely.

For the exact failed field,
the FCF claim should be isolated conceptually as:

```text
"잉여현금흐름 감소와 순부채 증가의 동반 확인은 약화 조건"
```

or another equivalent bounded span.

It must NOT inherit the entire field merely because
FCF is handled by a specialized validator.

---

# 17. Multiple FCF/OCF concepts in one risk context

The exact text also contains:

```text
"지속적인 음의 영업현금흐름은 무효화 조건"
```

This is OCF, not FCF.

The FCF specialized span must not absorb:

```text
operating_cash_flow
```

as if it were the same FCF claim.

Preserve:

```text
FCF ≠ OCF.
```

If the same field contains both concepts,
classify each independently.

---

# 18. Current-fulfillment polarity contract

Introduce a bounded internal classifier such as:

```text
CurrentFulfillmentPolarity
```

with roles:

```text
AFFIRMATIVE_CURRENT_FULFILLMENT

NEGATED_CURRENT_FULFILLMENT

CURRENT_SCOPE_MARKER_ONLY

NONE

AMBIGUOUS
```

Exact names may differ.

Do not let one regex match decide semantic polarity.

---

# 19. Negated current-fulfillment forms

Recognize bounded forms such as:

```text
"현재 충족된 사실은 아니다."

"현재 충족된 상태가 아니다."

"현재 충족된 것은 아니다."

"현재 확인된 사실은 아니다."

"아직 충족되지 않았다."

"아직 확인되지 않았다."

"현재까지 충족되지 않았다."

"현재까지 확인되지 않았다."

"not currently fulfilled"

"is not currently fulfilled"

"has not been fulfilled"

"has not been met"

"is not currently confirmed"

"has not been confirmed"
```

These are NOT affirmative fulfillment.

---

# 20. Current-scope marker is not automatically fulfillment

Preferred semantic cleanup:

```text
현재 / 지금 / already / now
```

should be treated as:

```text
CURRENT_SCOPE_MARKER
```

not by itself:

```text
AFFIRMATIVE_CURRENT_FULFILLMENT.
```

Actual currentness must still be established by:

```text
current magnitude

current numeric claim

affirmative change/fulfillment predicate

current-state assertion that fails closed.
```

If a broad refactor is risky,
implement equivalent behavior in the bounded FCF temporal classifier.

Do not let the bare word `현재` turn:

```text
"현재 충족된 사실은 아니다"
```

into a current fulfilled FCF claim.

---

# 21. Negation must be local

Do NOT implement:

```text
if "아니다" anywhere in field → safe.
```

Counterexample:

```text
"현재 조건이 충족된 것은 아니지만
 FCF는 이미 감소했다."
```

Expected:

```text
current FCF claim detected
→ current evidence required.
```

Counterexample:

```text
"현재 충족되지는 않았다고 했지만
 실제로는 FCF 감소가 확인됐다."
```

Expected:

```text
current fulfilled claim.
```

Negation only applies to the local predicate it governs.

---

# 22. Exact M12AY positive replay

After repair,
all four exact failed risk contexts must satisfy:

```text
FCF local clause =
configured weakening condition

configured free_cash_flow support =
true

current magnitude =
false

current numeric =
false

affirmative current fulfillment =
false

negated current fulfillment =
true or outside local FCF span

FCF temporal role =
PROSPECTIVE_RISK_SCENARIO

current FCF evidence required =
false

unsupported_current_fcf_claim =
0
```

Rows:

```text
FIC-FIN-01 PASS
FIC-FIN-02 PASS
FIC-FIN-03 PASS
FIC-FIN-04 PASS
```

Do not rewrite model output.

---

# 23. Standalone future FCF control — preserve

M12AY already reported:

```text
standalone_future_fcf_claim_role =
FUTURE_REEVALUATION_CONDITION

standalone_future_fcf_requires_current_evidence =
false
```

Preserve.

---

# 24. Current FCF negative controls

These MUST still fail without safe current FCF evidence:

```text
"현재 FCF가 감소했다."

"FCF가 이미 악화됐다."

"현재 FCF는 음수다."

"FCF는 100이다."

"FCF 감소가 확인됐다."

"현재 FCF 감소가 확인됐다."
```

Even if:

```text
a configured FCF weaken signal is cited elsewhere.
```

No regression.

---

# 25. Negated fulfillment + real current FCF negative fixture

Required:

```text
"약화 조건이 현재 충족된 사실은 아니다.
 다만 FCF는 이미 감소했다."
```

Expected:

```text
current FCF assertion detected
→ FAIL without current safe FCF evidence.
```

This proves the negation fix is local.

---

# 26. Negated fulfillment + current numeric FCF

Required:

```text
"현재 조건이 충족된 사실은 아니지만
 FCF는 100이다."
```

Expected:

```text
current numeric FCF
→ FAIL without safe current FCF evidence.
```

No blanket negation immunity.

---

# 27. Negated fulfillment + current FCF confirmation

Required:

```text
"현재 충족되지는 않았다고 했지만
 FCF 감소는 이미 확인됐다."
```

Expected:

```text
AFFIRMATIVE current FCF fulfillment
→ FAIL without safe current FCF evidence.
```

---

# 28. Pure negated configured condition positive fixture

Required:

```text
configured weaken support:
"FCF 감소와 순부채 증가가 동반"

risk:
"FCF 감소는 약화 조건이나
 현재 충족된 사실은 아니다."
```

Expected:

```text
prospective configured condition
→ PASS.
```

---

# 29. Pure "아직 미충족" positive fixture

Required:

```text
"FCF 감소는 하향 조건이지만
 아직 충족되지 않았다."
```

with configured FCF support.

Expected:

```text
prospective
→ PASS.
```

---

# 30. English negation parity

Positive:

```text
"FCF decline is a weakening condition,
 but it is not currently fulfilled."
```

with configured support:

```text
PASS / prospective.
```

Negative:

```text
"the condition is not currently fulfilled,
 but FCF has already declined."
```

without safe current FCF:

```text
FAIL.
```

---

# 31. Net-debt semantics must remain unchanged

The exact M12AY risk contexts also contain:

```text
net debt
```

Their net-debt local span already avoids the FCF false reject.

Preserve:

```text
current net-debt hard safety

future configured net-debt safety

nominal-condition safety

monitoring-obligation safety

mixed current/future claim split.
```

No changes unless required by shared polarity helpers.

---

# 32. Configured FCF support mapping — freeze

Preserve:

```text
metric_refs ["FCF"] → free_cash_flow support

explicit FCF text fallback

OCF does not map

OCF-PPE does not map

configured support ≠ current fulfillment
```

No changes.

---

# 33. Configured-signal field ownership — freeze

Preserve:

```text
configured-only current buy/sell driver → FAIL

configured-only material anchor → FAIL

configured-only dominant current evidence → FAIL

configured-only reevaluation / prospective risk → PASS.
```

No changes.

---

# 34. Business-delta semantics — freeze

Preserve:

```text
BusinessDeltaEvidenceView single source

AI_JUDGMENT

UNCHANGED_ONLY

no legacy raw-text eligibility/direction rederivation.
```

No changes.

---

# 35. Market-expectation semantics — freeze

Preserve:

```text
MarketExpectationEvidenceView

conditional expectation context-only
cannot become independent material anchor.
```

No changes.

---

# 36. Financial-sector semantics — freeze

Preserve:

```text
bank/insurance specialized frameworks

industrial net-debt/WC exclusion

"배제하고" connective semantics

true misuse hard failure.
```

No changes.

---

# 37. Stage-2 lexical safety — freeze

Preserve:

```text
true 주가 contamination detection

수주가 / 발주가 false-positive avoidance.
```

No changes.

---

# 38. No model-facing semantic change expected

M12AZ should be:

```text
post-model FCF temporal claim-scope / polarity repair only.
```

Required default:

```text
model_prompt_semantic_change_count = 0

model_schema_semantic_change_count = 0

configured_signal_view_change_count = 0

configured_financial_support_concept_change_count = 0

BusinessDeltaEvidenceView change_count = 0

MarketExpectationEvidenceView change_count = 0

financial_evidence_projection_change_count = 0

two_stage_semantic_change_count = 0
```

If any model-facing semantic hash changes:

```text
STOP
UNPLANNED_MODEL_SURFACE_DRIFT
```

Review before proof.

---

# 39. Offline M12AY 36-row re-audit

Before new model calls,
revalidate all completed M12AY proof rows:

```text
completed rows = 36

prior PASS rows = 32

failed rows = 4
```

Required after M12AZ:

```text
36 / 36 PASS

32 prior PASS rows remain PASS

4 M12AY false rejects become PASS

new false accept = 0

new false reject = 0
```

Diagnostic only.

Do not stitch old generation.

---

# 40. Prior replay regressions

Also revalidate:

```text
M12AV completed 40 rows = 40 / 40 PASS

M12AW first-call rows = 4 / 4 PASS

M12AX first-call rows = 4 / 4 PASS

M12AY exact 4 failed rows = 4 / 4 PASS after repair.
```

No regression.

---

# 41. Mandatory polarity fixtures

At minimum:

## NEG-FUL-P01

```text
"FCF 감소는 약화 조건이나
 현재 충족된 사실은 아니다."
configured FCF support
→ PASS
```

## NEG-FUL-P02

```text
"FCF 감소는 무효화 조건이지만
 아직 충족되지 않았다."
configured support
→ PASS
```

## NEG-FUL-P03

```text
"FCF 감소는 하향 조건이며
 현재 확인된 사실은 아니다."
configured support
→ PASS
```

## NEG-FUL-P04

```text
English:
"FCF decline is a downside condition
but is not currently fulfilled."
→ PASS
```

## NEG-FUL-N01

```text
"현재 FCF가 감소했다."
→ FAIL without current safe FCF
```

## NEG-FUL-N02

```text
"조건은 현재 충족된 사실은 아니지만
 FCF는 이미 감소했다."
→ FAIL
```

## NEG-FUL-N03

```text
"아직 충족되지 않았지만
 FCF 감소는 이미 확인됐다."
→ FAIL
```

## NEG-FUL-N04

```text
"현재 충족된 사실은 아니지만
 FCF는 100이다."
→ FAIL
```

## NEG-FUL-N05

```text
"FCF 감소는 조건이고 현재 충족됐다."
→ FAIL unless safe current FCF evidence
```

---

# 42. Mandatory FCF local-span fixtures

At minimum:

## FCF-SPAN-P01

```text
risk_context:
"경쟁 압박이 있다.
 FCF 감소는 약화 조건이나 현재 충족된 사실은 아니다."

configured FCF support
→ local FCF claim = prospective
→ PASS
```

## FCF-SPAN-P02

```text
risk_context:
"FCF 감소는 약화 조건이고
 음의 OCF는 무효화 조건이나,
 현재 둘 다 충족된 사실은 아니다."

configured FCF + configured OCF support
→ FCF and OCF concepts remain distinct
→ FCF prospective
```

## FCF-SPAN-N01

```text
"FCF 감소는 약화 조건이다.
 현재 FCF는 실제로 감소했다."
→ current FCF claim separately detected
→ FAIL without safe current FCF evidence.
```

## FCF-SPAN-N02

```text
"현재 FCF는 감소했고,
 추가 감소는 약화 조건이다."
→ current FCF claim remains current
→ FAIL without safe evidence.
```

---

# 43. Current safe FCF evidence audit

Audit whether the repository has a true safe current metric:

```text
free_cash_flow
reported_free_cash_flow
management-defined FCF
```

If present:

```text
current FCF assertions may pass only with that evidence
and correct period/basis.
```

If absent:

```text
current FCF assertions remain unsupported.
```

Do not synthesize from OCF-PPE.

---

# 44. Generic polarity helper scope

If a new:

```text
affirmative_current_fulfillment()
```

helper replaces direct `_CURRENT_FULFILLMENT_LANGUAGE.search()`,
audit every caller.

Required:

```text
no current net-debt false accept

no current WC false accept

no current financial-sector false accept

no configured-signal false fulfillment

no business-delta behavior change.
```

Do not introduce a global semantic change without regression proof.

---

# 45. Deterministic gate before model calls

Require:

```text
latest ZIP integrity PASS

M12AY exact 4-row replay = 4 / 4 PASS

M12AY completed 36-row replay = 36 / 36 PASS

M12AV 40-row replay = 40 / 40 PASS

M12AW 4-row replay = 4 / 4 PASS

M12AX 4-row replay = 4 / 4 PASS

negated-fulfillment positive fixtures PASS

negated-fulfillment true-current negative fixtures PASS

FCF local-span fixtures PASS

configured FCF support regressions PASS

OCF-not-FCF regressions PASS

OCF-PPE-not-FCF regressions PASS

current unsupported FCF regressions PASS

monitoring-obligation regressions PASS

nominal-condition regressions PASS

mixed-risk regressions PASS

configured-signal field-ownership regressions PASS

BusinessDeltaEvidenceView regressions PASS

MarketExpectationEvidenceView regressions PASS

financial-sector regressions PASS

Stage-2 lexical regressions PASS

QTD/YTD / WC / debt / ADR safety PASS

focused tests PASS

full local tests PASS

ruff PASS

git diff --check PASS

production firewall PASS

model = gpt-5.6-sol / xhigh
```

No model calls before all pass.

---

# 46. NEW full fictional proof is mandatory

M12AY stopped after:

```text
9 / 12
```

formal model calls.

M12AT's model-facing configured-signal field fencing
still has not completed one whole formal proof.

Therefore:

```text
NEW generation ID

NEW full 12-call proof from scratch
```

is mandatory.

Do NOT:

```text
continue M12AY

stitch its 32 PASS rows

run only the missing calls

rerun only failed subjects.
```

---

# 47. Formal proof topology

Subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Topology:

```text
3 repetitions

2 contexts / repetition

4 subjects / context

Stage 1 calls = 6

Stage 2 calls = 6

total = 12

final compositions = 24
```

Model:

```text
gpt-5.6-sol / xhigh
```

No judge calls.

No fallback.

No selective rerun.

---

# 48. Fictional hard-stop policy

Hard stop only for:

```text
runtime/schema hard failure

invalid evidence identity

FCF claim-local temporal-scope violation

negated-vs-affirmative fulfillment polarity violation

true unsupported current FCF claim

true PPE-proxy-as-FCF violation

configured FCF support mapping violation

current financial completeness failure

monitoring-obligation violation

nominal-condition violation

mixed-risk scope violation

configured-signal field-use violation

configured-signal false fulfillment

business-delta violation

market-expectation anchor violation

financial-sector scope violation

price/technical/supply Core contamination

Stage-2 actual contamination

ADR/security-basis failure

core mutation

aggregate finalization failure
```

Do NOT stop for:

```text
safe negated current-fulfillment statements

safe configured future FCF conditions

valid decision-material variance.
```

---

# 49. Formal fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

FCF field-level scope leakage = 0

negated fulfillment false current classification = 0

affirmative current fulfillment false negative = 0

current unsupported FCF false accept = 0

proxy-as-FCF violation = 0

configured FCF support false negative = 0

OCF-as-FCF support false positive = 0

OCF-PPE-as-FCF support false positive = 0

monitoring-obligation false reject = 0

nominal-condition false reject = 0

mixed-risk false reject = 0

configured-only current-driver violation = 0

configured-signal false fulfillment = 0

business-delta violation = 0

expectation anchor violation = 0

financial-sector false reject = 0

Stage-2 lexical false positive = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0
```

Decision-material variance does not block shadow.

---

# 50. Formal diagnostics

Across all 3 repetitions report:

```text
overall_direction

directional_balance

business_thesis_change

new-buyer stance

holder stance

directional confidence
```

Retain explicit diagnostics for:

```text
FIC-FIN-02 delta materiality

FIC-FIN-05 primary threshold

FIC-FIN-06 delta materiality

FIC-FIN-08 holder boundary.
```

Do not repair these in M12AZ.

---

# 51. Shadow authorization

If formal fictional hard gates pass:

```text
run a NEW full active-monitored same-packet shadow.
```

No fresh unseen issuers.

Existing monitored names are an exposed compatibility cohort.

---

# 52. Active monitored universe

Re-enumerate active monitored names read-only.

Last verified reference:

```text
22 active names
```

Use actual task-start list.

No monitoring membership mutation.

---

# 53. Same-packet contract

For every active ticker:

```text
one reproducible local frozen packet

same packet hash feeds:
monolithic
Stage 1
Stage 2
```

No provider refresh.

Required:

```text
provider_source_fetches = 0
```

---

# 54. Canonical view equality

Require monolithic / Stage-1 semantic equality for:

```text
ConfiguredSignalEvidenceView

configured financial support concepts

BusinessDeltaEvidenceView

MarketExpectationEvidenceView

financial evidence projection

sector framework.
```

No architecture comparison if these differ.

---

# 55. Shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic calls = context_count

Stage 1 calls = context_count

Stage 2 calls = context_count

total = 3 × context_count
```

At N=22:

```text
18 calls
```

No repetitions.

---

# 56. Shadow hard-stop policy

Hard stop only for:

```text
runtime/schema hard failure

manifest/hash mismatch

packet mismatch

invalid evidence identity

FCF claim-local temporal-scope violation

negated/affirmative fulfillment polarity violation

true unsupported current FCF claim

true PPE-proxy-as-FCF violation

configured FCF support violation

current financial semantic violation

monitoring/nominal/mixed-risk temporal violation

configured-signal field-use violation

configured-signal false fulfillment

business-delta violation

market-expectation anchor violation

financial-sector scope violation

Stage-2 contamination

ADR/security-basis violation

core mutation

production-side-effect attempt

aggregate finalization failure
```

Do not stop because investment decisions differ.

---

# 57. Shadow FCF claim-local audit

For every FCF occurrence report:

```text
ticker

field_path

full_field_text

local_fcf_clause

local_clause_start/end

FCF semantic role

configured support refs

current scope marker

current magnitude detected

current numeric detected

affirmative current fulfillment detected

negated current fulfillment detected

temporal role

requires current FCF evidence

current safe FCF evidence refs

validation result.
```

This audit is mandatory.

---

# 58. Shadow negated-fulfillment audit

For every candidate containing:

```text
not currently fulfilled
현재 충족된 사실은 아니다
아직 충족되지 않았다
현재 확인된 사실은 아니다
```

or equivalent,
report:

```text
negated predicate span

financial concepts governed by the negation

any separate affirmative current financial claim

final currentness result.
```

No blanket field immunity.

---

# 59. Shadow comparison taxonomy

Use:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED
```

Neither architecture is automatically ground truth.

---

# 60. Full compatibility goal

M12AZ must finally produce:

```text
one final comparison row per active ticker

all active tickers represented exactly once

aggregate finalization PASS

complete monolithic-vs-two-stage compatibility cohort.
```

Do not introduce decision-policy fixes mid-generation.

---

# 61. Policy handoff after complete shadow

If full shadow completes without systematic architecture regression:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

Use:

```text
NEW M12AZ formal fictional proof

+

M12AZ complete monitored shadow
```

Focus:

```text
1. remaining adjacent primary-direction differences

2. FIC-FIN-02 / FIC-FIN-06 business-delta materiality

3. FIC-FIN-08 holder HOLDABLE / REVIEW boundary

4. new-buyer stance differences with stable core

5. which previous compatibility differences disappeared after:
   - expectation independence
   - configured-signal field fencing
   - claim-local FCF support
   - clause-local financial temporal scope
   - nominalized conditions
   - monitoring obligations
   - negated fulfillment polarity.
```

Do not perform that policy review inside M12AZ.

---

# 62. Local-only / production firewall

M12AZ is LOCAL-ONLY.

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0
```

Do not request GitHub push authorization.

Monitoring schedules remain paused.

---

# 63. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12az-scope-freeze

04-integrated-main-lineage-freeze

05-m12ay-four-row-failure-reproduction

06-fcf-whole-row-vs-local-span-forensic

07-current-fulfillment-regex-polarity-audit

08-netdebt-vs-fcf-temporal-path-comparison

09-fcf-local-span-architecture-options

10-fcf-local-span-architecture-decision
```

---

# 64. Required semantic-contract artifacts

Produce:

```text
11-fcf-temporal-claim-span-contract

12-current-fulfillment-polarity-contract

13-negated-current-fulfillment-contract

14-affirmative-current-fulfillment-contract

15-current-scope-marker-contract

16-local-negation-scope-contract

17-fcf-ocf-concept-separation-contract

18-current-fcf-evidence-requirement-contract
```

---

# 65. Required exact replay / fixture artifacts

Produce:

```text
19-m12ay-fic-fin-01-exact-offline-replay

20-m12ay-fic-fin-02-exact-offline-replay

21-m12ay-fic-fin-03-exact-offline-replay

22-m12ay-fic-fin-04-exact-offline-replay

23-negated-fulfillment-positive-fixtures

24-negated-plus-current-claim-negative-fixtures

25-fcf-local-span-positive-fixtures

26-fcf-local-span-current-negative-fixtures

27-english-negated-fulfillment-parity-fixtures
```

---

# 66. Required prior-proof offline re-audits

Produce:

```text
28-m12ay-completed-36-row-offline-reaudit

29-m12av-completed-40-row-offline-reaudit

30-m12aw-first-call-four-row-offline-reaudit

31-m12ax-first-call-four-row-offline-reaudit
```

Expected:

```text
M12AY completed rows = 36 / 36 PASS

M12AV = 40 / 40 PASS

M12AW = 4 / 4 PASS

M12AX = 4 / 4 PASS.
```

Diagnostic only.

Do not stitch old generations.

---

# 67. Required frozen-regression artifacts

Produce:

```text
32-m12ay-configured-fcf-support-freeze

33-m12ax-monitoring-obligation-freeze

34-m12aw-nominal-condition-freeze

35-m12av-clause-local-scope-freeze

36-m12au-fcf-case-semantic-freeze

37-m12at-configured-signal-field-ownership-freeze

38-m12as-unchanged-claim-scope-freeze

39-m12ar-fcf-claim-scope-freeze

40-m12aq-financial-sector-scope-freeze

41-m12ap-expectation-independence-freeze

42-m12ao-business-delta-convergence-freeze

43-m12an-ppe-proxy-label-freeze

44-m12am-stage2-lexical-freeze

45-monitoring-transition-ownership-freeze

46-qtd-ytd-wc-debt-safety-freeze

47-adr-security-basis-freeze

48-two-stage-ownership-freeze

49-price-timing-renderer-no-change
```

---

# 68. Required model-facing impact artifacts

Produce:

```text
50-model-prompt-semantic-hash-freeze

51-model-schema-semantic-hash-freeze

52-configured-signal-view-semantic-hash-freeze

53-configured-financial-support-concept-hash-freeze

54-business-delta-view-semantic-hash-freeze

55-expectation-view-semantic-hash-freeze

56-financial-evidence-projection-semantic-hash-freeze

57-model-facing-no-change-decision
```

Expected:

```text
NO_MODEL_FACING_SEMANTIC_CHANGE
```

for the M12AZ repair.

---

# 69. Required deterministic test gate

Produce:

```text
58-focused-test-results

59-full-local-test-results

60-ruff-and-diff-results

61-hosted-ci-portability-observation

62-new-fictional-model-call-gate
```

No model calls before artifact 62 PASS.

---

# 70. Required NEW fictional artifacts

Produce:

```text
63-fictional-generation-manifest

64-fictional-configured-signal-view-manifest

65-fictional-configured-financial-support-concept-manifest

66-fictional-delta-view-manifest

67-fictional-expectation-view-manifest

68-stage1-run1-context01

69-stage1-run1-context02

70-stage2-run1-context01

71-stage2-run1-context02

72-stage1-run2-context01

73-stage1-run2-context02

74-stage2-run2-context01

75-stage2-run2-context02

76-stage1-run3-context01

77-stage1-run3-context02

78-stage2-run3-context01

79-stage2-run3-context02

80-fictional-context-hard-semantic-audit

81-fictional-fcf-local-temporal-scope-audit

82-fictional-negated-fulfillment-polarity-audit

83-fictional-configured-fcf-support-audit

84-fictional-monitoring-obligation-audit

85-fictional-nominal-condition-audit

86-fictional-mixed-risk-context-audit

87-fictional-configured-signal-field-use-audit

88-fictional-business-delta-audit

89-fictional-market-expectation-audit

90-fictional-financial-sector-audit

91-fictional-stage2-language-audit

92-fictional-final-composition-audit

93-fictional-aggregate-finalization-audit

94-fictional-primary-direction-diagnostic

95-fictional-delta-materiality-diagnostic

96-fictional-new-buyer-diagnostic

97-fictional-holder-diagnostic

98-fictional-core-immutability-audit

99-fictional-runtime-audit

100-fictional-shadow-gate-decision
```

---

# 71. Required shadow setup artifacts

If artifact 100 authorizes shadow:

```text
101-task-start-active-monitored-universe

102-shadow-packet-inventory

103-shadow-packet-hash-manifest

104-shadow-configured-signal-view-manifest

105-shadow-configured-financial-support-concept-manifest

106-shadow-delta-view-manifest

107-shadow-expectation-view-manifest

108-shadow-frozen-context-manifest

109-shadow-batching-manifest

110-shadow-model-call-gate
```

---

# 72. Required full shadow artifacts

Produce:

```text
111-shadow-monolithic-model-artifacts

112-shadow-stage1-model-artifacts

113-shadow-stage2-model-artifacts

114-shadow-context-hard-semantic-audit

115-shadow-fcf-local-temporal-scope-audit

116-shadow-negated-fulfillment-polarity-audit

117-shadow-configured-fcf-support-audit

118-shadow-monitoring-obligation-audit

119-shadow-nominal-condition-audit

120-shadow-mixed-risk-context-audit

121-shadow-configured-signal-field-use-audit

122-shadow-business-delta-audit

123-shadow-market-expectation-audit

124-shadow-financial-sector-audit

125-shadow-stage2-language-audit

126-shadow-final-composition-audit

127-shadow-aggregate-finalization-audit

128-shadow-per-ticker-comparison

129-shadow-core-direction-differences

130-shadow-business-delta-differences

131-shadow-new-buyer-differences

132-shadow-holder-differences

133-shadow-same-direction-calibration-differences

134-shadow-expected-contract-corrections

135-shadow-potential-architecture-regressions

136-shadow-unresolved-review-required

137-shadow-adr-security-basis-audit

138-shadow-cyclical-valuation-audit

139-shadow-core-immutability-audit

140-shadow-runtime-audit

141-shadow-aggregate-summary

142-shadow-architecture-decision
```

---

# 73. Required combined diagnostics

If full shadow completes:

```text
143-fic-fin-05-vs-monitored-primary-boundary-analogs

144-fic-fin-02-vs-monitored-delta-materiality-analogs

145-fic-fin-06-vs-monitored-positive-delta-analogs

146-fic-fin-08-vs-monitored-holder-analogs

147-new-buyer-monolithic-vs-two-stage-analogs

148-real-fcf-local-scope-lessons

149-real-negated-fulfillment-lessons

150-real-configured-fcf-support-lessons

151-combined-fictional-monitored-root-cause-summary

152-next-bounded-policy-decision
```

---

# 74. Required completion artifacts

Produce:

```text
153-fcf-local-temporal-scope-repair-success-decision

154-negated-fulfillment-polarity-repair-success-decision

155-configured-fcf-support-preservation-decision

156-monitoring-obligation-preservation-decision

157-nominal-condition-preservation-decision

158-mixed-risk-context-preservation-decision

159-configured-signal-field-ownership-preservation-decision

160-new-fictional-proof-success-decision

161-full-shadow-completion-decision

162-existing-monitored-impact-summary

163-two-stage-shadow-compatibility-decision

164-fresh-real-proof-readiness-decision

165-final-main-merge-readiness-note

166-production-no-change

167-schedule-pause-observation

168-remote-push-prohibition-audit

169-master-workflow-update

170-program-completion
```

---

# 75. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch

latest_result_zip_sha256
latest_result_integrity

m12ay_failed_tickers
m12ay_failure_error
m12ay_failure_suffix

fcf_temporal_scope_root_cause
current_fulfillment_polarity_root_cause

fcf_temporal_claim_span_contract_version
current_fulfillment_polarity_contract_version

fcf_local_span_enabled
negated_current_fulfillment_enabled
affirmative_current_fulfillment_preserved
current_scope_marker_separated_from_fulfillment

m12ay_four_row_replay_status
m12ay_36_row_reaudit_status
m12av_40_row_reaudit_status
m12aw_four_row_reaudit_status
m12ax_four_row_reaudit_status

fcf_field_level_scope_leak_count
negated_fulfillment_false_current_count
affirmative_current_fulfillment_false_negative_count

configured_fcf_support_regression_count
ocf_as_fcf_support_false_positive_count
ocf_ppe_as_fcf_support_false_positive_count

current_unsupported_fcf_false_accept_count
proxy_as_fcf_regression_count

monitoring_obligation_regression_count
nominal_condition_regression_count
mixed_risk_scope_regression_count

configured_signal_field_ownership_regression_count
configured_signal_false_fulfillment_count

business_delta_regression_count
expectation_regression_count
financial_sector_regression_count
stage2_lexical_regression_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
configured_signal_view_change_count
configured_financial_support_concept_change_count
business_delta_view_change_count
expectation_view_change_count
financial_evidence_projection_change_count
two_stage_semantic_change_count

investment_judgment_model_target
investment_judgment_reasoning_effort

fictional_generation_id

fictional_stage1_model_calls
fictional_stage2_model_calls
fictional_model_calls_total

fictional_stage1_row_count
fictional_stage2_row_count
fictional_final_composition_count

fictional_fcf_field_level_scope_leak_count
fictional_negated_fulfillment_false_current_count
fictional_affirmative_current_fulfillment_false_negative_count

fictional_configured_fcf_support_false_negative_count
fictional_ocf_as_fcf_support_false_positive_count
fictional_ocf_ppe_as_fcf_support_false_positive_count

fictional_current_unsupported_fcf_false_accept_count
fictional_proxy_as_fcf_violation_count

fictional_monitoring_obligation_false_reject_count
fictional_nominal_condition_false_reject_count
fictional_mixed_risk_false_reject_count

fictional_configured_signal_current_driver_violation_count
fictional_configured_signal_false_fulfillment_count

fictional_business_delta_violation_count
fictional_expectation_anchor_violation_count
fictional_financial_sector_violation_count
fictional_stage2_language_false_positive_count

fictional_primary_direction_unstable_subject_count
fictional_business_delta_materiality_variance_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count

fictional_core_mutation_count
fictional_timeout_count
fictional_orphan_count
fictional_wrapper_retry_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status

shadow_fcf_field_level_scope_leak_count
shadow_negated_fulfillment_false_current_count
shadow_affirmative_current_fulfillment_false_negative_count

shadow_configured_fcf_support_false_negative_count
shadow_ocf_as_fcf_support_false_positive_count
shadow_ocf_ppe_as_fcf_support_false_positive_count

shadow_current_unsupported_fcf_false_accept_count
shadow_proxy_as_fcf_violation_count

shadow_monitoring_obligation_false_reject_count
shadow_nominal_condition_false_reject_count
shadow_mixed_risk_false_reject_count

shadow_configured_signal_field_violation_count
shadow_configured_signal_false_fulfillment_count

shadow_business_delta_violation_count
shadow_expectation_anchor_violation_count
shadow_financial_sector_violation_count
shadow_stage2_language_false_positive_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count
shadow_timeout_count
shadow_orphan_count
shadow_wrapper_retry_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count
```

Anything not measured:

```text
NOT_MEASURED
```

---

# 76. Fresh-real / main / production readiness

At M12AZ completion:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY
```

Even if fictional and shadow complete.

The next bounded task should be:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

No fresh unseen proof inside M12AZ.

---

# 77. Failure handling

## A. Exact M12AY four rows still false-reject

```text
next_scope =
FCF_TEMPORAL_CLAIM_SPAN_ARCHITECTURE_REVIEW
```

No model calls.

## B. Negated fulfillment causes real current FCF claim to pass

```text
STOP
NEGATED_FULFILLMENT_REPAIR_TOO_PERMISSIVE
```

## C. FCF local span merges OCF into FCF

```text
STOP
FCF_OCF_TEMPORAL_SCOPE_COLLAPSE
```

## D. Configured FCF support regresses

```text
STOP
CONFIGURED_FCF_SUPPORT_REGRESSION
```

## E. Current unsupported FCF begins passing

```text
STOP
CURRENT_FCF_SAFETY_REGRESSION
```

## F. Configured-signal field fencing regresses

```text
STOP
CONFIGURED_SIGNAL_FIELD_OWNERSHIP_REGRESSION
```

## G. NEW fictional hard semantic failure

Do not run shadow.

Use the smallest bounded failing contract.

## H. Full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

---

# 78. Artifact integrity

Freeze all local artifacts before final artifact index.

Required:

```text
hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Report final ZIP SHA-256.

Do not push report/raw artifacts to GitHub.

---

# 79. Final task principle

M12AY fixed the configured FCF support map correctly.

The new failure exposes the last major asymmetry
inside the FCF temporal path:

```text
net debt:
already classified from a local financial claim span

FCF:
still classified from the entire FinancialClaimRow text
```

The full risk context says:

```text
"FCF/net-debt deterioration is a weakening condition...
but it is not currently fulfilled."
```

The FCF currentness helper sees the whole field,
finds the bare word:

```text
현재
```

and treats it as current fulfillment,
even though the sentence says the exact opposite.

The correct M12AZ flow is:

```text
reuse the existing local financial clause/span architecture

→ give FCF its own local temporal claim span
  without adding FCF to the industrial framework scanner

→ distinguish affirmative current fulfillment
  from negated current fulfillment

→ keep current magnitude / numeric / affirmative fulfillment hard safety

→ prove negation does not immunize a separate current FCF claim

→ offline re-audit M12AY 36 rows and all prior regressions

→ start a completely NEW full 12-call fictional proof

→ if all hard gates pass,
   run a completely NEW full active-monitored shadow

→ finally hand the complete clean cohort
   to the real boundary / delta-materiality / holder policy review
```

Do NOT:

```text
treat every "아니다" as safe

remove current FCF safety

make bare "현재" equal fulfilled

merge FCF and OCF

turn OCF-PPE into FCF

add FCF blindly to industrial framework application scanning

reopen configured FCF support mapping

reopen configured-signal field ownership

reopen BusinessDeltaEvidenceView

reopen MarketExpectationEvidenceView

resume/stitch M12AY

selectively rerun only the failed four rows

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring
```

A sentence saying a condition is NOT currently fulfilled
must not be classified as evidence that it IS currently fulfilled.
