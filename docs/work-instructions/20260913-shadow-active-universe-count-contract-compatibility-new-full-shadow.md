# Thesis Monitor — Shadow Active-Universe Count Contract Compatibility + New Full Monitored Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260913-shadow-active-universe-count-contract-compatibility-new-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260913-shadow-active-universe-count-contract-compatibility-new-full-shadow-report.zip
```

Master-workflow phase:

```text
M12BE — Shadow Preflight Report-Contract Compatibility
         A. Freeze successful M12BD formal fictional proof
         B. Reproduce exact shadow preflight active_count/subject_count failure
         C. Normalize the active-universe count at the report-contract consumer boundary
         D. Preserve authoritative market-expectation manifest contract
         E. Reuse M12BD formal fictional proof only after strict harness-only identity checks
         F. Create a NEW full frozen shadow generation
         G. Run all 18 monitored compatibility model calls if active count remains 22
         H. Complete per-ticker compatibility diagnostics
         I. Hand off the clean complete cohort to boundary / delta / holder policy review
```

M12BD successfully completed both model-facing repairs it was designed to make:

```text
Stage-2 WC checkpoint binding parity = PASS

optional lexical audit zero-observation coverage is nonblocking = PASS
```

The NEW M12BD fictional proof fully passed:

```text
model calls = 12 / 12

Stage 1 rows = 24 / 24

Stage 2 rows = 24 / 24

final compositions = 24

aggregate finalization = PASS

hard semantic failures = 0

core mutation = 0

Stage-1 WC grounding failures = 0

Stage-2 WC grounding failures = 0

Stage-2 WC metric-specific mismatch = 0

Stage-2 narrative-only substitution = 0

configured-signal violations = 0

FCF hard failures = 0

business-delta hard failures = 0

expectation hard failures = 0

financial-sector hard failures = 0

Stage-2 lexical false positives = 0

timeout = 0
orphan = 0
wrapper retry = 0
```

The fictional shadow gate explicitly passed:

```text
shadow_authorized = true
```

A monitored shadow was then prepared with:

```text
active names = 22

frozen packets = 22

context count = 6

planned shadow model calls = 18
```

but stopped BEFORE any model call because a stale M12BA wrapper consumer expected:

```text
active_count
```

while the authoritative market-expectation manifest exposes:

```text
subject_count = 22
```

and:

```text
len(rows) = 22.
```

Exact error:

```text
KeyError: active_count
```

Shadow model calls started:

```text
0
```

This is a report-contract compatibility bug only.

Do not rerun the 12-call fictional proof unless a model-facing semantic hash changes.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260913-stage2-working-capital-binding-parity-audit-coverage-validity-separation-full-proof-full-shadow-report.zip
```

Verified SHA-256:

```text
675c49e921fa5bfeedd00596bae2a803b3c7a68ed9ca95a78aa7961419f965c5
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Final artifact index:

```text
artifact_count = 930

ZIP entries:
930 indexed payloads
+ artifact-index.json
= 931

hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute independently.

---

# 2. M12BD program status

Reported:

```text
phase = M12BD

status = BLOCKED

two_stage_shadow_compatibility_classification =
FORMAL_COMPLETE_SHADOW_PREFLIGHT_BLOCKED
```

The word `BLOCKED` refers only to the monitored-shadow preflight.

Formal fictional proof status:

```text
PASS
```

Shadow status:

```text
preflight blocked

shadow model calls started = 0

shadow model calls completed = 0.
```

No monitored candidate exists in the stopped shadow generation.

---

# 3. M12BD fictional generation — freeze successful

Generation:

```text
20260911-m12ai-fictional-20260913T113957Z-07ac29f97bc6
```

Preserve all frozen model artifacts.

Required immutable counts:

```text
model calls = 12

Stage 1 rows = 24

Stage 2 rows = 24

final compositions = 24.
```

The repaired M12BC context-boundary finalizer passed.

Do not alter this generation.

---

# 4. M12BD fictional hard semantic result — freeze

Program completion reports:

```text
fictional_hard_semantic_failure_count = 0

fictional_context_boundary_status = PASS

fictional_aggregate_finalization_status = PASS

fictional_core_mutation_count = 0

fictional_stage1_wc_grounding_failure_count = 0

fictional_stage2_wc_grounding_failure_count = 0

fictional_stage2_wc_metric_specific_mismatch_count = 0

fictional_stage2_wc_narrative_only_substitution_count = 0

fictional_configured_signal_violation_count = 0

fictional_fcf_hard_failure_count = 0

fictional_business_delta_hard_failure_count = 0

fictional_expectation_hard_failure_count = 0

fictional_financial_sector_hard_failure_count = 0

fictional_stage2_language_false_positive_count = 0.
```

Preserve.

---

# 5. Optional audit coverage — freeze successful

M12BD reports:

```text
monitoring_audit_zero_observation_status = PASS

monitoring_audit_zero_observation_readiness_blocking = false

optional_audit_zero_observation_hard_failure_count = 0

fictional_optional_audit_zero_observation_nonblocking_count = 5.
```

Do not reopen audit coverage/validity separation.

---

# 6. Fictional decision variance — diagnostic only

M12BD diagnostics:

```text
primary-direction unstable subjects = 1

business-delta unstable subjects = 0

new-buyer unstable subjects = 1

holder unstable subjects = 1

same-direction calibration variance subjects = 3.
```

Exact important rows:

## FIC-FIN-05

```text
overall_direction =
HOLD / SELL / SELL

new buyer =
WAIT / AVOID / WAIT

holder =
REVIEW / REVIEW / REVIEW.
```

## FIC-FIN-08

```text
holder =
REVIEW / HOLDABLE / REVIEW.
```

## Business delta

```text
all 8 fictional subjects stable across 3 repetitions.
```

These are policy-review diagnostics.

Do not convert them into readiness failures.

Do not force enum stability.

---

# 7. Shadow authorization — freeze

M12BD fictional shadow gate:

```json
{
  "status": "PASS",
  "shadow_authorized": true,
  "model_calls": 12,
  "stage1_rows": 24,
  "stage2_rows": 24,
  "final_compositions": 24,
  "provider_source_fetches": 0,
  "production_side_effects": 0
}
```

Preserve this authorization
if and only if M12BE changes no model-facing semantics.

---

# 8. Task-start active monitored universe

M12BD re-enumerated:

```text
22 active names
```

Reference list:

```text
000660
003690
005490
005930
010120
012450
047810
086280
CORZ
CPNG
CRCL
GOOGL
HUT
IBM
MU
RXRX
SKHY
SNDK
TSLA
TSM
WRD
WULF
```

M12BE must re-enumerate the active universe read-only again.

Do not hard-code 22 as authoritative.

Use the actual task-start active set.

---

# 9. M12BD shadow generation that must NOT be reused

Stopped shadow generation:

```text
20260911-m12ai-shadow-20260913T121804Z-145705ea053d
```

State before stop:

```text
frozen packet count = 22

context count = 6

model calls started = 0

model calls completed = 0.
```

Do NOT resume this generation.

Do NOT stitch it.

Create a NEW shadow generation after the report-contract repair.

---

# 10. Exact preflight failure

Failure:

```text
phase =
M12BD_MONITORED_SHADOW_PREFLIGHT

stop reason =
SHADOW_REPORT_CONTRACT_COMPATIBILITY_FAILURE
```

Consumer:

```text
scripts/financial_sector_replacement_verb_parity_m12ba.py
function:
prepare_shadow
```

Current stale code:

```python
int(upstream(101)["active_count"]) == EXPECTED_ACTIVE_COUNT
```

But the upstream report is:

```text
101-shadow-expectation-view-manifest.json
```

whose authoritative contract is:

```text
market-expectation-evidence-view-v1
```

and exposes:

```text
subject_count = 22

rows = 22 entries.
```

No `active_count` field exists.

---

# 11. Exact authoritative expectation manifest

Observed top-level metadata:

```text
contract =
market-expectation-evidence-view-v1

status =
PASS

subject_count =
22

expectation_ref_count =
22

independence_unknown_count =
22

independent_directional_support_count =
0

conditional_context_only_count =
0

already_reflected_counterweight_count =
0

len(rows) =
22.
```

The report itself is internally consistent.

Do not change its semantic contract merely to satisfy one stale consumer.

---

# 12. Code-surface audit result

Repository/report bundle scan found the direct mapping access:

```python
["active_count"]
```

in exactly ONE Python location relevant to this failure:

```text
scripts/financial_sector_replacement_verb_parity_m12ba.py
prepare_shadow
```

M12BE should independently reproduce this scan.

If additional active-count consumer mismatches exist in the current repository:

```text
include them in the same bounded compatibility repair
only if they consume the same active-universe/report-count contract.
```

Do not broaden beyond that contract family.

---

# 13. Root-cause classification

Freeze:

```text
SHADOW_ACTIVE_UNIVERSE_REPORT_COUNT_FIELD_NAME_DRIFT
```

or exact audited equivalent.

More specifically:

```text
consumer expects:
active_count

authoritative market-expectation manifest provides:
subject_count.
```

This is NOT:

```text
a market-expectation semantic failure

an active universe enumeration failure

a packet inventory failure

a model output failure

a Stage-1/Stage-2 grounding failure

a provider failure.
```

---

# 14. Do NOT patch by blindly duplicating `active_count`

Avoid the lowest-quality fix:

```text
just add active_count = subject_count
to every market-expectation manifest.
```

Why:

```text
two count fields can drift

producer contract becomes polluted by one consumer

future code may disagree on which field is authoritative.
```

Preferred:

```text
consumer reads the canonical count field
for the report contract it consumes.
```

---

# 15. Preferred repair: contract-aware subject-count consumer

Preferred deterministic helper:

```text
report_subject_count(report)
```

or equivalent.

For:

```text
contract =
market-expectation-evidence-view-v1
```

the authoritative count is:

```text
subject_count.
```

Require:

```text
subject_count == len(rows).
```

Do not silently fall back across arbitrary unknown fields.

---

# 16. Active-universe cross-check

Shadow preflight should not only compare one integer.

Require identity across:

```text
task-start active monitored universe

frozen shadow packet inventory

expectation-view manifest rows

delta-view manifest rows

configured-signal-view manifest rows

WC binding-view manifest rows

frozen contexts / batching manifest.
```

At minimum verify:

```text
count equality

exact ticker-set equality

duplicate count = 0

missing count = 0

extra count = 0.
```

This is safer than preserving the old single `active_count == 22` check.

---

# 17. Canonical count names by artifact

Do not force every artifact to use the same legacy field name.

Instead record the actual contract-native count.

Examples may include:

```text
subject_count

packet_count

active_monitor_count

ticker_count

row_count.
```

The preflight adapter should understand the specific artifact contract
or compare ticker sets directly.

Do not create a generic "first count-looking field wins" parser.

---

# 18. Strongest source of truth

For the actual shadow universe,
the strongest identity check is:

```text
task-start active ticker set
==
frozen packet ticker set
==
per-ticker view manifest ticker sets.
```

Integer counts are a secondary sanity check.

If count says 22 but ticker sets differ:

```text
FAIL.
```

Do not proceed.

---

# 19. Exact M12BD preflight offline replay

Before any new shadow generation,
run the repaired preflight against the frozen M12BD shadow setup artifacts
OFFLINE.

Expected:

```text
task-start active count = 22

expectation manifest subject_count = 22

expectation len(rows) = 22

packet count = 22

ticker-set mismatch = 0

duplicate count = 0

status = PASS.
```

Diagnostic only.

Do NOT resume the old shadow generation.

---

# 20. Negative preflight fixtures

At minimum:

## COUNT-N01

```text
subject_count = 22
len(rows) = 21
→ FAIL.
```

## COUNT-N02

```text
subject_count = 22
rows = 22
but one ticker duplicated and one ticker missing
→ FAIL.
```

## COUNT-N03

```text
active universe = 22 tickers
expectation manifest = different 22 tickers
→ FAIL.
```

## COUNT-N04

```text
unknown report contract
with only arbitrary count fields
→ fail closed.
```

## COUNT-N05

```text
market-expectation-evidence-view-v1
missing subject_count
→ FAIL.
```

Do not restore permissive field guessing.

---

# 21. Positive preflight fixtures

## COUNT-P01

```text
contract = market-expectation-evidence-view-v1
subject_count = 22
len(rows) = 22
exact ticker set matches active universe
→ PASS.
```

## COUNT-P02

```text
non-22 active set
e.g. 21 names
all manifests/packets agree on exact 21 ticker set
→ PASS.
```

This proves the code does not hard-code 22.

---

# 22. M12BD fictional proof reuse

M12BE changes:

```text
shadow preflight/report-contract compatibility only.
```

Expected model-facing semantic change counts:

```text
model prompt = 0

model schema = 0

Stage-1 WC binding view = 0

Stage-2 WC binding view = 0

configured signal view = 0

configured financial support concept = 0

BusinessDeltaEvidenceView = 0

MarketExpectationEvidenceView = 0

financial evidence projection = 0

two-stage core semantics = 0

final user schema = 0.
```

If all hashes remain identical,
reuse the completed M12BD fictional proof.

Required:

```text
formal_fictional_reuse_status =
REUSE_AUTHORIZED_M12BD_COMPLETE_PROOF
```

and:

```text
new_fictional_model_calls = 0.
```

---

# 23. Proof-reuse identity gate

Before reuse require:

```text
generation id unchanged:
20260911-m12ai-fictional-20260913T113957Z-07ac29f97bc6

all 12 receipts present

all model outputs present

all prompt hashes unchanged

all schema hashes unchanged

Stage-1 rows = 24

Stage-2 rows = 24

final compositions = 24

hard semantic failures = 0

aggregate finalization = PASS

core mutation = 0.
```

If any fail:

```text
formal_fictional_reuse_status =
NEW_FORMAL_PROOF_REQUIRED
```

and run a new full 12-call proof.

Do not partially reuse.

---

# 24. New shadow generation is mandatory

Even though the old stopped shadow had:

```text
0 model calls
```

create a NEW shadow generation.

Reason:

```text
preflight/report-contract code changed

the old generation stopped under a failed preflight contract

clean provenance requires one new frozen generation after the repair.
```

Do not reuse:

```text
20260911-m12ai-shadow-20260913T121804Z-145705ea053d.
```

---

# 25. Re-enumerate active monitored names read-only

At new shadow start:

```text
list active monitored names
```

read-only.

No registrations.

No stops.

No assessment writes.

Use actual current universe.

If count changed from 22:

```text
recompute context count and planned calls dynamically.
```

---

# 26. New frozen packet generation

Create one reproducible packet per active ticker.

Required:

```text
provider_source_fetches = 0

packet unavailable count = 0
or hard stop before model calls

packet mismatch count = 0.
```

Same packet hash must feed:

```text
monolithic

Stage 1

Stage 2 evidence context.
```

---

# 27. Shadow topology

For:

```text
N active names
```

compute:

```text
context_count = ceil(N / 4)

monolithic calls = context_count

Stage 1 calls = context_count

Stage 2 calls = context_count

total calls = 3 × context_count.
```

If N remains 22:

```text
context_count = 6

total calls = 18.
```

No repetitions.

---

# 28. Shadow model

Use:

```text
gpt-5.6-sol / xhigh
```

No Astra.

No fallback.

No judge calls.

No selective rerun.

---

# 29. Shadow hard-stop policy

Hard stop only for objective failures:

```text
runtime/schema failure

manifest/hash mismatch

packet mismatch

active-universe identity mismatch

invalid evidence identity

Stage-1 WC grounding failure

Stage-2 WC grounding failure

WC metric-specific ref mismatch

WC narrative-only substitution

irrelevant typed financial grounding

unsafe WC auto-direction

configured-signal field-use violation

configured-signal false fulfillment

FCF hard semantic violation

financial-sector hard semantic violation

business-delta hard semantic violation

market-expectation hard semantic violation

Stage-2 actual contamination

ADR/security-basis failure

core mutation

aggregate finalization failure

production side-effect attempt.
```

Do NOT hard-stop for decision differences.

---

# 30. Optional lexical audit coverage remains nonblocking

Preserve M12BD:

```text
zero observed monitoring-obligation claims
→ coverage fact, not semantic failure.
```

Likewise for other optional lexical families.

Report:

```text
observed_claim_count

semantic_violation_count

coverage_status

semantic_status

readiness_blocking.
```

No regression.

---

# 31. Stage-2 WC binding remains hard

Preserve M12BD Stage-2 parity.

For stance-owned WC condition fields:

```text
fundamental_new_buyer.confirmation_business_condition

fundamental_holder.business_invalidation_condition
```

require claim-local typed WC refs
under the same M12BB/M12BD binding contract.

Shadow acceptance requires:

```text
Stage-2 WC grounding failure = 0

metric mismatch = 0

narrative substitution = 0.
```

---

# 32. Context-preserving finalization remains hard

Preserve M12BC:

```text
no cross-context >4 candidate DirectionalCoreBatch

cross_context_model_batch_construction_count = 0

context boundary identity preserved

one final comparison row per ticker.
```

No change.

---

# 33. Decision variance remains diagnostic

Use the established comparison taxonomy:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED.
```

A decision difference alone is not an architecture failure.

---

# 34. Potential architecture regression requires objective evidence

Examples:

```text
hard semantic error

invalid ref

lost material grounding

configured-signal ownership violation

FCF safety violation

financial-sector framework misuse

Stage-2 core mutation

price/timing contamination.
```

Do not label:

```text
HOLD vs SELL
WAIT vs AVOID
HOLDABLE vs REVIEW
```

as regression merely because they differ.

---

# 35. Full shadow acceptance

Require:

```text
all planned model calls complete

all active tickers represented exactly once

exact active-universe ticker-set equality

packet mismatch = 0

all monolithic hard semantics PASS

all Stage-1 hard semantics PASS

all Stage-2 hard semantics PASS

all final compositions PASS

aggregate finalization PASS

core mutation = 0

Stage-1 WC grounding failure = 0

Stage-2 WC grounding failure = 0

WC metric mismatch = 0

WC narrative substitution = 0

irrelevant financial grounding = 0

unsafe WC auto-direction = 0

configured-signal violations = 0

FCF hard failures = 0

financial-sector hard failures = 0

business-delta hard failures = 0

expectation hard failures = 0

Stage-2 contamination = 0

ADR/security-basis failure = 0

timeout = 0

orphan = 0

wrapper retry = 0

production side effects = 0.
```

Decision-material comparison counts may be nonzero.

---

# 36. Per-ticker compatibility output

For every active ticker report:

```text
ticker

monolithic primary direction

two-stage primary direction

monolithic business delta

two-stage business delta

monolithic new-buyer stance

two-stage new-buyer stance

monolithic holder stance

two-stage holder stance

balance/calibration differences

comparison taxonomy

objective semantic violations if any

review note.
```

All active tickers exactly once.

---

# 37. Combined policy-review handoff

If the full shadow is objectively clean,
combine it with the frozen M12BD fictional proof.

Preserve fictional diagnostics:

```text
FIC-FIN-05:
HOLD / SELL / SELL
WAIT / AVOID / WAIT
REVIEW / REVIEW / REVIEW

FIC-FIN-08 holder:
REVIEW / HOLDABLE / REVIEW

business delta:
stable across all fictional subjects

same-direction calibration variance subjects = 3.
```

Then combine with real monitored compatibility differences.

Do NOT resolve them inside M12BE.

---

# 38. Next scope if shadow is clean

Required:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

and:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY.
```

Do NOT jump to fresh unseen proof.

---

# 39. No fictional rerun by default

Expected preferred path:

```text
report-contract repair

→ deterministic tests

→ M12BD fictional proof reuse authorized

→ new shadow generation

→ 18 calls if active names remain 22

→ full compatibility report

→ policy-review handoff.
```

Do NOT waste 12 new fictional model calls
unless model-facing semantic identity fails.

---

# 40. No model-facing change

M12BE should change only:

```text
shadow preflight / report-contract consumption

shadow count/ticker-set validation

reporting/provenance.
```

Expected:

```text
model_prompt_semantic_change_count = 0

model_schema_semantic_change_count = 0

stage1_wc_binding_semantic_change_count = 0

stage2_wc_binding_semantic_change_count = 0

configured_signal_view_change_count = 0

configured_financial_support_concept_change_count = 0

business_delta_view_change_count = 0

expectation_view_change_count = 0

financial_evidence_projection_change_count = 0

two_stage_core_semantic_change_count = 0

final_user_schema_change_count = 0.
```

Any nonzero model-facing change prohibits proof reuse.

---

# 41. Do not change authoritative expectation manifest semantics

Preserve:

```text
contract =
market-expectation-evidence-view-v1

subject_count

rows.
```

Do not add consumer-specific lifecycle semantics
such as `active_count` into this evidence-view contract
unless a repository-wide versioned schema migration explicitly requires it.

Preferred:

```text
fix the consumer.
```

---

# 42. Contract-aware count adapter

If introducing a helper,
it should be strict.

Example semantic contract:

```text
market-expectation-evidence-view-v1
→ authoritative count key = subject_count

also require:
subject_count == len(rows)
```

If contract unknown:

```text
FAIL.
```

Do not inspect random fields heuristically.

---

# 43. Exact ticker-set adapter

Prefer helper outputs such as:

```text
count

tickers

duplicate_count

contract.
```

For a per-ticker manifest:

```text
tickers = row["ticker"] for row in rows.
```

Fail on:

```text
missing ticker

empty ticker

duplicate ticker.
```

No silent deduplication.

---

# 44. Cross-manifest shadow preflight report

Produce one authoritative preflight matrix:

```text
artifact

contract

reported_count

actual_row_count

ticker_set_sha256

matches_active_universe

duplicate_count

status.
```

Include at least:

```text
task-start active universe

packet inventory

configured-signal view

configured financial support concept view

BusinessDeltaEvidenceView

MarketExpectationEvidenceView

Stage-1 WC binding view

Stage-2 WC binding view

frozen context manifest

batching manifest.
```

---

# 45. Required exact regression artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12be-scope-freeze

04-m12bd-fictional-proof-identity-freeze

05-shadow-preflight-active-count-failure-reproduction

06-expectation-manifest-contract-audit

07-active-count-consumer-code-audit

08-active-count-hardcode-repository-scan

09-shadow-count-contract-options

10-shadow-count-contract-decision

11-contract-aware-subject-count-adapter-contract

12-active-universe-ticker-set-identity-contract

13-cross-manifest-shadow-preflight-contract.
```

---

# 46. Required count/ticker fixtures

Produce:

```text
14-subject-count-positive-fixture

15-non22-dynamic-active-universe-positive-fixture

16-subject-count-row-count-mismatch-negative-fixture

17-same-count-different-ticker-set-negative-fixture

18-duplicate-ticker-negative-fixture

19-unknown-contract-negative-fixture

20-missing-subject-count-negative-fixture.
```

---

# 47. Required frozen M12BD preflight replay

Produce:

```text
21-m12bd-stopped-shadow-preflight-offline-replay
```

Expected:

```text
active universe count = 22

expectation subject_count = 22

expectation row count = 22

packet count = 22

ticker set mismatch = 0

duplicate = 0

status = PASS.
```

This only proves the repaired preflight.

Do not reuse old shadow generation.

---

# 48. Required semantic freeze artifacts

Produce:

```text
22-m12bd-stage2-wc-binding-freeze

23-m12bd-optional-audit-coverage-freeze

24-m12bc-context-finalizer-freeze

25-m12bc-diagnostic-variance-readiness-freeze

26-m12bb-stage1-wc-binding-freeze

27-m12ba-financial-sector-replacement-verb-freeze

28-m12az-fcf-local-temporal-scope-freeze

29-m12ay-configured-fcf-support-freeze

30-m12ax-monitoring-semantics-freeze

31-m12aw-nominal-condition-freeze

32-m12av-mixed-risk-scope-freeze

33-m12at-configured-signal-field-ownership-freeze

34-m12ap-expectation-independence-freeze

35-m12ao-business-delta-convergence-freeze

36-adr-security-basis-freeze

37-two-stage-core-immutability-freeze.
```

---

# 49. Required model-facing hash artifacts

Produce:

```text
38-model-prompt-semantic-hash-freeze

39-model-schema-semantic-hash-freeze

40-stage1-wc-binding-view-hash-freeze

41-stage2-wc-binding-view-hash-freeze

42-configured-signal-view-hash-freeze

43-configured-financial-support-concept-hash-freeze

44-business-delta-view-hash-freeze

45-expectation-view-semantic-hash-freeze

46-financial-evidence-projection-hash-freeze

47-final-user-schema-hash-freeze

48-fictional-proof-reuse-decision.
```

Artifact 48:

```text
REUSE_AUTHORIZED_M12BD_COMPLETE_PROOF
```

or:

```text
NEW_FORMAL_PROOF_REQUIRED.
```

No ambiguous value.

---

# 50. Deterministic test gate

Produce:

```text
49-count-contract-unit-tests

50-shadow-preflight-ticker-set-tests

51-stopped-shadow-offline-preflight-replay

52-focused-test-results

53-full-local-test-results

54-ruff-and-diff-results

55-hosted-ci-portability-observation

56-new-shadow-model-call-gate.
```

No shadow model calls before artifact 56 PASS.

---

# 51. New shadow setup artifacts

Produce:

```text
57-task-start-active-monitored-universe

58-new-shadow-generation-manifest

59-shadow-packet-inventory

60-shadow-packet-hash-manifest

61-shadow-stage1-wc-binding-view-manifest

62-shadow-stage2-wc-binding-view-manifest

63-shadow-configured-signal-view-manifest

64-shadow-configured-financial-support-concept-manifest

65-shadow-delta-view-manifest

66-shadow-expectation-view-manifest

67-shadow-cross-manifest-preflight-matrix

68-shadow-frozen-context-manifest

69-shadow-batching-manifest

70-shadow-model-call-gate.
```

---

# 52. Required full shadow artifacts

Produce:

```text
71-shadow-monolithic-model-artifacts

72-shadow-stage1-model-artifacts

73-shadow-stage2-model-artifacts

74-shadow-context-hard-semantic-audit

75-shadow-stage1-wc-binding-audit

76-shadow-stage2-wc-binding-audit

77-shadow-financial-grounding-audit

78-shadow-optional-audit-coverage-matrix

79-shadow-configured-signal-field-use-audit

80-shadow-fcf-audit

81-shadow-business-delta-audit

82-shadow-market-expectation-audit

83-shadow-financial-sector-audit

84-shadow-stage2-language-audit

85-shadow-final-composition-audit

86-shadow-aggregate-finalization-audit

87-shadow-per-ticker-comparison

88-shadow-core-direction-differences

89-shadow-business-delta-differences

90-shadow-new-buyer-differences

91-shadow-holder-differences

92-shadow-same-direction-calibration-differences

93-shadow-expected-contract-corrections

94-shadow-potential-architecture-regressions

95-shadow-unresolved-review-required

96-shadow-adr-security-basis-audit

97-shadow-cyclical-valuation-audit

98-shadow-core-immutability-audit

99-shadow-runtime-audit

100-shadow-aggregate-summary

101-shadow-architecture-decision.
```

---

# 53. Required combined handoff artifacts

If full shadow completes:

```text
102-fictional-primary-boundary-summary

103-fictional-delta-materiality-summary

104-fictional-new-buyer-boundary-summary

105-fictional-holder-boundary-summary

106-monitored-primary-difference-summary

107-monitored-delta-difference-summary

108-monitored-new-buyer-difference-summary

109-monitored-holder-difference-summary

110-same-direction-calibration-summary

111-combined-fictional-monitored-policy-input

112-next-bounded-policy-decision.
```

---

# 54. Required completion artifacts

Produce:

```text
113-shadow-count-contract-repair-success-decision

114-m12bd-fictional-proof-reuse-success-decision

115-new-full-shadow-completion-decision

116-two-stage-shadow-compatibility-decision

117-existing-monitored-impact-summary

118-fresh-real-proof-readiness-decision

119-final-main-merge-readiness-note

120-production-no-change

121-schedule-pause-observation

122-remote-push-prohibition-audit

123-master-workflow-update

124-program-completion.
```

---

# 55. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch
final_local_head_sha

latest_result_zip_sha256
latest_result_integrity

m12bd_fictional_generation_id
m12bd_fictional_model_calls
m12bd_stage1_rows
m12bd_stage2_rows
m12bd_final_compositions
m12bd_fictional_hard_semantic_failure_count
m12bd_fictional_aggregate_finalization_status

shadow_preflight_root_cause

old_shadow_generation_id
old_shadow_model_calls_started
old_shadow_model_calls_completed

active_count_consumer_hardcode_count_before
active_count_consumer_hardcode_count_after

expectation_manifest_contract
expectation_manifest_count_field

count_adapter_contract_version

stopped_shadow_offline_preflight_replay_status

task_start_active_monitor_count
task_start_active_monitor_tickers

cross_manifest_count_mismatch_count
cross_manifest_ticker_set_mismatch_count
cross_manifest_duplicate_ticker_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
stage1_wc_binding_semantic_change_count
stage2_wc_binding_semantic_change_count
configured_signal_view_change_count
configured_financial_support_concept_change_count
business_delta_view_change_count
expectation_view_change_count
financial_evidence_projection_change_count
two_stage_core_semantic_change_count
final_user_schema_change_count

formal_fictional_reuse_status
new_fictional_model_calls

shadow_generation_id
shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status
shadow_hard_semantic_failure_count

shadow_stage1_wc_grounding_failure_count
shadow_stage2_wc_grounding_failure_count
shadow_stage2_wc_metric_specific_mismatch_count
shadow_stage2_wc_narrative_only_substitution_count

shadow_optional_audit_zero_observation_nonblocking_count

shadow_configured_signal_violation_count
shadow_fcf_hard_failure_count
shadow_business_delta_hard_failure_count
shadow_expectation_hard_failure_count
shadow_financial_sector_hard_failure_count
shadow_stage2_language_false_positive_count

shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_same_direction_calibration_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count
shadow_timeout_count
shadow_orphan_count
shadow_wrapper_retry_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count.
```

Anything unmeasured:

```text
NOT_MEASURED.
```

---

# 56. Expected completion state if shadow is clean

Expected:

```text
formal_fictional_reuse_status =
REUSE_AUTHORIZED_M12BD_COMPLETE_PROOF

new_fictional_model_calls = 0

shadow_completed_ticker_count =
task_start_active_monitor_count

shadow_aggregate_finalization_status =
PASS

shadow_hard_semantic_failure_count =
0

two_stage_shadow_compatibility_classification =
COMPLETE_WITH_POLICY_DIAGNOSTICS

fresh_real_proof_readiness =
NOT_READY

final_main_merge_readiness =
NOT_READY

production_readiness =
NOT_READY

next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

---

# 57. Failure handling

## A. Repaired preflight still cannot consume subject_count

```text
STOP
SHADOW_REPORT_COUNT_CONTRACT_REPAIR_FAILED.
```

No model calls.

## B. Count matches but ticker sets differ

```text
STOP
SHADOW_ACTIVE_UNIVERSE_IDENTITY_MISMATCH.
```

No model calls.

## C. Model-facing semantic hashes changed unexpectedly

```text
formal_fictional_reuse_status =
NEW_FORMAL_PROOF_REQUIRED.
```

Run a new full 12-call proof before shadow.

## D. New full shadow reveals an objective hard semantic failure

Do not proceed to policy review.

Use the smallest bounded architecture/semantic repair.

## E. New full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

---

# 58. Local-only / production firewall

M12BE is LOCAL-ONLY.

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0.
```

Monitoring schedules remain paused.

Do not ask for GitHub push authorization.

---

# 59. Artifact integrity

Freeze all local artifacts before final artifact index.

Required:

```text
hash mismatch = 0

size mismatch = 0

secret scan failure = 0.
```

Report final ZIP SHA-256.

Do not push raw/report artifacts to GitHub.

---

# 60. Final task principle

M12BD achieved the milestone we were trying to reach on the fictional side:

```text
12/12 calls complete

24/24 Stage-1

24/24 Stage-2

24 final compositions

hard semantic failures = 0

aggregate finalization = PASS

shadow authorization = PASS.
```

The monitored shadow did not fail semantically.

It never started.

One stale consumer asked an authoritative evidence-view manifest for:

```text
active_count
```

while that manifest's versioned contract correctly exposes:

```text
subject_count.
```

The correct M12BE flow is therefore:

```text
keep the authoritative market-expectation contract unchanged

→ fix the stale consumer at the report-contract boundary

→ strengthen preflight from integer-only checking
   to exact active-universe ticker-set identity

→ offline replay the stopped M12BD preflight

→ prove all model-facing hashes are unchanged

→ reuse the complete clean M12BD fictional proof

→ create a NEW shadow generation

→ if active set remains 22,
   run the full 18-call monitored compatibility shadow

→ finish one comparison row per active ticker

→ if all objective hard gates are clean,
   stop engineering semantic fixes

→ hand the complete fictional + monitored cohort to:
   DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

Do NOT:

```text
rerun the 12-call fictional proof unnecessarily

add active_count blindly to the expectation-view producer

hard-code 22

reuse the stopped shadow generation

weaken ticker-set identity checks

reopen Stage-2 WC binding

reopen optional audit coverage policy

reopen context-preserving finalization

reopen decision-variance readiness policy

reopen FCF semantics

reopen financial-sector semantics

reopen configured-signal field ownership

reopen BusinessDeltaEvidenceView

reopen MarketExpectationEvidenceView

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring.
```

This is a report-contract adapter repair followed by the full monitored compatibility proof,
not another investment-judgment model repair.
