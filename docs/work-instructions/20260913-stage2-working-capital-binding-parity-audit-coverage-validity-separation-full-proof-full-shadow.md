# Thesis Monitor — Stage-2 Working-Capital Binding Parity + Audit Coverage/Validity Separation + New Full Proof + Full Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260913-stage2-working-capital-binding-parity-audit-coverage-validity-separation-full-proof-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260913-stage2-working-capital-binding-parity-audit-coverage-validity-separation-full-proof-full-shadow-report.zip
```

Master-workflow phase:

```text
M12BD — Integrated-Main Stage-2 Grounding Parity / Audit Readiness
         A. Freeze successful M12BC context-preserving finalization
         B. Freeze successful diagnostic-only decision variance policy
         C. Reproduce exact FIC-FIN-02 Stage-2 WC binding failure
         D. Extend WorkingCapitalCheckpointBindingView to Stage-2 stance-owned condition fields
         E. Add the same claim-local typed-ref grounding rule to the Stage-2 model contract
         F. Keep the hard post-compose WC validator unchanged
         G. Separate semantic validity from stochastic audit coverage
         H. Remove "zero observed monitoring-obligation claims" as a hard proof failure
         I. Start a NEW full 8 × 3 two-stage fictional proof
         J. If hard gates pass, run NEW full active-monitored same-packet shadow
         K. Hand off the complete clean cohort to boundary / delta-materiality / holder policy review
```

M12BC successfully fixed the proof-harness context-boundary problem and the obsolete
decision-stability readiness policy.

The M12BC report shows:

```text
context-preserving aggregate finalization = PASS

cross-context model batch construction count = 0

max model-call batch candidate count = 4

24 final compositions preserved

decision variance readiness_blocking = false

holder exact-enum readiness hardcode count = 0
```

The frozen M12BB proof was correctly NOT reused because the repaired final audit
found a real historical semantic failure in the frozen generation.

M12BC therefore correctly ran a NEW formal fictional proof:

```text
generation =
20260911-m12ai-fictional-20260913T100541Z-103a7a3e4c03

model =
gpt-5.6-sol / xhigh

model calls =
12 / 12

Stage 1 rows =
24

Stage 2 rows =
24

final compositions =
24

aggregate context-boundary finalization =
PASS
```

Decision-material variance was correctly treated as diagnostic-only.

The new proof blocked on exactly two items:

```text
1. FIC-FIN-02
   run-3 / context-01
   working_capital_checkpoint_typed_ref_binding_failure

2. NO_MONITORING_OBLIGATION_CLAIMS_OBSERVED
   monitoring_claim_count = 0
```

These have DIFFERENT root causes and must be repaired separately.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260913-fictional-aggregate-context-boundary-readiness-policy-alignment-full-shadow-report.zip
```

Verified SHA-256:

```text
84c73ca678bf646e977f2c3a72a7ccb903782f2eecd2348478e2ba22dc1c6779
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Final artifact index:

```text
artifact_count = 1569

hash mismatch = 0
size mismatch = 0
secret scan failure = 0

independent missing = 0
independent extra = 0
independent hash mismatch = 0
independent size mismatch = 0
```

Recompute independently.

---

# 2. M12BC repository / program status

Reported:

```text
integration branch =
codex/20260913-fictional-aggregate-context-boundary-m12bc

base integration head =
39ab9a095e7eb5ea56312bb603a7f9b928ae52b4

final local head =
7ba278a70a96c726b37e243fd8b89e1009c22ee3
```

Program:

```text
status =
BLOCKED

formal_fictional_reuse_status =
NEW_FORMAL_PROOF_REQUIRED

new_fictional_model_calls =
12

new_formal_proof_status =
FAIL

shadow calls =
0
```

Safety:

```text
provider fetches = 0

production DB mutations = 0
monitoring registration/stop mutations = 0
assessment mutations = 0
warning mutations = 0
notification writes = 0
production sends = 0

remote pushes = 0
raw model artifact remote pushes = 0

main branch mutations = 0
main merges = 0
deployments = 0
```

Keep LOCAL-ONLY.

---

# 3. M12BC finalizer repair — freeze successful

Preserve:

```text
context-preserving-proof-finalization-v1
```

Model-call batch type:

```text
DirectionalCoreBatch
max candidate count = 4
```

must remain scoped to one original model-call context.

Final report aggregation occurs over:

```text
audit rows / final composition identities
```

not by creating an 8-candidate model-call batch.

Required forever:

```text
cross_context_model_batch_construction_count = 0
```

No change in M12BD.

---

# 4. M12BC readiness policy — freeze successful

Preserve:

```text
hard semantic validity
!=
decision-material repetition stability
```

These remain DIAGNOSTIC:

```text
primary direction variance

business delta variance

new-buyer stance variance

holder stance variance

same-direction calibration/confidence variance
```

They do NOT block fictional readiness
when all objective hard semantic/runtime/core-immutability gates pass.

No return to:

```text
exact FIC-FIN-05 direction target

exact stance ratio

exact holder enum hardcode

majority resolver

post-hoc score override.
```

---

# 5. M12BC formal diagnostics — preserve as policy input

New M12BC proof diagnostics:

## FIC-FIN-05

```text
overall direction:
HOLD / HOLD / SELL

new buyer:
WAIT / WAIT / WAIT

holder:
REVIEW / REVIEW / REVIEW
```

## Business delta

```text
all subjects stable across 3 repetitions
```

## Holder

```text
all subjects stable across 3 repetitions
```

## New buyer

```text
all subjects stable across 3 repetitions
```

## Primary direction

```text
unstable subjects = 1
FIC-FIN-05
```

Do NOT repair this boundary behavior inside M12BD.

---

# 6. First M12BD root cause: Stage-2 WC binding parity

The M12BC formal proof hard failure is:

```text
FIC-FIN-02
run-3
context-01

error =
working_capital_checkpoint_typed_ref_binding_failure
```

Critically:

```text
Stage-1 FIC-FIN-02 core WC binding = PASS
```

The failing claim appears only after Stage 2 composition.

---

# 7. Exact FIC-FIN-02 Stage-1 WC controls

Stage-1 selected typed WC item:

```text
alias =
E04

metric =
inventory

evidence_kind =
TYPED_FINANCIAL

comparison =
prior_year_end

period =
POINT_IN_TIME
```

Stage-1 checkpoint claims directly bind E04.

Examples:

```text
risk_context
→ E04 + contextual refs
→ PASS

sell_drivers[1]
→ E04 + contextual refs
→ PASS

dominant_evidence
→ E04 + OCF typed ref
→ PASS

core_investment_judgment
→ E04 + OCF typed ref + context
→ PASS

business_reevaluation_down[0]
→ E04 + OCF typed ref
→ PASS
```

Stage-1 binding view:

```text
selected_working_capital_metrics =
["inventory"]

selected_typed_aliases =
["E04"]

working_capital_checkpoint_count =
5

grounded_working_capital_checkpoint_count =
5

working_capital_grounding_failure_count =
0
```

This proves the M12BB Stage-1 binding contract works.

---

# 8. Exact FIC-FIN-02 Stage-2 failure

Stage-2 output:

```text
fundamental_new_buyer.confirmation_business_condition =

"영업현금흐름과 영업이익률의 동반 개선,
 운전자본 사용의 가역성,
 안전한 현재 가치평가가 확인될 때
 신규 진입을 재평가한다."
```

Refs:

```text
E13
E09
E03
```

where:

```text
E09 =
remaining unknown:
cause/reversibility of working-capital use unknown

E03 =
valuation limit

E13 =
configured strengthen/current context according to the frozen alias set
```

The condition says:

```text
운전자본 사용의 가역성
```

which is a GENERIC WORKING-CAPITAL checkpoint.

Selected typed WC alias exists:

```text
E04 = inventory TYPED_FINANCIAL
```

but Stage-2 confirmation refs omit E04.

Post-compose binding audit:

```text
checkpoint_field =
fundamental_new_buyer.confirmation_business_condition

generic_working_capital_cue =
true

required_typed_aliases =
["E04"]

typed_refs_directly_bound =
[]

narrative_only_substitution =
true

validation_result =
FAIL
```

This is a genuine claim-local grounding failure.

---

# 9. Positive Stage-2 contrast

In the SAME candidate:

```text
fundamental_holder.business_invalidation_condition
```

says:

```text
"재고 증가와 누적 영업현금흐름 약화가
 다음 기간에도 이어져 운전자본 흡수가 지속적이라고 확인되면
 보유 가설을 무효화한다."
```

Refs include:

```text
E04 inventory typed
E08 OCF typed
E02 structural risk
```

This passes.

Therefore:

```text
Stage-2 can bind typed WC evidence correctly.

The failure is contract propagation / prompting,
not schema impossibility.
```

---

# 10. Exact Stage-2 prompt gap

Current Stage-1 prompt begins with the binding rule:

```text
WORKING_CAPITAL_CHECKPOINT_BINDING is the claim-local typed-evidence contract.
In every checkpoint field present in the current output schema,
when a claim names a selected inventory, trade-receivables,
or trade-payables metric, that same claim's evidence_refs must include
every corresponding TYPED_FINANCIAL alias...
A generic working-capital checkpoint must cite at least one selected
working-capital typed alias.
Narrative/context refs may supplement but never substitute.
```

Current Stage-2 prompt does NOT include this rule.

Current `_stage2_context()` copies only:

```text
ticker
company_name
market
assessment_date
evidence
financial_decision_context
frozen_stage1_core
core_snapshot_sha256
```

It does NOT expose the WC checkpoint binding view.

This is the main model-facing parity defect.

---

# 11. Stage-2 binding contract

M12BD must apply the same claim-local typed-evidence principle
to Stage-2-owned WC checkpoint condition fields:

```text
fundamental_new_buyer.confirmation_business_condition

fundamental_holder.business_invalidation_condition
```

Do NOT automatically apply it to summaries.

The Stage-2 output has paired refs:

```text
confirmation_business_condition
↔ confirmation_business_condition_refs

business_invalidation_condition
↔ business_invalidation_condition_refs
```

The typed ref must be bound in the paired ref field.

---

# 12. Stage-2 WC binding view

Preferred:

Create/project:

```text
WorkingCapitalCheckpointBindingView
```

into every Stage-2 per-ticker context.

Stage-2 projection should contain at least:

```text
contract =
working-capital-checkpoint-binding-view-v1

selected_working_capital_items

metric_to_typed_aliases

checkpoint_fields = [
  "fundamental_new_buyer.confirmation_business_condition",
  "fundamental_holder.business_invalidation_condition"
]
```

Do not expose Stage-1-only checkpoint fields
as if Stage 2 could write them.

---

# 13. Stage-2 concise prompt rule

Add ONE concise generic Stage-2 rule,
semantically identical to Stage 1:

```text
WORKING_CAPITAL_CHECKPOINT_BINDING is claim-local.
When a Stage-2 confirmation or invalidation condition names a selected
inventory, receivables, or payables metric, its paired refs must include
the corresponding TYPED_FINANCIAL alias.
A generic working-capital condition must cite at least one selected
working-capital typed alias.
Narrative/unknown/context refs may supplement but never substitute.
This grounds the fact and does not assign direction.
```

No ticker-specific examples.

No FIC-FIN-02 special case.

---

# 14. Do NOT relax the hard post-compose validator

The current FIC-FIN-02 failure is legitimate.

Do NOT:

```text
accept candidate-global typed WC refs

accept typed refs only in Stage-1 core

accept typed refs only in another stance condition

post-hoc inject E04

rewrite condition text

remove Stage-2 stance condition fields from checkpoint validation.
```

The stance condition that says:

```text
운전자본 사용의 가역성
```

must directly cite typed WC evidence
when selected typed WC evidence exists.

---

# 15. Generic WC condition semantics

For:

```text
"운전자본 사용의 가역성"
```

the model does not name:

```text
inventory
receivables
payables
```

specifically.

Therefore existing generic rule applies:

```text
if selected typed WC evidence exists
→ cite at least one relevant typed WC alias.
```

For FIC-FIN-02:

```text
E04 inventory
```

is sufficient typed grounding.

Do not require a non-existent receivables typed ref.

---

# 16. Specific metric Stage-2 conditions

If Stage-2 condition says:

```text
재고 증가 지속성
```

require:

```text
inventory typed alias.
```

If it says:

```text
매출채권 회수
```

require:

```text
trade-receivables typed alias.
```

If it says both and both typed metrics are selected:

```text
require both.
```

Preserve M12BB metric-specific semantics.

---

# 17. Narrative refs remain supplemental

Stage-2 condition may also cite:

```text
remaining unknown

structural risk

configured strengthen/weaken/invalidation

valuation limitation
```

where relevant.

But:

```text
narrative refs may supplement
never substitute for selected typed WC evidence.
```

No change.

---

# 18. Stage-2 schema support

M12BB already established:

```text
conditional JSON-Schema binding unsupported / not safely portable
```

Do not reopen unsupported schema tricks.

Use:

```text
Stage-2 binding view
+
concise prompt contract
+
existing alias-constrained schema
+
existing hard post-compose validator.
```

No `if/then` schema experiment unless platform capability has materially changed
and a separate portability proof is produced.

Default architecture:

```text
CLAIM_LOCAL_TYPED_REF_BINDING_WITH_MODEL_VIEW_AND_HARD_VALIDATOR
```

for BOTH stages.

---

# 19. Monolithic parity

The monolithic path already receives the shared/current binding rule
through the core prompt path.

Audit that monolithic stance-owned checkpoint fields
receive the same selected typed binding semantics.

Required:

```text
monolithic_stage2_wc_binding_semantic_equality = PASS
```

If monolithic path does not enforce stance-owned binding,
fix parity before shadow.

Do not compare architectures with asymmetric grounding rules.

---

# 20. Exact FIC-FIN-02 historical candidate handling

The M12BC historical Stage-2 candidate remains:

```text
INVALID
```

Do not reinterpret it as valid.

Expected after M12BD offline replay:

```text
Stage-1 core binding = PASS

Stage-2 new-buyer confirmation =
FAIL historical candidate

reason =
missing selected typed WC alias E04

holder invalidation =
PASS.
```

This is a historical negative control.

---

# 21. Corrected-equivalent Stage-2 fixture

Create a fixture with identical FIC-FIN-02 confirmation text:

```text
"영업현금흐름과 영업이익률의 동반 개선,
 운전자본 사용의 가역성,
 안전한 현재 가치평가가 확인될 때
 신규 진입을 재평가한다."
```

but refs including:

```text
E04 inventory typed

plus relevant:
E13 / E09 / E03
within allowed ref count.
```

Expected:

```text
PASS.
```

Do not mutate the historical model artifact.

---

# 22. Holder positive control

Preserve exact M12BC holder invalidation:

```text
재고 증가 + OCF 약화 + persistent WC absorption condition
```

with direct E04 typed binding.

Expected:

```text
PASS.
```

---

# 23. Stage-2 no-selected-WC case

If ticker has:

```text
no selected typed WC evidence
```

a stance condition mentioning working capital
must follow existing fail-closed semantics.

Do not invent typed evidence.

Do not derive inventory/receivables values from narrative prose.

---

# 24. Working-capital direction remains unspecified

Stage-2 typed grounding does NOT mean:

```text
inventory increase = negative

receivables increase = negative

working-capital use = automatically REVIEW/AVOID.
```

The stance model still judges:

```text
materiality
persistence
reversibility
confirmation need
```

under stance contracts.

Required:

```text
unsafe_wc_auto_direction_count = 0.
```

---

# 25. Second M12BD root cause: monitoring audit coverage != semantic validity

M12BC formal proof also hard-failed:

```text
NO_MONITORING_OBLIGATION_CLAIMS_OBSERVED

monitoring_claim_count = 0.
```

Audit artifact:

```text
candidate_count = 48

monitoring_claim_count = 0

monitoring_obligation_false_reject_count = 0

current_financial_false_accept_count = 0

no_configured_support_false_accept_count = 0

violations = []
```

This is NOT a semantic violation.

It means only:

```text
the stochastic proof did not happen to emit
one of the lexical monitoring-obligation forms.
```

---

# 26. Monitoring-obligation deterministic fixtures already carry coverage

M12AX/M12AY deterministic regression suites already verify:

```text
safe prospective monitoring language → PASS

current magnitude + monitoring language → current / FAIL without evidence

current fulfillment + monitoring language → current / FAIL without evidence

no configured support → fail closed

English parity → correct.
```

Therefore formal stochastic generation does NOT need to contain
at least one monitoring-obligation sentence
for semantic readiness.

Do not force the model to emit specific lexical forms.

---

# 27. Audit contract redesign: validity vs coverage

Every optional lexical/semantic audit should report separately:

```text
observed_claim_count

semantic_violation_count

coverage_status

semantic_status

readiness_blocking.
```

For monitoring-obligation audit:

If:

```text
observed_claim_count = 0
semantic_violation_count = 0
```

expected:

```text
semantic_status = PASS

coverage_status = NO_OBSERVED_CLAIMS
or NOT_OBSERVED

readiness_blocking = false.
```

Top-level audit status may be:

```text
PASS
```

or:

```text
NOT_APPLICABLE
```

according to repository conventions,
but MUST NOT be `FAIL`.

---

# 28. Coverage cannot masquerade as a hard failure

Remove:

```text
failure_reason =
NO_MONITORING_OBLIGATION_CLAIMS_OBSERVED
```

from hard semantic readiness.

If retained as diagnostic metadata:

```text
coverage_note =
NO_MONITORING_OBLIGATION_CLAIMS_OBSERVED
```

is acceptable.

But:

```text
hard failure list must NOT include it.
```

---

# 29. Apply the same principle to optional audits

Audit the following helpers for the same bug class:

```text
monitoring-obligation

nominal-condition

mixed-risk temporal forms

negated-fulfillment forms

FCF configured-future form families

financial-sector replacement-verb families.
```

Do NOT mechanically change them if they already pass with zero observed claims.

The principle:

```text
absence of an optional lexical form
!=
semantic failure.
```

Hard failure is justified only for:

```text
a claim is observed and misclassified

a forbidden claim is falsely accepted

a required objective contract for a specific fixture is violated.
```

---

# 30. Coverage-required exceptions

A coverage count may be mandatory ONLY in:

```text
explicit deterministic fixture tests
```

designed to exercise that contract.

For those fixtures:

```text
expected_match_count > 0
```

may be a test assertion.

Do not transfer that requirement to stochastic model-proof outputs.

---

# 31. Exact monitoring audit expected after repair

For the existing M12BC generation:

```text
monitoring_claim_count = 0

false rejects = 0

current false accepts = 0

no-support false accepts = 0.
```

Expected offline re-audit:

```text
semantic_status = PASS

coverage_status = NO_OBSERVED_CLAIMS

readiness_blocking = false.
```

This removes the SECOND hard failure.

---

# 32. M12BC finalizer/readiness policy — freeze

Do not reopen:

```text
context-preserving finalization

diagnostic-only decision variance

holder exact-enum hardcode removal.
```

The next proof must continue to report:

```text
primary direction variance

business delta variance

new-buyer variance

holder variance

calibration/confidence variance
```

without blocking readiness.

---

# 33. Frozen M12BB reuse decision — preserve

M12BC correctly found a real hard semantic failure
during the offline M12BB re-finalization:

```text
FIC-FIN-07
run-3 / context-02

net_debt_claim_without_complete_net_debt_evidence.
```

Therefore:

```text
formal_fictional_reuse_status =
NEW_FORMAL_PROOF_REQUIRED
```

was correct.

Do NOT attempt to salvage/reuse M12BB now.

M12BD starts from the repaired integrated code
and must run a NEW full proof because Stage-2 model-facing grounding changes.

---

# 34. M12BD model-facing semantic impact

This task intentionally changes:

```text
Stage-2 prompt semantics

Stage-2 model context
WorkingCapitalCheckpointBindingView projection
```

Expected:

```text
model_prompt_semantic_change_count >= 1

stage2_prompt_semantic_change_count >= 1

working_capital_checkpoint_binding_view_change_count >= 1
```

Potential schema semantic change:

```text
0
```

unless implementation unexpectedly requires one.

External/user schema change:

```text
0.
```

Because Stage-2 model-facing semantics change:

```text
NEW full formal fictional proof is mandatory.
```

No proof reuse.

---

# 35. Stage-1 semantics must remain unchanged

Required:

```text
Stage-1 prompt binding contract unchanged

Stage-1 WC binding view unchanged

Stage-1 output schema unchanged

Stage-1 hard validator unchanged.
```

M12BD only propagates the same contract to stance-owned fields.

---

# 36. Stage-2 stance contracts remain unchanged

Do NOT change:

```text
ATTRACTIVE / WAIT / AVOID semantics

HOLDABLE / REVIEW / REDUCE semantics

SELL does not force REDUCE

UNCHANGED does not force REVIEW

price/technical/supply cannot create REDUCE.
```

No stance policy repair in M12BD.

---

# 37. Stage-2 core immutability remains hard

Preserve:

```text
Stage 2 cannot emit or modify core fields.

core_snapshot_sha256
==
post_compose_core_sha256.
```

Required:

```text
core mutation = 0.
```

---

# 38. Deterministic M12BD fixture set

## STAGE2-WC-P01

FIC-FIN-02 generic WC confirmation condition:

```text
"운전자본 사용의 가역성..."
```

refs include selected E04 typed inventory.

Expected:

```text
PASS.
```

## STAGE2-WC-N01

Same condition,
refs omit E04 and use only narrative/unknown/valuation refs.

Expected:

```text
FAIL:
working_capital_checkpoint_typed_ref_binding_failure.
```

## STAGE2-WC-P02

Holder invalidation names inventory
and directly cites E04.

Expected:

```text
PASS.
```

## STAGE2-WC-N02

Specific inventory condition
but typed receivables ref only.

Expected:

```text
FAIL metric-specific mismatch.
```

## STAGE2-WC-N03

Generic WC condition,
selected typed inventory exists,
but only narrative refs supplied.

Expected:

```text
FAIL narrative-only substitution.
```

## STAGE2-WC-P03

No WC wording in stance condition.

Expected:

```text
no typed WC ref requirement.
```

Do not force irrelevant financial refs.

---

# 39. Monitoring-audit fixtures

## AUDIT-COVERAGE-P01

Input candidates contain:

```text
0 monitoring-obligation claims.
```

No semantic violations.

Expected:

```text
semantic_status = PASS
coverage_status = NO_OBSERVED_CLAIMS
readiness_blocking = false.
```

## AUDIT-COVERAGE-P02

One valid monitoring-obligation claim.

Expected:

```text
semantic_status = PASS
observed_claim_count = 1.
```

## AUDIT-COVERAGE-N01

One current financial claim incorrectly accepted as prospective
because it has monitoring language.

Expected:

```text
semantic_status = FAIL
readiness_blocking = true.
```

## AUDIT-COVERAGE-N02

One monitoring claim with no configured support
is incorrectly accepted.

Expected:

```text
FAIL.
```

This keeps validity strict while coverage remains diagnostic.

---

# 40. Optional-audit coverage matrix

Produce a report for:

```text
audit name

observed claim count

semantic violation count

zero-observation behavior

readiness-blocking rule.
```

At minimum include:

```text
monitoring obligation

nominal condition

mixed-risk temporal claim

negated fulfillment

configured FCF future claim

financial-sector replacement verb.
```

No audit may become permissive.

---

# 41. M12BC frozen-output offline diagnostic replay

Before new model calls,
re-run the M12BC final audit with ONLY
the monitoring coverage/readiness harness change.

Expected:

```text
monitoring hard failure disappears.

FIC-FIN-02 Stage-2 WC binding failure remains hard.
```

This proves the two root causes are independent.

Do not claim M12BC proof is valid.

---

# 42. Stage-2 WC replay after model-contract repair

The historical M12BC Stage-2 candidate remains INVALID.

Do not edit it.

Use deterministic corrected fixtures
to prove the new model-facing contract.

Because the prompt/context changes,
a NEW model proof is required
to show the model follows the contract naturally.

---

# 43. Prior regression freeze

Re-run and preserve:

```text
M12BC context-preserving finalizer

M12BC diagnostic-only variance readiness

M12BB Stage-1 WC binding

M12BA financial-sector replacement verb parity

M12AZ FCF local temporal span / fulfillment polarity

M12AY configured FCF support mapping

M12AX monitoring-obligation semantics

M12AW nominal-condition semantics

M12AV mixed-risk temporal scope

M12AT configured-signal field ownership

M12AS UNCHANGED claim scope

M12AR FCF claim-local evidence scope

M12AP MarketExpectationEvidenceView

M12AQ financial-sector exclusions

M12AO BusinessDeltaEvidenceView convergence

M12AN PPE proxy semantics

M12AM Korean Stage-2 lexical safety

QTD/YTD

WC direction-unspecified semantics

debt completeness

ADR/security basis

core immutability.
```

No regression.

---

# 44. Deterministic gate before model calls

Require:

```text
latest result ZIP integrity PASS

M12BC context-preserving finalizer PASS

decision variance readiness_blocking = false

FIC-FIN-02 historical Stage-2 invalid replay = FAIL_EXPECTED

corrected-equivalent Stage-2 WC fixture = PASS

Stage-2 holder positive WC fixture = PASS

Stage-2 metric mismatch negative fixture = FAIL_EXPECTED

Stage-2 narrative-only substitution negative fixture = FAIL_EXPECTED

Stage-2 no-WC condition = PASS without irrelevant typed ref requirement

monitoring zero-observation audit = PASS/nonblocking

monitoring true semantic negative controls still FAIL

all optional audit coverage/readiness rules reviewed

all prior regression families PASS

focused tests PASS

full local tests PASS

ruff PASS

git diff --check PASS

production firewall PASS

model =
gpt-5.6-sol / xhigh.
```

No model calls before all pass.

---

# 45. NEW full fictional proof

Run:

```text
NEW generation ID

8 fictional subjects

2 contexts

4 subjects/context

3 repetitions

Stage 1 calls = 6

Stage 2 calls = 6

total = 12

24 Stage-1 rows

24 Stage-2 rows

24 final compositions.
```

No selective rerun.

No stitching.

No fallback.

No judge calls.

---

# 46. Fictional hard-stop policy

Hard stop only for objective violations:

```text
runtime/schema failure

invalid evidence identity

Stage-1 WC grounding failure

Stage-2 WC grounding failure

WC metric-specific ref mismatch

WC narrative-only substitution

irrelevant financial typed-ref grounding

unsafe WC auto-direction

configured-signal field-use violation

configured-signal false fulfillment

FCF hard semantic violation

financial-sector hard semantic violation

business-delta hard semantic violation

market-expectation hard semantic violation

Stage-2 contamination

ADR/security-basis violation

core mutation

context-boundary finalization failure

aggregate finalization failure.
```

Do NOT hard-stop for:

```text
zero optional lexical coverage

primary direction variance

delta materiality variance

new-buyer variance

holder variance

same-direction calibration/confidence variance.
```

---

# 47. Formal fictional acceptance

Require:

```text
12 / 12 model calls complete

Stage 1 = 24 / 24 hard PASS

Stage 2 = 24 / 24 hard PASS

final compositions = 24 / 24 hard PASS

aggregate finalization = PASS

context-boundary integrity = PASS

core mutation = 0

Stage-1 WC grounding failure = 0

Stage-2 WC grounding failure = 0

WC metric mismatch = 0

WC narrative substitution = 0

irrelevant financial typed-ref grounding = 0

unsafe WC auto-direction = 0

configured-signal current-driver violation = 0

configured-signal false fulfillment = 0

FCF hard failure = 0

financial-sector hard failure = 0

business-delta hard failure = 0

expectation hard failure = 0

Stage-2 lexical false positive = 0

timeout = 0

orphan = 0

wrapper retry = 0.
```

Optional audit observed counts may be zero.

---

# 48. Formal diagnostics

Report:

```text
primary-direction values ×3

business-delta values ×3

new-buyer stances ×3

holder stances ×3

balance/confidence values ×3.
```

Keep:

```text
readiness_blocking = false
```

for valid decision variance.

Preserve special visibility into:

```text
FIC-FIN-02 delta / Stage-2 WC conditions

FIC-FIN-05 primary boundary

FIC-FIN-06 delta materiality

FIC-FIN-08 holder stance.
```

No policy repair inside M12BD.

---

# 49. Formal Stage-2 WC audit

For every Stage-2 stance condition report:

```text
ticker

stance field

condition text

paired ref field

selected WC metrics

typed aliases

specific metric cues

generic WC cue

required typed aliases

actual refs

typed refs directly bound

narrative refs directly bound

grounded?

metric mismatch?

narrative substitution?

validation result.
```

This is mandatory.

---

# 50. Shadow authorization

If formal proof passes all objective hard gates:

```text
run a NEW full active-monitored same-packet shadow.
```

No fresh unseen issuers.

Existing monitored names remain the compatibility cohort.

---

# 51. Active monitored universe

Re-enumerate active monitored stocks read-only.

Last verified reference:

```text
22 active names.
```

Use actual task-start list.

No monitoring registration/stop mutation.

---

# 52. Same-packet contract

For each active ticker:

```text
one frozen reproducible packet

same packet hash feeds:
monolithic
Stage 1
Stage 2.
```

No provider refresh.

Required:

```text
provider_source_fetches = 0.
```

---

# 53. Monolithic / two-stage binding parity

For every ticker require semantic equality of:

```text
WorkingCapitalCheckpointBindingView

selected typed WC items

metric_to_typed_aliases

stance-owned WC checkpoint semantics

ConfiguredSignalEvidenceView

BusinessDeltaEvidenceView

MarketExpectationEvidenceView

configured financial support concepts

financial evidence projection.
```

No architecture comparison if these differ.

---

# 54. Full shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic = context_count

Stage 1 = context_count

Stage 2 = context_count

total = 3 × context_count.
```

At N=22:

```text
18 calls.
```

No repetitions.

---

# 55. Shadow hard-stop policy

Hard stop only for objective semantic/runtime/ownership failures.

Do NOT hard-stop on monolithic-vs-two-stage decision differences.

Measure those with the established taxonomy.

---

# 56. Shadow comparison taxonomy

Use:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED.
```

Neither architecture is automatically ground truth.

---

# 57. Potential architecture regression

Classify as architecture regression only if objective evidence exists:

```text
hard semantic violation

invalid ref

WC grounding loss

configured-signal ownership violation

FCF safety violation

financial-sector misuse

price/timing contamination

core mutation

Stage-2 contamination.
```

A plain decision difference is not automatically a regression.

---

# 58. Full shadow acceptance

Require:

```text
all planned calls complete

all active tickers represented exactly once

packet mismatch = 0

all monolithic hard semantics PASS

all Stage-1 hard semantics PASS

all Stage-2 hard semantics PASS

all final compositions PASS

aggregate finalization PASS

core mutation = 0

Stage-1 WC grounding failure = 0

Stage-2 WC grounding failure = 0

metric-specific mismatch = 0

narrative substitution = 0

irrelevant financial grounding = 0

unsafe WC auto-direction = 0

configured-signal violations = 0

FCF hard failures = 0

financial-sector hard failures = 0

business-delta hard failures = 0

expectation hard failures = 0

Stage-2 contamination = 0

ADR/security-basis failure = 0

timeout = 0

orphan = 0

wrapper retry = 0

production side effects = 0.
```

Decision differences may be nonzero.

---

# 59. Shadow Stage-2 WC audit

For every stance condition containing WC language,
report the same claim-local binding fields as Section 49.

Required:

```text
Stage-2 WC narrative substitution = 0

Stage-2 WC metric mismatch = 0.
```

---

# 60. Shadow optional semantic audit coverage

For each optional lexical audit report:

```text
observed_claim_count

semantic_violation_count

coverage_status

semantic_status

readiness_blocking.
```

Zero observed claims:

```text
must not block readiness.
```

---

# 61. Current workflow handoff — freeze M12BC correction

If formal + full shadow pass:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY

next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

Do NOT jump to fresh unseen proof.

---

# 62. Why policy review is next

After a clean full cohort,
remaining differences are expected to be judgment policy questions:

```text
HOLD ↔ SELL/BUY boundary

5.5 ↔ 6.0 directional threshold

business-delta materiality

new-buyer WAIT ↔ AVOID/ATTRACTIVE

holder HOLDABLE ↔ REVIEW

confidence/calibration variance.
```

These belong to an explicit policy task,
not more lexical/grounding validators.

---

# 63. Local-only / production firewall

M12BD is LOCAL-ONLY.

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0.
```

Monitoring schedules remain paused.

Do not ask for GitHub push authorization.

---

# 64. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12bd-scope-freeze

04-integrated-main-lineage-freeze

05-m12bc-fic-fin-02-stage2-wc-failure-reproduction

06-m12bc-stage1-wc-pass-contrast

07-stage2-prompt-binding-gap-audit

08-stage2-context-binding-view-gap-audit

09-stage2-wc-binding-options

10-stage2-wc-binding-decision

11-monitoring-audit-coverage-vs-validity-root-cause

12-optional-audit-readiness-policy-audit.
```

---

# 65. Required Stage-2 binding artifacts

Produce:

```text
13-stage2-working-capital-checkpoint-binding-contract

14-stage2-working-capital-binding-view-contract

15-stage2-specific-metric-ref-contract

16-stage2-generic-wc-ref-contract

17-stage2-narrative-supplement-not-substitute-contract

18-stage2-monolithic-binding-parity-contract

19-stage2-hard-postcompose-validator-preservation-contract.
```

---

# 66. Required audit policy artifacts

Produce:

```text
20-optional-semantic-audit-coverage-contract

21-monitoring-obligation-zero-observation-contract

22-semantic-validity-vs-coverage-contract

23-coverage-required-only-in-deterministic-fixtures-contract

24-hard-readiness-audit-classification-contract.
```

---

# 67. Required exact replay / fixtures

Produce:

```text
25-m12bc-fic-fin-02-stage2-historical-invalid-replay

26-fic-fin-02-stage2-corrected-equivalent-positive-fixture

27-stage2-holder-wc-positive-control

28-stage2-specific-metric-mismatch-negative-fixture

29-stage2-narrative-only-substitution-negative-fixture

30-stage2-no-wc-no-typed-ref-required-fixture

31-monitoring-zero-observation-nonblocking-fixture

32-monitoring-observed-valid-fixture

33-monitoring-current-false-accept-negative-fixture

34-monitoring-no-support-false-accept-negative-fixture.
```

---

# 68. Required frozen-regression artifacts

Produce:

```text
35-m12bc-context-preserving-finalizer-freeze

36-m12bc-diagnostic-variance-readiness-freeze

37-m12bb-stage1-wc-binding-freeze

38-m12ba-financial-sector-replacement-verb-freeze

39-m12az-fcf-local-temporal-scope-freeze

40-m12ay-configured-fcf-support-freeze

41-m12ax-monitoring-semantics-freeze

42-m12aw-nominal-condition-freeze

43-m12av-mixed-risk-scope-freeze

44-m12at-configured-signal-field-ownership-freeze

45-m12as-unchanged-claim-scope-freeze

46-m12ar-fcf-claim-scope-freeze

47-m12ap-expectation-independence-freeze

48-m12aq-financial-sector-exclusion-freeze

49-m12ao-business-delta-convergence-freeze

50-m12an-ppe-proxy-freeze

51-m12am-stage2-lexical-freeze

52-qtd-ytd-wc-debt-safety-freeze

53-adr-security-basis-freeze

54-two-stage-core-immutability-freeze.
```

---

# 69. Required model-facing impact artifacts

Produce:

```text
55-stage1-prompt-semantic-hash-freeze

56-stage1-schema-semantic-hash-freeze

57-stage2-prompt-semantic-diff

58-stage2-schema-semantic-hash-freeze

59-stage2-context-semantic-diff

60-working-capital-binding-view-stage2-diff

61-configured-signal-view-semantic-hash-freeze

62-business-delta-view-semantic-hash-freeze

63-expectation-view-semantic-hash-freeze

64-configured-financial-support-concept-hash-freeze

65-final-user-schema-no-change-proof

66-new-formal-proof-decision.
```

Expected:

```text
66 =
NEW_FORMAL_PROOF_REQUIRED.
```

---

# 70. Required deterministic test gate

Produce:

```text
67-focused-test-results

68-full-local-test-results

69-ruff-and-diff-results

70-hosted-ci-portability-observation

71-new-fictional-model-call-gate.
```

No model calls before artifact 71 PASS.

---

# 71. Required NEW fictional artifacts

Produce:

```text
72-fictional-generation-manifest

73-fictional-stage1-wc-binding-view-manifest

74-fictional-stage2-wc-binding-view-manifest

75-fictional-configured-signal-view-manifest

76-fictional-configured-financial-support-concept-manifest

77-fictional-delta-view-manifest

78-fictional-expectation-view-manifest

79-stage1-run1-context01
80-stage1-run1-context02
81-stage2-run1-context01
82-stage2-run1-context02
83-stage1-run2-context01
84-stage1-run2-context02
85-stage2-run2-context01
86-stage2-run2-context02
87-stage1-run3-context01
88-stage1-run3-context02
89-stage2-run3-context01
90-stage2-run3-context02

91-fictional-context-hard-semantic-audit

92-fictional-stage1-wc-binding-audit

93-fictional-stage2-wc-binding-audit

94-fictional-financial-grounding-audit

95-fictional-optional-audit-coverage-matrix

96-fictional-configured-signal-field-use-audit

97-fictional-fcf-audit

98-fictional-business-delta-audit

99-fictional-market-expectation-audit

100-fictional-financial-sector-audit

101-fictional-stage2-language-audit

102-fictional-final-composition-audit

103-fictional-aggregate-finalization-audit

104-fictional-decision-variance-diagnostics

105-fictional-core-immutability-audit

106-fictional-runtime-audit

107-fictional-shadow-gate-decision.
```

---

# 72. Required shadow setup artifacts

If artifact 107 authorizes shadow:

```text
108-task-start-active-monitored-universe

109-shadow-packet-inventory

110-shadow-packet-hash-manifest

111-shadow-stage1-wc-binding-view-manifest

112-shadow-stage2-wc-binding-view-manifest

113-shadow-configured-signal-view-manifest

114-shadow-configured-financial-support-concept-manifest

115-shadow-delta-view-manifest

116-shadow-expectation-view-manifest

117-shadow-frozen-context-manifest

118-shadow-batching-manifest

119-shadow-model-call-gate.
```

---

# 73. Required full shadow artifacts

Produce:

```text
120-shadow-monolithic-model-artifacts

121-shadow-stage1-model-artifacts

122-shadow-stage2-model-artifacts

123-shadow-context-hard-semantic-audit

124-shadow-stage1-wc-binding-audit

125-shadow-stage2-wc-binding-audit

126-shadow-financial-grounding-audit

127-shadow-optional-audit-coverage-matrix

128-shadow-configured-signal-field-use-audit

129-shadow-fcf-audit

130-shadow-business-delta-audit

131-shadow-market-expectation-audit

132-shadow-financial-sector-audit

133-shadow-stage2-language-audit

134-shadow-final-composition-audit

135-shadow-aggregate-finalization-audit

136-shadow-per-ticker-comparison

137-shadow-core-direction-differences

138-shadow-business-delta-differences

139-shadow-new-buyer-differences

140-shadow-holder-differences

141-shadow-same-direction-calibration-differences

142-shadow-expected-contract-corrections

143-shadow-potential-architecture-regressions

144-shadow-unresolved-review-required

145-shadow-adr-security-basis-audit

146-shadow-cyclical-valuation-audit

147-shadow-core-immutability-audit

148-shadow-runtime-audit

149-shadow-aggregate-summary

150-shadow-architecture-decision.
```

---

# 74. Required combined policy-handoff diagnostics

If full shadow completes:

```text
151-fictional-primary-boundary-summary

152-fictional-delta-materiality-summary

153-fictional-new-buyer-boundary-summary

154-fictional-holder-boundary-summary

155-monitored-primary-difference-summary

156-monitored-delta-difference-summary

157-monitored-new-buyer-difference-summary

158-monitored-holder-difference-summary

159-same-direction-calibration-summary

160-working-capital-stage2-grounding-lessons

161-combined-fictional-monitored-policy-input

162-next-bounded-policy-decision.
```

---

# 75. Required completion artifacts

Produce:

```text
163-stage2-wc-binding-parity-success-decision

164-optional-audit-coverage-validity-success-decision

165-context-preserving-finalizer-preservation-decision

166-diagnostic-variance-readiness-preservation-decision

167-new-fictional-proof-success-decision

168-full-shadow-completion-decision

169-existing-monitored-impact-summary

170-two-stage-shadow-compatibility-decision

171-fresh-real-proof-readiness-decision

172-final-main-merge-readiness-note

173-production-no-change

174-schedule-pause-observation

175-remote-push-prohibition-audit

176-master-workflow-update

177-program-completion.
```

---

# 76. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch
final_local_head_sha

latest_result_zip_sha256
latest_result_integrity

m12bc_generation_id
m12bc_model_calls
m12bc_stage1_rows
m12bc_stage2_rows
m12bc_final_compositions

m12bc_context_boundary_status
m12bc_primary_direction_unstable_subject_count
m12bc_business_delta_unstable_subject_count
m12bc_new_buyer_unstable_subject_count
m12bc_holder_unstable_subject_count

m12bc_hard_failures

stage2_wc_binding_root_cause
stage2_wc_binding_contract_version

stage2_wc_binding_view_present
stage2_wc_binding_prompt_rule_present

stage1_wc_binding_semantic_change_count
stage2_wc_binding_semantic_change_count

historical_fic_fin_02_stage2_status
corrected_equivalent_fic_fin_02_stage2_status
holder_stage2_wc_positive_control_status

stage2_wc_grounding_failure_count
stage2_wc_metric_specific_mismatch_count
stage2_wc_narrative_only_substitution_count
stage2_irrelevant_financial_ref_grounding_failure_count
stage2_unsafe_wc_auto_direction_count

monitoring_audit_zero_observation_status
monitoring_audit_zero_observation_readiness_blocking

optional_audit_zero_observation_hard_failure_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
stage2_prompt_semantic_change_count
stage2_context_semantic_change_count
working_capital_checkpoint_binding_view_change_count

configured_signal_view_change_count
configured_financial_support_concept_change_count
business_delta_view_change_count
expectation_view_change_count
financial_evidence_projection_change_count
two_stage_core_semantic_change_count
final_user_schema_change_count

new_formal_proof_required

fictional_generation_id
fictional_model_calls_total
fictional_stage1_row_count
fictional_stage2_row_count
fictional_final_composition_count

fictional_context_boundary_status
fictional_aggregate_finalization_status
fictional_hard_semantic_failure_count

fictional_stage1_wc_checkpoint_count
fictional_stage1_wc_grounding_failure_count

fictional_stage2_wc_checkpoint_count
fictional_stage2_wc_grounding_failure_count
fictional_stage2_wc_metric_specific_mismatch_count
fictional_stage2_wc_narrative_only_substitution_count

fictional_optional_audit_zero_observation_nonblocking_count

fictional_configured_signal_violation_count
fictional_fcf_hard_failure_count
fictional_business_delta_hard_failure_count
fictional_expectation_hard_failure_count
fictional_financial_sector_hard_failure_count
fictional_stage2_language_false_positive_count

fictional_primary_direction_unstable_subject_count
fictional_business_delta_unstable_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count
fictional_same_direction_calibration_variance_subject_count

fictional_core_mutation_count
fictional_timeout_count
fictional_orphan_count
fictional_wrapper_retry_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id
shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status
shadow_hard_semantic_failure_count

shadow_stage1_wc_grounding_failure_count
shadow_stage2_wc_grounding_failure_count
shadow_stage2_wc_metric_specific_mismatch_count
shadow_stage2_wc_narrative_only_substitution_count

shadow_optional_audit_zero_observation_nonblocking_count

shadow_configured_signal_violation_count
shadow_fcf_hard_failure_count
shadow_business_delta_hard_failure_count
shadow_expectation_hard_failure_count
shadow_financial_sector_hard_failure_count
shadow_stage2_language_false_positive_count

shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_same_direction_calibration_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count
shadow_timeout_count
shadow_orphan_count
shadow_wrapper_retry_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count.
```

Anything not measured:

```text
NOT_MEASURED.
```

---

# 77. Completion state if clean

If new formal proof and full monitored shadow both pass:

```text
two_stage_shadow_compatibility_classification =
COMPLETE_WITH_POLICY_DIAGNOSTICS
```

and:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY

next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

Do not jump to fresh unseen proof.

---

# 78. Failure handling

## A. Stage-2 FIC-FIN-02 generic WC confirmation still omits typed E04

Do not relax validator.

```text
next_scope =
STAGE2_WC_REF_SELECTION_ARCHITECTURE_REVIEW
```

No shadow.

## B. Adding Stage-2 binding forces irrelevant typed refs into non-WC conditions

```text
STOP
STAGE2_WC_BINDING_TOO_BROAD.
```

## C. Stage-2 WC binding changes stance enum mechanically

Investigate before proceeding.

Typed grounding must not become an automatic stance rule.

## D. Zero monitoring claims still block readiness

```text
STOP
OPTIONAL_AUDIT_COVERAGE_STILL_MISCLASSIFIED_AS_SEMANTIC_FAILURE.
```

## E. Optional audit becomes permissive for an observed invalid claim

```text
STOP
AUDIT_VALIDITY_REPAIR_TOO_PERMISSIVE.
```

## F. New formal proof reveals another objective hard semantic failure

Do not run shadow.

Use the smallest bounded failing contract.

## G. Full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

---

# 79. Artifact integrity

Freeze all local artifacts before final artifact index.

Required:

```text
hash mismatch = 0

size mismatch = 0

secret scan failure = 0.
```

Report final ZIP SHA-256.

Do not push raw/report artifacts to GitHub.

---

# 80. Final task principle

M12BC successfully fixed the proof harness:

```text
context boundary = correct

decision variance = diagnostic-only

aggregate finalization = PASS.
```

The new proof exposed two bounded remaining issues.

First:

```text
Stage 1 knows:
selected inventory typed alias E04

Stage 1 checkpoint claims cite E04 correctly

Stage 2 receives evidence aliases
but does NOT receive the WC binding view/rule

Stage 2 new-buyer confirmation says:
"운전자본 사용의 가역성"

but cites only narrative/unknown/valuation refs

post-compose hard validator correctly rejects it.
```

The correct response is:

```text
propagate the same claim-local typed-ref grounding contract
into Stage-2 stance-owned confirmation/invalidation fields.
```

Second:

```text
the proof happened to emit zero monitoring-obligation lexical forms

semantic violations = 0

but the audit treated zero coverage as hard failure.
```

The correct response is:

```text
separate coverage from validity.

Zero observed optional claims is nonblocking;
an observed misclassified claim remains a hard failure.
```

Then:

```text
run a completely NEW full 12-call fictional proof

→ if objective hard gates pass,
   run a completely NEW full monitored shadow

→ if that cohort is clean,
   hand it to
   DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

Do NOT:

```text
candidate-globalize WC grounding

remove the Stage-2 stance condition from the grounding contract

post-hoc inject E04

force the model to emit "감시해야 한다" only to satisfy coverage

turn optional lexical coverage into semantic validity

reopen context-preserving finalization

reopen decision-variance readiness policy

reopen FCF semantics

reopen financial-sector replacement semantics

reopen configured-signal field ownership

reopen BusinessDeltaEvidenceView

reopen MarketExpectationEvidenceView

resume/stitch M12BC

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring.
```

A Stage-2 financial condition must be grounded by the same typed evidence standard
as Stage 1, while the absence of an optional linguistic form is only a coverage fact,
not a semantic failure.
