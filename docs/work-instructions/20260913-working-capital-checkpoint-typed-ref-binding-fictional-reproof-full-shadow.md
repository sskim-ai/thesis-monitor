# Thesis Monitor — Working-Capital Checkpoint Typed-Ref Binding + New Full Fictional Proof + Full Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260913-working-capital-checkpoint-typed-ref-binding-fictional-reproof-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260913-working-capital-checkpoint-typed-ref-binding-fictional-reproof-full-shadow-report.zip
```

Master-workflow phase:

```text
M12BB — Integrated-Main Working-Capital Claim Grounding
         A. Freeze successful M12BA replacement-verb parity
         B. Reproduce exact FIC-FIN-06 run-2 claim-local grounding failure
         C. Freeze the existing working-capital checkpoint grounding contract
         D. Add pre-model claim-local typed-ref binding for checkpoint fields
         E. Preserve direction-unspecified working-capital semantics
         F. Preserve hard post-model grounding validation
         G. Start a NEW full 8 × 3 two-stage fictional proof
         H. If hard gates pass, run NEW full active-monitored same-packet shadow
         I. Hand off the complete clean cohort to boundary / delta-materiality / holder policy review
```

M12BA successfully repaired the financial-sector replacement-verb parity issue.

Before the new proof:

```text
exact M12AZ FIC-FIN-08 run-3 replay = PASS

run-1 FIC-FIN-08 regression = PASS

run-2 FIC-FIN-08 regression = PASS

"해석한다 / 해석합니다" replacement application = supported

negative "해석하지 않는다" false accept = 0

unknown replacement concept false accept = 0

shared replacement-verb family = enabled

replacement verb family definition count = 1

financial-sector exclusion false reject = 0

financial-sector true misuse false accept = 0

M12AZ completed 40-row offline re-audit = PASS

M12AY / M12AV / M12AW / M12AX re-audits = PASS
```

A NEW M12BA formal fictional proof then progressed to:

```text
6 / 12 model calls

Stage 1 calls = 4
Stage 2 calls = 2

Stage 1 rows = 16
Stage 2 rows = 8

target FIC-FIN-08 in the failed call = PASS

replacement-verb failure count = 0
```

The sole hard failure was:

```text
FIC-FIN-06
run-2 / Stage 1 / context-02

error =
working_capital_grounding_failure
```

The model did NOT misuse working-capital semantics.

It correctly said that rising inventory and receivables
are confirmation points, not automatic negatives.

The failure was EVIDENCE BINDING:

```text
risk_context:
mentions inventory + receivables
but cites narrative refs only

sell_drivers:
mentions inventory + receivables
but cites narrative refs only

sector_interpretation:
cites the canonical typed inventory + receivables refs
```

The existing grounding contract is intentionally claim-local.

A typed ref in some OTHER field does not ground
a directional working-capital checkpoint claim.

M12BB must preserve that contract and stabilize
the model-facing ref binding.

Do NOT relax the validator into candidate-global grounding.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260913-financial-sector-replacement-interpretation-verb-parity-fictional-reproof-full-shadow-report.zip
```

Verified SHA-256:

```text
bd16c7e286338151e90b9253af102ec87d92b4c6509831f5b794d04a74a957b1
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact-index verification:

```text
artifact_count = 552

ZIP entries including artifact-index = 553

hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute independently.

---

# 2. M12BA repository / proof status

Reported:

```text
integration branch =
codex/20260913-financial-sector-verb-parity-m12ba

base integration head =
e32b6da0d2e223545112bf42f28b2dbcaa6d45e6

final local head =
bc2206ae7c63553035eb6864e93354e9246bd4af
```

Formal proof generation:

```text
20260911-m12ai-fictional-20260913T061344Z-ae4ac52e00ff
```

Status:

```text
BLOCKED_AFTER_6_OF_12_FICTIONAL_CALLS
```

No monitored shadow was run.

Safety:

```text
provider_source_fetches = 0

production_db_mutations = 0
assessment persistence mutations = 0
warning mutations = 0
notification writes = 0
production sends = 0

remote pushes = 0
raw artifact remote pushes = 0

main branch mutations = 0
main merges = 0
deployments = 0

scheduler mutation = 0
automatic monitoring resume = 0
```

Keep LOCAL-ONLY.

---

# 3. M12BA replacement-verb repair — freeze successful

Preserve:

```text
korean-replacement-application-verb-v1
```

including:

```text
본다
봅니다

판단한다
판단합니다

평가한다
평가합니다

해석한다
해석합니다
```

with:

```text
negative predicates excluded

recognized replacement concept required

contradictory industrial-framework use remains hard
```

Do not reopen this scope.

---

# 4. Exact FIC-FIN-06 financial context

FIC-FIN-06 selected typed financial context:

```text
E06
metric = inventory

reported balance =
higher than prior year-end

semantic category =
WORKING_CAPITAL

comparison kind =
prior_year_end
```

and:

```text
E05
metric = trade_accounts_receivable

reported balance =
higher than prior year-end

semantic category =
WORKING_CAPITAL

comparison kind =
prior_year_end
```

These are:

```text
TYPED_FINANCIAL

DIRECT_REPORTED

verified
```

They are not automatically directional negatives.

Preserve:

```text
inventory higher
receivables higher
→ direction UNSPECIFIED
unless safe business context establishes material meaning.
```

---

# 5. Exact model-facing evidence

FIC-FIN-06 input includes:

```text
E03
STRUCTURAL_RISK
"Inventory and trade receivables have risen since year-end."

E05
TYPED_FINANCIAL
trade_accounts_receivable

E06
TYPED_FINANCIAL
inventory

E10
DATA_QUALITY_LIMIT
demand quality / collection timing unknown

E11
SECTOR_OPERATING_CURRENT
"Inventory and receivables are confirmation points,
 not automatic negatives."
```

Therefore the model has both:

```text
narrative/context refs
and
canonical typed refs.
```

The issue is not missing data.

It is where the typed refs are bound.

---

# 6. FIC-FIN-06 run-1 PASS control

Run-1 Stage 1 passed.

`risk_context`:

```text
text:
"연말 이후 늘어난 재고와 매출채권은
 자동 악화가 아니라 수요 품질과 회수 시점을 확인할 지점이다."

refs:
E05
E06
E10
```

`sell_drivers[0]`:

```text
text:
"연말 이후 늘어난 재고와 매출채권은
 현금 전환 품질의 확인 필요성을 높인다."

refs:
E05
E06
```

Grounding:

```text
working_capital_checkpoint_count = 3

grounded_working_capital_checkpoint_count = 3

financial_checkpoint_refs =
inventory typed ref
trade-receivables typed ref

working_capital_grounding_failure_count = 0

status = PASS
```

This is the target valid binding shape.

---

# 7. FIC-FIN-06 run-2 FAIL

Run-2 `risk_context`:

```text
text:
"연말 이후 재고와 매출채권 증가는
 자동적인 악화가 아니라 확인 지점이며,
 지속적인 음의 영업현금흐름이 구조적으로 확인되면
 향후 사업 무효화 위험이 된다."

refs:
E03
E07
E11
```

Run-2 `sell_drivers[0]`:

```text
text:
"재고와 매출채권 증가는
 부정적 결론이 아니라
 수요의 질과 회수 시점을 확인해야 하는 제약이다."

refs:
E03
E10
E11
```

The typed refs:

```text
E05
E06
```

appear only in:

```text
sector_interpretation
```

with:

```text
"연말 이후 재고와 매출채권이 늘었지만
 잔액만으로 악화를 단정할 수 없고
 수요·회수 전환을 확인해야 한다."
```

---

# 8. Exact failure audit

Observed:

```text
working_capital_checkpoint_count = 2

grounded_working_capital_checkpoint_count = 0

working_capital_grounding_required = true

financial_checkpoint_refs = []

working_capital_grounding_failure_count = 1
```

But candidate-global financial use shows:

```text
used_financial_refs =
inventory typed ref
trade-receivables typed ref
```

The failure forensic correctly records:

```text
direct_typed_refs_bound_outside_checkpoint:
inventory
trade receivables

direct_typed_refs_field =
sector_interpretation

checkpoint_paths:
risk_context
sell_drivers
```

This proves the current validator is claim-local.

---

# 9. Root-cause classification

Freeze the primary root cause as:

```text
WORKING_CAPITAL_CHECKPOINT_TYPED_REFS_AVAILABLE
BUT_MODEL_BINDS_THEM_TO_NON_CHECKPOINT_FIELD
```

Secondary root cause:

```text
MODEL_OUTPUT_SCHEMA_PERMITS_NARRATIVE_ONLY_REFS
FOR A CLAIM THAT LEXICALLY ASSERTS A SELECTED TYPED
WORKING_CAPITAL CHECKPOINT
```

This is NOT:

```text
a working-capital direction error

a working-capital validator false positive

a missing financial data error

a BusinessDeltaEvidenceView error

a financial-sector error.
```

---

# 10. Do NOT relax claim-local grounding

Forbidden primary repair:

```text
if typed inventory/receivable ref appears anywhere
in the candidate,
consider every working-capital checkpoint grounded.
```

This would allow:

```text
risk_context:
asserts inventory/receivable fact
with narrative-only refs

sector_interpretation:
cites unrelated typed refs

→ candidate-global accidental grounding
```

That weakens evidence provenance.

Keep:

```text
the claim that makes the financial checkpoint
must directly cite the relevant typed evidence.
```

---

# 11. Do NOT post-hoc inject refs

Forbidden:

```text
after model output,
automatically add E05/E06 to risk_context/sell_drivers.
```

No candidate mutation.

No semantic repair after generation.

The model-facing contract must make the valid binding clear/structural,
and the post-model validator must continue to verify it.

---

# 12. Do NOT make rising WC automatically bearish

The exact FIC-FIN-06 candidate is semantically conservative:

```text
inventory / receivables increased

BUT

that does not automatically prove deterioration

→ demand quality and collection conversion need confirmation.
```

Preserve this.

Typed ref binding does NOT imply:

```text
SELL
WEAKENED
negative direction
```

It only grounds the factual checkpoint.

No unsafe metric auto-direction.

---

# 13. Freeze the checkpoint field contract

Before implementation, inspect the actual grounding validator
and freeze the fields that count as directional WC checkpoints.

Expected from current audit:

```text
risk_context

sell_drivers
```

Do not expand to every field just because a WC term appears.

If code audit shows additional existing checkpoint paths,
record them exactly.

Do not invent new checkpoint fields inside M12BB.

---

# 14. WorkingCapitalCheckpointBindingView

Preferred internal model-facing view:

```text
WorkingCapitalCheckpointBindingView
```

or equivalent.

Per ticker include:

```text
selected_working_capital_items:
- alias
- canonical ref
- metric
- evidence kind
- comparison kind
- period type

metric_to_typed_aliases:
inventory -> [...]
trade_accounts_receivable -> [...]
trade_accounts_payable -> [...]

checkpoint_fields:
risk_context
sell_drivers
```

No user-facing schema.

No persistence.

This view is for:

```text
model-facing ref-binding constraints
and
post-model audit identity.
```

---

# 15. Claim-local metric binding

For checkpoint fields:

If claim text mentions a specific selected metric,
require that SAME claim's `evidence_refs`
include the typed alias for that metric.

Examples:

```text
text mentions 재고 / inventory
→ include selected inventory typed alias

text mentions 매출채권 / trade receivables / receivables
→ include selected trade-receivable typed alias
```

If both are mentioned:

```text
require BOTH relevant typed aliases
```

when both selected typed metrics are available.

Do not accept:

```text
inventory typed ref alone
for a receivables-specific claim
```

and vice versa.

---

# 16. Generic working-capital language

For checkpoint claims that say only:

```text
운전자본
working capital
working-capital quality
```

without naming a specific metric:

If selected typed WC evidence exists,
require:

```text
at least one relevant selected typed working-capital ref
```

when the claim is a current checkpoint.

Do not require every selected WC metric
for generic WC wording.

If no selected typed WC evidence exists:

```text
do not manufacture grounding

retain existing fail-closed / narrative-only semantics.
```

---

# 17. Metric cue family

Use a bounded deterministic cue family.

At minimum:

```text
inventory:
재고
inventory
inventories

trade receivables:
매출채권
trade receivable
trade receivables
accounts receivable
receivables

trade payables if selected by the actual context:
매입채무
trade payable
trade payables
accounts payable
payables

generic:
운전자본
working capital
working-capital
```

Do not use ambiguous acronyms such as:

```text
AR
AP
```

without structural context.

---

# 18. Preferred enforcement: schema-level conditional binding

First audit whether the actual Codex / Structured Output schema dialect
supports safe use of:

```text
if / then

pattern

contains

allOf

anyOf
```

for the output schema.

If supported by the actual runner,
preferred implementation:

For each ticker and checkpoint field:

```text
IF claim.text mentions inventory
THEN claim.evidence_refs contains an inventory typed alias

IF claim.text mentions receivables
THEN claim.evidence_refs contains a receivables typed alias

IF generic WC wording
THEN claim.evidence_refs contains at least one selected typed WC alias
```

For `sell_drivers` this applies per array item.

This is structural:
the invalid run-2 candidate should fail the new schema
before semantic finalization.

---

# 19. Schema portability gate

Do not assume JSON Schema keyword support.

Before choosing schema-conditional enforcement,
create a local deterministic portability check using the same:

```text
Codex output-schema path

schema parser / validator path

hosted CI schema validation path
```

Required:

```text
conditional schema accepted locally

conditional schema accepted by output-schema validator

no unsupported keyword silently ignored.
```

If not provable:

```text
DO NOT use unsupported schema keywords.
```

Use the fallback architecture below.

---

# 20. Fallback architecture if conditional schema is unsupported

If the Structured Output subset cannot express claim-text → ref constraints safely:

Use:

```text
WorkingCapitalCheckpointBindingView
+
one concise generic Stage-1 instruction
+
existing hard post-model grounding validator.
```

Suggested prompt rule:

```text
When risk_context or sell_drivers mentions a selected
working-capital metric, that same claim must cite the
corresponding TYPED_FINANCIAL alias.
Narrative/context refs may supplement but never substitute.
```

Also expose compact per-ticker binding metadata such as:

```text
working_capital_checkpoint_binding:
{
  inventory: ["E06"],
  trade_accounts_receivable: ["E05"]
}
```

Do not add ticker-specific prose examples to the core prompt.

---

# 21. Preferred choice hierarchy

Use:

```text
1. safe schema-conditional binding
2. if unsupported:
   explicit binding view + concise prompt contract + hard validator
```

Do NOT choose:

```text
candidate-global grounding

post-hoc ref injection

validator relaxation.
```

Record the architecture decision before new model calls.

---

# 22. Existing alias constraints — preserve

M12AT already field-fences configured-only future signals
from current driver fields.

Preserve all existing:

```text
buy_drivers alias constraints

sell_drivers alias constraints

core judgment constraints

dominant evidence constraints

material anchor constraints.
```

M12BB only adds the WC checkpoint typed-binding contract.

Do not reopen configured-signal ownership.

---

# 23. Narrative refs may supplement

A valid checkpoint claim may cite:

```text
typed inventory/receivable ref

+

structural-risk narrative ref

+

remaining unknown / sector context
```

if within max ref count.

Example:

```text
risk_context refs:
E05
E06
E10
```

is valid.

Do not force removal of all narrative refs.

The rule is:

```text
narrative may supplement
but may not substitute for typed checkpoint evidence.
```

---

# 24. Narrative-only duplicates cannot ground the checkpoint

Examples:

```text
E03:
"Inventory and trade receivables have risen since year-end."

E11:
"Inventory and receivables are confirmation points, not automatic negatives."
```

These remain useful contextual evidence.

But if a checkpoint claim says:

```text
"재고와 매출채권이 증가했다..."
```

and only cites E03/E11:

```text
working-capital checkpoint remains ungrounded.
```

Preserve the validator.

---

# 25. Exact run-2 historical candidate result

After M12BB:

The exact preserved M12BA run-2 candidate should NOT
be reinterpreted as valid.

Expected:

```text
historical candidate semantic grounding =
FAIL
```

because:

```text
risk_context/sell_drivers
still lack direct typed WC refs.
```

If schema-conditional enforcement is selected:

```text
historical raw candidate
fails new Stage-1 schema binding constraint.
```

This is expected.

Do not rewrite the historical candidate.

---

# 26. Exact run-1 historical candidate result

The exact M12BA run-1 FIC-FIN-06 candidate must remain:

```text
PASS
```

because the WC checkpoint fields directly cite:

```text
inventory typed ref

trade-receivables typed ref.
```

This is the positive historical control.

---

# 27. Corrected-equivalent fixture

Create a deterministic fixture using the run-2 text
but direct typed refs on each checkpoint claim.

Example:

```text
risk_context:
same run-2 text

refs include:
inventory typed ref
trade-receivables typed ref
plus allowed contextual refs
```

and:

```text
sell_driver:
same run-2 text

refs include:
inventory typed ref
trade-receivables typed ref
plus optional contextual refs
```

Expected:

```text
PASS
```

This is a fixture only.

Do not mutate the historical model artifact.

---

# 28. Metric-specific negative fixture

Example:

```text
text:
"매출채권 증가의 회수 전환을 확인해야 한다."

refs:
inventory typed ref only

Expected:
FAIL
```

A typed WC ref is not automatically relevant
to every WC metric.

---

# 29. Multi-metric negative fixture

Example:

```text
text:
"재고와 매출채권 증가를 확인해야 한다."

refs:
inventory typed ref only
```

If both selected typed metrics are available:

```text
FAIL
```

because receivables are ungrounded.

Likewise receivables-only ref:

```text
FAIL
```

for the inventory half.

---

# 30. Generic-WC positive fixture

Example:

```text
text:
"운전자본의 질은 추가 확인이 필요하다."

selected typed WC refs exist

evidence_refs:
at least one relevant typed WC ref
```

Expected:

```text
PASS
```

if that field is an existing checkpoint path.

Do not require direction.

---

# 31. No selected typed WC fixture

If a ticker has:

```text
no selected typed inventory/receivable/payable evidence
```

and candidate checkpoint says:

```text
"재고 증가가 확인됐다."
```

a narrative ref must not magically become typed grounding.

Expected:

```text
existing fail-closed behavior
```

or no model access to the unsupported checkpoint
if schema architecture safely enforces it.

Do not synthesize financial values.

---

# 32. Working-capital direction remains unspecified

Preserve M12AJ:

```text
inventory higher/lower
trade receivables higher/lower
```

are context-dependent.

Typed grounding means:

```text
the checkpoint fact is real
```

not:

```text
it is automatically good/bad.
```

Required:

```text
unsafe_metric_auto_direction_count = 0
```

No automatic:

```text
inventory ↑ → WEAKENED
receivables ↑ → SELL
```

---

# 33. Business-delta semantics remain separate

FIC-FIN-06 current typed WC refs are eligible observed change
but direction-unspecified where appropriate.

Preserve:

```text
BusinessDeltaEvidenceView

AI_JUDGMENT

direction-unspecified eligible refs

known safe operating-evidence direction hint.
```

Do not let the new checkpoint-binding schema
force business_thesis_change direction.

---

# 34. Market-expectation semantics — freeze

Preserve M12AP:

```text
MarketExpectationEvidenceView

context-only expectation
not an independent material anchor.
```

No changes.

---

# 35. Financial-sector semantics — freeze

Preserve M12BA/M12AQ:

```text
financial-sector exclusion

replacement verb family

해석한다 parity

true industrial-framework misuse hard fail.
```

No changes.

---

# 36. FCF temporal/support semantics — freeze

Preserve M12AZ/M12AY/M12AR/M12AN:

```text
FCF local temporal claim span

negated fulfillment polarity

configured FCF support mapping

FCF ≠ OCF

FCF ≠ OCF-PPE proxy

unsupported current FCF hard safety.
```

No changes.

---

# 37. Monitoring / nominal / mixed-risk semantics — freeze

Preserve:

```text
M12AX monitoring-obligation semantics

M12AW nominal-condition semantics

M12AV mixed-risk clause scope.
```

No changes.

---

# 38. Model-facing semantic impact

M12BB will likely change:

```text
Stage-1 model-facing schema
and/or
Stage-1 prompt / model-facing binding metadata.
```

Therefore record:

```text
model_schema_semantic_change_count

model_prompt_semantic_change_count

working_capital_checkpoint_binding_view_change_count
```

Do not pretend this is validator-only
if pre-model constraints change.

A NEW full formal fictional proof is mandatory either way.

---

# 39. External/user-facing schema

Do NOT change:

```text
final external directional result schema

persisted monitoring schema

public API schemas.
```

The binding constraint is only for:

```text
internal model-generation contract.
```

Required:

```text
final_user_schema_change_count = 0
```

---

# 40. Monolithic / Stage-1 parity

The same WC checkpoint binding contract must apply
to both architecture paths used in the later shadow:

```text
monolithic
Stage 1
```

Do not constrain Stage 1 while leaving monolithic free
to use narrative-only checkpoint refs.

Required:

```text
monolithic_stage1_wc_binding_view_equality = PASS
```

---

# 41. Stage-2 contract

Stage 2 does not rewrite the frozen Stage-1 core.

Do not add WC checkpoint ref logic to Stage 2
unless Stage 2 actually owns a field with the same existing grounding contract.

Default:

```text
Stage-2 schema unchanged.
```

Preserve core immutability.

---

# 42. Exact offline controls before model calls

Before new proof:

1. Exact M12BA run-1 FIC-FIN-06:

```text
PASS
```

2. Exact M12BA run-2 FIC-FIN-06:

```text
historical candidate remains INVALID
for claim-local WC grounding.
```

3. If schema conditional selected:

```text
run-2 raw candidate fails new binding schema.
```

4. Corrected-equivalent run-2 fixture:

```text
PASS
```

5. Metric-specific mismatch fixtures:

```text
FAIL
```

6. Generic-WC binding fixture:

```text
PASS
```

7. Unsafe direction inference:

```text
0
```

---

# 43. Re-audit completed M12BA proof rows

Offline revalidate all completed M12BA rows.

Do NOT expect the old run-2 FIC-FIN-06 candidate
to become semantically valid.

Instead report:

```text
prior PASS rows remain PASS

known run-2 FIC-FIN-06 grounding failure remains correctly invalid

new schema/binding contract identifies it pre-model
if structural enforcement is selected

no new false accept

no new false reject among prior valid rows.
```

This is diagnostic only.

Do not stitch old proof generations.

---

# 44. Prior regression suite

Re-run:

```text
M12BA replacement-verb regressions

M12AZ FCF local temporal regressions

M12AY configured FCF support regressions

M12AX monitoring-obligation regressions

M12AW nominal-condition regressions

M12AV mixed-risk regressions

M12AT configured-signal field ownership regressions

M12AS UNCHANGED claim-scope regressions

M12AR FCF claim-scope regressions

M12AP expectation-independence regressions

M12AQ financial-sector regressions

M12AO business-delta convergence regressions

QTD/YTD
WC
debt
ADR/security-basis

Stage-2 lexical safety

core immutability.
```

No regressions.

---

# 45. Deterministic test gate

Before model calls require:

```text
latest ZIP integrity PASS

working-capital checkpoint field contract frozen

schema capability audit complete

binding architecture decision frozen

run-1 positive historical control PASS

run-2 historical invalid control correctly rejected

corrected-equivalent run-2 fixture PASS

metric-specific mismatch fixtures FAIL as expected

generic WC fixture PASS

no typed WC selected fixture fail-closed

unsafe metric auto-direction = 0

all prior regression families PASS

focused tests PASS

full local tests PASS

ruff PASS

git diff --check PASS

production firewall PASS

model =
gpt-5.6-sol / xhigh.
```

No model calls before all pass.

---

# 46. Architecture decision values

Choose exactly one:

```text
CLAIM_LOCAL_TYPED_REF_BINDING_WITH_SCHEMA_CONDITIONAL
```

or, only if schema dialect cannot safely express it:

```text
CLAIM_LOCAL_TYPED_REF_BINDING_WITH_MODEL_VIEW_AND_HARD_VALIDATOR
```

If neither can be made safe:

```text
STOP
WORKING_CAPITAL_CHECKPOINT_BINDING_ARCHITECTURE_REVIEW_REQUIRED
```

No model calls.

---

# 47. NEW full fictional proof is mandatory

M12BA stopped after:

```text
6 / 12
```

formal model calls.

The new WC binding is model-facing.

Therefore:

```text
NEW generation ID

NEW full 12-call fictional proof
```

is mandatory.

Do NOT:

```text
continue M12BA

run only remaining calls

reuse old PASS rows as formal proof

rerun only FIC-FIN-06.
```

---

# 48. Formal proof topology

Subjects:

```text
FIC-FIN-01
FIC-FIN-02
FIC-FIN-03
FIC-FIN-04
FIC-FIN-05
FIC-FIN-06
FIC-FIN-07
FIC-FIN-08
```

Topology:

```text
3 repetitions

2 contexts / repetition

4 subjects / context

Stage 1 calls = 6

Stage 2 calls = 6

total = 12

final compositions = 24.
```

Model:

```text
gpt-5.6-sol / xhigh
```

No judge calls.

No fallback.

No selective rerun.

---

# 49. Formal hard-stop policy

Hard stop only for:

```text
runtime/schema hard failure

invalid evidence identity

working-capital checkpoint binding violation

working-capital grounding failure

irrelevant financial ref grounding

unsafe WC direction inference

true financial-sector misuse

legitimate financial-sector exclusion false reject

FCF temporal/support violation

configured-signal field-use violation

configured-signal false fulfillment

business-delta violation

market-expectation anchor violation

price/technical/supply Core contamination

Stage-2 actual contamination

ADR/security-basis failure

core mutation

aggregate finalization failure.
```

Do NOT stop for:

```text
valid direction variance

valid delta materiality variance

valid stance variance.
```

---

# 50. Formal fictional acceptance

Require:

```text
12 / 12 model calls complete

24 / 24 Stage-1 rows hard PASS

24 / 24 Stage-2 rows hard PASS

24 final compositions

aggregate finalization PASS

working-capital checkpoint grounding failure = 0

WC checkpoint narrative-only substitution = 0

WC metric-specific ref mismatch = 0

irrelevant financial ref grounding failure = 0

unsafe WC auto-direction = 0

configured-only current-driver violation = 0

configured-signal false fulfillment = 0

business-delta violation = 0

expectation anchor violation = 0

financial-sector exclusion false reject = 0

financial-sector true misuse false accept = 0

FCF local-scope violation = 0

current unsupported FCF false accept = 0

Stage-2 lexical false positive = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0.
```

Decision-material variance does not block shadow.

---

# 51. FIC-FIN-06 formal diagnostic

Across all 3 repetitions report:

```text
risk_context text
risk_context refs

sell_driver texts
sell_driver refs

sector_interpretation text
sector_interpretation refs

selected WC typed refs

financial_checkpoint_refs

working_capital_checkpoint_count

grounded_working_capital_checkpoint_count

working_capital_grounding_failure_count

irrelevant_financial_ref_grounding_failure_count

business_thesis_change

overall direction

directional balance.
```

This is mandatory.

---

# 52. Formal policy diagnostics

Also report each subject ×3:

```text
overall_direction

directional_balance

business_thesis_change

new-buyer stance

holder stance

confidence.
```

Preserve visibility into:

```text
FIC-FIN-02 delta materiality

FIC-FIN-05 primary threshold

FIC-FIN-06 delta materiality

FIC-FIN-08 holder boundary.
```

Do not repair those in M12BB.

---

# 53. Shadow authorization

If formal fictional hard gates pass:

```text
run a NEW full active-monitored same-packet shadow.
```

No fresh unseen issuers.

Existing monitored names remain compatibility subjects.

---

# 54. Active monitored universe

Re-enumerate active monitored stocks read-only.

Last verified reference:

```text
22 active monitored names.
```

Use actual task-start set.

No registration / stop mutation.

---

# 55. Same-packet shadow contract

For every active ticker:

```text
one reproducible local frozen packet

same packet hash feeds:
monolithic
Stage 1
Stage 2.
```

No provider refresh.

Required:

```text
provider_source_fetches = 0.
```

---

# 56. Monolithic / Stage-1 binding equality

For every ticker with selected typed WC context,
require equality of:

```text
WorkingCapitalCheckpointBindingView

metric_to_typed_aliases

checkpoint fields

typed financial selection

narrative duplicate classification.
```

Required:

```text
monolithic_stage1_wc_binding_view_equality = PASS.
```

No architecture comparison otherwise.

---

# 57. Full shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic calls = context_count

Stage 1 calls = context_count

Stage 2 calls = context_count

total = 3 × context_count.
```

At N=22:

```text
18 calls.
```

No repetitions.

---

# 58. Shadow hard-stop policy

Hard stop only for:

```text
runtime/schema failure

manifest/hash mismatch

packet mismatch

invalid evidence identity

working-capital checkpoint typed-binding violation

working-capital grounding failure

irrelevant financial grounding

unsafe WC direction inference

true financial semantic violation

financial-sector scope violation

FCF temporal/support violation

configured-signal field-use violation

configured-signal false fulfillment

business-delta violation

market-expectation anchor violation

Stage-2 contamination

ADR/security-basis failure

core mutation

production-side-effect attempt

aggregate finalization failure.
```

Do not stop because monolithic and two-stage decisions differ.

---

# 59. Shadow WC checkpoint audit

For every candidate with selected typed WC evidence,
report:

```text
ticker

selected WC metrics

typed aliases

checkpoint field

claim text

metric cues detected

required typed aliases

actual claim refs

typed refs directly bound

narrative refs directly bound

claim grounded?

metric-specific mismatch?

validation result.
```

This is mandatory.

---

# 60. Shadow narrative-only duplicate audit

For narrative evidence that substantially duplicates
selected typed WC facts, report:

```text
ticker

narrative ref

duplicate typed metric/ref

fields where selected

whether it substituted for typed checkpoint grounding

validation result.
```

Required:

```text
narrative-only substitution count = 0.
```

Do not necessarily ban contextual supplementation.

---

# 61. Shadow comparison taxonomy

Use:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED.
```

Neither architecture is automatically ground truth.

---

# 62. Complete compatibility goal

M12BB must produce:

```text
one final comparison row per active ticker

all active tickers represented exactly once

aggregate finalization PASS

complete monolithic-vs-two-stage compatibility cohort.
```

Do not introduce boundary/delta/holder policy changes mid-generation.

---

# 63. Policy handoff after complete shadow

If full shadow completes without systematic architecture regression:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN
```

Use:

```text
NEW M12BB formal fictional proof

+

M12BB complete monitored shadow.
```

Do NOT perform the policy repair inside M12BB.

---

# 64. Local-only / production firewall

M12BB is LOCAL-ONLY.

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0.
```

Do not request GitHub push authorization.

Monitoring schedules remain paused.

---

# 65. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12bb-scope-freeze

04-integrated-main-lineage-freeze

05-m12ba-fic-fin-06-run2-failure-reproduction

06-fic-fin-06-run1-pass-contrast

07-fic-fin-06-run2-ref-binding-forensic

08-working-capital-grounding-validator-code-audit

09-stage1-alias-schema-code-audit

10-structured-output-conditional-schema-capability-audit

11-working-capital-checkpoint-binding-options

12-working-capital-checkpoint-binding-decision
```

---

# 66. Required binding-contract artifacts

Produce:

```text
13-working-capital-checkpoint-binding-view-contract

14-checkpoint-field-contract

15-metric-cue-family-contract

16-specific-metric-typed-ref-binding-contract

17-generic-working-capital-binding-contract

18-narrative-supplement-not-substitute-contract

19-metric-specific-relevance-contract

20-no-selected-typed-wc-fail-closed-contract

21-monolithic-stage1-wc-binding-equality-contract
```

---

# 67. Required exact replay / fixtures

Produce:

```text
22-m12ba-fic-fin-06-run1-positive-replay

23-m12ba-fic-fin-06-run2-historical-invalid-replay

24-m12ba-fic-fin-06-run2-new-schema-rejection
    if schema-conditional architecture selected

25-run2-corrected-equivalent-positive-fixture

26-inventory-only-binding-fixtures

27-receivables-only-binding-fixtures

28-multi-metric-binding-fixtures

29-generic-working-capital-binding-fixtures

30-no-selected-typed-wc-negative-fixtures

31-direction-unspecified-working-capital-regressions
```

---

# 68. Required prior-regression artifacts

Produce:

```text
32-m12ba-replacement-verb-freeze

33-m12az-fcf-local-temporal-scope-freeze

34-m12ay-configured-fcf-support-freeze

35-m12ax-monitoring-obligation-freeze

36-m12aw-nominal-condition-freeze

37-m12av-mixed-risk-scope-freeze

38-m12at-configured-signal-field-ownership-freeze

39-m12as-unchanged-claim-scope-freeze

40-m12ar-fcf-claim-scope-freeze

41-m12aq-financial-sector-scope-freeze

42-m12ap-expectation-independence-freeze

43-m12ao-business-delta-convergence-freeze

44-m12an-ppe-proxy-label-freeze

45-m12am-stage2-lexical-freeze

46-qtd-ytd-wc-debt-safety-freeze

47-adr-security-basis-freeze

48-two-stage-ownership-freeze

49-price-timing-renderer-no-change.
```

---

# 69. Required model-facing impact artifacts

Produce:

```text
50-model-prompt-semantic-diff

51-model-schema-semantic-diff

52-working-capital-checkpoint-binding-view-diff

53-business-delta-view-semantic-hash-freeze

54-expectation-view-semantic-hash-freeze

55-configured-signal-view-semantic-hash-freeze

56-configured-financial-support-concept-hash-freeze

57-final-user-schema-no-change-proof

58-fictional-formal-reproof-decision.
```

Expected:

```text
artifact 58 =
NEW_FORMAL_PROOF_REQUIRED.
```

Do not claim proof reuse.

---

# 70. Required deterministic test gate

Produce:

```text
59-focused-test-results

60-full-local-test-results

61-ruff-and-diff-results

62-hosted-ci-portability-observation

63-new-fictional-model-call-gate.
```

No model calls before artifact 63 PASS.

---

# 71. Required NEW fictional artifacts

Produce:

```text
64-fictional-generation-manifest

65-fictional-wc-checkpoint-binding-view-manifest

66-fictional-configured-signal-view-manifest

67-fictional-configured-financial-support-concept-manifest

68-fictional-delta-view-manifest

69-fictional-expectation-view-manifest

70-stage1-run1-context01

71-stage1-run1-context02

72-stage2-run1-context01

73-stage2-run1-context02

74-stage1-run2-context01

75-stage1-run2-context02

76-stage2-run2-context01

77-stage2-run2-context02

78-stage1-run3-context01

79-stage1-run3-context02

80-stage2-run3-context01

81-stage2-run3-context02

82-fictional-context-hard-semantic-audit

83-fictional-working-capital-checkpoint-binding-audit

84-fictional-financial-grounding-audit

85-fictional-configured-signal-field-use-audit

86-fictional-fcf-local-temporal-scope-audit

87-fictional-business-delta-audit

88-fictional-market-expectation-audit

89-fictional-financial-sector-audit

90-fictional-stage2-language-audit

91-fictional-final-composition-audit

92-fictional-aggregate-finalization-audit

93-fictional-primary-direction-diagnostic

94-fictional-delta-materiality-diagnostic

95-fictional-new-buyer-diagnostic

96-fictional-holder-diagnostic

97-fictional-core-immutability-audit

98-fictional-runtime-audit

99-fictional-shadow-gate-decision.
```

---

# 72. Required shadow setup artifacts

If artifact 99 authorizes shadow:

```text
100-task-start-active-monitored-universe

101-shadow-packet-inventory

102-shadow-packet-hash-manifest

103-shadow-wc-checkpoint-binding-view-manifest

104-shadow-configured-signal-view-manifest

105-shadow-configured-financial-support-concept-manifest

106-shadow-delta-view-manifest

107-shadow-expectation-view-manifest

108-shadow-frozen-context-manifest

109-shadow-batching-manifest

110-shadow-model-call-gate.
```

---

# 73. Required full shadow artifacts

Produce:

```text
111-shadow-monolithic-model-artifacts

112-shadow-stage1-model-artifacts

113-shadow-stage2-model-artifacts

114-shadow-context-hard-semantic-audit

115-shadow-working-capital-checkpoint-binding-audit

116-shadow-financial-grounding-audit

117-shadow-configured-signal-field-use-audit

118-shadow-fcf-local-temporal-scope-audit

119-shadow-business-delta-audit

120-shadow-market-expectation-audit

121-shadow-financial-sector-audit

122-shadow-stage2-language-audit

123-shadow-final-composition-audit

124-shadow-aggregate-finalization-audit

125-shadow-per-ticker-comparison

126-shadow-core-direction-differences

127-shadow-business-delta-differences

128-shadow-new-buyer-differences

129-shadow-holder-differences

130-shadow-same-direction-calibration-differences

131-shadow-expected-contract-corrections

132-shadow-potential-architecture-regressions

133-shadow-unresolved-review-required

134-shadow-adr-security-basis-audit

135-shadow-cyclical-valuation-audit

136-shadow-core-immutability-audit

137-shadow-runtime-audit

138-shadow-aggregate-summary

139-shadow-architecture-decision.
```

---

# 74. Required combined diagnostics

If full shadow completes:

```text
140-fic-fin-05-vs-monitored-primary-boundary-analogs

141-fic-fin-02-vs-monitored-delta-materiality-analogs

142-fic-fin-06-vs-monitored-delta-and-wc-grounding-analogs

143-fic-fin-08-vs-monitored-holder-analogs

144-new-buyer-monolithic-vs-two-stage-analogs

145-real-working-capital-checkpoint-binding-lessons

146-real-configured-signal-field-use-lessons

147-combined-fictional-monitored-root-cause-summary

148-next-bounded-policy-decision.
```

---

# 75. Required completion artifacts

Produce:

```text
149-working-capital-checkpoint-binding-success-decision

150-financial-grounding-contract-preservation-decision

151-m12ba-replacement-verb-preservation-decision

152-m12az-fcf-local-scope-preservation-decision

153-configured-signal-field-ownership-preservation-decision

154-new-fictional-proof-success-decision

155-full-shadow-completion-decision

156-existing-monitored-impact-summary

157-two-stage-shadow-compatibility-decision

158-fresh-real-proof-readiness-decision

159-final-main-merge-readiness-note

160-production-no-change

161-schedule-pause-observation

162-remote-push-prohibition-audit

163-master-workflow-update

164-program-completion.
```

---

# 76. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch

latest_result_zip_sha256
latest_result_integrity

m12ba_failure_ticker
m12ba_failure_error

wc_checkpoint_binding_root_cause

wc_checkpoint_binding_architecture

wc_checkpoint_binding_view_contract_version

schema_conditional_binding_supported
schema_conditional_binding_used

checkpoint_fields

inventory_typed_alias_binding_enabled
receivables_typed_alias_binding_enabled
payables_typed_alias_binding_enabled

generic_working_capital_binding_enabled

historical_run1_fic_fin_06_status
historical_run2_fic_fin_06_status
historical_run2_new_schema_status
corrected_equivalent_run2_status

metric_specific_ref_mismatch_false_accept_count
narrative_only_substitution_false_accept_count

unsafe_wc_auto_direction_count
irrelevant_financial_ref_grounding_failure_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
working_capital_checkpoint_binding_view_change_count
final_user_schema_change_count

configured_signal_view_change_count
business_delta_view_change_count
expectation_view_change_count
configured_financial_support_concept_change_count
two_stage_semantic_change_count

investment_judgment_model_target
investment_judgment_reasoning_effort

fictional_generation_id

fictional_stage1_model_calls
fictional_stage2_model_calls
fictional_model_calls_total

fictional_stage1_row_count
fictional_stage2_row_count
fictional_final_composition_count

fictional_wc_checkpoint_count
fictional_grounded_wc_checkpoint_count
fictional_wc_grounding_failure_count
fictional_narrative_only_substitution_count
fictional_metric_specific_ref_mismatch_count
fictional_irrelevant_financial_ref_grounding_failure_count
fictional_unsafe_wc_auto_direction_count

fictional_configured_signal_current_driver_violation_count
fictional_configured_signal_false_fulfillment_count

fictional_fcf_local_scope_violation_count
fictional_current_unsupported_fcf_false_accept_count

fictional_business_delta_violation_count
fictional_expectation_anchor_violation_count
fictional_financial_sector_violation_count
fictional_stage2_language_false_positive_count

fictional_primary_direction_unstable_subject_count
fictional_business_delta_materiality_variance_subject_count
fictional_new_buyer_unstable_subject_count
fictional_holder_unstable_subject_count

fictional_core_mutation_count
fictional_timeout_count
fictional_orphan_count
fictional_wrapper_retry_count

task_start_active_monitor_count
task_start_active_monitor_tickers

shadow_generation_id

shadow_packet_available_count
shadow_packet_unavailable_count
shadow_packet_mismatch_count

shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status

shadow_wc_checkpoint_count
shadow_grounded_wc_checkpoint_count
shadow_wc_grounding_failure_count
shadow_narrative_only_substitution_count
shadow_metric_specific_ref_mismatch_count
shadow_irrelevant_financial_ref_grounding_failure_count
shadow_unsafe_wc_auto_direction_count

shadow_configured_signal_field_violation_count
shadow_configured_signal_false_fulfillment_count

shadow_fcf_local_scope_violation_count
shadow_current_unsupported_fcf_false_accept_count

shadow_business_delta_violation_count
shadow_expectation_anchor_violation_count
shadow_financial_sector_violation_count
shadow_stage2_language_false_positive_count

shadow_no_decision_material_change_count
shadow_same_direction_calibration_change_count
shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count
shadow_timeout_count
shadow_orphan_count
shadow_wrapper_retry_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count.
```

Anything unmeasured:

```text
NOT_MEASURED.
```

---

# 77. Fresh-real / main / production readiness

At M12BB completion:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY.
```

Even if fictional and shadow complete.

The next bounded task should be:

```text
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

No fresh unseen proof inside M12BB.

---

# 78. Failure handling

## A. Structured-output conditional schema is unsupported

Do NOT fake it.

Use:

```text
CLAIM_LOCAL_TYPED_REF_BINDING_WITH_MODEL_VIEW_AND_HARD_VALIDATOR
```

with the concise generic prompt rule.

Run a NEW full proof.

## B. New schema rejects valid run-1 checkpoint binding

```text
STOP
WC_CHECKPOINT_SCHEMA_TOO_RESTRICTIVE
```

## C. Metric-specific mismatch passes

Example:

```text
receivables claim
cites inventory typed ref only
```

If accepted:

```text
STOP
WC_METRIC_BINDING_TOO_PERMISSIVE
```

## D. Narrative-only refs start satisfying checkpoint grounding

```text
STOP
WC_GROUNDING_PROVENANCE_REGRESSION
```

## E. Binding adds automatic negative direction

```text
STOP
WC_DIRECTION_SEMANTIC_REGRESSION
```

## F. NEW fictional hard semantic failure

Do not run shadow.

Use the smallest bounded failing contract.

## G. Full shadow completes cleanly

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

---

# 79. Artifact integrity

Freeze all local artifacts before final artifact index.

Required:

```text
hash mismatch = 0

size mismatch = 0

secret scan failure = 0.
```

Report final ZIP SHA-256.

Do not push report/raw artifacts to GitHub.

---

# 80. Final task principle

M12BA's target financial-sector replacement-verb repair worked.

The new hard stop is not another semantic parser false positive.

Run-1 and run-2 FIC-FIN-06 say essentially the same cautious thing:

```text
inventory and receivables increased

but the balances are confirmation points,
not automatic negatives.
```

Run-1 directly cites:

```text
typed inventory
typed receivables
```

inside the checkpoint claims
and passes.

Run-2 cites only narrative/context refs
inside `risk_context` and `sell_drivers`,
while placing the typed refs only in `sector_interpretation`,
and fails.

That failure is desirable under the claim-local grounding contract.

The correct M12BB flow is:

```text
keep the post-model grounding validator strict

→ make selected WC typed aliases explicit in a
   WorkingCapitalCheckpointBindingView

→ when a checkpoint claim mentions inventory,
   receivables, payables, or generic working capital,
   require the relevant typed alias in THAT claim

→ prefer a safe conditional output-schema constraint
   if the actual Structured Output dialect supports it

→ otherwise use a compact model-facing binding view
   + one concise claim-local grounding rule
   + the existing hard validator

→ keep narrative refs as supplemental context,
   never substitute evidence

→ keep WC direction context-dependent

→ start a completely NEW full 12-call fictional proof

→ if all hard gates pass,
   run a completely NEW full active-monitored shadow

→ finally hand the complete clean cohort
   to the actual boundary / delta-materiality / holder policy review.
```

Do NOT:

```text
candidate-globalize financial grounding

accept typed refs merely because they appear somewhere else

post-hoc inject refs

turn inventory/receivable increases into automatic negatives

reopen financial-sector replacement verbs

reopen FCF temporal semantics

reopen configured-signal field ownership

reopen BusinessDeltaEvidenceView

reopen MarketExpectationEvidenceView

resume/stitch M12BA

rerun only FIC-FIN-06

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring.
```

A financial checkpoint is grounded by the evidence attached to that checkpoint,
not by evidence attached somewhere else in the candidate.
