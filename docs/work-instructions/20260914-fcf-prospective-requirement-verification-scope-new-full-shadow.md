# Thesis Monitor — FCF Prospective Requirement / Verification Scope + New Full Monitored Shadow

## 0. Task identity

Suggested work-instruction filename:

```text
20260914-fcf-prospective-requirement-verification-scope-new-full-shadow.md
```

Suggested result bundle:

```text
thesis-monitor-20260914-fcf-prospective-requirement-verification-scope-new-full-shadow-report.zip
```

Master-workflow phase:

```text
M12BG — Real-Shadow FCF Requirement Semantics
         A. Freeze successful M12BF frozen-context layout repair
         B. Freeze complete clean M12BD fictional proof
         C. Reproduce exact 005490 monolithic FCF false reject
         D. Distinguish prospective requirement / verification language from current FCF assertions
         E. Preserve hard current-FCF, numeric-FCF, PPE-proxy, and directional-anchor safety
         F. Offline re-audit the exact M12BF first monolithic output
         G. Offline re-audit the complete M12BD fictional proof under the repaired classifier
         H. Reuse M12BD proof only if all model-facing semantic hashes remain unchanged
         I. Create a NEW full shadow generation
         J. Run the complete monitored compatibility shadow
         K. If objectively clean, stop semantic engineering and hand off to boundary / delta / holder policy review
```

M12BF successfully completed the frozen-context dual-layout verifier repair.

Before the new shadow call:

```text
legacy flat frozen layout = supported

nested inputs frozen layout = supported

dual/partial layout ambiguity = hard fail

M12BE stopped-shadow nested layout offline replay = PASS

M12BD fictional legacy layout offline replay = PASS

active-universe count contract = PASS

formal fictional reuse =
REUSE_AUTHORIZED_M12BD_COMPLETE_PROOF
```

M12BF then created a NEW shadow generation and reached the actual model layer.

The first monolithic call completed successfully at transport/schema level:

```text
context = 01

tickers =
000660
003690
005490
005930

transport = PASS

candidate count = 4

semantic PASS = 3

semantic FAIL = 1

failed ticker =
005490 POSCO홀딩스
```

The whole generation correctly hard-stopped after one model call.

The only failure was:

```text
unsupported_current_fcf_claim
```

on two FCF clauses that do NOT assert current FCF state.

Exact clauses:

```text
business_thesis_context:
"철강 수익성 회복과 소재 흑자 지속이
 FCF와 ROIC 개선으로 이어져야 한다는
 기존 논리가 유지된다."

market_expectation_context:
"철강 개선과 소재 흑자 전환 기대가 균형적으로 반영되어
 실제 FCF와 ROIC 증명이 필요하다."
```

These mean:

```text
FCF improvement is a future/required validation outcome
```

not:

```text
FCF has already improved.
```

M12BG must repair this bounded currentness-classification gap
without weakening any true current FCF safety rule.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260913-frozen-context-input-layout-compatibility-new-full-shadow-report.zip
```

Verified SHA-256:

```text
239feaee5ffac6ad19a2ca9b95baf7c14896c870dcf9e2d01cfcb18ccca50ea0
```

Recompute at task start.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent archive verification:

```text
artifact_count = 856

ZIP entries =
856 indexed payloads
+ artifact-index.json
= 857

missing = 0
extra = 0

hash mismatch = 0
size mismatch = 0
secret scan failure = 0
```

Recompute independently.

---

# 2. M12BF program state

Reported:

```text
phase = M12BF

status = BLOCKED

two_stage_shadow_compatibility_classification =
BLOCKED_AFTER_FIRST_MONOLITHIC_HARD_SEMANTIC_FAILURE
```

New shadow generation:

```text
20260911-m12ai-shadow-20260913T141850Z-fdfcb99c8668
```

Planned:

```text
active tickers = 22

contexts = 6

monolithic calls = 6

Stage-1 calls = 6

Stage-2 calls = 6

planned total = 18.
```

Completed:

```text
monolithic calls = 1

Stage-1 calls = 0

Stage-2 calls = 0

remaining calls not run = 17.
```

No timeout, orphan, or retry.

---

# 3. M12BF frozen-layout repair — freeze successful

Preserve:

```text
frozen-context-input-layout-v1
```

with:

```text
LEGACY_FLAT

NESTED_INPUTS_V1

strict path/hash verification

dual-layout ambiguity hard failure

required input-set enforcement.
```

Reported:

```text
frozen_context_layout_repair_status = PASS

shadow_frozen_input_verification_failure_count = 0

shadow_frozen_input_hash_mismatch_count = 0

shadow_frozen_input_layout_ambiguity_count = 0.
```

Do not reopen this scope.

---

# 4. M12BE active-universe identity repair — freeze

Preserve:

```text
report-subject-identity-v1

market-expectation-evidence-view-v1
count field = subject_count

exact ticker-set equality

duplicate/missing/extra checks.
```

Reported:

```text
cross_manifest_count_mismatch_count = 0

cross_manifest_ticker_set_mismatch_count = 0

cross_manifest_duplicate_ticker_count = 0.
```

No change.

---

# 5. M12BD fictional proof — freeze successful

Generation:

```text
20260911-m12ai-fictional-20260913T113957Z-07ac29f97bc6
```

Preserve:

```text
12 / 12 model calls

24 Stage-1 rows

24 Stage-2 rows

24 final compositions

aggregate finalization = PASS

hard semantic failures = 0

core mutation = 0.
```

M12BF already authorized:

```text
formal_fictional_reuse_status =
REUSE_AUTHORIZED_M12BD_COMPLETE_PROOF

new_fictional_model_calls = 0.
```

M12BG should preserve this reuse if its repair is post-model semantic validation only
and all frozen proof rows replay cleanly.

---

# 6. Exact 005490 candidate — no candidate edit

The exact monolithic candidate must remain immutable.

Important fields:

```text
overall_direction =
HOLD

directional_balance =
5.0 / 5.0

business_thesis_change =
UNCHANGED

directional_confidence =
LOW

new buyer =
WAIT

holder =
HOLDABLE.
```

Other audits already passed:

```text
BusinessDeltaEvidenceView = PASS

financial grounding = PASS

MarketExpectationEvidenceView = PASS

ownership = PASS

QTD/YTD = PASS

working-capital binding = PASS

configured-signal current-driver violations = 0

configured-signal false fulfillment = 0.
```

Only FCF currentness failed.

---

# 7. Exact false-rejected claim 1

Field:

```text
business_thesis_context.text
```

Text:

```text
"철강 수익성 회복과 소재 흑자 지속이
 FCF와 ROIC 개선으로 이어져야 한다는 기존 논리가 유지된다."
```

Evidence:

```text
E14
```

E14 is the stored current thesis context:

```text
POSCO홀딩스의 재평가 조건은
철강 본업의 스프레드·수익성 회복과
리튬·이차전지 소재의 흑자 지속이 동시에 나타나고,
그 성장이 FCF와 ROIC 개선으로 연결되는 것이다...
```

This is a thesis criterion.

It does NOT assert:

```text
current FCF improved

current FCF is positive

current FCF is a specific number.
```

Expected:

```text
CURRENT_FCF_EVIDENCE_NOT_REQUIRED.
```

---

# 8. Exact false-rejected claim 2

Field:

```text
market_expectation_context.text
```

Text:

```text
"철강 개선과 소재 흑자 전환 기대가 균형적으로 반영되어
 실제 FCF와 ROIC 증명이 필요하다."
```

Evidence:

```text
E13
```

E13 market-expectation source itself says:

```text
summary:
"철강 회복과 리튬·소재 흑자 전환 기대가 공존하며
 실제 ROIC·FCF 증명이 필요한 구간이다."
```

This is a verification requirement.

It does NOT assert:

```text
current FCF changed.
```

Expected:

```text
CURRENT_FCF_EVIDENCE_NOT_REQUIRED.
```

---

# 9. Root-cause classification

Freeze the primary root cause as:

```text
FCF_CURRENTNESS_CLASSIFIER_TREATS_CURRENT_CORE_FIELD_ROLE
AS_SUFFICIENT_FOR_CURRENT_FCF_ASSERTION
AND_DOES_NOT_RECOGNIZE_PROSPECTIVE_REQUIREMENT_LANGUAGE
```

or exact code-audited equivalent.

Secondary manifestation:

```text
modal/requirement predicates:
이어져야 한다
증명이 필요하다

are not distinguished from:
개선됐다
증명됐다
확인됐다.
```

This is NOT:

```text
a missing current FCF fact

a configured FCF support failure

a market-expectation independence failure

a BusinessDeltaEvidenceView failure

a shadow packet failure

a frozen-state failure.
```

---

# 10. Core semantic principle

A field may describe CURRENT CONTEXT
without every metric mention being a CURRENT METRIC ASSERTION.

Separate:

```text
field semantic role
```

from:

```text
local financial claim temporal/polarity role.
```

Examples:

```text
business_thesis_context is current thesis context
but may describe a future validation requirement.

market_expectation_context is current expectation context
but may describe what the market still requires to be proven.
```

Do not infer current FCF state from field ownership alone.

---

# 11. Preferred internal semantic

Introduce or extend a bounded role such as:

```text
PROSPECTIVE_VERIFICATION_REQUIREMENT
```

or exact repository-compatible equivalent.

If adding a new generic `FinancialClaimRole` enum value would create
unnecessary surface area,
a dedicated FCF temporal role is acceptable.

The semantic requirement is:

```text
this local FCF clause expresses
a required/expected future outcome or proof condition

AND

does not assert that FCF has currently achieved that state.
```

Do not force the role into `PROSPECTIVE_RISK_SCENARIO`
if that would be semantically misleading.

---

# 12. Requirement/verification predicate family

Support bounded Korean requirement forms such as:

```text
FCF 개선으로 이어져야 한다

FCF 개선으로 연결되어야 한다

FCF 개선이 나타나야 한다

FCF 개선이 확인되어야 한다

FCF 개선이 입증되어야 한다

FCF 개선이 증명되어야 한다

FCF 개선의 확인이 필요하다

FCF 개선의 입증이 필요하다

FCF 개선의 증명이 필요하다

FCF 개선의 검증이 필요하다

실제 FCF 증명이 필요하다

FCF 개선 여부를 확인해야 한다

FCF가 투자 확대를 상쇄해야 한다.
```

Only when the local claim does NOT also make an affirmative current state assertion.

---

# 13. English parity

Support bounded equivalents:

```text
growth must translate into better free cash flow

FCF improvement needs to be demonstrated

proof of FCF improvement is still needed

FCF improvement needs to be confirmed

FCF must offset the investment increase

the thesis requires stronger free cash flow

the market still requires evidence of FCF improvement.
```

Again:

```text
requirement != current fulfillment.
```

---

# 14. Current affirmative predicates remain hard

These MUST continue to require safe current FCF evidence:

```text
"현재 FCF가 개선됐다."

"FCF가 이미 증가했다."

"FCF 개선이 확인됐다."

"FCF 개선이 입증됐다."

"FCF 개선이 증명됐다."

"현재 FCF는 양수다."

"FCF는 100이다."

"최근 FCF가 악화됐다."
```

No prospective-requirement exemption.

---

# 15. Modal vs fulfilled morphology

Critical distinctions:

```text
이어져야 한다
vs
이어졌다

확인되어야 한다
vs
확인됐다

입증되어야 한다
vs
입증됐다

증명되어야 한다
vs
증명됐다

필요하다
vs
확인됐다 / 나타났다.
```

Requirement/modality does not prove present state.

Completed/affirmative predicates do.

---

# 16. Current numeric override

These remain current even if requirement language also appears:

```text
"현재 FCF는 100이며 추가 개선이 필요하다."

"현재 FCF는 음수이고 개선으로 이어져야 한다."

"FCF가 -100으로 확인됐고 향후 개선이 필요하다."
```

Required:

```text
current numeric/state claim detected

current safe FCF evidence required.
```

No whole-clause prospective exemption.

---

# 17. Current magnitude/state override

These remain current:

```text
"현재 FCF가 약해 개선이 필요하다."

"현재 FCF가 악화돼 증명이 필요하다."

"현재 현금흐름이 부족해 FCF 개선이 필요하다."
```

If the first clause asserts current FCF state,
that current claim still requires evidence.

If clause splitting can isolate the later requirement,
classify both claims separately.

Fail closed if deterministic separation is unsafe.

---

# 18. Negated current state remains non-affirmative

Preserve M12AZ polarity semantics:

```text
"현재 FCF 개선은 확인되지 않았다."

"FCF 개선은 아직 입증되지 않았다."

"현재 FCF가 개선됐다는 증거는 없다."
```

These do NOT assert successful current FCF improvement.

Do not turn negative evidence availability into current fulfillment.

Any separate affirmative current FCF claim remains hard.

---

# 19. Field scope — do not whitelist only the two exact paths

Do NOT hard-code:

```text
business_thesis_context
market_expectation_context
```

as magical exceptions.

Instead classify the LOCAL FCF claim semantically.

However, field ownership still matters for directional eligibility.

A prospective requirement can be valid contextual prose,
but cannot become current directional evidence.

---

# 20. Directional driver safety

If:

```text
buy_drivers
sell_drivers
dominant_evidence
material_directional_anchor_basis
```

uses only prospective requirement language such as:

```text
"FCF 개선이 필요하다"
```

that requirement must NOT become a current directional anchor.

M12BG must preserve:

```text
current driver / anchor evidence eligibility

configured-signal current-driver fencing

material-anchor eligibility

dominant-evidence eligibility.
```

The currentness repair only says:

```text
do not demand current FCF evidence
for a non-current FCF requirement claim.
```

It does NOT say:

```text
prospective requirement is positive/negative current evidence.
```

---

# 21. Core-investment judgment safety

If `core_investment_judgment` says only:

```text
"FCF 개선 증명이 필요하다"
```

it may express an uncertainty/confirmation rationale,
but must not be treated as a current FCF fact.

If the direction relies on that phrase as a material current anchor,
existing ownership/material-anchor rules must still govern.

Do not create new directional eligibility.

---

# 22. MarketExpectationEvidenceView — freeze

The exact failure uses:

```text
market_expectation_context
```

and E13.

Preserve M12AP:

```text
expectation role =
INDEPENDENCE_UNKNOWN

material_anchor_eligible = false

dominant_evidence_eligible = false.
```

The FCF currentness fix must not promote E13 into current directional evidence.

No change to expectation semantics.

---

# 23. BusinessDeltaEvidenceView — freeze

The exact 005490 packet has:

```text
capability = UNCHANGED_ONLY
```

and the candidate correctly returns:

```text
business_thesis_change = UNCHANGED.
```

Preserve.

Thesis requirement language from E14 is baseline context,
not observed business delta evidence.

No change.

---

# 24. Configured FCF support — freeze

Preserve M12AY:

```text
metric_refs ["FCF"]
→ free_cash_flow configured-support concept

OCF ≠ FCF

OCF-PPE ≠ FCF

configured support ≠ current fulfillment.
```

The exact false-rejected claims do NOT require
configured-signal support to be recognized as requirements.

Why:

```text
one is stored-thesis context
one is market-expectation context.
```

Do not incorrectly require a configured weaken/strengthen signal
for every prospective verification statement.

---

# 25. Requirement currentness vs overall grounding

The FCF currentness validator answers only:

```text
does this claim require current FCF evidence?
```

It does NOT answer:

```text
is the prose properly grounded overall?
```

Other validators continue to own:

```text
evidence identity

field ownership

market-expectation independence

directional anchors

business delta

configured-signal lifecycle.
```

Do not overload the FCF currentness classifier.

---

# 26. Exact 005490 offline replay

After repair,
revalidate the exact immutable M12BF 005490 candidate.

Required:

```text
business_thesis_context FCF clause:
prospective requirement

current FCF evidence required = false

market_expectation_context FCF clause:
prospective verification requirement

current FCF evidence required = false

unsupported_current_fcf_claim_count = 0

all other existing audits remain PASS

candidate status = PASS.
```

Do not edit model output.

---

# 27. Exact context-01 replay

Revalidate all four exact monolithic candidates:

```text
000660 PASS remains PASS

003690 PASS remains PASS

005490 becomes PASS

005930 PASS remains PASS.
```

Required:

```text
new false accept count = 0

new false reject count = 0.
```

This is diagnostic only.

Do not resume the stopped generation.

---

# 28. Positive requirement fixtures

At minimum:

## FCF-REQ-P01

```text
"성장이 FCF와 ROIC 개선으로 이어져야 한다는 기존 논리가 유지된다."
→ current FCF evidence NOT required.
```

## FCF-REQ-P02

```text
"실제 FCF와 ROIC 증명이 필요하다."
→ current FCF evidence NOT required.
```

## FCF-REQ-P03

```text
"FCF 개선이 확인되어야 투자 논리를 강화한다."
→ prospective verification requirement.
```

## FCF-REQ-P04

```text
"FCF 개선의 입증이 필요하다."
→ prospective requirement.
```

## FCF-REQ-P05

```text
"FCF가 투자 확대를 상쇄해야 한다."
→ prospective thesis requirement.
```

## FCF-REQ-P06

```text
English:
"growth must translate into improved free cash flow."
→ prospective.
```

## FCF-REQ-P07

```text
English:
"proof of FCF improvement is still needed."
→ prospective.
```

---

# 29. Current-claim negative fixtures

At minimum:

## FCF-REQ-N01

```text
"현재 FCF가 개선됐다."
→ current
→ FAIL without safe current FCF evidence.
```

## FCF-REQ-N02

```text
"FCF 개선이 이미 확인됐다."
→ current
→ FAIL without safe current FCF evidence.
```

## FCF-REQ-N03

```text
"실제 FCF 개선이 증명됐다."
→ current
→ FAIL.
```

## FCF-REQ-N04

```text
"현재 FCF는 100이다."
→ current numeric
→ FAIL.
```

## FCF-REQ-N05

```text
"현재 FCF가 악화돼 향후 개선이 필요하다."
→ current state claim detected
→ FAIL without safe evidence.
```

## FCF-REQ-N06

```text
"FCF 개선은 필요하지만 현재 FCF는 이미 감소했다."
→ separate current claim
→ FAIL.
```

## FCF-REQ-N07

```text
English:
"FCF improvement is needed, but FCF has already declined."
→ current affirmative claim remains hard.
```

---

# 30. Directional-field negative fixtures

## FCF-REQ-D01

```text
buy_driver:
"FCF 개선이 필요하다"

only baseline/configured future refs

Expected:
does NOT become valid current buy evidence.
```

## FCF-REQ-D02

```text
material anchor:
future FCF requirement only

Expected:
does NOT become current material anchor.
```

## FCF-REQ-D03

```text
dominant evidence:
market-expectation FCF proof requirement

Expected:
expectation independence/material-anchor restrictions remain active.
```

These may fail under ownership/anchor validators
even though the FCF currentness validator itself does not demand current FCF evidence.

That is correct.

---

# 31. FCF / ROIC co-mention handling

The exact claims mention:

```text
FCF + ROIC.
```

M12BG is scoped to FCF currentness only.

Do not invent or broaden ROIC current-evidence semantics
unless an existing ROIC validator independently requires it.

The clause may be:

```text
prospective for FCF
prospective for ROIC
```

but only the FCF false reject is in scope.

No cross-metric scope creep.

---

# 32. Current safe FCF evidence contract — freeze

Preserve:

```text
free_cash_flow
reported_free_cash_flow

or another separately audited management/reporting-defined FCF metric
```

as the only safe current FCF evidence families.

Do NOT synthesize from:

```text
OCF

OCF-PPE

cash generation

generic cash flow.
```

No change.

---

# 33. PPE proxy safety — freeze

Preserve:

```text
OCF-PPE cash-conversion proxy
!=
management-defined FCF.
```

True proxy-as-FCF assertions remain hard failures.

No change.

---

# 34. Existing future FCF roles — freeze

Preserve all previously valid future roles:

```text
FUTURE_REEVALUATION_CONDITION

STANCE_CONFIRMATION_CONDITION

STANCE_INVALIDATION_CONDITION

PROSPECTIVE_RISK_SCENARIO

monitoring-obligation semantics

nominalized-condition semantics.
```

M12BG only adds/extends:

```text
prospective requirement / verification semantics
for FCF mentions in contextual clauses.
```

No regression.

---

# 35. Model-facing semantic impact

Preferred M12BG repair is:

```text
post-model financial semantic classifier only.
```

Expected:

```text
model_prompt_semantic_change_count = 0

model_schema_semantic_change_count = 0

Stage-1 WC binding view change = 0

Stage-2 WC binding view change = 0

configured-signal view change = 0

configured financial support concept change = 0

BusinessDeltaEvidenceView change = 0

MarketExpectationEvidenceView change = 0

financial evidence projection change = 0

two-stage core semantic change = 0

final user schema change = 0.
```

If any model-facing semantic hash changes:

```text
formal fictional reuse prohibited.
```

---

# 36. Complete M12BD fictional offline re-audit

Before shadow,
replay the complete frozen M12BD proof
under the repaired FCF classifier.

Required:

```text
Stage-1 rows = 24 / 24 PASS

Stage-2 rows = 24 / 24 PASS

final compositions = 24 / 24 hard PASS

aggregate finalization = PASS

hard semantic failure = 0

core mutation = 0.
```

Also report:

```text
newly classified prospective requirement claims

new false accepts = 0

new false rejects = 0.
```

No candidate edits.

---

# 37. Fictional proof reuse

If:

```text
M12BD offline re-audit = PASS

and

all model-facing semantic hashes unchanged
```

authorize:

```text
formal_fictional_reuse_status =
REUSE_AUTHORIZED_M12BD_COMPLETE_PROOF

new_fictional_model_calls = 0.
```

This is preferred.

If any model-facing semantic surface changed:

```text
NEW_FORMAL_PROOF_REQUIRED.
```

Run a fresh full 12-call fictional proof first.

No partial reuse.

---

# 38. New shadow generation mandatory

Do NOT resume:

```text
20260911-m12ai-shadow-20260913T141850Z-fdfcb99c8668.
```

One model call already occurred under the old semantic validator.

After the classifier changes:

```text
create a NEW shadow generation from scratch.
```

The exact old output is only an offline positive control.

---

# 39. Re-enumerate active monitored universe

At new shadow start,
read active monitored names again.

Last reference:

```text
22 active names:
000660
003690
005490
005930
010120
012450
047810
086280
CORZ
CPNG
CRCL
GOOGL
HUT
IBM
MU
RXRX
SKHY
SNDK
TSLA
TSM
WRD
WULF.
```

Do not hard-code 22 as authoritative.

No registrations.

No stops.

---

# 40. Same-packet contract

For every active ticker:

```text
one frozen local packet

same packet hash feeds:
monolithic
Stage 1
Stage 2.
```

Provider fetch:

```text
0.
```

No production data refresh.

---

# 41. Shadow topology

For N active names:

```text
context_count = ceil(N / 4)

monolithic calls = context_count

Stage-1 calls = context_count

Stage-2 calls = context_count

total = 3 × context_count.
```

If N remains 22:

```text
contexts = 6

calls = 18.
```

No repetitions.

---

# 42. Shadow model

Use:

```text
gpt-5.6-sol / xhigh.
```

No Astra.

No fallback.

No judge calls.

No selective rerun.

---

# 43. Frozen-state / count contracts remain hard

Preserve M12BE/M12BF:

```text
exact active ticker-set identity

subject_count contract

nested/legacy frozen input resolution

path existence

SHA-256 identity

no ambiguous dual layout

current code-hash freeze.
```

No weakening.

---

# 44. Shadow hard-stop policy

Hard stop only for objective failures:

```text
runtime/schema failure

frozen input mismatch

active universe mismatch

packet mismatch

invalid evidence identity

true unsupported current FCF claim

PPE proxy-as-FCF violation

FCF requirement false classification

Stage-1 WC grounding failure

Stage-2 WC grounding failure

WC metric mismatch

WC narrative substitution

irrelevant typed financial grounding

unsafe WC auto-direction

configured-signal field-use violation

configured-signal false fulfillment

financial-sector hard semantic violation

business-delta hard semantic violation

market-expectation hard semantic violation

Stage-2 contamination

ADR/security-basis failure

core mutation

aggregate finalization failure

production-side-effect attempt.
```

Do NOT hard-stop on valid decision differences.

---

# 45. Optional lexical audit coverage remains nonblocking

Preserve M12BD.

Zero observed:

```text
monitoring obligation

nominal condition

negated fulfillment

requirement language
```

is a coverage fact,
not a semantic failure,
unless a deterministic fixture explicitly requires coverage.

Report:

```text
observed count

semantic violation count

coverage status

readiness blocking.
```

---

# 46. Shadow FCF requirement audit

For every FCF occurrence in the shadow,
report:

```text
ticker

field path

full field text

local FCF clause

field semantic role

current magnitude detected?

current numeric detected?

affirmative current fulfillment detected?

negated current fulfillment detected?

prospective requirement detected?

requirement predicate family

current FCF evidence required?

safe current FCF evidence refs

configured FCF support refs if relevant

validation result.
```

This is mandatory.

---

# 47. Shadow direction-eligibility audit

For every prospective FCF requirement appearing in:

```text
buy_drivers

sell_drivers

dominant_evidence

material anchors

core judgment
```

report whether it was incorrectly treated as current directional evidence.

Required:

```text
prospective_requirement_as_current_anchor_false_accept_count = 0.
```

Do not let the currentness repair weaken direction ownership.

---

# 48. Per-ticker compatibility taxonomy

Use:

```text
NO_DECISION_MATERIAL_CHANGE

SAME_DIRECTION_CALIBRATION_CHANGE

PRIMARY_DIRECTION_CHANGE

BUSINESS_DELTA_CHANGE

NEW_BUYER_STANCE_CHANGE

HOLDER_STANCE_CHANGE

MULTI_FIELD_DECISION_CHANGE

EXPECTED_CONTRACT_CORRECTION

POTENTIAL_ARCHITECTURE_REGRESSION

OTHER_REVIEW_REQUIRED.
```

Neither monolithic nor two-stage is automatically ground truth.

---

# 49. Full shadow acceptance

Require:

```text
all planned calls complete

all active tickers represented exactly once

frozen-state verification PASS

packet identity PASS

monolithic hard semantics PASS

Stage-1 hard semantics PASS

Stage-2 hard semantics PASS

final compositions PASS

aggregate finalization PASS

current FCF false accept = 0

prospective FCF requirement false reject = 0

prospective requirement as current anchor false accept = 0

PPE proxy-as-FCF = 0

Stage-1 WC grounding failure = 0

Stage-2 WC grounding failure = 0

WC metric mismatch = 0

WC narrative substitution = 0

unsafe WC auto-direction = 0

configured-signal violations = 0

financial-sector hard failures = 0

business-delta hard failures = 0

expectation hard failures = 0

Stage-2 contamination = 0

ADR/security-basis failure = 0

core mutation = 0

timeout = 0

orphan = 0

wrapper retry = 0

production side effects = 0.
```

Decision comparison counts may be nonzero.

---

# 50. Per-ticker compatibility output

For every active ticker report:

```text
ticker

monolithic primary direction

two-stage primary direction

monolithic business delta

two-stage business delta

monolithic new-buyer stance

two-stage new-buyer stance

monolithic holder stance

two-stage holder stance

same-direction calibration difference

comparison taxonomy

objective semantic violations if any

review note.
```

One row per active ticker.

---

# 51. Combined policy-review handoff

If the full shadow is objectively clean,
combine:

```text
M12BD complete fictional proof

+

M12BG complete monitored shadow.
```

Preserve fictional diagnostics including:

```text
FIC-FIN-05 primary boundary

FIC-FIN-08 holder boundary

same-direction calibration variance

business-delta materiality diagnostics.
```

Add monitored compatibility differences.

Do not resolve them inside M12BG.

---

# 52. Next scope if clean

Required:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

Also:

```text
fresh_real_proof_readiness = NOT_READY

final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY.
```

Do NOT start fresh unseen proof yet.

---

# 53. Required forensic artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12bg-scope-freeze

04-m12bd-fictional-proof-identity-freeze

05-m12bf-shadow-first-call-failure-reproduction

06-posco-business-thesis-fcf-requirement-forensic

07-posco-market-expectation-fcf-requirement-forensic

08-fcf-currentness-classifier-code-audit

09-current-field-role-vs-local-claim-role-audit

10-fcf-prospective-requirement-options

11-fcf-prospective-requirement-decision.
```

---

# 54. Required semantic-contract artifacts

Produce:

```text
12-fcf-prospective-requirement-contract

13-fcf-verification-requirement-predicate-contract

14-fcf-current-affirmative-predicate-contract

15-fcf-current-numeric-precedence-contract

16-fcf-current-state-precedence-contract

17-fcf-negated-current-state-regression-contract

18-fcf-requirement-directional-eligibility-separation-contract

19-market-expectation-independence-preservation-contract

20-business-delta-baseline-preservation-contract.
```

---

# 55. Required exact replay / fixtures

Produce:

```text
21-m12bf-005490-exact-offline-replay

22-m12bf-context01-four-candidate-offline-replay

23-fcf-requirement-positive-fixtures

24-fcf-current-claim-negative-fixtures

25-fcf-current-numeric-negative-fixtures

26-fcf-requirement-plus-current-claim-negative-fixtures

27-fcf-directional-field-negative-fixtures

28-fcf-english-requirement-parity-fixtures.
```

---

# 56. Required frozen regression artifacts

Produce:

```text
29-m12bf-frozen-layout-freeze

30-m12be-active-universe-identity-freeze

31-m12bd-stage2-wc-binding-freeze

32-m12bd-optional-audit-coverage-freeze

33-m12bc-context-finalizer-freeze

34-m12bc-decision-variance-readiness-freeze

35-m12bb-stage1-wc-binding-freeze

36-m12ba-financial-sector-replacement-verb-freeze

37-m12az-fcf-local-temporal-scope-freeze

38-m12ay-configured-fcf-support-freeze

39-m12ax-monitoring-semantics-freeze

40-m12aw-nominal-condition-freeze

41-m12av-mixed-risk-scope-freeze

42-m12at-configured-signal-field-ownership-freeze

43-m12ap-expectation-independence-freeze

44-m12ao-business-delta-convergence-freeze

45-adr-security-basis-freeze

46-two-stage-core-immutability-freeze.
```

---

# 57. Required model-facing identity artifacts

Produce:

```text
47-model-prompt-semantic-hash-freeze

48-model-schema-semantic-hash-freeze

49-stage1-wc-binding-view-hash-freeze

50-stage2-wc-binding-view-hash-freeze

51-configured-signal-view-hash-freeze

52-configured-financial-support-concept-hash-freeze

53-business-delta-view-hash-freeze

54-expectation-view-hash-freeze

55-financial-evidence-projection-hash-freeze

56-two-stage-core-semantics-freeze

57-final-user-schema-freeze

58-fictional-proof-reuse-decision.
```

Artifact 58 exactly one:

```text
REUSE_AUTHORIZED_M12BD_COMPLETE_PROOF
```

or:

```text
NEW_FORMAL_PROOF_REQUIRED.
```

---

# 58. Required M12BD offline re-audit

Produce:

```text
59-m12bd-complete-fictional-offline-reaudit.
```

Required if proof reuse is authorized:

```text
Stage 1 = 24 / 24 PASS

Stage 2 = 24 / 24 PASS

final = 24 / 24 PASS

aggregate = PASS

hard semantic failures = 0

new false accept = 0

new false reject = 0.
```

---

# 59. Deterministic test gate

Produce:

```text
60-fcf-requirement-unit-tests

61-current-fcf-safety-regression-tests

62-directional-eligibility-regression-tests

63-m12bf-exact-output-replay-tests

64-m12bd-fictional-offline-reaudit-tests

65-focused-test-results

66-full-local-test-results

67-ruff-and-diff-results

68-hosted-ci-portability-observation

69-new-shadow-model-call-gate.
```

No shadow model calls before artifact 69 PASS.

---

# 60. New shadow setup artifacts

Produce:

```text
70-task-start-active-monitored-universe

71-new-shadow-generation-manifest

72-shadow-packet-inventory

73-shadow-packet-hash-manifest

74-shadow-stage1-wc-binding-view-manifest

75-shadow-stage2-wc-binding-view-manifest

76-shadow-configured-signal-view-manifest

77-shadow-configured-financial-support-concept-manifest

78-shadow-delta-view-manifest

79-shadow-expectation-view-manifest

80-shadow-frozen-context-manifest

81-shadow-frozen-input-layout-audit

82-shadow-cross-manifest-preflight-matrix

83-shadow-batching-manifest

84-shadow-model-call-gate.
```

---

# 61. Required full shadow artifacts

Produce:

```text
85-shadow-monolithic-model-artifacts

86-shadow-stage1-model-artifacts

87-shadow-stage2-model-artifacts

88-shadow-context-hard-semantic-audit

89-shadow-fcf-prospective-requirement-audit

90-shadow-directional-requirement-eligibility-audit

91-shadow-stage1-wc-binding-audit

92-shadow-stage2-wc-binding-audit

93-shadow-financial-grounding-audit

94-shadow-optional-audit-coverage-matrix

95-shadow-configured-signal-field-use-audit

96-shadow-business-delta-audit

97-shadow-market-expectation-audit

98-shadow-financial-sector-audit

99-shadow-stage2-language-audit

100-shadow-final-composition-audit

101-shadow-aggregate-finalization-audit

102-shadow-per-ticker-comparison

103-shadow-core-direction-differences

104-shadow-business-delta-differences

105-shadow-new-buyer-differences

106-shadow-holder-differences

107-shadow-same-direction-calibration-differences

108-shadow-expected-contract-corrections

109-shadow-potential-architecture-regressions

110-shadow-unresolved-review-required

111-shadow-adr-security-basis-audit

112-shadow-cyclical-valuation-audit

113-shadow-core-immutability-audit

114-shadow-runtime-audit

115-shadow-aggregate-summary

116-shadow-architecture-decision.
```

---

# 62. Required combined handoff artifacts

If full shadow completes:

```text
117-fictional-primary-boundary-summary

118-fictional-delta-materiality-summary

119-fictional-new-buyer-boundary-summary

120-fictional-holder-boundary-summary

121-monitored-primary-difference-summary

122-monitored-delta-difference-summary

123-monitored-new-buyer-difference-summary

124-monitored-holder-difference-summary

125-same-direction-calibration-summary

126-real-fcf-requirement-scope-lessons

127-combined-fictional-monitored-policy-input

128-next-bounded-policy-decision.
```

---

# 63. Required completion artifacts

Produce:

```text
129-fcf-prospective-requirement-repair-success-decision

130-m12bf-frozen-layout-preservation-decision

131-m12bd-fictional-proof-reuse-success-decision

132-new-full-shadow-completion-decision

133-two-stage-shadow-compatibility-decision

134-existing-monitored-impact-summary

135-fresh-real-proof-readiness-decision

136-final-main-merge-readiness-note

137-production-no-change

138-schedule-pause-observation

139-remote-push-prohibition-audit

140-master-workflow-update

141-program-completion.
```

---

# 64. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch
final_local_head_sha

latest_result_zip_sha256
latest_result_integrity

m12bf_shadow_generation_id
m12bf_shadow_model_calls_completed
m12bf_shadow_failure_ticker
m12bf_shadow_failure_error

fcf_requirement_root_cause

fcf_prospective_requirement_contract_version

m12bf_005490_offline_replay_status
m12bf_context01_offline_replay_status

fcf_prospective_requirement_false_reject_count
fcf_current_claim_false_accept_count
fcf_current_numeric_false_accept_count
fcf_requirement_directional_anchor_false_accept_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
stage1_wc_binding_semantic_change_count
stage2_wc_binding_semantic_change_count
configured_signal_view_change_count
configured_financial_support_concept_change_count
business_delta_view_change_count
expectation_view_change_count
financial_evidence_projection_change_count
two_stage_core_semantic_change_count
final_user_schema_change_count

formal_fictional_reuse_status
new_fictional_model_calls
m12bd_offline_reaudit_status

task_start_active_monitor_count
task_start_active_monitor_tickers

new_shadow_generation_id
shadow_context_count
shadow_monolithic_model_calls
shadow_stage1_model_calls
shadow_stage2_model_calls
shadow_model_calls_total

shadow_completed_ticker_count
shadow_final_composition_count
shadow_aggregate_finalization_status
shadow_hard_semantic_failure_count

shadow_fcf_prospective_requirement_false_reject_count
shadow_current_fcf_false_accept_count
shadow_prospective_requirement_as_current_anchor_false_accept_count
shadow_proxy_as_fcf_violation_count

shadow_stage1_wc_grounding_failure_count
shadow_stage2_wc_grounding_failure_count
shadow_wc_metric_specific_mismatch_count
shadow_wc_narrative_only_substitution_count
shadow_unsafe_wc_auto_direction_count

shadow_configured_signal_violation_count
shadow_business_delta_hard_failure_count
shadow_expectation_hard_failure_count
shadow_financial_sector_hard_failure_count
shadow_stage2_language_false_positive_count

shadow_primary_direction_change_count
shadow_business_delta_change_count
shadow_new_buyer_change_count
shadow_holder_change_count
shadow_same_direction_calibration_change_count
shadow_multi_field_change_count

shadow_expected_contract_correction_count
shadow_potential_architecture_regression_count
shadow_unresolved_review_required_count

shadow_core_mutation_after_stance_count
shadow_timeout_count
shadow_orphan_count
shadow_wrapper_retry_count

provider_source_fetches

production_db_mutations
monitoring_registrations
monitoring_stops
assessment_persistence_mutations
warning_mutations
notification_queue_writes
production_sends

remote_push_count
raw_model_artifact_remote_push_count

main_branch_mutations
main_merges
deployments

observed_paused_schedule_count
scheduler_mutation_count
automatic_monitoring_resume

two_stage_shadow_compatibility_classification

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count.
```

Anything unmeasured:

```text
NOT_MEASURED.
```

---

# 65. Expected clean completion

Expected if the full shadow is objectively clean:

```text
formal_fictional_reuse_status =
REUSE_AUTHORIZED_M12BD_COMPLETE_PROOF

new_fictional_model_calls =
0

shadow_completed_ticker_count =
task_start_active_monitor_count

shadow_aggregate_finalization_status =
PASS

shadow_hard_semantic_failure_count =
0

shadow_fcf_prospective_requirement_false_reject_count =
0

shadow_current_fcf_false_accept_count =
0

shadow_prospective_requirement_as_current_anchor_false_accept_count =
0

two_stage_shadow_compatibility_classification =
COMPLETE_WITH_POLICY_DIAGNOSTICS

fresh_real_proof_readiness =
NOT_READY

final_main_merge_readiness =
NOT_READY

production_readiness =
NOT_READY

next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

---

# 66. Failure handling

## A. Exact 005490 requirement clauses still false-reject

```text
STOP
FCF_REQUIREMENT_CLASSIFIER_REPAIR_FAILED.
```

No new shadow.

## B. Current FCF assertions begin passing

```text
STOP
FCF_CURRENTNESS_REPAIR_TOO_PERMISSIVE.
```

## C. Prospective requirement language becomes current directional evidence

```text
STOP
FCF_REQUIREMENT_DIRECTIONAL_ELIGIBILITY_REGRESSION.
```

## D. Market expectation FCF proof requirement becomes material anchor

```text
STOP
MARKET_EXPECTATION_INDEPENDENCE_REGRESSION.
```

## E. M12BD frozen fictional replay gains a hard semantic failure

```text
formal_fictional_reuse_status =
NEW_FORMAL_PROOF_REQUIRED.
```

Run a fresh full 12-call proof.

## F. New full shadow finds another objective hard semantic failure

Do not proceed to policy review.

Use the smallest bounded repair.

## G. Full shadow completes objectively clean

Expected:

```text
next_scope =
DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

---

# 67. Local-only / production firewall

M12BG is LOCAL-ONLY.

Required:

```text
model_calls_real_fresh_unseen = 0

provider_source_fetches = 0

production_db_mutations = 0

monitoring_registrations = 0

monitoring_stops = 0

assessment_persistence_mutations = 0

warning_mutations = 0

notification_queue_writes = 0

production_sends = 0

remote_push_count = 0

raw_model_artifact_remote_push_count = 0

main_branch_mutations = 0

main_merges = 0

deployments = 0

automatic_monitoring_resume = 0.
```

Monitoring schedules remain paused.

Do not ask for GitHub push authorization.

---

# 68. Artifact integrity

Freeze all local artifacts before final artifact index.

Required:

```text
hash mismatch = 0

size mismatch = 0

secret scan failure = 0.
```

Report final ZIP SHA-256.

Do not push raw/report artifacts to GitHub.

---

# 69. Final task principle

M12BF achieved the frozen-state compatibility repair successfully
and finally reached the monitored model layer.

The first real monitored shadow candidate exposed a bounded semantic gap:

```text
"FCF improved"
and
"FCF improvement must be proven"
are not the same statement.
```

The exact POSCO candidate says:

```text
the existing thesis requires growth to translate into FCF/ROIC improvement

and

the market still requires proof of actual FCF/ROIC improvement.
```

Neither sentence claims that current FCF has already improved.

The correct M12BG flow is:

```text
keep current FCF safety hard

→ classify modal/verification requirement language separately
   from affirmative current FCF state

→ preserve local claim scope and current numeric/state precedence

→ preserve directional-driver/material-anchor eligibility rules

→ preserve MarketExpectationEvidenceView restrictions

→ offline replay the exact 005490 candidate

→ offline replay the complete clean M12BD fictional proof

→ if model-facing hashes are unchanged,
   reuse M12BD without new fictional calls

→ create a NEW full shadow generation

→ run the complete monitored compatibility cohort

→ if objectively clean,
   stop semantic engineering

→ hand the complete fictional + monitored evidence set to:

DECISION_BOUNDARY_DELTA_HOLDER_POLICY_REVIEW_ON_INTEGRATED_MAIN.
```

Do NOT:

```text
treat every mention of FCF in a current-core field as current state

treat every "필요하다" as safe without local scope

weaken current numeric/state FCF checks

turn future requirements into current directional anchors

promote market expectations into material evidence

reopen frozen-context layout

reopen active-universe count semantics

reopen Stage-2 WC binding

reopen optional audit coverage

reopen context-preserving finalization

reopen decision-variance readiness policy

reopen configured-signal ownership

reopen BusinessDeltaEvidenceView

push raw artifacts to GitHub

merge into main

deploy

start fresh unseen proof

return to Astra

resume production monitoring.
```

A requirement that FCF must improve is a validation criterion,
not evidence that FCF has already improved.
