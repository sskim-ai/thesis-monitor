# Thesis Monitor — New Fresh Unseen Real Proof on Canonical Integrated Pipeline

## 0. Task identity

Suggested work-instruction filename:

```text
20260914-new-fresh-unseen-real-proof-canonical-integrated-pipeline.md
```

Suggested result bundle:

```text
thesis-monitor-20260914-new-fresh-unseen-real-proof-canonical-integrated-pipeline-report.zip
```

Master-workflow phase:

```text
M12BO — Final Fresh-Unseen Real Analysis Proof
         A. Freeze M12BI semantic single-source convergence
         B. Freeze M12BJ complete monitored compatibility proof
         C. Freeze M12BK-R2 real-cohort policy validation
         D. Freeze M12BN local Persistence V2 proof
         E. Build a project-wide prior-subject exclusion registry
         F. Select a deterministic, sector-diverse, truly fresh KR/US cohort
         G. Freeze subject selection BEFORE model calls
         H. Fetch current real provider evidence using supported FREE providers only
         I. Freeze one immutable evidence packet per fresh subject
         J. Run the active canonical fresh/new-issuer model pipeline
         K. Run canonical semantic audit + final composition with NO wrapper bypass
         L. Complete the entire frozen cohort without replacing failed candidates
         M. Verify user-facing Initial Analysis lifecycle semantics
         N. Verify Persistence V2 applicability without fabricating monitoring identity
         O. If all required fresh proof gates pass, mark main-merge readiness
            READY_FOR_EXPLICIT_USER_APPROVAL only
         P. Do NOT merge, deploy, enable V2, register stocks, or resume schedules
```

This is the first NEW real fresh/unseen model proof after:

```text
semantic convergence

complete monitored compatibility shadow

real-cohort policy validation

Persistence V2 local executable proof.
```

It must be a genuine holdout.

Do NOT reuse:

```text
active monitored subjects

historical fresh/new-issuer subjects

previous fresh model artifacts

previous candidate outputs

previously rejected fresh subjects.
```

Do NOT replace a frozen subject because the model output is inconvenient.

No score target.
No BUY/HOLD/SELL distribution target.
No majority vote.
No judge calls.
No fallback model.
No post-hoc candidate edits.
No selective rerun.

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260914-persistence-v2-implementation-local-ephemeral-replay-report.zip
```

Verified SHA-256:

```text
c98eee1b2de414d83a7b6b85b3693060b304c1995cdda59a62a1e9530e02a1ba
```

Recompute independently.

Sidecar must match exactly.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent archive verification:

```text
indexed payloads = 142

ZIP entries =
142 indexed payloads
+ artifact-index.json
= 143

missing = 0
extra = 0
hash mismatch = 0
size mismatch = 0
secret scan failure = 0.
```

Recompute independently.

---

# 2. M12BN authoritative completion

Freeze:

```text
status = PASS

top_level_implementation_result =
PERSISTENCE_V2_LOCAL_IMPLEMENTATION_PROOF_PASS

fresh_real_proof_readiness =
READY_FOR_SEPARATELY_AUTHORIZED_PROOF

next_scope =
NEW_FRESH_UNSEEN_PROOF_ON_CANONICAL_INTEGRATED_PIPELINE.
```

Repository reference:

```text
integration branch =
codex/20260914-persistence-v2-local-proof-m12bn

base integration head =
5e80c0122ca4c49cdd1ecc8cdf1eae5ad8ad5d94

final local head =
2cd59975650a05021e4bbd6376b4db30aa02efe6.
```

Tests:

```text
focused V2 =
33 / 33 PASS

existing persistence regression =
80 / 80 PASS

full local =
3988 PASS

Ruff =
PASS

git diff --check =
PASS.
```

---

# 3. Persistence V2 proof — freeze

M12BN proved locally:

```text
7 V2 tables

CanonicalAcceptanceReceiptV1

external/manual receipt minting = impossible

22 / 22 M12BJ canonical positives:
receipt PASS
local V2 persistence PASS
readback loss = 0
provenance mismatch = 0

16 / 16 historical fresh canonical negatives:
accepted receipt = 0
V2 persistence = 0
warning transition = 0
outbox = 0

duplicate replay = idempotent

same-date distinct generations = retained

stale replay = no current/warning/outbox regression

actual concurrent races = PASS

warning replay / confirmation / worsening / recovery / recurrence = PASS

outbox dedupe / retry / dead-letter = PASS

failure injection = PASS

post-acceptance semantic re-derivation = 0.
```

Do NOT reopen Persistence V2 design in M12BO.

---

# 4. V2 production gates remain OFF

Required throughout M12BO:

```text
v2_production_writer_enabled = false

v2_production_read_preference_enabled = false

v2_production_manual_registry_enabled = false

v2_production_warning_enabled = false

v2_production_outbox_delivery_enabled = false.
```

Do not change these defaults.

Do not run production migration.

---

# 5. Historical fresh proof — permanent proof exclusion

Historical generation:

```text
20260907-new-issuer-proof-20260907T055608Z-0446826566f6
```

Subjects:

```text
NVMI
SKYH
WKSP
EROC
373160
452200
389470
380550
008970
047080
068270
475830
033160
079940
103140
278280.
```

Canonical result:

```text
16 / 16 FAIL

accepted receipt = 0

canonical V2 persistence = 0.
```

These subjects are NOT eligible for M12BO.

Do not “retry the failed 16.”

M12BO needs new subjects.

---

# 6. Current monitored universe — exclude read-only

At task start,
re-enumerate the current active monitored universe read-only.

Last confirmed reference:

```text
000660
003690
005490
005930
010120
012450
047810
086280

CORZ
CPNG
CRCL
GOOGL
HUT
IBM
MU
RXRX
SKHY
SNDK
TSLA
TSM
WRD
WULF.
```

Do NOT hard-code this as the authoritative current set.

Use the actual read-only task-start universe.

Every active monitored ticker is excluded from fresh selection.

No monitoring registrations.
No monitoring stops.

---

# 7. Fresh-unseen definition

A selected M12BO subject must satisfy ALL:

```text
not active monitored

not in historical fresh proof subjects

not in any earlier new-issuer/fresh proof generation

not in any earlier fresh/new-issuer model candidate artifact

not an existing frozen fresh holdout subject

not a fictional subject

no earlier M12BO-equivalent model attempt.
```

A ticker appearing only in:

```text
generic documentation

provider metadata

market-wide symbol lists

non-model test constants
```

does NOT automatically make it seen.

The key is:

```text
no prior project fresh/new-issuer model decision artifact.
```

Be conservative.

If uncertain:

```text
exclude.
```

---

# 8. Project-wide seen-subject registry

Before candidate selection,
build:

```text
fresh-proof-seen-subject-registry-v1.
```

Sources must include:

```text
active monitored universe

all historical fresh/new-issuer proof manifests

all fresh/new-issuer model artifact manifests

all prior rejected/holdout fresh cohorts

M12BO task-local prior attempts if any.
```

Report:

```text
source artifact

generation id

ticker

reason excluded.
```

Do not include fictional IDs as real tickers.

---

# 9. Fresh cohort size

Target:

```text
12 real issuers.
```

Market balance:

```text
6 KR

6 US / supported US-listed foreign securities.
```

Do not silently reduce the cohort.

If 12 objectively eligible provider-ready unseen subjects cannot be selected:

```text
STOP_BEFORE_MODEL
FRESH_UNSEEN_COHORT_SELECTION_INCOMPLETE.
```

Do not proceed with a smaller favorable subset.

---

# 10. Sector-diversity target

The 12 subjects must collectively include at least:

```text
2 financial-sector subjects
   at least 1 KR
   at least 1 US

2 capital-intensive / cyclical / industrial subjects

2 technology / software / semiconductor-type subjects

2 consumer / service / logistics subjects

2 healthcare / life-science subjects

2 energy / materials / utility / infrastructure subjects.
```

One subject may satisfy only ONE primary selection bucket.

Use company profile classification before model calls.

Do not choose sector based on model output.

---

# 11. Optional ADR / ADS coverage

If a provider-ready unseen US-listed ADR/ADS subject exists
without weakening cohort selection integrity:

prefer one slot to exercise:

```text
security basis

ordinary/depositary share basis

currency/per-share caveat.
```

This is optional.

Do NOT force an ADR if:

```text
ratio/security basis is ambiguous

provider identity is incomplete.
```

No fabricated ADR ratio.

---

# 12. Candidate-universe source

Preferred candidate universe:

```text
existing repository/provider-supported issuer universe or symbol discovery source.
```

If the repository has no suitable deterministic discovery source,
a task-local fallback pool may be used ONLY as a pre-model candidate pool.

Fallback KR examples:

```text
000270
000810
003550
006400
009150
011200
018260
024110
028260
032830
034730
035420
035720
051910
055550
066570
086790
090430
096770
105560
207940
316140.
```

Fallback US examples:

```text
AAPL
MSFT
AMZN
META
JPM
BAC
GS
CAT
DE
UNH
LLY
PFE
XOM
CVX
NEE
CRM
ORCL
PLTR
SNOW
COST
WMT
NKE
F
GM
GE
V
MA
ABBV.
```

These are candidate-pool examples only.

Every symbol must still pass:

```text
seen-subject exclusion

identity validation

provider/data readiness

sector-slot eligibility.
```

Do not assume the fallback symbols are unseen.

---

# 13. Provider/data readiness preflight

Before final subject freeze,
a candidate may be excluded ONLY for objective input-readiness reasons such as:

```text
ticker/company identity unresolved

unsupported market/security

provider cannot assemble required base evidence packet

security basis ambiguous in a way that prevents safe analysis

required free provider unavailable for that subject

fresh packet builder returns an objective structural validation failure.
```

Do NOT exclude for:

```text
unattractive valuation

weak company quality

messy thesis

uncertain direction

likely HOLD

likely semantic difficulty.
```

No outcome cherry-picking.

---

# 14. Provider policy

Use only:

```text
existing supported FREE providers
```

and current repository provider flows.

No paid API.

No new paid dependency.

No scraping workaround added merely for the proof.

For KR:

use the supported company / earnings / OpenDART / snapshot flow
according to current repository contracts.

For US/foreign:

use the supported provider flow,
without guessing a provider name if repository routing already owns it.

---

# 15. Selection algorithm

After:

```text
seen-subject filtering

objective provider/data readiness

sector classification
```

select within each market/sector slot deterministically.

Recommended:

```text
rank =
sha256(
  "M12BO-20260914-FRESH-UNSEEN-V1|" + ticker
)
```

ascending.

Use the first eligible subject per required slot.

If one ticker is eligible for multiple slots,
assign it deterministically to the first unmet slot
according to a frozen slot order.

No manual attractiveness selection.

---

# 16. Subject freeze

Before ANY model call,
write:

```text
fresh-unseen-subject-freeze-manifest.
```

Include:

```text
12 selected tickers

company names

market

sector bucket

selection rank

seen-registry exclusion result

provider-readiness result

security-basis classification

selection timestamp

selection contract/hash.
```

After this manifest is frozen:

```text
NO SUBJECT REPLACEMENT.
```

A model failure does not reopen selection.

---

# 17. Provider failures after freeze

After subject freeze:

```text
no replacement subject.
```

If a frozen subject later has a provider failure:

record:

```text
FROZEN_SUBJECT_INPUT_FAILURE
```

with exact provider/input reason.

Continue other frozen subjects where safe.

Overall proof cannot PASS unless required final proof coverage is achieved.

Do not backfill with a substitute subject.

---

# 18. Evidence packet generation

For each frozen subject,
build one immutable real evidence packet.

Packet should include only current supported evidence,
such as applicable:

```text
company identity/profile

business/sector exposure

earnings checkpoints

official/reliable thesis events

current snapshot / valuation context

security/share basis

market expectation context

current price/positioning only if supported

important macro exposure only if actual transmission path exists.
```

Use existing packet builder contracts.

Do not create M12BO-specific semantic fields.

---

# 19. Freshness

This is a NEW real proof.

Use current data fetched during M12BO.

Do not reuse:

```text
old fresh proof packets

old monitored packets

old cached model inputs
```

as the primary current evidence packet.

Provider caches may be used only according to the repository's normal
freshness/validation contract.

Record all source as-of dates.

---

# 20. Packet freeze

Before model calls:

```text
serialize packet canonically

hash packet

freeze packet inventory

freeze evidence-view identities.
```

For each ticker report applicable:

```text
packet hash

BusinessDeltaEvidenceView identity

MarketExpectationEvidenceView identity

ConfiguredSignalEvidenceView applicability

WorkingCapitalCheckpointBindingView identity

financial evidence projection identity

security-basis identity.
```

No packet mutation after first model call.

---

# 21. Fresh Initial Analysis lifecycle

M12BO is:

```text
Initial Analysis / fresh issuer
```

not:

```text
Daily Monitoring.
```

Required user-facing lifecycle behavior:

```text
do not present "today strengthened/weakened/no material change"
as if a prior monitored thesis exists.
```

Internal compatibility fields may follow the frozen canonical fresh lifecycle contract.

M12BI determined:

```text
fresh Initial Analysis BusinessDelta semantics are decision-active internally

without safe baseline:
BusinessDeltaEvidenceView capability = UNCHANGED_ONLY

candidate must respect that contract.
```

Do not change this rule in M12BO.

Do not expose the internal compatibility field as a daily-delta user claim.

---

# 22. No fake monitored baseline

Fresh subjects have no monitored thesis baseline.

Do NOT create:

```text
fake watchlist item

fake previous assessment

fake configured monitoring signal

fake thesis version

fake holder history

fake price warning rule.
```

The fresh packet must remain genuinely pre-monitoring.

---

# 23. Persistence / thesis-version applicability preflight

Before the first model call,
audit the current fresh/new-issuer path against M12BN Persistence V2.

Question:

```text
Does a pre-monitoring fresh Initial Analysis already own
a legitimate canonical thesis_version / acceptance identity
under existing product contracts?
```

Do NOT assume:

```text
thesis_version = 1.
```

Do NOT create a monitoring item.

Do NOT register the stock.

---

# 24. Fresh persistence applicability outcomes

Choose exactly one BEFORE model calls:

```text
A. CANONICAL_RECEIPT_APPLICABLE_PRE_MONITORING

B. CANONICAL_RECEIPT_NOT_APPLICABLE_UNTIL_EXPLICIT_MONITORING_REGISTRATION

C. FRESH_ACCEPTANCE_IDENTITY_CONTRACT_GAP.
```

Use repository evidence.

### A
Only if an existing legitimate pre-monitoring thesis identity contract exists.

### B
If Initial Analysis is valid analysis output
but persistence receipt legitimately begins only after explicit monitoring registration.

This is acceptable and does NOT block the fresh semantic proof,
because M12BN already proved Persistence V2 separately.

### C
Use only if the product/runtime ambiguously expects fresh analysis
to persist canonically but has no legal identity contract.

If C:

```text
STOP_BEFORE_MODEL
```

and define one bounded integration-contract scope.

Do not fabricate identity just to continue.

---

# 25. No auto-monitoring side effects

Regardless of persistence applicability:

```text
monitoring registrations = 0

monitoring stops = 0

watchlist mutations = 0

thesis-version production mutations = 0

warning mutations = 0

notification writes = 0.
```

Fresh analysis alone must not enroll a ticker into monitoring.

---

# 26. Model architecture

Use the ACTIVE intended fresh/new-issuer production architecture
after M12BI convergence.

Do NOT run the old historical fresh path if it has been superseded.

Do NOT run monolithic compatibility architecture merely to compare.

This is proof of the intended canonical path.

Before model calls, report:

```text
entrypoint

Stage topology

context batching

planned model call count

model-facing schema hashes

prompt hashes

canonical semantic orchestrator identity.
```

---

# 27. Model

Use:

```text
gpt-5.6-sol

reasoning effort =
xhigh.
```

No Astra.

No fallback.

No judge calls.

No model substitution.

---

# 28. Model-call batching

Use the actual active fresh pipeline batch maximum.

Do not exceed:

```text
4 subjects per model call
```

unless the current canonical schema explicitly uses a stricter maximum.

For 12 subjects:

```text
context_count should normally be 3
```

for each batched stage.

But derive actual call count from the active architecture.

Freeze:

```text
planned_model_calls
```

before the first model call.

---

# 29. No repetitions

This is a holdout proof,
not a stochastic calibration experiment.

Run:

```text
one planned attempt per frozen context/stage.
```

No repetition ×3.

No majority vote.

No selective retry for semantic output.

---

# 30. Network readiness

Before first model call:

run one standard network-readiness preflight.

If unavailable:

```text
STOP
NETWORK_ENVIRONMENT_BLOCKER_NO_SEMANTIC_CHANGE.
```

No code change.

Do not alter candidate selection.

Do not replace subjects.

A later run uses the SAME frozen subject selection if its identity is still valid
and no model call was started.

---

# 31. Provider/network retries

Do not add new retry loops.

Existing bounded provider runtime behavior may remain.

Model wrapper retry:

```text
0.
```

If a model process starts and fails:

record objective runtime failure.

Do not retry selectively.

---

# 32. Complete frozen cohort

Candidate-specific semantic failures should NOT cause the whole task
to stop immediately.

Reason:

```text
M12BO is a final holdout cohort measurement.
```

Continue the remaining pre-frozen subjects where runtime integrity remains safe.

Immediate global hard-stop only for:

```text
production firewall violation

frozen packet mutation

canonical service provenance corruption

model schema/runtime infrastructure failure that invalidates later calls

receipt/trust-boundary corruption

security/secret incident.
```

At completion:

```text
ANY candidate hard semantic failure
→ fresh proof does not PASS.
```

But complete the cohort to measure the failure pattern.

---

# 33. Canonical semantic audit

Every candidate must go through:

```text
directional-core-semantic-audit-v1
```

and current canonical services.

Required provenance:

```text
proof-critical bypass = 0

legacy duplicate semantic participation = 0.
```

No fresh-specific independent semantic engine.

---

# 34. Required semantic families

Audit applicable:

```text
financial temporal semantics

FCF current/prospective

PPE-proxy vs FCF

net-debt completeness/scope

working-capital typed grounding

financial-sector specialized framework

ConfiguredSignalEvidenceView applicability

BusinessDeltaEvidenceView

MarketExpectationEvidenceView

direction/timing ownership

QTD/YTD comparability

ADR/security basis

core immutability

Stage-2 contamination.
```

No new regex engine.

---

# 35. Fresh configured-signal semantics

Fresh pre-monitoring issuers normally have no configured monitoring signals.

Expected:

```text
ConfiguredSignalEvidenceView =
explicit empty / not-applicable canonical lifecycle
```

according to M12BI contract.

Do not synthesize configured signals.

No configured signal is needed merely to express:

```text
future reevaluation criteria
```

owned by fresh Initial Analysis.

---

# 36. Fresh Business Delta — critical regression target

The old fresh proof failed here.

Required for each subject:

```text
BusinessDeltaEvidenceView capability

eligible observed baseline-change evidence

candidate business_thesis_change

canonical validation result.
```

Without a safe monitored baseline:

expected compatibility behavior follows the current frozen fresh contract.

No raw-text fallback.

Fresh proof PASS requires:

```text
BusinessDelta hard failure count = 0.
```

---

# 37. Market expectation

Fresh Initial Analysis must distinguish:

```text
market expectation context

from

independent directional business evidence.
```

Expectation-only evidence must not become
a material business anchor without canonical independence eligibility.

Required hard failure count:

```text
0.
```

---

# 38. Financial-sector subjects

At least two fresh subjects must exercise
the financial-sector specialized framework.

Verify:

```text
no industrial net-debt/WC framework misuse

capital / underwriting / bank or insurer-specific metrics interpreted appropriately

contrastive replacement semantics valid

generic industrial ratios not used as the primary framework when inappropriate.
```

Do not require identical metrics across different financial subsectors.

---

# 39. Capital-intensive / cyclical subjects

Verify:

```text
no peak-cycle low-PER shortcut

cash conversion / capex / debt claims use safe evidence

working-capital direction is context-specific

FCF not synthesized from OCF-PPE.
```

---

# 40. Healthcare / life-science subjects

Do not force PER where inapplicable.

Use the existing sector-appropriate valuation/evidence contract.

If earnings are negative:

```text
no fabricated meaningful PER.
```

No industry-irrelevant SaaS metric requirements.

---

# 41. User-facing Initial Analysis coverage

For each accepted fresh subject,
the final product-facing artifact must support the existing Initial Analysis structure:

```text
핵심 결론

회사와 사업 구조

산업과 포지셔닝

재무와 이익의 질

시장 기대

핵심 투자 논리

Valuation

촉매

리스크

Early Warning / Kill Condition

중요한 Macro exposure where material

가격·수급/포지셔닝 only where actual data exists

다음 확인 숫자

최종 한 줄.
```

Use current renderer/product contract.

Do not create unavailable metrics.

Do not invent price rules.

---

# 42. Fact / Interpretation / Unknown

For every accepted subject verify:

```text
Fact

Interpretation

Unknown
```

remain separable.

Unknown must state:

```text
what is unknown

why it matters

what would confirm it
```

where product contract supports it.

No hidden fabrication to fill gaps.

---

# 43. Market expectation level

Where current output owns the level,
use only supported levels:

```text
depressed
low
balanced
elevated
very_high
speculative
unknown.
```

Do not infer a level from price alone.

---

# 44. Valuation safety

For each accepted subject:

```text
industry-appropriate primary method

secondary method only where useful

current multiples only when safe

no denominator reconstruction from price/provider multiple

no one-quarter annualized PER

negative valid TTM EPS → N/M

ADR/share basis verified before per-share use.
```

No post-model recalculation with unsafe denominators.

---

# 45. Price / positioning scope

Use price/supply only when actual packet data supports it.

Do not allow:

```text
technical

price move

short-term flow
```

to create fundamental:

```text
business delta

holder REDUCE

invalidation.
```

No invented support/resistance/target/stop.

---

# 46. Fresh warning / kill condition scope

Initial Analysis may describe:

```text
future Early Warning / Kill Condition
```

as analysis criteria.

This must NOT:

```text
open a real warning

write warning_state_v2

queue a notification

create monitoring configuration.
```

Product prose criteria and runtime warning state are separate.

---

# 47. Final composition

Every accepted subject requires:

```text
Stage/final composition PASS

core hash frozen

stance hash frozen

no Stage-2 core mutation.
```

Do not accept a semantically valid intermediate core
if final composition fails.

---

# 48. Persistence compatibility diagnostic

After final acceptance,
apply the pre-frozen result from section 24.

### If A — receipt applicable pre-monitoring
Use TEMPORARY / EPHEMERAL V2 only.

Required:

```text
valid trusted receipt

lossless local V2 write/read

no warning/outbox unless explicitly applicable

no production gate enablement.
```

### If B — receipt not applicable until explicit registration
Do NOT fabricate a receipt.

Report:

```text
fresh persistence receipt =
NOT_APPLICABLE_PRE_MONITORING

reason =
explicit monitoring registration owns thesis/version identity.
```

This does not reduce semantic proof status.

### If C
The task should already have stopped before model calls.

---

# 49. No local persistence if it implies fake monitoring identity

Even in ephemeral DB,
do NOT set a fabricated:

```text
watchlist id

monitoring state

thesis_version
```

merely to prove V2.

M12BN already proved V2 with real monitored accepted outputs.

M12BO fresh proof does not need to re-prove V2 by inventing product state.

---

# 50. No production persistence

Regardless of section 48:

```text
production_db_mutations = 0

assessment_production_writes = 0

warning_production_mutations = 0

notification_production_queue_writes = 0

production_sends = 0.
```

---

# 51. Fresh proof success criteria

Top-level fresh proof may PASS only if:

```text
12 / 12 frozen subjects have valid immutable input packets

all planned model calls complete

all 12 produce schema-valid candidates

all 12 pass canonical hard semantic audit

all 12 pass final composition

BusinessDelta hard failures = 0

FCF hard failures = 0

net-debt hard failures = 0

WC grounding failures = 0

financial-sector hard failures = 0

expectation hard failures = 0

QTD/YTD hard failures = 0

security-basis hard failures = 0

Stage-2 contamination = 0

core mutation = 0

proof-critical canonical bypass = 0

legacy duplicate semantic participation = 0

user-facing Initial Analysis lifecycle violation = 0

subject replacement after freeze = 0

selective semantic reruns = 0

production side effects = 0.
```

No required direction distribution.

---

# 52. Accepted / rejected counts

Report:

```text
fresh_subject_count

schema_valid_count

canonical_semantic_pass_count

canonical_semantic_fail_count

final_composition_pass_count

accepted_count.
```

For top-level PASS:

```text
accepted_count = 12.
```

If not:

```text
overall proof fails
```

but all pre-frozen attempted results remain evidence.

Do not replace failures.

---

# 53. Failure taxonomy

Classify any non-accepted result into exactly one primary category:

```text
INPUT_PACKET_FAILURE

MODEL_RUNTIME_FAILURE

MODEL_SCHEMA_FAILURE

BUSINESS_DELTA_CONTRACT_FAILURE

FINANCIAL_SEMANTIC_FAILURE

WORKING_CAPITAL_GROUNDING_FAILURE

FINANCIAL_SECTOR_FAILURE

MARKET_EXPECTATION_FAILURE

DIRECTION_TIMING_OWNERSHIP_FAILURE

SECURITY_BASIS_FAILURE

STAGE2_CONTAMINATION

CORE_MUTATION

FINAL_COMPOSITION_FAILURE

OTHER_CANONICAL_HARD_FAILURE.
```

Do not hide multiple secondary errors.

---

# 54. No micro-patch during M12BO

If a fresh candidate fails a canonical semantic contract:

```text
record it

complete the frozen cohort where safe

do NOT patch the validator or prompt inside M12BO

do NOT rerun the failed subject.
```

After cohort completion:
determine whether the pattern is:

```text
model-contract compliance issue

or
canonical semantic engine defect

or
input packet defect.
```

M12BO is proof, not repair.

---

# 55. No old fresh-path fallback

Do not use an old fresh-only partial semantic audit.

Required:

```text
fresh canonical semantic service provenance =
same single-source architecture confirmed by M12BI.
```

Any fresh-specific audit may only be:

```text
ownership/domain/input-lifecycle additive checks.
```

No independent re-derivation.

---

# 56. Selection fairness audit

Produce an audit proving:

```text
subjects were selected before model calls

selection did not use model output

selection did not use direction/valuation attractiveness

post-freeze replacements = 0

failed candidates remain in denominator.
```

This is a required final-proof artifact.

---

# 57. Current data provenance

Per ticker report:

```text
profile as-of

earnings checkpoint as-of

event evidence window

snapshot as-of

security basis as-of

provider source identities

packet hash.
```

Do not package secrets/tokens.

---

# 58. Source attribution

Separate:

```text
company official / filing

market/provider

derived deterministic calculation

model interpretation.
```

Do not attribute provider estimates as company guidance.

Do not treat related-company news as target-company fact.

---

# 59. Provider failures

If a provider response is:

```text
null

partial

stale

ambiguous
```

preserve that state.

Do not fill missing fields with guesses.

A noncritical unknown may remain Unknown.

A proof-critical identity/input ambiguity may fail the subject.

---

# 60. Model artifact retention

Store raw model artifacts locally only.

Do not push raw model artifacts remotely.

Artifact index may include:

```text
hash

size

relative path

model identity

call identity.
```

No secrets.

---

# 61. No retries / judge / post-hoc resolution

Required:

```text
wrapper retry count = 0

judge calls = 0

fallback calls = 0

selective rerun count = 0

post-hoc decision override count = 0.
```

Existing deterministic parsing validation is allowed.

---

# 62. Output distribution is diagnostic only

Report:

```text
BUY count

HOLD count

SELL count

new-buyer stance distribution

holder stance distribution

Business Delta distribution.
```

Do NOT require:

```text
at least one BUY

at least one SELL

balanced distribution.
```

No distribution forcing.

---

# 63. Confidence / calibration

Confidence differences or unexpected distribution are not failures by themselves.

Hard failure requires:

```text
objective contract violation.
```

Do not introduce a stability score target.

One unseen run per subject is sufficient.

---

# 64. Fresh subject independence

No subject may be dropped because another subject in the same sector passed.

Each is independent evidence.

No cross-subject majority.

No sector winner/loser adjudication.

---

# 65. Formal proof result

Choose exactly one:

```text
FRESH_UNSEEN_CANONICAL_PROOF_PASS

FRESH_UNSEEN_CANONICAL_PROOF_FAIL

NETWORK_ENVIRONMENT_BLOCKER_NO_SEMANTIC_CHANGE

FRESH_UNSEEN_PREMODEL_CONTRACT_GAP.
```

No vague partial-ready result.

---

# 66. Conditions for PASS

Require all section 51 gates.

Additionally:

```text
fresh_unseen_subject_selection_status = PASS

project_seen_registry_status = PASS

provider_readiness_freeze_status = PASS

packet_freeze_status = PASS

model_call_plan_freeze_status = PASS

canonical_provenance_status = PASS

production_firewall_status = PASS.
```

---

# 67. Main merge readiness after PASS

If and only if M12BO PASS:

```text
final_main_merge_readiness =
READY_FOR_EXPLICIT_USER_APPROVAL.
```

This is NOT permission to merge.

Do not run:

```text
git checkout main

git merge

git push

deploy.
```

Wait for explicit user approval in a later task.

---

# 68. Production readiness after PASS

Even after M12BO PASS:

```text
production_readiness =
NOT_READY_PENDING_EXPLICIT_CUTOVER_APPROVAL.
```

Reasons include:

```text
V2 production gates remain disabled

production migration not run

warning/outbox delivery not enabled

schedules remain paused

no explicit user cutover approval.
```

Do not enable any of these.

---

# 69. Next scope after PASS

Required:

```text
next_scope =
FINAL_MAIN_MERGE_AND_PRODUCTION_CUTOVER_APPROVAL_GATE.
```

This must be a separate user-authorized step.

Do not prepare an automatic deployment task.

---

# 70. If proof fails

If one or more fresh subjects fail:

```text
final_main_merge_readiness = NOT_READY

production_readiness = NOT_READY.
```

Next scope must be selected from evidence:

```text
BOUNDED_FRESH_MODEL_CONTRACT_COMPLIANCE_REPAIR

BOUNDED_CANONICAL_SEMANTIC_ENGINE_REPAIR

BOUNDED_FRESH_INPUT_PACKET_REPAIR

FRESH_ACCEPTANCE_IDENTITY_CONTRACT_REPAIR

NETWORK_RETRY_SAME_FROZEN_COHORT.
```

Do not mix unrelated repairs.

---

# 71. If network fails before any model call

If subject and packets are frozen
but model network readiness fails before call 1:

```text
preserve frozen cohort identity

model_calls_started = 0

next_scope =
RETRY_SAME_FROZEN_FRESH_COHORT_WHEN_NETWORK_READY.
```

No re-selection.

No semantic change.

---

# 72. If network/runtime fails after calls begin

Do NOT selectively retry.

Record exact completed/failed calls.

Overall proof:

```text
FAIL / runtime incomplete.
```

A future rerun must be explicitly authorized.

Do not mix old and new call outputs into a synthetic complete generation.

---

# 73. No fresh monitoring registration

This is analysis proof only.

Required:

```text
monitoring_registrations = 0

monitoring_stops = 0

new watchlist rows = 0

production thesis versions created = 0.
```

Do not call monitoring-management endpoints.

---

# 74. No warning / notification automation

Fresh Initial Analysis may produce analytical:

```text
Early Warning

Kill Condition
```

prose/structured conditions.

Runtime automation:

```text
warning_state_v2 rows = 0
notification_outbox_v2 rows = 0
production sends = 0
```

unless an explicitly local ephemeral receipt applicability test in section 48
requires a non-automating canonical row.

No runtime alert activation.

---

# 75. Local Persistence V2 gates remain disabled

Do not enable default production settings.

A one-off ephemeral test may instantiate local services explicitly if section 48A applies.

At completion verify:

```text
production writer = false

production read preference = false

production warning = false

production outbox delivery = false.
```

---

# 76. Test regression gate

Before model calls:

```text
focused fresh pipeline tests

M12BI semantic convergence regressions

M12BN persistence V2 focused tests
```

must PASS.

After fresh proof:

```text
full local test suite

Ruff

git diff --check.
```

No expected semantic-output edits merely to get green.

---

# 77. Code-change policy

M12BO is a PROOF task.

Preferred code change count:

```text
0
```

outside proof harness/artifact generation.

If proof setup requires a small test/report harness,
it must not change production semantics.

If a production/model-facing code change is required BEFORE proof:

```text
STOP_BEFORE_MODEL
FRESH_UNSEEN_PREMODEL_CONTRACT_GAP.
```

Do not implement and prove in one task.

---

# 78. Required provenance artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12bo-scope-freeze

04-m12bi-semantic-convergence-freeze

05-m12bj-monitored-proof-freeze

06-m12bk-r2-policy-freeze

07-m12bn-persistence-v2-proof-freeze

08-production-firewall-precheck

09-model-semantic-hash-freeze.
```

---

# 79. Required unseen-selection artifacts

Produce:

```text
10-current-active-monitor-universe

11-project-wide-fresh-seen-subject-registry

12-candidate-universe-manifest

13-provider-data-readiness-preflight

14-sector-bucket-eligibility

15-selection-ranking-manifest

16-fresh-unseen-subject-freeze-manifest

17-selection-fairness-audit

18-no-post-freeze-replacement-proof.
```

---

# 80. Required persistence-applicability artifacts

Produce BEFORE model calls:

```text
19-fresh-thesis-version-ownership-audit

20-fresh-persistence-applicability-decision

21-no-auto-monitoring-contract-proof.
```

Artifact 20 exactly one:

```text
CANONICAL_RECEIPT_APPLICABLE_PRE_MONITORING

CANONICAL_RECEIPT_NOT_APPLICABLE_UNTIL_EXPLICIT_MONITORING_REGISTRATION

FRESH_ACCEPTANCE_IDENTITY_CONTRACT_GAP.
```

If the third:

```text
STOP_BEFORE_MODEL.
```

---

# 81. Required fresh input artifacts

Produce:

```text
22-fresh-provider-source-manifest

23-fresh-company-profile-manifest

24-fresh-earnings-checkpoint-manifest

25-fresh-event-evidence-manifest

26-fresh-current-snapshot-manifest

27-fresh-security-basis-manifest

28-fresh-evidence-view-manifest

29-fresh-packet-inventory

30-fresh-packet-hash-manifest

31-fresh-frozen-context-manifest

32-fresh-model-call-plan.
```

---

# 82. Required model artifacts

Produce:

```text
33-fresh-model-network-readiness

34-fresh-stage1-model-artifacts

35-fresh-stage2-model-artifacts
```

or actual active-stage equivalents.

If fresh architecture has different stage names:

use the real names,
but preserve raw-call identity.

No monolithic compatibility calls unless the active fresh production path actually uses them.

---

# 83. Required semantic audit artifacts

Produce:

```text
36-fresh-canonical-service-provenance-audit

37-fresh-business-delta-audit

38-fresh-financial-semantics-audit

39-fresh-working-capital-audit

40-fresh-financial-sector-audit

41-fresh-market-expectation-audit

42-fresh-direction-timing-ownership-audit

43-fresh-qtd-ytd-audit

44-fresh-security-basis-audit

45-fresh-stage2-contamination-audit

46-fresh-core-immutability-audit.
```

---

# 84. Required final-output artifacts

Produce:

```text
47-fresh-final-composition-audit

48-fresh-user-facing-initial-analysis-lifecycle-audit

49-fresh-fact-interpretation-unknown-audit

50-fresh-valuation-safety-audit

51-fresh-price-positioning-scope-audit

52-fresh-early-warning-kill-condition-scope-audit

53-fresh-per-subject-final-summary

54-fresh-output-distribution-diagnostic.
```

---

# 85. Required persistence compatibility artifacts

If section 20 = A:

produce:

```text
55-fresh-local-receipt-issuance

56-fresh-local-v2-lossless-replay

57-fresh-local-v2-no-warning-outbox-proof.
```

If section 20 = B:

produce:

```text
55-fresh-local-receipt-issuance =
NOT_APPLICABLE_PRE_MONITORING

56-fresh-local-v2-lossless-replay =
NOT_APPLICABLE_PRE_MONITORING

57-fresh-local-v2-no-warning-outbox-proof =
PASS_ZERO_AUTOMATION.
```

Do not fabricate.

---

# 86. Required cohort result artifacts

Produce:

```text
58-fresh-subject-result-matrix

59-fresh-hard-failure-matrix

60-fresh-failure-taxonomy

61-fresh-accepted-count-decision

62-fresh-unseen-canonical-proof-decision

63-main-merge-readiness-decision

64-production-readiness-decision

65-next-scope-decision

66-master-workflow-update

67-program-completion.
```

---

# 87. Subject result matrix

One row per frozen subject:

```text
ticker

company

market

sector bucket

unseen status

provider readiness

packet hash

model call id(s)

schema valid

BusinessDelta status

financial semantic status

WC status

financial-sector status

expectation status

direction/timing status

security-basis status

Stage-2 status

core immutability

final composition

accepted

primary failure category if any

overall direction

new-buyer stance

holder stance

confidence.
```

Decision fields are diagnostic.

No target distribution.

---

# 88. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch
final_local_head_sha

latest_result_zip_sha256
latest_result_integrity

m12bi_semantic_convergence_status
m12bj_monitored_proof_status
m12bk_r2_policy_validation_status
m12bn_persistence_v2_status

task_start_active_monitor_count
task_start_active_monitor_tickers

fresh_seen_registry_subject_count
fresh_candidate_pool_count
fresh_provider_ready_candidate_count

fresh_target_subject_count
fresh_selected_subject_count
fresh_selected_tickers
fresh_kr_subject_count
fresh_us_subject_count
fresh_financial_subject_count
fresh_cyclical_industrial_subject_count
fresh_technology_subject_count
fresh_consumer_service_subject_count
fresh_healthcare_subject_count
fresh_energy_materials_utility_subject_count

post_freeze_subject_replacement_count

fresh_persistence_applicability

planned_model_call_count
model_calls_started
model_calls_completed
wrapper_retry_count
fallback_model_call_count
judge_call_count
selective_rerun_count
posthoc_decision_override_count

fresh_packet_count
fresh_packet_hash_mismatch_count
fresh_packet_mutation_count

fresh_schema_valid_count
fresh_canonical_semantic_pass_count
fresh_canonical_semantic_fail_count
fresh_final_composition_pass_count
fresh_accepted_count

fresh_business_delta_hard_failure_count
fresh_financial_semantic_hard_failure_count
fresh_fcf_hard_failure_count
fresh_netdebt_hard_failure_count
fresh_working_capital_hard_failure_count
fresh_financial_sector_hard_failure_count
fresh_market_expectation_hard_failure_count
fresh_direction_timing_hard_failure_count
fresh_qtd_ytd_hard_failure_count
fresh_security_basis_hard_failure_count
fresh_stage2_contamination_count
fresh_core_mutation_count

fresh_user_facing_initial_lifecycle_violation_count
fresh_fact_interpretation_unknown_violation_count
fresh_valuation_safety_violation_count
fresh_price_positioning_scope_violation_count
fresh_runtime_warning_activation_count

fresh_primary_direction_distribution
fresh_new_buyer_distribution
fresh_holder_distribution
fresh_business_delta_distribution
fresh_confidence_distribution

proof_critical_canonical_bypass_count
legacy_duplicate_semantic_participation_count

fresh_local_receipt_count
fresh_local_v2_persisted_count
fresh_local_warning_count
fresh_local_outbox_count

model_prompt_semantic_change_count
model_schema_semantic_change_count
canonical_semantic_service_change_count
decision_policy_change_count
persistence_v2_contract_change_count

provider_source_fetches
paid_provider_dependency_count

production_db_mutations
monitoring_registrations
monitoring_stops
watchlist_mutations
production_thesis_version_mutations
assessment_production_writes
warning_production_mutations
notification_production_queue_writes
production_sends

v2_production_writer_enabled
v2_production_read_preference_enabled
v2_production_warning_enabled
v2_production_outbox_delivery_enabled

remote_push_count
raw_model_artifact_remote_push_count
main_branch_mutations
main_merges
deployments
scheduler_mutation_count
automatic_monitoring_resume

top_level_fresh_proof_result

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

focused_test_result
full_test_result
ruff_result
git_diff_check

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count.
```

Anything genuinely unavailable:

```text
NOT_MEASURED
```

with reason.

Do not replace unknown with zero.

---

# 89. Expected clean result

If all proof gates pass:

```text
top_level_fresh_proof_result =
FRESH_UNSEEN_CANONICAL_PROOF_PASS

fresh_target_subject_count = 12

fresh_selected_subject_count = 12

fresh_schema_valid_count = 12

fresh_canonical_semantic_pass_count = 12

fresh_canonical_semantic_fail_count = 0

fresh_final_composition_pass_count = 12

fresh_accepted_count = 12

post_freeze_subject_replacement_count = 0

wrapper_retry_count = 0

fallback_model_call_count = 0

judge_call_count = 0

selective_rerun_count = 0

proof_critical_canonical_bypass_count = 0

legacy_duplicate_semantic_participation_count = 0

paid_provider_dependency_count = 0

production_db_mutations = 0

monitoring_registrations = 0

watchlist_mutations = 0

warning_production_mutations = 0

notification_production_queue_writes = 0

production_sends = 0

v2_production_writer_enabled = false

v2_production_read_preference_enabled = false

v2_production_warning_enabled = false

v2_production_outbox_delivery_enabled = false

fresh_real_proof_readiness =
PROOF_COMPLETE

final_main_merge_readiness =
READY_FOR_EXPLICIT_USER_APPROVAL

production_readiness =
NOT_READY_PENDING_EXPLICIT_CUTOVER_APPROVAL

next_scope =
FINAL_MAIN_MERGE_AND_PRODUCTION_CUTOVER_APPROVAL_GATE.
```

Do NOT force this result.

---

# 90. Failure handling — semantic/model contract

If multiple fresh candidates fail the same contract:

do NOT patch in M12BO.

After completing the cohort,
classify whether:

```text
model contract compliance issue

canonical semantic engine issue

input packet issue.
```

Next scope must be bounded.

No per-ticker patch.

---

# 91. Failure handling — one isolated candidate

Even one hard semantic failure means:

```text
FRESH_UNSEEN_CANONICAL_PROOF_FAIL.
```

Do not drop the subject.

Do not replace it.

Do not reinterpret failure as harmless because 11/12 passed.

Use exact evidence to decide the smallest next scope.

---

# 92. Failure handling — fresh identity/persistence contract

If section 20 = C:

```text
STOP_BEFORE_MODEL

top_level_fresh_proof_result =
FRESH_UNSEEN_PREMODEL_CONTRACT_GAP

next_scope =
FRESH_ACCEPTANCE_IDENTITY_CONTRACT_REPAIR.
```

No model calls.

No fake thesis version.

---

# 93. Failure handling — provider selection

If fewer than 12 provider-ready unseen subjects satisfy required buckets:

```text
STOP_BEFORE_MODEL
FRESH_UNSEEN_COHORT_SELECTION_INCOMPLETE.
```

Report the candidate pool and exclusion reasons.

Do not reduce the target.

---

# 94. Failure handling — production firewall

Any:

```text
production write

registration

warning mutation

notification queue mutation

send

scheduler mutation

remote raw artifact push
```

causes:

```text
STOP
PRODUCTION_FIREWALL_VIOLATION.
```

Do not continue model calls.

---

# 95. No merge/deploy

Even after PASS:

```text
main merge = 0

deployment = 0

remote push = 0

scheduler resume = 0.
```

Only produce readiness evidence.

Explicit user approval is required later.

---

# 96. Local / nonproduction boundary

Provider/model network calls are allowed because this is the actual real fresh proof.

Production system mutation is not.

Allowed:

```text
read-only provider fetches

model calls on frozen fresh packets

local proof artifacts

temporary local DB only if section 20A applies.
```

Forbidden:

```text
production DB

production queue

production sender

production scheduler mutation

watchlist registration.
```

---

# 97. Secret safety

Do not package:

```text
API keys

tokens

DB URLs with credentials

provider secrets

model auth secrets.
```

Report provider names/contracts without secret values.

Artifact secret scan:

```text
0 failures.
```

---

# 98. Final task principle

M12BN proved persistence mechanics locally.

M12BO is now the final NEW holdout model-quality proof.

Its integrity depends on:

```text
new subjects

fixed before model output

real current provider data

no historical fresh reuse

no monitored subject reuse

sector diversity

same canonical semantics as monitored path

no wrapper bypass

no candidate replacement

no retries/judges/overrides

no production state mutation.
```

The causal flow is:

```text
build prior-subject exclusion registry

→ select 12 deterministic fresh unseen issuers

→ freeze subject list

→ verify fresh pre-monitoring persistence/thesis-version applicability
   without fabricating state

→ fetch/freeze current real evidence packets

→ freeze model call plan

→ run gpt-5.6-sol / xhigh once per planned stage/context

→ audit through canonical single-source semantics

→ complete final composition

→ keep Initial Analysis distinct from Daily Monitoring

→ verify no auto-monitoring / warning / notification side effect

→ if 12/12 pass:
   mark main merge READY_FOR_EXPLICIT_USER_APPROVAL only

→ stop and wait for explicit user approval before merge/cutover.
```

Do NOT:

```text
retry the historical failed 16

reuse monitored names

select easy subjects after seeing outputs

replace failed subjects

change semantic rules

change decision policy

change Persistence V2

fabricate thesis_version

auto-register fresh issuers

open warnings

queue notifications

force BUY/HOLD/SELL mix

run a judge

merge main

deploy

enable V2 production gates

resume schedules.
```
