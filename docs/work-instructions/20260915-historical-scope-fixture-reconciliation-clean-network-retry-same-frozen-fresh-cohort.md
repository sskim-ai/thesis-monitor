# Thesis Monitor — Historical Scope-Fixture Reconciliation + Clean Network Retry of Same Frozen Fresh Cohort

## 0. Task identity

Suggested work-instruction filename:

```text
20260915-historical-scope-fixture-reconciliation-clean-network-retry-same-frozen-fresh-cohort.md
```

Suggested result bundle:

```text
thesis-monitor-20260915-historical-scope-fixture-reconciliation-clean-network-retry-same-frozen-fresh-cohort-report.zip
```

Master-workflow phase:

```text
M12BQ — Clean Fresh-Proof Retry
         A. Freeze successful M12BP KR-financial input repair
         B. Freeze the exact 12-subject cohort and all packet/context hashes
         C. Reconcile ONLY the five historical exact-scope freeze fixtures
         D. Require the full local suite to return green before any model call
         E. Do one normal network-readiness preflight
         F. Create a NEW proof generation
         G. Re-run the entire frozen six-call plan from call 1
         H. Do not reuse, stitch, or selectively retry the timed-out M12BP invocation
         I. Complete the frozen cohort wherever infrastructure remains safe
         J. If 12/12 canonical proof passes, stop at explicit user merge/cutover approval gate
```

This task is NOT another KR-financial repair.

M12BP already proved the bounded input repair works.

This task has two narrowly separated phases:

```text
Phase 1:
historical exact-scope fixture maintenance only

Phase 2:
clean network retry of the exact frozen fresh cohort.
```

Do NOT change:

```text
KR-financial cold-start business logic

financial-holding classification

sector-operating evidence projection

source sufficiency

model prompts

model schemas

canonical semantic services

decision policy

Persistence V2

subject selection

packet contents

context batching

model timeout

model effort

model identity.
```

---

# 1. Authoritative latest result

Authoritative latest result bundle:

```text
thesis-monitor-20260915-kr-financial-coldstart-evidence-projection-repair-conditional-fresh-proof-retry-report.zip
```

Verified SHA-256:

```text
f289e67189f98c24ed36a52d3e1c3c3b50e73e6e7a0a847ebfb29e804821c10a
```

Recompute independently.

Sidecar must match exactly.

If mismatch:

```text
STOP
LATEST_RESULT_BUNDLE_CHECKSUM_MISMATCH
```

Independent artifact verification:

```text
indexed payloads = 155

ZIP entries =
155 indexed payloads
+ artifact-index.json
= 156

missing = 0
extra = 0
hash mismatch = 0
size mismatch = 0
secret scan failure = 0.
```

Recompute independently.

---

# 2. M12BP authoritative outcome

Freeze:

```text
KR financial cold-start repair =
PASS

six-candidate replay =
6 / 6 directional-ready

KR financial slot fillability =
PASS

regulatory-capital fabrication count =
0

generic revenue relabel count =
0

nonfinancial 64992 negative control =
PASS

model prompt semantic changes =
0

model schema semantic changes =
0

canonical semantic service changes =
0

decision policy changes =
0

Persistence V2 changes =
0.
```

The input repair is complete.

Do NOT reopen it merely because the subsequent model transport timed out.

---

# 3. Exact six repaired KR-financial candidates

M12BP replayed:

```text
138930
139130
000810
316140
086790
032830
```

Result:

```text
6 / 6 current/provider-ready.
```

Generic contracts:

```text
financial holding classification =
official code 64992
+
official legal-name financial-holding identity

financial sector operating projection =
exact official IFRS taxonomy sector-operating current evidence.
```

No ticker-specific waiver.

No fabricated capital ratio.

No industrial revenue relabeling.

Freeze this result.

---

# 4. Frozen fresh cohort

M12BP created a complete frozen cohort:

```text
generation =
20260915-m12bp-fresh-20260914T101849Z-9329c13191bd
```

Subjects, in frozen selection order:

```text
138930
BAC
000270
GM
018260
INTU
004170
SBUX
207940
ABBV
096770
CVX.
```

Counts:

```text
total = 12

KR = 6

US = 6

financial = 2.
```

Post-freeze subject replacement:

```text
0.
```

This cohort is the ONLY cohort allowed in M12BQ.

No reselection.

No provider-based replacement.

No “easier” substitute.

---

# 5. Frozen packet hashes

Freeze exactly:

```text
138930 =
22db6eab6479d4a2fbdd4fa5b027389fc4a569286bbdf836f5495520c10386cb

BAC =
d934438859098b14dad29a512cf0dc84e56e181edb0d4dcb0c9b3f26b45e143f

000270 =
140e4441ec3c4abd708a85e7b2caf9804be11cdb193a3ef387838c0b97df3ac9

GM =
cfdd3ea42c46c9e0b7579145248edd41d50ed9ce39711f6166c85a282ea8e566

018260 =
997222358a9828fcab11a212f4a627b00a12e87dc5de20733ca979ca19ce2a91

INTU =
9294df7445b2303d3ebeff05d139cdfaa8462e108f4ba2f0174366b6f9566820

004170 =
f33e55a208c9c2186b20b3ab98dd1bb64b33fa7c501d188481bb8cfa1d8e0eb2

SBUX =
df050827a72fb0e0f20e8751760cd94fac6ce780d5455dd7ddc338f64fbf4d1f

207940 =
86662915cfb42238b63d728f6bc93e0c5ad84e7506d8b1f4befb9e00bd3c6f92

ABBV =
6194ccaa1ea1d203d982c3f544c28d4f5e48260f4f83d9487665c1a530925a06

096770 =
e107c72785da5c65a3d66ef7bc826a909e82213970e597fd57639b889ff2919a

CVX =
78d23e9d706b4aa14fc1674c9c1c54125258e1498e9c16a332e93cbcdf84f89a.
```

Source lock:

```text
fbba423ff404d7bdc5ecad0cc49bef29ccc2634bd15652914909d15568ad1e82
```

Prompt/schema lock:

```text
40d06ebe385d42492e44d9bb2d7ae5c2b18f3f01c0584de8ba22aff1df91afa0.
```

Before any model call, recompute every identity.

Required:

```text
packet hash mismatch = 0

source lock mismatch = 0

prompt/schema lock mismatch = 0

frozen context mismatch = 0.
```

If any mismatch:

```text
STOP
FROZEN_FRESH_COHORT_IDENTITY_MISMATCH.
```

Do not regenerate the packet.

---

# 6. Frozen call plan

M12BP froze exactly six calls:

## Call 1

```text
stage =
DIRECTIONAL_CORE

tickers =
138930
BAC
000270
GM
```

## Call 2

```text
stage =
DIRECTIONAL_CORE

tickers =
018260
INTU
004170
SBUX
```

## Call 3

```text
stage =
DIRECTIONAL_CORE

tickers =
207940
ABBV
096770
CVX
```

## Call 4

```text
stage =
PRICE_TIMING

tickers =
138930
BAC
000270
GM
```

## Call 5

```text
stage =
PRICE_TIMING

tickers =
018260
INTU
004170
SBUX
```

## Call 6

```text
stage =
PRICE_TIMING

tickers =
207940
ABBV
096770
CVX.
```

Frozen model contract:

```text
model =
gpt-5.6-sol

reasoning_effort =
xhigh

max_subjects_per_call =
4

attempt_limit =
1

planned calls =
6

fallback =
0

judge =
0

selective rerun =
0.
```

Do not alter the call topology.

---

# 7. M12BP transport failure — classify correctly

M12BP first call:

```text
started =
1

completion receipt =
1

usable model output =
0

stdout bytes =
0

result file =
none

websocket disconnect observation =
1

watchdog timeout =
1800 seconds

wrapper retry =
0

fallback =
0

judge =
0

selective rerun =
0.
```

This is:

```text
TRANSPORT_INCOMPLETE
```

not:

```text
semantic failure.
```

No schema/canonical/final candidate was produced.

Do not infer model judgment from the timeout.

---

# 8. Old incomplete generation must never be stitched

Forbidden:

```text
use the old timed-out call as call 1

resume calls 2-6 only

combine a later call 1 with old-generation artifacts

treat completion receipt as model output

reuse partial runtime files as candidate output.
```

The old M12BP generation remains immutable forensic evidence.

M12BQ must create:

```text
a NEW proof generation id.
```

It replays all SIX calls from call 1.

Same frozen cohort and inputs,
new execution generation.

---

# 9. Timeout contract — freeze

M12BP used:

```text
1800 seconds.
```

M12BQ must NOT change the timeout merely because the prior call timed out.

Do not:

```text
increase to 3600

reduce it

split batch 1

change context size

lower reasoning effort

switch model.
```

The task is a clean network/runtime retry,
not a transport-policy experiment.

If the same transport problem repeats:

record it.

Do not redesign runtime inside M12BQ.

---

# 10. Full-suite issue discovered after model attempt

M12BP:

```text
focused =
79 / 79 PASS

Ruff =
PASS

git diff --check =
PASS

full suite =
4003 PASS
5 FAIL
1 warning.
```

The report identifies the 5 failures as:

```text
historical exact-scope freeze assertions

families:
M12E
M12U
M12F
M12B
M12W.
```

They detected the newly authorized M12BP service/test files
as outside their old frozen approved-change scope.

The report explicitly says:

```text
these were not functional calculation failures.
```

But M12BQ must NOT rely on that prose alone.

Reproduce all five failures offline first.

---

# 11. Phase 1 — exact-scope fixture reproduction

Before modifying any fixture:

run the five exact failing tests individually.

For each record:

```text
test node id

assertion

expected frozen path set

observed path set

new path delta

whether any semantic value/output assertion also failed.
```

Required finding for bounded maintenance:

```text
all 5 failures are ONLY approved-path/scope-set mismatches.
```

If even one failure contains:

```text
semantic output regression

financial calculation regression

BusinessDelta regression

model schema regression

canonical semantic regression

Persistence V2 regression

runtime production-path regression
```

then:

```text
STOP
FULL_SUITE_FAILURE_NOT_SCOPE_ONLY.
```

No model calls.

---

# 12. Identify the exact M12BP authorized change set

Derive the actual authorized M12BP code/test changes from:

```text
M12BP branch diff

work instruction

implementation reports

commits.
```

At minimum M12BP reports identify:

```text
app/services/company_profile_service.py

app/services/coldstart_fundamental_enrichment_service.py

scripts/kr_financial_coldstart_repair_m12bp.py

tests/test_kr_financial_coldstart_repair_m12bp.py.
```

The M12BP handoff states that six approved service/test files were detected by
the historical exact-scope fixtures.

Do NOT guess the remaining paths.

Determine the exact six-file approved scope from git/repository evidence.

Freeze:

```text
m12bp_authorized_change_path_manifest.
```

---

# 13. Phase 1 repair rule

The five historical freeze fixtures may be updated ONLY to recognize
the already-authorized M12BP change set.

Allowed changes:

```text
approved path lists

approved scope manifests

expected exact file-set hashes/counts
that are mechanically derived from the approved M12BP path set.
```

Forbidden:

```text
semantic expected values

model expected outputs

financial calculations

provider readiness expectations

BusinessDelta expectations

warning/persistence expectations

schema expectations

allowing wildcard future paths

disabling exact-scope checks.
```

Preserve the historical fixture's purpose:

```text
unexpected files still fail.
```

Do not convert exact scope into:

```text
any app/services/** is allowed.
```

---

# 14. Historical fixture negative controls

For each updated scope fixture,
add or preserve one negative control:

```text
an unrelated fake changed path
must still fail the scope assertion.
```

Examples may be task-local fake paths.

Do not touch a real unrelated file to test this.

Required:

```text
unexpected-path rejection = PASS.
```

---

# 15. Phase 1 full-suite gate

After exact-scope reconciliation:

run:

```text
the five repaired historical tests

M12BP focused 79 tests

M12BN Persistence V2 focused/regression tests

semantic convergence critical regressions

full pytest

Ruff

git diff --check.
```

Required before model call:

```text
ALL PASS.
```

Do NOT proceed if full pytest remains red.

Record exact pass count.

Do not hard-code a target count if test collection legitimately differs,
but no failing tests are permitted.

---

# 16. Phase 1 code-scope freeze

Before network/model call:

freeze the post-maintenance code state.

Required:

```text
no production semantic code changed after M12BP

only exact-scope fixture maintenance + proof harness/report code
allowed in M12BQ.
```

Recompute:

```text
model prompt semantic hashes

model schema hashes

canonical semantic service hashes

KR financial production-service hashes

Persistence V2 contract hashes

decision-policy hashes.
```

Expected:

```text
all model/semantic/persistence behavior hashes unchanged
from M12BP frozen model-call gate.
```

Historical test fixture files may change.

If KR financial production service hashes change:

```text
STOP
NETWORK_RETRY_CODE_IDENTITY_CHANGED.
```

No model call.

---

# 17. Frozen packet identity must survive fixture repair

After Phase 1 fixture changes,
recompute the twelve frozen packet/context identities.

Required:

```text
all packet hashes match section 5

source lock matches

prompt/schema lock matches

frozen subject list matches

call plan matches.
```

Exact-scope fixture maintenance must not mutate inputs.

---

# 18. No provider refetch

M12BQ is a transport retry of a frozen current-data proof.

Do NOT rebuild packets from current provider data.

Do NOT refresh:

```text
prices

OpenDART

SEC/companyfacts

events

financial statements

profiles.
```

Use the exact M12BP frozen packet files.

If a required frozen packet artifact is missing:

```text
STOP
FROZEN_FRESH_PACKET_UNAVAILABLE.
```

Do not refetch and call it the same proof.

---

# 19. Network readiness

After all deterministic gates are green:

run one normal network-readiness check.

Require:

```text
safe_to_spawn = true

active conflicting model jobs = 0

resolved address count > 0
```

according to current runtime contract.

No dummy model content call.

If preflight fails:

```text
STOP
NETWORK_ENVIRONMENT_BLOCKER_NO_SEMANTIC_CHANGE.
```

No model call.

---

# 20. New proof generation

If network readiness passes:

create a NEW generation.

Suggested lineage fields:

```text
parent_frozen_input_generation =
20260915-m12bp-fresh-20260914T101849Z-9329c13191bd

execution_reason =
CLEAN_NETWORK_RETRY_AFTER_ZERO_OUTPUT_TIMEOUT

input_identity_reused =
true

old_model_output_reused =
false.
```

Do not reuse the old generation directory as writable state.

---

# 21. Six-call clean retry

Run all six planned calls from the beginning.

For each:

```text
attempt_limit = 1

wrapper_retry = 0.
```

No per-call retry after timeout.

No fallback.

No judge.

No selective rerun.

---

# 22. Transport instrumentation

For every model call record:

```text
call id

generation id

stage

batch

tickers

spawn timestamp

process/runtime identity

stdout bytes

stderr diagnostic bytes if safe

result file identity

transport receipt

websocket disconnect observations

watchdog result

elapsed seconds

usable output parsed?
```

Do not treat:

```text
process completion receipt
```

as:

```text
usable model output.
```

Those are separate fields.

---

# 23. Candidate-specific semantic failures

If a call returns valid schema outputs
but one candidate fails canonical semantics:

continue the remaining preplanned calls where safe.

Do not patch.

Do not rerun.

Do not replace.

Final proof fails unless:

```text
12 / 12 accepted.
```

Preserve complete cohort evidence.

---

# 24. Transport failure after retry begins

If any model call has a transport/runtime failure:

```text
no selective retry in M12BQ.
```

Continue later calls only if the active harness architecture can do so
without requiring the missing upstream result.

For example:

```text
PRICE_TIMING for a batch requires its valid DIRECTIONAL_CORE
```

so do NOT run a dependent stage on missing upstream core.

Independent later core batches may be completed if the frozen orchestration contract permits it.

Document dependency-based NOT_RUN separately from failure.

Do not invent partial final candidates.

---

# 25. Canonical semantics

All usable candidate outputs go through the existing canonical single-source path.

Required:

```text
directional-core-semantic-audit-v1

proof-critical bypass = 0

legacy duplicate semantic participation = 0.
```

No M12BQ-specific semantic classifier.

---

# 26. BusinessDelta critical gate

The old historical fresh proof failed BusinessDelta.

M12BQ must explicitly report per subject:

```text
BusinessDeltaEvidenceView capability

candidate value

validation result.
```

Required for PASS:

```text
BusinessDelta hard failures = 0.
```

No raw-text fallback.

---

# 27. KR financial subject audit

For:

```text
138930
```

preserve the M12BP input-repair evidence.

Verify:

```text
financial-specialized framework

SECTOR_OPERATING_CURRENT

official account identities

no fabricated regulatory capital

no generic revenue relabel

no industrial revenue readiness requirement.
```

Do not modify the packet.

This is read-only verification of frozen input.

---

# 28. Final composition

For every accepted subject require:

```text
schema valid

canonical hard semantics PASS

final composition PASS

core immutability PASS.
```

No final composition from missing core.

No Stage-2/price-timing core mutation.

---

# 29. Fresh Initial Analysis lifecycle

Preserve:

```text
CANONICAL_RECEIPT_NOT_APPLICABLE_UNTIL_EXPLICIT_MONITORING_REGISTRATION.
```

No fake thesis version.

No watchlist registration.

No persistence receipt merely to make proof look complete.

The fresh model proof is independent of monitoring enrollment.

---

# 30. Fresh proof PASS criteria

Require:

```text
frozen subjects = 12

subject replacement = 0

frozen packet mismatch = 0

full pre-model test suite = PASS

planned calls = 6

all required calls complete with usable output

schema-valid subjects = 12

canonical semantic PASS = 12

final composition PASS = 12

accepted = 12

BusinessDelta failures = 0

financial semantic failures = 0

working-capital failures = 0

financial-sector failures = 0

market-expectation failures = 0

security-basis failures = 0

Stage-2/price-stage contamination = 0

core mutation = 0

canonical bypass = 0

legacy duplicate participation = 0

wrapper retry = 0

fallback = 0

judge = 0

selective rerun = 0

post-hoc override = 0

production side effects = 0.
```

No direction distribution target.

---

# 31. Output distributions are diagnostic only

Report:

```text
BUY / HOLD / SELL distribution

BusinessDelta distribution

new-buyer stance distribution

holder stance distribution

confidence distribution.
```

No required mix.

Do not force diversity.

---

# 32. No transport tuning after seeing the result

Do NOT change during this task:

```text
timeout

batch size

reasoning effort

model

prompt size

call order

subject grouping.
```

If transport fails again,
that is the evidence.

Do not tune and rerun.

---

# 33. Production firewall

Required throughout:

```text
production_db_mutations = 0

assessment_production_writes = 0

monitoring_registrations = 0

monitoring_stops = 0

watchlist_mutations = 0

production_thesis_version_mutations = 0

warning_production_mutations = 0

notification_production_queue_writes = 0

production_sends = 0

V2 production writer = false

V2 production read preference = false

V2 production warning = false

V2 production outbox delivery = false

scheduler_mutation_count = 0

automatic_monitoring_resume = 0

remote push = 0

raw model artifact remote push = 0

main merge = 0

deployment = 0.
```

Schedules remain paused.

---

# 34. Top-level result taxonomy

Choose exactly one:

```text
FRESH_UNSEEN_CANONICAL_PROOF_PASS

FRESH_UNSEEN_CANONICAL_PROOF_FAIL

NETWORK_TRANSPORT_INCOMPLETE_SAME_FROZEN_COHORT

PREMODEL_TEST_GATE_FAILURE

FROZEN_COHORT_IDENTITY_FAILURE.
```

No vague partial readiness.

---

# 35. PASS consequence

If and only if:

```text
FRESH_UNSEEN_CANONICAL_PROOF_PASS
```

then:

```text
fresh_real_proof_readiness =
PROOF_COMPLETE

final_main_merge_readiness =
READY_FOR_EXPLICIT_USER_APPROVAL

production_readiness =
NOT_READY_PENDING_EXPLICIT_CUTOVER_APPROVAL

next_scope =
FINAL_MAIN_MERGE_AND_PRODUCTION_CUTOVER_APPROVAL_GATE.
```

Do NOT merge.

Do NOT deploy.

Do NOT enable V2.

Do NOT resume schedules.

---

# 36. Transport-incomplete consequence

If network/runtime fails again:

```text
fresh_real_proof_readiness =
PROOF_INCOMPLETE_TRANSPORT

final_main_merge_readiness =
NOT_READY

production_readiness =
NOT_READY.
```

Do not automatically create another retry loop.

Report:

```text
exact call

elapsed time

stdout bytes

disconnect observations

whether any independent batches completed

usable candidate count.
```

Next scope must be determined from evidence.

---

# 37. Semantic failure consequence

If usable model output exists and one or more candidates fail:

```text
top-level =
FRESH_UNSEEN_CANONICAL_PROOF_FAIL.
```

Complete the cohort where safe.

Then choose one bounded next category:

```text
BOUNDED_FRESH_MODEL_CONTRACT_COMPLIANCE_REPAIR

BOUNDED_CANONICAL_SEMANTIC_ENGINE_REPAIR

BOUNDED_FRESH_INPUT_PACKET_REPAIR.
```

Do not patch in M12BQ.

---

# 38. Required Phase-1 artifacts

Produce:

```text
01-repository-provenance

02-latest-result-integrity

03-m12bq-scope-freeze

04-m12bp-input-repair-freeze

05-m12bp-frozen-cohort-identity-freeze

06-historical-five-failure-reproduction

07-m12bp-authorized-change-path-manifest

08-m12e-exact-scope-reconciliation

09-m12u-exact-scope-reconciliation

10-m12f-exact-scope-reconciliation

11-m12b-exact-scope-reconciliation

12-m12w-exact-scope-reconciliation

13-unexpected-path-negative-control

14-post-scope-repair-focused-tests

15-post-scope-repair-full-tests

16-post-scope-repair-ruff-diff

17-model-semantic-production-hash-freeze

18-frozen-packet-identity-reaudit.
```

No model call before 18 PASS.

---

# 39. Required retry artifacts

If deterministic gates pass:

```text
19-network-readiness

20-new-retry-generation-manifest

21-frozen-six-call-plan-identity

22-model-call-01-transport

23-model-call-02-transport

24-model-call-03-transport

25-model-call-04-transport

26-model-call-05-transport

27-model-call-06-transport

28-raw-model-artifact-manifest

29-fresh-schema-audit

30-fresh-canonical-service-provenance

31-fresh-business-delta-audit

32-fresh-financial-semantics-audit

33-fresh-working-capital-audit

34-fresh-financial-sector-audit

35-138930-frozen-financial-input-audit

36-fresh-market-expectation-audit

37-fresh-direction-timing-audit

38-fresh-security-basis-audit

39-fresh-stage2-price-stage-contamination-audit

40-fresh-core-immutability-audit

41-fresh-final-composition-audit

42-fresh-initial-analysis-lifecycle-audit

43-fresh-per-subject-result-matrix

44-fresh-output-distribution-diagnostic.
```

---

# 40. Required final decisions

Produce:

```text
45-fresh-unseen-canonical-proof-decision

46-main-merge-readiness-decision

47-production-readiness-decision

48-next-scope-decision

49-production-firewall-proof

50-master-workflow-update

51-program-completion.
```

---

# 41. Program-completion fields

Include at least:

```text
base_integration_head_sha
integration_branch
final_local_head_sha

latest_result_zip_sha256
latest_result_integrity

m12bp_input_repair_status

m12bp_frozen_generation_id
m12bp_frozen_subject_count
m12bp_frozen_subjects

m12bp_packet_identity_status
m12bp_source_lock_status
m12bp_prompt_schema_lock_status

historical_scope_failure_count_before
historical_scope_failure_test_ids
historical_scope_failures_scope_only
m12bp_authorized_change_path_count
m12bp_authorized_change_paths
historical_scope_failure_count_after
unexpected_path_negative_control_status

focused_test_result
full_test_result
ruff_result
git_diff_check

model_prompt_semantic_change_count
model_schema_semantic_change_count
canonical_semantic_service_change_count
kr_financial_production_service_change_count_after_m12bp
persistence_v2_contract_change_count
decision_policy_change_count

retry_generation_id
retry_parent_frozen_generation_id

network_readiness_status

planned_model_call_count
model_calls_started
model_calls_with_usable_output
model_calls_transport_failed
model_calls_dependency_not_run

model_call_1_elapsed_seconds
model_call_1_stdout_bytes
model_call_1_websocket_disconnect_count

wrapper_retry_count
fallback_model_call_count
judge_call_count
selective_rerun_count
posthoc_decision_override_count

fresh_subject_count
fresh_schema_valid_count
fresh_canonical_semantic_pass_count
fresh_canonical_semantic_fail_count
fresh_final_composition_pass_count
fresh_accepted_count

fresh_business_delta_hard_failure_count
fresh_financial_semantic_hard_failure_count
fresh_working_capital_hard_failure_count
fresh_financial_sector_hard_failure_count
fresh_market_expectation_hard_failure_count
fresh_security_basis_hard_failure_count
fresh_stage2_contamination_count
fresh_core_mutation_count

proof_critical_canonical_bypass_count
legacy_duplicate_semantic_participation_count

post_freeze_subject_replacement_count

fresh_persistence_applicability

production_db_mutations
assessment_production_writes
monitoring_registrations
monitoring_stops
watchlist_mutations
production_thesis_version_mutations
warning_production_mutations
notification_production_queue_writes
production_sends

v2_production_writer_enabled
v2_production_read_preference_enabled
v2_production_warning_enabled
v2_production_outbox_delivery_enabled

scheduler_mutation_count
automatic_monitoring_resume
remote_push_count
raw_model_artifact_remote_push_count
main_branch_mutations
main_merges
deployments

top_level_result

fresh_real_proof_readiness
final_main_merge_readiness
production_readiness

next_scope

artifact_count
artifact_hash_mismatch_count
artifact_size_mismatch_count
artifact_secret_scan_failure_count.
```

Anything genuinely unavailable:

```text
NOT_MEASURED
```

with reason.

Do not report zero for unknown.

---

# 42. Expected pre-model result

Before network/model:

```text
M12BP input repair = frozen PASS

historical scope failures before = 5

all 5 confirmed scope-only

historical scope failures after = 0

unexpected-path negative control = PASS

full pytest = PASS

Ruff = PASS

git diff = PASS

KR-financial production behavior unchanged from M12BP

frozen 12 packet hashes = exact match

source lock = exact match

prompt/schema lock = exact match.
```

Only then may the model run.

---

# 43. Expected clean final result

If the retry completes successfully:

```text
top_level_result =
FRESH_UNSEEN_CANONICAL_PROOF_PASS

fresh_subject_count = 12

fresh_schema_valid_count = 12

fresh_canonical_semantic_pass_count = 12

fresh_canonical_semantic_fail_count = 0

fresh_final_composition_pass_count = 12

fresh_accepted_count = 12

wrapper_retry_count = 0

fallback_model_call_count = 0

judge_call_count = 0

selective_rerun_count = 0

posthoc_decision_override_count = 0

proof_critical_canonical_bypass_count = 0

legacy_duplicate_semantic_participation_count = 0

production side effects = 0

fresh_real_proof_readiness =
PROOF_COMPLETE

final_main_merge_readiness =
READY_FOR_EXPLICIT_USER_APPROVAL

production_readiness =
NOT_READY_PENDING_EXPLICIT_CUTOVER_APPROVAL

next_scope =
FINAL_MAIN_MERGE_AND_PRODUCTION_CUTOVER_APPROVAL_GATE.
```

Do NOT force this result.

---

# 44. Failure handling

## A. One of the five old tests is not scope-only

```text
STOP
FULL_SUITE_FAILURE_NOT_SCOPE_ONLY.
```

No model calls.

## B. Scope fixture repair broadens allowed future changes

```text
STOP
HISTORICAL_EXACT_SCOPE_GUARD_WEAKENED.
```

No model calls.

## C. Frozen packet/hash identity changed

```text
STOP
FROZEN_FRESH_COHORT_IDENTITY_MISMATCH.
```

No model call.

## D. Full suite remains red

```text
STOP
PREMODEL_TEST_GATE_FAILURE.
```

No model call.

## E. Network readiness fails before first call

```text
NETWORK_ENVIRONMENT_BLOCKER_NO_SEMANTIC_CHANGE.
```

No model call.

## F. First or later model call times out again

No selective retry.

Do not tune timeout.

Do not split batches.

Close as:

```text
NETWORK_TRANSPORT_INCOMPLETE_SAME_FROZEN_COHORT.
```

## G. Model returns valid output but semantic failure occurs

Complete cohort where safe.

No patch/rerun.

Close as:

```text
FRESH_UNSEEN_CANONICAL_PROOF_FAIL.
```

## H. 12/12 pass

Stop at explicit user approval gate.

No merge/deploy.

---

# 45. Local / production boundary

Model network calls are authorized by this work instruction ONLY after
the full deterministic pre-model gate passes.

No provider refetch is authorized.

No production mutation is authorized.

Allowed:

```text
frozen packet reads

local tests

model transmission of frozen contexts

local raw result artifacts

canonical semantic audits.
```

Forbidden:

```text
provider packet refresh

production DB writes

monitoring registration

warning/outbox production writes

external notification sends

scheduler changes

remote raw artifact push

main merge

deployment.
```

---

# 46. Artifact integrity

Final report ZIP must have:

```text
missing = 0

extra = 0

hash mismatch = 0

size mismatch = 0

secret scan failure = 0.
```

Report final ZIP SHA-256.

---

# 47. Final task principle

M12BP achieved the substantive input repair.

The six KR financial candidates became safe/provider-ready,
and a valid 12-subject fresh holdout was frozen.

The proof did NOT produce a bad investment judgment.

It produced:

```text
zero model output
after a single 1,800-second transport timeout.
```

Separately,
the repository full suite exposed five old exact-scope guard fixtures
that had not yet learned about the authorized M12BP change set.

The clean next step is therefore:

```text
reconcile only those five historical path-scope guards

→ prove the entire local suite is green

→ prove the frozen cohort/packets/prompts are byte-identical

→ create a new execution generation

→ run all six frozen calls from call 1

→ never stitch the old timeout generation

→ never tune the timeout/batch/model after seeing the failure

→ if 12/12 canonical proof passes,
   stop at explicit main-merge / production-cutover approval.
```

Do NOT:

```text
reopen KR financial semantics

refetch packets

reselect subjects

change the frozen 12

change timeout

split batch 1

lower reasoning effort

switch models

reuse old partial runtime state

selectively retry a semantic failure

merge generations

merge main

deploy

enable V2

resume monitoring.
```
