# Track D — US Price Structure Natural Proof

Do not manually trigger.

Wait for the next natural US/foreign monitored-stock message cycle.

Verify exact live messages:
- correct session/security/currency basis
- Price Structure only where eligible
- near/major labels safe
- Fib only when safe
- stored rules separate
- no target/stop
- exactly once

Only after natural PASS:
`US_PRICE_STRUCTURE = LIVE_PASS`.
