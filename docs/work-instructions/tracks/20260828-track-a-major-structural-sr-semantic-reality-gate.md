# Track A — Major Structural S/R Semantic Reality Gate

Trace and repair the shared selector.

Key negative control:

```text
GOOGL $424.82~$426.96
BOLLINGER_MONTHLY only
reaction_count = 0
→ must not be MAJOR_RESISTANCE
```

Also verify why dynamic indicator observation date was represented as `last_meaningful_interaction`.

Implement a generic `price-anchor` eligibility gate before major-zone ranking.

Do not rewrite near-SR policy.
