# Track B — Current Quote vs Regular-Close Price Label Clarity

When quote and Price Structure basis differ, render:

```text
• 현재가: ...
• 가격 구조 기준 종가(정규장): ...
```

Include session/quote-state labels only when backend evidence exists.

If identical, avoid redundant duplicate lines.

No valuation/security-basis behavior change.
