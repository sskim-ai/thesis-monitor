# Track B — AI Candidate → Validation → Accepted → V2 Selector
model state, candidate, validation errors, correction, accepted artifact,
claim-bound final artifact, completion receipt, V2 eligibility predicate를 추출한다.
explicit V2가 처음 불가능해진 지점을 확정한다.
