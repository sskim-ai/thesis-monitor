# Track E — Root Cause + Repair Handoff
operating SHA, TLS, claim, candidate, validator, accepted,
completion receipt, selector, renderer, delivery metadata, backup reuse 중
first material failure를 하나로 확정한다.
root cause와 secondary effects를 분리한다.
이번 작업에서 patch 금지.
다음 작업용 file/function/state-key만 기록한다.
