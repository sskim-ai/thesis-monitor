# Track A — Model Selection Provenance + GPT-5.6 Production Equivalence

Find exactly why the prior fresh shadow resolved to GPT-5.5.

Trace:
script/config/env/CLI/app-server/default/fallback.

Discover exact current production model identifier and reasoning effort.

New promotion-equivalent shadow must resolve to the exact production GPT-5.6 family.
No silent fallback to GPT-5.5.
If model provenance is unknown or mismatch remains, stop.
