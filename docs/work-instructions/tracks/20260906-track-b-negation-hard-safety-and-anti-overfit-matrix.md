# Track B — Negation Hard-Safety + Anti-Overfit Matrix

Before model calls create a ticker-free synthetic matrix:

>=12 NEGATED
>=12 ACTIONABLE
>=6 DESCRIPTIVE/NONE
>=6 contrast/double-negation adversarial examples

Re-run historical:
- 즉시 매수가 아닌
- 즉시 매수 신호가 아니며
- 즉시 매수
- 반드시 매도

The historical WULF sentence may be a regression fixture,
but never the primary implementation target.

Known hard-safety regression must remain 0.
