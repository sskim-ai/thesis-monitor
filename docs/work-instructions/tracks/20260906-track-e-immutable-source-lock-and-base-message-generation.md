# Track E — Immutable Source Lock + Deterministic Base Context

For final unseen candidates:
- assemble fresh immutable packet
- validate identity/security basis/accounting/numerics
- generate deterministic base context
- retain Unknown/stale/partial states
- use predeclared ranked replacements only for objective pre-model failures

Minimum eligible = 12.

Then freeze ordered cohort + packet hashes + base-context hashes + market/session dates.

No source refresh or packet mutation through FIRST/A/B/C.
